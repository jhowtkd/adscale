# Waitlist no Site de Apresentação — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Capturar leads interessados via modal no `site-adscale`, persistir no Postgres, sincronizar com Resend Audiences e enviar email de confirmação — substituindo "Criar conta" como CTA principal.

**Architecture:** Modal no marketing site chama `POST /api/waitlist` no app com CORS. App valida, persiste em `waitlist_signups`, sincroniza contato no Resend (fetch REST, mesmo padrão de `email.ts`) e dispara email transacional via `email-template.ts`. Link discreto "Já tenho acesso" mantém `/login`.

**Tech Stack:** Next.js App Router, Drizzle ORM, Zod, Vitest, Resend REST API, Vite/React (site-adscale), Tailwind.

**Spec:** `docs/superpowers/specs/2026-06-08-waitlist-design.md`

---

## File Map

### ADScale_2 — Created

| File | Responsibility |
|------|----------------|
| `app/drizzle/0034_waitlist_signups.sql` | Migration da tabela |
| `app/src/server/waitlist/schema.ts` | Zod schema + enum de setores |
| `app/src/server/waitlist/normalize.ts` | Normalização email/WhatsApp |
| `app/src/server/waitlist/normalize.test.ts` | Testes de normalização |
| `app/src/server/repositories/waitlist.ts` | CRUD mínimo (create + findByEmail) |
| `app/src/server/repositories/waitlist.test.ts` | Testes do repository |
| `app/src/server/services/resend-contacts.ts` | Sync contato → Resend Audiences |
| `app/src/server/services/resend-contacts.test.ts` | Testes com mock fetch |
| `app/src/lib/cors-marketing.ts` | Helpers CORS para origens do marketing |
| `app/src/app/api/waitlist/route.ts` | POST + OPTIONS públicos |
| `app/src/app/api/waitlist/route.test.ts` | Testes da rota |
| `app/tests/integration/waitlist.test.ts` | Integration test (opcional se route.test cobre) |

### ADScale_2 — Modified

| File | Responsibility |
|------|----------------|
| `app/src/server/db/schema.ts` | Tabela `waitlistSignups` + types |
| `app/src/server/services/email.ts` | `sendWaitlistConfirmationEmail` |
| `app/src/server/services/email.test.ts` | Teste do novo email |
| `app/messages/pt-BR.json` | Copy `transactionalEmails.waitlist` |
| `app/messages/en.json` | Copy EN |
| `app/.env.example` | Novas env vars |
| `docs/CONFIGURATION.md` | Documentar env vars |

### site-adscale — Created

| File | Responsibility |
|------|----------------|
| `src/components/WaitlistModal.tsx` | Modal com formulário |
| `src/context/WaitlistContext.tsx` | Provider `openWaitlist()` global |
| `src/lib/waitlist-sectors.ts` | Labels dos setores |

### site-adscale — Modified

| File | Responsibility |
|------|----------------|
| `src/config/site.ts` | `waitlistApiUrl`, `loginUrl` |
| `src/App.tsx` | Wrap com `WaitlistProvider` |
| `src/components/Hero.tsx` | CTA waitlist |
| `src/components/Navbar.tsx` | CTA waitlist + "Já tenho acesso" |
| `src/components/FinalCTA.tsx` | CTA waitlist |
| `src/components/Pricing.tsx` | Copy + CTAs |
| `src/components/Footer.tsx` | Link "Já tenho acesso" |
| `.env.example` | Comentário sobre `VITE_APP_URL` |
| `README.md` | Nota sobre waitlist local |

---

## Task 1: Migration + Drizzle schema

**Files:**
- Create: `app/drizzle/0034_waitlist_signups.sql`
- Modify: `app/src/server/db/schema.ts`

- [ ] **Step 1: Criar migration SQL**

```sql
CREATE TABLE "adscale_app"."waitlist_signups" (
  "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
  "name" text NOT NULL,
  "email" text NOT NULL,
  "sector" text NOT NULL,
  "sector_other" text,
  "whatsapp" text NOT NULL,
  "consent_at" timestamp NOT NULL,
  "consent_version" text NOT NULL,
  "locale" text NOT NULL DEFAULT 'pt-BR',
  "source" text NOT NULL DEFAULT 'marketing-site',
  "resend_contact_id" text,
  "created_at" timestamp DEFAULT now() NOT NULL
);
--> statement-breakpoint
CREATE UNIQUE INDEX "waitlist_signups_email_uidx" ON "adscale_app"."waitlist_signups" USING btree ("email");
```

- [ ] **Step 2: Adicionar tabela em `schema.ts`** (antes dos exports de types no final do arquivo)

```typescript
export const waitlistSignups = adscaleSchema.table(
  "waitlist_signups",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    name: text("name").notNull(),
    email: text("email").notNull(),
    sector: text("sector").notNull(),
    sectorOther: text("sector_other"),
    whatsapp: text("whatsapp").notNull(),
    consentAt: timestamp("consent_at", { mode: "date" }).notNull(),
    consentVersion: text("consent_version").notNull(),
    locale: text("locale").notNull().default("pt-BR"),
    source: text("source").notNull().default("marketing-site"),
    resendContactId: text("resend_contact_id"),
    createdAt: timestamp("created_at", { mode: "date" }).notNull().defaultNow(),
  },
  (table) => [uniqueIndex("waitlist_signups_email_uidx").on(table.email)]
);

export type WaitlistSignup = typeof waitlistSignups.$inferSelect;
export type NewWaitlistSignup = typeof waitlistSignups.$inferInsert;
```

- [ ] **Step 3: Rodar migration local**

```bash
cd app && npm run db:migrate
```

Expected: migration `0034_waitlist_signups` aplicada sem erro.

- [ ] **Step 4: Commit**

```bash
git add app/drizzle/0034_waitlist_signups.sql app/src/server/db/schema.ts
git commit -m "feat(waitlist): add waitlist_signups table"
```

---

## Task 2: Validação e normalização

**Files:**
- Create: `app/src/server/waitlist/schema.ts`
- Create: `app/src/server/waitlist/normalize.ts`
- Create: `app/src/server/waitlist/normalize.test.ts`

- [ ] **Step 1: Escrever testes falhando**

`app/src/server/waitlist/normalize.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { normalizeEmail, normalizeWhatsapp } from "./normalize";

describe("normalizeEmail", () => {
  it("lowercases and trims", () => {
    expect(normalizeEmail("  Maria@Empresa.COM ")).toBe("maria@empresa.com");
  });
});

describe("normalizeWhatsapp", () => {
  it("keeps digits with leading country code", () => {
    expect(normalizeWhatsapp("+55 (11) 99999-9999")).toBe("5511999999999");
  });

  it("adds 55 when 11 digits without country code", () => {
    expect(normalizeWhatsapp("11999999999")).toBe("5511999999999");
  });
});
```

- [ ] **Step 2: Rodar testes — devem falhar**

```bash
cd app && npm test -- src/server/waitlist/normalize.test.ts
```

Expected: FAIL — module not found.

- [ ] **Step 3: Implementar schema + normalize**

`app/src/server/waitlist/schema.ts`:

```typescript
import { z } from "zod";

export const WAITLIST_SECTORS = [
  "agency",
  "ecommerce",
  "saas",
  "infoproduct",
  "retail",
  "other",
] as const;

export type WaitlistSector = (typeof WAITLIST_SECTORS)[number];

export const waitlistSignupSchema = z
  .object({
    name: z.string().trim().min(2).max(100),
    email: z.string().trim().email().max(254),
    sector: z.enum(WAITLIST_SECTORS),
    sectorOther: z.string().trim().max(100).optional().nullable(),
    whatsapp: z.string().trim().min(8).max(30),
    consent: z.literal(true),
    locale: z.enum(["pt-BR", "en"]).default("pt-BR"),
    website: z.string().optional(), // honeypot
  })
  .superRefine((data, ctx) => {
    if (data.sector === "other" && !data.sectorOther?.trim()) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        message: "sectorOtherRequired",
        path: ["sectorOther"],
      });
    }
  });

export type WaitlistSignupInput = z.infer<typeof waitlistSignupSchema>;

export const WAITLIST_CONSENT_VERSION = "2026-06-08";
```

`app/src/server/waitlist/normalize.ts`:

```typescript
export function normalizeEmail(email: string): string {
  return email.trim().toLowerCase();
}

export function normalizeWhatsapp(raw: string): string {
  const digits = raw.replace(/\D/g, "");
  if (digits.length === 11) return `55${digits}`;
  return digits;
}

export function isValidWhatsappLength(digits: string): boolean {
  return digits.length >= 10 && digits.length <= 15;
}
```

- [ ] **Step 4: Rodar testes — devem passar**

```bash
cd app && npm test -- src/server/waitlist/normalize.test.ts
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add app/src/server/waitlist/
git commit -m "feat(waitlist): add validation schema and normalizers"
```

---

## Task 3: Repository

**Files:**
- Create: `app/src/server/repositories/waitlist.ts`
- Create: `app/src/server/repositories/waitlist.test.ts`

- [ ] **Step 1: Teste falhando**

```typescript
import { describe, it, expect, vi } from "vitest";

vi.mock("@/server/db", () => ({
  db: { insert: vi.fn(), select: vi.fn() },
}));

import { db } from "@/server/db";
import { createWaitlistSignup, findWaitlistSignupByEmail } from "./waitlist";

describe("waitlist repository", () => {
  it("creates signup with normalized email", async () => {
    const row = { id: "w1", email: "a@b.com" };
    const returning = vi.fn().mockResolvedValue([row]);
    const values = vi.fn().mockReturnValue({ returning });
    (db.insert as ReturnType<typeof vi.fn>).mockReturnValue({ values });

    const result = await createWaitlistSignup({
      name: "Ana",
      email: "a@b.com",
      sector: "saas",
      sectorOther: null,
      whatsapp: "5511999999999",
      consentAt: new Date(),
      consentVersion: "2026-06-08",
      locale: "pt-BR",
    });

    expect(result).toEqual(row);
    expect(values).toHaveBeenCalledWith(expect.objectContaining({ email: "a@b.com" }));
  });
});
```

- [ ] **Step 2: Implementar repository**

`app/src/server/repositories/waitlist.ts`:

```typescript
import { eq } from "drizzle-orm";
import { db } from "@/server/db";
import { waitlistSignups, type WaitlistSignup } from "@/server/db/schema";

export async function findWaitlistSignupByEmail(email: string): Promise<WaitlistSignup | null> {
  const [row] = await db
    .select()
    .from(waitlistSignups)
    .where(eq(waitlistSignups.email, email))
    .limit(1);
  return row ?? null;
}

export async function createWaitlistSignup(input: {
  name: string;
  email: string;
  sector: string;
  sectorOther: string | null;
  whatsapp: string;
  consentAt: Date;
  consentVersion: string;
  locale: string;
  resendContactId?: string | null;
}): Promise<WaitlistSignup> {
  const [row] = await db
    .insert(waitlistSignups)
    .values({
      name: input.name,
      email: input.email,
      sector: input.sector,
      sectorOther: input.sectorOther,
      whatsapp: input.whatsapp,
      consentAt: input.consentAt,
      consentVersion: input.consentVersion,
      locale: input.locale,
      resendContactId: input.resendContactId ?? null,
    })
    .returning();
  return row;
}

export async function updateWaitlistResendContactId(id: string, resendContactId: string) {
  await db
    .update(waitlistSignups)
    .set({ resendContactId })
    .where(eq(waitlistSignups.id, id));
}
```

- [ ] **Step 3: Rodar testes e commit**

```bash
cd app && npm test -- src/server/repositories/waitlist.test.ts
git add app/src/server/repositories/waitlist.ts app/src/server/repositories/waitlist.test.ts
git commit -m "feat(waitlist): add waitlist repository"
```

---

## Task 4: Resend Audiences sync

**Files:**
- Create: `app/src/server/services/resend-contacts.ts`
- Create: `app/src/server/services/resend-contacts.test.ts`
- Modify: `app/.env.example`

- [ ] **Step 1: Adicionar env vars em `.env.example`**

```env
# Waitlist (marketing site → Resend Audiences)
RESEND_WAITLIST_SEGMENT_ID=""
MARKETING_ALLOWED_ORIGINS="http://localhost:5173"
```

- [ ] **Step 2: Teste falhando**

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/server/validation/env", () => ({
  env: {
    RESEND_API_KEY: "re_live_test",
    RESEND_WAITLIST_SEGMENT_ID: "seg_test",
  },
}));

import { syncWaitlistContact } from "./resend-contacts";

describe("syncWaitlistContact", () => {
  beforeEach(() => {
    vi.stubGlobal("fetch", vi.fn());
  });
  afterEach(() => {
    vi.unstubAllGlobals();
  });

  it("creates contact in segment", async () => {
    (fetch as ReturnType<typeof vi.fn>).mockResolvedValue({
      ok: true,
      json: async () => ({ id: "contact_1" }),
    });

    const id = await syncWaitlistContact({
      email: "a@b.com",
      name: "Ana Silva",
      sector: "saas",
      whatsapp: "5511999999999",
    });

    expect(id).toBe("contact_1");
    expect(fetch).toHaveBeenCalledWith(
      "https://api.resend.com/contacts",
      expect.objectContaining({ method: "POST" })
    );
  });
});
```

- [ ] **Step 3: Implementar serviço**

`app/src/server/services/resend-contacts.ts`:

```typescript
import { env } from "@/server/validation/env";
import { logger } from "@/lib/logger";

type SyncInput = {
  email: string;
  name: string;
  sector: string;
  whatsapp: string;
};

function splitName(name: string) {
  const parts = name.trim().split(/\s+/);
  return { firstName: parts[0], lastName: parts.slice(1).join(" ") || undefined };
}

async function resendFetch(path: string, init: RequestInit) {
  const response = await fetch(`https://api.resend.com${path}`, {
    ...init,
    headers: {
      Authorization: `Bearer ${env.RESEND_API_KEY}`,
      "Content-Type": "application/json",
      ...init.headers,
    },
    signal: AbortSignal.timeout(10_000),
  });
  const body = await response.json().catch(() => ({}));
  return { response, body };
}

export async function syncWaitlistContact(input: SyncInput): Promise<string | null> {
  const segmentId = process.env.RESEND_WAITLIST_SEGMENT_ID?.trim();
  if (!env.RESEND_API_KEY || env.RESEND_API_KEY.startsWith("re_test") || !segmentId) {
    logger.warn("[resend-contacts] Skipping sync — missing API key or segment id");
    return null;
  }

  const { firstName, lastName } = splitName(input.name);
  const payload = {
    email: input.email,
    first_name: firstName,
    last_name: lastName,
    unsubscribed: false,
    properties: {
      sector: input.sector,
      whatsapp: input.whatsapp,
      source: "marketing-site",
    },
    segments: [{ id: segmentId }],
  };

  const { response, body } = await resendFetch("/contacts", {
    method: "POST",
    body: JSON.stringify(payload),
  });

  if (response.ok) {
    return (body as { id?: string }).id ?? null;
  }

  // Contact may already exist — try update
  if (response.status === 409 || response.status === 422) {
    const { response: patchRes, body: patchBody } = await resendFetch(
      `/contacts/${encodeURIComponent(input.email)}`,
      {
        method: "PATCH",
        body: JSON.stringify({
          first_name: firstName,
          last_name: lastName,
          properties: payload.properties,
        }),
      }
    );
    if (patchRes.ok) {
      return (patchBody as { id?: string }).id ?? null;
    }
  }

  logger.error("[resend-contacts] Failed to sync contact", {
    status: response.status,
    email: input.email,
  });
  return null;
}
```

- [ ] **Step 4: Rodar testes e commit**

```bash
cd app && npm test -- src/server/services/resend-contacts.test.ts
git add app/src/server/services/resend-contacts.ts app/src/server/services/resend-contacts.test.ts app/.env.example
git commit -m "feat(waitlist): sync contacts to Resend Audiences"
```

---

## Task 5: Email de confirmação

**Files:**
- Modify: `app/src/server/services/email.ts`
- Modify: `app/src/server/services/email.test.ts`
- Modify: `app/messages/pt-BR.json`
- Modify: `app/messages/en.json`

- [ ] **Step 1: Adicionar copy i18n**

Em `transactionalEmails` em ambos JSON:

```json
"waitlist": {
  "subject": "Você está na lista de espera do ADScale",
  "preview": "Obrigado pelo interesse — avisaremos quando abrirmos.",
  "title": "Você entrou na lista de espera",
  "body": "Recebemos seu cadastro. Quando o ADScale abrir para novos usuários, você será um dos primeiros a saber.",
  "text": "Você está na lista de espera do ADScale. Avisaremos por email quando abrirmos o acesso."
}
```

(EN equivalente em `en.json`.)

- [ ] **Step 2: Implementar `sendWaitlistConfirmationEmail` em `email.ts`**

```typescript
export async function sendWaitlistConfirmationEmail(input: {
  to: string;
  locale?: string | null;
}) {
  const { t } = await getTransactionalEmailTranslations(input.locale);

  await sendEmail({
    to: input.to,
    subject: t("waitlist.subject"),
    text: t("waitlist.text"),
    html: renderTransactionalEmail({
      preview: t("waitlist.preview"),
      title: t("waitlist.title"),
      bodyHtml: escapeHtml(t("waitlist.body")),
      footerFallback: t("footerFallback"),
      footerIgnore: t("footerIgnore"),
      footerSignature: t("footerSignature"),
    }),
  });
}
```

- [ ] **Step 3: Adicionar teste em `email.test.ts`** (padrão dos testes existentes de verification)

- [ ] **Step 4: Rodar testes e commit**

```bash
cd app && npm test -- src/server/services/email.test.ts
git add app/src/server/services/email.ts app/src/server/services/email.test.ts app/messages/pt-BR.json app/messages/en.json
git commit -m "feat(waitlist): add confirmation email"
```

---

## Task 6: CORS helper + API route

**Files:**
- Create: `app/src/lib/cors-marketing.ts`
- Create: `app/src/app/api/waitlist/route.ts`
- Create: `app/src/app/api/waitlist/route.test.ts`

- [ ] **Step 1: CORS helper**

`app/src/lib/cors-marketing.ts`:

```typescript
import { NextResponse } from "next/server";

function getAllowedOrigins(): string[] {
  const raw = process.env.MARKETING_ALLOWED_ORIGINS ?? "";
  return raw
    .split(",")
    .map((o) => o.trim())
    .filter(Boolean);
}

export function corsMarketingHeaders(request: Request): HeadersInit {
  const origin = request.headers.get("origin");
  const allowed = getAllowedOrigins();
  if (origin && allowed.includes(origin)) {
    return {
      "Access-Control-Allow-Origin": origin,
      "Access-Control-Allow-Methods": "POST, OPTIONS",
      "Access-Control-Allow-Headers": "Content-Type",
      Vary: "Origin",
    };
  }
  return {};
}

export function jsonWithCors(request: Request, body: unknown, status: number) {
  return NextResponse.json(body, { status, headers: corsMarketingHeaders(request) });
}
```

- [ ] **Step 2: Implementar rota**

`app/src/app/api/waitlist/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { getTranslations } from "next-intl/server";
import { rateLimit } from "@/lib/rate-limit";
import { corsMarketingHeaders, jsonWithCors } from "@/lib/cors-marketing";
import { waitlistSignupSchema, WAITLIST_CONSENT_VERSION } from "@/server/waitlist/schema";
import { normalizeEmail, normalizeWhatsapp, isValidWhatsappLength } from "@/server/waitlist/normalize";
import { createWaitlistSignup, findWaitlistSignupByEmail, updateWaitlistResendContactId } from "@/server/repositories/waitlist";
import { syncWaitlistContact } from "@/server/services/resend-contacts";
import { sendWaitlistConfirmationEmail } from "@/server/services/email";
import { logger } from "@/lib/logger";

export async function OPTIONS(request: Request) {
  return new NextResponse(null, { status: 204, headers: corsMarketingHeaders(request) });
}

export async function POST(request: Request) {
  const cors = corsMarketingHeaders(request);

  try {
    const limited = await rateLimit(request, "auth");
    if (!limited.success) {
      return jsonWithCors(request, { error: "rateLimitExceeded" }, 429);
    }

    const body = await request.json();
    const parsed = waitlistSignupSchema.safeParse(body);
    if (!parsed.success) {
      return jsonWithCors(request, { error: "invalidInput", details: parsed.error.flatten() }, 400);
    }

    // Honeypot — silent success
    if (parsed.data.website?.trim()) {
      return jsonWithCors(request, { status: "created" }, 201);
    }

    const email = normalizeEmail(parsed.data.email);
    const whatsapp = normalizeWhatsapp(parsed.data.whatsapp);
    if (!isValidWhatsappLength(whatsapp)) {
      return jsonWithCors(request, { error: "invalidInput", code: "invalidWhatsapp" }, 400);
    }

    const existing = await findWaitlistSignupByEmail(email);
    if (existing) {
      const t = await getTranslations({ locale: parsed.data.locale, namespace: "waitlist" });
      return jsonWithCors(
        request,
        { status: "already_registered", message: t("duplicateEmail") },
        409
      );
    }

    const signup = await createWaitlistSignup({
      name: parsed.data.name.trim(),
      email,
      sector: parsed.data.sector,
      sectorOther: parsed.data.sector === "other" ? parsed.data.sectorOther?.trim() ?? null : null,
      whatsapp,
      consentAt: new Date(),
      consentVersion: WAITLIST_CONSENT_VERSION,
      locale: parsed.data.locale,
    });

    try {
      const contactId = await syncWaitlistContact({
        email,
        name: signup.name,
        sector: signup.sector,
        whatsapp,
      });
      if (contactId) await updateWaitlistResendContactId(signup.id, contactId);
    } catch (err) {
      logger.error("[waitlist] Resend sync failed", { id: signup.id, err });
    }

    try {
      await sendWaitlistConfirmationEmail({ to: email, locale: parsed.data.locale });
    } catch (err) {
      logger.error("[waitlist] Confirmation email failed", { id: signup.id, err });
    }

    return jsonWithCors(request, { status: "created" }, 201);
  } catch (err) {
    logger.error("[waitlist] POST failed", { err });
    return NextResponse.json({ error: "generic" }, { status: 500, headers: cors });
  }
}
```

- [ ] **Step 3: Adicionar namespace `waitlist` nas messages** (mensagem de duplicata)

`pt-BR.json`:
```json
"waitlist": {
  "duplicateEmail": "Este email já está na nossa lista de espera."
}
```

`en.json`:
```json
"waitlist": {
  "duplicateEmail": "This email is already on our waitlist."
}
```

- [ ] **Step 4: Testes da rota** — mock repository + email + resend; cobrir 201, 409, 400, honeypot.

- [ ] **Step 5: Rodar testes e commit**

```bash
cd app && npm test -- src/app/api/waitlist/route.test.ts
git add app/src/lib/cors-marketing.ts app/src/app/api/waitlist/ app/messages/
git commit -m "feat(waitlist): add public POST /api/waitlist with CORS"
```

---

## Task 7: Documentação de config

**Files:**
- Modify: `docs/CONFIGURATION.md`

- [ ] **Step 1: Documentar `RESEND_WAITLIST_SEGMENT_ID` e `MARKETING_ALLOWED_ORIGINS`**

- [ ] **Step 2: Commit**

```bash
git add docs/CONFIGURATION.md
git commit -m "docs: document waitlist env vars"
```

---

## Task 8: WaitlistContext + Modal (site-adscale)

**Repo:** `site-adscale` (commits separados)

**Files:**
- Create: `src/context/WaitlistContext.tsx`
- Create: `src/lib/waitlist-sectors.ts`
- Create: `src/components/WaitlistModal.tsx`
- Modify: `src/config/site.ts`
- Modify: `src/App.tsx`

- [ ] **Step 1: Atualizar `site.ts`**

```typescript
export const siteConfig = {
  appUrl,
  siteUrl: siteUrl || undefined,
  signupUrl: `${appUrl}/signup`,
  loginUrl: `${appUrl}/login`,
  waitlistApiUrl: `${appUrl}/api/waitlist`,
  privacyUrl: `${appUrl}/privacy`,
  termsUrl: `${appUrl}/terms`,
} as const;
```

- [ ] **Step 2: Criar `waitlist-sectors.ts`**

```typescript
export const WAITLIST_SECTORS = [
  { value: "agency", label: "Agência de marketing / performance" },
  { value: "ecommerce", label: "E-commerce" },
  { value: "saas", label: "SaaS / Tecnologia" },
  { value: "infoproduct", label: "Infoproduto / Educação" },
  { value: "retail", label: "Varejo" },
  { value: "other", label: "Outro" },
] as const;
```

- [ ] **Step 3: Criar `WaitlistContext.tsx`**

Provider com `openWaitlist()`, `closeWaitlist()`, renderiza `<WaitlistModal open={...} />`.

- [ ] **Step 4: Criar `WaitlistModal.tsx`**

Estados: idle | submitting | success | duplicate | error.

Campos: name, email, sector (select), sectorOther (condicional), whatsapp, consent checkbox, honeypot `website` (hidden, tabIndex -1).

Submit:
```typescript
const res = await fetch(siteConfig.waitlistApiUrl, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    name, email, sector,
    sectorOther: sector === "other" ? sectorOther : null,
    whatsapp,
    consent: true,
    locale: navigator.language.startsWith("pt") ? "pt-BR" : "en",
    website: honeypot,
  }),
});
```

Mensagens conforme spec. Focus trap + Esc para fechar. Estilo consistente com `btn-pill`, `font-mono`, cores do design system.

- [ ] **Step 5: Wrap `App.tsx`**

```tsx
import { WaitlistProvider } from './context/WaitlistContext';

export default function App() {
  return (
    <WaitlistProvider>
      {/* existing content */}
    </WaitlistProvider>
  );
}
```

- [ ] **Step 6: typecheck**

```bash
cd site-adscale && npm run typecheck
```

- [ ] **Step 7: Commit no repo site-adscale**

```bash
git add src/
git commit -m "feat: add waitlist modal and context"
```

---

## Task 9: Atualizar CTAs (site-adscale)

**Files:**
- Modify: `src/components/Hero.tsx`
- Modify: `src/components/Navbar.tsx`
- Modify: `src/components/FinalCTA.tsx`
- Modify: `src/components/Pricing.tsx`
- Modify: `src/components/Footer.tsx`

- [ ] **Step 1: Hero + FinalCTA**

Trocar `MagneticButton href={siteConfig.signupUrl}` por `onClick={() => openWaitlist()}` com label "Entrar na waitlist".

- [ ] **Step 2: Navbar**

- CTA pill: `openWaitlist()` — "Entrar na waitlist"
- Link discreto antes do CTA: `href={siteConfig.loginUrl}` — "Já tenho acesso"
- Mobile menu: mesmo padrão

- [ ] **Step 3: Pricing**

Atualizar copy beta para mencionar waitlist. CTA principal → `openWaitlist()`. Manter nota: quem tem código beta acessa pelo login.

- [ ] **Step 4: Footer**

Substituir link "Criar conta" por "Já tenho acesso" → `loginUrl`.

- [ ] **Step 5: typecheck + lint + commit**

```bash
cd site-adscale && npm run typecheck && npm run lint
git add src/components/
git commit -m "feat: replace signup CTAs with waitlist"
```

---

## Task 10: Verificação E2E local

- [ ] **Step 1: Configurar env local**

App `.env`:
```env
MARKETING_ALLOWED_ORIGINS=http://localhost:5173
RESEND_WAITLIST_SEGMENT_ID=<segment do dashboard Resend>
```

Site `.env`:
```env
VITE_APP_URL=http://localhost:3000
```

- [ ] **Step 2: Subir ambos**

```bash
# terminal 1
cd app && npm run dev

# terminal 2
cd site-adscale && npm run dev
```

- [ ] **Step 3: Smoke manual**

1. Abrir `http://localhost:5173`
2. Clicar "Entrar na waitlist" → modal abre
3. Preencher e enviar → sucesso
4. Reenviar mesmo email → mensagem de duplicata
5. "Já tenho acesso" → `http://localhost:3000/login`
6. Verificar registro no banco: `SELECT * FROM adscale_app.waitlist_signups;`
7. Verificar contato no Resend Dashboard

- [ ] **Step 4: Rodar suite de testes do app**

```bash
cd app && npm test
```

Expected: all pass

---

## Spec Coverage Checklist

| Spec requirement | Task |
|------------------|------|
| Modal no site-adscale | Task 8–9 |
| Campos nome/email/setor/whatsapp | Task 2, 8 |
| Setor lista + outro | Task 2, 8 |
| LGPD checkbox | Task 8 |
| Postgres `waitlist_signups` | Task 1, 3 |
| Resend Audiences sync | Task 4 |
| Email confirmação | Task 5 |
| Duplicata 409 | Task 6 |
| CORS | Task 6 |
| Honeypot | Task 6 |
| Link "Já tenho acesso" | Task 9 |
| Rate limit | Task 6 |
| Env vars | Task 4, 7, 10 |
| Testes | Tasks 2–6 |

---

## Production Checklist (manual)

1. Criar segmento `waitlist` no Resend Dashboard
2. Setar `RESEND_WAITLIST_SEGMENT_ID` e `MARKETING_ALLOWED_ORIGINS` no Render/Vercel (app)
3. Confirmar `VITE_APP_URL` aponta para app de produção (site)
4. Deploy app antes do site (API precisa existir)
5. Smoke em produção após deploy
