<!-- generated-by: gsd-doc-writer -->

# Deployment

How ADScale is built, deployed, and operated in production. The primary path is **Render** via the root Blueprint (`render.yaml`). **Docker** remains available for local or self-hosted runs. Environment variables and validation rules are documented in [CONFIGURATION.md](./CONFIGURATION.md).

## Deployment targets

| Target | Config | Runtime | Database | Role |
|--------|--------|---------|----------|------|
| **Render (production)** | `render.yaml` | Node.js web service (`runtime: node`) | Managed PostgreSQL 16 (`adscale-postgres`) | Primary production host |
| **Docker (local / self-hosted)** | `app/Dockerfile`, `app/docker-compose.yml` | Node 20 Alpine, Next.js `standalone` | Postgres 16 container or external URL | Local stack; optional self-hosted production |
| **GitHub Actions** | `.github/workflows/ci.yml` | `ubuntu-latest`, Node 20 | Ephemeral Postgres 16 service | CI only — no deploy job |

### Render Blueprint (`render.yaml`)

**Web service `adscale-app`**

| Setting | Value |
|---------|-------|
| `rootDir` | `app` |
| `branch` | `main` |
| `plan` | `free` |
| `region` | `oregon` |
| `autoDeployTrigger` | `commit` |
| `buildCommand` | `npm ci && npm run build` |
| `preDeployCommand` | none |
| `startCommand` | `npm run db:migrate && npm run start:prod` |
| `healthCheckPath` | `/api/health` |

**Managed database `adscale-postgres`**

| Setting | Value |
|---------|-------|
| `databaseName` | `adscale_db` |
| `user` | `adscale` |
| `postgresMajorVersion` | `16` |
| `plan` | `free` |
| `ipAllowList` | `[]` (all IPs allowed) |

`DATABASE_URL` is injected from the database connection string. `BETTER_AUTH_SECRET` is generated by Render (`generateValue: true`).

Production public URL (custom domain, set in Blueprint): `https://adscale.jhonatansoares.com`.

Blueprint quick start: see [render-deployment.md](./render-deployment.md) for the secrets checklist and deploy steps.

### Docker

- **Image:** multi-stage `app/Dockerfile` (`node:20-alpine`), non-root user `nextjs`, port `3000`, `CMD` runs `docker/entrypoint.sh`.
- **Entrypoint:** waits for `postgres:5432`, runs `npx drizzle-kit migrate`, starts `node server.js`.
- **Compose:** `app/docker-compose.yml` — `postgres` + `app`; optional `inngest` dev profile. Details: `app/DOCKER.md`.

Production Docker requires an external database and Inngest Cloud credentials; the bundled Compose file targets local development.

## Build pipeline

### CI (GitHub Actions)

Workflow: `.github/workflows/ci.yml` (`name: CI`).

**Triggers:** push to `main`, pull requests to `main`.

**Job `test`** (`ubuntu-latest`):

1. Checkout
2. Node.js 20 with npm cache (`app/package-lock.json`)
3. `cd app && npm ci`
4. `npm run lint`
5. `npm run typecheck`
6. `npx drizzle-kit migrate` (Postgres 16 service: `adscale_test` / `test:test@localhost:5432`)
7. `npm test -- --run`
8. `npm run build` (mock env vars for Zod/build — see workflow file)

There is **no deploy or release job** in CI. Render deploys when commits land on `main` (`autoDeployTrigger: commit`).

### Render deploy sequence

On each deploy to `adscale-app`:

1. **Build** (in `app/`): `npm ci && npm run build` → Next.js production build (`next build --webpack`, `output: 'standalone'` in `app/next.config.ts`).
2. **Start:** `npm run db:migrate && npm run start:prod` → run migrations (with retry via `app/scripts/migrate-with-retry.mjs`), start Next.js, then sync Inngest functions through `PUT /api/inngest` (via `app/scripts/start-with-inngest-sync.mjs`; Render sets `PORT`).

Migrations run at container start, not in a separate `preDeployCommand`. Each new instance applies pending migrations before accepting traffic.

### Local production-like build

```bash
cd app
npm ci
npm run build
npm run db:migrate   # requires DATABASE_URL
npm run start:prod
```

## Environment setup

Production configuration is defined in `render.yaml` and completed in the **Render Dashboard** for keys marked `sync: false`.

### Set in Blueprint / auto-populated

| Variable | How it is set |
|----------|----------------|
| `NODE_ENV` | `production` |
| `NEXT_TELEMETRY_DISABLED` | `1` |
| `DATABASE_URL` | From `adscale-postgres` (`fromDatabase`) |
| `BETTER_AUTH_SECRET` | Render `generateValue: true` |
| `BETTER_AUTH_URL`, `APP_URL` | `https://adscale.jhonatansoares.com` |
| `OPENAI_TEXT_MODEL` | `gpt-5-mini` |
| `OPENAI_IMAGE_MODEL` | `gpt-image-2-2026-04-21` |
| `STRIPE_SUCCESS_URL` | `https://adscale.jhonatansoares.com/settings?tab=billing&checkout=success` |
| `STRIPE_CANCEL_URL` | `https://adscale.jhonatansoares.com/settings?tab=plans&checkout=cancel` |
| `DEV_ADMIN_EMAIL` | `jhonatan.marcela@gmail.com` |

### Must set manually (`sync: false`)

`OPENAI_API_KEY`, all `R2_*`, `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY`, `RESEND_API_KEY`, `EMAIL_FROM`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and the three `STRIPE_*_PRICE_ID` values.

For Resend: create an API key in the Resend dashboard and set `RESEND_API_KEY`. Revoke and replace any key that was exposed in chat or logs. `EMAIL_FROM` can start as `ADScale <onboarding@resend.dev>` for smoke tests; for production, verify your sending domain in Resend and use an address on that domain.

Optional production vars not in `render.yaml` (set in Dashboard if used): `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, OAuth (`GOOGLE_*`, `GITHUB_*`), `MEM0_*`. See [CONFIGURATION.md](./CONFIGURATION.md).

### External integrations (same public URL as `APP_URL`)

| Integration | Endpoint |
|-------------|----------|
| Inngest | `https://adscale.jhonatansoares.com/api/inngest` |
| Stripe webhooks | `https://adscale.jhonatansoares.com/api/billing/webhook` |

If the public URL changes (different custom domain or Render subdomain), update `BETTER_AUTH_URL`, `APP_URL`, `STRIPE_SUCCESS_URL`, `STRIPE_CANCEL_URL` in `render.yaml` and re-register endpoints in the Inngest and Stripe dashboards.

### First-time Blueprint deploy

1. Commit and push `render.yaml` to the Git remote.
2. Open the Blueprint deeplink:

```text
https://dashboard.render.com/blueprint/new?repo=https://github.com/jhowtkd/adscale
```

3. Fill every `sync: false` secret.
4. Apply the Blueprint.
5. Confirm `GET /api/health` returns 200 after the deploy is live.

Do not copy values from `.env.docker` or `.env.local` into tracked files.

### Pre-deploy checklist

- All `sync: false` secrets set in Render.
- Migration SQL committed under `app/drizzle/`.
- `BETTER_AUTH_URL` and `APP_URL` match how users reach the app.
- Stripe and Inngest dashboards point at the production URLs.
- CI green on `main` (lint, typecheck, tests, build).

## Rollback procedure

### Render (application)

1. **Revert and redeploy:** `git revert <commit>` and push to `main` — Render auto-deploys the revert.
2. **Manual deploy:** Render Dashboard → service `adscale-app` → deploy a specific earlier commit.

<!-- VERIFY: Rollback features and deploy history retention depend on your Render plan tier -->

### Database

Drizzle Kit does not ship automatic down-migrations. Options:

1. **Forward fix:** add a new migration that reverses the schema change, then deploy (migrations run in `startCommand` via `npm run db:migrate`).
2. **Restore backup:** restore Postgres from Render backup or a manual `pg_dump` before re-deploying app code.

<!-- VERIFY: Free Render Postgres may not include automated backups; confirm backup policy before production cutover -->

### Docker

```bash
git checkout <stable-commit>
cd app && docker compose up --build -d
```

Use the same `DATABASE_URL` and env as before the failed release.

## Monitoring

### In-repo observability

| Signal | Location |
|--------|----------|
| **Health check** | `GET /api/health` → `{"ok":true,"service":"adscale-app","timestamp":"..."}` (`app/src/app/api/health/route.ts`) |
| **Render health** | `healthCheckPath: /api/health` — deploy marked live when this returns 2xx |
| **Sentry** | `@sentry/nextjs` in `app/src/instrumentation.ts` — active when `SENTRY_DSN` is set (`tracesSampleRate` 0.1 in production) |
| **Application logs** | stdout/stderr on Render or `docker compose logs -f app` |
| **Background jobs** | Inngest Cloud dashboard (events to `/api/inngest`) |

### Render logs

```bash
render logs --service adscale-app
```

<!-- VERIFY: Render CLI must be installed and authenticated; service name must match your Dashboard -->

### Recommended external checks

- Uptime monitor on `GET https://adscale.jhonatansoares.com/api/health`
- Sentry alerts when `SENTRY_DSN` is configured
- Stripe webhook delivery and Inngest run failure notifications in their dashboards
- Postgres metrics in Render (or your external DB provider)

## Related documentation

- [CONFIGURATION.md](./CONFIGURATION.md) — full env var reference and Zod validation
- [render-deployment.md](./render-deployment.md) — Render secrets and URL checklist
- [GETTING-STARTED.md](./GETTING-STARTED.md) — local clone and first run
- [ARCHITECTURE.md](./ARCHITECTURE.md) — system overview and data flow
- `app/DOCKER.md` — Docker Compose quick reference
