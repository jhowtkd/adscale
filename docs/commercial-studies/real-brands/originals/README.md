Drop the campaign files locally into this directory.

Do not scrape campaign ads, YouTube frames, or web images.

Do not commit binaries. This folder is gitignored except for this README.

The seed refuses to run until each file's sha256 matches `manifest.json`.
