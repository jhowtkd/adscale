# Beta feedback — 2026-06-28

## Resumo
- Total novos (janela): 0
- Abertos (new/reviewing): 0
- P0: 0 | P1: 0 | P2: 0 | P3: 0

## P0 — ação imediata
Nenhum feedback beta P0 na janela.

## P1 — hoje
Nenhum feedback beta P1 na janela.

## P2/P3 — backlog
Nenhum feedback beta P2/P3 novo na janela.

## Mission insights (informativo)
Nenhum mission insight novo na janela.

## Sem novidades
Não houve novos registros em `adscale_app.feedback_reports` na janela analisada.

## Coleta
- Janela analisada em `America/Sao_Paulo`: `2026-06-27T09:05:19-03:00` até `2026-06-28T09:05:00.126398-03:00`.
- O ponto inicial veio de `.planning/ops/beta-feedback-daily/last-run.json` via `exportedAt=2026-06-27T12:05:19Z`.
- Tentativa 1 do one-off job Render `job-d90gqr3tqb8s73fqtmf0` falhou com `column "utc" does not exist` por quoting incorreto no `AT TIME ZONE 'UTC'` dentro do `node -e`.
- Tentativa 2 do one-off job Render `job-d90grr68bjmc739780l0` concluiu a consulta SQL com `FEEDBACK_DAILY_COUNT=0`, lendo os logs do próprio recurso do job.
- A query manteve o corte em UTC via `created_at >= ('...::timestamptz AT TIME ZONE 'UTC')` e `created_at < ('...::timestamptz AT TIME ZONE 'UTC')` porque `feedback_reports.created_at` é `timestamp` sem timezone no schema.

Próxima execução recomendada: amanhã ~09:00 BRT
