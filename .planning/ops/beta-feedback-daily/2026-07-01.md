# Beta feedback — 2026-07-01

## Resumo
- Total novos (janela): 0
- Abertos (new/reviewing): 0
- P0: 0 | P1: 0 | P2: 0 | P3: 0

## P0 — ação imediata
Nenhum feedback beta P0 na janela.

## P1 — hoje
Nenhum feedback beta P1 na janela.

## P2/P3 — backlog
Nenhum feedback beta P2/P3 novo na janela.

## Mission insights (informativo)
Nenhum mission insight novo na janela.

## Sem novidades
Não houve novos registros em `adscale_app.feedback_reports` na janela analisada.

## Coleta
- Janela analisada em `America/Sao_Paulo`: `2026-06-27T09:05:19-03:00` até `2026-07-01T06:01:16-03:00`.
- O ponto inicial veio de `.planning/ops/beta-feedback-daily/last-run.json` via `exportedAt=2026-06-27T12:05:19Z`.
- Tentativa 1 do one-off job Render `job-d92g3fkm0tmc73e39fk0` terminou com `FEEDBACK_DAILY_SINCE=undefined` e `FEEDBACK_DAILY_UNTIL=undefined`, então o resultado foi descartado.
- A coleta válida final veio do one-off job Render `job-d92g4ssm0tmc73e3b940`, lendo os logs do próprio recurso com `FEEDBACK_DAILY_COUNT=0`.
- A query manteve o corte em UTC via `created_at >= ($1::timestamptz AT TIME ZONE 'UTC')` e `created_at < ($2::timestamptz AT TIME ZONE 'UTC')` porque `feedback_reports.created_at` é `timestamp` sem timezone no schema.

Próxima execução recomendada: amanhã ~09:00 BRT
