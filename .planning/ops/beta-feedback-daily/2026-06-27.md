# Beta feedback — 2026-06-27

## Resumo
- Total novos (janela): 1
- Abertos (new/reviewing): 1
- P0: 0 | P1: 0 | P2: 1 | P3: 0

## P0 — ação imediata
Nenhum feedback beta P0 na janela.

## P1 — hoje
Nenhum feedback beta P1 na janela.

## P2/P3 — backlog
- [8bac5380-163e-417e-b1c1-549f1466f664] medium/ui — a missão não tem sentido.
  - rota: `/`
  - hipótese: a reclamação bate com a experiência da dashboard, onde a "missão ativa" é mostrada como trilha principal sem contexto suficiente sobre por que aquela etapa importa agora. O problema mais provável está no acoplamento entre a inferência automática da missão ativa e a copy/apresentação do card na home, o que faz a estrutura de missão parecer arbitrária para quem ainda não entrou no fluxo da campanha.
  - arquivos suspeitos: `app/src/components/dashboard/MissionPathCard.tsx`, `app/src/server/progression/missions/service.ts`, `app/src/server/progression/missions/status.ts`, `app/src/server/progression/missions/hrefs.ts`

## Mission insights (informativo)
Nenhum mission insight novo na janela.

## Coleta
- Janela analisada em `America/Sao_Paulo`: `2026-06-26T09:03:27.616326-03:00` até `2026-06-27T09:05:19-03:00`.
- O ponto inicial veio de `.planning/ops/beta-feedback-daily/last-run.json` via `exportedAt=2026-06-26T12:03:27.616326Z`.
- A coleta válida final veio do one-off job Render `job-d8vs5ataeets73de9uag`, lendo os logs do próprio recurso com `FEEDBACK_DAILY_COUNT=1` e a linha completa do registro.
- Tentativa 1 anterior (`job-d8vro9daeets73dds9hg`) falhou com `PSQL_PASSWORD` ausente no ambiente do job.
- Tentativa 2 anterior (`job-d8vrp077f7vs739gait0`) confirmou a contagem, mas falhou ao listar linhas por reutilizar uma CTE em dois statements.
- A query manteve o corte em UTC via `created_at >= ($from::timestamptz AT TIME ZONE 'UTC')` e `created_at < ($to::timestamptz AT TIME ZONE 'UTC')` porque `feedback_reports.created_at` é `timestamp` sem timezone no schema.

Próxima execução recomendada: amanhã ~09:00 BRT
