---
phase: 201
plan: 01
slug: automated-uat-gap-closure
status: in_progress
requirements: [QA-02, QA-03]
depends_on: [200-01]
autonomous: true
---

# Plan 201-01 — Automated UAT Gap Closure

## Objective

Close QA-02 and QA-03 audit gaps with full authenticated Playwright scenario matrix, accessibility/keyboard coverage, and stable login for guided journey E2E.

## Tasks

### Task 1: Stabilize guided journey auth helper
- Extract retry-based login from visual-auth pattern into `tests/e2e/support/guided-auth.ts`
- Add cookie consent init script and 3-attempt retry on login redirect timeout

### Task 2: Expand Playwright scenario matrix (QA-03)
- Add `guided-assistant-scenarios.spec.ts` covering:
  - reload/resume (resume banner after mid-journey thread load)
  - correction (brief review edit affordance)
  - switch/restart (guided flow controls preview + confirm)
  - conflict (revision conflict surfaces alert on controls)
  - retry (recoverable error + clear_error)
  - stale card (confirm rejected, card stays pending)
  - resource replacement (references panel selection)
  - existing_creative diagnosis path
  - keyboard navigation on journey cards and controls

### Task 3: Accessibility component tests (QA-02)
- `GuidedFlowControls.test.tsx` — role=status preview, role=alert errors, keyboard-operable buttons
- `FromZeroProgressiveBriefPanel.test.tsx` — labeled inputs, keyboard submit
- Expand `GuidedFlowResumeBanner.test.tsx` — recoverable error message

### Task 4: Playwright config and verification
- Add `playwright.guided.config.ts` with webServer, retries=1 for login flakes
- Run `npm test` for new component tests
- Run `npm run test:guided-e2e` when dev server available

## Success Criteria

- QA-03 scenario matrix automated (mocked API, no provider credits)
- QA-02 keyboard/focus/announcement coverage via component + browser tests
- Login flake mitigation via shared retry helper
- All new unit/component tests pass
