# Phase 159-01 Plan — Global Queue API and Filters

**Status:** complete  
**Requirements:** QUEUE-01, QUEUE-02, QUEUE-05

## Tasks

- [x] `parseCorpusQueueFilters` for cohort, mode, format, status, source label, dates
- [x] `listCorpusQueueItems()` with candidate join for `sourceLabel`
- [x] `getCorpusOperationsProgress()` with dimensional buckets
- [x] `listCorpusQueue` / GET route uses filters without required `workspaceId`

## Verification

- `tests/unit/human-quality/queue-filters.test.ts` — pass
- `src/app/api/feedback/human-quality-corpus/route.test.ts` — pass
