---
phase: 167-global-cross-client-promotion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/calibration/types.ts
  - app/src/server/human-quality/learning/cross-client.ts
  - app/tests/unit/human-quality/learning/cross-client.test.ts
autonomous: true
requirements:
  - GLOBAL-01
  - GLOBAL-02
  - GLOBAL-03
user_setup: []

must_haves:
  truths:
    - "Same primaryFailureReason with approved corpus_quality rules in 2+ clientProfileIds and 6+ cross-client evals produces proposed rubric_calibration_adjustments"
    - "Global proposal evidenceRefs.fixtureOnly is true when all supporting evaluations are synthetic_fixture"
    - "Global proposal evidenceRefs.supportingClientRuleIds lists every approved corpus_quality rule that triggered promotion"
    - "Global proposal evidenceRefs.promotionSource is cross_client"
  artifacts:
    - path: app/src/server/human-quality/learning/cross-client.ts
      provides: "detectAndPersistCrossClientGlobalProposals with enriched evidenceRefs"
      exports: ["detectAndPersistCrossClientGlobalProposals", "extractPrimaryFailureReasonFromRationale"]
    - path: app/src/server/human-quality/calibration/types.ts
      provides: "Extended CalibrationAdjustmentEvidence cross-client fields"
  key_links:
    - from: cross-client.ts
      to: listApprovedCorpusQualityRules
      via: "group by rationale prefix failure reason"
      pattern: "listApprovedCorpusQualityRules"
    - from: cross-client.ts
      to: persistProposedAdjustments
      via: "enriched evidenceRefs before insert"
      pattern: "supportingClientRuleIds|fixtureOnly"
    - from: cross-client.ts
      to: listEvaluatedCorpusWithEvaluations
      via: "sourceLabel for fixtureOnly gate"
      pattern: "sourceLabel"
---

# Plan 167-01: Cross-Client Detector Hardening

<objective>
Harden the existing cross-client global promotion detector with complete evidence metadata for GLOBAL-01..03 server contract.

Purpose: Core detection ships on branch — close fixtureOnly source gate, supporting client rule links, and promotionSource tagging without re-implementing thresholds.
Output: Extended types, enriched `cross-client.ts`, expanded unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-global-cross-client-promotion/167-RESEARCH.md
@.planning/phases/167-global-cross-client-promotion/167-VALIDATION.md
@docs/superpowers/specs/2026-06-21-corpus-learning-loop-design.md
@app/src/server/human-quality/learning/cross-client.ts
@app/tests/unit/human-quality/learning/cross-client.test.ts
@app/src/server/human-quality/learning/aggregate.ts

<interfaces>
From `calibration/types.ts` — extend:
```typescript
export interface CalibrationAdjustmentEvidence {
  corpusItemIds: string[];
  sliceStats: { count: number; meanSignedDelta: number; meanAbsError: number | null };
  itemRefs: CalibrationAdjustmentEvidenceItemRef[];
  fixtureOnly?: boolean;
  supportingClientRuleIds?: string[];
  primaryFailureReason?: string;
  promotionSource?: "cross_client";
}
```

Thresholds (unchanged):
- MIN_DISTINCT_CLIENTS = 2
- CROSS_CLIENT_MIN_EVALUATIONS = 6
- CROSS_CLIENT_DIVERGENCE_THRESHOLD = 15
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend evidence type + cross-client enrichment</name>
  <files>app/src/server/human-quality/calibration/types.ts, app/src/server/human-quality/learning/cross-client.ts</files>
  <behavior>
    - Test: mixed source (1 real_customer) → fixtureOnly false on persisted evidence
    - Test: all synthetic_fixture → fixtureOnly true
    - Test: supportingClientRuleIds contains both approved rule IDs for failure reason
    - Test: promotionSource cross_client on persisted proposals
    - Test: existing 2-client + 6-eval happy path still passes (GLOBAL-01 regression)
  </behavior>
  <action>
    Extend `CalibrationAdjustmentEvidence` with optional `fixtureOnly`, `supportingClientRuleIds`, `primaryFailureReason`, `promotionSource`.

    In `detectAndPersistCrossClientGlobalProposals`, after building `filteredRows`:
    1. `fixtureOnly = filteredRows.length > 0 && filteredRows.every((r) => r.sourceLabel === "synthetic_fixture")`
    2. `supportingClientRuleIds = rules.map((r) => r.id)`
    3. After `proposeAdjustments`, map each proposal to enrich `evidenceRefs` with the four new fields before `persistProposedAdjustments`.

    Do NOT change threshold constants or auto-accept behavior.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/cross-client.test.ts</automated>
  </verify>
  <done>cross-client.test.ts green with fixtureOnly, supportingClientRuleIds, and promotionSource assertions</done>
</task>

</tasks>

<verification>
- [ ] GLOBAL-01 detection thresholds unchanged and tested
- [ ] GLOBAL-02 fixtureOnly computed from sourceLabel
- [ ] GLOBAL-03 supportingClientRuleIds populated on evidenceRefs
</verification>

## PLANNING COMPLETE
