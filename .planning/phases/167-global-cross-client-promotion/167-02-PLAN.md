---
phase: 167-global-cross-client-promotion
plan: "02"
type: execute
wave: 2
depends_on:
  - "167-01"
files_modified:
  - app/drizzle/0054_rubric_calibration_reject.sql
  - app/drizzle/meta/_journal.json
  - app/src/server/db/schema.ts
  - app/src/server/repositories/rubric-calibration-adjustments.ts
  - app/src/server/human-quality/improvement/accept.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.test.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/reject/route.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/reject/route.test.ts
  - app/tests/unit/human-quality/improvement/accept.test.ts
  - app/tests/unit/human-quality/calibration-adjustments-repository.test.ts
autonomous: true
requirements:
  - GLOBAL-03
  - GLOBAL-04
user_setup: []

must_haves:
  truths:
    - "Owner can reject a proposed global adjustment with required reason text"
    - "Rejected adjustments cannot be accepted (409 adjustment_not_proposed)"
    - "Accepting fixtureOnly cross_client proposal requires acknowledgeFixtureOnly true"
    - "No code path auto-accepts proposed global adjustments"
  artifacts:
    - path: app/src/app/api/feedback/calibration-adjustments/[id]/reject/route.ts
      provides: PATCH reject API with platform-owner auth
      exports: ["PATCH"]
    - path: app/src/server/repositories/rubric-calibration-adjustments.ts
      provides: rejectAdjustment repository method
    - path: app/src/server/human-quality/improvement/accept.ts
      provides: fixture ack gate for cross_client proposals
  key_links:
    - from: reject/route.ts
      to: rejectAdjustment
      via: "status proposed → rejected with reason"
      pattern: "rejectAdjustment"
    - from: accept.ts
      to: evidenceRefs.fixtureOnly
      via: "acknowledgeFixtureOnly when promotionSource cross_client"
      pattern: "acknowledgeFixtureOnly"
    - from: learning-proposal-aggregator.ts
      to: detectAndPersistCrossClientGlobalProposals
      via: "insert proposed only — never accept"
      pattern: "persistProposedAdjustments"
---

# Plan 167-02: Global Reject Lifecycle + Accept Hardening

<objective>
Add reject lifecycle for global rubric_calibration_adjustments and fixture acknowledgment on cross-client accept — closing GLOBAL-03 accept contract and GLOBAL-04 human-authority gate.

Purpose: Phase 132 shipped accept-only; schema blocks rejected status. Mirror client_learning_proposals reject pattern for global adjustments.
Output: Migration, reject repo + API, accept fixture ack, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-global-cross-client-promotion/167-RESEARCH.md
@.planning/phases/167-global-cross-client-promotion/167-VALIDATION.md
@app/src/server/repositories/rubric-calibration-adjustments.ts
@app/src/server/human-quality/improvement/accept.ts
@app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts
@app/src/app/api/admin/quality/learning/proposals/[id]/reject/route.ts

<interfaces>
Migration shape:
```sql
ALTER TABLE rubric_calibration_adjustments
  ADD COLUMN rejected_reason text,
  ADD COLUMN rejected_at timestamp,
  ADD COLUMN rejected_by text REFERENCES "user"(id);
-- Update status check to include 'rejected'
```

Reject body schema (mirror client proposals):
```typescript
z.object({ reason: z.string().min(1).max(500) })
```

Accept body extension:
```typescript
z.object({
  changeSpec: qualityImprovementChangeSpecSchema.optional(),
  acknowledgeFixtureOnly: z.boolean().optional(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + reject repository</name>
  <files>app/drizzle/0053_rubric_calibration_reject.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts, app/src/server/repositories/rubric-calibration-adjustments.ts, app/tests/unit/human-quality/calibration-adjustments-repository.test.ts</files>
  <behavior>
    - Test: rejectAdjustment sets status rejected, rejectedReason, rejectedAt, rejectedBy
    - Test: reject on non-proposed throws adjustment_not_proposed
    - Test: reject without reason throws validation error at route layer
  </behavior>
  <action>
    Add migration `0054_rubric_calibration_reject.sql`:
    - Add `rejected_reason`, `rejected_at`, `rejected_by` columns
    - Update check constraint to include `rejected` status

    Implement `rejectAdjustment({ adjustmentId, reviewerUserId, reason })` in repository.
    Update schema.ts types accordingly.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/calibration-adjustments-repository.test.ts</automated>
  </verify>
  <done>Repository reject tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject API + accept fixture ack</name>
  <files>app/src/app/api/feedback/calibration-adjustments/[id]/reject/route.ts, app/src/app/api/feedback/calibration-adjustments/[id]/reject/route.test.ts, app/src/server/human-quality/improvement/accept.ts, app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts, app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.test.ts, app/tests/unit/human-quality/improvement/accept.test.ts</files>
  <behavior>
    - Test: PATCH reject with reason → 200, status rejected
    - Test: PATCH reject without reason → 400
    - Test: PATCH accept on rejected → 409
    - Test: accept cross_client + fixtureOnly without ack → 422 insufficient_acknowledgment
    - Test: accept cross_client + fixtureOnly with ack → 200
    - Test: aggregator/generate only persist proposed (regression comment test)
  </behavior>
  <action>
    Create `PATCH .../calibration-adjustments/[id]/reject` with `requireCalibrationAccess`, Zod reason validation, calls `rejectAdjustment`.

    Extend `acceptProposedAdjustment`:
    - When `evidenceRefs.promotionSource === "cross_client"` AND `evidenceRefs.fixtureOnly === true`, require `input.acknowledgeFixtureOnly === true` (mirror `learning/proposals.ts`).

    Extend accept route body schema with optional `acknowledgeFixtureOnly`.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/feedback/calibration-adjustments/ tests/unit/human-quality/improvement/accept.test.ts</automated>
  </verify>
  <done>Accept and reject route tests green</done>
</task>

</tasks>

<verification>
- [ ] GLOBAL-04 reject requires reason; no auto-accept
- [ ] GLOBAL-03 accept preserves supportingClientRuleIds on returned adjustment
- [ ] GLOBAL-02 fixture ack enforced on accept when fixtureOnly
</verification>

## PLANNING COMPLETE
