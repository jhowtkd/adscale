---
phase: 167-global-cross-client-promotion
plan: "03"
type: execute
wave: 3
depends_on:
  - "167-01"
  - "167-02"
files_modified:
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  - app/tests/unit/human-quality/learning/global-promotion-isolation.test.ts
  - .planning/phases/167-global-cross-client-promotion/167-VERIFICATION.md
autonomous: true
requirements:
  - GLOBAL-03
  - GLOBAL-05
user_setup: []

must_haves:
  truths:
    - "Calibration tab shows cross-client global proposals with supporting client rule IDs and fixtureOnly badge"
    - "Owner can accept or reject global proposals from Calibration tab"
    - "Accepted global rubric adjustments do not inject corpus_quality rules into another clientProfile prompt"
    - "Per-brand prompt isolation from 164-03 remains intact after global promotion"
  artifacts:
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: "CalibrationTabContent accept/reject + cross-client metadata"
    - path: app/tests/unit/human-quality/learning/global-promotion-isolation.test.ts
      provides: "GLOBAL-05 isolation regression"
    - path: .planning/phases/167-global-cross-client-promotion/167-VERIFICATION.md
      provides: "Phase sign-off report"
  key_links:
    - from: HumanQualityCorpusPanel.tsx
      to: calibration-adjustments accept/reject APIs
      via: "PATCH with acknowledgeFixtureOnly when fixtureOnly"
      pattern: "calibration-adjustments"
    - from: global-promotion-isolation.test.ts
      to: prompt-calibration-loader.ts
      via: "loadPromptCalibrationContext per profile after mock global accept"
      pattern: "loadPromptCalibrationContext"
    - from: global-promotion-isolation.test.ts
      to: prompt-rule-isolation.test.ts
      via: "extend 164-03 pattern — global rubric does not add corpus_quality"
      pattern: "appliedCorpusRuleIds"
---

# Plan 167-03: Calibration UI + Isolation Regression

<objective>
Surface cross-client global proposals in the owner Calibration tab with accept/reject actions and prove GLOBAL-05 — global promotion does not bypass per-brand prompt isolation.

Purpose: Detection and lifecycle ship in 167-01/02; this plan closes operator UX and isolation proof for v13.2 finale.
Output: Enhanced CalibrationTabContent, isolation test, phase verification doc.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/167-global-cross-client-promotion/167-RESEARCH.md
@.planning/phases/167-global-cross-client-promotion/167-VALIDATION.md
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/tests/unit/ai/prompt-rule-isolation.test.ts
@app/src/components/feedback/LearningProposalsTab.tsx

<interfaces>
Score calibration report adjustment shape (extend display only):
```typescript
{
  targetModule: string;
  targetKey: string;
  adjustmentVersion: string;
  evidenceCount: number;
  status: string;
  evidenceRefs?: {
    fixtureOnly?: boolean;
    supportingClientRuleIds?: string[];
    promotionSource?: string;
    primaryFailureReason?: string;
  };
}
```

Accept/reject handlers mirror LearningProposalsTab fixture ack checkbox pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibration tab cross-client UI</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <behavior>
    - Test: proposed adjustment with promotionSource cross_client shows badge and supporting rule count
    - Test: fixtureOnly proposal shows fixture warning + requires checkbox before accept
    - Test: accept calls PATCH calibration-adjustments with acknowledgeFixtureOnly when fixtureOnly
    - Test: reject calls PATCH reject with reason text
  </behavior>
  <action>
    Enhance `CalibrationTabContent` proposed adjustments section:
    1. Show `Cross-client` badge when `evidenceRefs.promotionSource === "cross_client"`
    2. List `supportingClientRuleIds` as monospace links/IDs (read-only)
    3. Show fixture-only warning banner when `evidenceRefs.fixtureOnly`
    4. Add Accept / Reject buttons (owner actions) — replace read-only list for `status === "proposed"`
    5. Fixture ack checkbox before accept (mirror LearningProposalsTab)

    Ensure score-calibration API response includes full `evidenceRefs` if currently stripped — extend route/report mapper only if needed.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/feedback/HumanQualityCorpusPanel.test.tsx -x</automated>
  </verify>
  <done>HumanQualityCorpusPanel cross-client UI tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GLOBAL-05 isolation regression + verification</name>
  <files>app/tests/unit/human-quality/learning/global-promotion-isolation.test.ts, .planning/phases/167-global-cross-client-promotion/167-VERIFICATION.md</files>
  <behavior>
    - Test: profile A prompt contains only profile A corpus_quality rules after mock global adjustment accepted
    - Test: profile B appliedCorpusRuleIds never includes profile A rule IDs
    - Test: global rubric adjustment (score_ceiling) does not appear in prompt text as corpus_quality constraint
  </behavior>
  <action>
    Create `global-promotion-isolation.test.ts` extending 164-03 pattern:
    - Mock `listAcceptedAdjustments` returning a cross_client global adjustment
    - Mock per-profile calibration rules unchanged
    - Assert `loadPromptCalibrationContext` / `buildDerivationPrompt` for each profile excludes cross-profile rule IDs

    Write `167-VERIFICATION.md` mirroring `166-VERIFICATION.md` structure with GLOBAL-01..05 sign-off table.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/global-promotion-isolation.test.ts tests/unit/ai/prompt-rule-isolation.test.ts</automated>
  </verify>
  <done>Isolation test green; 167-VERIFICATION.md drafted</done>
</task>

</tasks>

<verification>
- [ ] GLOBAL-03 owner can accept global adjustment with supporting rule visibility
- [ ] GLOBAL-05 cross-profile prompt isolation proven
- [ ] Phase verification doc ready for execute-phase sign-off
</verification>

## PLANNING COMPLETE
