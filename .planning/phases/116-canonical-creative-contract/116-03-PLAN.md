---
phase: 116-canonical-creative-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - "116-01"
  - "116-02"
files_modified:
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/ai/quality-prompt-regression.test.ts
autonomous: true
requirements:
  - CONT-03
must_haves:
  truths:
    - "art_variation MODE block uses tier-aware preservation language — mandatory tier meaning preserved; condensable/decorative may merge or omit per canonical contract"
    - "format_adaptation preserves copy/facts exactly while layout prominence follows three-zone hierarchy — no undifferentiated PRESERVE EXACTLY at equal visual weight for all modules"
    - "bold/extreme creativity templates clarify INVIOLABLE applies to mandatory tier spelling; consolidation applies to condensable modules only"
    - "No prompt simultaneously demands preserve every important piece at full weight AND simplify hierarchy without RULE PRECEDENCE block winning"
  artifacts:
    - path: app/src/server/ai/prompt-builder.ts
      provides: tier-aware mode blocks and creativity template edits
    - path: app/src/server/ai/prompt-builder.test.ts
      provides: CONT-03 negative and precedence assertions
  key_links:
    - from: app/src/server/ai/prompt-builder.ts MODE art_variation
      to: canonical contract RULE PRECEDENCE
      via: tier-aware MANDATORY PRESERVATION wording references mandatory/condensable/decorative
      pattern: mandatory tier|Tier mandatory|condensable
    - from: app/src/server/ai/prompt-builder.ts reference paragraphs
      to: VISUAL HIERARCHY CONTRACT
      via: removed preserve-all without precedence at L467
      pattern: cleaner hierarchy
---

# Plan 116-03: Resolve Preserve-All vs Hierarchy Conflicts

<objective>
Edit conflicting mode-block and creativity-template language so no derivation prompt simultaneously demands literal preservation of every module and hierarchy simplification without the canonical precedence block governing conflicts (CONT-03).

Purpose: Models follow the strongest literal instruction — removing contradictory preserve-all text is required, not optional stacking.

Output: Tier-aware art_variation and format_adaptation MODE blocks, updated bold/extreme templates, negative regression asserts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-canonical-creative-contract/116-RESEARCH.md
@app/src/server/ai/prompt-builder.ts

**Conflicts to resolve (file:line verified):**

| Location | Current | Fix |
|----------|---------|-----|
| L338 | `preserve every important piece of campaign information` | Tier-aware: preserve **mandatory tier** meaning |
| L345 | `Do not solve a crowded layout by deleting information` | Condense/omit condensable/decorative per precedence |
| L353 | `PRESERVE EXACTLY: ... all text copy` at equal weight | Split: copy/facts verbatim; layout prominence follows three zones |
| L94-95 bold | INVIOLABLE TEXT + consolidate modules | Mandatory tier spelling vs condensable consolidation |
| L467 | `cleaner hierarchy that still includes all critical copy` | Align with tier language; no preserve-all contradiction |

Plans 116-01 and 116-02 must be complete — canonical + integrity blocks already injected before MODE.
</context>

<tasks>

<task id="116-03-01" type="auto" tdd="true">
  <name>Edit art_variation MODE and creativity templates for tier-aware preservation</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - art_variation MODE does not contain "preserve every important piece" without tier qualification
    - MODE contains tier-aware language referencing mandatory / condensable / decorative
    - bold template: INVIOLABLE applies to mandatory tier; consolidation for condensable only
    - extreme template: does not require every block at equal size
    - Tests: describe `precedence` asserts RULE PRECEDENCE present; describe `no preserve-all` asserts prompt does not match /preserve every important piece/i without /mandatory tier|Tier mandatory/i nearby
  </behavior>
  <action>
Edit `prompt-builder.ts` art_variation MODE block (currently ~L334-347):

- Replace `MANDATORY PRESERVATION: preserve every important piece...` with tier-aware wording per RESEARCH conflict table: preserve **mandatory tier** content in meaning; condensable/decorative per canonical contract tiers and RULE PRECEDENCE.

- Replace `Do not solve a crowded layout by deleting information` with: do not drop **mandatory tier** meaning; condense or omit condensable/decorative modules per precedence.

- Update bold template (~L87-96): add explicit line that INVIOLABLE TEXT applies to mandatory tier spelling; consolidation applies to condensable modules only.

- Update extreme template (~L98-107): align INVIOLABLE CONTENT BLOCKS with mandatory tier; allow condensable/decorative reorganization.

- Edit reference paragraph at ~L467: replace "all critical copy" preserve-all phrasing with tier-aligned hierarchy language consistent with canonical contract.

Add tests in `prompt-builder.test.ts`:
- `describe("precedence")` — prompt contains RULE PRECEDENCE; mode section references mandatory tier
- `describe("no preserve-all conflict")` — negative assert on undifferentiated preserve-all pattern

Update inline snapshots in prompt-builder.test.ts if mode section text changed (run vitest -u only for affected snapshots).
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "precedence|no preserve-all"</automated>
  </verify>
  <done>art_variation and creativity templates tier-aware; CONT-03 negative tests pass.</done>
</task>

<task id="116-03-02" type="auto" tdd="true">
  <name>Edit format_adaptation MODE for factual vs visual prominence split</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts, app/tests/unit/ai/quality-prompt-regression.test.ts</files>
  <behavior>
    - format_adaptation MODE still requires all copy/facts preserved verbatim (legal, CTA, headlines)
    - MODE explicitly states factual completeness does not require equal visual weight — three-zone hierarchy applies to layout prominence
    - format_adaptation regression snapshots still match layout adaptation invariants (no letterboxing regression)
    - No simultaneous undifferentiated PRESERVE EXACTLY for all modules AND hierarchy simplification without precedence
  </behavior>
  <action>
Edit format_adaptation MODE block (~L348-359):

- Reframe `PRESERVE EXACTLY` to distinguish **copy and facts** (verbatim mandatory) from **layout prominence** (three information zones per VISUAL HIERARCHY CONTRACT).

- Add explicit line: factual completeness ≠ equal visual weight; decorative chrome may yield to hook + offer + CTA zones.

- Keep format-specific layout rules (9:16, 4:5, 1:1) and anti-letterboxing language unchanged.

- Do NOT remove literal copy preservation for format mode — CONT-03 allows tier-aware wording, not dropping legal/CTA preservation.

Update `quality-prompt-regression.test.ts` format_adaptation fixture asserts:
- mode still matches layout adaptation / native composition
- mode contains tier or hierarchy language
- `extractPromptModeSection` does not match undifferentiated preserve-all without precedence context

Re-run full prompt-builder and quality-prompt-regression suites; update snapshots only where mode text intentionally changed.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/ai/quality-prompt-regression.test.ts</automated>
  </verify>
  <done>format_adaptation tier-split complete; regression suite green; no preserve-all/hierarchy contradiction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MODE block → image model | Edited instructions directly affect generation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Tampering | weakened format copy preservation | mitigate | format_adaptation keeps verbatim copy/facts; only visual prominence tiered; regression asserts legal/CTA language retained |
| T-116-06 | Denial of Service | prompt length growth | accept | Edits replace conflicting lines; net length should not grow >15% per RESEARCH pitfall guidance |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/ai/quality-prompt-regression.test.ts` passes.
Phase gate: `cd app && npm test && npm run lint && npm run build`.
</verification>

<success_criteria>
- CONT-03: No prompt demands literal preservation of all modules AND hierarchy simplification without RULE PRECEDENCE governing conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/116-canonical-creative-contract/116-03-SUMMARY.md`
</output>
