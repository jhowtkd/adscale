---
phase: 116-canonical-creative-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "116-01"
files_modified:
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/ai/quality-prompt-regression.test.ts
  - app/src/server/jobs/derivation.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-04
must_haves:
  truths:
    - "buildDerivationPrompt injects CANONICAL CREATIVE CONTRACT before MODE for art_variation, format_adaptation, and restyling"
    - "VISUAL_HIERARCHY_CONTRACT and ANTI_HALLUCINATION_RULES appear in every applicable derivation prompt via buildIntegrityPromptSection"
    - "Injection order: HARD RULES → canonical contract → integrity → restyling factual-source (if restyling) → MODE"
    - "extractPromptIntegritySection and extractPromptCanonicalContractSection enable regression tests to prove injection"
  artifacts:
    - path: app/src/server/ai/prompt-builder.ts
      provides: buildIntegrityPromptSection, wired canonical + integrity injection, extractors
      exports:
        - buildIntegrityPromptSection
        - extractPromptIntegritySection
        - extractPromptCanonicalContractSection
    - path: app/src/server/ai/prompt-builder.test.ts
      provides: integrity injection tests per mode
    - path: app/tests/unit/ai/quality-prompt-regression.test.ts
      provides: QUALITY_FIXTURES integrity assertions
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/canonical-creative-contract.ts
      via: buildCanonicalContractPromptSection(contract ?? resolved default)
      pattern: buildCanonicalContractPromptSection
    - from: app/src/server/ai/prompt-builder.ts
      to: VISUAL_HIERARCHY_CONTRACT constant
      via: buildIntegrityPromptSection parts.push
      pattern: VISUAL HIERARCHY CONTRACT
---

# Plan 116-02: Integrity Injection, Extractors, and Regression Tests

<objective>
Wire the canonical contract prompt section and previously dead `VISUAL_HIERARCHY_CONTRACT` / `ANTI_HALLUCINATION_RULES` constants into `buildDerivationPrompt` for all three derivation modes, with test extractors proving injection (CONT-04).

Purpose: Close the audit root cause — integrity rules declared but never reaching the image model.

Output: Updated prompt assembly order, exported extractors, Vitest coverage across modes and QUALITY_FIXTURES.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-canonical-creative-contract/116-RESEARCH.md
@.planning/phases/116-canonical-creative-contract/116-VALIDATION.md
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/canonical-creative-contract.ts
@app/tests/unit/ai/quality-prompt-regression.test.ts

<interfaces>
From Plan 116-01 (import if not yet merged — implement against these contracts):

```typescript
export function buildCanonicalContractPromptSection(contract: CreativeContract): string[];
export function resolveCanonicalCreative(contract: CreativeContract, campaign?: Campaign | null, diagnosis?: CreativeDiagnosis | null): CanonicalCreative;
export function buildIntegrityPromptSection(): string[]; // to be created in this plan
```

Existing extractors in `prompt-builder.ts`:
```typescript
export function extractPromptHardRulesSection(prompt: string): string;
export function extractPromptModeSection(prompt: string): string;
export function extractPromptRestylingFactualSourceSection(prompt: string): string;
```

Dead constants at L116-128 — must be referenced by `buildIntegrityPromptSection`, not duplicated.
</interfaces>
</context>

<tasks>

<task id="116-02-01" type="auto" tdd="true">
  <name>Wire canonical and integrity sections into buildDerivationPrompt</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - buildDerivationPrompt for art_variation, format_adaptation, restyling each contain "CANONICAL CREATIVE CONTRACT:" before "MODE:"
    - Each mode prompt contains "VISUAL HIERARCHY CONTRACT:" and "ANTI-HALLUCINATION RULES:"
    - Canonical section appears before integrity section; integrity before MODE
    - When contract.canonicalCreative is absent, resolveCanonicalCreative runs with config.campaign and config.creativeDiagnosis
    - extractPromptIntegritySection returns both hierarchy and anti-hallucination blocks
    - extractPromptCanonicalContractSection returns canonical block through RULE PRECEDENCE
  </behavior>
  <action>
In `prompt-builder.ts`:

1. Import `buildCanonicalContractPromptSection`, `resolveCanonicalCreative` from `canonical-creative-contract.ts`.

2. Add `buildIntegrityPromptSection(): string[]` returning `["", VISUAL_HIERARCHY_CONTRACT, "", ANTI_HALLUCINATION_RULES]`.

3. In `buildDerivationPrompt`, immediately after `buildHardRulesSection` push:
   - Resolve effective contract: merge `contract` with defaults or call `resolveCanonicalCreative` when `canonicalCreative` missing
   - `parts.push(...buildCanonicalContractPromptSection(effectiveContract))`
   - `parts.push(...buildIntegrityPromptSection())`

4. **Reorder** restyling factual-source block to AFTER integrity, BEFORE MODE (per locked injection order in RESEARCH).

5. Export `extractPromptIntegritySection(prompt)` — slice from "VISUAL HIERARCHY CONTRACT:" until "\nMODE:" or "\nRESTYLING FACTUAL-SOURCE RULE:" (whichever comes first after integrity).

6. Export `extractPromptCanonicalContractSection(prompt)` — slice from "CANONICAL CREATIVE CONTRACT:" until "VISUAL HIERARCHY CONTRACT:".

7. Update `extractPromptHardRulesSection` end markers to include `"\nCANONICAL CREATIVE CONTRACT:"` and `"\nVISUAL HIERARCHY CONTRACT:"` before `"\nRESTYLING FACTUAL-SOURCE RULE:"` and `"\nMODE:"`.

Add `prompt-builder.test.ts` describe `integrity injection`:
  - `describe.each(["art_variation", "format_adaptation", "restyling"])` asserts extractors non-empty and contain required headers
  - Assert canonical section precedes integrity precedes MODE via indexOf ordering
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t integrity</automated>
  </verify>
  <done>All three derivation modes receive canonical + integrity blocks in correct order; extractors exported.</done>
</task>

<task id="116-02-02" type="auto">
  <name>Extend quality-prompt-regression and derivation job tests</name>
  <files>app/tests/unit/ai/quality-prompt-regression.test.ts, app/src/server/jobs/derivation.test.ts</files>
  <action>
In `quality-prompt-regression.test.ts`:
- Import `extractPromptIntegritySection`, `extractPromptCanonicalContractSection`
- In each QUALITY_FIXTURES iteration, assert `extractPromptIntegritySection(prompt)` contains "VISUAL HIERARCHY CONTRACT" and "ANTI-HALLUCINATION RULES"
- Assert `extractPromptCanonicalContractSection(prompt)` contains "RULE PRECEDENCE"

Extend `derivation.test.ts` (if mock buildDerivationPrompt path exists): assert resolved contract passed to prompt builder includes `canonicalCreative` OR add focused unit test on contract resolution helper used by job — minimal assertion that job wiring from Plan 01 is not bypassed. If derivation.test.ts is integration-heavy, add one test verifying `resolveCanonicalCreative` is invoked when building prompt context (spy or direct helper test already in 116-01 — document skip with comment only if truly redundant).

Do NOT green `corpus-baseline.test.ts` — out of scope Phase 120.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts</automated>
  </verify>
  <done>QUALITY_FIXTURES prove integrity injection; derivation test documents job canonical wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt-builder → OpenAI images API | Assembled prompt crosses to external model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Tampering | campaign fields in canonical section | mitigate | Reuse existing plain-text prompt assembly; no new user-controlled HTML |
| T-116-04 | Repudiation | unwired integrity regression | mitigate | extractPromptIntegritySection + per-mode Vitest asserts (CONT-04) |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts -t integrity` and `cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts` pass.
</verification>

<success_criteria>
- CONT-04: VISUAL_HIERARCHY_CONTRACT and ANTI_HALLUCINATION_RULES injected in all applicable derivation prompts with automated proof
</success_criteria>

<output>
After completion, create `.planning/phases/116-canonical-creative-contract/116-02-SUMMARY.md`
</output>
