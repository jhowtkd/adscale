---
phase: 116-canonical-creative-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/creative-contract.ts
  - app/src/server/ai/canonical-creative-contract.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/ai/prompt-builder.test-fixtures.ts
  - app/tests/unit/creative-contract.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
must_haves:
  truths:
    - "CreativeContract carries optional canonicalCreative with dominant idea, hook, proof zone, invariant identity, and three content tiers"
    - "resolveCanonicalCreative() produces sensible defaults from campaign/contract fields and creativeDiagnosis.detectedConcept without breaking stored contracts missing new fields"
    - "buildCanonicalContractPromptSection() renders dominant idea, hook, proof/offer zone, CTA line, invariant identity, content tiers, and RULE PRECEDENCE (facts > hierarchy > decoration) as model-facing prose"
    - "derivation job populates canonicalCreative on resolvedContract before prompt build and persistence"
  artifacts:
    - path: app/src/server/ai/canonical-creative-contract.ts
      provides: CanonicalCreative types, PRECEDENCE_RULES, resolveCanonicalCreative, buildCanonicalContractPromptSection
      exports:
        - CanonicalCreative
        - ContentTiers
        - InvariantIdentity
        - PRECEDENCE_RULES
        - resolveCanonicalCreative
        - buildCanonicalContractPromptSection
    - path: app/src/server/ai/creative-contract.ts
      provides: CreativeContract extended with optional canonicalCreative
    - path: app/src/server/jobs/derivation.ts
      provides: resolvedContract.canonicalCreative populated at job time
    - path: app/tests/unit/creative-contract.test.ts
      provides: canonical resolution and prompt section unit tests
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/canonical-creative-contract.ts
      via: resolveCanonicalCreative(resolvedContract, campaign, diagnosis)
      pattern: resolveCanonicalCreative
    - from: app/src/server/ai/canonical-creative-contract.ts
      to: app/src/server/ai/creative-contract.ts
      via: import CreativeContract, CtaSemantics
      pattern: buildCanonicalContractPromptSection
---

# Plan 116-01: Canonical Contract Types, Resolution, and Job Wiring

<objective>
Extend the server `CreativeContract` spine with machine-readable canonical creative fields and runtime resolution so every derivation job carries dominant idea, three-zone hierarchy metadata, and content-tier precedence (CONT-01, CONT-02).

Purpose: Establish the contract layer the prompt builder will render in Plan 02 — without contradictory preserve-all semantics at the type level.

Output: `canonical-creative-contract.ts`, extended `CreativeContract`, job-populated `canonicalCreative`, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-canonical-creative-contract/116-RESEARCH.md
@.planning/phases/116-canonical-creative-contract/116-VALIDATION.md
@app/src/server/ai/creative-contract.ts
@app/src/server/jobs/derivation.ts

<interfaces>
From `app/src/server/ai/creative-contract.ts`:
```typescript
export interface CreativeContract {
  generationMode: GenerationMode;
  targetFormat: string;
  ctaSemantics: CtaSemantics;
  baseAssetId: string | null;
  styleAssetId: string | null;
  client: string | null;
  product: string | null;
  offer: string | null;
  constraints: string | null;
  sourcePackage?: SourcePackage;
  factualSourceRules?: FactualSourceRules;
}
export function resolveCtaSemantics(ctaText: string | null | undefined, _mode: GenerationMode): CtaSemantics;
```

From `app/src/server/ai/prompt-builder.ts`:
```typescript
export interface CreativeDiagnosis {
  detectedConcept: string;
  elementsToPreserve: string[];
  variationOpportunities: string[];
}
export interface Campaign { name: string; client: string | null; product: string | null; objective: string | null; offer: string | null; ... }
```

Job assembly at `derivation.ts:443-476` builds `resolvedContract` then passes to `buildDerivationPrompt` at L501-517.

**Do NOT import** `app/scripts/creative-contract.ts` — unrelated validation script type.
</interfaces>
</context>

<tasks>

<task id="116-01-01" type="auto" tdd="true">
  <name>Define canonical creative types and prompt section builder</name>
  <files>app/src/server/ai/canonical-creative-contract.ts, app/src/server/ai/creative-contract.ts, app/tests/unit/creative-contract.test.ts</files>
  <behavior>
    - resolveCanonicalCreative with campaign offer/objective populates dominantIdea and hook
    - creativeDiagnosis.detectedConcept overrides dominantIdea when present
    - buildCanonicalContractPromptSection includes "CANONICAL CREATIVE CONTRACT:", "Dominant idea:", "Primary hook", "Proof/offer zone", "Invariant identity", "CONTENT TIERS:", "RULE PRECEDENCE", and precedence order facts > hierarchy > decoration
    - All new CreativeContract fields are optional; missing canonicalCreative resolves at read time
  </behavior>
  <action>
Create `canonical-creative-contract.ts` exporting:

- `InvariantIdentity` — campaign, brand, product, palette, people: string[]
- `ContentTiers` — mandatory, condensable, decorative: string[]
- `CanonicalCreative` — dominantIdea, hook, proofZone, invariantIdentity, tiers
- `PRECEDENCE_RULES` — shared string constant for facts > hierarchy > decoration (exported for Phase 120 reuse)
- `resolveCanonicalCreative(contract, campaign?, diagnosis?)` — defaults per RESEARCH Pattern 3:
  - dominantIdea: diagnosis?.detectedConcept ?? campaign?.objective ?? contract.offer ?? fallback string
  - hook: contract.offer ?? "Primary headline from reference"
  - proofZone: contract.product ?? contract.client ?? "Supporting proof from reference"
  - invariantIdentity from campaign.name, contract.client, contract.product, palette "from reference and brand kit", people []
  - tiers with mandatory/condensable/decorative lists from research
- `buildCanonicalContractPromptSection(contract)` — renders canonical block including CTA line from `contract.ctaSemantics` (explicit text or "inherit from reference")
- Closing line: "When mode instructions conflict with this block, this block wins."

Extend `CreativeContract` in `creative-contract.ts` with optional `canonicalCreative?: CanonicalCreative` (re-export types from canonical module or import type-only).

Add tests in `creative-contract.test.ts` describe block `canonical creative contract`:
  - resolveCanonicalCreative defaults from campaign fixture
  - detectedConcept overrides dominantIdea
  - buildCanonicalContractPromptSection contains all CONT-01/02 required labels and precedence prose
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/creative-contract.test.ts -t "canonical"</automated>
  </verify>
  <done>Types, resolver, and prompt section builder tested; old contracts without canonicalCreative still type-check.</done>
</task>

<task id="116-01-02" type="auto">
  <name>Wire canonical resolution into derivation job and fixtures</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/ai/prompt-builder.test-fixtures.ts, app/tests/unit/creative-contract.test.ts</files>
  <action>
In `derivation.ts` after `resolvedContract` is assembled (both branches: childStoredContract merge and fresh object), set:

```typescript
canonicalCreative:
  childStoredContract?.canonicalCreative ??
  resolveCanonicalCreative(resolvedContract, campaign, normalizeCreativeDiagnosis(campaign.creativeDiagnosis)),
```

Import `resolveCanonicalCreative` from `canonical-creative-contract.ts`. Do NOT wire into `buildDerivationPrompt` yet (Plan 02).

Extend `prompt-builder.test-fixtures.ts` contract fixtures to include `canonicalCreative` via `resolveCanonicalCreative` helper or explicit minimal fixture so downstream tests have canonical fields.

Add test asserting `resolveCanonicalCreative` output shape matches `CanonicalCreative` when called with art_variation contract + campaignFixture.

Do not change gate, score, or quality-fixtures in this plan.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/creative-contract.test.ts</automated>
  </verify>
  <done>Job path populates canonicalCreative on every new resolvedContract; fixtures carry canonical fields for Plan 02 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign/diagnosis → canonicalCreative | Untrusted campaign text becomes prompt-bound canonical fields |
| stored creativeContract JSON → job merge | Legacy rows may lack canonicalCreative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | resolveCanonicalCreative inputs | mitigate | Plain-text join only; no HTML; trim/sanitize empty strings at resolution |
| T-116-02 | Spoofing | invented dominantIdea defaults | accept | Phase 120 gate blocks invented entities; 116 supplies structure only |
</threat_model>

<verification>
`cd app && npm test -- tests/unit/creative-contract.test.ts` passes with canonical describe blocks green.
</verification>

<success_criteria>
- CONT-01: Contract type and prompt section declare dominant idea, hook, proof zone, CTA, invariant identity
- CONT-02: Content tiers and precedence fatos > hierarquia > decoração encoded in types and `buildCanonicalContractPromptSection`
</success_criteria>

<output>
After completion, create `.planning/phases/116-canonical-creative-contract/116-01-SUMMARY.md`
</output>
