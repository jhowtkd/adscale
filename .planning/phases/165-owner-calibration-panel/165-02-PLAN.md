---
phase: 165-owner-calibration-panel
plan: 02
type: execute
wave: 2
depends_on:
  - "165-01"
files_modified:
  - app/src/components/admin/BrandTasteProfilePanel.tsx
  - app/src/components/admin/BrandCalibrationRulesPanel.tsx
  - app/src/components/admin/calibration-status-copy.ts
  - app/src/components/admin/OwnerCalibrationPanel.tsx
  - app/src/components/admin/OwnerCalibrationPanel.test.tsx
  - app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-04
  - PANEL-05
user_setup: []

must_haves:
  truths:
    - "Owner sees brand combobox populated from list API and can switch clientProfileId"
    - "Profile tab shows evidence level, source composition, caveats, and pattern groups"
    - "Rules tab shows approved and candidate rules in separate sections"
    - "Voice tab reuses BrandVoiceInspectPanel without regression"
    - "Fixture-only brands show honest status copy — no misleading fully-calibrated badge"
    - "Non-owner sees forbidden state consistent with BrandVoiceInspectPanel"
  artifacts:
    - path: "app/src/components/admin/OwnerCalibrationPanel.tsx"
      provides: "Tabbed orchestrator with brand selector"
      min_lines: 80
    - path: "app/src/components/admin/BrandTasteProfilePanel.tsx"
      provides: "Profile tab rendering PANEL-04 honesty"
    - path: "app/src/components/admin/BrandCalibrationRulesPanel.tsx"
      provides: "Rules tab approved/candidate tables"
    - path: "app/src/components/admin/calibration-status-copy.ts"
      provides: "PT-BR neutral labels + fixture-only banner logic"
  key_links:
    - from: "OwnerCalibrationPanel.tsx"
      to: "/api/admin/quality/brands"
      via: "useQuery brand list for combobox"
      pattern: "api/admin/quality/brands"
    - from: "BrandTasteProfilePanel.tsx"
      to: "/api/admin/quality/brands/{id}/profile"
      via: "useQuery profile"
      pattern: "api/admin/quality/brands/.*/profile"
    - from: "BrandCalibrationRulesPanel.tsx"
      to: "/api/admin/quality/brands/{id}/rules"
      via: "useQuery rules"
      pattern: "api/admin/quality/brands/.*/rules"
    - from: "OwnerCalibrationPanel.tsx"
      to: "BrandVoiceInspectPanel"
      via: "Voice tab child"
      pattern: "BrandVoiceInspectPanel"
---

<objective>
Build unified `OwnerCalibrationPanel` with brand selector and Profile · Voice · Rules tabs, including honest calibration status copy (PANEL-04).

Purpose: Productize per-brand owner workflow on existing `/admin/quality/brands/[clientProfileId]` route.
Output: Tabbed panel shell; Proposals tab deferred to Plan 165-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-owner-calibration-panel/165-RESEARCH.md
@app/src/components/admin/BrandVoiceInspectPanel.tsx
@app/src/components/layout/ResponsiveTabs.tsx
@app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx

<interfaces>
Profile API response (from Plan 165-01):
```typescript
type BrandProfileResponse = BrandTasteProfile & {
  fixtureOnly: boolean;
  corpusSignalsNote: string | null;
};
```

Rules API response:
```typescript
type BrandRulesResponse = {
  clientProfileId: string;
  workspaceId: string;
  approved: CalibrationRuleCandidate[];
  candidate: CalibrationRuleCandidate[];
};
```

Brand list API:
```typescript
type BrandListItem = {
  id: string;
  name: string;
  workspaceId: string;
  workspaceName: string;
};
```

`BrandVoiceInspectPanel` props: `{ clientProfileId: string }` — reuse unchanged on Voice tab.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibration status copy helper + Profile tab</name>
  <files>app/src/components/admin/calibration-status-copy.ts, app/src/components/admin/BrandTasteProfilePanel.tsx</files>
  <behavior>
    - Test: `getCalibrationStatusDisplay({ fixtureOnly: true, evidenceLevel: 'seed_calibrated' })` never returns strings containing "Totalmente calibrada" or "customer-validated"
    - Test: fixture-only returns warning variant with PT-BR operator-only copy
    - Test: `real_customer > 0` maps evidence levels to neutral PT-BR labels (not marketing superlatives)
    - Test: BrandTasteProfilePanel renders source composition counts and caveats list from mocked fetch
    - Test: BrandTasteProfilePanel renders pattern groups — `positivePatterns`, `rejectionPatterns`, `quasePatterns` — each with section heading and at least one row when API returns non-empty arrays
    - Test: empty pattern arrays show neutral empty state (not error)
  </behavior>
  <action>
    Create `calibration-status-copy.ts` with pure functions (no server imports):

    - `EVIDENCE_LEVEL_LABELS` — neutral PT-BR map for `uncalibrated`, `seed_calibrated`, `assisted`, `evidence_backed` (e.g. seed_calibrated → "Calibração inicial (operador)" NOT "Totalmente calibrada")
    - `getCalibrationStatusDisplay(profile)` — returns `{ label, variant: 'neutral' | 'warning', bannerText? }`
    - When `fixtureOnly` OR `sourceComposition.real_customer === 0` with `decisionCount > 0`: `variant: 'warning'`, banner "Evidência apenas de fixture/operador — não validado com cliente real"
    - Never emit green success badge when `fixtureOnly` (PANEL-04)

    Create `BrandTasteProfilePanel.tsx`:
    - Fetch `GET /api/admin/quality/brands/${clientProfileId}/profile` via `apiFetch` + `useQuery`
    - Handle 403 like `BrandVoiceInspectPanel` (forbidden copy in PT-BR)
    - Render status banner via `getCalibrationStatusDisplay`
    - Source composition bar/table: `synthetic_fixture`, `operator_imported`, `real_customer` counts
    - List `caveats` + optional `corpusSignalsNote` when present
    - **Pattern groups (PANEL-01):** three subsections with PT-BR headings:
      - "Padrões positivos" — map `positivePatterns` (label/summary, mismatchBucket if present, count)
      - "Padrões de rejeição" — map `rejectionPatterns`
      - "Quase acertos" — map `quasePatterns`
      Use compact list/table styling consistent with Rules tab; show neutral empty copy when a group is empty.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/calibration-status-copy.test.ts src/components/admin/BrandTasteProfilePanel.test.tsx -x</automated>
  </verify>
  <done>Honest status copy utility tested; Profile tab renders evidence data, pattern groups, and fixture-only guardrails</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rules tab panel</name>
  <files>app/src/components/admin/BrandCalibrationRulesPanel.tsx</files>
  <behavior>
    - Test: renders approved rules table when API returns approved array
    - Test: renders candidate section separately from approved
    - Test: 403 shows owner-restricted message
    - Test: empty arrays show neutral empty states (not error)
  </behavior>
  <action>
    Create `BrandCalibrationRulesPanel.tsx` fetching `GET .../rules`.

    Two subsections with PT-BR headings:
    - "Regras aprovadas" — table: category, rationale (truncated), confidence, approvedAt
    - "Regras pendentes (candidatas)" — same columns minus approvedAt

    Use existing table styling from `LearningProposalsTab` (border, text-xs). Read-only — no approve/reject actions here (candidates from corpus proposals are Plan 165-03).

    Match 403 UX pattern from `BrandVoiceInspectPanel`.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/BrandCalibrationRulesPanel.test.tsx -x</automated>
  </verify>
  <done>Rules tab lists approved and candidate rules with owner gate UX</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OwnerCalibrationPanel shell + page integration</name>
  <files>app/src/components/admin/OwnerCalibrationPanel.tsx, app/src/components/admin/OwnerCalibrationPanel.test.tsx, app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx</files>
  <behavior>
    - Test: renders ResponsiveTabs with Perfil, Voz, Regras labels
    - Test: brand selector calls router or onBrandChange when selection changes
    - Test: forbidden on profile fetch shows panel-level restricted message (PANEL-05)
    - Test: Voice tab mounts BrandVoiceInspectPanel with clientProfileId prop
  </behavior>
  <action>
    Create `OwnerCalibrationPanel.tsx`:

    Props: `{ clientProfileId: string; onBrandChange?: (id: string) => void }`

    1. **Brand selector (PANEL-01):** `useQuery` on `GET /api/admin/quality/brands`. Render `<select>` or combobox showing `{name} ({workspaceName})`. On change, call `onBrandChange(id)` for parent navigation (`router.push(/admin/quality/brands/${id})`).

    2. **Tabs:** `ResponsiveTabs` with ids `profile`, `voice`, `rules` — labels PT-BR: Perfil, Voz, Regras. Do **not** add Proposals tab yet (Plan 165-03).

    3. Tab content: `BrandTasteProfilePanel`, `BrandVoiceInspectPanel`, `BrandCalibrationRulesPanel` — each receives `clientProfileId`.

    4. If profile query returns forbidden, show top-level "Painel restrito a proprietários da plataforma" (match voice panel tone).

    Update `page.tsx`:
    - Title: "Calibração da marca" (or similar PT-BR)
    - Description: per-brand taste governance
    - Replace direct `BrandVoiceInspectPanel` with `OwnerCalibrationPanel`
    - Pass `onBrandChange` using `useRouter().push`

    Component tests with mocked fetch/query — follow `BrandVoiceInspectPanel` test patterns if they exist, else RTL + vi.mock apiFetch.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/OwnerCalibrationPanel.test.tsx -x</automated>
  </verify>
  <done>Unified panel live on brand route with selector and three tabs; page updated; PANEL-01/04/05 UI satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Session cookie; 403 must not leak profile data in UI |
| copy layer | UI must not override server `caveats` with false positives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-05 | Repudiation | calibration-status-copy | mitigate | Block marketing labels when `fixtureOnly`; test forbidden strings |
| T-165-06 | Information disclosure | OwnerCalibrationPanel | mitigate | Treat 403 as forbidden state; no retry with credentials |
| T-165-07 | Elevation | Page route | accept | API enforcement is primary; UI shows forbidden copy only |
</threat_model>

<verification>
```bash
cd app && npm test -- --run src/components/admin/OwnerCalibrationPanel.test.tsx src/components/admin/BrandTasteProfilePanel.test.tsx src/components/admin/BrandCalibrationRulesPanel.test.tsx src/components/admin/calibration-status-copy.test.ts -x
```
Manual spot-check: navigate to `/admin/quality/brands/{id}` as owner — combobox populated, tabs switch, Cenbrap shows fixture warning if applicable.
</verification>

<success_criteria>
- Owner selects brand from combobox without pasting UUIDs
- Profile/Rules/Voice visible in one panel
- Fixture-only brands never show "fully calibrated" marketing copy
- Non-owner sees restricted messaging
</success_criteria>

<output>
After completion, create `.planning/phases/165-owner-calibration-panel/165-02-SUMMARY.md`
</output>
