---
phase: 165-owner-calibration-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/app/api/admin/quality/brands/route.ts
  - app/src/app/api/admin/quality/brands/route.test.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.test.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/rules/route.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/rules/route.test.ts
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-05
user_setup: []

must_haves:
  truths:
    - "Platform owner can list all client profiles cross-workspace for brand selection"
    - "Platform owner can fetch taste profile JSON for a clientProfileId"
    - "Platform owner can fetch approved and candidate calibration rules for a clientProfileId"
    - "Non-owner receives 403 on all new brand calibration read APIs"
  artifacts:
    - path: "app/src/app/api/admin/quality/brands/route.ts"
      provides: "Owner-only GET brand list for combobox"
      exports: ["GET"]
    - path: "app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.ts"
      provides: "Taste profile via buildBrandTasteProfile"
      exports: ["GET"]
    - path: "app/src/app/api/admin/quality/brands/[clientProfileId]/rules/route.ts"
      provides: "Calibration rules grouped by status"
      exports: ["GET"]
  key_links:
    - from: "profile/route.ts"
      to: "buildBrandTasteProfile"
      via: "listCalibrationSignalsForClientProfile + buildBrandTasteProfile"
      pattern: "buildBrandTasteProfile"
    - from: "rules/route.ts"
      to: "listCalibrationRulesForClientProfile"
      via: "mapRuleRowToCandidate"
      pattern: "listCalibrationRulesForClientProfile"
    - from: "brands/route.ts"
      to: "client_profiles + workspaces"
      via: "owner-scoped join for id, name, workspaceName"
      pattern: "from\\(clientProfiles\\)"
---

<objective>
Add owner-only read APIs for cross-workspace brand listing, per-brand taste profile, and calibration rules.

Purpose: Backend contracts for Phase 165 panel — no taste-profile math in UI; reuse Phase 162–164 server services.
Output: Three GET routes with Vitest coverage mirroring `voice/route.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/165-owner-calibration-panel/165-RESEARCH.md

# Pattern source — mirror exactly
@app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.ts
@app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.test.ts

<interfaces>
From `@/server/brand-taste/taste-profile`:
```typescript
export function buildBrandTasteProfile(input: {
  clientProfileId: string;
  workspaceId: string;
  signals: CalibrationSignal[];
}): BrandTasteProfile;
```

From `@/server/repositories/calibration-signal`:
```typescript
export async function listCalibrationSignalsForClientProfile(input: {
  workspaceId: string;
  clientProfileId: string;
}): Promise<CalibrationSignal[]>;
```

From `@/server/brand-taste/calibration-rules`:
```typescript
export function mapRuleRowToCandidate(row: CalibrationRule): CalibrationRuleCandidate;
```

From `@/server/repositories/calibration-rule`:
```typescript
export async function listCalibrationRulesForClientProfile(input: {
  workspaceId: string;
  clientProfileId: string;
  status?: string;
}): Promise<CalibrationRule[]>;
```

`BrandTasteProfile` includes: `evidenceLevel`, `sourceComposition`, `caveats`, `positivePatterns`, `rejectionPatterns`, `quasePatterns`, `decisionCount`, `comparableCount`.
</interfaces>

# Open question resolution (RESEARCH)
- **Brand selector:** `GET /api/admin/quality/brands` list API (no index page required); combobox wired in Plan 165-02.
- **Taste profile sources:** `calibration_signals` only via `buildBrandTasteProfile` — corpus evals are out of scope for v13.2 profile payload.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Owner brand list API</name>
  <files>app/src/app/api/admin/quality/brands/route.ts, app/src/app/api/admin/quality/brands/route.test.ts</files>
  <behavior>
    - Test: platform owner GET returns 200 with `{ brands: [{ id, name, workspaceId, workspaceName }] }`
    - Test: non-owner GET returns 403
    - Test: empty DB returns `{ brands: [] }` (not error)
  </behavior>
  <action>
    Create `GET /api/admin/quality/brands` with `requirePlatformOwner(request)` first.

    Query `client_profiles` joined to `workspaces` for `workspaces.name` as `workspaceName`. Select `clientProfiles.id`, `clientProfiles.name`, `clientProfiles.workspaceId`. Order by `clientProfiles.name` asc.

    Return `{ brands: [...] }` — do **not** use workspace-scoped `/api/client-profiles` (owner is cross-workspace per RESEARCH pitfall #1).

    Mirror `voice/route.test.ts` mock pattern: mock `requirePlatformOwner`, mock `db.select` chain or repository if extracted.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/route.test.ts -x</automated>
  </verify>
  <done>List API returns all brands for owner; 403 for non-owner; tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Taste profile read API</name>
  <files>app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.ts, app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.test.ts</files>
  <behavior>
    - Test: owner + valid profile returns 200 with `evidenceLevel`, `sourceComposition`, `caveats`, pattern arrays
    - Test: non-owner returns 403
    - Test: unknown clientProfileId returns 404
    - Test: invalid UUID returns 400
  </behavior>
  <action>
    Create `GET .../profile/route.ts` following `voice/route.ts` structure:

    1. `requirePlatformOwner` → Zod UUID on `clientProfileId`
    2. Resolve `workspaceId` from `client_profiles` by PK (never trust client workspace)
    3. `listCalibrationSignalsForClientProfile({ workspaceId, clientProfileId })`
    4. `buildBrandTasteProfile({ clientProfileId, workspaceId, signals })`
    5. Return JSON profile as-is (signals-only composition per open question #2)

    Add computed helper fields for downstream PANEL-04 UI (server-side, no client import of `evaluateClaimsMatrix`):
    - `fixtureOnly: boolean` — `sourceComposition.real_customer === 0 && decisionCount > 0`
    - `corpusSignalsNote: string | null` — when `decisionCount === 0`, set PT-BR note that corpus evaluations may exist separately until signal bridge ships

    Tests mock `listCalibrationSignalsForClientProfile` and `buildBrandTasteProfile` or use fixture signals — mirror voice route 403/404/400 cases.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/\\[clientProfileId\\]/profile/route.test.ts -x</automated>
  </verify>
  <done>Profile API returns full taste profile + fixtureOnly flag; owner gate and validation tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Calibration rules read API</name>
  <files>app/src/app/api/admin/quality/brands/[clientProfileId]/rules/route.ts, app/src/app/api/admin/quality/brands/[clientProfileId]/rules/route.test.ts</files>
  <behavior>
    - Test: owner returns `{ approved: RuleCandidate[], candidate: RuleCandidate[] }` scoped to profile
    - Test: non-owner returns 403
    - Test: unknown profile returns 404
    - Test: approved and candidate arrays are disjoint by status
  </behavior>
  <action>
    Create `GET .../rules/route.ts` with same owner + profile resolution pattern.

    Call `listCalibrationRulesForClientProfile` twice (or once without status filter then partition in memory):
    - `status: "approved"` → map with `mapRuleRowToCandidate`
    - `status: "candidate"` → map with `mapRuleRowToCandidate`

    Response shape: `{ clientProfileId, workspaceId, approved, candidate }`. Exclude `rejected`/`deprecated` from panel display (read-only inspect).

    Per RESEARCH assumption A3: pending rules = `candidate` status rows; `proposed` learning proposals are separate (Plan 165-03).

    Add route tests mirroring voice route forbidden/not_found/validation cases.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/\\[clientProfileId\\]/rules/route.test.ts -x</automated>
  </verify>
  <done>Rules API returns approved + candidate groups; 403/404/400 covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted `clientProfileId` param; owner session required |
| API→DB | Queries must scope by server-resolved `(workspaceId, clientProfileId)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Spoofing | All new routes | mitigate | `requirePlatformOwner` before any DB access |
| T-165-02 | Information disclosure | profile/rules routes | mitigate | Resolve workspace from `client_profiles` PK; scope repository calls |
| T-165-03 | Tampering | Query params | mitigate | Zod UUID validation on `clientProfileId` |
| T-165-04 | Elevation | Non-owner access | mitigate | 403 via `WorkspaceAuthError`; route tests per endpoint |
</threat_model>

<verification>
```bash
cd app && npm test -- --run src/app/api/admin/quality/brands/
```
All new route tests pass; no regression on existing `voice/route.test.ts`.
</verification>

<success_criteria>
- Owner can fetch brand list, taste profile, and rules via HTTP
- Non-owner blocked on all three endpoints
- Profile payload uses `buildBrandTasteProfile` only (no duplicated thresholds)
</success_criteria>

<output>
After completion, create `.planning/phases/165-owner-calibration-panel/165-01-SUMMARY.md`
</output>
