---
phase: 165-owner-calibration-panel
plan: 03
type: execute
wave: 3
depends_on:
  - "165-02"
files_modified:
  - app/src/components/feedback/LearningProposalsTab.tsx
  - app/src/components/feedback/LearningProposalsTab.test.tsx
  - app/src/components/admin/OwnerCalibrationPanel.tsx
  - app/scripts/capture-ui-screenshots.ts
autonomous: true
requirements:
  - PANEL-03
  - PANEL-04
user_setup: []

must_haves:
  truths:
    - "Owner accepts/rejects learning proposals from Proposals tab in brand panel"
    - "Proposals list filtered to selected clientProfileId only"
    - "Fixture-only proposals require explicit acknowledgment before accept"
    - "Corpus Learning tab keeps workspace-wide proposals (no removal)"
  artifacts:
    - path: "app/src/components/feedback/LearningProposalsTab.tsx"
      provides: "clientProfileId filter + fixture ack on accept"
    - path: "app/src/components/feedback/LearningProposalsTab.test.tsx"
      provides: "Automated PANEL-03 coverage"
  key_links:
    - from: "LearningProposalsTab.tsx"
      to: "/api/admin/quality/learning/proposals"
      via: "clientProfileId query param"
      pattern: "clientProfileId"
    - from: "LearningProposalsTab accept"
      to: "accept/route.ts"
      via: "POST body acknowledgeFixtureOnly: true"
      pattern: "acknowledgeFixtureOnly"
    - from: "OwnerCalibrationPanel.tsx"
      to: "LearningProposalsTab"
      via: "Propostas tab with clientProfileId + workspaceId"
      pattern: "LearningProposalsTab"
---

<objective>
Wire per-brand learning proposals into the owner panel with `clientProfileId` filtering and fixture-only accept acknowledgment.

Purpose: Complete PANEL-03 in unified panel without duplicating accept/reject backend or removing corpus Learning tab.
Output: Proposals tab + hardened `LearningProposalsTab` + screenshot script label update.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-owner-calibration-panel/165-RESEARCH.md
@app/src/components/feedback/LearningProposalsTab.tsx
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/src/app/api/admin/quality/learning/proposals/route.ts
@app/src/app/api/admin/quality/learning/proposals/[id]/accept/route.ts

<interfaces>
Proposals list API (existing):
```
GET /api/admin/quality/learning/proposals?status=proposed&workspaceId=...&clientProfileId=...
```

Accept API body (existing):
```typescript
{ acknowledgeFixtureOnly?: boolean }
```

Proposal `evidenceRefs` from aggregate (extend client type):
```typescript
evidenceRefs: {
  corpusItemIds?: string[];
  stats: { count: number };
  fixtureOnly?: boolean;
};
```

`LearningProposalsTab` current props:
```typescript
{ workspaceId?: string; onOpenCalibration: () => void }
```

HumanQualityCorpusPanel usage must remain valid — `clientProfileId` prop optional.
</interfaces>

# Open question resolution (RESEARCH)
- **Keep corpus Learning tab:** Do not remove `LearningProposalsTab` from `HumanQualityCorpusPanel`; brand panel passes `clientProfileId` for filtered view only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LearningProposalsTab clientProfileId + fixture ack</name>
  <files>app/src/components/feedback/LearningProposalsTab.tsx, app/src/components/feedback/LearningProposalsTab.test.tsx</files>
  <behavior>
    - Test: when `clientProfileId` prop set, fetch URL includes `clientProfileId` query param
    - Test: queryKey includes clientProfileId for cache isolation
    - Test: proposal with `evidenceRefs.fixtureOnly === true` shows acknowledgment checkbox before Accept enabled
    - Test: accept POST includes `{ acknowledgeFixtureOnly: true }` when fixture proposal acknowledged
    - Test: without `clientProfileId`, behavior unchanged for corpus panel (workspace-only filter)
  </behavior>
  <action>
    Extend `LearningProposalsTab` props:
    ```typescript
    {
      workspaceId?: string;
      clientProfileId?: string;
      onOpenCalibration: () => void;
      variant?: "workspace" | "brand"; // optional — brand hides generate button if desired
    }
    ```

    **Fetch filter (PANEL-03):** In `fetchLearningProposals`, add `clientProfileId` to `URLSearchParams` when prop provided. Update `queryKey` to `["learning-proposals", workspaceId, clientProfileId]`.

    **Fixture ack (PANEL-03 ∩ PANEL-04):**
    - Extend `LearningProposal` type with `evidenceRefs.fixtureOnly?: boolean`
    - Per-row state: `fixtureAcknowledged[proposalId]` or inline checkbox "Reconheço que esta proposta usa apenas evidência de fixture"
    - Accept button disabled until checked when `fixtureOnly === true`
    - `acceptMutation` sends `JSON.stringify({ acknowledgeFixtureOnly: true })` with `content-type: application/json` when fixture-only (matches `accept/route.test.ts`)

    **Corpus tab preservation:** When `clientProfileId` undefined, keep existing workspace-wide behavior — no breaking change to `HumanQualityCorpusPanel`.

    For brand `variant`, optionally hide "Generate proposals" (workspace-wide op) — use `variant === "brand"` to show read-only header "Propostas de aprendizado desta marca" without generate button. Corpus tab keeps generate.

    Create `LearningProposalsTab.test.tsx` with mocked `apiFetch`.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/feedback/LearningProposalsTab.test.tsx -x</automated>
  </verify>
  <done>Proposals filtered by brand; fixture accept requires ack; corpus usage unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Proposals tab in OwnerCalibrationPanel</name>
  <files>app/src/components/admin/OwnerCalibrationPanel.tsx, app/src/components/admin/OwnerCalibrationPanel.test.tsx</files>
  <action>
    Add fourth tab to `OwnerCalibrationPanel`:
    - Tab id: `proposals`, label: "Propostas"
    - Content: `<LearningProposalsTab workspaceId={workspaceId} clientProfileId={clientProfileId} variant="brand" onOpenCalibration={() => setActiveTab("rules")} />`

    Resolve `workspaceId` from brand list query item matching current `clientProfileId`, or from profile API `workspaceId` if already fetched — prefer list query to avoid extra round-trip.

    Update `OwnerCalibrationPanel.test.tsx`:
    - Proposals tab renders when selected
    - Passes `clientProfileId` to `LearningProposalsTab` (mock child or check props via data-testid)

    Extend existing tests — do not remove Profile/Voice/Rules coverage.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/OwnerCalibrationPanel.test.tsx -x</automated>
  </verify>
  <done>Owner can accept/reject proposals from same panel as profile and rules</done>
</task>

<task type="auto">
  <name>Task 3: Screenshot script + wave verification</name>
  <files>app/scripts/capture-ui-screenshots.ts</files>
  <action>
    In `capture-ui-screenshots.ts`, rename capture entry from "Admin — Brand Voice Inspect" to "Admin — Owner Calibration Panel" (same route `/admin/quality/brands/${id}`).

    Run wave-scoped test suite to confirm no regressions across plans 01–03.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/ src/components/admin/ src/components/feedback/LearningProposalsTab.test.tsx -x</automated>
  </verify>
  <done>Screenshot label reflects unified panel; full phase quick-run green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accept POST | Operator must explicitly ack fixture-only before rule materialization |
| proposal filter | Wrong filter could accept another brand's proposal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-08 | Repudiation | fixture ack UI | mitigate | Checkbox required; POST `acknowledgeFixtureOnly: true`; component tests |
| T-165-09 | Tampering | wrong-brand accept | mitigate | Always pass `clientProfileId` in list query; test URL includes param |
| T-165-10 | Information disclosure | proposals tab | mitigate | Inherits owner 403 from proposals API |
</threat_model>

<verification>
```bash
cd app && npm test -- --run src/app/api/admin/quality/brands/ src/components/admin/ src/components/feedback/LearningProposalsTab.test.tsx
```
Confirm `HumanQualityCorpusPanel` still renders `LearningProposalsTab` without `clientProfileId` (smoke: existing corpus tests if any, or grep import unchanged).
</verification>

<success_criteria>
- PANEL-03: accept/reject from brand panel Proposals tab
- PANEL-04: fixture-only accept requires operator acknowledgment
- Corpus Learning tab unchanged for workspace-wide ops
</success_criteria>

<output>
After completion, create `.planning/phases/165-owner-calibration-panel/165-03-SUMMARY.md`
</output>
