---
phase: 207-iterative-copilot-integration-and-uat
plan: "04"
type: execute
wave: 3
depends_on: ["207-02", "207-03"]
files_modified:
  - app/tests/e2e/iterative-copilot-loop.desktop.spec.ts
  - app/tests/e2e/iterative-copilot-loop.mobile.spec.ts
  - app/tests/e2e/support/iterative-copilot-mocks.ts
  - app/playwright.guided.config.ts
autonomous: true
requirements: [QA-02]
must_haves:
  truths:
    - "D-08: Playwright uses guided-auth API sign-in and route mocks — no live LLM/credits/Inngest spend."
    - "D-09: Desktop and mobile specs cover plan revision → confirm → creative revision → compare → promote → reload → stale card."
    - "D-10: Comparison dialog and version history expose accessible landmarks (role=dialog, aria-labelledby)."
    - "D-13: User-facing assertions use Portuguese copy; test identifiers remain English."
  artifacts:
    - path: "app/tests/e2e/iterative-copilot-loop.desktop.spec.ts"
      provides: "Desktop viewport iteration loop E2E"
    - path: "app/tests/e2e/iterative-copilot-loop.mobile.spec.ts"
      provides: "Mobile viewport iteration loop E2E"
  key_links:
    - from: "app/tests/e2e/iterative-copilot-loop.desktop.spec.ts"
      to: "app/tests/e2e/support/guided-auth.ts"
      via: "loginGuidedJourney + mockClientProfiles in beforeEach"
      pattern: "loginGuidedJourney"
    - from: "app/tests/e2e/support/iterative-copilot-mocks.ts"
      to: "app/api/assistant/threads/**"
      via: "page.route mocks for plan-revisions, creative-revisions, compare, promote"
      pattern: "page.route"
---

<objective>
Add authenticated Playwright coverage for the full plan→creative iteration loop on desktop and mobile viewports (D-08–D-10).

Purpose: Close the explicit Phase 206 browser deferral; prove end-to-end UX with mocked providers.
Output: Two E2E spec files + shared mock helper; guided config includes iteration specs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-iterative-copilot-integration-and-uat/207-CONTEXT.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-RESEARCH.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-PATTERNS.md
@app/tests/e2e/guided-assistant-journeys.spec.ts
@app/tests/e2e/support/guided-auth.ts
@app/playwright.guided.config.ts

Journey under test (D-09):
1. Open assistant thread with artifact version state
2. Submit plan revision feedback → review proposal → confirm
3. Submit creative revision → confirm generation (mocked)
4. Open version comparison dialog → acknowledge → promote
5. Reload page → assert promoted version is current
6. Simulate concurrent head change → assert stale action card (mocked API response)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared iteration mock helpers</name>
  <files>app/tests/e2e/support/iterative-copilot-mocks.ts, app/playwright.guided.config.ts</files>
  <read_first>
    - app/tests/e2e/guided-assistant-journeys.spec.ts
    - app/tests/e2e/support/guided-auth.ts
    - app/playwright.guided.config.ts
    - app/src/lib/assistant/artifact-version.ts
  </read_first>
  <action>
    Create `iterative-copilot-mocks.ts` exporting helpers: `mockIterationThread(page, state)`, `mockPlanRevisionFlow(page)`, `mockCreativeRevisionFlow(page)`, `mockComparePromoteFlow(page)`, `mockStaleHeadChange(page)`. Use `page.route` for `/api/assistant/threads/**`, `plan-revisions`, `creative-revisions`, `artifact-versions/compare`, `artifact-versions/promote`, `comparison-acknowledgements`, `actions/*/confirm`, credits, and Inngest — return deterministic JSON matching production DTO shapes (no signed URLs in fixtures per SAFE-03). Update `playwright.guided.config.ts` `testMatch` to include `**/iterative-copilot-loop.*.spec.ts` alongside existing guided specs.
  </action>
  <verify>
    <automated>cd app && npx playwright test --config playwright.guided.config.ts --list 2>&1 | grep -q iterative-copilot-loop</automated>
  </verify>
  <acceptance_criteria>
    - `iterative-copilot-mocks.ts` exports at least `mockIterationThread`
    - `playwright.guided.config.ts` lists iterative-copilot-loop specs in `--list` output
    - Mock fixtures omit `signedUrl`, `prompt`, `reasoning` fields
  </acceptance_criteria>
  <done>Shared mocks ready; guided config discovers iteration specs.</done>
</task>

<task type="auto">
  <name>Task 2: Desktop iteration loop spec</name>
  <files>app/tests/e2e/iterative-copilot-loop.desktop.spec.ts</files>
  <read_first>
    - app/tests/e2e/support/iterative-copilot-mocks.ts
    - app/tests/e2e/guided-assistant-journeys.spec.ts
    - app/src/components/assistant/VersionComparisonDialog.tsx
    - app/src/components/assistant/VersionHistory.tsx
  </read_first>
  <action>
    Create desktop spec per D-09: `beforeEach` calls `mockClientProfiles`, `loginGuidedJourney`, and iteration mocks. Test flow: navigate to assistant thread → plan feedback (Portuguese placeholder text) → confirm plan revision card → creative feedback → confirm generation card → open version history → select two versions → open comparison dialog → assert `getByRole("dialog")` visible (D-10) → promote → `page.reload()` → assert current version label in Portuguese → trigger stale head mock → assert stale card copy. Use desktop project from guided config. No calls to live OpenAI or Inngest endpoints.
  </action>
  <verify>
    <automated>cd app && npx playwright test --config playwright.guided.config.ts --project=desktop tests/e2e/iterative-copilot-loop.desktop.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec file exists and runs under desktop project
    - Test includes `page.reload()` step after promote
    - `getByRole("dialog")` assertion present for comparison
    - At least one assertion uses Portuguese string (e.g. plan/creative confirm label)
  </acceptance_criteria>
  <done>Desktop E2E green locally with mocks.</done>
</task>

<task type="auto">
  <name>Task 3: Mobile iteration loop spec</name>
  <files>app/tests/e2e/iterative-copilot-loop.mobile.spec.ts</files>
  <read_first>
    - app/tests/e2e/iterative-copilot-loop.desktop.spec.ts
    - app/tests/e2e/support/iterative-copilot-mocks.ts
    - app/playwright.guided.config.ts
  </read_first>
  <action>
    Create mobile spec per D-09 reusing `iterative-copilot-mocks.ts` helpers. Run under mobile project (viewport from guided config). Cover same journey as desktop with mobile-appropriate selectors: version history `data-testid="version-history"` and `aria-labelledby="version-history-title"` assertion (D-10). Assert stacked comparison layout visible on narrow viewport. Reuse Portuguese copy assertions. Include stale card step with mocked concurrent head change.
  </action>
  <verify>
    <automated>cd app && npx playwright test --config playwright.guided.config.ts --project=mobile tests/e2e/iterative-copilot-loop.mobile.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mobile spec runs under mobile project
    - Version history a11y: `getByTestId("version-history")` or `#version-history-title` referenced
    - Stale card assertion present after head change mock
  </acceptance_criteria>
  <done>Mobile E2E green; D-09 satisfied for both viewports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E mocks → API | Fixtures must not include real credentials or signed URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-07 | Information disclosure | iterative-copilot-mocks.ts | mitigate | Omit signed URLs and prompts from mock JSON |
| T-207-08 | Spoofing | guided-auth sign-in | transfer | Reuse proven API sign-in from Phase 201 |
</threat_model>

<verification>
- `cd app && npx playwright test --config playwright.guided.config.ts tests/e2e/iterative-copilot-loop`
</verification>

<success_criteria>
- Desktop and mobile Playwright specs pass with route mocks only
- a11y landmarks asserted on comparison dialog and version history
- Full D-09 journey automated without provider spend
</success_criteria>

<output>
After completion, create `.planning/phases/207-iterative-copilot-integration-and-uat/207-04-SUMMARY.md`
</output>
