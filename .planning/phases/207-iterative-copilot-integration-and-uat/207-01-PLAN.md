---
phase: 207-iterative-copilot-integration-and-uat
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0068_assistant_artifact_iteration_events.sql
  - app/drizzle/meta/_journal.json
  - app/src/server/db/schema.ts
  - app/src/server/assistant/artifact-iteration-telemetry.ts
  - app/src/server/assistant/artifact-iteration-telemetry.test.ts
  - app/src/server/repositories/artifact-iteration-telemetry.ts
  - app/src/server/repositories/artifact-iteration-telemetry.test.ts
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "D-01: Artifact iteration telemetry uses a dedicated table and emitter, not guided-flow events."
    - "D-02: All twelve locked event keys are defined and validated at insert time."
    - "D-03: Metadata rejects denied persistence keys and non-allowlisted fields."
    - "Operator can persist scoped telemetry rows without prompts, URLs, or snapshots."
  artifacts:
    - path: "app/drizzle/0068_assistant_artifact_iteration_events.sql"
      provides: "Append-only assistant_artifact_iteration_events table"
    - path: "app/src/server/assistant/artifact-iteration-telemetry.ts"
      provides: "Sanitizer, event keys, fire-and-forget emitArtifactIterationTelemetry"
      exports: ["ARTIFACT_ITERATION_EVENT_KEYS", "emitArtifactIterationTelemetry", "sanitizeArtifactIterationMetadata"]
    - path: "app/src/server/repositories/artifact-iteration-telemetry.ts"
      provides: "insertArtifactIterationTelemetryEvent, listArtifactIterationEventsForOwner"
  key_links:
    - from: "app/src/server/assistant/artifact-iteration-telemetry.ts"
      to: "app/src/server/repositories/artifact-iteration-telemetry.ts"
      via: "recordArtifactIterationTelemetryEvent after sanitize"
      pattern: "insertArtifactIterationTelemetryEvent"
    - from: "app/src/server/assistant/artifact-iteration-telemetry.ts"
      to: "app/src/server/repositories/assistant-types.ts"
      via: "containsDeniedPersistenceKeys denylist"
      pattern: "containsDeniedPersistenceKeys"
---

<objective>
Create the artifact-iteration telemetry foundation: migration, schema, sanitizer, repository, and unit tests mirroring Phase 190 guided-flow telemetry.

Purpose: QA-01 requires a safe, queryable event stream before service-boundary wiring (D-01, D-03).
Output: `assistant_artifact_iteration_events` table, emitter module, repository with owner list, passing unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-CONTEXT.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-RESEARCH.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-PATTERNS.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-VALIDATION.md

<interfaces>
From `app/src/server/assistant/guided-flow-telemetry.ts` (mirror pattern):
```typescript
export const GUIDED_FLOW_EVENT_KEYS = [...] as const;
export function emitGuidedFlowTelemetry(input): void { void recordGuidedFlowTelemetryEvent(input); }
export function sanitizeGuidedFlowMetadata(input: unknown): GuidedFlowMetadata;
```

From `app/drizzle/0059_assistant_guided_flow_events.sql` — table shape: scope columns, event_key, metadata jsonb, occurred_at, indexes on workspace + occurred_at.

D-02 locked keys: `proposal_created`, `proposal_confirmed`, `generation_enqueued`, `generation_succeeded`, `generation_failed`, `comparison_opened`, `comparison_acknowledged`, `promotion_requested`, `promotion_succeeded`, `promotion_conflict`, `retry_requested`, `proposal_staled`.

D-03 allowlisted metadata scalars: workspaceId, clientProfileId, campaignId, threadId, artifactType, lineageId, versionNumber, proposalId, actionId, operationId, reasonCode, idempotent, replayed, staleReason, conflictReason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + schema for artifact iteration events</name>
  <files>app/drizzle/0068_assistant_artifact_iteration_events.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts</files>
  <read_first>
    - app/drizzle/0059_assistant_guided_flow_events.sql
    - app/drizzle/0067_assistant_artifact_approvals.sql
    - app/drizzle/meta/_journal.json
    - app/src/server/db/schema.ts
  </read_first>
  <behavior>
    - Table `assistant_artifact_iteration_events` exists with workspace_id, client_profile_id, campaign_id, thread_id, event_key, metadata jsonb, occurred_at
    - Indexes on (workspace_id, occurred_at) and (thread_id, occurred_at)
    - Drizzle schema exports table for repository use
  </behavior>
  <action>
    Create migration `0068_assistant_artifact_iteration_events.sql` per D-01 using `assistant_artifact_iteration_events` table name (RESEARCH A2). Mirror guided-flow scope denormalization columns. Register in `_journal.json` and add Drizzle table definition in `schema.ts` matching migration columns. Metadata column stores JSON object only — no raw text columns for prompts.
  </action>
  <verify>
    <automated>cd app && npm run test:db:setup && npm test -- --run src/server/repositories/artifact-iteration-telemetry.test.ts -t "inserts scoped event" 2>/dev/null || test -f drizzle/0068_assistant_artifact_iteration_events.sql</automated>
  </verify>
  <acceptance_criteria>
    - `app/drizzle/0068_assistant_artifact_iteration_events.sql` exists
    - `schema.ts` contains `assistantArtifactIterationEvents` (or equivalent) table export
    - `_journal.json` references migration 0068
  </acceptance_criteria>
  <done>Migration and Drizzle schema committed; table name is `assistant_artifact_iteration_events`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Telemetry sanitizer + emitter module</name>
  <files>app/src/server/assistant/artifact-iteration-telemetry.ts, app/src/server/assistant/artifact-iteration-telemetry.test.ts</files>
  <read_first>
    - app/src/server/assistant/guided-flow-telemetry.ts
    - app/src/server/assistant/guided-flow-telemetry.test.ts
    - app/src/server/repositories/assistant-types.ts
  </read_first>
  <behavior>
    - All D-02 event keys accepted; unknown keys rejected
    - Metadata with `snapshot`, `previewUrl`, `prompt`, `signedUrl` throws sanitization error
    - `emitArtifactIterationTelemetry` is fire-and-forget (void promise, logs warn on failure)
    - Allowlisted scalars pass strict zod parse
  </behavior>
  <action>
    Implement `artifact-iteration-telemetry.ts` mirroring `guided-flow-telemetry.ts` per D-01–D-03. Export `ARTIFACT_ITERATION_EVENT_KEYS` const array with exactly the twelve D-02 keys. Use `containsDeniedPersistenceKeys` before zod `strictObject` on metadata. Implement `emitArtifactIterationTelemetry` as synchronous wrapper calling async `recordArtifactIterationTelemetryEvent` with swallowed errors + structured `logger.warn`. Co-locate tests asserting denied keys, unknown event keys, and valid minimal metadata.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/artifact-iteration-telemetry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ARTIFACT_ITERATION_EVENT_KEYS` length is 12 and includes `promotion_conflict`
    - Test rejects metadata containing `snapshot` key
    - `emitArtifactIterationTelemetry` exported
  </acceptance_criteria>
  <done>Sanitizer unit tests green; emitter module ready for service wiring in 207-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repository insert + owner list</name>
  <files>app/src/server/repositories/artifact-iteration-telemetry.ts, app/src/server/repositories/artifact-iteration-telemetry.test.ts</files>
  <read_first>
    - app/src/server/repositories/guided-flow-telemetry.ts
    - app/src/server/repositories/guided-flow-telemetry.test.ts
    - app/src/server/assistant/artifact-iteration-telemetry.ts
  </read_first>
  <behavior>
    - Insert requires matching workspace/thread scope on input
    - `listArtifactIterationEventsForOwner` filters by workspaceId and occurredAt range
    - Cross-workspace insert with mismatched scope rejected
  </behavior>
  <action>
    Create repository with `insertArtifactIterationTelemetryEvent` and `listArtifactIterationEventsForOwner(filters)` mirroring guided-flow-telemetry repository. Assert scope dimensions on insert (four-dimensional isolation per SAFE-01). Owner list accepts `OwnerAnalyticsQuery` filters from `parseOwnerAnalyticsQuery` pattern. Tests use `test:db:setup` fixtures; include scoped insert + owner filter + denied metadata rejection at repository boundary.
  </action>
  <verify>
    <automated>cd app && npm run test:db:setup && npm test -- --run src/server/repositories/artifact-iteration-telemetry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `insertArtifactIterationTelemetryEvent` and `listArtifactIterationEventsForOwner` exported
    - Repository test inserts row and lists it with workspace filter
    - Scope mismatch insert throws or rejects
  </acceptance_criteria>
  <done>Repository tests green; 207-02 can wire emits and owner route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → telemetry repository | Untrusted metadata must be sanitized before insert |
| Operator → owner list API | Only platform owners may query cross-workspace events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-01 | Information disclosure | artifact-iteration-telemetry.ts | mitigate | D-03 allowlist + `containsDeniedPersistenceKeys` before insert |
| T-207-02 | Elevation of privilege | artifact-iteration-telemetry repository | mitigate | Assert workspace/thread scope on insert matching artifact scope |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/assistant/artifact-iteration-telemetry.test.ts src/server/repositories/artifact-iteration-telemetry.test.ts`
</verification>

<success_criteria>
- Dedicated telemetry table exists separate from `assistant_guided_flow_events`
- All twelve D-02 event keys defined in code
- Unit tests prove metadata sanitization rejects unsafe keys
</success_criteria>

<output>
After completion, create `.planning/phases/207-iterative-copilot-integration-and-uat/207-01-SUMMARY.md`
</output>
