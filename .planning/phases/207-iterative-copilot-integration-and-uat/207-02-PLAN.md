---
phase: 207-iterative-copilot-integration-and-uat
plan: "02"
type: execute
wave: 2
depends_on: ["207-01"]
files_modified:
  - app/src/server/assistant/plan-iteration/proposal.ts
  - app/src/server/assistant/creative-iteration/proposal.ts
  - app/src/server/assistant/action-execution/handlers/revise-creative.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/assistant/artifact-version/comparison.ts
  - app/src/server/assistant/artifact-version/promotion.ts
  - app/src/server/repositories/artifact-version.ts
  - app/src/server/assistant/creative-iteration/service.ts
  - app/src/app/api/assistant/threads/[threadId]/artifact-versions/promote/route.ts
  - app/src/app/api/feedback/analytics/artifact-iteration/route.ts
  - app/src/app/api/feedback/analytics/artifact-iteration/route.test.ts
  - app/src/server/assistant/plan-iteration/proposal.test.ts
  - app/src/server/assistant/creative-iteration/proposal.test.ts
  - app/src/server/jobs/derivation.test.ts
  - app/src/server/assistant/artifact-version/promotion.test.ts
  - app/src/server/assistant/artifact-version/comparison.test.ts
autonomous: true
requirements: [QA-01]
must_haves:
  truths:
    - "D-04: Every D-02 event emits from a server service boundary after the owning mutation succeeds."
    - "D-05: Platform owner can query artifact-iteration events with workspace/time filters."
    - "Idempotent replays do not emit duplicate success events when replayed=true."
    - "Failure paths emit generation_failed and promotion_conflict without leaking unsafe payloads."
  artifacts:
    - path: "app/src/app/api/feedback/analytics/artifact-iteration/route.ts"
      provides: "Owner read-only telemetry query"
      exports: ["GET"]
  key_links:
    - from: "app/src/server/assistant/plan-iteration/proposal.ts"
      to: "app/src/server/assistant/artifact-iteration-telemetry.ts"
      via: "emit after proposePlanRevision / confirmPlanRevision"
      pattern: "emitArtifactIterationTelemetry"
    - from: "app/src/app/api/feedback/analytics/artifact-iteration/route.ts"
      to: "app/src/server/repositories/artifact-iteration-telemetry.ts"
      via: "listArtifactIterationEventsForOwner"
      pattern: "listArtifactIterationEventsForOwner"
---

<objective>
Wire artifact-iteration telemetry at all service boundaries (D-04) and expose the owner query route (D-05).

Purpose: Complete QA-01 — operators can observe the full iteration funnel without UI-only hooks.
Output: Emits in proposal/confirm/derivation/compare/promote/stale paths; owner GET route with tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-iterative-copilot-integration-and-uat/207-01-SUMMARY.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-CONTEXT.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-RESEARCH.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-PATTERNS.md

<interfaces>
Emit map from RESEARCH Pattern 2:
| Event | Location |
| proposal_created | plan-iteration/proposal.ts, creative-iteration/proposal.ts |
| proposal_confirmed | same confirm functions |
| generation_enqueued | revise-creative.ts after inngest.send |
| generation_succeeded/failed | jobs/derivation.ts |
| retry_requested | revise-creative.ts retry path |
| comparison_opened | artifact-version/comparison.ts |
| comparison_acknowledged | artifact-version/promotion.ts |
| promotion_requested/succeeded/conflict | promotion.ts + promote route |
| proposal_staled | artifact-version.ts staleSiblingProposals; creative-iteration/service.ts markCreativeProposalsStaleOnPlanChange |

From `app/src/app/api/feedback/analytics/guided-flow-funnel/route.ts`:
```typescript
export async function GET(request: Request) {
  await requirePlatformOwner(request);
  const filters = parseOwnerAnalyticsQuery(new URL(request.url).searchParams);
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire proposal and generation emits</name>
  <files>app/src/server/assistant/plan-iteration/proposal.ts, app/src/server/assistant/creative-iteration/proposal.ts, app/src/server/assistant/action-execution/handlers/revise-creative.ts, app/src/server/jobs/derivation.ts, app/src/server/assistant/plan-iteration/proposal.test.ts, app/src/server/assistant/creative-iteration/proposal.test.ts, app/src/server/jobs/derivation.test.ts</files>
  <read_first>
    - app/src/server/assistant/artifact-iteration-telemetry.ts
    - app/src/server/assistant/plan-iteration/proposal.ts
    - app/src/server/assistant/creative-iteration/proposal.ts
    - app/src/server/assistant/action-execution/handlers/revise-creative.ts
    - app/src/server/jobs/derivation.ts
    - app/src/server/assistant/guided-flow-telemetry-lifecycle.test.ts
  </read_first>
  <action>
    Per D-04, add `emitArtifactIterationTelemetry` calls AFTER successful mutations in: `proposePlanRevision` → `proposal_created`; `confirmPlanRevision` → `proposal_confirmed` (include `idempotent` metadata from service result); `proposeCreativeRevision` → `proposal_created`; `confirmCreativeRevision` → `proposal_confirmed`; `revise-creative.ts` after `inngest.send` → `generation_enqueued`; retry idempotency path → `retry_requested`; `derivation.ts` on creative_revision success → `generation_succeeded`; `onFailure` for creative_revision → `generation_failed`. Metadata uses D-03 allowlist only (artifactType, lineageId, versionNumber, proposalId, actionId, operationId, reasonCode). Mock emitter in tests via `vi.mock("../artifact-iteration-telemetry")` pattern from guided-flow-lifecycle tests. Extend proposal.test.ts and derivation.test.ts with `describe("telemetry")` blocks asserting correct event_key after happy path.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/proposal.test.ts src/server/assistant/creative-iteration/proposal.test.ts src/server/jobs/derivation.test.ts -t telemetry</automated>
  </verify>
  <acceptance_criteria>
    - `proposal.test.ts` asserts `proposal_created` emit on propose
    - `derivation.test.ts` asserts `generation_succeeded` on creative_revision completion
    - No emit calls in React components or hooks
  </acceptance_criteria>
  <done>Proposal and generation boundary emits wired with integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire comparison, promotion, and stale emits</name>
  <files>app/src/server/assistant/artifact-version/comparison.ts, app/src/server/assistant/artifact-version/promotion.ts, app/src/server/repositories/artifact-version.ts, app/src/server/assistant/creative-iteration/service.ts, app/src/app/api/assistant/threads/[threadId]/artifact-versions/promote/route.ts, app/src/server/assistant/artifact-version/promotion.test.ts, app/src/server/assistant/artifact-version/comparison.test.ts</files>
  <read_first>
    - app/src/server/assistant/artifact-version/comparison.ts
    - app/src/server/assistant/artifact-version/promotion.ts
    - app/src/server/repositories/artifact-version.ts
    - app/src/server/assistant/creative-iteration/service.ts
    - app/src/app/api/assistant/threads/[threadId]/artifact-versions/promote/route.ts
  </read_first>
  <action>
    Emit `comparison_opened` after `compareArtifactVersions` builds DTO. Emit `comparison_acknowledged` after `acknowledgeLinkedPlanComparison` persists. Emit `promotion_requested` at promotion entry; `promotion_succeeded` only when transaction commits with `replayed: false`; `promotion_conflict` on `ArtifactHeadConflictError` / 409 conflict DTO in promote route. Emit `proposal_staled` from `staleSiblingProposals` and `markCreativeProposalsStaleOnPlanChange` with `staleReason` metadata. Per RESEARCH A1: skip `promotion_succeeded` / `proposal_confirmed` when service returns `replayed: true`. Extend promotion.test.ts and comparison.test.ts with telemetry assertions.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/artifact-version/promotion.test.ts src/server/assistant/artifact-version/comparison.test.ts -t telemetry</automated>
  </verify>
  <acceptance_criteria>
    - `promotion.test.ts` asserts no `promotion_succeeded` when `replayed: true`
    - `comparison.test.ts` asserts `comparison_opened` after successful compare
    - `proposal_staled` emit present in staleSiblingProposals path
  </acceptance_criteria>
  <done>Compare/promote/stale emits complete with replay-safe behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Owner analytics route</name>
  <files>app/src/app/api/feedback/analytics/artifact-iteration/route.ts, app/src/app/api/feedback/analytics/artifact-iteration/route.test.ts</files>
  <read_first>
    - app/src/app/api/feedback/analytics/guided-flow-funnel/route.ts
    - app/src/app/api/feedback/analytics/guided-flow-funnel/route.test.ts
    - app/src/server/repositories/artifact-iteration-telemetry.ts
    - app/src/server/assistant/beta-analytics/query.ts
  </read_first>
  <behavior>
    - Non-owner GET returns 403
    - Owner GET returns filtered events array
    - workspaceId and date range query params parsed via parseOwnerAnalyticsQuery
  </behavior>
  <action>
    Create `GET /api/feedback/analytics/artifact-iteration` per D-05 mirroring guided-flow-funnel: `requirePlatformOwner`, `parseOwnerAnalyticsQuery`, `listArtifactIterationEventsForOwner`, JSON `{ filters, events }`. Route test: 403 without owner; 200 with owner session; events filtered by workspaceId query param. Do not expose funnel summary in v1 unless trivial — raw list satisfies D-05.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/feedback/analytics/artifact-iteration/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Route file exports async GET
    - route.test.ts asserts 403 for non-owner
    - route.test.ts asserts 200 and events array for owner with workspace filter
  </acceptance_criteria>
  <done>QA-01 operator query route live with auth tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant → owner analytics route | Must not leak cross-tenant telemetry |
| Service emit → DB | Metadata must stay allowlisted at every boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-03 | Information disclosure | Service emit metadata | mitigate | Emit only after sanitize; never pass version DTOs directly |
| T-207-04 | Information disclosure | artifact-iteration route | mitigate | `requirePlatformOwner` + workspace filter on list |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/assistant/plan-iteration/proposal.test.ts src/server/assistant/creative-iteration/proposal.test.ts src/server/assistant/artifact-version/promotion.test.ts src/app/api/feedback/analytics/artifact-iteration/route.test.ts`
</verification>

<success_criteria>
- All twelve D-02 event keys have at least one emit path in production code
- Owner route returns scoped events for platform owner only
- Replay paths do not double-count success events
</success_criteria>

<output>
After completion, create `.planning/phases/207-iterative-copilot-integration-and-uat/207-02-SUMMARY.md`
</output>
