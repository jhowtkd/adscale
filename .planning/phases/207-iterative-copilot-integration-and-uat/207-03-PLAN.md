---
phase: 207-iterative-copilot-integration-and-uat
plan: "03"
type: execute
wave: 2
depends_on: ["207-01"]
files_modified:
  - app/src/server/assistant/plan-iteration/service.test.ts
  - app/src/server/assistant/creative-iteration/service.test.ts
  - app/src/server/repositories/artifact-version.test.ts
  - app/src/server/assistant/artifact-version/service.test.ts
  - app/src/components/assistant/VersionHistory.test.tsx
  - app/src/components/assistant/AssistantActionCard.test.tsx
  - app/src/lib/hooks/use-assistant-threads.test.tsx
autonomous: true
requirements: [QA-02]
must_haves:
  truths:
    - "D-06: Nyquist gaps from 203–206 covered — cross-thread isolation, reload projection, idempotent replay, first-approval null head, compound rollback."
    - "D-07: Component tests prove reload after promotion and stale card after head change."
    - "D-14: Tests do not bypass scope checks or weaken SAFE invariants."
    - "Existing repository tests for replay mismatch and compound rollback remain green with telemetry cross-assertions where applicable."
  artifacts:
    - path: "app/src/components/assistant/VersionHistory.test.tsx"
      provides: "Reload-after-promotion UI assertion"
    - path: "app/src/components/assistant/AssistantActionCard.test.tsx"
      provides: "Stale card after concurrent head change"
  key_links:
    - from: "app/src/components/assistant/VersionHistory.test.tsx"
      to: "app/src/lib/hooks/use-assistant-threads.test.tsx"
      via: "artifactVersionState projection after promote"
      pattern: "artifactVersionState"
---

<objective>
Close Nyquist unit/integration/component gaps (D-06, D-07) by extending existing Vitest files — no new harnesses.

Purpose: QA-02 automated coverage for isolation, reload, stale UI, and inherited 203–206 scenarios deferred from Phase 206.
Output: Extended tests green; VALIDATION map rows 207-03-01 through 207-03-03 satisfied.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-iterative-copilot-integration-and-uat/207-CONTEXT.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-RESEARCH.md
@.planning/phases/207-iterative-copilot-integration-and-uat/207-VALIDATION.md
@.planning/phases/206-version-compare-and-approval/206-VERIFICATION.md

Nyquist gap inventory (RESEARCH): cross-thread isolation, reload projection, idempotent replay mismatch, first-approval null head, compound promotion rollback already exist at repository layer — extend service + component layers. Phase 206 deferred reload-after-promotion and stale-card to human — automate here per D-07.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service-layer cross-thread and confirm-scope tests</name>
  <files>app/src/server/assistant/plan-iteration/service.test.ts, app/src/server/assistant/creative-iteration/service.test.ts</files>
  <read_first>
    - app/src/server/assistant/plan-iteration/service.test.ts
    - app/src/server/assistant/creative-iteration/service.test.ts
    - app/src/server/repositories/artifact-version.test.ts
    - app/src/server/assistant/plan-iteration/draft.test.ts
  </read_first>
  <action>
    Per D-06, extend `plan-iteration/service.test.ts` and `creative-iteration/service.test.ts` with cases rejecting confirm/propose when `threadId` in command does not match artifact scope (cross-thread isolation). Use existing scoped fixtures — do not disable scope asserts (D-14). Name tests with `cross-thread` for VALIDATION map filter. Re-run existing `artifact-version.test.ts` replay and rollback tests unchanged; add optional telemetry mock assertion in promotion path if 207-02 merged (assert `promotion_conflict` on rollback scenario).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/service.test.ts src/server/assistant/creative-iteration/service.test.ts src/server/repositories/artifact-version.test.ts -t "cross-thread|rejects replay|rolls back|first official"</automated>
  </verify>
  <acceptance_criteria>
    - service.test.ts files contain `cross-thread` named tests that expect rejection
    - `artifact-version.test.ts` tests for replay mismatch and compound rollback still pass
    - No `skip` or `only` modifiers left in committed tests
  </acceptance_criteria>
  <done>Service and repository Nyquist scenarios green.</done>
</task>

<task type="auto">
  <name>Task 2: Reload projection after promotion (component)</name>
  <files>app/src/components/assistant/VersionHistory.test.tsx, app/src/lib/hooks/use-assistant-threads.test.tsx, app/src/server/assistant/artifact-version/service.test.ts</files>
  <read_first>
    - app/src/components/assistant/VersionHistory.test.tsx
    - app/src/components/assistant/VersionHistory.tsx
    - app/src/lib/hooks/use-assistant-threads.test.tsx
    - app/src/server/assistant/artifact-version/service.test.ts
  </read_first>
  <action>
    Per D-07, add `VersionHistory.test.tsx` case named `reload` simulating post-promotion `artifactVersionState`: working head updates, previously-official label appears on promoted version, Portuguese copy for current version label (D-13). Extend or reference `use-assistant-threads.test.tsx` parsing of nested `artifactVersionState` dates if needed. Verify `service.test.ts` reload projection test still passes — do not duplicate repository-level coverage.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/assistant/VersionHistory.test.tsx src/lib/hooks/use-assistant-threads.test.tsx src/server/assistant/artifact-version/service.test.ts -t "reload|artifactVersionState"</automated>
  </verify>
  <acceptance_criteria>
    - VersionHistory.test.tsx has test matching `/reload/i`
    - Test asserts updated current version label after promotion state change
    - Portuguese user-facing strings in assertions (e.g. "Versão atual" or project equivalent)
  </acceptance_criteria>
  <done>Reload-after-promotion component coverage satisfies D-07.</done>
</task>

<task type="auto">
  <name>Task 3: Stale action card after head change</name>
  <files>app/src/components/assistant/AssistantActionCard.test.tsx</files>
  <read_first>
    - app/src/components/assistant/AssistantActionCard.test.tsx
    - app/src/components/assistant/AssistantActionCard.tsx
    - app/src/server/assistant/creative-iteration/service.test.ts
  </read_first>
  <action>
    Per D-07, add `AssistantActionCard.test.tsx` case named `stale` rendering stale proposal card when `artifactVersionState` head changed since proposal creation (mock props reflecting `markCreativeProposalsStaleOnPlanChange` outcome). Assert Portuguese stale messaging and disabled confirm CTA. Do not weaken SAFE-02 idempotency checks in mocks. Follow existing action card test patterns from Phase 205/206.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/assistant/AssistantActionCard.test.tsx -t stale</automated>
  </verify>
  <acceptance_criteria>
    - AssistantActionCard.test.tsx contains `stale` named test
    - Stale card shows user-visible stale reason in Portuguese
    - Confirm/generate button disabled or absent on stale card
  </acceptance_criteria>
  <done>Stale card component test closes Phase 206 human deferral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → production scope rules | Tests must use realistic scope, not bypass auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-207-05 | Elevation of privilege | service.test.ts fixtures | mitigate | Cross-thread tests assert rejection, not success |
| T-207-06 | Tampering | Component stale state | accept | jsdom-only; E2E covered in 207-04 |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/assistant/plan-iteration/service.test.ts src/server/assistant/creative-iteration/service.test.ts src/components/assistant/VersionHistory.test.tsx src/components/assistant/AssistantActionCard.test.tsx`
</verification>

<success_criteria>
- All D-06 Nyquist scenarios have automated coverage in extended files
- D-07 component scenarios for reload and stale card pass
- No new test harness directories created
</success_criteria>

<output>
After completion, create `.planning/phases/207-iterative-copilot-integration-and-uat/207-03-SUMMARY.md`
</output>
