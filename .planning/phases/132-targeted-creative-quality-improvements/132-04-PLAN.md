---
phase: 132-targeted-creative-quality-improvements
plan: "04"
type: execute
wave: 4
depends_on: ["132-02", "132-03"]
files_modified:
  - app/src/server/human-quality/improvement/reevaluate.ts
  - app/src/server/human-quality/improvement/service.ts
  - app/scripts/run-quality-improvement.ts
  - app/scripts/check-quality-improvement-evidence.mjs
  - .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json
  - app/package.json
  - app/src/app/api/feedback/quality-improvement/route.ts
  - app/src/app/api/feedback/quality-improvement/route.test.ts
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  - app/tests/unit/human-quality/improvement/reevaluate.test.ts
autonomous: false
requirements:
  - QUALITY-04
must_haves:
  truths:
    - "Report compares targeted failure-reason frequency before vs after on same human evaluation dimensions"
    - "Before arm uses baseline/pre_learning cohort or selectedAt before improvementDeployedAt"
    - "After arm uses post_learning cohort evaluations (or insufficient_sample when afterCount=0)"
    - "Fixture arm reports gate detection pass rate on targeted CORPUS_ARCHETYPE_FIXTURES"
    - "Checker rejects improvement headline when status is insufficient_sample or afterCount=0"
    - "CLI emits 132-EVIDENCE.json; owner UI shows read-only Quality tab with honest insufficient-sample messaging"
  artifacts:
    - path: app/src/server/human-quality/improvement/reevaluate.ts
      provides: failure-frequency before/after aggregation
      exports: ["buildQualityImprovementReport", "failureRatesByReason"]
    - path: app/src/server/human-quality/improvement/service.ts
      provides: runQualityImprovement orchestrator
      exports: ["runQualityImprovement"]
    - path: app/scripts/run-quality-improvement.ts
      provides: CLI evidence generation
    - path: app/src/app/api/feedback/quality-improvement/route.ts
      provides: GET quality improvement report API
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: Quality read-only tab
  key_links:
    - from: app/src/server/human-quality/improvement/reevaluate.ts
      to: listEvaluatedCorpusWithEvaluations
      via: cohort filter baseline|pre_learning vs post_learning
      pattern: "post_learning|baseline"
    - from: app/scripts/run-quality-improvement.ts
      to: runQualityImprovement
      via: tsx --all-workspaces --improvement-deployed-at
      pattern: "runQualityImprovement"
    - from: app/scripts/check-quality-improvement-evidence.mjs
      to: 132-EVIDENCE.template.json
      via: deltaRateByReason honesty when insufficient_sample
      pattern: "insufficient_sample|failureFrequencyAfter"
---

# Plan 132-04: Re-Evaluation Report, CLI, API and Quality Tab

<objective>
Ship failure-frequency re-evaluation comparing human corpus before/after arms and deterministic fixture detection delta, with CLI evidence, CI honesty gates, and read-only Quality tab — proving whether targeted failures decreased without false improvement claims (QUALITY-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-targeted-creative-quality-improvements/132-RESEARCH.md
@.planning/phases/132-targeted-creative-quality-improvements/132-VALIDATION.md
@app/scripts/run-score-calibration.ts
@app/scripts/run-learning-impact.ts
@app/src/server/human-quality/calibration/compare.ts
@app/src/server/human-quality/calibration/aggregate.ts
@app/src/server/ai/corpus-fixtures.ts

<interfaces>
From `calibration/compare.ts`:
```typescript
export function buildCalibrationComparisons(rows: EvaluatedCorpusRow[]): CalibrationComparison[]
```

From RESEARCH Pattern 4 report shape:
```typescript
export interface QualityImprovementReport {
  schemaVersion: 1;
  rubricCalibrationVersion: string;
  status: "ok" | "insufficient_sample";
  targetedFailureReasons: readonly HumanQualityFailureReason[];
  visualMetrics: { failureFrequencyBefore; failureFrequencyAfter; deltaRateByReason };
  factualMetrics: { factualPassRateBefore; factualPassRateAfter };
  fixtureMetrics?: { targetedArchetypePassRateBefore; targetedArchetypePassRateAfter };
  acceptedAdjustments: Array<{ adjustmentId; targetModule; targetKey }>;
}
```

From `HumanQualityCorpusPanel` tab pattern (Phase 131):
```typescript
// Tabs: queue | calibration | impact → add quality
```
</interfaces>
</context>

<tasks>

<task id="132-04-01" type="auto" tdd="true">
  <name>Re-evaluation engine and orchestrator</name>
  <files>app/src/server/human-quality/improvement/reevaluate.ts, app/src/server/human-quality/improvement/service.ts, app/tests/unit/human-quality/improvement/reevaluate.test.ts</files>
  <behavior>
    - failureRatesByReason: counts/rates for five targeted visual reasons from CalibrationComparison[]
    - before arm: cohort in baseline|pre_learning OR selectedAt &lt; improvementDeployedAt
    - after arm: cohort post_learning only
    - status insufficient_sample when any targeted reason has &lt; MIN_SLICE_SAMPLE in either arm
    - deltaRateByReason null when insufficient_sample; negative delta means improvement (rate decreased)
    - factualPassRateBefore/After computed separately — never blended into visual headline
    - fixtureMetrics: run gate classification on targeted archetype fixtures (8 visual archetypes from 132-02); before rate uses pre-132-02 baseline snapshot constant in test, after rate uses live classification
  </behavior>
  <action>
    Implement `reevaluate.ts` with `failureRatesByReason`, `buildQualityImprovementReport`, `computeFixtureArchetypePassRate` using `CORPUS_ARCHETYPE_FIXTURES` filtered to five visual human reasons via `resolveGateTargets`. Implement `service.ts` `runQualityImprovement({ workspaceId?, improvementDeployedAt?, cohort? })` loading evaluated corpus via `listEvaluatedCorpusWithEvaluations`, building comparisons, joining `listAcceptedAdjustments` for `acceptedAdjustments` list. Reuse `post_learning` cohort for after arm (per RESEARCH discretion — no new cohort enum unless after-arm empty in production). Set `improvementDeployedAt` default to earliest acceptedAt among accepted adjustments. Export `TARGETED_VISUAL_FAILURE_REASONS` from improvement/types if not already.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/improvement/reevaluate.test.ts</automated>
  </verify>
  <done>Re-evaluation report aggregates human failure frequency and fixture pass rates with honesty gates.</done>
</task>

<task id="132-04-02" type="auto">
  <name>CLI evidence and complete checker</name>
  <files>app/scripts/run-quality-improvement.ts, app/scripts/check-quality-improvement-evidence.mjs, .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json, app/package.json</files>
  <behavior>
    - CLI flags: --out, --all-workspaces, --workspace-id, --improvement-deployed-at ISO date
    - Writes 132-EVIDENCE.json with visualMetrics, factualMetrics, fixtureMetrics, regressionMetrics, requirements QUALITY-01..04
    - Checker: when status insufficient_sample, assert deltaRateByReason values are null and no improvementClaimed:true
    - Checker: when afterCount=0 for all targeted reasons, reject improvement headline fields
    - Template updated with ok and insufficient_sample example shapes
  </behavior>
  <action>
    Create `run-quality-improvement.ts` mirroring `run-learning-impact.ts`. Extend `check-quality-improvement-evidence.mjs` from 132-03 with QUALITY-04 rules: validate `visualMetrics.failureFrequencyBefore/After`, `deltaRateByReason`, separate `factualMetrics`, optional `fixtureMetrics`. Update `132-EVIDENCE.template.json` with full ok + insufficient_sample examples. Update npm script to run against generated evidence path. Wire `--run-regression` flag for Phase 133 precursor.
  </action>
  <verify>
    <automated>node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>CLI generates checkable evidence; checker enforces QUALITY-04 honesty rules.</done>
</task>

<task id="132-04-03" type="auto" tdd="true">
  <name>Quality improvement API and UI tab</name>
  <files>app/src/app/api/feedback/quality-improvement/route.ts, app/src/app/api/feedback/quality-improvement/route.test.ts, app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <behavior>
    - GET quality-improvement: requireCalibrationAccess; returns QualityImprovementReport
    - Caps slice drill-down rows at 100 with truncated:true
    - Quality tab shows before/after failure frequency table per targeted reason
    - insufficient_sample shows explicit message — no positive delta headline
    - factualMetrics section separate from visualMetrics
    - Panel hides only when queue, calibration, impact, AND quality all return 403
  </behavior>
  <action>
    Add GET `/api/feedback/quality-improvement` calling `runQualityImprovement`. Add `{ id: "quality", label: "Quality" }` tab to `HumanQualityCorpusPanel` fetching report via useQuery. Render `QualityImprovementReportView`: accepted adjustments list, failure frequency table, fixture metrics subsection, factual pass rates. Extend hide-when-forbidden logic per Phase 131 pattern. Add route and component tests.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/quality-improvement/route.test.ts src/components/feedback/HumanQualityCorpusPanel.test.tsx -t "quality"</automated>
  </verify>
  <done>Read-only Quality tab and API expose re-evaluation report with metric separation.</done>
</task>

<task id="132-04-04" type="checkpoint:human-verify" gate="blocking">
  <name>Verify quality improvement CLI and UI</name>
  <files>.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json, app/src/components/feedback/HumanQualityCorpusPanel.tsx</files>
  <action>
    Operator verifies full Phase 132 pipeline: accepted adjustments, module edits, regression green, re-evaluation report.

    Automated pre-check (Claude runs before pausing):
    1. `cd app && npm test -- tests/unit/human-quality/improvement src/app/api/feedback/quality-improvement src/components/feedback/HumanQualityCorpusPanel.test.tsx -t "quality"`
    2. `node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests`

    Operator manual steps:
    3. Run `cd app && npx tsx scripts/run-quality-improvement.ts --all-workspaces --out ../.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.json`
    4. Run `node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.json --run-regression`
    5. Open owner feedback panel → Quality tab; confirm before/after table and insufficient_sample messaging if after corpus is empty
    6. Optional: enqueue post-change derivations, human-evaluate as post_learning, re-run CLI to populate after arm

    Resume signal: Type "approved" or describe issues with regression or re-evaluation display.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/improvement && node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>Operator confirms CLI evidence, regression guards, and Quality tab match report schema; automated tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Re-evaluation report → operator | Must not claim improvement without after-arm human data |
| API → global rollup | Platform-owner sees cross-workspace failure frequencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-10 | D | False improvement claim | mitigate | insufficient_sample + checker null deltas |
| T-132-11 | I | Corpus ID exposure in API | mitigate | Report aggregates only; no prompt fields |
| T-132-12 | T | quality-improvement GET | mitigate | requireCalibrationAccess dual auth |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/improvement`
- `node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests`
- `cd app && npm test && npm run lint && npm run build`
</verification>

<success_criteria>
- QUALITY-04: Re-evaluation report shows targeted failure-frequency before/after on human dimensions and fixture arm; honest insufficient_sample when after corpus missing.
</success_criteria>

<output>
After completion, create `.planning/phases/132-targeted-creative-quality-improvements/132-04-SUMMARY.md`.
</output>
