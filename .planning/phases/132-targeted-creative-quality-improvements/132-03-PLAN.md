---
phase: 132-targeted-creative-quality-improvements
plan: "03"
type: execute
wave: 3
depends_on: ["132-02"]
files_modified:
  - app/scripts/check-quality-improvement-evidence.mjs
  - .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json
  - app/package.json
autonomous: true
requirements:
  - QUALITY-03
must_haves:
  truths:
    - "Phase 132 evidence checker enforces regressionMetrics.factualFidelityRate and safetyGuardPassRate at 1.0 from evidence JSON"
    - "With --run-regression flag, checker shells to check-creative-validation-evidence --stage final and check-output-learning-evidence.mjs"
    - "Default npm script uses --skip-tests; vitest subsets (gate-failure-matrix, guards.test.ts) run unless --skip-tests"
    - "Evidence JSON separates regressionMetrics from visualImprovementMetrics — no blended pass field"
    - "Phase 133 gate uses --run-regression for full v12.3/v12.4 script regression; Plan 132-04 checkpoint documents pass/fail"
  artifacts:
    - path: app/scripts/check-quality-improvement-evidence.mjs
      provides: CI regression guard for QUALITY-03
    - path: .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json
      provides: schema with regressionMetrics and acceptedAdjustments sections
    - path: app/package.json
      provides: npm script quality-improvement-evidence
  key_links:
    - from: app/scripts/check-quality-improvement-evidence.mjs
      to: check-creative-validation-evidence.mjs
      via: exec or shared threshold asserts for factualFidelityRate
      pattern: "factualFidelityRate|check-creative-validation"
    - from: app/scripts/check-quality-improvement-evidence.mjs
      to: check-output-learning-evidence.mjs
      via: safetyGuardPassRate >= 1.0 assertion
      pattern: "safetyGuardPassRate"
    - from: app/scripts/check-quality-improvement-evidence.mjs
      to: gate-failure-matrix.test.ts
      via: vitest execFileSync unless --skip-tests
      pattern: "gate-failure-matrix"
---

# Plan 132-03: Regression Guard Wiring for v12.3 and v12.4

<objective>
Wire Phase 132 evidence checker to re-run established v12.3 factual hard-failure protections and v12.4 learning safety guards after visual module edits — blocking merge if factualFidelityRate or safetyGuardPassRate regresses (QUALITY-03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-targeted-creative-quality-improvements/132-RESEARCH.md
@.planning/phases/132-targeted-creative-quality-improvements/132-VALIDATION.md
@app/scripts/check-creative-validation-evidence.mjs
@app/scripts/check-output-learning-evidence.mjs
@app/scripts/check-score-calibration-evidence.mjs

<interfaces>
From `check-output-learning-evidence.mjs`:
```javascript
const OUTPUT_LEARNING_EVAL_TESTS = [..., "src/server/output-learning/safety/guards.test.ts", ...];
const V12_3_FACTUAL_REGRESSION_TESTS = ["tests/unit/ai/gate-failure-matrix.test.ts", "tests/unit/ai/creative-quality-gate.test.ts"];
```

From RESEARCH Pattern 3 commands:
```bash
node app/scripts/check-creative-validation-evidence.mjs --stage final
node app/scripts/check-output-learning-evidence.mjs
cd app && npm test -- tests/unit/ai/gate-failure-matrix.test.ts src/server/output-learning/safety/guards.test.ts
```
</interfaces>
</context>

<tasks>

<task id="132-03-01" type="auto">
  <name>Regression sections in evidence checker</name>
  <files>app/scripts/check-quality-improvement-evidence.mjs</files>
  <behavior>
    - Checker loads evidence JSON and validates schemaVersion and rubricCalibrationVersion
    - regressionMetrics.factualFidelityRate must be null or >= 1.0; rejects &lt; 1.0
    - regressionMetrics.safetyGuardPassRate must be null or >= 1.0; rejects &lt; 1.0
    - Rejects blended fields like overallQualityPass or combinedScore in root
    - When --skip-tests not set: runs gate-failure-matrix, creative-quality-gate, guards.test.ts
    - Optionally shells to check-creative-validation-evidence.mjs --stage final --skip-tests when evidence embeds precomputed rates
  </behavior>
  <action>
    Create `check-quality-improvement-evidence.mjs` mirroring `check-score-calibration-evidence.mjs` structure. Args: `--evidence PATH` (default `../.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.json`), `--skip-tests`, `--run-regression` (when set, exec `check-creative-validation-evidence.mjs --stage final` and `check-output-learning-evidence.mjs --skip-tests`, merge factualFidelityRate and safetyGuardPassRate into validation). Validate `regressionMetrics` object separate from `visualMetrics` and `factualMetrics` (re-eval section added in 132-04). Enforce requirements array includes `QUALITY-03`. Fail with clear `QUALITY-IMPROVEMENT-EVIDENCE:` prefix on regression breach.
  </action>
  <verify>
    <automated>node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>Evidence checker validates QUALITY-03 regression metrics and runs targeted vitest subsets.</done>
</task>

<task id="132-03-02" type="auto">
  <name>Evidence template and npm script</name>
  <files>.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json, app/package.json</files>
  <behavior>
    - Template includes ok example with regressionMetrics at 1.0 and acceptedAdjustments array
    - Template includes regression_fail example showing checker rejection shape (for docs only — checker validates ok template)
    - npm script quality-improvement-evidence runs checker against template with --skip-tests
    - requirements field lists QUALITY-01 through QUALITY-04 placeholders; regression section populated for QUALITY-03
  </behavior>
  <action>
    Create `132-EVIDENCE.template.json` with `schemaVersion: 1`, `rubricCalibrationVersion: "1.1.0"`, `status: "ok"`, `regressionMetrics: { factualFidelityRate: 1.0, safetyGuardPassRate: 1.0, gateMatrixPass: true }`, `acceptedAdjustments: [{ adjustmentId, targetModule, targetKey }]`, stub `visualMetrics` and `factualMetrics` objects (filled by 132-04). Add `"quality-improvement-evidence": "node scripts/check-quality-improvement-evidence.mjs --evidence ../.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests"` to app/package.json scripts. Run full regression locally once: `node app/scripts/check-creative-validation-evidence.mjs --stage final` and `node app/scripts/check-output-learning-evidence.mjs` — document pass/fail in SUMMARY.
  </action>
  <verify>
    <automated>cd app && npm run quality-improvement-evidence</automated>
  </verify>
  <done>Template validates in CI; npm script registered; v12.3/v12.4 regression commands documented as phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI evidence checker | Must not accept visual improvement claims that mask factual regression |
| Evidence JSON | Public to repo — no prompts or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-07 | E | Blended metrics in evidence | mitigate | Reject overallPass/combinedScore fields; separate regressionMetrics |
| T-132-08 | T | False green regression | mitigate | Run gate-failure-matrix + guards tests unless --skip-tests |
| T-132-09 | I | Evidence file tampering | mitigate | Checker re-runs regression scripts with --run-regression in Phase 133 gate |
</threat_model>

<verification>
- `node app/scripts/check-quality-improvement-evidence.mjs --evidence .planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.template.json --skip-tests`
- `node app/scripts/check-creative-validation-evidence.mjs --stage final`
- `node app/scripts/check-output-learning-evidence.mjs`
- `cd app && npm test -- tests/unit/ai/gate-failure-matrix.test.ts src/server/output-learning/safety/guards.test.ts`
</verification>

<success_criteria>
- QUALITY-03: Visual module edits from 132-02 pass v12.3 factual and v12.4 safety regression suite with rates at 1.0.
</success_criteria>

<output>
After completion, create `.planning/phases/132-targeted-creative-quality-improvements/132-03-SUMMARY.md`.
</output>
