---
phase: 132-targeted-creative-quality-improvements
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0046_rubric_calibration_accept_metadata.sql
  - app/drizzle/meta/_journal.json
  - app/src/server/db/schema.ts
  - app/src/server/repositories/rubric-calibration-adjustments.ts
  - app/src/server/human-quality/improvement/types.ts
  - app/src/server/human-quality/improvement/accept.ts
  - app/src/server/human-quality/improvement/apply.ts
  - app/src/server/human-quality/calibration/report.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts
  - app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.test.ts
  - app/tests/unit/human-quality/improvement/accept.test.ts
  - app/tests/unit/human-quality/improvement/apply.test.ts
autonomous: true
requirements:
  - QUALITY-02
must_haves:
  truths:
    - "Platform-owner can promote proposed calibration adjustments to accepted with reviewer audit trail"
    - "Only adjustments with evidenceRefs.corpusItemIds.length >= MIN_SLICE_SAMPLE (3) can be accepted"
    - "Prior accepted rows for same slice_key+target_module+target_key are superseded on new accept"
    - "buildApplyPlan returns only accepted rows with valid evidence — proposed rows never appear"
    - "RUBRIC_CALIBRATION_VERSION bumps to 1.1.0 signaling Phase 132 apply tranche"
  artifacts:
    - path: app/src/server/human-quality/improvement/accept.ts
      provides: acceptProposedAdjustment lifecycle
      exports: ["acceptProposedAdjustment", "AcceptAdjustmentInput"]
    - path: app/src/server/human-quality/improvement/apply.ts
      provides: evidence-bound apply plan builder
      exports: ["buildApplyPlan", "computeBoundedCeiling"]
    - path: app/src/server/repositories/rubric-calibration-adjustments.ts
      provides: acceptAdjustment, listAcceptedAdjustments, supersedeAcceptedForSlice
    - path: app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts
      provides: PATCH accept API with platform-owner auth
  key_links:
    - from: app/src/server/human-quality/improvement/accept.ts
      to: rubric_calibration_adjustments
      via: acceptAdjustment updates status proposed→accepted
      pattern: "status.*accepted"
    - from: app/src/server/human-quality/improvement/apply.ts
      to: failure-bridge.ts
      via: resolveAdjustmentTarget per slice failure reason
      pattern: "resolveAdjustmentTarget"
    - from: app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts
      to: requireCalibrationAccess
      via: platform-owner or workspace admin gate
      pattern: "requireCalibrationAccess"
---

# Plan 132-01: Accept Lifecycle and Evidence-Bound Apply Contracts

<objective>
Ship Postgres accept lifecycle for Phase 130 `proposed` calibration adjustments, bounded apply-plan contracts, and owner API — enabling evidence-bound module edits in 132-02 without drive-by rubric changes (QUALITY-02 partial).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-targeted-creative-quality-improvements/132-RESEARCH.md
@.planning/phases/132-targeted-creative-quality-improvements/132-VALIDATION.md
@app/src/server/repositories/rubric-calibration-adjustments.ts
@app/src/server/human-quality/calibration/adjustments.ts
@app/src/server/human-quality/calibration/failure-bridge.ts
@app/src/server/human-quality/calibration/report.ts
@app/src/server/auth/calibration-access.ts

<interfaces>
From `calibration/report.ts`:
```typescript
export const RUBRIC_CALIBRATION_VERSION = "1.0.0";
export const MIN_SLICE_SAMPLE = 3;
```

From `rubric-calibration-adjustments.ts` (existing):
```typescript
export async function insertProposedAdjustment(input: InsertProposedAdjustmentInput)
export async function listProposedAdjustments(filters?: ListProposedAdjustmentsFilters)
export async function findProposedAdjustmentBySlice(sliceKey, adjustmentVersion, targetModule, targetKey)
```

From `calibration/types.ts`:
```typescript
export interface CalibrationAdjustmentEvidence {
  corpusItemIds: string[];
  sliceStats: { count: number; meanSignedDelta: number; meanAbsError: number | null };
  itemRefs: CalibrationAdjustmentEvidenceItemRef[];
}
```

From `failure-bridge.ts`:
```typescript
export function resolveAdjustmentTarget(reason: HumanQualityFailureReason): AdjustmentTarget | null
```
</interfaces>
</context>

<tasks>

<task id="132-01-01" type="auto" tdd="true">
  <name>Migration and repository accept lifecycle</name>
  <files>app/drizzle/0046_rubric_calibration_accept_metadata.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts, app/src/server/repositories/rubric-calibration-adjustments.ts, app/tests/unit/human-quality/improvement/accept.test.ts</files>
  <behavior>
    - acceptAdjustment: proposed row with 3+ corpusItemIds → status accepted, acceptedAt set, acceptedBy reviewerUserId
    - acceptAdjustment: row not proposed → throws or returns validation error
    - acceptAdjustment: evidenceRefs.corpusItemIds.length &lt; MIN_SLICE_SAMPLE → rejected
    - supersedeAcceptedForSlice: prior accepted for same slice_key+version+module+key → status superseded
    - listAcceptedAdjustments: returns only status=accepted for given adjustmentVersion
  </behavior>
  <action>
    Add migration `0046_rubric_calibration_accept_metadata.sql` with nullable `accepted_at timestamp` and `accepted_by uuid` on `rubric_calibration_adjustments` (references users.id if FK pattern exists in project; otherwise text reviewer id per calibration-access session user). Update Drizzle schema. Extend repository with `acceptAdjustment({ adjustmentId, reviewerUserId, changeSpec? })`, `listAcceptedAdjustments({ adjustmentVersion, status: 'accepted' })`, `supersedeAcceptedForSlice(sliceKey, adjustmentVersion, targetModule, targetKey, excludeId)`. Store optional bounded `changeSpec` jsonb on row OR in separate metadata column — Zod schema: `ceilingDelta` number in [-5,5], `rubricTightening` enum minor|moderate, `gateMarkerAdditions` string[] max length 10. Do NOT auto-accept proposals. Mock DB in tests following Phase 130 repository pattern.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/improvement/accept.test.ts</automated>
  </verify>
  <done>Repository supports proposed→accepted→superseded lifecycle with evidence minimum and reviewer audit fields.</done>
</task>

<task id="132-01-02" type="auto" tdd="true">
  <name>Accept service and apply plan builder</name>
  <files>app/src/server/human-quality/improvement/types.ts, app/src/server/human-quality/improvement/accept.ts, app/src/server/human-quality/improvement/apply.ts, app/tests/unit/human-quality/improvement/apply.test.ts</files>
  <behavior>
    - acceptProposedAdjustment: calls repository accept + supersede in transaction
    - buildApplyPlan: filters accepted rows with corpusItemIds.length >= 3
    - buildApplyPlan: maps sliceKey failure reason via resolveAdjustmentTarget — never invents new targets
    - computeBoundedCeiling: clamps delta to ±5 and result to [0,100]
    - factual_issue accepted rows included in plan but flagged applyKind=factual_guard_only (no ceiling edits in 132-02)
  </behavior>
  <action>
    Create `improvement/types.ts` with `ApplyPlanItem`, `AcceptAdjustmentInput`, `QualityImprovementChangeSpec` per RESEARCH Pattern 1. Implement `acceptProposedAdjustment` orchestrating supersede-then-accept. Implement `buildApplyPlan(adjustmentVersion)` reading `listAcceptedAdjustments` and attaching `gateTargets` from `failure-bridge.ts`. Export `computeBoundedCeiling` helper for 132-02. Export `TARGETED_VISUAL_FAILURE_REASONS` constant listing the five visual reasons. Do NOT edit creative-score-ceilings.ts or gate modules in this plan.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/improvement/apply.test.ts</automated>
  </verify>
  <done>Pure accept/apply contracts exist; apply plan is evidence-filtered and failure-bridge-bound.</done>
</task>

<task id="132-01-03" type="auto" tdd="true">
  <name>Accept API and calibration version bump</name>
  <files>app/src/server/human-quality/calibration/report.ts, app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts, app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.test.ts</files>
  <behavior>
    - PATCH accept: platform-owner or workspace admin via requireCalibrationAccess succeeds
    - PATCH accept: non-admin receives 403
    - PATCH accept: invalid changeSpec (ceilingDelta=99) returns 400
    - PATCH accept: proposed row with insufficient evidence returns 422
    - RUBRIC_CALIBRATION_VERSION is 1.1.0 after bump
  </behavior>
  <action>
    Bump `RUBRIC_CALIBRATION_VERSION` from `1.0.0` to `1.1.0` in `calibration/report.ts` (per RESEARCH discretion — signals Phase 132 apply tranche). Add PATCH `/api/feedback/calibration-adjustments/[id]/accept` with Zod body `{ changeSpec?: QualityImprovementChangeSpec }`, call `requireCalibrationAccess`, pass session user id as `reviewerUserId`, return updated row. Mirror auth tests from `score-calibration/route.test.ts`. Document operator prerequisite in plan summary: run `npx tsx app/scripts/run-score-calibration.ts` if zero proposals exist, then accept targeted visual slices via API or future UI.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/calibration-adjustments/\[id\]/accept/route.test.ts</automated>
  </verify>
  <done>Authenticated accept API works; calibration version bumped for Phase 132 adjustments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → accept PATCH | Untrusted changeSpec must be bounded before affecting global rubric |
| Accept API → Postgres | Only owner/admin may promote proposals affecting global scoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | T | accept route | mitigate | requireCalibrationAccess; audit acceptedBy |
| T-132-02 | I | changeSpec payload | mitigate | Zod bounds on ceilingDelta ±5 and gateMarkerAdditions max 10 |
| T-132-03 | E | Accept without evidence | mitigate | Reject when corpusItemIds.length &lt; MIN_SLICE_SAMPLE |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/improvement`
- `cd app && npm run lint -- src/server/human-quality/improvement src/server/repositories/rubric-calibration-adjustments.ts`
</verification>

<success_criteria>
- QUALITY-02 (accept chain): Proposed adjustments can be accepted with corpus evidence and reviewer audit; apply plan excludes unaccepted rows.
</success_criteria>

<output>
After completion, create `.planning/phases/132-targeted-creative-quality-improvements/132-01-SUMMARY.md`.
</output>
