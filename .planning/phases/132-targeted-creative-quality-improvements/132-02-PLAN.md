---
phase: 132-targeted-creative-quality-improvements
plan: "02"
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - app/src/server/ai/creative-score-ceilings.ts
  - app/src/server/ai/observable-rubric.ts
  - app/src/server/ai/creative-quality-gate.ts
  - app/src/server/ai/creative-quality-taxonomy.ts
  - app/src/server/ai/regeneration-correction-brief.ts
  - app/src/server/ai/corpus-fixtures.ts
  - app/tests/unit/ai/corpus-fixtures.test.ts
  - app/tests/unit/ai/corpus-baseline.test.ts
  - app/tests/unit/ai/gate-failure-matrix.test.ts
  - app/tests/unit/human-quality/calibration/failure-bridge.test.ts
  - app/tests/unit/ai/quality-rubric-regression.test.ts
autonomous: false
requirements:
  - QUALITY-01
  - QUALITY-02
must_haves:
  truths:
    - "Every code edit to ceilings/rubric/gate maps to an accepted adjustmentId with corpus evidence"
    - "Five targeted visual failure reasons have strengthened detection (gate markers, rubric prose, or ceilings)"
    - "Three missing corpus archetypes (weak_hierarchy, illegible_cta, unfocused_composition) exist for deterministic re-eval"
    - "FIDELITY_HARD_FAILURE_CODES and factual classifier patterns remain unchanged"
  artifacts:
    - path: app/src/server/ai/creative-score-ceilings.ts
      provides: tuned SCORE_CEILING_BY_FAILURE per accepted score_ceiling targets
    - path: app/src/server/ai/observable-rubric.ts
      provides: tightened VISUAL_OVERLOAD_RUBRIC / GENERIC_TEMPLATE_RUBRIC per accepted rows
    - path: app/src/server/ai/creative-quality-gate.ts
      provides: visual gate markers aligned to human failure bridge
    - path: app/src/server/ai/corpus-fixtures.ts
      provides: archetype fixtures for weak_hierarchy, illegible_cta, unfocused_composition
  key_links:
    - from: app/src/server/ai/creative-score-ceilings.ts
      to: buildApplyPlan accepted rows
      via: computeBoundedCeiling per targetKey + adjustmentId comment
      pattern: "132-adjustment:"
    - from: app/src/server/ai/corpus-fixtures.ts
      to: failure-bridge.ts
      via: expectedHardFailureCodes match human visual reasons
      pattern: "missing_dominant_idea|unreadable_required_text|decorative_only"
    - from: app/src/server/ai/creative-quality-taxonomy.ts
      to: creative-quality-gate.ts
      via: shared regex patterns for visual markers only
      pattern: "OVERLOAD_NOTE_MARKERS|ILLEGIBILITY"
---

# Plan 132-02: Evidence-Bound Module Edits and Archetype Fixtures

<objective>
Apply bounded ceiling/rubric/gate edits driven solely by accepted calibration adjustments, add three missing visual corpus archetypes, and strengthen detection for all five targeted failure reasons — each diff citing adjustmentId (QUALITY-01, QUALITY-02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-targeted-creative-quality-improvements/132-RESEARCH.md
@app/src/server/human-quality/improvement/apply.ts
@app/src/server/human-quality/calibration/failure-bridge.ts
@app/src/server/ai/creative-score-ceilings.ts
@app/src/server/ai/observable-rubric.ts
@app/src/server/ai/creative-quality-gate.ts
@app/src/server/ai/corpus-fixtures.ts

<interfaces>
From `improvement/apply.ts` (Plan 132-01):
```typescript
export function buildApplyPlan(adjustmentVersion: string): Promise<ApplyPlanItem[]>
export function computeBoundedCeiling(targetKey, ceilingDelta, maxDelta = 5): number
export const TARGETED_VISUAL_FAILURE_REASONS: readonly HumanQualityFailureReason[]
```

From `failure-bridge.ts` mapping table (RESEARCH Pattern 2):
| Human reason | targetModule | targetKey |
| visual_overload | score_ceiling | visual_overload |
| weak_hierarchy | score_ceiling | missing_dominant_idea |
| generic_template_feel | score_ceiling | generic_template_aesthetic |
| illegible_cta | score_ceiling | unreadable_required_text or cta_drift |
| unfocused_composition | score_ceiling | missing_dominant_idea / decorative_only_variation |
</interfaces>
</context>

<tasks>

<task id="132-02-00" type="checkpoint:human-action" gate="blocking">
  <name>Operator accepts visual calibration proposals</name>
  <files>app/src/app/api/feedback/calibration-adjustments/[id]/accept/route.ts</files>
  <action>
    Operator must accept at least one visual calibration proposal before module edits (QUALITY-02 evidence binding).

    Operator steps:
    1. Run `cd app && npx tsx scripts/run-score-calibration.ts --all-workspaces` to ensure proposals exist (or verify DB has proposed rows).
    2. PATCH accept at least one visual-failure proposal (visual_overload, weak_hierarchy, generic_template_feel, illegible_cta, or unfocused_composition) via `/api/feedback/calibration-adjustments/[id]/accept` with optional bounded changeSpec.
    3. Confirm `listAcceptedAdjustments` returns ≥1 row for adjustmentVersion `1.1.0`.

    Resume signal: Type "accepted" when at least one visual proposal is accepted, or describe blocker.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/improvement/accept.test.ts</automated>
  </verify>
  <done>At least one accepted visual adjustment exists in Postgres for version 1.1.0 before ceiling/rubric/gate edits.</done>
</task>

<task id="132-02-01" type="auto" tdd="true">
  <name>Apply score ceiling and observable rubric edits</name>
  <files>app/src/server/ai/creative-score-ceilings.ts, app/src/server/ai/observable-rubric.ts, app/tests/unit/ai/quality-rubric-regression.test.ts</files>
  <behavior>
    - For each accepted score_ceiling row: ceiling value changes by bounded delta from changeSpec (default -5 when meanSignedDelta positive = auto over-scores)
    - For each accepted observable_rubric row: rubric prose tightens per rubricTightening minor|moderate
    - No ceiling edit when no matching accepted row for targetKey
    - Each changed constant block includes comment `// 132-adjustment:{adjustmentId}`
    - factual_issue accepted rows do not modify ceilings
  </behavior>
  <action>
    Load `buildApplyPlan("1.1.0")` at implementation time (or read accepted rows in tests via fixtures). For each `targetModule: score_ceiling` item, apply `computeBoundedCeiling` to `SCORE_CEILING_BY_FAILURE[targetKey]`. For `observable_rubric` items, tighten `VISUAL_OVERLOAD_RUBRIC`, `GENERIC_TEMPLATE_RUBRIC`, or related rubric strings per accepted slice — add explicit detection phrases from human corpus patterns cited in rationale (do not parse rationale automatically; use fixed phrase additions mapped per targetKey). Skip any targetKey with no accepted row. Do NOT touch `FIDELITY_HARD_FAILURE_CODES` or factual rubric sections. Update `quality-rubric-regression.test.ts` if rubric injection expectations change.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts tests/unit/ai/creative-score-ceilings.test.ts 2>/dev/null || cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts</automated>
  </verify>
  <done>Ceiling and rubric modules reflect only accepted adjustment-driven bounded changes with adjustmentId traceability.</done>
</task>

<task id="132-02-02" type="auto" tdd="true">
  <name>Apply gate markers and regeneration directives</name>
  <files>app/src/server/ai/creative-quality-gate.ts, app/src/server/ai/creative-quality-taxonomy.ts, app/src/server/ai/regeneration-correction-brief.ts, app/tests/unit/ai/gate-failure-matrix.test.ts, app/tests/unit/human-quality/calibration/failure-bridge.test.ts</files>
  <behavior>
    - Accepted gate_classifier rows add markers from changeSpec.gateMarkerAdditions to taxonomy sets used by visual classifiers only
    - classifyQaNote still promotes visual_overload, missing_dominant_idea, generic_template_aesthetic, unreadable_required_text, decorative_only_variation for matching notes
    - FIDELITY_HARD_FAILURE_CODES detection cases in gate-failure-matrix.test.ts remain green unchanged
    - Optional: HUMAN_FAILURE_CORRECTION_DIRECTIVES map five visual reasons → existing FAILURE_CORRECTION_DIRECTIVES via failure-bridge gate codes
  </behavior>
  <action>
    For each accepted `gate_classifier` apply item, append bounded markers to the appropriate taxonomy constant (`OVERLOAD_NOTE_MARKERS`, `MISSING_DOMINANT_IDEA_MARKERS`, `GENERIC_TEMPLATE_MARKERS`, `ILLEGIBILITY_PATTERN`, `DECORATIVE_ONLY_PATTERN`) — max 10 additions per adjustment. Comment each addition with `132-adjustment:{id}`. Do NOT modify factual/legibility-preservation patterns that gate invented_factual_entity or style_reference_contamination. Optionally add thin `HUMAN_FAILURE_CORRECTION_DIRECTIVES` in `regeneration-correction-brief.ts` delegating to gate codes via `resolveGateTargets` — only for accepted visual slices. Run gate-failure-matrix tests after every marker edit.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/gate-failure-matrix.test.ts tests/unit/human-quality/calibration/failure-bridge.test.ts</automated>
  </verify>
  <done>Gate classifiers detect targeted visual failures earlier; factual matrix tests still pass.</done>
</task>

<task id="132-02-03" type="auto" tdd="true">
  <name>Add three visual corpus archetype fixtures</name>
  <files>app/src/server/ai/corpus-fixtures.ts, app/tests/unit/ai/corpus-fixtures.test.ts, app/tests/unit/ai/corpus-baseline.test.ts</files>
  <behavior>
    - New fixtures: weak_hierarchy, illegible_cta, unfocused_composition archetypes with synthetic rawQaModelOutput
    - weak_hierarchy fixture expects missing_dominant_idea hard failure
    - illegible_cta fixture expects unreadable_required_text or cta_drift
    - unfocused_composition fixture expects decorative_only_variation or missing_dominant_idea
    - CORPUS_ARCHETYPE_FIXTURES length becomes 8; catalog integrity test updated
    - All five TARGETED_VISUAL_FAILURE_REASONS have at least one archetype mapping via failure-bridge codes
  </behavior>
  <action>
    Extend `CorpusArchetype` union with `weak_hierarchy`, `illegible_cta`, `unfocused_composition`. Add three fixtures to `CORPUS_ARCHETYPE_FIXTURES` using synthetic `qaModelOutput` notes mirroring June 2026 audit observations (no new PNG renders). Map `expectedHardFailureCodes` to bridge targets per RESEARCH Pitfall 4. Update `corpus-fixtures.test.ts` length assertion from 5 to 8 and archetype coverage set. Update `corpus-baseline.test.ts` covered archetype set if it asserts enum completeness. Per QUALITY-01: strengthens first-class failure reason detection in deterministic arm.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-fixtures.test.ts tests/unit/ai/corpus-baseline.test.ts</automated>
  </verify>
  <done>Eight archetype fixtures cover all five targeted visual failure patterns for QUALITY-04 fixture arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apply plan → scoring modules | Global rubric changes affect all workspaces |
| Gate taxonomy | Shared classifyQaNote path — visual edits must not broaden factual matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04 | T | creative-score-ceilings.ts | mitigate | Only edit keys present in accepted apply plan |
| T-132-05 | E | creative-quality-gate.ts markers | mitigate | gate-failure-matrix.test.ts after each edit; no FIDELITY pattern changes |
| T-132-06 | D | Drive-by rubric edit | mitigate | Comment trace adjustmentId; skip keys without accepted row |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/gate-failure-matrix.test.ts tests/unit/ai/corpus-fixtures.test.ts tests/unit/ai/quality-rubric-regression.test.ts`
- Grep changed files for `132-adjustment:` — every scoring diff must cite an adjustmentId
</verification>

<success_criteria>
- QUALITY-01: All five visual failure reasons have strengthened detection surfaces (ceilings, rubric, gate, and/or archetype fixtures).
- QUALITY-02: No module edit without matching accepted adjustment row and evidence refs.
</success_criteria>

<output>
After completion, create `.planning/phases/132-targeted-creative-quality-improvements/132-02-SUMMARY.md`.
</output>
