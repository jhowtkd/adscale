---
phase: 202
plan: 01
slug: staging-release-gate
status: pending
requirements: [QA-04]
depends_on: [201-01]
autonomous: false
---

# Plan 202-01 — Staging Evidence and Release Gate

## Objective

Collect real staging evidence for both adaptive journeys, verify OpenAI/Inngest lifecycle, reach ≥5 operational guided starts, and pass `npm run v13-8-release-gate` **without** `--allow-pending-staging`.

## Tasks

### Task 1: Execute staging runbook (human)
- Follow `docs/staging/guided-journeys-v13-8-runbook.md`
- Walk `existing_creative` and `from_zero` with authenticated staging credentials
- Record safe notes in `200-EVIDENCE.json` → `stagingEvidence.paths`

### Task 2: Operational sample
- Observe ≥5 real guided journey starts in staging/production telemetry
- Update `200-EVIDENCE.json` → `operationalSample.guidedStarts` and `status: "sufficient"`

### Task 3: Live Inngest lifecycle
- Confirm one action through to completion or failure with job evidence
- Set `inheritedDebt.liveInngestLifecycle` to `verified` with safe note

### Task 4: Release gate without bypass
- Run `cd app && npm run v13-8-release-gate` (no `--allow-pending-staging`)
- Target evidence `status: "passed"`

## Blockers (environment)

Requires staging credentials, live deployment, and operator time. Cannot be fully automated in local dev.

## Manual Steps for Operator

1. Apply migration `0062_assistant_guided_flow_adaptive_state` on staging
2. Complete runbook checklist (both paths, reload, conflict tab, action confirm)
3. Update `.planning/phases/200-real-staging-evidence-and-release-gate/200-EVIDENCE.json`:
   - `stagingEvidence.status`: `"recorded"`
   - `operationalSample.guidedStarts`: ≥5, `status`: `"sufficient"`
   - `inheritedDebt.liveInngestLifecycle`: `"verified"`
4. Re-run release gate without `--allow-pending-staging`
