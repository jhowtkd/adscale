---
phase: 80-credit-estimate-transparency
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - app/src/components/workspace/PreviewGatePanel.tsx
  - app/src/components/workspace/PreviewGatePanel.test.tsx
  - app/messages/pt-BR.json
  - app/messages/en.json
autonomous: true
requirements:
  - CRED-01
  - CRED-02
  - CRED-04
must_haves:
  truths:
    - "Preview gate shows mode-aware formula (CTAs or formatos × 5 = total) replacing single batchCost line"
    - "Workspace credit balance always visible in credit box"
    - "Approve batch disabled when balance < batch total, jobCount is 0, or balance still loading"
    - "creditEstimateNote present in PT-BR and EN (subtle disclaimer)"
  artifacts:
    - path: app/src/components/workspace/PreviewGatePanel.tsx
      provides: Credit transparency UI
    - path: app/messages/pt-BR.json
      provides: previewGate i18n keys
  key_links:
    - from: PreviewGatePanel
      to: useRecordBetaEvent
      via: "unchanged cockpit_stage_* events"
---

<objective>
Implement preview gate credit transparency UI: formula breakdown, balance, insufficient-credit block, and i18n.

Purpose: Satisfy CRED-01, CRED-02, CRED-04 per 80-CONTEXT.md user decisions.
Output: Updated PreviewGatePanel + tests + PT/EN strings.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-credit-estimate-transparency/80-CONTEXT.md
@.planning/phases/80-credit-estimate-transparency/80-RESEARCH.md

@app/src/components/workspace/PreviewGatePanel.tsx
@app/src/components/workspace/PreviewGatePanel.test.tsx
@app/messages/pt-BR.json
@app/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreviewGatePanel credit box — formula, balance, block</name>
  <files>app/src/components/workspace/PreviewGatePanel.tsx, app/messages/pt-BR.json, app/messages/en.json</files>
  <action>
Update `PreviewGatePanelProps`:
- `batchBreakdown: BatchCreditBreakdown` (import type from strategy-recipes) OR mirror fields: `jobCount`, `unitCost`, `batchCredits`, `generationMode`
- `creditBalance?: number` (undefined = loading)

Credit box layout (top to bottom):
1. `previewSpent` — unchanged
2. **Balance** — new line: `balanceRemaining` i18n with `{balance}` (always show when number defined; skeleton/muted "—" while loading per discretion)
3. **Batch formula** — replace `batchCost`:
   - If `jobCount > 0`: `batchFormulaCta` or `batchFormulaFormat` with `{count}`, `{unit}`, `{total}` based on `generationMode`
   - If `jobCount === 0`: keep `batchCostPending`
4. `creditEstimateNote` — muted, unchanged tone (CRED-04)

**CRED-02 block:**
- `const insufficient = typeof creditBalance === 'number' && batchBreakdown.totalCredits > 0 && creditBalance < batchBreakdown.totalCredits`
- When insufficient: show `insufficientCredits` i18n `{estimate, balance}`; disable Approve button (still allow Revise)
- **Also disable Approve when `jobCount === 0`** (prevents 400 `noCtasProvided` / `invalidTargetFormats` — 80-RESEARCH Pitfall 2)
- **Disable Approve while `creditBalance === undefined`** (billing loading); balance line shows muted `—` until resolved (Pitfall 3)
- When `isGenerating` or `isApproving`, keep existing disabled rules
- No dev-admin UI branch (`creditBalance: 999_999` from API is sufficient)

i18n keys under `strategyRecipes.previewGate` (PT + EN):
- `balanceRemaining`
- `batchFormulaCta` — e.g. "{count} CTAs × {unit} créditos = {total}"
- `batchFormulaFormat` — e.g. "{count} formatos × {unit} créditos = {total}"
- `insufficientCredits` — e.g. "Estimativa {estimate} · saldo {balance}"

Remove or stop using `batchCost` key in UI (can leave in JSON for backward compat).

Do NOT add alert icons for disclaimer. Do NOT combine preview + batch into one total.
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Panel renders formula, balance, block state; PT/EN keys exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PreviewGatePanel regression tests</name>
  <files>app/src/components/workspace/PreviewGatePanel.test.tsx</files>
  <behavior>
    - art_variation breakdown shows batchFormulaCta with count 3
    - balance line visible when creditBalance provided
    - insufficient: approve disabled, insufficientCredits copy, revise still enabled
    - sufficient: approve enabled when balance >= total
    - Existing cockpit_stage_* event tests still pass
  </behavior>
  <action>
Update tests to pass `batchBreakdown` + `creditBalance` props instead of only `batchCredits`.

Add cases:
1. Formula CTA mode — jobCount 3 → expect batchFormulaCta JSON in output
2. Formula format mode — generationMode format_adaptation, jobCount 2
3. insufficientCredits when balance 10, total 15 — approve button disabled
4. balance 20, total 15 — approve enabled (mock preview completed)
5. jobCount 0 — approve disabled, `batchCostPending` shown
6. creditBalance undefined — approve disabled, balance shows `—`

Keep all existing event emission tests green.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/workspace/PreviewGatePanel.test.tsx</automated>
  </verify>
  <done>CRED-01/02/04 covered by component tests; beta events unchanged.</done>
</task>

</tasks>

<verification>
```bash
cd app && npm test -- src/components/workspace/PreviewGatePanel.test.tsx src/server/ai/strategy-recipes.test.ts
cd app && npm run lint && npm run build
```
</verification>

<success_criteria>
- All three CRED requirements satisfied in UI
- No regression to cockpit instrumentation on preview gate
</success_criteria>
