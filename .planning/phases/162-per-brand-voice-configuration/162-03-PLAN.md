---
phase: 162-per-brand-voice-configuration
plan: "03"
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.test.ts
  - app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx
  - app/src/components/admin/BrandVoiceInspectPanel.tsx
autonomous: false
requirements:
  - VOICE-05
must_haves:
  truths:
    - "Platform owner can GET active voice config for any clientProfileId"
    - "Non-owner receives 403 on voice inspect API"
    - "Owner UI displays reviewStatus, source, and voice sections read-only (no edit controls)"
    - "Missing config returns 404 with clear message (not a hard error page)"
  artifacts:
    - path: app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.ts
      provides: owner-only GET voice inspect
    - path: app/src/components/admin/BrandVoiceInspectPanel.tsx
      provides: read-only voice section display
    - path: app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx
      provides: owner-gated brand voice page
  key_links:
    - from: app/src/components/admin/BrandVoiceInspectPanel.tsx
      to: /api/admin/quality/brands/[clientProfileId]/voice
      via: apiFetch GET
      pattern: "admin/quality/brands/.*/voice"
    - from: app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.ts
      to: app/src/server/repositories/client-profile-olhar-config.ts
      via: getOlharVoiceConfigByClientProfileId
      pattern: "getOlharVoiceConfigByClientProfileId"
---

<objective>
Expose owner-only read-only inspection of per-brand voice configuration via API and admin UI.

Purpose: VOICE-05 view-only surface per D-04 (no freeform editor).
Output: GET route, route tests, read-only inspect panel and page.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-per-brand-voice-configuration/162-CONTEXT.md
@.planning/phases/162-per-brand-voice-configuration/162-RESEARCH.md
@.planning/phases/162-per-brand-voice-configuration/162-01-SUMMARY.md
@app/src/app/api/admin/quality/learning/proposals/route.ts
@app/src/server/auth/platform-owner.ts

<interfaces>
Admin API pattern (`learning/proposals/route.ts`):
```typescript
import { requirePlatformOwner } from "@/server/auth/platform-owner";
export async function GET(request: Request) {
  await requirePlatformOwner(request);
  // zod validate params, return NextResponse.json(...)
}
```

Expected GET response shape (Zod-validated):
```typescript
{
  clientProfileId: string;
  workspaceId: string;
  voiceId: string;
  displayName: string;
  reviewStatus: "pending_review" | "approved" | "changes_requested";
  source: string;
  config: {
    principles: string[];
    positiveSignals: string[];
    negativeSignals: string[];
    authorityAndClaims: string[];
    inviteRhythm: string[];
    correctButSoulless: string[];
    matchTerms?: string[]; // display only
  };
  approvedAt: string | null;
}
```
No POST/PATCH — view-only per deferred editor scope.
</interfaces>
</context>

<tasks>

<task id="162-03-01" type="auto">
  <name>Owner-only GET voice inspect API</name>
  <files>app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.ts, app/src/app/api/admin/quality/brands/[clientProfileId]/voice/route.test.ts</files>
  <action>
    Per D-04 (owner read-only GET at `/api/admin/quality/brands/[clientProfileId]/voice`):

    1. Create route with `requirePlatformOwner` (match `admin/quality/learning/*` error handling via `handleApiError`).

    2. Validate `clientProfileId` param as UUID. Resolve workspace from `client_profiles` row (404 if profile missing).

    3. Fetch olhar config via `getOlharVoiceConfigByClientProfileId`. Return 404 `{ error: "voice_config_not_found" }` when no config (per D-02: no config is valid state for generation, but inspect API should be explicit).

    4. Response exposes structured sections for UI — do NOT return raw prompt assembly secrets beyond config arrays.

    5. Route tests:
       - Platform owner → 200 with expected fields when config exists
       - Non-owner → 403
       - Unknown clientProfileId → 404
       - Missing config → 404
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/\[clientProfileId\]/voice/route.test.ts</automated>
  </verify>
  <done>VOICE-05 API: owner can view voice config; non-owner blocked.</done>
</task>

<task id="162-03-02" type="auto">
  <name>Read-only brand voice inspect UI</name>
  <files>app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx, app/src/components/admin/BrandVoiceInspectPanel.tsx</files>
  <action>
    Per D-04 admin quality shell:

    1. Create `BrandVoiceInspectPanel` client component:
       - Fetches GET `/api/admin/quality/brands/{clientProfileId}/voice`
       - Displays `displayName`, `reviewStatus`, `source`, `voiceId`
       - Renders each config section (principles, positiveSignals, etc.) as read-only lists
       - No textarea, save button, or PATCH — view-only
       - Empty/404 state: "Nenhuma configuração de voz para esta marca" (or i18n key if project uses translations on admin)

    2. Create page at `(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx`:
       - Gate: redirect or 403 message for non-platform-owner (reuse existing owner check pattern from feedback/owner panels)
       - Render `BrandVoiceInspectPanel` with `clientProfileId` from params

    3. Use existing layout components (`PageFrame`, `PageHeader`, `Panel`) for consistency with `(dashboard)/feedback`.
  </action>
  <verify>
    <automated>cd app && npm run build</automated>
  </verify>
  <done>Owner has navigable read-only UI for brand voice inspection.</done>
</task>

<task id="162-03-03" type="checkpoint:human-verify" gate="blocking">
  <name>Verify owner voice inspect UI readability</name>
  <files>app/src/app/(dashboard)/admin/quality/brands/[clientProfileId]/page.tsx, app/src/components/admin/BrandVoiceInspectPanel.tsx</files>
  <action>
    Automated pre-check (Claude runs before pausing):
    1. `cd app && npm test -- --run src/app/api/admin/quality/brands/\[clientProfileId\]/voice/route.test.ts`
    2. `cd app && npm run build`

    Operator manual steps:
    3. Run seed if needed: `cd app && npx tsx scripts/seed-cenbrap-voice-config.ts`
    4. Log in as platform owner; open `/admin/quality/brands/{cenbrapClientProfileId}`
    5. Confirm reviewStatus `approved`, source `seeded`, all voice sections visible, no edit controls
    6. As non-owner: confirm forbidden on page/API

    Resume signal: Type "approved" or describe UI issues.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/\[clientProfileId\]/voice/route.test.ts && cd app && npm run build</automated>
  </verify>
  <done>Platform owner confirms read-only voice inspect UI is legible and correctly gated.</done>
  <what-built>Read-only brand voice page at `/admin/quality/brands/[clientProfileId]` with structured section display and no edit controls.</what-built>
  <how-to-verify>
    1. Run seed script if needed: `cd app && npx tsx scripts/seed-cenbrap-voice-config.ts`
    2. Log in as platform owner
    3. Navigate to `/admin/quality/brands/{cenbrapClientProfileId}`
    4. Confirm: reviewStatus shows `approved`, source shows `seeded`, all voice sections visible
    5. Confirm: no edit inputs or save actions present
    6. As non-owner (or incognito): page/API returns forbidden
  </how-to-verify>
  <resume-signal>Type "approved" or describe UI issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → admin voice API | Untrusted users must not read other brands' voice config |
| UI → API | Only platform owner session may fetch voice data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-05 | I | voice GET route | mitigate | `requirePlatformOwner` on every request; workspace resolved server-side |
| T-162-06 | I | admin voice page | mitigate | Client-side owner gate + API 403 as defense in depth |
</threat_model>

<verification>
- `cd app && npm test -- --run src/app/api/admin/quality/brands/\[clientProfileId\]/voice/route.test.ts`
- `cd app && npm run build`
- Human checkpoint 162-03-03 approved
</verification>

<success_criteria>
- VOICE-05: Owner inspects voice config without freeform editing capability.
- API and UI consistent on reviewStatus, source, and section content.
</success_criteria>

<output>
After completion, create `.planning/phases/162-per-brand-voice-configuration/162-03-SUMMARY.md`
</output>
