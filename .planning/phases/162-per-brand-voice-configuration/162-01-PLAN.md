---
phase: 162-per-brand-voice-configuration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0053_client_profile_olhar_config.sql
  - app/src/server/db/schema.ts
  - app/src/server/repositories/client-profile-olhar-config.ts
  - app/src/server/repositories/client-profile-olhar-config.test.ts
  - app/scripts/seed-cenbrap-voice-config.ts
  - app/src/server/ai/voices/voice-config-parity.test.ts
autonomous: true
requirements:
  - VOICE-01
  - VOICE-03
must_haves:
  truths:
    - "Structured voice/olhar configuration persists per clientProfileId with workspace isolation"
    - "Cenbrap clientProfileId has seeded config with review_status approved and source seeded"
    - "Prompt section built from DB config matches hardcoded CENBRAP_VOICE output"
  artifacts:
    - path: app/drizzle/0053_client_profile_olhar_config.sql
      provides: client_profile_olhar_config table with review_status and config JSONB
    - path: app/src/server/db/schema.ts
      provides: Drizzle model clientProfileOlharConfig
    - path: app/src/server/repositories/client-profile-olhar-config.ts
      provides: getOlharVoiceConfigByClientProfileId insert/upsert helpers
    - path: app/scripts/seed-cenbrap-voice-config.ts
      provides: idempotent Cenbrap seed from CENBRAP_VOICE
  key_links:
    - from: app/scripts/seed-cenbrap-voice-config.ts
      to: app/src/server/repositories/client-profile-olhar-config.ts
      via: upsert with CENBRAP_VOICE fields
      pattern: "review_status.*approved"
    - from: app/src/server/repositories/client-profile-olhar-config.ts
      to: app/drizzle/0053_client_profile_olhar_config.sql
      via: Drizzle schema mapping
      pattern: "client_profile_olhar_config"
---

<objective>
Create the `client_profile_olhar_config` persistence layer and seed Cenbrap voice from `CENBRAP_VOICE` with approved review status so downstream generation can resolve voice by `clientProfileId`.

Purpose: Replace hardcoded-only voice with durable per-brand storage (D-01 table decision).
Output: Migration, repository, seed script, parity tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-per-brand-voice-configuration/162-CONTEXT.md
@.planning/phases/162-per-brand-voice-configuration/162-RESEARCH.md
@.planning/phases/162-per-brand-voice-configuration/162-VALIDATION.md
@.planning/REQUIREMENTS.md
@app/src/server/ai/voices/cenbrap.ts
@app/src/server/repositories/client-learning-proposal.ts

<interfaces>
From `app/src/server/ai/voices/cenbrap.ts`:
```typescript
export interface ClientVoice {
  id: string;
  displayName: string;
  matchTerms: string[];
  principles: string[];
  positiveSignals: string[];
  negativeSignals: string[];
  authorityAndClaims: string[];
  inviteRhythm: string[];
  correctButSoulless: string[];
  buildPromptSection(): string[];
}
export const CENBRAP_VOICE: ClientVoice;
```

Repository pattern (from `client-learning-proposal.ts`): Drizzle `db.insert().values().returning()`, filters by `(workspaceId, clientProfileId)`.
</interfaces>
</context>

<tasks>

<task id="162-01-01" type="auto">
  <name>Migration, schema, and repository for olhar voice config</name>
  <files>app/drizzle/0053_client_profile_olhar_config.sql, app/src/server/db/schema.ts, app/src/server/repositories/client-profile-olhar-config.ts, app/src/server/repositories/client-profile-olhar-config.test.ts</files>
  <action>
    Per D-01 (dedicated `client_profile_olhar_config` table 1:1 with `client_profiles`):

    1. Add migration `0053_client_profile_olhar_config.sql` in `adscale_app` schema with:
       - `client_profile_id` UUID PK FK → `client_profiles(id)` ON DELETE CASCADE
       - `workspace_id` UUID NOT NULL FK → `workspaces(id)`
       - `voice_id` TEXT NOT NULL, `display_name` TEXT NOT NULL
       - `config` JSONB NOT NULL (ClientVoice-shaped: principles, positiveSignals, negativeSignals, authorityAndClaims, inviteRhythm, correctButSoulless; matchTerms optional for display only)
       - `review_status` TEXT NOT NULL CHECK (`pending_review`|`approved`|`changes_requested`)
       - `source` TEXT NOT NULL DEFAULT `seeded`
       - `approved_at`, `approved_by`, `created_at`, `updated_at`
       - Unique index on `(workspace_id, client_profile_id)`

    2. Add `clientProfileOlharConfig` table + exported types to `schema.ts` following `clientLearningProposals` conventions.

    3. Create repository with:
       - `getOlharVoiceConfigByClientProfileId({ workspaceId, clientProfileId })` → row | null
       - `upsertOlharVoiceConfig(input)` for seed script
       - Workspace isolation: queries always filter by `workspaceId`

    4. Unit tests: insert + fetch, null when missing, wrong workspace returns null.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/repositories/client-profile-olhar-config.test.ts</automated>
  </verify>
  <done>VOICE-01 satisfied: structured config persists per clientProfileId with status metadata.</done>
</task>

<task id="162-01-02" type="auto">
  <name>Cenbrap seed script and prompt parity golden test</name>
  <files>app/scripts/seed-cenbrap-voice-config.ts, app/src/server/ai/voices/voice-config-parity.test.ts, app/src/server/ai/voices/client-voice.ts</files>
  <action>
    Per D-03 (seed from CENBRAP_VOICE with review_status approved):

    1. Add `app/scripts/seed-cenbrap-voice-config.ts` (follow `seed-cenbrap-calibration-corpus.ts` patterns):
       - Find Cenbrap `client_profiles` row per workspace via `name ILIKE '%cenbrap%'` (Claude discretion on exact query)
       - Upsert config JSONB from `CENBRAP_VOICE` fields (exclude runtime `buildPromptSection` method; store arrays only)
       - Set `voice_id: cenbrap`, `review_status: approved`, `source: seeded`, `approved_at: now()`
       - Idempotent: safe to re-run

    2. Add `buildClientVoiceFromConfig(row)` helper in `client-voice.ts` (or colocated module) that reconstructs a `ClientVoice`-like object with `buildPromptSection()` matching `CENBRAP_VOICE.buildPromptSection()` logic.

    3. Parity test (`voice-config-parity.test.ts`): golden compare — prompt lines from DB-shaped fixture must deep-equal `CENBRAP_VOICE.buildPromptSection()`.

    Do NOT wire generation path yet (Plan 02).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/voices/voice-config-parity.test.ts</automated>
  </verify>
  <done>VOICE-03 satisfied: seeded Cenbrap config produces identical prompt section to pre-migration hardcode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → repository | Untrusted rows must not bypass workspace isolation |
| Seed script → DB | Operator script must not overwrite approved configs without explicit upsert semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | I | client-profile-olhar-config repository | mitigate | Always filter by workspaceId; FK cascade on profile delete |
| T-162-02 | T | seed script | mitigate | Idempotent upsert; set source=seeded; log affected profile ids |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/repositories/client-profile-olhar-config.test.ts src/server/ai/voices/voice-config-parity.test.ts`
- Migration applies cleanly on fresh schema
</verification>

<success_criteria>
- VOICE-01: Table stores match terms (optional in config), prompt lines, and review status per clientProfileId.
- VOICE-03: Cenbrap parity test green; seed script documented in script header.
</success_criteria>

<output>
After completion, create `.planning/phases/162-per-brand-voice-configuration/162-01-SUMMARY.md`
</output>
