---
phase: 162-per-brand-voice-configuration
plan: "02"
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - app/src/server/ai/voices/voice-config-resolver.ts
  - app/src/server/ai/voices/voice-config-resolver.test.ts
  - app/src/server/ai/voices/client-voice.ts
  - app/src/server/ai/voices/voice-review-gate.ts
  - app/src/server/ai/olhar/generation-direction.ts
  - app/src/server/ai/olhar/generation-direction.test.ts
  - app/src/server/ai/prompt-builder.ts
autonomous: true
requirements:
  - VOICE-02
  - VOICE-04
must_haves:
  truths:
    - "Generation resolves brand voice by clientProfileId, not campaign name string matching"
    - "Missing voice config yields no voice overlay (generation continues with Olhar global only)"
    - "Cenbrap profile with seeded approved config injects CLIENT VOICE section"
    - "resolveClientVoice is not called from generation-direction or prompt-builder"
  artifacts:
    - path: app/src/server/ai/voices/voice-config-resolver.ts
      provides: resolveVoiceForClientProfile(workspaceId, clientProfileId)
    - path: app/src/server/ai/olhar/generation-direction.ts
      provides: profile-based voice injection
    - path: app/src/server/ai/voices/voice-review-gate.ts
      provides: DB-backed review_status check
  key_links:
    - from: app/src/server/ai/olhar/generation-direction.ts
      to: app/src/server/ai/voices/voice-config-resolver.ts
      via: resolveVoiceForClientProfile
      pattern: "resolveVoiceForClientProfile"
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/olhar/generation-direction.ts
      via: clientProfileId + workspaceId on GenerationDirectionInput
      pattern: "clientProfileId"
    - from: app/src/server/ai/voices/voice-review-gate.ts
      to: app/src/server/repositories/client-profile-olhar-config.ts
      via: review_status lookup
      pattern: "review_status"
---

<objective>
Wire generation to resolve brand voice by `clientProfileId`, generalize the review gate to DB `review_status`, and remove string-matching from the generation path.

Purpose: Fulfill D-02 resolution decision and VOICE-04 deprecation.
Output: Profile-based resolver, updated generation-direction + prompt-builder, generalized review gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-per-brand-voice-configuration/162-CONTEXT.md
@.planning/phases/162-per-brand-voice-configuration/162-RESEARCH.md
@.planning/phases/162-per-brand-voice-configuration/162-01-SUMMARY.md
@app/src/server/ai/olhar/generation-direction.ts
@app/src/server/ai/voices/voice-review-gate.ts
@app/src/server/ai/voices/client-voice.ts
@app/src/server/ai/prompt-builder.ts

<interfaces>
From Plan 01 (expected exports):
```typescript
// app/src/server/repositories/client-profile-olhar-config.ts
export async function getOlharVoiceConfigByClientProfileId(params: {
  workspaceId: string;
  clientProfileId: string;
}): Promise<ClientProfileOlharConfig | null>;

// app/src/server/ai/voices/client-voice.ts
export function buildClientVoiceFromConfig(row: ClientProfileOlharConfig): ClientVoice;
```

Current generation input (`generation-direction.ts`):
```typescript
export interface GenerationDirectionInput {
  contract: CreativeContract;
  campaign?: { name?, client?, product?, ... } | null;
  generationMode: "art_variation" | "format_adaptation" | "restyling";
  allowClientVoice?: boolean;
}
```

Campaign DB already has `clientProfileId`; `prompt-builder.ts` Campaign interface needs extension.
</interfaces>
</context>

<tasks>

<task id="162-02-01" type="auto" tdd="true">
  <name>Profile-based voice resolver and generation-direction wiring</name>
  <files>app/src/server/ai/voices/voice-config-resolver.ts, app/src/server/ai/voices/voice-config-resolver.test.ts, app/src/server/ai/olhar/generation-direction.ts, app/src/server/ai/olhar/generation-direction.test.ts, app/src/server/ai/prompt-builder.ts</files>
  <behavior>
    - resolveVoiceForClientProfile with valid Cenbrap profileId returns voice; prompt contains "CLIENT VOICE — Cenbrap"
    - resolveVoiceForClientProfile with unknown profileId returns null; no CLIENT VOICE section
    - Campaign name "CENBRAP NR1" without clientProfileId does NOT inject voice (no string fallback)
    - Missing clientProfileId: no voice overlay, no throw
  </behavior>
  <action>
    Per D-02 (resolve by clientProfileId only; no config = no overlay):

    1. Implement `resolveVoiceForClientProfile({ workspaceId, clientProfileId })` in `voice-config-resolver.ts`:
       - Returns `ClientVoice | null` via repository + `buildClientVoiceFromConfig`
       - No `matchTerms` / campaign name logic

    2. Extend `GenerationDirectionInput` with optional `workspaceId` and `clientProfileId`.

    3. Replace `resolveClientVoice({ name, client, product })` in `buildGenerationDirectionSection` with profile resolver. When `clientProfileId` or `workspaceId` absent, skip voice (no string fallback).

    4. Extend `Campaign` interface in `prompt-builder.ts` with `clientProfileId?: string | null`. Pass `workspaceId: campaign.workspaceId` and `clientProfileId: campaign.clientProfileId` into `buildGenerationDirectionSection`.

    5. Update `generation-direction.test.ts`:
       - Remove/replace tests that expect Cenbrap match via campaign name alone
       - Add tests mocking resolver: approved config injects; missing profile skips
       - Spy/mock `resolveClientVoice` to assert NOT called from generation-direction
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/voices/voice-config-resolver.test.ts src/server/ai/olhar/generation-direction.test.ts</automated>
  </verify>
  <done>VOICE-02 satisfied: generation uses clientProfileId lookup exclusively.</done>
</task>

<task id="162-02-02" type="auto">
  <name>Generalize review gate and deprecate string-matching resolver</name>
  <files>app/src/server/ai/voices/voice-review-gate.ts, app/src/server/ai/voices/client-voice.ts, app/src/server/ai/voices/client-voice.test.ts</files>
  <action>
    Per D-03 seed (approved Cenbrap) and VOICE-04:

    1. Refactor `isClientVoiceInjectionAllowed` to accept `{ voiceId, clientProfileId, workspaceId, reviewStatus? }`:
       - When `reviewStatus` provided (from DB row), allow injection iff `reviewStatus === 'approved'` or `forceApproved`
       - Remove hardcoded `CENBRAP_VOICE_REVIEW_STATUS === 'pending_review'` block for generation path
       - Keep `forceApproved` for operator/test override

    2. Update `generation-direction.ts` to pass `reviewStatus` from config row into review gate.

    3. Mark `resolveClientVoice` and `REGISTERED_VOICES` as `@deprecated` in `client-voice.ts`. Keep function for unit tests only — NOT imported by generation-direction or prompt-builder.

    4. Update `client-voice.test.ts`: keep deprecated resolver tests under `describe.skip` or `describe('deprecated resolveClientVoice')` documenting legacy behavior; ensure no generation tests depend on string match.

    5. Grep guard: `resolveClientVoice` must not appear in `generation-direction.ts` or `prompt-builder.ts` after this task.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/voices/ && ! rg "resolveClientVoice" app/src/server/ai/olhar/generation-direction.ts app/src/server/ai/prompt-builder.ts</automated>
  </verify>
  <done>VOICE-04 satisfied: hardcode string-matching removed from generation path; resolver deprecated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generation → DB voice config | Untrusted clientProfileId must not leak cross-workspace voice |
| Review gate | pending_review config must not inject without forceApproved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-03 | I | voice-config-resolver | mitigate | Repository enforces workspaceId filter on every lookup |
| T-162-04 | E | voice-review-gate | mitigate | Only `approved` review_status allows injection; pending_review blocked |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/ai/voices/ src/server/ai/olhar/generation-direction.test.ts`
- `cd app && npm run build`
</verification>

<success_criteria>
- VOICE-02: Profile-based resolution with null-safe missing config.
- VOICE-04: No `resolveClientVoice` in generation path; deprecated marker on legacy function.
- Cenbrap seeded+approved config injects voice when campaign has matching clientProfileId.
</success_criteria>

<output>
After completion, create `.planning/phases/162-per-brand-voice-configuration/162-02-SUMMARY.md`
</output>
