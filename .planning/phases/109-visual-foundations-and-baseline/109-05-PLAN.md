---
phase: 109-visual-foundations-and-baseline
plan: "05"
type: execute
wave: 5
depends_on:
  - 109-04
files_modified:
  - app/src/components/ui/dialog.tsx
  - app/src/components/ui/sheet.tsx
  - app/src/components/ui/dropdown-menu.tsx
  - app/src/components/ui/select.tsx
  - app/src/components/ui/tooltip.tsx
  - app/src/components/ui/visual-overlays.test.tsx
  - app/scripts/check-visual-contract.mjs
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
must_haves:
  truths:
    - Dialog, sheet, dropdown, select, and tooltip consume canonical surface, geometry, motion, and layer roles.
    - Existing Base UI exports, props, portals, keyboard close, focus containment, and focus return remain unchanged.
    - Popovers use the popover layer; dialog/sheet backdrops and content use backdrop/overlay layers with no local global escalation.
    - No basic control, route, shell, provider, hook, API, or business file changes in this plan.
  artifacts:
    - path: app/src/components/ui/visual-overlays.test.tsx
      provides: Public interaction and layer regression coverage for overlay primitives.
    - path: app/src/components/ui/dialog.tsx
      provides: Canonical backdrop/overlay contract proof.
    - path: app/src/components/ui/dropdown-menu.tsx
      provides: Canonical popover-layer proof consumed by the authenticated shell.
---

# Plan 109-05: Overlay Primitives

<objective>
Converge only shared overlay primitives on the canonical layer and geometry contract, preserving Base UI behavior before browser stress validation.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/109-visual-foundations-and-baseline/109-04-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-RESEARCH.md
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
@app/src/app/globals.css
@app/src/components/ui/dialog.tsx
@app/src/components/ui/sheet.tsx
@app/src/components/ui/dropdown-menu.tsx
@app/src/components/ui/select.tsx
@app/src/components/ui/tooltip.tsx
</context>

<tasks>

<task id="109-05-01" type="auto">
  <name>Normalize dialog and sheet contracts</name>
  <files>app/src/components/ui/dialog.tsx, app/src/components/ui/sheet.tsx, app/src/components/ui/visual-overlays.test.tsx</files>
  <action>
Before changing any declared file, run `node app/scripts/check-plan-scope.mjs begin --plan 109-05 --plan-file .planning/phases/109-visual-foundations-and-baseline/109-05-PLAN.md` and protected verify-only mode. Then use public-interface tests to update Dialog and Sheet wrappers to canonical overlay surface, border, radius, shadow, motion, backdrop/overlay layers, scroll regions, and mobile geometry. Preserve all exports, slots, props, portals, close controls, open-state hooks, and className overrides. Test accessible role/name, trigger opens content, Escape closes, focus containment, scroll behavior, and focus return.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-overlays.test.tsx -t "dialog|sheet|focus return|escape" &amp;&amp; node scripts/check-visual-contract.mjs --section layers</automated>
  </verify>
  <done>Dialog and sheet behavior remains compatible and their backdrop/content use canonical layers.</done>
</task>

<task id="109-05-02" type="auto">
  <name>Normalize dropdown, select, and tooltip popover contracts</name>
  <files>app/src/components/ui/dropdown-menu.tsx, app/src/components/ui/select.tsx, app/src/components/ui/tooltip.tsx, app/src/components/ui/visual-overlays.test.tsx</files>
  <action>
Move dropdown, select, and tooltip wrappers to canonical popover surface, border, radius, floating shadow, motion, and popover layer. Preserve existing exports, props, portal placement, keyboard selection, accessible naming, collision handling, and caller overrides. Add tests for opening, keyboard operation, selection, dismissal, and layer class/token consumption.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-overlays.test.tsx -t "dropdown|select|tooltip|popover" &amp;&amp; node scripts/check-visual-contract.mjs --section layers</automated>
  </verify>
  <done>Popover primitives preserve interaction behavior and use the canonical popover layer without raw global z-index values.</done>
</task>

<task id="109-05-03" type="auto">
  <name>Run overlay consumer compatibility regressions</name>
  <files>app/src/components/ui/visual-overlays.test.tsx</files>
  <action>
Run focused overlay tests plus existing consumers that exercise dropdown/dialog/sheet behavior, including `TopBar.test.tsx`, `FeedbackModal.test.tsx`, and `derivation-flow-regression.test.tsx`. Fix only shared overlay implementation regressions. Do not edit those consumer tests/components unless an existing shared API was accidentally broken; preserve route and business behavior.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-overlays.test.tsx src/components/layout/TopBar.test.tsx src/components/feedback/FeedbackModal.test.tsx src/components/workspace/derivation-flow-regression.test.tsx</automated>
  </verify>
  <done>Representative overlay consumers remain green with no public API or workflow change.</done>
</task>

<task id="109-05-04" type="auto">
  <name>Close overlay scope, enforcement, and protected-worktree checks</name>
  <files>app/scripts/check-visual-contract.mjs, app/src/components/ui/visual-overlays.test.tsx</files>
  <action>
Reject new raw z-index escalation, undefined layer tokens, layout-property animation, decorative glass defaults, and route-local changes in this plan. Audit the changed file list to ensure it contains only declared overlay/checker/test paths. Verify the protected dirty snapshot after lint and tests.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; node scripts/check-visual-contract.mjs &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-overlays.test.tsx &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-05 &amp;&amp; git diff --check -- app/src/components/ui app/scripts/check-visual-contract.mjs</automated>
  </verify>
  <done>Overlay primitives and consumers are green, scope is bounded, and protected files remain identical to the snapshot.</done>
</task>

</tasks>

<verification>
Run overlay unit/interaction tests, existing consumer regressions, layer/static checks, lint, dirty snapshot verification, and changed-file audit.
</verification>

<completion_criteria>
- FOUND-04 and overlay portions of FOUND-05 have executable evidence.
- Base UI behavior and public APIs remain compatible.
- Overlay layers follow the single canonical order without route migration.
- No basic-control, route, shell, or business files are changed.
- Protected dirty files remain unchanged.
</completion_criteria>
