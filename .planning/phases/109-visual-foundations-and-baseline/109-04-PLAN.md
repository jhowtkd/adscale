---
phase: 109-visual-foundations-and-baseline
plan: "04"
type: execute
wave: 4
depends_on:
  - 109-03
files_modified:
  - app/src/components/ui/button.tsx
  - app/src/components/ui/input.tsx
  - app/src/components/ui/textarea.tsx
  - app/src/components/ui/badge.tsx
  - app/src/components/ui/StatusBadge.tsx
  - app/src/components/ui/table.tsx
  - app/src/components/ui/skeleton.tsx
  - app/src/components/ui/EmptyState.tsx
  - app/src/components/ui/visual-foundations.test.tsx
  - app/scripts/check-visual-contract.mjs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-05
must_haves:
  truths:
    - Basic controls and data-state primitives consume canonical tokens while preserving every existing export, prop, variant, size, and caller behavior.
    - Button, field, badge/status, table, skeleton, and empty-state primitives share one compact professional vocabulary.
    - Empty, loading, error, disabled, invalid, and status meaning remain accessible and are not communicated by color alone.
    - No overlay, route, shell, provider, hook, API, or business file changes in this plan.
  artifacts:
    - path: app/src/components/ui/visual-foundations.test.tsx
      provides: Public-interface tests for basic controls and data states.
    - path: app/src/components/ui/StatusBadge.tsx
      provides: Typed product statuses mapped to semantic state families.
    - path: app/src/components/ui/table.tsx
      provides: Compact, semantically correct, horizontally contained data primitive.
---

# Plan 109-04: Basic Controls and Data States

<objective>
Prove the canonical foundation through bounded basic controls and data-state primitives, preserving public APIs and deferring all overlay behavior to Plan 109-05.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/109-visual-foundations-and-baseline/109-03-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-RESEARCH.md
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
@app/src/app/globals.css
@app/src/components/ui/button.tsx
@app/src/components/ui/input.tsx
@app/src/components/ui/textarea.tsx
@app/src/components/ui/badge.tsx
@app/src/components/ui/StatusBadge.tsx
@app/src/components/ui/table.tsx
@app/src/components/ui/skeleton.tsx
@app/src/components/ui/EmptyState.tsx
</context>

<tasks>

<task id="109-04-01" type="auto">
  <name>Normalize buttons and fields through public behavior tests</name>
  <files>app/src/components/ui/button.tsx, app/src/components/ui/input.tsx, app/src/components/ui/textarea.tsx, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Before changing any declared file, run `node app/scripts/check-plan-scope.mjs begin --plan 109-04 --plan-file .planning/phases/109-visual-foundations-and-baseline/109-04-PLAN.md` and protected verify-only mode. Then use vertical behavior slices to cover Button current variants/sizes, icon naming, disabled/loading/aria-invalid/focus-visible semantics, and Input/Textarea placeholder, mobile 16px text, compact desktop density, disabled and invalid states. Replace primitive-local raw heights, radii, durations, focus colors, and state colors with canonical roles. Preserve exports, Base UI behavior, default variants, size names, props, and className extension. Do not touch callers.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "button|input|textarea" &amp;&amp; node scripts/check-visual-contract.mjs --section dialect</automated>
  </verify>
  <done>Buttons and fields pass public state/accessibility tests through unchanged APIs and canonical roles.</done>
</task>

<task id="109-04-02" type="auto">
  <name>Normalize badge and typed status semantics</name>
  <files>app/src/components/ui/badge.tsx, app/src/components/ui/StatusBadge.tsx, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Map neutral/success/warning/danger/info variants and every product status to the canonical semantic families. Preserve existing props and typed status names. Add tests proving visible text plus dot/icon/non-color meaning, className compatibility, light/dark role usage, and accessible names where applicable.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "badge|status" &amp;&amp; node scripts/check-visual-contract.mjs --section tokens</automated>
  </verify>
  <done>Badge and status primitives express semantic meaning consistently without color-only communication.</done>
</task>

<task id="109-04-03" type="auto">
  <name>Normalize table, skeleton, empty, loading, and error proof states</name>
  <files>app/src/components/ui/table.tsx, app/src/components/ui/skeleton.tsx, app/src/components/ui/EmptyState.tsx, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Make Table use canonical compact density, borders, text roles, tabular numbers, and explicit horizontal containment while preserving table semantics and exports. Make Skeleton use canonical tonal surfaces and reduced-motion-safe animation. Make EmptyState use open/tonal structure and canonical Button actions. Add tests for long table content, semantic roles, skeleton geometry/motion, empty heading/description/action, deterministic loading geometry, recoverable error composition using the same primitives, and className compatibility. Do not migrate route states.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "table|skeleton|empty state|loading state|error state" &amp;&amp; node scripts/check-visual-contract.mjs</automated>
  </verify>
  <done>Data, empty, loading, and error proof states share canonical geometry and accessible recovery behavior.</done>
</task>

<task id="109-04-04" type="auto">
  <name>Close basic-control compatibility and protected-worktree checks</name>
  <files>app/scripts/check-visual-contract.mjs, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Keep debt allowlists exact and narrow. Reject new deprecated aliases, raw global layers, arbitrary type/radius, decorative glass defaults, side stripes, gradient text, or layout-property animation in changed primitives. Audit changed files to ensure no overlay or route files entered this plan. Verify the protected dirty snapshot after all tests.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; node scripts/check-visual-contract.mjs &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "button|input|textarea|badge|status|table|skeleton|empty state|loading state|error state" &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-04 &amp;&amp; git diff --check -- app/src/components/ui app/scripts/check-visual-contract.mjs</automated>
  </verify>
  <done>Basic controls/data states are green, scope is bounded, and protected dirty files are byte/index/status identical to the snapshot.</done>
</task>

</tasks>

<verification>
Run all basic-control/data-state tests, checker self-tests, full visual contract check, lint, dirty snapshot verification, and changed-file audit. No overlay files may appear.
</verification>

<completion_criteria>
- FOUND-01, FOUND-02, and basic-control/data-state portions of FOUND-05 have executable evidence.
- Public APIs and behavior remain compatible.
- Empty, loading, and error primitive states are explicitly covered.
- No overlay, route, or business migration occurs.
- Protected dirty files remain unchanged in HEAD/index/worktree/status terms.
</completion_criteria>
