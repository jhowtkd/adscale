---
phase: 109-visual-foundations-and-baseline
plan: "06"
type: execute
wave: 6
depends_on:
  - 109-05
files_modified:
  - app/tests/e2e/visual-foundations.spec.ts
  - app/tests/e2e/support/visual-layer-harness.ts
  - app/scripts/check-visual-evidence.mjs
  - .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json
  - .planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md
  - .planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
  - .planning/phases/109-visual-foundations-and-baseline/109-VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - QA-14
must_haves:
  truths:
    - Post-change browser evidence exactly pairs every required populated, empty, loading, error, overlay, viewport, theme, and locale key from the before matrix.
    - Structured validation rejects missing artifacts, CSS hash mismatch, malformed rows, failed/blocked/pending results, invalid owners, missing masks, or incomplete matrices.
    - Existing TopBar account dropdown, Settings Brand Kit delete confirmation, and workspace derivation-review sheet triggers prove real app behavior; the named Playwright-only layer harness proves deterministic combined layer ordering without shipping publicly.
    - Full quality gates and protected dirty-state comparison are recorded honestly.
  artifacts:
    - path: .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json
      provides: Complete structured before/after/final evidence source of truth.
    - path: .planning/phases/109-visual-foundations-and-baseline/109-VERIFICATION.md
      provides: Goal-backward Phase 109 conclusions generated from validated evidence.
    - path: app/scripts/check-visual-evidence.mjs
      provides: CSS hash, matrix, artifact hash, result-state, mask, owner, and evidence-schema enforcement.
---

# Plan 109-06: Browser Proof and Validation Closure

<objective>
Pair structured before/after evidence, prove real and synthetic overlay compositions, close all requirements, and run the complete release-quality gate without expanding into route remediation.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/109-visual-foundations-and-baseline/109-01-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-02-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-03-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-04-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-05-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-OWNERSHIP.md
@.planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
</context>

<tasks>

<task id="109-06-01" type="auto">
  <name>Capture complete paired post-change route and state matrix</name>
  <files>app/tests/e2e/visual-foundations.spec.ts, app/scripts/check-visual-evidence.mjs, .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json, .planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md</files>
  <action>
Before changing any declared file, run `node app/scripts/check-plan-scope.mjs begin --plan 109-06 --plan-file .planning/phases/109-visual-foundations-and-baseline/109-06-PLAN.md` and protected verify-only mode. Then run the exact before matrix with `-after.png` artifacts using the same synthetic fixture IDs, scenario/state keys, viewport, theme, locale, request interceptions, and masks. Cover populated, dense, empty, loading, error, and validation-error states. Assert no document overflow, heading/primary action visibility, main/fixed-nav nonintersection, focus visibility, no unexpected browser errors, unchanged route/action availability, and bounded vs wide behavior at 1920. Existing defects may defer only to one owner 110-113 and only when not worsened.

Extend `check-visual-evidence.mjs --stage after` to require one-to-one before/after keys, distinct before/current CSS hashes, exact current CSS hash, artifact SHA-256 integrity, synthetic reserved-domain identity, all required identity/client masks, and pass/deferred results only. Generate `109-BASELINE.md` from validated JSON.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx playwright test --config playwright.visual.config.ts visual-foundations.spec.ts --grep "foundation matrix|empty state|loading state|error state" &amp;&amp; cd .. &amp;&amp; node app/scripts/check-visual-evidence.mjs --stage after</automated>
  </verify>
  <done>Every required before key has a validated after pair and no new, worsened, unmasked, malformed, or unowned regression exists.</done>
</task>

<task id="109-06-02" type="auto">
  <name>Prove real overlay triggers and deterministic combined layer stress</name>
  <files>app/tests/e2e/visual-foundations.spec.ts, app/tests/e2e/support/visual-layer-harness.ts, .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json</files>
  <action>
Use concrete existing triggers: TopBar account button opens `DropdownMenu`; Settings → Brand Kit delete action in `BrandKitTab.tsx` opens `ConfirmDialog`; a populated campaign workspace derivation Preview/Review action opens `DerivationReviewSheet`. Verify Escape, focus containment where modal, and return to each trigger. For combined states unavailable through normal product flows, import `mountVisualLayerHarness(page)` from the Playwright-only support file. It uses `page.setContent()` with no app route/import and receives layer values extracted from `globals.css`; compose sticky+popover+toast and shell+backdrop+overlay+toast at 390 and 1440. Assert computed z-index order, stacking-context ancestry, bounding boxes, and no shell control above backdrop. Record structured evidence and artifact hashes.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx playwright test --config playwright.visual.config.ts visual-foundations.spec.ts --grep "account dropdown|settings confirmation dialog|derivation review sheet|layer harness" &amp;&amp; cd .. &amp;&amp; node app/scripts/check-visual-evidence.mjs --stage after --section overlays</automated>
  </verify>
  <done>Real app overlay behavior and deterministic combined layer ordering pass at mobile and desktop without a public test route.</done>
</task>

<task id="109-06-03" type="auto">
  <name>Complete structured requirement evidence and verification documents</name>
  <files>app/scripts/check-visual-evidence.mjs, .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json, .planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md, .planning/phases/109-visual-foundations-and-baseline/109-VERIFICATION.md</files>
  <action>
Populate structured requirement rows for FOUND-01 through FOUND-05 and QA-14 with exact automated command IDs, browser scenario IDs, manual review IDs, artifact hashes, and result `pass`. `check-visual-evidence.mjs --stage final` must reject absent/duplicate requirements, `pending`, `failed`, `blocked`, unknown result values, malformed command records, missing artifacts, CSS hash mismatch, incomplete matrix, invalid owner 114, or missing dirty-snapshot verification. Generate human-readable validation/verification summaries from the validated JSON, including compact-not-compressed, restrained green, tonal planes, actionable cards, light/dark, PT-BR/EN, focus/touch, and no business behavior change.
  </action>
  <verify>
    <automated>node app/scripts/check-visual-ownership.mjs &amp;&amp; node app/scripts/check-visual-evidence.mjs --stage final &amp;&amp; cd app &amp;&amp; node scripts/check-visual-contract.mjs &amp;&amp; cd .. &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-06</automated>
  </verify>
  <done>Structured evidence accepts exactly six passing requirements and generated documents contain no unsupported completion claims.</done>
</task>

<task id="109-06-04" type="auto">
  <name>Run full quality gate and final protected-state audit</name>
  <files>.planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json, .planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md, .planning/phases/109-visual-foundations-and-baseline/109-VERIFICATION.md</files>
  <action>
Run checker self-tests, focused foundation/control/overlay tests, full test, lint, build, complete Playwright matrix, ownership, final structured evidence, protected dirty snapshot, and diff checks. Record exact exit codes and counts in structured command records. Any failure remains failed/blocked and prevents final validation; scoped reruns cannot convert a failed full gate to pass. Never stage or modify protected paths and use explicit file lists for any execution commit.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx src/components/ui/visual-overlays.test.tsx src/components/layout/TopBar.test.tsx &amp;&amp; node scripts/check-visual-contract.mjs &amp;&amp; npm test &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npx playwright test --config playwright.visual.config.ts visual-foundations.spec.ts &amp;&amp; cd .. &amp;&amp; node app/scripts/check-visual-ownership.mjs &amp;&amp; node app/scripts/check-visual-evidence.mjs --stage final &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-06 &amp;&amp; git diff --check</automated>
  </verify>
  <done>Every full gate is green and recorded; otherwise execution stops at a truthful blocker without modifying protected work.</done>
</task>

</tasks>

<verification>
Run Task 109-06-04 exactly. Final completion requires structured evidence pass, full matrix pairing, real and test-only overlay proof, and unchanged protected HEAD/index/worktree/status snapshot.
</verification>

<completion_criteria>
- FOUND-01 through FOUND-05 and QA-14 each have validated automated, browser, and manual evidence.
- Populated, dense, empty, loading, error, validation-error, and overlay states are paired before/after.
- CSS hash chronology and artifact hashes are structurally validated.
- Real triggers and Playwright-only layer harness prove overlay behavior without shipping a route.
- Full gates pass and protected dirty work remains identical.
</completion_criteria>
