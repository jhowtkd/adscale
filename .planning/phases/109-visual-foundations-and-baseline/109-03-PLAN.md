---
phase: 109-visual-foundations-and-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - 109-02
files_modified:
  - app/src/app/globals.css
  - app/scripts/check-visual-contract.mjs
  - app/scripts/check-visual-contract.test.mjs
  - app/src/components/ui/visual-foundations.test.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - Canonical semantic roles are the source of truth in light and dark themes, while live legacy names remain one-way compatibility aliases.
    - Typography, spacing, control density, radii, motion, shell geometry, content widths, gutters, sticky offsets, and layers have named shared contracts.
    - Tailwind semantic mappings point to canonical roles rather than deprecated hue aliases.
    - Existing route composition and business behavior remain unchanged.
    - New visual dialect violations fail deterministically while existing debt is allowlisted explicitly.
  artifacts:
    - path: app/src/app/globals.css
      provides: Canonical visual foundation contract and additive compatibility layer.
    - path: app/scripts/check-visual-contract.mjs
      provides: Token completeness, alias direction, undefined-variable, geometry, layer, and no-new-dialect enforcement.
    - path: app/src/components/ui/visual-foundations.test.tsx
      provides: Executable token, theme, density, geometry, motion, and layer contract proof.
  key_links:
    - from: app/src/app/globals.css
      to: Tailwind @theme inline roles
      via: Canonical canvas, surface, content, border, focus, accent, and semantic state variables.
    - from: app/scripts/check-visual-contract.mjs
      to: app/src/app/globals.css
      via: Parsed declarations/references and explicit debt allowlists.
---

# Plan 109-03: Canonical Foundation Contract

<objective>
Define the additive canonical visual contract that later phases can consume, preserving existing APIs and visual behavior closely while preventing further token, geometry, density, and layer drift.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/109-visual-foundations-and-baseline/109-02-PLAN.md
@.planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md
@.planning/phases/109-visual-foundations-and-baseline/109-CONTEXT.md
@.planning/phases/109-visual-foundations-and-baseline/109-RESEARCH.md
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
@PRODUCT.md
@DESIGN.md
@app/src/app/globals.css
</context>

<tasks>

<task id="109-03-01" type="auto">
  <name>Create Wave 0 contract tests and debt-aware static enforcement</name>
  <files>app/scripts/check-visual-contract.mjs, app/scripts/check-visual-contract.test.mjs, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Before changing any declared file, run `node app/scripts/check-plan-scope.mjs begin --plan 109-03 --plan-file .planning/phases/109-visual-foundations-and-baseline/109-03-PLAN.md`, then run protected verify-only mode and `node app/scripts/check-visual-evidence.mjs --stage before`; abort unless all pass and the recorded CSS hash equals the current file. Then create the Wave 0 checker, checker self-tests, and foundation test file. The static checker must parse CSS custom-property declarations/references and authenticated TS/TSX class usage. Encode the canonical token inventory from research; distinguish theme-specific from global contracts; permit approved external/font variables; and baseline existing debt by exact file plus rule. Fail on undefined CSS variables, missing canonical light/dark values, compatibility aliases that do not point one-way to canonical roles, new use of deprecated hue aliases, new raw global `z-*` levels, new arbitrary authenticated font/radius values, pure `#000`/`#fff` canonical values, and missing reduced-motion/motion/layer contracts. Provide `--section tokens|geometry|layers|dialect` filters.

Add green `node:test` self-tests for checker parsing and diagnostics using temporary fixture strings/files: a valid minimal contract exits 0; a missing canonical token exits with a specific `MISSING_CANONICAL_TOKEN` diagnostic; undefined variables, reverse aliases, raw layers, and malformed CSS each produce distinct diagnostic codes; syntax/config/import failures remain non-assertion errors. Add a green `foundation test harness loads` Vitest smoke plus focused specifications for the real stylesheet; only the named smoke runs in this task, so expected missing-contract assertions cannot hide syntax/config/import failures.
  </action>
  <verify>
    <automated>node app/scripts/check-visual-evidence.mjs --stage before &amp;&amp; cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "foundation test harness loads"</automated>
  </verify>
  <done>The before-evidence CSS hash is valid, checker self-tests are green, diagnostic codes distinguish expected contract failures from harness failures, and the real-contract specifications load successfully.</done>
</task>

<task id="109-03-02" type="auto">
  <name>Implement canonical colors, semantic states, and additive aliases</name>
  <files>app/src/app/globals.css, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Refactor token declarations so canonical roles are primary: canvas; base/raised/inset/overlay surfaces; primary/secondary/muted/disabled/on-accent text; subtle/default/strong borders; focus ring; primary accent/hover/subtle/text; and neutral/success/warning/danger/info bg/border/text/dot families. Use OKLCH for new canonical values, tint extreme neutrals slightly toward the brand hue, match the existing visual character closely, and define required theme-specific values in both `:root` and `.dark`.

Map product statuses (draft, active, queued, processing, generating, completed, approved, rejected, failed) to semantic state families. Repoint `@theme inline` to canonical roles. Keep live names such as `--deep-bg`, `--border-dim`, `--border-medium`, and `--accent-green*` as one-way compatibility aliases. Keep deprecated blue/teal/purple/mint/pale/cream aliases only because callers still exist; mark them deprecated and never make a canonical role depend on them. Do not broadly recolor routes or remove aliases in this phase.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; node scripts/check-visual-contract.mjs --section tokens &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "tokens|themes|aliases|semantic states"</automated>
  </verify>
  <done>Canonical semantic roles pass in both themes, Tailwind points to them, and compatibility aliases preserve current consumers without reverse dependencies.</done>
</task>

<task id="109-03-03" type="auto">
  <name>Implement geometry, density, typography, radius, motion, and content-width contracts</name>
  <files>app/src/app/globals.css, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Add the researched 4px spacing scale, compact control heights plus 44px touch target contract, fixed product typography roles, tabular-number utility, control/panel/object/overlay/pill radii, product durations/easings, and floating/overlay shadows. Add shell geometry variables for mobile/desktop top bars, rail/sidebar, bottom navigation, sticky gap, safe-area-aware bottom offset, and responsive page gutters. Add named width recipes `reading`, `form`, `operational`, `workspace`, `wide`, and `fluid`, plus small reusable utilities that apply shared gutters and bounded widths without migrating routes.

Preserve existing utility/API behavior through aliases where necessary. Do not introduce `PageFrame`, `RouteHeader`, shell consumption, route migration, or viewport-fluid authenticated headings. Keep mobile form text at 16px and allow compact desktop text through media/pointer-aware rules. Keep cards meaningful and do not turn static sections into card utilities.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; node scripts/check-visual-contract.mjs --section geometry &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx -t "geometry|density|typography|radius|motion|content width"</automated>
  </verify>
  <done>All named foundation scales and width/shell contracts exist, are testable, and add no route-level composition changes.</done>
</task>

<task id="109-03-04" type="auto">
  <name>Implement the single global layer contract and close foundation checks</name>
  <files>app/src/app/globals.css, app/scripts/check-visual-contract.mjs, app/src/components/ui/visual-foundations.test.tsx</files>
  <action>
Define the complete ordered layer scale from base through skip-link. Add documented usage rules in stylesheet comments: feature content only base/raised/sticky; shell only shell/shell-floating; dropdown/select/tooltip popover; dialog/sheet backdrop and overlay; toast above overlay; tour and skip-link highest. Provide utility hooks compatible with Tailwind arbitrary-variable use and document `isolation: isolate` for local component internals.

Finish the explicit debt allowlist from the pre-change tree. It may permit existing raw `z-*`, deprecated hue aliases, and arbitrary type/radius values, but only at exact existing locations; changed or new violations fail. Ensure the global error styles consume canonical roles where technically possible without changing error-page behavior. Preserve reduced-motion handling and replace only foundation-level motion aliases, not route animations.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/check-visual-contract.test.mjs &amp;&amp; node scripts/check-visual-contract.mjs &amp;&amp; node scripts/check-visual-contract.mjs --section layers &amp;&amp; npx vitest run --config config/vitest.config.ts src/components/ui/visual-foundations.test.tsx &amp;&amp; npm run lint &amp;&amp; cd .. &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-03</automated>
  </verify>
  <done>The full canonical contract is green, existing debt is frozen rather than expanded, and no authenticated route or business code was migrated.</done>
</task>

</tasks>

<verification>
Run the complete visual contract checker, focused foundation test file, lint, `git diff --check`, and compare the changed file list against this plan. Do not run post-change screenshot approval yet; Plan 109-03 must first stabilize primitive consumers.
</verification>

<completion_criteria>
- FOUND-01 through FOUND-04 have executable token/geometry/density/layer evidence.
- Canonical roles are primary in light and dark; aliases remain additive and one-way.
- Existing visual debt cannot grow without an explicit reviewed allowlist update.
- No shell, route, data-flow, permission, or workflow behavior changes are introduced.
- Unrelated billing and preview-fix files are neither modified nor staged.
</completion_criteria>
