---
phase: 109-visual-foundations-and-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - 109-01
files_modified:
  - .planning/phases/109-visual-foundations-and-baseline/109-OWNERSHIP.md
  - .planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md
  - .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json
  - app/scripts/check-visual-ownership.mjs
  - app/scripts/check-visual-evidence.mjs
  - app/scripts/seed-visual-foundations.ts
  - app/tests/e2e/visual-foundations.spec.ts
  - app/tests/e2e/support/visual-layer-harness.ts
  - app/playwright.visual.config.ts
autonomous: true
requirements:
  - QA-14
must_haves:
  truths:
    - Every authenticated route and every visible shared component family has exactly one implementation owner in Phases 109-113 or an explicit out-of-scope rationale; Phase 114 is only a browser scenario/release-gate target.
    - Every owned route and family has at least one named Phase 114 browser scenario.
    - Representative authenticated screenshots and runtime observations are captured before app/src/app/globals.css changes.
    - Fixtures use a synthetic `example.test` identity, deterministic populated/empty/loading/error states, and contain no production customer data, access tokens, personal emails, or visible client identity.
    - Existing billing and preview-fix work retains identical HEAD blob, index blob, worktree hash, and porcelain-v2 status throughout Phase 109.
  artifacts:
    - path: .planning/phases/109-visual-foundations-and-baseline/109-OWNERSHIP.md
      provides: Canonical route and visible component-family ownership matrix with Phase 114 scenarios.
    - path: .planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md
      provides: Before-change fixture, viewport, theme, locale, screenshot, and observed-defect ledger.
    - path: app/scripts/check-visual-ownership.mjs
      provides: Deterministic QA-14 uniqueness and completeness validator.
    - path: app/tests/e2e/visual-foundations.spec.ts
      provides: Authenticated baseline preflight and before-capture harness.
    - path: .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json
      provides: Machine-readable CSS hash, scenario matrix, artifact, result, and dirty-state evidence.
  key_links:
    - from: app/scripts/check-visual-ownership.mjs
      to: .planning/phases/109-visual-foundations-and-baseline/109-OWNERSHIP.md
      via: Parsed route and family rows validated against the live authenticated route manifest.
    - from: app/tests/e2e/visual-foundations.spec.ts
      to: .planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md
      via: Stable scenario IDs, fixture IDs, viewport/theme/locale variants, and screenshot paths.
---

# Plan 109-02: Inventory and Pre-change Baseline

<objective>
Freeze Phase 109 ownership and deterministic browser evidence before any global visual contract changes, so later phases have unique responsibility and before/after comparisons remain trustworthy.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-visual-foundations-and-baseline/109-CONTEXT.md
@.planning/phases/109-visual-foundations-and-baseline/109-RESEARCH.md
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
@PRODUCT.md
@DESIGN.md
@app/playwright.config.ts
@app/tests/e2e/restyle.spec.ts
@app/scripts/seed-dev-admin.ts
@app/scripts/seed-testsprite.ts
</context>

<tasks>

<task id="109-02-01" type="auto">
  <name>Create the exhaustive route and component-family ownership contract</name>
  <files>.planning/phases/109-visual-foundations-and-baseline/109-OWNERSHIP.md, app/scripts/check-visual-ownership.mjs</files>
  <read_first>
    - Enumerate every page/layout under app/src/app/(dashboard), including dynamic routes and server redirects.
    - Enumerate visible families under app/src/components/ui, layout, campaigns, workspace, dashboard, settings, templates, restyling, feedback, mission-insights, animations, and providers.
    - Read the ownership tables and exceptions in 109-RESEARCH.md before assigning owners.
  </read_first>
  <action>
Before changing any declared file, run `node app/scripts/check-plan-scope.mjs begin --plan 109-02 --plan-file .planning/phases/109-visual-foundations-and-baseline/109-02-PLAN.md` and abort if a baseline already exists or protected-state verification fails.

Write `109-OWNERSHIP.md` as the canonical QA-14 source with two machine-parseable tables: authenticated routes and visible component families. Preserve the research assignments: foundation primitives/globals to 109, shell/layout to 110, campaign listing/settings to 111, workspace and campaign-detail-only panels to 112, dashboard/library/templates/restyling/feedback/visible mission insights/route motion to 113. Represent `/campaigns/new`, `/campaigns/[id]`, the authenticated layout, mixed campaign subfamilies, non-visible providers, and explicitly out-of-scope auth/public-share/cookie-consent surfaces without collapsing them into ambiguous directory ownership.

Each row must contain a stable ID, path or family matcher, exactly one implementation owner (`109`-`113`, `no visual migration`, or `out of milestone` with rationale), boundary, and a non-empty Phase 114 scenario ID. Phase 114 must be rejected as an implementation owner. Add a scenario catalog that defines the observable browser state for every referenced scenario.

Create `check-visual-ownership.mjs` using structured parsing, not loose substring counting. It must enumerate the live authenticated route files, compare them with the inventory, validate unique row IDs/matchers, reject duplicate or missing owners, reject implementation phases outside 109-113 including Phase 114, require rationale for exceptions, require every route/family to reference an existing Phase 114 scenario, and fail when a newly visible top-level component family is unclassified. Keep its configured exclusions explicit and narrow.
  </action>
  <verify>
    <automated>node app/scripts/check-visual-ownership.mjs</automated>
  </verify>
  <done>The validator reports complete, unique ownership for all authenticated routes and visible component families, with zero missing Phase 114 scenarios.</done>
</task>

<task id="109-02-02" type="auto">
  <name>Build deterministic authenticated fixtures and baseline preflight</name>
  <files>app/scripts/seed-visual-foundations.ts, app/tests/e2e/visual-foundations.spec.ts, app/tests/e2e/support/visual-layer-harness.ts, app/playwright.visual.config.ts</files>
  <read_first>
    - app/scripts/seed-dev-admin.ts and app/scripts/seed-testsprite.ts for local account/workspace seeding and idempotency.
    - app/tests/e2e/restyle.spec.ts for the existing email/password login pattern and locale cookie handling.
    - app/playwright.config.ts for current server assumptions, project settings, timeouts, and artifact policy.
  </read_first>
  <action>
Create an idempotent visual-foundation seed script with the reserved-domain synthetic identity `visual-foundations@example.test`, synthetic workspace/client labels, and a test-only password supplied by environment or generated local test configuration. Do not reuse dev-admin identity or any existing user. Create stable fixtures for: populated dashboard; dashboard empty, loading, and deterministic API-error states; dense campaign list with long PT-BR/EN content; campaign-list empty/loading/error states; populated workspace plus deterministic loading/error states; and settings profile/billing plus deterministic validation error. Loading/error states must be activated by explicit test-only request interception or fixture query parameters handled only inside Playwright, never by production behavior. Emit a local JSON manifest containing only synthetic fixture IDs and route URLs.

Create a scoped Playwright visual config and `visual-foundations.spec.ts` Wave 0 harness. Log in only as the synthetic account, set deterministic locale/theme/cookie-consent state, disable transitions, wait on route-ready selectors, and mask all visible email, user, workspace, client, campaign IDs, timestamps, and generated-image regions before screenshots. Add request-interception helpers that deterministically hold loading responses and return known error payloads. Add `support/visual-layer-harness.ts`, imported only by Playwright tests, whose `mountVisualLayerHarness(page)` uses `page.setContent()` to create named sticky/popover/toast and shell/backdrop/overlay/toast compositions styled from extracted canonical layer values; it is never imported by app source, bundled by Next, or exposed as a route. Implement `baseline preflight` to verify identity, manifest states, masks, screenshot directories, and the harness mechanism. Prepare all six milestone viewports.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx tsc --noEmit --pretty false &amp;&amp; npx playwright test --config playwright.visual.config.ts visual-foundations.spec.ts --grep "baseline preflight"</automated>
  </verify>
  <done>The deterministic fixture manifest is generated, local authentication succeeds, and the baseline preflight passes without exposing private data.</done>
</task>

<task id="109-02-03" type="auto">
  <name>Capture and document the immutable before-change baseline</name>
  <files>.planning/phases/109-visual-foundations-and-baseline/109-BASELINE.md, .planning/phases/109-visual-foundations-and-baseline/109-EVIDENCE.json, app/scripts/check-visual-evidence.mjs, app/tests/e2e/visual-foundations.spec.ts</files>
  <read_first>
    - The representative matrix in 109-RESEARCH.md and browser baseline contract in 109-VALIDATION.md.
    - Current git diff for app/src/app/globals.css and all Plan 109 files.
  </read_first>
  <action>
Before any edit to `app/src/app/globals.css`, run the representative capture matrix from research plus deterministic state coverage: dashboard populated at 390/1024/1440/1920 and dashboard empty/loading/error across 390, 768, and 1280; campaign list dense at 390/768/1280/1920 and campaign list empty/loading/error across 390, 768, and 1280; workspace populated at 390/1024/1440 and workspace loading/error at 390 and 1024; settings at 390/768/1280 including a deterministic validation error; plus existing account dropdown, an existing settings confirmation dialog trigger, an existing workspace derivation-review sheet trigger, and both test-only layer-harness compositions at 390 and 1440. Collectively represent light/dark and PT-BR/EN.

Store screenshots under the Phase 109 evidence directory with stable `-before.png` names and mandatory identity/client masks. Create `109-EVIDENCE.json` as the source of truth with schema version, captured-at timestamp, exact `git hash-object app/src/app/globals.css` before hash, scenario/state/viewport/theme/locale keys, artifact hashes, mask list, result, and exactly one deferred owner phase per defect. Create `check-visual-evidence.mjs` to validate JSON schema, exact required matrix completeness, file existence/hash, synthetic identity, required masks, unique keys, valid owners 110-113, and a CSS hash equal to the current pre-change file for `--stage before`. Generate/update `109-BASELINE.md` from the structured evidence rather than treating Markdown as authoritative.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx playwright test --config playwright.visual.config.ts visual-foundations.spec.ts --grep "before baseline|empty state|loading state|error state" &amp;&amp; cd .. &amp;&amp; node app/scripts/check-visual-ownership.mjs &amp;&amp; node app/scripts/check-visual-evidence.mjs --stage before &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-02</automated>
  </verify>
  <done>All required before screenshots exist, runtime defects are assigned once, and the documented globals.css hash proves the baseline predates token edits.</done>
</task>

</tasks>

<verification>
Run ownership/evidence validators, Playwright baseline checks, protected verify-only mode, and `node app/scripts/check-plan-scope.mjs verify --plan 109-02`.
</verification>

<completion_criteria>
- QA-14 ownership and scenario coverage validates with no gaps or duplicates.
- Deterministic local fixtures and authenticated browser harness work without production data.
- Every required representative `before` capture exists before global CSS changes.
- Observed defects are assigned to exactly one later owner phase.
- Unrelated billing and preview-fix files remain untouched and unstaged by this plan.
</completion_criteria>
