---
phase: 109-visual-foundations-and-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scripts/snapshot-visual-dirty-state.mjs
  - app/scripts/snapshot-visual-dirty-state.test.mjs
  - app/scripts/check-plan-scope.mjs
  - app/scripts/check-plan-scope.test.mjs
  - .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json
autonomous: true
requirements:
  - QA-14
must_haves:
  truths:
    - The unrelated billing and preview-fix paths are captured once before any Phase 109 product, evidence, or contract edit.
    - Protected-state verification cannot overwrite or refresh its baseline.
    - Every later plan can detect committed, staged, unstaged, renamed, deleted, and untracked paths outside its declared files_modified allowlist.
    - Guard baselines are immutable and stored outside the worktree after their one-time creation.
  artifacts:
    - path: app/scripts/snapshot-visual-dirty-state.mjs
      provides: One-time protected-path capture and verify-only comparison.
    - path: app/scripts/check-plan-scope.mjs
      provides: Per-plan HEAD/index/worktree/untracked allowlist enforcement.
    - path: .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json
      provides: Auditable initial state of unrelated dirty paths.
---

# Plan 109-01: Execution Guardrails and Immutable Snapshot

<objective>
Establish immutable execution guardrails before any Phase 109 visual, evidence, fixture, or inventory work begins.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/109-visual-foundations-and-baseline/109-CONTEXT.md
@.planning/phases/109-visual-foundations-and-baseline/109-RESEARCH.md
@.planning/phases/109-visual-foundations-and-baseline/109-VALIDATION.md
</context>

<tasks>

<task id="109-01-01" type="auto">
  <name>Create guard tools and immediately freeze protected state</name>
  <files>app/scripts/snapshot-visual-dirty-state.mjs, app/scripts/check-plan-scope.mjs, .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json</files>
  <action>
Create the two guard scripts before any other Phase 109 execution edit. `snapshot-visual-dirty-state.mjs capture` must record the current repository HEAD plus HEAD blob, index stage/mode/blob, worktree SHA-256 or absent marker, and exact porcelain-v2 record for the four protected paths listed in 109-VALIDATION.md. Capture must use exclusive creation and fail if the destination already exists; only `verify` may be used afterward. Immediately run capture after the script exists, before creating tests or changing any other phase-owned file.

Create `check-plan-scope.mjs` with `begin` and `verify` modes. `begin --plan <id> --plan-file <path>` parses that plan's exact `files_modified` list, records the starting HEAD in `.git/gsd-guards/<id>.json` using exclusive creation, and records index/worktree/untracked state. `verify` compares the baseline through current HEAD commits plus current index, worktree, renames, deletions and untracked files, and fails on every changed path outside the exact allowlist. It must never reset an existing baseline. Bootstrap Plan 109-01 by allowing only its declared files while comparing all changes since its starting HEAD.
  </action>
  <verify>
    <automated>node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-01</automated>
  </verify>
  <done>The protected snapshot exists exactly once and Plan 109-01 has an immutable scope baseline.</done>
</task>

<task id="109-01-02" type="auto">
  <name>Prove guard tools reject recapture and undeclared changes</name>
  <files>app/scripts/snapshot-visual-dirty-state.test.mjs, app/scripts/check-plan-scope.test.mjs</files>
  <action>
Add green `node:test` suites using temporary git repositories and fixture paths. Prove protected capture rejects overwrite, verify detects HEAD/index/worktree/status changes, and verify succeeds only for an unchanged snapshot. Prove scope begin rejects overwrite and verify detects committed, staged, unstaged, renamed, deleted and untracked undeclared paths while allowing every declared path. Tests must also prove parser failure and malformed baseline failure cannot be mistaken for success.
  </action>
  <verify>
    <automated>cd app &amp;&amp; node --test scripts/snapshot-visual-dirty-state.test.mjs scripts/check-plan-scope.test.mjs &amp;&amp; cd .. &amp;&amp; node app/scripts/snapshot-visual-dirty-state.mjs verify .planning/phases/109-visual-foundations-and-baseline/109-DIRTY-SNAPSHOT.json &amp;&amp; node app/scripts/check-plan-scope.mjs verify --plan 109-01</automated>
  </verify>
  <done>Guard self-tests are green, protected state is unchanged, and the plan scope contains no undeclared path.</done>
</task>

</tasks>

<verification>
Run both guard self-test files, protected verify-only mode, and Plan 109-01 scope verification. Never run protected capture again.
</verification>

<completion_criteria>
- Protected billing/preview state was captured before all other execution work.
- Capture and plan-start baselines cannot be overwritten.
- Scope verification covers HEAD commits, index, worktree, renames, deletions and untracked paths.
- All later plans can use begin once and verify-only thereafter.
</completion_criteria>
