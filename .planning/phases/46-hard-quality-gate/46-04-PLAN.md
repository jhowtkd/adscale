---
phase: 46-hard-quality-gate
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - app/src/server/ai/creative-score.ts
  - app/src/server/ai/creative-quality-gate.ts
  - app/src/server/jobs/derivation.ts
  - app/src/app/api/derivations/[id]/regenerate/route.ts
  - app/src/app/api/derivations/[id]/regenerate/route.test.ts
  - app/tests/unit/creative-score.test.ts
autonomous: true
requirements:
  - QA-04
must_haves:
  truths:
    - "Regeneration suggestion lists hard failure codes and per-failure fix text"
    - "Suggestion preserves mode, target format, CTA semantics, and restyling asset IDs"
    - "Regenerate without user feedback uses stored structured suggestion"
  artifacts:
    - path: app/src/server/ai/creative-score.ts
      provides: "buildHardFailureRegenerationSuggestion"
      exports:
        - buildHardFailureRegenerationSuggestion
    - path: app/src/app/api/derivations/[id]/regenerate/route.ts
      provides: "default feedback from regenerationSuggestion"
  key_links:
    - from: runCompletedDerivationQualityGate
      to: buildHardFailureRegenerationSuggestion
      via: "updateDerivationScore regenerationSuggestion"
      pattern: "buildHardFailureRegenerationSuggestion"
---

<objective>
Structure regeneration guidance from detected hard failures while preserving the Phase 45 creative contract (QA-04).

Purpose: Users and the job pipeline get actionable regen prompts tied to failure codes, not generic polish text.

Output: `buildHardFailureRegenerationSuggestion`, persisted on gate, regenerate route default feedback.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-hard-quality-gate/46-CONTEXT.md
@.planning/phases/46-hard-quality-gate/46-03-SUMMARY.md

<interfaces>
From `creative-score.ts` (extend, do not replace):
```typescript
export function buildRegenerationSuggestion(input: BuildSuggestionInput): string;

// Add:
export function buildHardFailureRegenerationSuggestion(input: {
  hardFailures: Array<{ code: CreativeHardFailureCode; message: string }>;
  contract: CreativeContract;
  scoreIssues?: string[];
  modelSuggestion?: string;
}): string;
```

Expected output shape (CONTEXT):
1. `Hard failures: [code1, code2].`
2. Per-failure fix lines from `message`
3. Tail from existing `buildRegenerationSuggestion` contract preservation (CTA semantics text, format, mode, base/style asset IDs)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildHardFailureRegenerationSuggestion</name>
  <files>app/src/server/ai/creative-score.ts, app/tests/unit/creative-score.test.ts, app/tests/unit/ai/creative-score.test.ts</files>
  <behavior>
    - Two hard failures → suggestion contains both codes and both messages
    - Restyling contract → suggestion includes baseAssetId and styleAssetId lines
    - Empty hardFailures → delegates to buildRegenerationSuggestion behavior
    - format_adaptation + inherited CTA → preservation tail uses resolveCtaSemantics wording (not "none")
  </behavior>
  <action>
    Implement `buildHardFailureRegenerationSuggestion` per CONTEXT QA-04. Reuse `buildRegenerationSuggestion` for the preservation tail by mapping contract fields to `BuildSuggestionInput` (ctaText from semantics, format, generationMode).

    Add unit tests in existing creative-score test file(s) — follow repo convention (two test paths exist; update the one that already tests `buildRegenerationSuggestion`).

    In `runCompletedDerivationQualityGate` (Plan 03), after classify:
    - If `hardFailures.length > 0`, set `regenerationSuggestion` via new builder and `updateDerivationScore` with only `regenerationSuggestion` field OR include in quality gate update if you add column support — prefer updating via `updateDerivationScore` to keep one source for regen text.

    Do NOT drop target format/mode/CTA from tail (per Phase 45 success criterion #5).
  </action>
  <verify>
    <automated>cd app && npm test -- creative-score.test.ts</automated>
  </verify>
  <done>Tests pass; suggestion format matches CONTEXT structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate route uses structured suggestion</name>
  <files>app/src/app/api/derivations/[id]/regenerate/route.ts, app/src/app/api/derivations/[id]/regenerate/route.test.ts</files>
  <behavior>
    - POST regenerate without body.feedback → child derivation feedback equals parent.regenerationSuggestion
    - POST with explicit feedback → uses user feedback (unchanged)
  </behavior>
  <action>
    In `regenerate/route.ts`, when `feedback` is undefined/empty and `original.regenerationSuggestion` is non-empty, set `createDerivation({ feedback: original.regenerationSuggestion })`.

    If parent has `hardFailures` but empty suggestion, reconstruct via `buildHardFailureRegenerationSuggestion` using persisted failures + contract rebuilt like qa route (import `resolveCtaSemantics`).

    Extend route.test.ts with cases above. Do not block regenerate when invalid (CONTEXT: regen allowed).
  </action>
  <verify>
    <automated>cd app && npm test -- regenerate/route.test.ts</automated>
  </verify>
  <done>Regenerate tests pass; child jobs receive structured failure feedback</done>
</task>

</tasks>

<verification>
  <automated>cd app && npm test -- creative-score.test.ts regenerate/route.test.ts</automated>
</verification>

<success_criteria>
- Invalid derivations get regenerationSuggestion mentioning hard failure codes
- Regenerate API preserves contract context in child job feedback
</success_criteria>

<output>
After completion, create `.planning/phases/46-hard-quality-gate/46-04-SUMMARY.md`
</output>
