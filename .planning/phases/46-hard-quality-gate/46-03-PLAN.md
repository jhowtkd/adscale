---
phase: 46-hard-quality-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - app/src/server/ai/creative-quality-gate.ts
  - app/src/server/ai/creative-score.ts
  - app/src/server/jobs/derivation.ts
  - app/tests/integration/derivation-job.test.ts
autonomous: true
requirements:
  - QA-01
  - QA-03
must_haves:
  truths:
    - "Every completed derivation runs automatic quality analysis after scoring"
    - "hardFailures force qualityVerdict invalid even when qualityScore is high"
    - "Gate step failure does not fail the derivation job"
  artifacts:
    - path: app/src/server/jobs/derivation.ts
      provides: "quality-gate Inngest step after score-derivation"
      contains: "quality-gate"
    - path: app/src/server/ai/creative-quality-gate.ts
      provides: "runCompletedDerivationQualityGate orchestration helper"
      exports:
        - runCompletedDerivationQualityGate
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: analyzeCreativeQa
      via: "runCompletedDerivationQualityGate"
      pattern: "analyzeCreativeQa"
    - from: runCompletedDerivationQualityGate
      to: updateDerivationQualityGate
      via: "classify + persist"
      pattern: "updateDerivationQualityGate"
---

<objective>
Wire automatic quality analysis into the derivation job immediately after `score-derivation`, and align scoring prompts so contract violations surface in `scoreIssues` (QA-03).

Purpose: Fulfills QA-01 automatic analysis; ensures score cannot imply approvability when hard failures exist.

Output: `quality-gate` Inngest step, orchestration helper, score prompt tweak, integration test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-hard-quality-gate/46-CONTEXT.md
@.planning/phases/46-hard-quality-gate/46-01-SUMMARY.md
@.planning/phases/46-hard-quality-gate/46-02-SUMMARY.md

<interfaces>
From `app/src/server/jobs/derivation.ts` (insert after line ~683):
```typescript
await step.run("score-derivation", async () => { ... scoreCompletedDerivation(..., contract); });
// ADD: quality-gate step here
await step.run("track-usage", async () => { ... });
```

From `scoreCompletedDerivation` — after visual score, row has `qualityScore`, `scoreIssues`, `regenerationSuggestion`.

`analyzeCreativeQa` signature in `creative-qa.ts` — same inputs as manual QA route (imageBuffer, campaign, derivation, contract).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: runCompletedDerivationQualityGate helper</name>
  <files>app/src/server/ai/creative-quality-gate.ts</files>
  <action>
    Add `runCompletedDerivationQualityGate({ derivationId, workspaceId, imageBuffer, mimeType, campaign, derivation, contract, locale })`:

    1. Call `analyzeCreativeQa` with the same contract/campaign/derivation shape as `qa/route.ts` (reuse contract from job — do not re-resolve differently).
    2. Load latest derivation row (or accept `scoreIssues`, `qualityScore`, `regenerationSuggestion` passed in from caller after score).
    3. Call `classifyCreativeQualityGate` + `deriveQualityVerdict`.
    4. Call `updateDerivationQualityGate` with results + set `qaChecklist`/`qaStatus`/`qaIssues`/`qaSuggestions` from QA result (persist checklist so Phase 47 can show without re-run) — optional but recommended for consistency.
    5. On any error: log warn, persist `qualityVerdict: "improvable"`, `hardFailures: []`, `polishSuggestions: []`, still set `qualityGatedAt` (per CONTEXT non-blocking rule).

    Export for manual QA route reuse in Plan 05.

    Do NOT block export APIs. Do NOT change `score-derivation` failure behavior.
  </action>
  <verify>
    <automated>cd app && npm test -- creative-quality-gate.test.ts</automated>
  </verify>
  <done>Helper compiles; unit tests still pass; helper documented in file header</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inngest quality-gate step + score prompt (QA-03)</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/ai/creative-score.ts, app/tests/integration/derivation-job.test.ts</files>
  <behavior>
    - After mocked score, quality-gate step invokes gate helper with contract
    - When classifier returns hard failure, persisted verdict is invalid
    - Scoring system prompt mentions contract violations must appear in scoreIssues
  </behavior>
  <action>
    In `derivation.ts`, add `await step.run("quality-gate", ...)` after `score-derivation`:
    - Re-download output buffer from `generated.outputKey` (same as score step) OR pass buffer if still in scope within same function — prefer single download in gate step for step isolation.
    - Pass `contract` from job scope (Phase 45 pattern).
    - Call `runCompletedDerivationQualityGate`.

    Update `creative-score.ts` vision prompt (analyzeDerivationCreative): instruct model that contract violations (CTA, brand, offer, format layout) must be listed in `scoreIssues` with explicit wording and must not be offset by high `visualQuality` alone.

    Extend `derivation-job.test.ts` (or add focused test): mock `analyzeCreativeQa` to return `ctaOffer: failed`, assert `updateDerivationQualityGate` called with `qualityVerdict: "invalid"` when wired through helper (mock at module boundary).

    When gate classifies invalid, optionally re-call `updateDerivationScore` only to refresh `regenerationSuggestion` in Plan 04 — here only persist gate fields.
  </action>
  <verify>
    <automated>cd app && npm test -- derivation-job.test.ts</automated>
  </verify>
  <done>quality-gate step exists in job; integration test passes; score prompt updated</done>
</task>

</tasks>

<verification>
  <automated>cd app && npm test -- derivation-job.test.ts creative-quality-gate.test.ts</automated>
</verification>

<success_criteria>
- Completed derivations have `qualityGatedAt` set without user running POST /qa
- Invalid contract outputs get `qualityVerdict: invalid` regardless of numeric score
</success_criteria>

<output>
After completion, create `.planning/phases/46-hard-quality-gate/46-03-SUMMARY.md`
</output>
