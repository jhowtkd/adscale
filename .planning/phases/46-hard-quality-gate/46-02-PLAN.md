---
phase: 46-hard-quality-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/db/schema.ts
  - app/drizzle/**/*.sql
  - app/src/server/repositories/derivation.ts
  - app/tests/unit/repositories/derivation.test.ts
  - app/src/lib/hooks/use-derivations.ts
  - app/src/lib/mock-data.ts
autonomous: true
requirements:
  - QA-05
must_haves:
  truths:
    - "Completed derivations can persist qualityVerdict, hardFailures, polishSuggestions, qualityGatedAt"
    - "API/client types expose the new fields on derivation reads"
  artifacts:
    - path: app/src/server/db/schema.ts
      provides: "qualityVerdict, hardFailures, polishSuggestions, qualityGatedAt columns"
      contains: "qualityVerdict"
    - path: app/src/server/repositories/derivation.ts
      provides: "updateDerivationQualityGate"
      exports:
        - updateDerivationQualityGate
  key_links:
    - from: app/src/server/repositories/derivation.ts
      to: derivations table
      via: "drizzle update set qualityVerdict"
      pattern: "qualityVerdict"
---

<objective>
Persist quality-gate results on the `derivations` row and surface them through repository + TypeScript client types (Phase 47 UI consumes these fields).

Purpose: `qualityVerdict` is the approval gate; do not overload `qaStatus` (per 46-CONTEXT).

Output: Drizzle migration, `updateDerivationQualityGate`, hook/mock types updated.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-hard-quality-gate/46-CONTEXT.md
@.planning/phases/46-hard-quality-gate/46-01-SUMMARY.md

<interfaces>
From `app/src/server/repositories/derivation.ts` (pattern to mirror):
```typescript
export async function updateDerivationScore(id, workspaceId, data: UpdateDerivationScoreInput) {
  return db.update(derivations).set({ qualityScore: ..., scoredAt: new Date(), ... }).returning();
}
```

Import types from `creative-quality-gate.ts` (Plan 01):
```typescript
import type { CreativeHardFailure, CreativeQualityVerdict } from "@/server/ai/creative-quality-gate";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration for quality gate columns</name>
  <files>app/src/server/db/schema.ts, app/drizzle/</files>
  <action>
    Add to `derivations` in `schema.ts` (per D-data-model in 46-CONTEXT):
    - `qualityVerdict`: `text("quality_verdict")` — nullable; values `invalid` | `improvable` | `acceptable`
    - `hardFailures`: `jsonb("hard_failures")` — default `[]` or nullable with app default `[]`
    - `polishSuggestions`: `jsonb("polish_suggestions")` — same
    - `qualityGatedAt`: `timestamp("quality_gated_at", { mode: "date" })` — nullable

    Generate and apply Drizzle migration via project convention (`npm run db:generate` / `db:migrate` as used in repo). Backfill existing rows: set `qualityVerdict = 'acceptable'` where `status = 'completed'` and verdict null (planner discretion per CONTEXT).

    Do NOT remove or repurpose `qaStatus`.
  </action>
  <verify>
    <automated>cd app && npm run db:generate 2>&1 | head -5; test -f app/drizzle/*.sql && echo migration_ok</automated>
  </verify>
  <done>Migration file exists; schema types compile; derivations table has four new columns</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository update + client types</name>
  <files>app/src/server/repositories/derivation.ts, app/tests/unit/repositories/derivation.test.ts, app/src/lib/hooks/use-derivations.ts, app/src/lib/mock-data.ts</files>
  <behavior>
    - updateDerivationQualityGate persists verdict, failures array, suggestions array, sets qualityGatedAt
    - getDerivationById returning row includes new fields (mock DB in test)
  </behavior>
  <action>
    Add `UpdateDerivationQualityGateInput` and `updateDerivationQualityGate(id, workspaceId, data)` setting all four fields + `updatedAt`.

    Extend `app/tests/unit/repositories/derivation.test.ts` with one test asserting persisted JSON matches input.

    Add optional fields to `use-derivations.ts` Derivation type and `mock-data.ts`:
    - `qualityVerdict?: "invalid" | "improvable" | "acceptable" | null`
    - `hardFailures?: Array<{ code: string; message: string; criterion?: string }> | null`
    - `polishSuggestions?: string[] | null`
    - `qualityGatedAt?: string | null`

    Optional helper in same repo file or `app/src/lib/derivation-quality.ts`: `scoreCappedForDisplay(qualityScore, qualityVerdict)` returns `min(score, 59)` when `invalid`, else raw score — for Phase 47; export if added.

    Campaign/derivation API routes return full DB rows today — no mapper change required unless a DTO strips columns; grep and fix any explicit pick lists.
  </action>
  <verify>
    <automated>cd app && npm test -- derivation.test.ts</automated>
  </verify>
  <done>Repository test passes; TypeScript types include quality gate fields</done>
</task>

</tasks>

<verification>
  <automated>cd app && npm run build 2>&1 | tail -3</automated>
</verification>

<success_criteria>
- Migration applied in dev workflow
- `updateDerivationQualityGate` is the single write path for gate persistence (used by Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/46-hard-quality-gate/46-02-SUMMARY.md`
</output>
