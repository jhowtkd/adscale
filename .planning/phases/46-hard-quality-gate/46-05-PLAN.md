---
phase: 46-hard-quality-gate
plan: "05"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - app/src/server/ai/creative-quality-gate.ts
  - app/src/app/api/derivations/[id]/review/route.ts
  - app/src/app/api/derivations/[id]/save-reference/route.ts
  - app/src/app/api/derivations/[id]/delivery-package/route.ts
  - app/src/app/api/derivations/[id]/qa/route.ts
  - app/src/app/api/derivations/[id]/qa/route.test.ts
  - app/src/lib/api-response.ts
  - app/messages/en.json
  - app/messages/pt-BR.json
  - app/scripts/test-creatives.ts
autonomous: true
requirements:
  - QA-01
  - QA-05
must_haves:
  truths:
    - "Approve, save-reference, and delivery-package return 409 when qualityVerdict is invalid"
    - "Export endpoints remain allowed for invalid outputs"
    - "Manual POST /qa uses the same classifier as the job"
    - "test-creatives.ts blocking uses shared classifier"
  artifacts:
    - path: app/src/app/api/derivations/[id]/review/route.ts
      provides: "409 guard on approve when invalid"
    - path: app/src/app/api/derivations/[id]/qa/route.ts
      provides: "classifier after analyzeCreativeQa"
  key_links:
    - from: review/route.ts
      to: qualityVerdict
      via: "getDerivationById before updateDerivationStatus"
      pattern: "qualityVerdict.*invalid"
---

<objective>
Enforce blocking vs advisory actions (QA-05), unify manual QA with the automatic classifier (QA-01 consistency), and align dev script quality aggregation.

Purpose: Users cannot approve/save-reference/start delivery on invalid outputs; export stays permissive per product policy.

Output: API 409 guards, shared classifier on POST /qa, test-creatives integration.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-hard-quality-gate/46-CONTEXT.md
@.planning/phases/46-hard-quality-gate/46-03-SUMMARY.md

<interfaces>
Blocking matrix (46-CONTEXT):
| Action | invalid verdict |
| approve PATCH | 409 |
| save-reference POST | 409 |
| delivery-package POST | 409 |
| export | allowed |

Add API error key e.g. `derivationHardFailures` with payload `{ qualityVerdict, hardFailures }` in 409 body (extend `apiError` details param if needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared assertDerivationApprovable + API 409 guards</name>
  <files>app/src/server/ai/creative-quality-gate.ts, app/src/app/api/derivations/[id]/review/route.ts, app/src/app/api/derivations/[id]/save-reference/route.ts, app/src/app/api/derivations/[id]/delivery-package/route.ts, app/src/lib/api-response.ts, app/messages/en.json, app/messages/pt-BR.json</files>
  <behavior>
    - approve on derivation with qualityVerdict invalid → 409 with hardFailures in body
    - approve on improvable → 200
    - save-reference on invalid → 409
    - delivery-package on invalid → 409
  </behavior>
  <action>
    Add `assertDerivationApprovable(derivation)` in `creative-quality-gate.ts` throwing a typed error or returning `{ ok: false, hardFailures, qualityVerdict }` when `qualityVerdict === "invalid"` OR `hardFailures?.length > 0` (defensive).

    In `review/route.ts`: before `updateDerivationStatus` with `approved`, guard completed derivations. Return `apiError("derivationHardFailures", 409, { qualityVerdict, hardFailures })` — add i18n keys for user message in en/pt-BR.

  Apply same guard to `save-reference/route.ts` and `delivery-package/route.ts` (source derivation).

    Do NOT add guards to export/ZIP routes (grep export under derivations API — leave unchanged).

    Add/update route tests where they exist (delivery-package already has route.test.ts).

    Export remains allowed — no change to export handlers.
  </action>
  <verify>
    <automated>cd app && npm test -- review delivery-package save-reference 2>&1 | tail -20</automated>
  </verify>
  <done>409 on three blocking actions; export routes untouched</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual QA + test-creatives use classifier</name>
  <files>app/src/app/api/derivations/[id]/qa/route.ts, app/src/app/api/derivations/[id]/qa/route.test.ts, app/scripts/test-creatives.ts</files>
  <behavior>
    - POST /qa after analyzeCreativeQa updates qualityVerdict/hardFailures via same classify path
    - test-creatives blockingIssues includes hard failure codes from classifier when QA checklist present
  </behavior>
  <action>
    In `qa/route.ts`, after `analyzeCreativeQa` and before/with `updateDerivationQa`:
    - Run `classifyCreativeQualityGate` + `deriveQualityVerdict` on QA result + existing score fields from derivation row.
    - Call `updateDerivationQualityGate`.
    - Optionally allow POST on `completed` (not only `approved`) when `qualityVerdict` is null — planner discretion per CONTEXT; minimum: after QA on approved, fields stay consistent.

    Refactor `test-creatives.ts` quality aggregation: import `classifyCreativeQualityGate` / helper instead of ad-hoc `blockingIssues` from raw QA failed only. Map hard failure codes to `blockingIssues` strings for HTML report.

    Update `qa/route.test.ts` to expect quality fields in response when gate runs.

    Do NOT maintain two classification philosophies.
  </action>
  <verify>
    <automated>cd app && npm test -- qa/route.test.ts</automated>
  </verify>
  <done>Manual QA persists same verdict fields as job; script uses shared classifier</done>
</task>

</tasks>

<verification>
  <automated>cd app && npm test -- qa/route.test.ts delivery-package/route.test.ts</automated>
</verification>

<success_criteria>
- Invalid outputs cannot be approved or saved as reference or used as delivery source
- API responses expose qualityVerdict and hardFailures for Phase 47 UI
- test-creatives and production share one classifier
</success_criteria>

<output>
After completion, create `.planning/phases/46-hard-quality-gate/46-05-SUMMARY.md`
</output>
