---
phase: 136-quality-trend-dashboard
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/human-quality/trend/service.ts
  - app/src/server/repositories/human-quality-corpus.ts
  - app/src/app/api/feedback/quality-trend/route.ts
  - app/src/app/api/feedback/quality-trend/route.test.ts
  - app/src/server/human-quality/sampling/coverage.ts
  - app/src/server/human-quality/sampling/service.ts
  - app/scripts/run-quality-trend.ts
  - app/scripts/check-quality-trend-evidence.mjs
  - .planning/phases/136-quality-trend-dashboard/136-EVIDENCE.template.json
  - app/tests/unit/human-quality/sampling/coverage.test.ts
  - app/tests/unit/human-quality/calibration-evaluated-repository.test.ts
autonomous: true
requirements:
  - TREND-02
  - TREND-03
  - TREND-04
user_setup: []
must_haves:
  truths:
    - "GET /api/feedback/quality-trend returns QualityTrendReport JSON with platform-owner auth"
    - "Server applies workspace, cohort, generationMode, format, clientProfileId and primaryFailureReason filters before aggregation"
    - "Coverage trend_global gate reflects real trend status including TREND_MIN_TIME_BUCKETS"
    - "Evidence CLI writes 136-EVIDENCE.json for Phase 137 audit precursor"
  artifacts:
    - path: app/src/server/human-quality/trend/service.ts
      provides: runQualityTrend orchestrator
      exports: ["runQualityTrend", "TREND_MAX_ROWS"]
    - path: app/src/app/api/feedback/quality-trend/route.ts
      provides: Authenticated trend GET endpoint
      exports: ["GET"]
    - path: app/scripts/run-quality-trend.ts
      provides: Evidence CLI mirroring run-sample-coverage.ts
    - path: app/scripts/check-quality-trend-evidence.mjs
      provides: Honesty checker for trend evidence JSON
  key_links:
    - from: app/src/app/api/feedback/quality-trend/route.ts
      to: app/src/server/human-quality/trend/service.ts
      via: "runQualityTrend with parsed query filters"
      pattern: "runQualityTrend"
    - from: app/src/server/human-quality/trend/service.ts
      to: app/src/server/repositories/human-quality-corpus.ts
      via: "listEvaluatedCorpusWithEvaluations with dimensional filters"
      pattern: "listEvaluatedCorpusWithEvaluations"
    - from: app/src/server/human-quality/sampling/coverage.ts
      to: app/src/server/human-quality/sampling/guidance.ts
      via: "buildTrendGuidance or optional trend report status for trend_global gate"
      pattern: "trend_global"
    - from: app/src/server/human-quality/sampling/service.ts
      to: app/src/server/human-quality/trend/service.ts
      via: "runQualityTrend inside runSampleCoverage orchestrator"
      pattern: "runQualityTrend"
    - from: app/src/server/human-quality/sampling/service.ts
      to: app/src/server/human-quality/sampling/coverage.ts
      via: "trend summary passed to buildSampleCoverageReport"
      pattern: "buildSampleCoverageReport"
---


<objective>
Expose the trend engine via authenticated API with server-honest dimensional filters, wire coverage trend_global gate to real trend status, and add evidence CLI + checker.

Purpose: TREND-02 requires filters applied before aggregation; TREND-03 coverage tab must stop using placeholder; TREND-04 needs API delivery of bounded evidence refs.
Output: `runQualityTrend` service, `GET /api/feedback/quality-trend`, updated coverage.ts, `run-quality-trend.ts`, `check-quality-trend-evidence.mjs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-quality-trend-dashboard/136-RESEARCH.md
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-03-SUMMARY.md

# Depends on 136-01 trend module
@.planning/phases/136-quality-trend-dashboard/136-01-PLAN.md

<interfaces>
From app/src/server/repositories/human-quality-corpus.ts (extend):
```typescript
export interface ListEvaluatedCorpusWithEvaluationsFilters {
  workspaceId?: string;
  cohort?: string;
  limit?: number;
  // ADD: generationMode?, format?, clientProfileId?, primaryFailureReason?
}
```

From app/src/server/human-quality/corpus.ts:
```typescript
export const HUMAN_QUALITY_CORPUS_COHORTS = ["baseline", "post_learning", ...];
export const HUMAN_QUALITY_FAILURE_REASONS = ["visual_overload", "weak_hierarchy", ...];
```

From app/src/server/human-quality/trend/report.ts (136-01):
```typescript
export function buildQualityTrendReport(input: { rows, capturedAt, truncated? }): QualityTrendReport;
```

API pattern from score-calibration/route.ts:
```typescript
await requireCalibrationAccess(request, parsed.data.workspaceId ?? null);
return NextResponse.json({ report });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository dimensional filters and trend service</name>
  <files>
    app/src/server/repositories/human-quality-corpus.ts
    app/src/server/human-quality/trend/service.ts
    app/tests/unit/human-quality/calibration-evaluated-repository.test.ts
  </files>
  <behavior>
    - listEvaluatedCorpusWithEvaluations filters by generationMode, format, clientProfileId, primaryFailureReason when provided
    - runQualityTrend sets truncated:true when row count hits TREND_MAX_ROWS (export constant, default 500 or explicit raise documented in comment)
    - runQualityTrend passes filtered rows to buildQualityTrendReport with capturedAt ISO string
  </behavior>
  <action>
    Extend `ListEvaluatedCorpusWithEvaluationsFilters` with optional:
    - `generationMode?: string`
    - `format?: string`
    - `clientProfileId?: string`
    - `primaryFailureReason?: string` (match evaluation column)

    Add WHERE conditions in `listEvaluatedCorpusWithEvaluations` for each filter (server-side only — never client-side post-filter).

    Create `trend/service.ts`:
    - `export const TREND_MAX_ROWS = 500` (match DEFAULT_EVALUATED_CORPUS_LIMIT; surface truncated on report when limit hit)
    - `runQualityTrend(options)` fetches rows with all filters + limit TREND_MAX_ROWS, calls `buildQualityTrendReport`
    - Return `{ report: QualityTrendReport }`

    Extend `calibration-evaluated-repository.test.ts` with cases for new filters (mock db or existing test pattern).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration-evaluated-repository.test.ts tests/unit/human-quality/trend/ -x</automated>
  </verify>
  <done>Repository applies all TREND-02 dimensions; service orchestrates fetch + report with truncated flag</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Quality trend API route</name>
  <files>
    app/src/app/api/feedback/quality-trend/route.ts
    app/src/app/api/feedback/quality-trend/route.test.ts
  </files>
  <behavior>
    - GET returns 403 when requireCalibrationAccess throws
    - GET returns 400 on invalid workspaceId UUID or invalid primaryFailureReason enum
    - GET returns 200 with { report } on success; report includes buckets and alertFlags
  </behavior>
  <action>
    Create `app/src/app/api/feedback/quality-trend/route.ts`:
    - Zod querySchema: workspaceId (uuid optional), cohort (enum HUMAN_QUALITY_CORPUS_COHORTS optional), generationMode, format, clientProfileId (uuid optional), primaryFailureReason (enum HUMAN_QUALITY_FAILURE_REASONS optional)
    - `requireCalibrationAccess(request, workspaceId ?? null)` per Phase 130 precedent
    - Call `runQualityTrend({ ...parsed.data, capturedAt: new Date().toISOString() })`
    - Return `{ report }` JSON (no unbounded payload beyond report's capped evidenceRefs)

    Create route.test.ts mirroring sample-coverage/route.test.ts: mock requireCalibrationAccess + runQualityTrend.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/quality-trend/route.test.ts -x</automated>
  </verify>
  <done>API route auth, validation and response shape verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Coverage trend_global wire and evidence CLI</name>
  <files>
    app/src/server/human-quality/sampling/coverage.ts
    app/src/server/human-quality/sampling/service.ts
    app/scripts/run-quality-trend.ts
    app/scripts/check-quality-trend-evidence.mjs
    .planning/phases/136-quality-trend-dashboard/136-EVIDENCE.template.json
    app/tests/unit/human-quality/sampling/coverage.test.ts
  </files>
  <behavior>
    - runSampleCoverage calls runQualityTrend and passes trend summary into buildSampleCoverageReport
    - trend_global gate status uses buildTrendGuidance / trend report status (not count ≥ 5 alone)
    - trend_global blockedClaims empty when ok; includes time-bucket guidance when only one populated week
    - run-quality-trend.ts writes .planning/phases/136-quality-trend-dashboard/136-EVIDENCE.json
    - check-quality-trend-evidence.mjs rejects movement claims when learningImpactStatus insufficient_sample in evidence
  </behavior>
  <action>
    Wire `runSampleCoverage` orchestrator in `sampling/service.ts`:
    - Add `runQualityTrend({ workspaceId, cohort, capturedAt })` to the parallel fetch alongside calibration/impact/quality (or await after Promise.all — same capturedAt)
    - Pass trend summary into `buildSampleCoverageReport`: `{ status: trendResult.report.status, sampleGuidance: trendResult.report.sampleGuidance, evaluatedItemCount: trendResult.report.evaluatedItemCount, populatedBucketCount: trendResult.report.populatedBucketCount }`
    - Without this orchestrator wire, Coverage tab keeps placeholder `trend_global` logic even if coverage.ts is updated in isolation

    Update `buildSampleCoverageReport` in coverage.ts:
    - Add required `trend` field on `BuildSampleCoverageReportInput`: `{ status: SamplingStatus; sampleGuidance: SampleGuidance[]; evaluatedItemCount: number; populatedBucketCount: number }`
    - Replace placeholder `trend_global` logic (lines ~117-153) with real status from `trend.status` and `collectBlockedClaims(trend.sampleGuidance.filter(g => g.gate === "trend_global" || g.gate === "trend_time_buckets"))`
    - Remove "trend charts deferred to Phase 136" from blockedClaims when trend engine provides real guidance

    Create `app/scripts/run-quality-trend.ts` mirroring `run-sample-coverage.ts`:
    - CLI flags: --out, --all-workspaces, --workspace-id, --cohort, plus dimensional filters
    - Default out: `.planning/phases/136-quality-trend-dashboard/136-EVIDENCE.json`
    - Evidence JSON: requirements TREND-01..04 pass/pending, report snapshot, alertFlags, sampleGuidance

    Create `app/scripts/check-quality-trend-evidence.mjs`:
    - Reuse patterns from `app/scripts/lib/evidence-honesty.mjs` and `check-sampling-sufficiency-evidence.mjs`
    - Export `validateEvidenceShape` for vitest import (same pattern as Phase 135 checker)
    - Assert separate alertFlags fields present; reject blended warning strings
    - Assert evidenceSource live_human; no fixture metrics in trend series
    - Reject movement claims when any bucket has `learningImpactStatus === "insufficient_sample"`

    Create `.planning/phases/136-quality-trend-dashboard/136-EVIDENCE.template.json` with minimal valid evidence shape (requirements TREND-01..04, report snapshot, alertFlags, sampleGuidance).

    Extend coverage.test.ts for real trend_global behavior via orchestrator-shaped inputs (trend summary required on BuildSampleCoverageReportInput).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/sampling/coverage.test.ts -x && node scripts/check-quality-trend-evidence.mjs --evidence ../.planning/phases/136-quality-trend-dashboard/136-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>Coverage gate honest; evidence CLI and checker exist; tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client query params → API route | Untrusted filter values cross into DB query |
| API → JSON response | Report may contain corpus item IDs visible to owner/admin only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-04 | I | quality-trend route cross-workspace | mitigate | requireCalibrationAccess + workspaceId repository filter |
| T-136-05 | T | UUID/filter injection | mitigate | Zod .uuid() and enum validation on querySchema |
| T-136-06 | D | TREND_MAX_ROWS truncation | mitigate | report.truncated flag; do not silently drop buckets without signaling |
</threat_model>

<verification>
```bash
cd app && npm test -- tests/unit/human-quality/trend/ tests/unit/human-quality/sampling/coverage.test.ts src/app/api/feedback/quality-trend/route.test.ts -x
```
</verification>

<success_criteria>
- TREND-02 filters enforced server-side before buildQualityTrendReport
- trend_global gate no longer placeholder-only; runSampleCoverage orchestrator wires runQualityTrend
- Evidence artifacts ready for Phase 137 operational gate
</success_criteria>

<output>
After completion, create `.planning/phases/136-quality-trend-dashboard/136-02-SUMMARY.md`
</output>
