---
phase: 136-quality-trend-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
autonomous: true
requirements:
  - TREND-01
  - TREND-02
  - TREND-03
  - TREND-04
user_setup: []
must_haves:
  truths:
    - "Owner sees dedicated Trend tab with LineChart for meanHumanVisualScore and factualPassRate over ISO weeks"
    - "Owner can filter trends by mode, format, client profile and failure reason alongside cohort"
    - "Regression, stale evidence and insufficient coverage render as separate alert chips"
    - "Selecting a bucket shows bounded evidence drilldown table with corpus item refs"
    - "Panel hides when all six APIs (including quality-trend) return 403"
  artifacts:
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: Trend tab, chart, filters, alert chips, drilldown
      contains: "trend"
    - path: app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
      provides: Trend tab and sixth 403 gate tests
  key_links:
    - from: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      to: /api/feedback/quality-trend
      via: "fetchQualityTrendReport with filter query params"
      pattern: "quality-trend"
    - from: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      to: recharts LineChart
      via: "dynamic import ssr:false matching CreditChart pattern"
      pattern: "LineChart"
---


<objective>
Deliver owner-facing Trend tab on HumanQualityCorpusPanel: recharts time series, dimensional filters, separate alert chips, and bounded evidence drilldown.

Purpose: TREND-01..04 require UI that makes live quality movement actionable without mixing snapshot tabs or fixture metrics.
Output: Trend tab integrated into panel with sixth 403-hide gate and component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-quality-trend-dashboard/136-RESEARCH.md
@.planning/phases/136-quality-trend-dashboard/136-02-PLAN.md

<interfaces>
From HumanQualityCorpusPanel.tsx (extend):
```typescript
const PANEL_TABS = [
  { id: "queue", label: "Queue" },
  { id: "calibration", label: "Calibration" },
  { id: "impact", label: "Impact" },
  { id: "quality", label: "Quality" },
  { id: "coverage", label: "Coverage" },
  // ADD: { id: "trend", label: "Trend" }
];
```

From CreditChart.tsx pattern:
```typescript
const LineChart = dynamic(() => import("recharts").then((m) => m.LineChart), { ssr: false });
// stroke="var(--color-chart-1)" etc.
```

API response shape (136-02):
```typescript
{ report: QualityTrendReport } // buckets[], alertFlags, sampleGuidance, status
```

Existing components to reuse:
- `SampleGuidanceList` for insufficientCoverage guidance
- Cohort filter row pattern from calibration/impact/quality tabs
- Impact/Calibration drilldown table column pattern (corpusItemId prefix, visual, factual, evaluatedAt)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trend tab data fetching and sixth 403 gate</name>
  <files>
    app/src/components/feedback/HumanQualityCorpusPanel.tsx
    app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  </files>
  <action>
    Add `{ id: "trend", label: "Trend" }` to PANEL_TABS.

    Add filter state for TREND-02 (alongside existing cohortFilter):
    - generationMode (select or text matching queue dimension values)
    - format (select)
    - clientProfileId (text input uuid)
    - primaryFailureReason (select from HUMAN_QUALITY_FAILURE_REASONS)

    Implement `fetchQualityTrendReport(workspaceId, cohort, filters)` → GET `/api/feedback/quality-trend` with URLSearchParams for all dimensions; return null on 403.

    Add `useQuery` for trend report keyed by workspace + cohort + all filter dimensions.

    Extend 403-hide logic:
    - `trendForbidden = trendQuery.isFetched && trendQuery.data === null`
    - Panel returns null only when all **six** APIs forbidden (queue, calibration, impact, quality, coverage, trend)

    On evaluation submit success, invalidate trend query (same as calibration/impact invalidation pattern).

    Extend HumanQualityCorpusPanel.test.tsx:
    - Mock sixth fetcher
    - Assert panel hidden when all six return 403
    - Assert Trend tab appears in tab list
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -x</automated>
  </verify>
  <done>Trend query wired; sixth 403 gate tested; filters passed to API</done>
</task>

<task type="auto">
  <name>Task 2: LineChart series and separate alert chips</name>
  <files>
    app/src/components/feedback/HumanQualityCorpusPanel.tsx
    app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  </files>
  <action>
  Render Trend tab content when activeTab === "trend":

  **Chart (TREND-01):**
  - Dynamic-import LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid (ssr: false) per CreditChart.tsx
  - X-axis: bucketKey labels sorted asc
  - Line 1: meanHumanVisualScore (0–100, left Y-axis, var(--color-chart-1))
  - Line 2: factualPassRate as 0–100% or 0–1 with clear axis label (var(--color-chart-2))
  - Per-bucket learningImpactStatus: badge under chart or below x-axis tick (`ok` green / `insufficient_sample` amber) — NOT a causal improvement claim; label "Learning impact comparability"
  - When report.status === insufficient_sample: show SampleGuidanceList + withhold trend direction annotation (no up/down arrow claims)

  **Alert chips (TREND-03) — three separate UI elements, never one combined banner:**
  - `insufficientCoverage`: amber, uses SampleGuidanceList when alertFlags.insufficientCoverage
  - `staleEvidence`: blue/info chip "Corpus refreshed — re-run evidence CLI" when alertFlags.staleEvidence
  - `regressionDetected`: red chip with bucket labels from alertFlags.reasons when alertFlags.regressionDetected

  **Honesty:**
  - Do NOT plot fixture metrics from Quality tab
  - Show report.truncated warning when true
  - Show evidenceSource live_human badge

  Extend tests: alert chips render independently; insufficient_sample shows guidance not direction claim.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -x</automated>
  </verify>
  <done>Chart and three independent alert treatments render; tests cover flag separation</done>
</task>

<task type="auto">
  <name>Task 3: Bucket evidence drilldown table</name>
  <files>
    app/src/components/feedback/HumanQualityCorpusPanel.tsx
    app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  </files>
  <action>
  **Drilldown (TREND-04):**
  - Add bucket selector (click chart point or dropdown of bucketKeys)
  - When bucket selected, show expandable/read-only table from bucket.evidenceRefs.itemRefs
  - Columns: corpusItemId (truncated prefix), visualScore, factualPass, evaluatedAt — match Impact tab row pattern
  - When evidenceRefs.truncated, show "Showing 100 of {totalCount}" message
  - No deep links to campaign review (audit anchors only)

  **Filter row UI (TREND-02):**
  - Show dimension filters on Trend tab (same row style as cohort filter on other tabs)
  - Changing any filter refetches trend query

  Extend tests:
  - Bucket selection renders itemRefs rows
  - truncated message when truncated:true
  - Filter change triggers refetch (mock fetch call count or query key)
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -x</automated>
  </verify>
  <done>Evidence drilldown matches calibration/impact bounded table pattern; all TREND requirements surfaced in UI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → React render | XSS via corpus IDs unlikely (UUIDs); render as text not HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-07 | I | Trend tab visible to non-owner | mitigate | 403-hide cascade; tab fetch returns null on forbidden |
| T-136-08 | E | Misleading learning causation from chart | mitigate | Status badges labeled "comparability"; no delta arrows without ok status |
</threat_model>

<verification>
```bash
cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -x
cd app && npm run build
```
</verification>

<success_criteria>
- Owner can view and filter live human quality trends over time
- Three alert types visually distinct
- Each bucket links to bounded corpus evidence for audit
- Full test suite green after `cd app && npm test`
</success_criteria>

<output>
After completion, create `.planning/phases/136-quality-trend-dashboard/136-03-SUMMARY.md`
</output>
