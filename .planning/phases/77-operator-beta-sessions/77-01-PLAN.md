---
phase: 77-operator-beta-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/beta-sessions/types.ts
  - app/src/server/beta-sessions/types.test.ts
  - app/src/server/repositories/beta-sessions.ts
  - app/src/server/repositories/beta-sessions.test.ts
  - app/src/server/db/schema.ts
autonomous: true
requirements:
  - SESS-01
must_haves:
  truths:
    - "Runbook stage keys are a closed enum matching 67-BETA-RUNBOOK.md"
    - "Repository can create, list, end, and fetch beta sessions"
    - "Stage notes merge without overwriting unrelated stages"
  artifacts:
    - path: app/src/server/beta-sessions/types.ts
      provides: "Runbook stages and operator note shapes"
      exports: ["BETA_RUNBOOK_STAGES", "BetaRunbookStage", "BetaStageNote"]
    - path: app/src/server/repositories/beta-sessions.ts
      provides: "Session lifecycle persistence"
      exports: ["createBetaSession", "listBetaSessions", "endBetaSession", "mergeBetaSessionNotes"]
  key_links:
    - from: app/src/server/repositories/beta-sessions.ts
      to: app/src/server/db/schema.ts
      via: "betaSessions table insert/update"
      pattern: "betaSessions"
---

<objective>
Define beta session contracts and repository layer for operator session lifecycle (SESS-01 foundation).

Purpose: Interface-first foundation before owner API routes. Per D-03 no session CRUD was built in Phase 75.
Output: Types, repository with tests, extended operator_notes jsonb type.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-operator-beta-sessions/77-CONTEXT.md
@.planning/phases/77-operator-beta-sessions/77-RESEARCH.md
@app/src/server/repositories/beta-analytics.ts
@app/src/server/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runbook stage types and Zod schemas</name>
  <files>app/src/server/beta-sessions/types.ts, app/src/server/beta-sessions/types.test.ts</files>
  <behavior>
    - BETA_RUNBOOK_STAGES has exactly 9 keys from CONTEXT
    - assistanceLevelSchema accepts hands_on and observe_only only
    - stageNoteSchema accepts notes, tags, completedAt, blockerIds, feedbackReportId
  </behavior>
  <action>
    Create `types.ts` with closed `BETA_RUNBOOK_STAGES` array and exported Zod schemas. Update `schema.ts` operator_notes `$type` to include `blockerIds?: string[]` and `feedbackReportId?: string`. Write tests first.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-sessions/types.test.ts</automated>
  </verify>
  <done>Types exported; schema jsonb type aligned; tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Beta sessions repository</name>
  <files>app/src/server/repositories/beta-sessions.ts, app/src/server/repositories/beta-sessions.test.ts</files>
  <behavior>
    - createBetaSession inserts row with default assistance_level hands_on
    - listBetaSessions filters by workspaceId and activeOnly (endedAt null)
    - endBetaSession sets endedAt
    - mergeBetaSessionNotes deep-merges stage keys only
    - getBetaSessionById returns session by id (operator scope)
  </behavior>
  <action>
    Implement repository using drizzle `betaSessions` table. Mock db in tests following `beta-analytics.test.ts` pattern. Re-export or wrap existing `getBetaSessionById(workspaceId, sessionId)` from beta-analytics if needed; add `getBetaSessionByIdOnly` for owner GET by id.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/repositories/beta-sessions.test.ts</automated>
  </verify>
  <done>All repository functions tested; merge preserves unrelated stages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository→DB | Operator session data persisted per workspace FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | T | createBetaSession | mitigate | Validate workspace exists before insert |
| T-77-02 | I | operator_notes merge | mitigate | Merge only validated stage keys from Zod |
</threat_model>

<verification>
`cd app && npm test -- src/server/beta-sessions src/server/repositories/beta-sessions.test.ts`
</verification>

<success_criteria>
Repository layer complete with typed runbook stages and merge semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/77-operator-beta-sessions/77-01-SUMMARY.md`
</output>
