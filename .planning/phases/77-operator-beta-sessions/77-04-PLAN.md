---
phase: 77-operator-beta-sessions
plan: 04
type: execute
wave: 4
depends_on:
  - 77-03
files_modified:
  - app/src/components/feedback/BetaSessionsPanel.tsx
  - app/src/components/feedback/BetaSessionsPanel.test.tsx
  - app/src/app/(dashboard)/feedback/page.tsx
  - app/src/server/repositories/beta-sessions.fixture.ts
  - app/src/server/repositories/beta-sessions.fixture.test.ts
  - .planning/phases/77-operator-beta-sessions/77-SESSION-ARTIFACTS.md
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
must_haves:
  truths:
    - "Operator sees Beta Sessions panel on /feedback"
    - "Starting session sets sessionStorage adscale_beta_session_id"
    - "Runbook checklist shows per-stage note form and mark complete"
    - "Example fixture session documented for tests"
    - "77-SESSION-ARTIFACTS.md template exists for operator UAT"
  artifacts:
    - path: app/src/components/feedback/BetaSessionsPanel.tsx
      provides: "Session runner UI"
    - path: .planning/phases/77-operator-beta-sessions/77-SESSION-ARTIFACTS.md
      provides: "Operator session evidence template"
    - path: app/src/server/repositories/beta-sessions.fixture.ts
      provides: "Documented example session for tests"
  key_links:
    - from: app/src/components/feedback/BetaSessionsPanel.tsx
      to: /api/feedback/beta-sessions
      via: "TanStack Query mutations"
      pattern: "api/feedback/beta-sessions"
    - from: app/src/components/feedback/BetaSessionsPanel.tsx
      to: sessionStorage
      via: "BETA_SESSION_STORAGE_KEY on start"
      pattern: "adscale_beta_session_id"
---

<objective>
Operator UI on /feedback, sessionStorage wiring, artifacts template, and fixture session (SESS-01 UI, SESS-02 UI, SESS-03 capability, SESS-04).

Purpose: Enable operator-run sessions with durable evidence. SESS-03 real session count is human UAT.
Output: BetaSessionsPanel, feedback page integration, SESSION-ARTIFACTS template, fixture.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/77-operator-beta-sessions/77-CONTEXT.md
@app/src/app/(dashboard)/feedback/page.tsx
@app/src/lib/beta-analytics/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BetaSessionsPanel component</name>
  <files>app/src/components/feedback/BetaSessionsPanel.tsx, app/src/components/feedback/BetaSessionsPanel.test.tsx</files>
  <action>
    Build panel: start form (workspaceId, cohortLabel, assistanceLevel), active session display (IDs with copy buttons), runbook checklist for all BETA_RUNBOOK_STAGES with textarea/tags/completed toggle, end session button. On successful start, `sessionStorage.setItem(BETA_SESSION_STORAGE_KEY, session.id)` and clear on end. Use TanStack Query. Test: renders stages, calls start API mock, sets sessionStorage.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/BetaSessionsPanel.test.tsx</automated>
  </verify>
  <done>Panel renders and sessionStorage set on start in tests</done>
</task>

<task type="auto">
  <name>Task 2: Wire panel into feedback page</name>
  <files>app/src/app/(dashboard)/feedback/page.tsx</files>
  <action>
    Add BetaSessionsPanel above triage grid (full-width section). Operator-only — panel handles 403 gracefully like existing page.
  </action>
  <verify>
    <automated>cd app && npm run lint -- --max-warnings=0 src/app/\(dashboard\)/feedback/page.tsx src/components/feedback/BetaSessionsPanel.tsx</automated>
  </verify>
  <done>Feedback page includes Beta Sessions section</done>
</task>

<task type="auto">
  <name>Task 3: Artifacts template and fixture session</name>
  <files>.planning/phases/77-operator-beta-sessions/77-SESSION-ARTIFACTS.md, app/src/server/repositories/beta-sessions.fixture.ts, app/src/server/repositories/beta-sessions.fixture.test.ts</files>
  <action>
    Create SESSION-ARTIFACTS.md template with fields: date, workspaceId, sessionId, stages completed, blockers, linked event/session IDs, feedback report links. Export `EXAMPLE_BETA_SESSION_FIXTURE` constant matching happy-path runbook stages for summary tests. Test asserts fixture shape satisfies summary builder expectations.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/repositories/beta-sessions.fixture.test.ts</automated>
  </verify>
  <done>Template exists; fixture documented and tested</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-07 | I | sessionStorage | accept | Operator-only UI; session id is non-secret grouping key |
</threat_model>

<verification>
`cd app && npm test -- src/components/feedback/BetaSessionsPanel src/server/repositories/beta-sessions.fixture.test.ts`
</verification>

<success_criteria>
Operator can run sessions from /feedback; artifacts template ready for SESS-03 UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/77-operator-beta-sessions/77-04-SUMMARY.md`
</output>
