---
phase: 123-visual-validation-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/ai/creative-validation-aggregation.ts
  - app/tests/unit/ai/creative-validation-thresholds.test.ts
  - app/scripts/check-creative-validation-evidence.mjs
  - .planning/phases/123-visual-validation-gate/123-EVIDENCE.template.json
  - .planning/phases/123-visual-validation-gate/123-EVIDENCE.before-fixture.json
autonomous: true
requirements:
  - QA-18
  - QA-19
must_haves:
  truths:
    - "Threshold aggregation computes meanQualityScore and factualFidelityRate from after captures using post-ceiling qualityScore"
    - "FIDELITY_CODES set matches production gate fidelity-class hard failure codes"
    - "check-creative-validation-evidence.mjs --stage before validates matrix completeness and before capture schema without requiring after captures"
    - "check-creative-validation-evidence.mjs --stage after validates after capture schema, PNG existence, and sha256 without enforcing aggregate thresholds"
  artifacts:
    - path: app/src/server/ai/creative-validation-aggregation.ts
      provides: shared threshold math for operator script and tests
      exports:
        - FIDELITY_HARD_FAILURE_CODES
        - computeCreativeValidationAggregate
        - capturePassesFactualFidelity
    - path: app/scripts/check-creative-validation-evidence.mjs
      provides: CI evidence validator (before stage implemented)
    - path: app/tests/unit/ai/creative-validation-thresholds.test.ts
      provides: QA-19 threshold unit tests
    - path: .planning/phases/123-visual-validation-gate/123-EVIDENCE.template.json
      provides: evidence JSON schema reference for operator
  key_links:
    - from: app/scripts/check-creative-validation-evidence.mjs
      to: app/scripts/creative-validation-matrix.ts
      via: import matrix keys for required beforeCaptures
      pattern: CREATIVE_VALIDATION_MATRIX
    - from: app/src/server/ai/creative-validation-aggregation.ts
      to: app/src/server/ai/creative-quality-gate.ts
      via: FIDELITY codes aligned with classifyCreativeQualityGate outputs
      pattern: invented_factual_entity
---

# Plan 123-02: Threshold Aggregation + Evidence Check Skeleton (QA-18, QA-19)

<objective>
Implement shared threshold aggregation (≥75 mean, ≥95% factual fidelity) and CI-safe evidence check script with `--stage before` and `--stage after` support plus evidence template.

Purpose: CI can validate evidence structure before and after operator capture; threshold math is unit-tested independently; aggregate thresholds deferred to `--stage final` in plan 04.

Output: `creative-validation-aggregation.ts`, `check-creative-validation-evidence.mjs` (before + after stages), threshold unit tests, evidence template, before-only fixture.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-visual-validation-gate/123-RESEARCH.md
@.planning/phases/123-visual-validation-gate/123-VALIDATION.md
@app/scripts/check-visual-evidence.mjs
@app/src/server/ai/creative-quality-gate.ts

<interfaces>
Fidelity codes (per RESEARCH Pattern 3 — QA-19/QA-20):
```typescript
const FIDELITY_HARD_FAILURE_CODES = new Set([
  "invented_factual_entity",
  "campaign_identity_drift",
  "style_reference_contamination",
  "wrong_brand",
  "unauthorized_brand_or_ip",
  "unsupported_offer",
  "replaced_source_subject",
]);
```

Aggregation (apply only to `afterCaptures`):
```typescript
function computeCreativeValidationAggregate(afterCaptures: AfterCapture[]): {
  meanQualityScore: number;
  factualFidelityRate: number;
  fidelityPassCount: number;
  totalCount: number;
};
```

Evidence paths (mirror Phase 109/114):
- Evidence: `.planning/phases/123-visual-validation-gate/123-EVIDENCE.json`
- After PNGs: `app/exports/render-creatives/validation-after/{matrixKey}__{shortHash}.png`
- Before refs: corpus path or `public_url` with sha256 in evidence
</interfaces>
</context>

<tasks>

<task id="123-02-01" type="auto" tdd="true">
  <name>Threshold aggregation module and unit tests</name>
  <files>app/src/server/ai/creative-validation-aggregation.ts, app/tests/unit/ai/creative-validation-thresholds.test.ts</files>
  <behavior>
    - meanQualityScore 76 with 6 captures passes ≥75 threshold check
    - meanQualityScore 74 fails
    - factualFidelityRate 5/6 (0.833) fails; 6/6 (1.0) passes ≥0.95
    - capture with invented_factual_entity hard failure fails fidelity check even if qualityScore is 90
    - empty afterCaptures throws or returns explicit error (no divide-by-zero)
  </behavior>
  <action>
1. Create `creative-validation-aggregation.ts` exporting:
   - `FIDELITY_HARD_FAILURE_CODES` (Set, frozen list per RESEARCH — do not invent new codes)
   - `capturePassesFactualFidelity(capture)` — true when no hardFailure.code in FIDELITY set
   - `computeCreativeValidationAggregate(afterCaptures)` — mean of `qualityScore`, fidelity rate
   - `assertThresholdsMet(aggregate)` — throws with descriptive message if mean &lt; 75 or rate &lt; 0.95
   - `twelveCriteriaPresent(capture)` — boolean helper verifying presence of all 6 QA `checklist` keys and all 7 `scoreBreakdown` keys (13 keys total; `briefMatch` appears in both surfaces — presence check is per-key, not deduplicated to 12). Used by `--stage after` schema validation.

2. Create `creative-validation-thresholds.test.ts` with fixture JSON objects (no images) covering pass/fail edge cases at 75 and 0.95 boundaries.

Use capped `qualityScore` values in fixtures (post-ceiling semantics per Phase 121).

Do NOT call OpenAI or gate classifiers in this task — pure aggregation on evidence-shaped objects.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-validation-thresholds.test.ts</automated>
  </verify>
  <done>QA-19 threshold math unit-tested; aggregation module ready for operator script import.</done>
</task>

<task id="123-02-02" type="auto">
  <name>Evidence template, before fixture, and check script --stage before</name>
  <files>app/scripts/check-creative-validation-evidence.mjs, .planning/phases/123-visual-validation-gate/123-EVIDENCE.template.json, .planning/phases/123-visual-validation-gate/123-EVIDENCE.before-fixture.json</files>
  <behavior>
    - `node app/scripts/check-creative-validation-evidence.mjs --stage before --evidence .planning/phases/123-visual-validation-gate/123-EVIDENCE.before-fixture.json` exits 0
    - Script rejects paths outside repo root (no `..` traversal)
    - Script requires every matrix key in beforeCaptures with source corpus, sha256, path or public_url
    - Unknown --stage values print usage and exit non-zero
  </behavior>
  <action>
1. Create `123-EVIDENCE.template.json` documenting schema:
   - `schemaVersion`, `capturedAt`, `seedSupported: false`
   - `pipeline`: `{ promptHash, openaiImageModel, matrixVersion }`
   - `beforeCaptures[]`: `{ key, source: "corpus", corpusRefId, path, sha256, optional public_url, optional auditArchetype }`
   - `afterCaptures[]`: shape for operator (key, source: "regenerated", path, sha256, qa, score, gate fields)
   - `aggregate`: `{ meanQualityScore, factualFidelityRate }` (populated at final)
   - `requirements`: QA-18–21 result rows

2. Create `123-EVIDENCE.before-fixture.json` — minimal before-only evidence for CI harness:
   - All 6 matrix keys in `beforeCaptures` with valid corpus refs and sha256 (compute from on-disk corpus PNG when present, or placeholder sha256 + `public_url` from manifest when local file missing per RESEARCH resolved policy)
   - Empty `afterCaptures: []`
   - No aggregate thresholds required

3. Create `check-creative-validation-evidence.mjs` adapted from `check-visual-evidence.mjs`:
   - Usage: `node app/scripts/check-creative-validation-evidence.mjs --stage before|after|final [--evidence PATH]` (default evidence path: `123-EVIDENCE.json`)
   - Load matrix keys dynamically from `creative-validation-matrix.ts` via `tsx` subprocess (requires plan 01 matrix)
   - **before stage**: validate evidence file; every matrix key has beforeCapture; sha256 matches on-disk PNG when path exists
   - **final stage**: stub throwing "not implemented — complete in plan 123-04"
   - Path resolve under `repoRoot`; reject `..`
   - SHA256 via `createHash` (mirror check-visual-evidence.mjs `hash()`)

4. If evidence missing, fail with message pointing to `123-EVIDENCE.template.json` and plan 123-03 operator run.

Do NOT enforce ≥75/≥95 in before stage.
  </action>
  <verify>
    <automated>node app/scripts/check-creative-validation-evidence.mjs --stage before --evidence .planning/phases/123-visual-validation-gate/123-EVIDENCE.before-fixture.json</automated>
  </verify>
  <done>Evidence schema documented; before-stage check exits 0 against committed before-only fixture.</done>
</task>

<task id="123-02-03" type="auto">
  <name>Check script --stage after (schema + sha256, no thresholds)</name>
  <files>app/scripts/check-creative-validation-evidence.mjs</files>
  <behavior>
    - `node app/scripts/check-creative-validation-evidence.mjs --stage after` exits 0 when after captures structurally valid (no aggregate threshold enforcement)
    - Requires `afterCaptures` for every matrix key with `source: "regenerated"`
    - Validates paths under `validation-after/`; sha256 matches on-disk PNG
    - Each after capture has `qualityScore`, `qualityVerdict`, `hardFailures`, `qa.checklist`, `score.scoreBreakdown`
    - `twelveCriteriaPresent` passes (6 QA keys + 7 score keys present)
    - Does NOT assert meanQualityScore ≥ 75 or factualFidelityRate ≥ 0.95 (deferred to --stage final in plan 04)
  </behavior>
  <action>
Extend `check-creative-validation-evidence.mjs` with **--stage after** (unblocks plan 03 checkpoint):

1. Require `afterCaptures` for every matrix key (in addition to before captures).
2. Validate `source: "regenerated"` paths resolve under `app/exports/render-creatives/validation-after/`.
3. sha256 match on disk for each after PNG.
4. Structural fields per capture: `qualityScore`, `qualityVerdict`, `hardFailures`, `qa.checklist`, `score.scoreBreakdown`.
5. Run `twelveCriteriaPresent` equivalent (import via tsx or inline parity with aggregation module).
6. Log fidelity hard failures as warnings only — do NOT fail on fidelity codes or aggregate thresholds (operator may iterate before plan 04 final gate).

**--stage final** remains stubbed until plan 123-04-01.

Do NOT enforce aggregate thresholds or prompt-hash freshness in after stage.
  </action>
  <verify>
    <automated>node app/scripts/check-creative-validation-evidence.mjs --stage after 2>&1 | grep -qE 'afterCaptures|after capture|CREATIVE-EVIDENCE'</automated>
  </verify>
  <done>After-stage structural validation implemented; checkpoint in plan 03 can verify evidence without waiting for plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence JSON paths → filesystem | Untrusted paths could escape repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-02 | Tampering | Evidence path traversal | mitigate | resolve under repoRoot; reject `..` |
| T-123-03 | Information disclosure | OPENAI_API_KEY in logs | accept | Check script never calls OpenAI |
| T-123-04 | Repudiation | Stale threshold math | mitigate | Single aggregation module + unit tests |
</threat_model>

<verification>
Per @.planning/phases/123-visual-validation-gate/123-VALIDATION.md:
- `cd app && npm test -- tests/unit/ai/creative-validation-thresholds.test.ts`
- `node app/scripts/check-creative-validation-evidence.mjs --stage before --evidence .planning/phases/123-visual-validation-gate/123-EVIDENCE.before-fixture.json`
- After plan 03 checkpoint: `node app/scripts/check-creative-validation-evidence.mjs --stage after`
</verification>

<success_criteria>
- QA-19: Threshold aggregation tested at boundaries
- QA-18: Before-stage evidence validates matrix pairing schema
</success_criteria>

<output>
After completion, create `.planning/phases/123-visual-validation-gate/123-02-SUMMARY.md`
</output>
