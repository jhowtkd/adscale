---
phase: 123-visual-validation-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - app/scripts/run-creative-validation.ts
  - app/scripts/load-env.ts
  - app/tests/fixtures/creative-corpus/base-assets/.gitkeep
  - app/tests/fixtures/creative-corpus/base-assets/nr1-1x1-base.png
  - app/tests/fixtures/creative-corpus/base-assets/educacao-base.png
  - app/tests/fixtures/creative-corpus/base-assets/educacao-style-ref.png
  - app/tests/fixtures/creative-corpus/base-assets/master-nr1-base.png
  - app/tests/fixtures/creative-corpus/base-assets/smoke-base.png
  - app/package.json
  - .planning/phases/123-visual-validation-gate/123-EVIDENCE.json
  - app/exports/render-creatives/validation-after/.gitkeep
autonomous: false
requirements:
  - QA-18
  - QA-19
  - QA-20
user_setup:
  - service: openai
    why: "Live image regeneration and vision QA/score for operator evidence capture"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard → API keys (operator machine only — never CI)"
must_haves:
  truths:
    - "Operator script regenerates post-fix images for all 6 matrix cells using production prompt-builder and images.edit path"
    - "Each after capture scored via analyzeCreativeQa + analyzeDerivationCreative + computeQualityGateFromAnalysis (post-ceiling)"
    - "123-EVIDENCE.json contains paired before/after rows for every matrix key with sha256 hashes"
    - "No after capture has fidelity-class hard failures (QA-20 gate codes)"
  artifacts:
    - path: app/scripts/run-creative-validation.ts
      provides: operator generate + score + evidence writer
    - path: .planning/phases/123-visual-validation-gate/123-EVIDENCE.json
      provides: committed validation evidence
    - path: app/exports/render-creatives/validation-after/
      provides: post-fix regenerated PNG subset
    - path: app/tests/fixtures/creative-corpus/base-assets/
      provides: sanitized committed base assets for matrix
  key_links:
    - from: app/scripts/run-creative-validation.ts
      to: app/src/server/ai/creative-qa.ts
      via: analyzeCreativeQa on regenerated buffer
      pattern: analyzeCreativeQa
    - from: app/scripts/run-creative-validation.ts
      to: app/src/server/ai/creative-quality-gate.ts
      via: computeQualityGateFromAnalysis
      pattern: computeQualityGateFromAnalysis
    - from: app/scripts/run-creative-validation.ts
      to: app/scripts/creative-validation-matrix.ts
      via: CREATIVE_VALIDATION_MATRIX drives loop
      pattern: CREATIVE_VALIDATION_MATRIX
---

# Plan 123-03: Operator Capture Script + Base Assets (QA-18, QA-19, QA-20)

<objective>
Build repo-relative operator orchestrator that regenerates post-fix creatives, scores via production analyzers, and commits `123-EVIDENCE.json` plus `validation-after/` PNGs meeting threshold targets.

Purpose: QA-18 live validation happens operator-side; evidence becomes CI input for plan 123-04.

Output: `run-creative-validation.ts`, committed base assets, evidence JSON, validation-after PNGs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-visual-validation-gate/123-RESEARCH.md
@.planning/phases/123-visual-validation-gate/123-VALIDATION.md
@app/scripts/creative-validation-matrix.ts
@app/src/server/ai/creative-validation-aggregation.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/derivation.ts
@app/exports/render-creatives/manifest.json

<interfaces>
From production scoring path (RESEARCH — reuse exactly, no hand-rolled entity detection per QA-20):
```typescript
import { analyzeCreativeQa } from "@/server/ai/creative-qa";
import { analyzeDerivationCreative } from "@/server/ai/creative-score";
import { computeQualityGateFromAnalysis } from "@/server/ai/creative-quality-gate";
import { buildDerivationPrompt } from "@/server/ai/prompt-builder";
```

Operator script must NOT use `generate-all-outputs.ts` desktop paths.

Reproducibility when seed unsupported (QA-18):
- Record `seedSupported: false`
- Record `promptProvenanceHash` = `git hash-object app/src/server/ai/prompt-builder.ts`
- Record `openaiImageModel` from env
</interfaces>
</context>

<tasks>

<task id="123-03-01" type="auto">
  <name>Commit sanitized base assets and package scripts</name>
  <files>app/tests/fixtures/creative-corpus/base-assets/*, app/package.json, app/exports/render-creatives/validation-after/.gitkeep</files>
  <action>
1. Create `app/tests/fixtures/creative-corpus/base-assets/` with committed PNGs referenced by matrix `baseAsset` fields:
   - `nr1-1x1-base.png` (teste-3-nr1 rows)
   - `educacao-base.png`, `educacao-style-ref.png` (nova-campanha restyling)
   - `master-nr1-base.png` (teste-campanha-nr1)
   - `smoke-base.png` (smoke)

Source images from corpus parent exports or canonical campaign fixtures — **sanitize**: no customer PII beyond already-public corpus; resize/normalize with sharp to reasonable dimensions (≤2048px). Do not commit full 34-piece corpus.

2. Add `app/package.json` scripts:
   - `"validate:creative": "node scripts/check-creative-validation-evidence.mjs --stage final"`
   - `"validate:creative:live": "npx tsx scripts/run-creative-validation.ts"`
   - `"creative-release-gate": "node scripts/run-creative-release-gate.mjs"` (orchestrator added in plan 04 — stub can fail gracefully until then)

3. Ensure `validation-after/` directory exists with `.gitkeep`.

Do NOT run live OpenAI in this task.
  </action>
  <verify>
    <automated>test -f app/tests/fixtures/creative-corpus/base-assets/nr1-1x1-base.png && test -f app/tests/fixtures/creative-corpus/base-assets/smoke-base.png</automated>
  </verify>
  <done>Base assets on disk; package.json scripts registered.</done>
</task>

<task id="123-03-02" type="auto">
  <name>Implement run-creative-validation.ts operator orchestrator</name>
  <files>app/scripts/run-creative-validation.ts</files>
  <action>
Create `app/scripts/run-creative-validation.ts` (import `./load-env` first):

**Before captures** (no regeneration):
- For each matrix row, resolve before PNG from `app/exports/render-creatives/{manifest fileName}` OR fetch `public_url` from manifest when local missing
- Compute sha256; record `source: "corpus"`, `corpusRefId`, `auditArchetype` when known

**After captures** (regeneration + scoring):
- Build `CreativeContract` from `CANONICAL_CAMPAIGNS[row.canonicalSlug]`
- `buildDerivationPrompt` with row mode/format/creativeLevel
- Call OpenAI `images.edit` following `derivation.ts` patterns (restyling: [base, style] asset order per Phase 121)
- Write PNG to `app/exports/render-creatives/validation-after/{key}__{sha256.slice(0,8)}.png`
- Run `analyzeCreativeQa` + `analyzeDerivationCreative` + `computeQualityGateFromAnalysis` (ceilings applied)
- Store normalized `qa`, `score`, `gate`, `hardFailures`, `qualityVerdict`, `qualityScore` on capture

**Evidence output**:
- Write `.planning/phases/123-visual-validation-gate/123-EVIDENCE.json`
- Set `pipeline.promptHash`, `seedSupported: false`, `openaiImageModel`
- Compute `aggregate` via `computeCreativeValidationAggregate`
- CLI flags: `--dry-run` (matrix + before only), `--skip-regen` (score existing after PNGs), `--matrix-key` (single cell)

**QA-20**: Do not add custom entity CV — gate `hardFailures` are authoritative. Script logs any fidelity-code failures prominently.

Reject if `OPENAI_API_KEY` missing (clear error message).

Repo-relative paths only — never `/Users/jhonatan/Desktop/...`.
  </action>
  <verify>
    <automated>cd app && npx tsx scripts/run-creative-validation.ts --dry-run</automated>
  </verify>
  <done>Operator script dry-run succeeds; full implementation ready for live capture.</done>
</task>

<task id="123-03-03" type="checkpoint:human-action" gate="blocking">
  <name>Operator live capture with OPENAI_API_KEY</name>
  <files>.planning/phases/123-visual-validation-gate/123-EVIDENCE.json, app/exports/render-creatives/validation-after/*.png</files>
  <action>
Human operator runs live capture — Claude cannot invoke OpenAI image generation without user API key and approval.

Steps for operator:
1. Set `OPENAI_API_KEY` in `app/.env.local`
2. Run `cd app && npm run validate:creative:live`
3. If thresholds fail, inspect `hardFailures` in evidence JSON and re-run after prompt fixes OR use `--matrix-key` for single-cell iteration
4. Git-add `123-EVIDENCE.json` and `validation-after/` PNGs when aggregate passes
  </action>
  <verify>
    <automated>node app/scripts/check-creative-validation-evidence.mjs --stage after</automated>
  </verify>
  <done>123-EVIDENCE.json committed with 6 after captures; each PNG sha256-valid; twelveCriteriaPresent passes; no aggregate threshold check at this checkpoint (thresholds enforced in plan 04 --stage final)</done>
  <what-built>`run-creative-validation.ts`, base assets, and evidence check `--stage after` from plans 01–02</what-built>
  <how-to-verify>
1. Ensure `OPENAI_API_KEY` is set in `app/.env.local` (operator machine only).
2. Run: `cd app && npm run validate:creative:live`
3. Confirm script completes and writes evidence + PNGs.
4. Verify outputs exist:
   - `.planning/phases/123-visual-validation-gate/123-EVIDENCE.json`
   - `app/exports/render-creatives/validation-after/*.png` (6 files)
5. Run structural check (no threshold enforcement): `node app/scripts/check-creative-validation-evidence.mjs --stage after`
6. Git-add evidence JSON and validation-after PNGs for plan 04 final gate.
  </how-to-verify>
  <resume-signal>Type "captured" when evidence is committed and --stage after passes, or describe structural failures to debug</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator machine → OpenAI API | API key and image bytes leave repo |
| regenerated PNG → vision models | Model output is untrusted until gate classifies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-05 | Information disclosure | OPENAI_API_KEY | mitigate | load-env only; never log key; operator-only script |
| T-123-06 | Tampering | Customer production assets | mitigate | Canonical fixtures + base-assets only |
| T-123-07 | Repudiation | Uncapped scores in evidence | mitigate | computeQualityGateFromAnalysis applies Phase 121 ceilings |
</threat_model>

<verification>
Per @.planning/phases/123-visual-validation-gate/123-VALIDATION.md:
- `cd app && npx tsx scripts/run-creative-validation.ts --dry-run`
- After checkpoint: `node app/scripts/check-creative-validation-evidence.mjs --stage after` (structural only; thresholds in plan 04)
</verification>

<success_criteria>
- QA-18: All 6 matrix cells have paired before/after captures in evidence
- QA-19: Evidence includes aggregate fields (threshold enforcement deferred to plan 04 `--stage final`)
- QA-20: After captures scored via production gate (fidelity hard-failure enforcement in plan 04 final check)
</success_criteria>

<output>
After completion, create `.planning/phases/123-visual-validation-gate/123-03-SUMMARY.md`
</output>
