---
phase: 123-visual-validation-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/scripts/creative-validation-matrix.ts
  - app/tests/unit/ai/creative-validation-matrix.test.ts
autonomous: true
requirements:
  - QA-18
must_haves:
  truths:
    - "Validation matrix defines exactly one row per representative mode×format cell with canonical campaign slug and before corpus anchor"
    - "All three derivation modes (art_variation, restyling, format_adaptation) appear in the matrix"
    - "Matrix keys are stable strings usable as evidence capture identifiers"
  artifacts:
    - path: app/scripts/creative-validation-matrix.ts
      provides: CREATIVE_VALIDATION_MATRIX single source of truth
      exports:
        - CREATIVE_VALIDATION_MATRIX
        - CreativeValidationMatrixRow
        - matrixKeys
    - path: app/tests/unit/ai/creative-validation-matrix.test.ts
      provides: QA-18 matrix coverage regression
  key_links:
    - from: app/scripts/creative-validation-matrix.ts
      to: app/src/server/ai/corpus-fixtures.ts
      via: beforeCorpusRefId anchors to audit archetypes
      pattern: beforeCorpusRefId
    - from: app/scripts/creative-validation-matrix.ts
      to: app/src/server/ai/creative-corpus.ts
      via: canonicalSlug resolves CANONICAL_CAMPAIGNS
      pattern: canonicalSlug
---

# Plan 123-01: Validation Matrix (QA-18)

<objective>
Define the canonical 6-cell creative validation matrix covering all three derivation modes with paired before-corpus anchors and committed base asset filenames.

Purpose: Every downstream script (operator capture, evidence check, release gate) reads one matrix — no ad-hoc campaign lists.

Output: `creative-validation-matrix.ts` + matrix coverage unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-visual-validation-gate/123-RESEARCH.md
@.planning/phases/123-visual-validation-gate/123-VALIDATION.md
@app/src/server/ai/corpus-fixtures.ts
@app/src/server/ai/creative-corpus.ts
@app/exports/render-creatives/manifest.json

<interfaces>
From `app/src/server/ai/creative-corpus.ts`:
```typescript
export type CanonicalCampaignSlug = "teste-3-nr1" | "nova-campanha" | "teste-campanha-nr1" | "smoke" | ...;
export const CANONICAL_CAMPAIGNS: Record<CanonicalCampaignSlug, CanonicalCampaign>;
```

From `app/src/server/ai/corpus-fixtures.ts`:
```typescript
export type CorpusArchetype =
  | "invented_factual_entity"
  | "campaign_identity_drift"
  | "style_reference_contamination"
  | "visual_overload"
  | ...;
// CORPUS_ARCHETYPE_FIXTURES[].corpusRefIds — use as beforeCorpusRefId anchors
```

Recommended 6-cell matrix (per RESEARCH Pattern 5 — honor exactly unless corpus ref missing):
| key | mode | format | canonicalSlug | beforeCorpusRefId |
|-----|------|--------|---------------|-------------------|
| teste-3-nr1:art_variation:1:1 | art_variation | 1:1 | teste-3-nr1 | 6fc63100 |
| teste-3-nr1:format_adaptation:9:16 | format_adaptation | 9:16 | teste-3-nr1 | 27069645 |
| teste-3-nr1:format_adaptation:4:5 | format_adaptation | 4:5 | teste-3-nr1 | c2c12774 |
| nova-campanha:restyling:1:1 | restyling | 1:1 | nova-campanha | d7d9d323 |
| teste-campanha-nr1:art_variation:1:1 | art_variation | 1:1 | teste-campanha-nr1 | 8a2bebf9 |
| smoke:art_variation:1:1 | art_variation | 1:1 | smoke | 8907bce5 |
</interfaces>
</context>

<tasks>

<task id="123-01-01" type="auto" tdd="true">
  <name>Define CREATIVE_VALIDATION_MATRIX with typed rows</name>
  <files>app/scripts/creative-validation-matrix.ts</files>
  <behavior>
    - matrixKeys() returns 6 unique keys matching `{slug}:{mode}:{format}` pattern
    - Every row has canonicalSlug present in CANONICAL_CAMPAIGNS
    - restyling row includes styleAsset; other modes omit it
    - Each row documents creativeLevel (balanced or per RESEARCH), baseAsset filename, beforeCorpusRefId
    - seedSupported: false exported as constant (OpenAI image API has no seed in ADScale)
  </behavior>
  <action>
Create `app/scripts/creative-validation-matrix.ts`:

1. Export `CreativeValidationMatrixRow` with fields: `key`, `canonicalSlug`, `mode`, `format`, `beforeCorpusRefId`, `creativeLevel`, `baseAsset`, optional `styleAsset`, optional `auditArchetype` (from manifest-index when known).

2. Export `CREATIVE_VALIDATION_MATRIX` as `const` array with exactly the 6 cells from RESEARCH (D-RESEARCH matrix — do not reduce to 3 cells).

3. Export helpers:
   - `matrixKeys(): string[]`
   - `matrixRowByKey(key: string): CreativeValidationMatrixRow | undefined`
   - `modesInMatrix(): Set&lt;DerivationMode&gt;` (all three modes must be present)

4. Export `CREATIVE_VALIDATION_SEED_SUPPORTED = false` for evidence JSON.

5. Resolve `beforeCorpusRefId` against `CORPUS_MANIFEST_INDEX` or manifest.json `idPrefix` — throw at module load if any ref id is unknown.

Do NOT add generation or scoring logic here — matrix only.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-validation-matrix.test.ts</automated>
  </verify>
  <done>Matrix module exports 6 rows; all corpus ref ids resolve; all three modes covered.</done>
</task>

<task id="123-01-02" type="auto">
  <name>Matrix coverage and corpus anchor tests</name>
  <files>app/tests/unit/ai/creative-validation-matrix.test.ts</files>
  <behavior>
    - Test asserts art_variation, restyling, format_adaptation each appear ≥1 time
    - Test asserts format_adaptation includes both 9:16 (failure archetype anchor) and 4:5 (faithful positive c2c12774)
    - Test asserts beforeCorpusRefId 27069645 (invented entity) and d7d9d323 (restyling contamination) are in matrix
    - Test asserts keys are unique and match `/^[\w-]+:(art_variation|restyling|format_adaptation):[\d:.]+$/`
  </behavior>
  <action>
Create `creative-validation-matrix.test.ts` importing from `../../../scripts/creative-validation-matrix.ts` (follow existing script import pattern in app tests).

Cover:
- Mode coverage (3/3 modes)
- Format coverage (1:1, 4:5, 9:16 at minimum)
- Corpus anchor presence for QA-20 failure archetypes (27069645, d7d9d323)
- Faithful positive anchor (c2c12774) for format_adaptation
- `canonicalSlug` values exist in `CANONICAL_CAMPAIGNS`
- `CREATIVE_VALIDATION_SEED_SUPPORTED === false`

No OpenAI, no image bytes.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-validation-matrix.test.ts</automated>
  </verify>
  <done>QA-18 matrix definition fully covered by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix → operator script | Fixed campaign contracts; no user-supplied slugs at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | Matrix row injection | mitigate | Const array + unit test key uniqueness |
| T-123-02 | Repudiation | Wrong corpus anchor | mitigate | Assert ref ids exist in manifest index at module load |
</threat_model>

<verification>
Per @.planning/phases/123-visual-validation-gate/123-VALIDATION.md task 123-01-01:
`cd app && npm test -- tests/unit/ai/creative-validation-matrix.test.ts`
</verification>

<success_criteria>
- QA-18: Matrix covers all modes in representative formats with stable keys and corpus before-anchors
- 6 cells committed; no scope reduction to fewer modes
</success_criteria>

<output>
After completion, create `.planning/phases/123-visual-validation-gate/123-01-SUMMARY.md`
</output>
