---
phase: 123-visual-validation-gate
plan: "04"
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - app/scripts/check-creative-validation-evidence.mjs
  - app/scripts/run-creative-release-gate.mjs
  - app/package.json
  - .planning/phases/123-visual-validation-gate/123-BASELINE.md
  - .planning/phases/123-visual-validation-gate/123-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - app/tests/unit/ai/creative-validation-evidence-guard.test.ts
autonomous: true
requirements:
  - QA-18
  - QA-19
  - QA-20
  - QA-21
must_haves:
  truths:
    - "check-creative-validation-evidence.mjs --stage final validates matrix pairing, sha256 hashes, thresholds, prompt hash freshness, and zero fidelity hard failures"
    - "run-creative-release-gate.mjs runs npm test, lint, build, then final evidence check"
    - "123-VERIFICATION.md documents QA-18–21 pass with automated command references"
    - "Milestone v12.3 visual validation gate is closable from CI without live OpenAI"
  artifacts:
    - path: app/scripts/run-creative-release-gate.mjs
      provides: QA-21 milestone release orchestrator
    - path: app/scripts/check-creative-validation-evidence.mjs
      provides: complete before/after/final validation
    - path: .planning/phases/123-visual-validation-gate/123-VERIFICATION.md
      provides: phase closure artifact
    - path: app/tests/unit/ai/creative-validation-evidence-guard.test.ts
      provides: deterministic QA-20 regression on evidence-shaped gate outputs
  key_links:
    - from: app/scripts/run-creative-release-gate.mjs
      to: app/scripts/check-creative-validation-evidence.mjs
      via: final stage after test/lint/build
      pattern: check-creative-validation-evidence.mjs --stage final
    - from: app/scripts/check-creative-validation-evidence.mjs
      to: app/src/server/ai/creative-validation-aggregation.ts
      via: threshold assertions (import via tsx or duplicated constants tested for parity)
      pattern: assertThresholdsMet
---

# Plan 123-04: Release Gate + Milestone Closure (QA-18–21)

<objective>
Complete CI evidence validator (`--stage final` only — `--stage after` delivered in plan 02), wire creative release gate orchestrator mirroring Phase 114, and close phase with verification artifacts and project state updates.

Purpose: QA-21 green regression + committed evidence thresholds = v12.3 shippable without live generation in CI.

Output: `run-creative-release-gate.mjs`, final-stage check script extension, `123-VERIFICATION.md`, roadmap/requirements/state updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-visual-validation-gate/123-RESEARCH.md
@.planning/phases/123-visual-validation-gate/123-VALIDATION.md
@app/scripts/run-release-gate.mjs
@app/scripts/check-visual-evidence.mjs
@app/scripts/check-creative-validation-evidence.mjs
@.planning/phases/123-visual-validation-gate/123-EVIDENCE.json

<interfaces>
From `app/scripts/run-release-gate.mjs` (mirror structure):
```javascript
const steps = [
  ["npm", ["test"], "unit"],
  ["npm", ["run", "lint"], "lint"],
  ["npm", ["run", "build"], "build"],
];
// then check script; update evidence.requirements
```

Final stage checks (RESEARCH):
- Every matrix key: before + after capture
- afterCaptures sha256 matches validation-after PNGs
- aggregate.meanQualityScore >= 75
- aggregate.factualFidelityRate >= 0.95
- No fidelity hard failures on any after capture
- pipeline.promptHash matches current prompt-builder.ts OR evidence.staleRefreshRequired documented
- twelveCriteriaPresent on each after capture
</interfaces>
</context>

<tasks>

<task id="123-04-01" type="auto">
  <name>Complete check script --stage final (thresholds + QA-20)</name>
  <files>app/scripts/check-creative-validation-evidence.mjs</files>
  <action>
Extend `check-creative-validation-evidence.mjs` — replace `--stage final` stub from plan 02 (do NOT re-implement `--stage after`; plan 02-03 owns structural after validation):

**--stage final** (runs all before + after structural checks from plan 02, then adds):
- Recompute aggregate from afterCaptures via `computeCreativeValidationAggregate`; assert meanQualityScore >= 75 and factualFidelityRate >= 0.95
- For each after capture: fail if any `hardFailures[].code` in FIDELITY_HARD_FAILURE_CODES (QA-20)
- `currentPromptHash()` via `git hash-object app/src/server/ai/prompt-builder.ts` — if mismatch with evidence.pipeline.promptHash, fail unless `evidence.pipeline.staleRefreshNote` documents refresh
- Write `123-BASELINE.md` (before/after table per matrix key with scores and verdicts)
- Write `123-VERIFICATION.md` (requirement rows QA-18–21)
- Set `evidence.requirements` pass rows with automated command strings
- Include dimension means in evidence/BASELINE for transparency (informationPreservation, briefMatch per capture) — informational only; QA-19 pass/fail uses gate-based fidelity rate per RESEARCH resolved policy
- Exit 0 only when all checks pass

Import aggregation helpers: use `npx tsx -e` wrapper or duplicate FIDELITY set with parity test in plan 04 task 2.

Mirror error prefix `CREATIVE-EVIDENCE:` for grep-friendly CI logs.
  </action>
  <verify>
    <automated>node app/scripts/check-creative-validation-evidence.mjs --stage final</automated>
  </verify>
  <done>Final evidence check passes on committed 123-EVIDENCE.json from plan 03; aggregate thresholds and fidelity hard failures enforced.</done>
</task>

<task id="123-04-02" type="auto" tdd="true">
  <name>Evidence guard test and creative release gate orchestrator</name>
  <files>app/scripts/run-creative-release-gate.mjs, app/package.json, app/tests/unit/ai/creative-validation-evidence-guard.test.ts</files>
  <behavior>
    - Guard test: synthetic after capture with invented_factual_entity fails fidelity assertion
    - Guard test: CORPUS_ARCHETYPE_FIXTURES gate outputs never produce pass when fidelity codes present in fixture JSON
    - run-creative-release-gate.mjs exits 0 when full suite + final evidence pass
  </behavior>
  <action>
1. Create `creative-validation-evidence-guard.test.ts`:
   - Test aggregation parity (optional cross-check constants match check script)
   - Test QA-20: evidence-shaped captures with fidelity codes fail `capturePassesFactualFidelity`
   - Import `CORPUS_ARCHETYPE_FIXTURES` synthetic QA through gate (existing pattern) — assert fidelity codes in validation denylist are never ignored

2. Create `run-creative-release-gate.mjs` mirroring `run-release-gate.mjs`:
   - `phaseDir` → `.planning/phases/123-visual-validation-gate`
   - `evidencePath` → `123-EVIDENCE.json`
   - Steps: `npm test` → `npm run lint` → `npm run build` → `node scripts/check-creative-validation-evidence.mjs --stage final`
   - Update `evidence.automated` and `evidence.requirements` for QA-18–21
   - Wire `app/package.json` `"creative-release-gate"` to this script (replace stub from plan 03)

3. Optionally add root-level npm script alias if monorepo pattern requires — follow Phase 114 convention (app-local only is fine).

Do NOT add live OpenAI to orchestrator.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-validation-evidence-guard.test.ts && node scripts/run-creative-release-gate.mjs</automated>
  </verify>
  <done>QA-21 release gate green; QA-20 guard tests pass.</done>
</task>

<task id="123-04-03" type="auto">
  <name>Phase verification and project state closure</name>
  <files>.planning/phases/123-visual-validation-gate/123-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
1. Ensure `123-VERIFICATION.md` generated by check script includes:
   - Goal-backward conclusion: all four success criteria met
   - Per-requirement automated command column
   - Aggregate scores from evidence JSON
   - Note `seedSupported: false` and prompt provenance hash

2. Update `.planning/ROADMAP.md` Phase 123:
   - Plans: 4/4
   - Plan list with checkboxes
   - Mark phase complete when gate passes

3. Update `.planning/REQUIREMENTS.md`: QA-18, QA-19, QA-20, QA-21 → Complete with phase reference

4. Update `.planning/STATE.md`:
   - Phase 123 complete
   - `status: ready for milestone audit` or similar
   - Note v12.3 Integridade Criativa delivery order item 9 complete

Do NOT run `gsd-audit-milestone` in this task — only prepare artifacts.
  </action>
  <verify>
    <automated>grep -q "QA-21" .planning/phases/123-visual-validation-gate/123-VERIFICATION.md</automated>
  </verify>
  <done>Phase 123 documented complete; requirements marked; state updated. (Full regression already verified by 123-04-02 release gate — do not duplicate npm test/lint/build here.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → committed evidence | CI trusts operator-produced JSON — integrity via sha256 + prompt hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-08 | Tampering | Stale evidence after prompt change | mitigate | promptHash check in --stage final |
| T-123-09 | Repudiation | Threshold bypass in CI | mitigate | final stage recomputes aggregate from captures |
| T-123-10 | Denial of service | Missing PNGs fail release | accept | intentional — evidence must be complete |
</threat_model>

<verification>
Per @.planning/phases/123-visual-validation-gate/123-VALIDATION.md:
- `node app/scripts/check-creative-validation-evidence.mjs --stage final`
- `node app/scripts/run-creative-release-gate.mjs`
- Full: `cd app && npm test && npm run lint && npm run build`
</verification>

<success_criteria>
- QA-18: Final check validates paired before/after for all matrix keys
- QA-19: Thresholds enforced in CI on committed evidence
- QA-20: Zero fidelity hard failures on after set; guard tests pass
- QA-21: test + lint + build + creative release gate green
</success_criteria>

<output>
After completion, create `.planning/phases/123-visual-validation-gate/123-04-SUMMARY.md`
</output>
