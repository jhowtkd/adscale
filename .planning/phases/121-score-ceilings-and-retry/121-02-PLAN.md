---
phase: 121-score-ceilings-and-retry
plan: 02
type: execute
wave: 2
depends_on:
  - "03"
files_modified:
  - app/src/server/ai/derivation-auto-retry-policy.ts
  - app/src/server/ai/derivation-auto-retry.ts
  - app/src/server/jobs/derivation.ts
  - app/tests/unit/ai/derivation-auto-retry-policy.test.ts
  - app/src/server/jobs/derivation.test.ts
autonomous: true
requirements:
  - SCR-04
must_haves:
  truths:
    - "Restyling derivations with style_reference_contamination are eligible for one auto-retry"
    - "Restyling retry uses original base asset key, never generated.outputKey"
    - "Restyling retry calls images.edit with base + style original files (two-image path)"
    - "invented_factual_entity and campaign_identity_drift are NOT auto-retried"
    - "autoRetryAttempted single-shot guard unchanged"
  artifacts:
    - path: app/src/server/ai/derivation-auto-retry-policy.ts
      provides: Mode-aware shouldAutoRetryDerivation
      exports: ["shouldAutoRetryDerivation"]
    - path: app/src/server/jobs/derivation.ts
      provides: Restyling retry enabled; base/style key resolution
      contains: "baseAsset"
    - path: app/src/server/ai/derivation-auto-retry.ts
      provides: Two-image restyling edit path
      contains: "styleReferenceKey"
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/derivation-auto-retry-policy.ts
      via: shouldAutoRetryDerivation(mode, failures, attempted)
      pattern: "shouldAutoRetryDerivation\\("
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/derivation-auto-retry.ts
      via: referenceKey from baseAsset.key; styleReferenceKey from styleAsset.key
      pattern: "styleReferenceKey"
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/regeneration-correction-brief.ts
      via: correctionFeedback from row.regenerationSuggestion (121-03 directives)
      pattern: "correctionFeedback"
---

<objective>
Enable mode-aware auto-retry for restyling (SCR-04) using original factual base + style assets and two-image OpenAI edit.

Purpose: Restyling is currently blanket-skipped at derivation.ts L925; contamination failures need one retry from uncorrupted sources with SCR-05 correction brief from Plan 03.
Output: Extended retry policy, job wiring, two-image auto-retry, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-score-ceilings-and-retry/121-RESEARCH.md
@.planning/phases/121-score-ceilings-and-retry/121-03-SUMMARY.md

<interfaces>
From `app/src/server/ai/derivation-auto-retry-policy.ts` (today):
```typescript
const RETRYABLE_FAILURE_CODES = new Set(["cta_drift", "unreadable_required_text"]);
export function shouldAutoRetryDerivation(
  hardFailures: CreativeHardFailure[] | null | undefined,
  autoRetryAttempted?: boolean
): boolean;
```

From `app/src/server/ai/derivation-auto-retry.ts`:
```typescript
export interface AutoRetryDerivationInput {
  referenceKey: string;
  referenceMimeType: string;
  correctionFeedback: string;
  generationMode: "art_variation" | "format_adaptation" | "restyling";
  // single image edit today L73-80
}
```

From `app/src/server/jobs/derivation.ts`:
```typescript
// L925-928: restyling early return — REMOVE
// L621-628: restyling first pass uses two images (base + style)
// L940-944: referenceKey fallback includes generated.outputKey — fix for restyling
// restylingBaseAsset / restylingStyleAsset resolved L421-438 during generation
```

Mode-aware retryable codes (121-RESEARCH Pattern 2):
| Mode | Codes |
|------|-------|
| art_variation | cta_drift, unreadable_required_text, decorative_only_variation, visual_overload |
| format_adaptation | cta_drift, unreadable_required_text, invalid_format_layout, cropped_critical_content |
| restyling | style_reference_contamination, cta_drift, unreadable_required_text, copied_style_reference_facts |

Do NOT retry: invented_factual_entity, campaign_identity_drift, wrong_brand, unauthorized_brand_or_ip
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode-aware retry policy</name>
  <files>app/src/server/ai/derivation-auto-retry-policy.ts, app/tests/unit/ai/derivation-auto-retry-policy.test.ts</files>
  <behavior>
    - restyling + style_reference_contamination → shouldAutoRetry true (attempted false)
    - restyling + invented_factual_entity → shouldAutoRetry false
    - art_variation + decorative_only_variation → true
    - format_adaptation + invalid_format_layout → true
    - autoRetryAttempted true → false for all modes
    - copied_style_reference_facts normalized to style_reference_contamination eligibility
  </behavior>
  <action>
Replace flat `RETRYABLE_FAILURE_CODES` with mode-specific sets per RESEARCH Pattern 2.

Change signature to:
```typescript
export function shouldAutoRetryDerivation(
  generationMode: CreativeContract["generationMode"],
  hardFailures: CreativeHardFailure[] | null | undefined,
  autoRetryAttempted?: boolean
): boolean
```

Update all call sites (`derivation-auto-retry.ts` L50, `derivation.ts` L926) to pass `generationMode`.

Use `normalizeHardFailureCode` when matching if legacy aliases appear in stored failures.

Keep single-shot `autoRetryAttempted` guard.

Extend `derivation-auto-retry-policy.test.ts` with `-t "restyling"` and mode matrix cases. Update any existing policy tests for new signature.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/derivation-auto-retry-policy.test.ts -t "restyling"</automated>
  </verify>
  <done>shouldAutoRetryDerivation is mode-aware; restyling contamination retryable; factual invention not retryable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job wiring — remove restyling skip + factual asset keys</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/jobs/derivation.test.ts</files>
  <behavior>
    - Restyling derivation with retryable failure triggers auto-retry step (not null skip)
    - referenceKey resolves to base asset key from resolvedContract.baseAssetId
    - referenceKey !== generated.outputKey for restyling retry
    - styleReferenceKey passed when restyling
  </behavior>
  <action>
In `derivation.ts` auto-retry step (~L916-970):

1. **REMOVE** `generated.effectiveGenerationMode === "restyling"` early return (L925).

2. Call `shouldAutoRetryDerivation(generated.effectiveGenerationMode, hardFailures, log.autoRetryAttempted)`.

3. For restyling, resolve assets from campaign:
   - `baseAsset` = find by `generated.resolvedContract.baseAssetId` (or role `"base"` fallback matching generation step L426)
   - `styleAsset` = find by `generated.resolvedContract.styleAssetId`
   - Assert both exist before retry; log and skip if missing (do not fall back to outputKey)

4. Set `referenceKey = baseAsset.key` for restyling; keep existing art_variation/format_adaptation logic but avoid `generated.outputKey` for restyling.

5. Pass `styleReferenceKey: styleAsset.key` and `styleReferenceMimeType` to `runDerivationAutoRetry`.

6. Use `row.regenerationSuggestion` or `buildHardFailureRegenerationSuggestion` for correctionFeedback (benefits from 121-03 directives).

Add mock test in `derivation.test.ts` `-t "restyling retry"` asserting retry invoked with base key (not output key) when restyling + style_reference_contamination.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/jobs/derivation.test.ts -t "restyling retry"</automated>
  </verify>
  <done>Job enables restyling retry from original base/style keys; SCR-04 source integrity satisfied</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two-image restyling auto-retry</name>
  <files>app/src/server/ai/derivation-auto-retry.ts, app/src/server/jobs/derivation.test.ts</files>
  <behavior>
    - runDerivationAutoRetry with generationMode restyling + styleReferenceKey calls images.edit with array [baseFile, styleFile]
    - Order matches first-pass restyling (base first, style second) per derivation.ts L621-628
    - Non-restyling modes unchanged (single image)
  </behavior>
  <action>
Extend `AutoRetryDerivationInput`:
```typescript
styleReferenceKey?: string;
styleReferenceMimeType?: string;
```

In `runDerivationAutoRetry`:
- When `generationMode === "restyling"` and `styleReferenceKey` provided, download both buffers, `toFile` both, pass `image: [baseImage, styleImage]` to `openai.images.edit` (mirror derivation.ts restyling first pass).
- When styleReferenceKey missing for restyling, log warning and fall back to single base image only (prefer failing fast in job wiring instead).

Update `shouldAutoRetryDerivation` call inside `runDerivationAutoRetry` to pass `input.generationMode`.

Add integration-style unit test mocking OpenAI to verify two-image call for restyling path.

Re-export updated `shouldAutoRetryDerivation` signature from `derivation-auto-retry.ts` if needed.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/derivation-auto-retry-policy.test.ts src/server/jobs/derivation.test.ts -t "restyling"</automated>
  </verify>
  <done>Restyling auto-retry uses two original images; matches first-pass API contract</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset repository → R2 download | referenceKey/styleReferenceKey from DB-resolved assets only |
| OpenAI images.edit | Server-side API call with workspace-scoped assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-02-01 | Elevation | referenceKey resolution | mitigate | Resolve via contract.baseAssetId/styleAssetId; never user-supplied paths; assert key !== outputKey in tests |
| T-121-02-02 | Denial of service | auto-retry | mitigate | Keep autoRetryAttempted single-shot; mode allowlist excludes expensive hopeless retries (invented entity) |
| T-121-02-03 | Tampering | correctionFeedback | transfer | Brief from structured hard failures (121-03); MAX_PROMPT_FEEDBACK_CHARS enforced upstream |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/derivation-auto-retry-policy.test.ts`
- `cd app && npm test -- src/server/jobs/derivation.test.ts -t "restyling"`
- `cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts` (no regression)
</verification>

<success_criteria>
- SCR-04: Restyling retry enabled; uses original factual base (+ style); never contaminated output
- Mode-aware policy with documented allowlist per mode
- Two-image edit path for restyling retry
</success_criteria>

<output>
After completion, create `.planning/phases/121-score-ceilings-and-retry/121-02-SUMMARY.md`
</output>
