---
phase: 121-score-ceilings-and-retry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/regeneration-correction-brief.ts
  - app/src/server/ai/derivation-auto-retry.ts
  - app/tests/unit/ai/regeneration-correction-brief.test.ts
autonomous: true
requirements:
  - SCR-05
must_haves:
  truths:
    - "Retry correction brief includes imperative directive per hard-failure code (not just code:message list)"
    - "style_reference_contamination brief says copy style only, facts from base"
    - "Restyling auto-retry prompt footer includes RESTYLING FACTUAL-SOURCE RULE"
    - "invented_factual_entity brief instructs remove entities not in allowed registry"
  artifacts:
    - path: app/src/server/ai/regeneration-correction-brief.ts
      provides: FAILURE_CORRECTION_DIRECTIVES map + integration in formatIssueSections
      contains: "FAILURE_CORRECTION_DIRECTIVES"
    - path: app/tests/unit/ai/regeneration-correction-brief.test.ts
      provides: SCR-05 specificity tests
  key_links:
    - from: app/src/server/ai/creative-score.ts
      to: app/src/server/ai/regeneration-correction-brief.ts
      via: buildHardFailureRegenerationSuggestion → buildRegenerationCorrectionBrief
      pattern: "buildRegenerationCorrectionBrief"
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/regeneration-correction-brief.ts
      via: correctionFeedback from regenerationSuggestion
      pattern: "regenerationSuggestion"
---

<objective>
Add failure-code-specific correction directives (SCR-05) and inject RESTYLING FACTUAL-SOURCE RULE into auto-retry prompts.

Purpose: Generic hard-failure lists do not tell the image model what to fix; SCR-05 requires specific imperatives (remove invented entity, restore subject, reduce modules, restore CTA/concept).
Output: FAILURE_CORRECTION_DIRECTIVES map, brief integration, restyling retry prompt footer, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-score-ceilings-and-retry/121-RESEARCH.md
@.planning/phases/117-factual-vs-visual-separation/117-SUMMARY.md

<interfaces>
From `app/src/server/ai/regeneration-correction-brief.ts`:
```typescript
export function buildRegenerationCorrectionBrief(input: {
  contract: CreativeContract;
  hardFailures?: Array<{ code: CreativeHardFailureCode | string; message: string }>;
  scoreIssues?: string[];
  qaChecklist?: QaChecklistInput;
  // ...
}): RegenerationCorrectionBrief;
// formatIssueSections L117-159 — prepend per-code directives here
const MAX_PROMPT_FEEDBACK_CHARS = 1800;
```

From `app/src/server/ai/creative-score.ts`:
```typescript
export function buildHardFailureRegenerationSuggestion(input: {...}): string {
  const brief = buildRegenerationCorrectionBrief({...});
  return brief.promptFeedback;
}
```

From `app/src/server/ai/prompt-builder.ts` (RESTYLING FACTUAL-SOURCE RULE prose — reuse verbatim):
```typescript
export function extractPromptRestylingFactualSourceSection(prompt: string): string;
// Rule text at L443-445 in buildDerivationPrompt restyling block
```

From `app/src/server/ai/derivation-auto-retry.ts`:
```typescript
export interface AutoRetryDerivationInput {
  correctionFeedback: string;
  generationMode: "art_variation" | "format_adaptation" | "restyling";
  // prompt assembled L64-67 with correctionFeedback appended
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-code correction directives map</name>
  <files>app/src/server/ai/regeneration-correction-brief.ts, app/tests/unit/ai/regeneration-correction-brief.test.ts</files>
  <behavior>
    - invented_factual_entity → prompt contains remove-entity / allowed-registry language
    - replaced_source_subject → restore original hero person
    - campaign_identity_drift → restore same campaign concept/CTA
    - style_reference_contamination → style only from reference, facts from base
    - visual_overload → reduce to three zones
    - decorative_only_variation → new mechanism not color-only
    - cta_drift → restore contract CTA
    - generic_template_aesthetic → remove generic template stacks
    - Multiple failures → each matching directive present
    - Brief still respects MAX_PROMPT_FEEDBACK_CHARS cap
  </behavior>
  <action>
Add `FAILURE_CORRECTION_DIRECTIVES: Partial<Record<CreativeHardFailureCode, string>>` in `regeneration-correction-brief.ts` with imperative lines per 121-RESEARCH Pattern 4 table:

| Code | Directive (paraphrase allowed, keep intent) |
|------|---------------------------------------------|
| invented_factual_entity | Remove person/brand/team/claim not in allowed-entity registry |
| replaced_source_subject | Restore original hero from factual base |
| wrong_brand, unauthorized_brand_or_ip | Restore correct campaign brand/logo |
| campaign_identity_drift | Restore original campaign concept, offer, CTA — same campaign |
| style_reference_contamination | Copy visual style only; facts/people/offers/CTA from base only |
| visual_overload | Reduce to ≤3 information zones; one dominant hook |
| missing_dominant_idea | Restore campaign dominant visual idea as focal point |
| decorative_only_variation | New visual mechanism — not background/glow/color-only |
| cta_drift | Restore contract CTA exactly |
| generic_template_aesthetic | Remove generic neon/glass/template unless brand-required |

In `formatIssueSections`, before the generic `Hard failures:` block, prepend a `Correction directives:` section listing unique directives for active failure codes (dedupe by directive text). Keep existing code:message lines for traceability.

Export `getFailureCorrectionDirectives(codes: string[]): string[]` if useful for tests.

Write tests first with `-t "specific"` in `regeneration-correction-brief.test.ts`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/regeneration-correction-brief.test.ts -t "specific"</automated>
  </verify>
  <done>buildRegenerationCorrectionBrief promptFeedback includes per-code imperatives for all SCR-05 categories</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyling factual-source rule on auto-retry prompt</name>
  <files>app/src/server/ai/regeneration-correction-brief.ts, app/src/server/ai/derivation-auto-retry.ts, app/tests/unit/ai/regeneration-correction-brief.test.ts</files>
  <behavior>
    - buildRegenerationCorrectionBrief with generationMode restyling + style_reference_contamination includes RESTYLING FACTUAL-SOURCE RULE text
    - runDerivationAutoRetry restyling path appends factual-source rule to correction prompt (if not already in brief)
    - Rule prose matches prompt-builder.ts L443-445 (Phase 117 locked injection)
  </behavior>
  <action>
Extract RESTYLING FACTUAL-SOURCE RULE constant to a shared export — prefer adding `buildRestylingFactualSourceRuleSection(): string[]` in `prompt-builder.ts` (or reuse `extractPromptRestylingFactualSourceSection` on a canonical template) to avoid duplicating prose.

When `input.contract.generationMode === "restyling"`, append factual-source rule to `promptFeedback` in `buildRegenerationCorrectionBrief` (before char cap) OR in `derivation-auto-retry.ts` when assembling the auto-retry prompt for `generationMode === "restyling"`.

Choose single injection point to avoid duplicate rule blocks — brief builder is preferred so `buildHardFailureRegenerationSuggestion` and job `correctionFeedback` both benefit.

Add test `-t "restyling"` asserting contamination brief contains "RESTYLING FACTUAL-SOURCE RULE" and "base image is the ONLY source" (or equivalent locked prose).

Do NOT block visualTokenBrief for restyling (Phase 117 decision — unrelated here).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/regeneration-correction-brief.test.ts -t "restyling"</automated>
  </verify>
  <done>Restyling retry correction includes factual-source rule; SCR-05 contamination case covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hardFailures → promptFeedback | Structured failure codes drive correction text |
| User notes merge | mergeUserRegenerationNotes may append user text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03-01 | Tampering | promptFeedback | mitigate | Directives built only from typed CreativeHardFailureCode keys; applyBriefCharCap (1800 chars) unchanged |
| T-121-03-02 | Tampering | mergeUserRegenerationNotes | accept | Existing merge path; machine directives precede user notes in brief assembly |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/regeneration-correction-brief.test.ts`
- Confirm `buildHardFailureRegenerationSuggestion` inherits directives via brief (no separate code path bypass)
</verification>

<success_criteria>
- SCR-05: Each listed correction type has specific imperative in brief output
- Restyling contamination retry brief reinforces factual-source rule from Phase 117
</success_criteria>

<output>
After completion, create `.planning/phases/121-score-ceilings-and-retry/121-03-SUMMARY.md`
</output>
