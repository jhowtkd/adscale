---
phase: 76-cockpit-and-mission-instrumentation
plan: 03
type: execute
wave: 2
depends_on:
  - "76-01"
files_modified:
  - app/src/lib/hooks/use-record-beta-event.ts
  - app/src/lib/hooks/use-record-beta-event.test.ts
  - app/src/components/workspace/CreativeReadinessPanel.tsx
  - app/src/components/workspace/CreativeReadinessPanel.test.tsx
  - app/src/components/workspace/GuidedBriefingPanel.tsx
  - app/src/components/workspace/GuidedBriefingPanel.test.tsx
  - app/src/components/workspace/StrategyRecipePanel.tsx
  - app/src/components/workspace/StrategyRecipePanel.test.tsx
  - app/src/components/workspace/PreviewGatePanel.tsx
  - app/src/components/workspace/PreviewGatePanel.test.tsx
autonomous: true
requirements:
  - INST-03
  - INST-04
must_haves:
  truths:
    - "useRecordBetaEvent posts to /api/analytics/events without blocking UI"
    - "Analytics failures are swallowed silently"
    - "Each cockpit panel emits cockpit_stage_entered on mount/open"
    - "Panels emit cockpit_stage_completed on explicit success"
    - "Panels emit cockpit_stage_abandoned on cancel/close without complete"
    - "Client events include sessionId from sessionStorage when present"
  artifacts:
    - path: app/src/lib/hooks/use-record-beta-event.ts
      provides: "Fire-and-forget client beta event hook"
      exports: ["useRecordBetaEvent"]
    - path: app/src/components/workspace/GuidedBriefingPanel.tsx
      provides: "Briefing stage enter/complete/abandon events"
    - path: app/src/components/workspace/StrategyRecipePanel.tsx
      provides: "Recipe stage enter/complete/abandon events"
    - path: app/src/components/workspace/PreviewGatePanel.tsx
      provides: "Preview gate stage enter/complete/abandon events"
    - path: app/src/components/workspace/CreativeReadinessPanel.tsx
      provides: "Readiness stage enter/complete/abandon events"
  key_links:
    - from: app/src/lib/hooks/use-record-beta-event.ts
      to: /api/analytics/events
      via: "fetch POST fire-and-forget"
      pattern: "/api/analytics/events"
    - from: app/src/components/workspace/GuidedBriefingPanel.tsx
      to: app/src/lib/hooks/use-record-beta-event.ts
      via: "useEffect enter + onComplete/abandon handlers"
      pattern: "cockpit_stage_entered"
---

<objective>
Add client-side cockpit stage instrumentation via a fire-and-forget hook and panel lifecycle hooks.

Purpose: Deliver INST-03 stage funnel signals for guided briefing, strategy recipe, preview gate, and readiness. Mirror MissionInsightProvider non-blocking pattern per D-03.
Output: `useRecordBetaEvent` hook and instrumented cockpit panels with component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-cockpit-and-mission-instrumentation/76-CONTEXT.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-VALIDATION.md

@app/src/components/mission-insights/MissionInsightProvider.tsx
@app/src/lib/hooks/use-mission-insight.ts
@app/src/components/workspace/GuidedBriefingPanel.tsx
@app/src/components/workspace/StrategyRecipePanel.tsx
@app/src/components/workspace/PreviewGatePanel.tsx
@app/src/components/workspace/CreativeReadinessPanel.tsx

<interfaces>
From Plan 76-01:
```typescript
export const BETA_SESSION_STORAGE_KEY = "adscale_beta_session_id";
```

Client event keys (D-01):
- `cockpit_stage_entered`, `cockpit_stage_completed`, `cockpit_stage_abandoned`

Stage → missionKey mapping (ARCHITECTURE.md):
| Panel | stage / missionKey |
| CreativeReadinessPanel | readiness |
| GuidedBriefingPanel | guided_briefing |
| StrategyRecipePanel | strategy_recipe |
| PreviewGatePanel | preview |

POST body shape (Phase 75):
```typescript
{ eventKey: string; sessionId?: string; campaignId?: string; properties?: Record<string, scalar> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRecordBetaEvent fire-and-forget hook</name>
  <files>app/src/lib/hooks/use-record-beta-event.ts, app/src/lib/hooks/use-record-beta-event.test.ts</files>
  <behavior>
    - Test: recordEvent calls fetch POST /api/analytics/events with eventKey and properties
    - Test: includes sessionId from sessionStorage when BETA_SESSION_STORAGE_KEY set
    - Test: includes campaignId when provided
    - Test: fetch rejection does not throw to caller
    - Test: properties include stage and missionKey when passed
  </behavior>
  <action>
    Per D-03, create `useRecordBetaEvent(campaignId: string)` returning stable `recordEvent` callback.

    Implementation:
    - Read `sessionStorage.getItem(BETA_SESSION_STORAGE_KEY)` inside callback (guard `typeof window !== "undefined"`)
    - `fetch("/api/analytics/events", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ eventKey, campaignId, sessionId, properties }) })` — no await in caller path; use `.catch(() => {})` on promise
    - Default `source` omitted (route sets client)
    - Import `BETA_SESSION_STORAGE_KEY` from `@/server/beta-analytics/session` OR duplicate constant in `lib/beta-analytics/constants.ts` if importing server module into client is blocked — **prefer** `lib/beta-analytics/constants.ts` exporting same key string to avoid client bundling server code

    If server import breaks client build, extract `BETA_SESSION_STORAGE_KEY` to `app/src/lib/beta-analytics/constants.ts` and re-export from session.ts.

    Vitest: mock global fetch, mock sessionStorage.
  </action>
  <verify>
    <automated>cd app && npm test -- src/lib/hooks/use-record-beta-event.test.ts</automated>
  </verify>
  <done>Hook posts events fire-and-forget; sessionId from sessionStorage; errors swallowed; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guided briefing and strategy recipe panel instrumentation</name>
  <files>app/src/components/workspace/GuidedBriefingPanel.tsx, app/src/components/workspace/GuidedBriefingPanel.test.tsx, app/src/components/workspace/StrategyRecipePanel.tsx, app/src/components/workspace/StrategyRecipePanel.test.tsx</files>
  <behavior>
    - Test: GuidedBriefingPanel mounts → cockpit_stage_entered with stage/missionKey guided_briefing
    - Test: onComplete callback path → cockpit_stage_completed before onComplete
    - Test: skip/cancel without complete → cockpit_stage_abandoned
    - Test: StrategyRecipePanel recipe selection success → cockpit_stage_completed
    - Test: StrategyRecipePanel close/back without selection → cockpit_stage_abandoned
  </behavior>
  <action>
    Per D-03 panels list, wire `useRecordBetaEvent(campaignId)`:

    **GuidedBriefingPanel:**
    - `useEffect` on mount → `cockpit_stage_entered` with `{ stage: "guided_briefing", missionKey: "guided_briefing" }`
    - Track `completedRef` boolean; set true when `onComplete` invoked (in handleAccept/handleSaveEdit final step paths)
    - Skip button / onOpenFullForm without complete → `cockpit_stage_abandoned`
    - `useEffect` cleanup on unmount: if !completedRef.current → `cockpit_stage_abandoned`

    **StrategyRecipePanel:**
    - Mount → `cockpit_stage_entered` with `strategy_recipe`
    - On recipe confirm/apply success handler → `cockpit_stage_completed`
    - On explicit cancel/back without confirm → `cockpit_stage_abandoned`
    - Unmount without complete → abandoned (same ref pattern)

    Mock hook in component tests; assert recordEvent call args.

    Do NOT instrument ClientApprovalPackagePanel — out of INST-03 scope (briefing, recipe, preview gate only; readiness in Task 3).
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/workspace/GuidedBriefingPanel.test.tsx src/components/workspace/StrategyRecipePanel.test.tsx</automated>
  </verify>
  <done>Briefing and recipe panels emit enter/complete/abandon; component tests verify call sequence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Readiness and preview gate panel instrumentation</name>
  <files>app/src/components/workspace/CreativeReadinessPanel.tsx, app/src/components/workspace/CreativeReadinessPanel.test.tsx, app/src/components/workspace/PreviewGatePanel.tsx, app/src/components/workspace/PreviewGatePanel.test.tsx</files>
  <behavior>
    - Test: CreativeReadinessPanel mount with assetId → cockpit_stage_entered readiness
    - Test: readiness status ready/needs_attention → cockpit_stage_completed once
    - Test: PreviewGatePanel mount → cockpit_stage_entered preview
    - Test: onApproveBatch → cockpit_stage_completed
    - Test: onReviseRecipe without approve → cockpit_stage_abandoned
  </behavior>
  <action>
    **CreativeReadinessPanel:**
    - Accept new optional prop `onStageClose?: () => void` only if needed for parent-driven close; prefer internal unmount abandon
    - Mount with valid assetId → `cockpit_stage_entered` `{ stage: "readiness", missionKey: "readiness" }`
    - When `data.readiness` transitions to terminal success (`status` ready or needs_attention after completed analysis) → `cockpit_stage_completed` once (use ref to dedupe)
    - Unmount before completion → `cockpit_stage_abandoned`

    **PreviewGatePanel:**
    - Add `campaignId` prop if not present (check parent page.tsx usage — pass from campaign workspace)
    - Mount → `cockpit_stage_entered` preview
    - `onApproveBatch` wrapper → emit completed then call prop
    - `onReviseRecipe` → emit abandoned then call prop

    Update `campaigns/[id]/page.tsx` only if PreviewGatePanel needs campaignId prop — minimal one-line pass-through.

    Extend panel tests with mocked useRecordBetaEvent.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx src/components/workspace/PreviewGatePanel.test.tsx</automated>
  </verify>
  <done>Readiness and preview panels instrumented; page passes campaignId if required; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/analytics/events | Client events best-effort; server events authoritative for credits/readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-07 | Tampering | Client inflates completed count | accept | Owner funnel cross-checks with server events in Phase 78 |
| T-76-08 | Denial of service | fetch blocks UI | mitigate | Fire-and-forget, no await (Task 1) |
| T-76-09 | Info disclosure | sessionStorage session id | accept | UUID only; workspace-scoped validation on server |
</threat_model>

<verification>
- `cd app && npm test -- src/lib/hooks/use-record-beta-event.test.ts src/components/workspace/CreativeReadinessPanel.test.tsx src/components/workspace/GuidedBriefingPanel.test.tsx src/components/workspace/StrategyRecipePanel.test.tsx src/components/workspace/PreviewGatePanel.test.tsx`
- `cd app && npm run lint` — no client importing server-only modules
</verification>

<success_criteria>
- All four cockpit panels emit client stage events per D-03
- Hook swallows errors; no UI regression
- sessionId attached when sessionStorage populated (INST-04 client side)
</success_criteria>

<output>
After completion, create `.planning/phases/76-cockpit-and-mission-instrumentation/76-03-SUMMARY.md`
</output>
