---
phase: 76-cockpit-and-mission-instrumentation
plan: 04
type: execute
wave: 3
depends_on:
  - "76-02"
  - "76-03"
files_modified:
  - app/src/server/beta-analytics/instrumentation.integration.test.ts
  - app/src/server/repositories/beta-analytics.test.ts
autonomous: true
requirements:
  - QA-01
  - INST-04
must_haves:
  truths:
    - "Integration test proves readiness_blocked or readiness_completed recorded on preflight path"
    - "Integration test proves mission_completed recorded on export path"
    - "Smoke test proves client event attaches session_id when beta_sessions fixture exists"
    - "At least one server and one client event path verified end-to-end through recordBetaAnalyticsEvent"
  artifacts:
    - path: app/src/server/beta-analytics/instrumentation.integration.test.ts
      provides: "QA-01 cross-path integration coverage"
    - path: app/src/server/repositories/beta-analytics.test.ts
      provides: "session_id FK attachment verification"
  key_links:
    - from: app/src/server/beta-analytics/instrumentation.integration.test.ts
      to: app/src/server/beta-analytics/record.ts
      via: "real sanitize + mocked insert asserting payload"
      pattern: "recordBetaAnalyticsEvent"
    - from: instrumentation.integration.test.ts
      to: app/src/server/repositories/beta-analytics.ts
      via: "insertBetaAnalyticsEvent with session fixture"
      pattern: "sessionId"
---

<objective>
Prove instrumentation works end-to-end with integration tests and session-grouping smoke coverage.

Purpose: Close QA-01 (readiness + mission completion paths) and INST-04 (events groupable under beta_session). Satisfies roadmap success criterion 4–5 before Phase 77 operator sessions.
Output: `instrumentation.integration.test.ts` exercising server paths and session_id attachment.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-cockpit-and-mission-instrumentation/76-CONTEXT.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-02-SUMMARY.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-03-SUMMARY.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-VALIDATION.md
@.planning/phases/75-event-schema-and-ingest-foundation/75-03-SUMMARY.md

@app/src/server/beta-analytics/record.test.ts
@app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.test.ts
@app/src/server/repositories/beta-analytics.test.ts

<interfaces>
QA-01 requirement text:
> Integration tests prove events are recorded on readiness block and mission completion paths.

INST-04 requirement text:
> Operator can group events under a beta_session linked to a workspace.

Phase 76 scope: read sessionId if present; smoke test inserts fixture beta_sessions row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Readiness path integration test</name>
  <files>app/src/server/beta-analytics/instrumentation.integration.test.ts</files>
  <behavior>
    - Test: simulate preflight readiness_blocked payload through recordBetaAnalyticsEvent → insert receives readiness_blocked with blockingCount > 0
    - Test: simulate readiness_completed payload with blockingCount 0
    - Test: unknown event_key still rejected (regression guard)
  </behavior>
  <action>
    Create `instrumentation.integration.test.ts` using vitest.

    **Approach:** Import `recordBetaAnalyticsEvent` with mocked `insertBetaAnalyticsEvent` and `getBetaSessionById` (same pattern as record.test.ts) but structure tests as **integration scenarios** named for QA paths:

    1. `readiness block path` — call recordBetaAnalyticsEvent with eventKey readiness_blocked, properties matching preflight emission shape (stage readiness, missionKey readiness, blockingCount 2, readinessStatus blocked), assert insert mock called once with source server.

    2. `readiness complete path` — readiness_completed with blockingCount 0, readinessStatus ready.

    Optionally add one test importing preflight route handler with all deps mocked EXCEPT recordBetaAnalyticsEvent spy — if route test already covers this in 76-02, keep integration file focused on record + repository contract to avoid duplication. **Minimum:** readiness scenarios in this file satisfy QA-01 "readiness block" path explicitly.

    Reference @.planning/phases/76-cockpit-and-mission-instrumentation/76-VALIDATION.md for command.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/instrumentation.integration.test.ts -t "readiness"</automated>
  </verify>
  <done>Readiness block and complete paths covered in integration test file; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mission completion path integration test</name>
  <files>app/src/server/beta-analytics/instrumentation.integration.test.ts</files>
  <behavior>
    - Test: mission_completed export scenario persists with missionKey export, stage export, source server
    - Test: mission_completed share scenario with missionKey share
    - Test: client cockpit_stage_completed preview scenario with source client default
  </behavior>
  <action>
    Extend `instrumentation.integration.test.ts`:

    1. **mission completion — export:** recordBetaAnalyticsEvent with mission_completed, properties { missionKey: "export", stage: "export", operation: "individual" }, campaignId set.

    2. **mission completion — share:** missionKey share.

    3. **client stage complete:** cockpit_stage_completed with properties { stage: "preview", missionKey: "preview" }, source default client — proves client path distinct from server mission_completed.

    Assert insert mock receives correct eventKey and properties after sanitization.

    This satisfies QA-01 mission completion path alongside 76-02 route tests.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/instrumentation.integration.test.ts -t "mission"</automated>
  </verify>
  <done>Mission completion and client complete scenarios pass; QA-01 both paths documented in test names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: beta_session session_id attachment smoke test</name>
  <files>app/src/server/beta-analytics/instrumentation.integration.test.ts, app/src/server/repositories/beta-analytics.test.ts</files>
  <behavior>
    - Test: insert fixture beta_sessions row (mock getBetaSessionById returning session) → recordBetaAnalyticsEvent with sessionId → insert called with matching sessionId
    - Test: invalid sessionId for workspace throws before insert
    - Test: listBetaAnalyticsEvents filter by sessionId returns inserted event (repository level)
  </behavior>
  <action>
    Per D-04 smoke requirement:

    1. In integration file, mock `getBetaSessionById` to return `{ id: "session-uuid", workspaceId: "ws-1" }`.

    2. Call `recordBetaAnalyticsEvent` with sessionId session-uuid and eventKey cockpit_stage_entered (client path) — assert insert receives sessionId.

    3. Extend `beta-analytics.test.ts` OR integration file: verify `listBetaAnalyticsEvents({ sessionId })` filter if repository test already has list coverage — add one test inserting event with sessionId and listing by filter.

    Document in test describe block: "INST-04 smoke — events attach session_id when beta_sessions fixture exists".

    No operator session APIs — Phase 77.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/instrumentation.integration.test.ts src/server/repositories/beta-analytics.test.ts</automated>
  </verify>
  <done>Session_id attachment proven; cross-workspace session rejected; INST-04 smoke test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test DB / mocks | No production data; fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-10 | Repudiation | Missing test coverage | mitigate | Named QA-01 scenarios in Task 1–2 |
| T-76-11 | Elevation | Session FK bypass | mitigate | getBetaSessionById rejection test Task 3 |
</threat_model>

<verification>
- `cd app && npm test -- src/server/beta-analytics/instrumentation.integration.test.ts src/server/repositories/beta-analytics.test.ts`
- `cd app && npm test -- src/server/beta-analytics src/app/api/analytics/events`
- `cd app && npm run lint`
- Phase 77 gate: manual DB smoke optional per 76-VALIDATION.md
</verification>

<success_criteria>
- QA-01: readiness block + mission completion paths have automated integration tests
- INST-04: session_id attachment verified with beta_sessions fixture
- Full phase test command in 76-VALIDATION.md passes
</success_criteria>

<output>
After completion, create `.planning/phases/76-cockpit-and-mission-instrumentation/76-04-SUMMARY.md`
</output>
