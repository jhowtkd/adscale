---
phase: 204-plan-iteration-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0065_assistant_plan_feedback_drafts.sql
  - app/drizzle/meta/_journal.json
  - app/src/server/db/schema.ts
  - app/src/server/assistant/plan-iteration/types.ts
  - app/src/server/assistant/plan-iteration/diff.ts
  - app/src/server/assistant/plan-iteration/diff.test.ts
  - app/src/server/assistant/plan-iteration/digest.ts
  - app/src/server/assistant/plan-iteration/digest.test.ts
  - app/src/server/assistant/plan-iteration/draft.ts
  - app/src/server/assistant/plan-iteration/draft.test.ts
  - app/src/server/assistant/plan-iteration/proposal.ts
  - app/src/server/assistant/plan-iteration/proposal.test.ts
  - app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts
  - app/src/server/assistant/action-contracts/contracts/index.ts
  - app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
  - app/src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts
  - app/src/server/assistant/action-execution/execute.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-03
  - PLAN-04
user_setup: []

must_haves:
  truths:
    - "Free-text feedback produces a pending plan_revision proposal while approved current and mutable creative_plans row stay unchanged"
    - "Semantic changes are computed server-side from before/after snapshots, not trusted from the model"
    - "Confirmation creates one ready child plan version with revision provenance and switches working head only"
    - "Unsent feedback drafts persist server-side per thread and never create proposals until the user sends"
    - "Pending proposals can be canceled explicitly without mutating versions"
  artifacts:
    - path: app/src/server/assistant/plan-iteration/diff.ts
      provides: "Pure semantic diff across strategy, angles, hooks, ctas, constraints"
      exports: ["buildPlanSemanticChanges"]
    - path: app/src/server/assistant/plan-iteration/proposal.ts
      provides: "Source resolution, LLM proposal authoring, confirm/cancel orchestration"
      exports: ["proposePlanRevision", "confirmPlanRevision", "cancelPlanRevision", "resolvePlanRevisionSource"]
    - path: app/src/server/assistant/plan-iteration/draft.ts
      provides: "Thread-scoped unsent feedback draft persistence"
      exports: ["getPlanFeedbackDraft", "savePlanFeedbackDraft", "clearPlanFeedbackDraft"]
    - path: app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts
      provides: "Zero-credit revise_creative_plan contract binding proposal digest and source version"
    - path: app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
      provides: "Sync confirm handler that creates ready version without creative_plans mutation"
  key_links:
    - from: app/src/server/assistant/plan-iteration/proposal.ts
      to: app/src/server/repositories/artifact-version.ts
      via: "createArtifactProposal on feedback; createArtifactVersion + updateArtifactHead working-only on confirm"
      pattern: "createArtifactProposal|createArtifactVersion|updateArtifactHead"
    - from: app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
      to: app/src/server/assistant/plan-iteration/proposal.ts
      via: "confirmPlanRevision inside transactional handler"
      pattern: "confirmPlanRevision"
    - from: app/src/server/assistant/plan-iteration/proposal.ts
      to: app/src/lib/assistant/artifact-version.ts
      via: "artifactProposalPayloadSchema and planVersionSnapshotSchema validation"
      pattern: "planVersionSnapshotSchema|artifactProposalPayloadSchema"
---

# Plan 204-01: Plan Revision Core Domain

<objective>
Build the server-owned plan iteration domain: semantic diffing, LLM-backed proposal authoring with clarification, thread-scoped draft persistence, the `revise_creative_plan` action contract, and the sync confirm handler.

Purpose: Wave 2 can wire chat, API, and UI against stable contracts without reimplementing version invariants.
Output: `plan-iteration/` module, draft table, action contract, execution handler, and focused unit/service/handler tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-plan-iteration-loop/204-CONTEXT.md
@.planning/phases/204-plan-iteration-loop/204-RESEARCH.md
@.planning/phases/204-plan-iteration-loop/204-VALIDATION.md
@.planning/phases/203-artifact-version-foundation/203-01-SUMMARY.md
@.planning/phases/203-artifact-version-foundation/203-02-SUMMARY.md
@app/src/lib/assistant/artifact-version.ts
@app/src/server/repositories/artifact-version.ts
@app/src/server/assistant/artifact-version/service.ts
@app/src/server/assistant/artifact-version/snapshots.ts
@app/src/server/assistant/action-contracts/contracts/quick-review.ts
@app/src/server/assistant/action-contracts/types.ts

<interfaces>
From `app/src/lib/assistant/artifact-version.ts`:

```typescript
export const planVersionSnapshotSchema = z.object({
  type: z.literal("plan"),
  strategy: boundedText.nullable(),
  angles: boundedTextList,
  hooks: boundedTextList,
  ctas: boundedTextList,
  constraints: boundedText.nullable(),
}).strict();

export const artifactProposalPayloadSchema = z.discriminatedUnion("type", [
  z.object({
    type: z.literal("plan_revision"),
    schemaVersion: z.literal(1),
    summary: z.string(),
    proposedSnapshot: planVersionSnapshotSchema,
    changes: z.array(z.object({ field: z.string(), description: z.string() })),
    writes: boundedTextList,
  }).strict(),
]);
```

Repository primitives (Phase 203): `createArtifactProposal`, `transitionArtifactProposal`, `staleSiblingProposals`, `createArtifactVersion`, `updateArtifactHead`, `getArtifactHead`.

Service primitives: `adoptArtifactForThread`, `getThreadArtifactVersionState`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semantic diff, payload digest, and draft persistence</name>
  <files>app/src/server/assistant/plan-iteration/types.ts, app/src/server/assistant/plan-iteration/diff.ts, app/src/server/assistant/plan-iteration/diff.test.ts, app/src/server/assistant/plan-iteration/digest.ts, app/src/server/assistant/plan-iteration/digest.test.ts, app/drizzle/0065_assistant_plan_feedback_drafts.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts, app/src/server/assistant/plan-iteration/draft.ts, app/src/server/assistant/plan-iteration/draft.test.ts</files>
  <behavior>
    - Test: `buildPlanSemanticChanges` emits one change per modified field among strategy, angles, hooks, ctas, constraints; identical snapshots yield empty array.
    - Test: list/array fields compare by normalized content, not reference identity.
    - Test: `canonicalProposalPayloadDigest` is stable for equivalent payload objects and changes when proposed snapshot or changes differ.
    - Test: draft save/load/clear is scoped to thread + workspace; cross-thread read returns null.
    - Test: draft text is bounded (max 2_000 chars) and never creates proposals.
    - Test: migration `0065` registered in `_journal.json`.
  </behavior>
  <action>
Create `plan-iteration/types.ts` with `PlanRevisionSource`, `PlanRevisionClarifyResult`, `PlanRevisionProposalResult`, and display metadata (`sourceVersionLabel`, `approvedVersionLabel`, `workingDiffersFromApproved`). Implement pure `buildPlanSemanticChanges(before, after)` — never trust model output for `changes[]` (per RESEARCH pitfall #4).

Add `canonicalProposalPayloadDigest(payload)` using stable JSON canonicalization of the `plan_revision` payload fields bound at confirm time.

Add `assistant_plan_feedback_drafts` table (unique `thread_id`, `draft_text`, `updated_at`, workspace scope denormalized) via migration `0065`. Implement `getPlanFeedbackDraft`, `savePlanFeedbackDraft`, `clearPlanFeedbackDraft` with scoped predicates. This satisfies D-08 server-persisted unsent drafts (refines Phase 203 local-only note).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/diff.test.ts src/server/assistant/plan-iteration/digest.test.ts src/server/assistant/plan-iteration/draft.test.ts</automated>
  </verify>
  <done>Pure diff/digest helpers are tested; thread-scoped draft persistence survives reload without creating proposals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proposal service with source resolution and clarification</name>
  <files>app/src/server/assistant/plan-iteration/proposal.ts, app/src/server/assistant/plan-iteration/proposal.test.ts</files>
  <behavior>
    - Test: `resolvePlanRevisionSource` picks working version by default; falls back to approved current when working is null (D-07).
    - Test: when working ≠ approved, result includes explicit warning metadata for downstream card copy (D-09).
    - Test: `proposePlanRevision` with sufficient feedback creates a separate pending proposal via `createArtifactProposal`; approved current version id unchanged (PLAN-01).
    - Test: each feedback call creates a new proposal; does not auto-replace or merge prior pending proposals on same source (D-05).
    - Test: vague feedback returns `{ kind: "clarify", question }` without creating a proposal (D-04); after 1–2 clarifications, service may propose.
    - Test: out-of-scope feedback (briefing/campaign fields) returns redirect result, not a proposal (D-06).
    - Test: LLM output is parsed with `planVersionSnapshotSchema`; `changes[]` and neutral PT-BR summary are built server-side (D-03 tone, PLAN-04).
    - Test: `cancelPlanRevision` transitions proposal to `canceled` without version mutation (D-10).
    - Test: proposed snapshot preserves all plan fields with feedback stored on proposal row (PLAN-04).
  </behavior>
  <action>
Implement `resolvePlanRevisionSource(scope, lineageId)` using `getArtifactHead`. When no plan lineage exists, return ask-user result (D-02). When multiple lineages could apply, ask which plan/version to revise before proposing.

Implement `proposePlanRevision({ scope, threadId, feedback, messageId })`:
1. Ensure plan lineage via `adoptArtifactForThread` when campaign-linked plan exists.
2. Classify feedback specificity (clarify vs propose vs out_of_scope vs ask_target).
3. On clarify: return assistant question; optionally persist partial text to draft.
4. On propose: load source snapshot, call OpenAI text model (`env.OPENAI_TEXT_MODEL`) with strict JSON schema for `planVersionSnapshotSchema` fields only, merge into full snapshot, compute `buildPlanSemanticChanges`, build neutral summary (not mirroring user wording per D-03), populate `writes[]` with confirm-time effects (e.g., "Cria v3 do plano", "Não altera versão aprovada atual").
5. `createArtifactProposal` with `proposalType: "plan_revision"` and feedback text.
6. Return structured payload for action card binding including `proposalId`, `lineageId`, `sourceVersionId`, digest, source version label.

Implement `cancelPlanRevision` calling `transitionArtifactProposal` → `canceled`.

Mock OpenAI in tests; do not hit live provider.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/proposal.test.ts</automated>
  </verify>
  <done>Proposal service creates inspectable pending revisions without touching approved current; clarification and scope rules match CONTEXT decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: revise_creative_plan contract and confirm handler</name>
  <files>app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts, app/src/server/assistant/action-contracts/contracts/index.ts, app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts, app/src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts, app/src/server/assistant/action-execution/execute.ts</files>
  <behavior>
    - Test: contract registers with `creditImpact: { kind: "fixed", credits: 0 }` and no `spendCredits` path (D-12).
    - Test: input schema requires `proposalId`, `lineageId`, `sourceVersionId`, `payloadDigest`.
    - Test: confirm rejects when proposal is `stale`, digest mismatch, or sourceVersionId mismatch (T-204-01 stale threat).
    - Test: confirm creates child version `status: "ready"` with `provenance.origin: "revision"`, `sourceVersionId`, `messageId`, `actionId` (PLAN-03, PLAN-04).
    - Test: `updateArtifactHead` updates `workingVersionId` only; `approvedCurrentVersionId` unchanged (D-11).
    - Test: `creative_plans` row is not updated on confirm.
    - Test: sibling pending proposals on same source become `stale` via `staleSiblingProposals`.
    - Test: proposal transitions to `confirmed`; handler is sync (no Inngest).
    - Test: duplicate confirm with same action id is idempotent (no duplicate versions).
  </behavior>
  <action>
Add `reviseCreativePlanContract` with `actionType: "revise_creative_plan"`, `confirmationPolicy: "required"`, zero credits using the same pattern as `quick-review` (`kind: "fixed", credits: 0`). Input binds `proposalId`, `lineageId`, `sourceVersionId`, `payloadDigest`. Register in `contracts/index.ts`.

Implement `executeReviseCreativePlan` calling `confirmPlanRevision` transactionally:
1. Re-read proposal; reject if not `pending` or digest/source mismatch.
2. `createArtifactVersion` with proposed snapshot and revision provenance.
3. `updateArtifactHead` working pointer only.
4. `staleSiblingProposals`.
5. `transitionArtifactProposal` → `confirmed`.
6. Never call `updatePlan` / mutable campaign plan writers.

Wire handler in `execute.ts` `HANDLERS` map. Post-journey iteration must bind via proposal digest, not `assertGuidedActionReady` (RESEARCH pitfall #6).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts</automated>
  </verify>
  <done>Confirm path creates immutable ready child version, updates working only, and rejects stale or mismatched proposals server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted feedback text and proposal/cancel requests |
| LLM→server | Untrusted proposed snapshot JSON |
| confirm→repository | Untrusted replay of stale or cross-scope proposal ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-01 | T | confirmPlanRevision | mitigate | Revalidate proposal pending status, payloadDigest, sourceVersionId, and head revision before version insert |
| T-204-02 | I | createArtifactProposal | mitigate | Scope predicates on workspace/client/campaign/thread; reject cross-lineage proposal confirms |
| T-204-03 | D | proposePlanRevision LLM output | mitigate | Parse with Zod strict schemas; build changes[] server-side; denylist unsafe persistence keys |
| T-204-04 | E | revise_creative_plan handler | mitigate | No spendCredits call; idempotent action execution keyed by actionId |
| T-204-05 | S | draft persistence | accept | Draft text is user-authored chat input bounded to 2k chars; no executable content |
</threat_model>

<verification>
Run `cd app && npm test -- --run src/server/assistant/plan-iteration/ src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts`.
</verification>

<success_criteria>
- Feedback proposals are pending, scoped, and leave approved current untouched.
- Semantic diffs and summaries are server-computed and field-complete.
- Confirm creates ready child versions with provenance; working head updates only.
- Drafts persist per thread; cancel transitions proposals without version writes.
- Zero credit spend on proposal or confirm.
</success_criteria>

<output>
After completion, create `.planning/phases/204-plan-iteration-loop/204-01-SUMMARY.md`.
</output>
