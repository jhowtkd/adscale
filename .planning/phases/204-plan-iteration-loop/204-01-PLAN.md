---
phase: 204-plan-iteration-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/assistant/plan-iteration/diff.ts
  - app/src/server/assistant/plan-iteration/diff.test.ts
  - app/src/server/assistant/plan-iteration/proposal.ts
  - app/src/server/assistant/plan-iteration/proposal.test.ts
  - app/src/server/assistant/plan-iteration/digest.ts
  - app/src/server/assistant/plan-iteration/types.ts
  - app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts
  - app/src/server/assistant/action-contracts/contracts/index.ts
  - app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
  - app/src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts
  - app/src/server/assistant/action-execution/execute.ts
  - app/src/server/repositories/assistant-thread.ts
  - app/src/server/db/schema.ts
  - app/drizzle/0065_assistant_thread_plan_draft.sql
  - app/drizzle/meta/_journal.json
autonomous: true
requirements:
  - PLAN-01
  - PLAN-03
  - PLAN-04
user_setup: []

must_haves:
  truths:
    - "Feedback creates a pending plan_revision proposal with semantic summary and proposed snapshot while approved current and mutable creative_plans stay unchanged"
    - "Server builds field-level semantic changes by comparing source and proposed snapshots; the model does not own the diff list"
    - "Confirmation creates one ready immutable child version linked to feedback, source version, message, and action without spending credits"
    - "Confirmation updates working selection only, stalemates sibling proposals on the same source, and rejects stale proposal confirmation server-side"
    - "Thread-scoped feedback drafts persist on the server and are not treated as proposals until the user sends feedback"
  artifacts:
    - path: app/src/server/assistant/plan-iteration/diff.ts
      provides: "Deterministic semantic plan change builder"
      exports: ["buildPlanSemanticChanges"]
    - path: app/src/server/assistant/plan-iteration/proposal.ts
      provides: "Proposal authoring, source resolution, clarify vs propose branching"
      exports: ["proposePlanRevision", "confirmPlanRevision", "cancelPlanRevision"]
    - path: app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts
      provides: "Zero-credit confirmation contract bound to proposal digest"
    - path: app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
      provides: "Sync confirm handler that appends ready version without mutating creative_plans"
  key_links:
    - from: app/src/server/assistant/plan-iteration/proposal.ts
      to: app/src/server/repositories/artifact-version.ts
      via: "createArtifactProposal and confirm path through createArtifactVersion plus head CAS"
      pattern: "createArtifactProposal|createArtifactVersion|staleSiblingProposals"
    - from: app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts
      to: app/src/server/assistant/plan-iteration/proposal.ts
      via: "shared transactional confirmPlanRevision"
      pattern: "confirmPlanRevision"
    - from: app/src/server/assistant/plan-iteration/proposal.ts
      to: app/src/server/assistant/artifact-version/service.ts
      via: "lazy plan lineage adoption before first revision"
      pattern: "adoptArtifactForThread"
---

# Plan 204-01: Plan Revision Domain and Confirmation

<objective>
Implement the server-side plan iteration core: semantic diffing, LLM-backed proposal authoring, zero-credit action contract, transactional confirmation into a ready child version, and server-persisted feedback drafts.

Purpose: Phase 204 needs a trustworthy proposal/confirm boundary before chat orchestration and UI can expose the loop.
Output: Plan-iteration module, action contract, execution handler, draft persistence, and focused tests.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-plan-iteration-loop/204-CONTEXT.md
@.planning/phases/204-plan-iteration-loop/204-RESEARCH.md
@.planning/phases/204-plan-iteration-loop/204-VALIDATION.md
@app/src/lib/assistant/artifact-version.ts
@app/src/server/repositories/artifact-version.ts
@app/src/server/assistant/artifact-version/service.ts
@app/src/server/assistant/action-contracts/contracts/create-creative-plan.ts
@app/src/server/assistant/action-execution/handlers/create-creative-plan.ts

<interfaces>
```typescript
type ProposePlanRevisionResult =
  | { kind: "clarify"; question: string }
  | { kind: "ask_plan"; question: string }
  | {
      kind: "proposal";
      proposalId: string;
      actionInput: {
        proposalId: string;
        lineageId: string;
        sourceVersionId: string;
        payloadDigest: string;
      };
      display: {
        label: string;
        summary: string;
        sourceVersionLabel: string;
        writes: string[];
        mismatchWarning?: string;
        proposalStatus: "pending" | "stale";
      };
    };

interface ReviseCreativePlanInput {
  proposalId: string;
  lineageId: string;
  sourceVersionId: string;
  payloadDigest: string;
}
```

Confirmation must create `status: "ready"` versions with `provenance.origin = "revision"` and leave `approvedCurrentVersionId` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic semantic diff and proposal digest helpers</name>
  <files>app/src/server/assistant/plan-iteration/diff.ts, app/src/server/assistant/plan-iteration/diff.test.ts, app/src/server/assistant/plan-iteration/digest.ts, app/src/server/assistant/plan-iteration/types.ts</files>
  <behavior>
    - Test: strategy, angles, hooks, ctas, and constraints each emit a change entry when values differ.
    - Test: identical snapshots return an empty changes array.
    - Test: array fields compare by normalized content, not reference identity.
    - Test: payload digest is stable for the same canonical proposal payload and changes when summary or proposed snapshot changes.
    - Test: helpers accept only planVersionSnapshotSchema-valid inputs.
  </behavior>
  <action>
Create pure helpers for semantic change extraction and canonical digest hashing. Keep descriptions neutral/technical in PT-BR when locale is pt-BR. Do not include signed URLs, prompts, or denied persistence keys. Export typed result shapes for downstream proposal and contract code.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/diff.test.ts</automated>
  </verify>
  <done>Semantic diff and digest utilities are deterministic and safe to use in proposal and confirm binding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement proposal authoring and source-version resolution</name>
  <files>app/src/server/assistant/plan-iteration/proposal.ts, app/src/server/assistant/plan-iteration/proposal.test.ts</files>
  <behavior>
    - Test: working version is chosen when present; approved current is used when working is null.
    - Test: no plan lineage and no campaign plan returns ask_plan clarification instead of a proposal.
    - Test: vague feedback returns clarify question and does not create a proposal.
    - Test: valid feedback creates exactly one pending plan_revision proposal bound to the exact sourceVersionId.
    - Test: approved current snapshot and mutable creative_plans row remain unchanged after proposal creation.
    - Test: working differs from approved and mismatchWarning metadata is returned for card display.
    - Test: out-of-scope feedback (briefing/campaign fields) returns redirect result without proposal.
    - Test: LLM output is validated with planVersionSnapshotSchema; invalid model JSON fails safely.
  </behavior>
  <action>
Implement `proposePlanRevision` using scoped thread/campaign lookup, lazy `adoptArtifactForThread` when a campaign plan exists, and OpenAI text generation with strict JSON parsing. Build `changes[]` and `writes[]` server-side from diff helpers. Persist proposal through `createArtifactProposal`. Return summary-only narrative for the action card. Mock the model in tests. Enforce one proposal per feedback message.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/proposal.test.ts</automated>
  </verify>
  <done>Feedback can create inspectable pending proposals without mutating approved current or campaign plan rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add zero-credit contract and transactional confirm handler</name>
  <files>app/src/server/assistant/action-contracts/contracts/revise-creative-plan.ts, app/src/server/assistant/action-contracts/contracts/index.ts, app/src/server/assistant/action-execution/handlers/revise-creative-plan.ts, app/src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts, app/src/server/assistant/action-execution/execute.ts, app/src/server/assistant/plan-iteration/proposal.ts</files>
  <behavior>
    - Test: contract has zero credit impact and required confirmation.
    - Test: confirm creates one child version with status ready, monotonic version number, and revision provenance links.
    - Test: confirm updates workingVersionId only; approvedCurrentVersionId unchanged.
    - Test: confirm marks proposal confirmed and stalemates sibling pending proposals on the same source.
    - Test: stale proposal confirmation is rejected without creating a version.
    - Test: digest, sourceVersionId, or lineage mismatch is rejected.
    - Test: creative_plans repository is not called for update on confirm.
    - Test: duplicate confirm/idempotent action does not create duplicate versions.
  </behavior>
  <action>
Register `revise_creative_plan` with strict Zod input and `creditImpact: { kind: "none" }`. Implement shared `confirmPlanRevision` transaction: revalidate pending proposal, `createArtifactVersion`, `updateArtifactHead` working only with CAS, `staleSiblingProposals`, `transitionArtifactProposal` to confirmed. Wire handler into execute map as sync completion. Extend `revalidateOnConfirm` to validate proposal binding without requiring guided-flow readiness for this action type.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/action-execution/handlers/revise-creative-plan.test.ts</automated>
  </verify>
  <done>Explicit confirmation appends a ready immutable child version with correct provenance and no credit spend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Persist server-scoped feedback drafts and proposal cancel</name>
  <files>app/drizzle/0065_assistant_thread_plan_draft.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts, app/src/server/repositories/assistant-thread.ts, app/src/server/assistant/plan-iteration/proposal.ts, app/src/server/assistant/plan-iteration/proposal.test.ts</files>
  <behavior>
    - Test: save and load thread-scoped plan feedback draft under workspace/thread scope.
    - Test: draft save does not create proposals or versions.
    - Test: cancel transitions pending proposal to canceled and leaves versions unchanged.
    - Test: cross-thread draft access is rejected.
  </behavior>
  <action>
Add minimal persisted draft storage on assistant thread metadata or dedicated column/table per planner discretion. Expose repository helpers `savePlanFeedbackDraft` and `getPlanFeedbackDraft`. Implement `cancelPlanRevision` via `transitionArtifactProposal` to canceled. Keep drafts bounded (max 2000 chars) and excluded from proposal payload until submitted.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/proposal.test.ts</automated>
  </verify>
  <done>Unsent feedback survives reload server-side and pending proposals can be explicitly discarded.</done>
</task>

</tasks>

<verification>
Run wave-1 gate from `204-VALIDATION.md` tasks 204-01-01 through 204-01-03, then `cd app && npm run build`.
</verification>

<success_criteria>
- PLAN-01: proposal creation leaves approved current unchanged.
- PLAN-03: confirmation creates immutable child version with feedback/source/action provenance.
- PLAN-04: snapshots preserve strategy, angles, hooks, CTAs, constraints, and typed provenance.
- No credit spend on propose or confirm.
- Stale sibling and stale proposal rules match Phase 203.
</success_criteria>

<output>
After completion, create `.planning/phases/204-plan-iteration-loop/204-01-SUMMARY.md`.
</output>
