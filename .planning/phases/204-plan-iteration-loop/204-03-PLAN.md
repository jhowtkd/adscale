---
phase: 204-plan-iteration-loop
plan: "03"
type: execute
wave: 3
gap_closure: true
depends_on:
  - "204-02"
files_modified:
  - app/src/lib/hooks/use-plan-feedback-draft.ts
  - app/src/lib/hooks/use-plan-feedback-draft.test.tsx
  - app/src/lib/hooks/use-assistant-actions.ts
  - app/src/lib/hooks/use-assistant-actions.test.tsx
  - app/src/components/assistant/AssistantChatCore.tsx
  - app/src/components/assistant/AssistantChatCore.test.tsx
  - app/src/components/assistant/AssistantChatInput.tsx
  - app/src/components/assistant/AssistantActionCard.tsx
  - app/src/components/assistant/AssistantActionCard.test.tsx
  - app/src/server/assistant/plan-iteration/service.test.ts
  - app/src/server/assistant/orchestrator.test.ts
  - app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.test.ts
  - app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.test.ts
  - app/src/lib/assistant/contract-display.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
user_setup: []

must_haves:
  truths:
    - "Reload restores server-persisted feedback drafts in the composer for campaign-linked threads"
    - "Unsent composer text autosaves to plan-revisions PUT with debounce and clears on successful send"
    - "Cancel on revise_creative_plan action card cancels the underlying artifact proposal, not only the action record"
    - "Wave 2 integration tests cover service facade, orchestrator branch, draft/cancel routes, and revise contract-display"
  artifacts:
    - path: app/src/lib/hooks/use-plan-feedback-draft.ts
      provides: "GET on mount + debounced PUT for thread-scoped plan feedback drafts"
      exports: ["usePlanFeedbackDraft"]
    - path: app/src/components/assistant/AssistantChatCore.tsx
      provides: "Composer wired to draft hook for campaign threads"
    - path: app/src/lib/hooks/use-assistant-actions.ts
      provides: "Cancel mutation cascades artifact-proposals cancel when proposalId present"
    - path: app/src/server/assistant/plan-iteration/service.test.ts
      provides: "Unit tests for handlePlanRevisionMessage and display builder"
    - path: app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.test.ts
      provides: "GET/PUT draft route tests"
    - path: app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.test.ts
      provides: "Proposal cancel route tests"
  key_links:
    - from: app/src/components/assistant/AssistantChatCore.tsx
      to: app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.ts
      via: "usePlanFeedbackDraft fetch GET/PUT"
      pattern: "plan-revisions"
    - from: app/src/components/assistant/AssistantActionCard.tsx
      to: app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.ts
      via: "useCancelAssistantAction with proposalId from display or inputSnapshot"
      pattern: "artifact-proposals.*cancel"
    - from: app/src/server/assistant/orchestrator.test.ts
      to: app/src/server/assistant/plan-iteration/service.ts
      via: "mock handlePlanRevisionMessage; assert pre-LLM branch"
      pattern: "handlePlanRevisionMessage"
---

# Plan 204-03: Gap Closure — Draft Reload, Proposal Cancel, Wave 2 Tests

<objective>
Close the three integration gaps from `204-VERIFICATION.md` so Phase 204 can re-verify as **passed** (11/11 truths) without changing domain behavior from plans 01–02.

Purpose: Server APIs and domain logic are complete; gaps are client wiring and missing Wave 2 test files.
Output: Composer draft persistence, proposal cancel cascade from action card, and the test files originally listed in 204-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-plan-iteration-loop/204-VERIFICATION.md
@.planning/phases/204-plan-iteration-loop/204-02-SUMMARY.md
@app/src/lib/hooks/use-assistant-actions.ts
@app/src/components/assistant/AssistantChatCore.tsx
@app/src/components/assistant/AssistantChatInput.tsx
@app/src/components/assistant/AssistantActionCard.tsx
@app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.ts
@app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.ts
@app/src/server/assistant/plan-iteration/service.ts

## Gap sources (from verification frontmatter)

1. **Draft reload** — GET/PUT route exists; zero client fetch calls.
2. **Proposal cancel** — Card cancel uses action-only hook; `artifact-proposals/.../cancel` orphaned.
3. **Test debt** — `service.test.ts`, orchestrator plan-revision scenarios, route tests, contract-display revise tests missing.

</context>

<tasks>

## Task 1: Wire composer draft persistence

<action>
Create `usePlanFeedbackDraft(threadId, { enabled })` hook:

- **GET** `/api/assistant/threads/{threadId}/plan-revisions` on mount when `enabled` (thread has `campaignId` from `useAssistantThread` data).
- **Debounced PUT** (≈500ms) when local draft text changes; skip PUT when text matches last server value.
- **Clear** draft on successful message send (call PUT with `""` or existing `clearPlanFeedbackDraft` via empty string per `savePlanFeedbackDraft` behavior).

Extend `AssistantChatInput` with optional controlled draft props:

```typescript
draftText?: string;
onDraftTextChange?: (text: string) => void;
```

When props provided, use controlled mode; otherwise keep existing local `useState` for non-campaign threads.

In `AssistantChatCore`:

- Enable draft hook only when `data?.thread?.campaignId` is set.
- Pass `draftText` / `onDraftTextChange` to `AssistantChatInput`.
- On `handleSend` success path, call `clearDraft()` from hook before/after `sendMessage`.

Add `use-plan-feedback-draft.test.tsx` with mocked `apiFetch` (GET restore, debounced PUT, clear on send).
Update `AssistantChatCore.test.tsx` to assert draft hook invoked for campaign threads (mock hook).
</action>

<verify>
`npm test -- --run src/lib/hooks/use-plan-feedback-draft.test.tsx src/components/assistant/AssistantChatCore.test.tsx`
</verify>

<done>
Campaign-thread composer restores unsent feedback after reload and autosaves edits server-side.
</done>

## Task 2: Cascade proposal cancel from revise action card

<action>
Expose `proposalId` on the action card surface for cancel:

- Add `proposalId` to `buildReviseCreativePlanActionDisplay` return (from `result.proposalId`).
- Ensure `createAssistantAction` display JSON includes `proposalId` (already in `inputSnapshot`; mirror on display for client parsing).

Extend `useCancelAssistantAction`:

```typescript
mutationFn: async ({ actionId, threadId, proposalId }) => {
  if (proposalId) {
    await cancelArtifactProposal(proposalId); // POST artifact-proposals/.../cancel
  }
  return cancelAssistantAction(actionId);
}
```

`AssistantActionCard` for `revise_creative_plan`:

- Read `proposalId` from `display.proposalId` or `payload.inputSnapshot.proposalId`.
- Pass `proposalId` to `cancelMutation.mutate`.

Add tests:

- `use-assistant-actions.test.tsx`: when `proposalId` provided, both endpoints called in order.
- `AssistantActionCard.test.tsx`: cancel on revise card triggers proposal cancel (mock fetch).

**Do not** change server `cancelAssistantAction` unless client cascade proves insufficient — prefer explicit client sequence to keep action and proposal lifecycles visible in network tab.
</action>

<verify>
`npm test -- --run src/lib/hooks/use-assistant-actions.test.tsx src/components/assistant/AssistantActionCard.test.tsx`
</verify>

<done>
Canceling a pending plan revision card sets proposal status to canceled; no orphan pending proposals after UI cancel.
</done>

## Task 3: Add Wave 2 integration tests (204-02 debt)

<action>
Add the test files planned in 204-02 but not committed:

| File | Minimum scenarios |
|------|-------------------|
| `service.test.ts` | `buildReviseCreativePlanActionDisplay` fields; `handlePlanRevisionMessage` returns clarify / action_card / continue (mock `proposePlanRevision`, `classifyPlanRevisionIntent`, `createAssistantAction`) |
| `orchestrator.test.ts` | Campaign thread + plan lineage → `handlePlanRevisionMessage` called before generic LLM; non-campaign → no call |
| `plan-revisions/route.test.ts` | GET returns draft; PUT saves; 404 without campaignId |
| `cancel/route.test.ts` | POST cancels pending proposal; auth/404 paths |
| `contract-display.test.ts` | `shouldShowCreditImpact` false for revise; parse extended display fields (`summary`, `sourceVersionLabel`, `writes`) |

Follow existing route test patterns (mock `requireWorkspaceAccess`, repositories). Keep mocks at repository boundary per project convention.
</action>

<verify>
`npm test -- --run src/server/assistant/plan-iteration/service.test.ts src/server/assistant/orchestrator.test.ts src/app/api/assistant/threads/\\[threadId\\]/plan-revisions/route.test.ts src/app/api/assistant/artifact-proposals/\\[proposalId\\]/cancel/route.test.ts src/lib/assistant/contract-display.test.ts`
</verify>

<done>
All five test files exist and pass; combined with Task 1–2 tests, gap #3 closed.
</done>

</tasks>

<verification>
After all tasks:

```bash
cd app && npm test -- --run \
  src/lib/hooks/use-plan-feedback-draft.test.tsx \
  src/lib/hooks/use-assistant-actions.test.tsx \
  src/components/assistant/AssistantChatCore.test.tsx \
  src/components/assistant/AssistantActionCard.test.tsx \
  src/server/assistant/plan-iteration/service.test.ts \
  src/server/assistant/orchestrator.test.ts \
  src/app/api/assistant/threads/\[threadId\]/plan-revisions/route.test.ts \
  src/app/api/assistant/artifact-proposals/\[proposalId\]/cancel/route.test.ts \
  src/lib/assistant/contract-display.test.ts

npm run build
```

Re-run verifier mental check against `204-VERIFICATION.md` truths #10, #11, and Wave 2 test gap — expect **passed** (11/11) after human UAT items remain optional.
</verification>

<success_criteria>
- Composer restores and autosaves plan feedback drafts via plan-revisions API
- Revise action card cancel calls artifact-proposals cancel with proposalId
- Five missing test files added and passing
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/204-plan-iteration-loop/204-03-SUMMARY.md`
</output>
