---
phase: 204-plan-iteration-loop
plan: "02"
type: execute
wave: 2
depends_on:
  - "204-01"
files_modified:
  - app/src/server/assistant/plan-iteration/intent.ts
  - app/src/server/assistant/plan-iteration/intent.test.ts
  - app/src/server/assistant/plan-iteration/service.ts
  - app/src/server/assistant/plan-iteration/service.test.ts
  - app/src/server/assistant/orchestrator.ts
  - app/src/server/assistant/orchestrator.test.ts
  - app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.ts
  - app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.test.ts
  - app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.ts
  - app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.test.ts
  - app/src/lib/assistant/contract-display.ts
  - app/src/lib/assistant/contract-display.test.ts
  - app/src/components/assistant/AssistantActionCard.tsx
  - app/src/components/assistant/AssistantActionCard.test.tsx
  - app/src/lib/hooks/use-assistant-actions.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
user_setup: []

must_haves:
  truths:
    - "Free-text chat in a campaign-linked thread with a plan lineage routes to plan revision propose/clarify before generic LLM when intent matches"
    - "Successful proposal emits an action_card in the same turn with summary-only review, source version label, write effects, and explicit confirm button"
    - "Reload restores pending proposals, draft text, working selection, and approved current without auto-confirming"
    - "Stale proposals remain visible with warning; confirm attempt is rejected server-side and recoverable"
    - "Out-of-scope revision requests redirect to guided journey copy without creating proposals"
  artifacts:
    - path: app/src/server/assistant/plan-iteration/service.ts
      provides: "Orchestrator-facing propose/clarify/cancel/draft facade"
      exports: ["handlePlanRevisionTurn"]
    - path: app/src/server/assistant/orchestrator.ts
      provides: "Pre-LLM plan revision branch and action_card emission"
    - path: app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.ts
      provides: "Draft save/load for client reload support"
    - path: app/src/components/assistant/AssistantActionCard.tsx
      provides: "Plan revision card rendering without field-by-field diff"
  key_links:
    - from: app/src/server/assistant/orchestrator.ts
      to: app/src/server/assistant/plan-iteration/service.ts
      via: "handlePlanRevisionTurn before generic model call"
      pattern: "handlePlanRevisionTurn"
    - from: app/src/server/assistant/orchestrator.ts
      to: app/src/server/assistant/tools/stubs/propose-action.ts
      via: "create revise_creative_plan action card with proposal binding"
      pattern: "revise_creative_plan"
    - from: app/src/components/assistant/AssistantActionCard.tsx
      to: app/src/lib/assistant/contract-display.ts
      via: "parse revise_creative_plan display fields"
      pattern: "revise_creative_plan"
---

# Plan 204-02: Chat Integration and Review Surface

<objective>
Wire plan iteration into the assistant turn loop, expose draft/cancel APIs for reload, and render the summary-only confirmation card in PT-BR.

Purpose: Users must experience the full feedback → proposal → confirm loop inside chat without waiting for Phase 206 compare UI.
Output: Orchestrator branch, routes, display parser updates, action card UX, and integration tests.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/204-plan-iteration-loop/204-CONTEXT.md
@.planning/phases/204-plan-iteration-loop/204-RESEARCH.md
@.planning/phases/204-plan-iteration-loop/204-VALIDATION.md
@.planning/phases/204-plan-iteration-loop/204-01-SUMMARY.md
@app/src/server/assistant/orchestrator.ts
@app/src/server/assistant/tools/stubs/propose-action.ts
@app/src/components/assistant/AssistantActionCard.tsx
@app/src/lib/assistant/contract-display.ts
@app/src/app/api/assistant/threads/[threadId]/route.ts

<interfaces>
Orchestrator branch contract:

```typescript
type PlanRevisionTurnResult =
  | { kind: "clarify"; assistantText: string }
  | { kind: "ask_plan"; assistantText: string }
  | { kind: "redirect"; assistantText: string }
  | { kind: "proposal"; actionRecordId: string; assistantText: string }
  | { kind: "continue" };
```

Display extension for `revise_creative_plan`:

```typescript
{
  actionType: "revise_creative_plan";
  label: string;
  summary: string;
  sourceVersionLabel: string;
  writes: string[];
  mismatchWarning?: string;
  proposalStatus: "pending" | "stale";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify plan-revision intent and facade service</name>
  <files>app/src/server/assistant/plan-iteration/intent.ts, app/src/server/assistant/plan-iteration/intent.test.ts, app/src/server/assistant/plan-iteration/service.ts, app/src/server/assistant/plan-iteration/service.test.ts</files>
  <behavior>
    - Test: campaign-linked thread with plan lineage and revision-like message returns revise intent.
    - Test: vague message returns clarify intent.
    - Test: briefing/campaign mutation language returns out_of_scope intent.
    - Test: unrelated general chat returns continue intent.
    - Test: facade calls proposePlanRevision and maps results to turn outcomes.
    - Test: reload path returns saved draft without creating proposals.
  </behavior>
  <action>
Implement lightweight `classifyPlanRevisionIntent` using deterministic heuristics plus optional model assist only if needed. Build `handlePlanRevisionTurn` facade over plan-iteration proposal/draft/cancel helpers from 204-01. Keep guided-flow briefing slots untouched.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/plan-iteration/intent.test.ts src/server/assistant/plan-iteration/service.test.ts</automated>
  </verify>
  <done>Orchestrator has a typed entry point for propose, clarify, redirect, and continue decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate orchestrator branch and emit action cards</name>
  <files>app/src/server/assistant/orchestrator.ts, app/src/server/assistant/orchestrator.test.ts</files>
  <behavior>
    - Test: revision feedback in same turn creates assistant message plus action_card event.
    - Test: clarify path returns assistant question without action_card.
    - Test: ask_plan path asks which plan/version to revise when no focus exists.
    - Test: out_of_scope path returns redirect copy without proposal.
    - Test: generic chat still reaches model path when intent is continue.
    - Test: proposal uses revise_creative_plan action type and zero-credit display.
  </behavior>
  <action>
Insert pre-LLM branch after guided-flow command handling when thread has campaignId. On proposal, create action via existing propose-action validation path with custom display payload. Yield `action_card` in the same turn. Persist neutral summary assistant text alongside the card. Do not auto-confirm.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/assistant/orchestrator.test.ts</automated>
  </verify>
  <done>Chat feedback produces a reviewable action card synchronously in the assistant turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose draft and cancel routes for reload recovery</name>
  <files>app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.ts, app/src/app/api/assistant/threads/[threadId]/plan-revisions/route.test.ts, app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.ts, app/src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.test.ts</files>
  <behavior>
    - Test: GET/PUT draft route reads and saves thread-scoped draft under authenticated workspace access.
    - Test: cancel route transitions pending proposal to canceled for owning thread scope.
    - Test: cross-thread and nonmember access return safe 404/403 without leakage.
    - Test: thread GET reload still includes pending proposals and unchanged approved current after draft save.
  </behavior>
  <action>
Add thin authenticated routes delegating to plan-iteration helpers. Use existing assistant auth/workspace patterns and strict Zod bodies. Map validation and ownership errors consistently with artifact-version routes.
  </action>
  <verify>
    <automated>cd app && npm test -- --run 'src/app/api/assistant/threads/[threadId]/plan-revisions/route.test.ts' 'src/app/api/assistant/artifact-proposals/[proposalId]/cancel/route.test.ts'</automated>
  </verify>
  <done>Clients can persist drafts and cancel proposals across reload without autonomous confirms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Render plan revision action card in PT-BR</name>
  <files>app/src/lib/assistant/contract-display.ts, app/src/lib/assistant/contract-display.test.ts, app/src/components/assistant/AssistantActionCard.tsx, app/src/components/assistant/AssistantActionCard.test.tsx, app/src/lib/hooks/use-assistant-actions.ts</files>
  <behavior>
    - Test: parser extracts summary, sourceVersionLabel, writes, mismatchWarning, and proposalStatus for revise_creative_plan.
    - Test: card shows summary-only content without field-by-field diff rows.
    - Test: explicit confirm button label is PT-BR for revise_creative_plan.
    - Test: zero-credit actions hide credit copy.
    - Test: stale proposal shows warning but keeps confirm affordance visible.
    - Test: cancel button calls cancel route or action cancel mutation for pending cards.
  </behavior>
  <action>
Extend `parseActionCardDisplay` and `AssistantActionCard` for `revise_creative_plan`. Render neutral summary, source version label, write effects, and optional mismatch warning. Hide credit section. Keep confirm/cancel behavior aligned with existing action cards. Add i18n strings under assistant.actionCard as needed.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/lib/assistant/contract-display.test.ts src/components/assistant/AssistantActionCard.test.tsx</automated>
  </verify>
  <done>Users can review summary-only plan proposals and confirm or discard from the chat card.</done>
</task>

</tasks>

<verification>
Run full Phase 204 gate from `204-VALIDATION.md`, then `cd app && npm run build`.
</verification>

<success_criteria>
- PLAN-01: chat feedback yields pending proposal without changing approved current.
- PLAN-02: user reviews semantic summary in action card before confirm.
- PLAN-04: reload preserves proposal queue, draft, lifecycle, and current plan pointers.
- Same-turn proposal emission.
- Explicit confirm only; stale server rejection preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/204-plan-iteration-loop/204-02-SUMMARY.md`.
</output>
