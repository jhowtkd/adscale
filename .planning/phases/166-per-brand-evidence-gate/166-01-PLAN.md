---
phase: 166-per-brand-evidence-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/brand-taste/calibration-evidence.ts
  - app/src/server/brand-taste/index.ts
  - app/tests/unit/brand-taste/calibration-evidence.test.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/evidence/route.ts
  - app/src/app/api/admin/quality/brands/[clientProfileId]/evidence/route.test.ts
autonomous: true
requirements:
  - EVIDENCE-01
  - EVIDENCE-02
  - EVIDENCE-03
  - EVIDENCE-05
user_setup: []

must_haves:
  truths:
    - "Each clientProfileId has a dedicated evidence report with evidenceLevel derived from its own signals"
    - "Per-brand claims matrix blocks customer_real_validation and commercial_quality_claim when gates fail"
    - "Insufficient brands receive missingConditions naming exact threshold gaps"
    - "Fixture-only brands block validated_against_customer_real; mixed-source brands may allow when evidence_backed"
  artifacts:
    - path: "app/src/server/brand-taste/calibration-evidence.ts"
      provides: "buildPerBrandEvidenceReport + buildMissingConditions"
      exports: ["buildPerBrandEvidenceReport", "buildMissingConditions", "PerBrandEvidenceReport"]
    - path: "app/src/app/api/admin/quality/brands/[clientProfileId]/evidence/route.ts"
      provides: "Owner-only per-brand evidence GET"
      exports: ["GET"]
  key_links:
    - from: "evidence/route.ts"
      to: "buildPerBrandEvidenceReport"
      via: "listCalibrationSignalsForClientProfile"
      pattern: "buildPerBrandEvidenceReport"
    - from: "buildPerBrandEvidenceReport"
      to: "evaluateClaimsMatrix"
      via: "brand-scoped fixtureOnly + agreement metrics"
      pattern: "evaluateClaimsMatrix"
    - from: "buildPerBrandEvidenceReport"
      to: "buildBrandTasteProfile"
      via: "shared evidenceLevel and sourceComposition"
      pattern: "buildBrandTasteProfile"
---

<objective>
Add per-brand evidence report builder and owner-only evidence API.

Purpose: Close EVIDENCE-01..03 and server-side EVIDENCE-05 — brand-scoped claims matrix and missing-conditions report without duplicating taste-profile thresholds.
Output: `buildPerBrandEvidenceReport`, `GET .../evidence`, expanded unit + route tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/166-per-brand-evidence-gate/166-RESEARCH.md

# Pattern sources
@app/src/server/brand-taste/calibration-evidence.ts
@app/src/server/human-quality/global-evidence.ts
@app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.ts
@app/src/app/api/admin/quality/brands/[clientProfileId]/profile/route.test.ts

# Do NOT modify
- `buildCalibrationEvidenceReport` multi-brand aggregate (unless adding export only)
- Global corpus evidence routes (`global-corpus-evidence`)

<interfaces>
From `@/server/brand-taste/taste-profile`:
```typescript
export function buildBrandTasteProfile(input: {
  clientProfileId: string;
  workspaceId: string;
  signals: CalibrationSignal[];
}): BrandTasteProfile;
```

From `@/server/brand-taste/calibration-evidence`:
```typescript
export function evaluateClaimsMatrix(input: {
  humanDecisionCount: number;
  comparableCount: number;
  agreementRate: number | null;
  fixtureOnly: boolean;
  evidenceLevel: EvidenceLevel;
}): { claimsAllowed: string[]; claimsBlocked: string[] };
```

Thresholds (import from taste-profile or duplicate as named constants only if not exported): SEED=5, ASSISTED=10, EVIDENCE_BACKED_REAL=3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-brand evidence builder</name>
  <files>app/src/server/brand-taste/calibration-evidence.ts, app/src/server/brand-taste/index.ts</files>
  <behavior>
    - Test: fixture-only 15 decisions → evidenceLevel assisted, fixtureOnly true, validated_against_customer_real blocked
    - Test: mixed-source (3 real_customer, 12 total) → evidence_backed when thresholds met
    - Test: missingConditions includes seed gate message when decisionCount &lt; 5
    - Test: missingConditions includes customer-real gate when real_customer === 0 and decisionCount &gt; 0
    - Test: withheldClaims equals subset of claimsBlocked that are customer-real or commercial quality keys
  </behavior>
  <action>
    Add `PER_BRAND_EVIDENCE_SCHEMA_VERSION = 1` and `PerBrandEvidenceReport` interface per RESEARCH.

    Implement `buildPerBrandEvidenceReport({ clientProfileId, workspaceId, signals })`:
    1. `profile = buildBrandTasteProfile({ clientProfileId, workspaceId, signals })`
    2. `agreement = computeAgreementRate(signals)` (brand-scoped)
    3. `fixtureOnly = profile.sourceComposition.real_customer === 0 && profile.decisionCount > 0`
    4. `claims = evaluateClaimsMatrix({ humanDecisionCount: profile.decisionCount, comparableCount: agreement.comparable, agreementRate: agreement.rate, fixtureOnly, evidenceLevel: profile.evidenceLevel })`
    5. `missingConditions = buildMissingConditions({ profile, agreement, claims })` — PT-BR strings naming exact counts needed
    6. `fixtureCaveat` — when fixtureOnly, set same text as `calibration-status-copy.ts` FIXTURE_ONLY_BANNER
    7. `withheldClaims` — blocked keys for `customer_real_validation`, `commercial_quality_claim`, `validated_against_customer_real`

    Export new types/functions from `index.ts`.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/brand-taste/calibration-evidence.test.ts -x</automated>
  </verify>
  <done>Builder returns per-brand report; fixture vs mixed tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner evidence read API</name>
  <files>app/src/app/api/admin/quality/brands/[clientProfileId]/evidence/route.ts, app/src/app/api/admin/quality/brands/[clientProfileId]/evidence/route.test.ts</files>
  <behavior>
    - Test: owner + valid profile returns 200 with evidenceLevel, claimsAllowed, claimsBlocked, missingConditions, fixtureCaveat
    - Test: non-owner returns 403
    - Test: unknown clientProfileId returns 404
    - Test: invalid UUID returns 400
    - Test: fixture-only fixture signals return fixtureOnly true and non-null fixtureCaveat
  </behavior>
  <action>
    Create `GET .../evidence/route.ts` mirroring `profile/route.ts`:
    1. `requirePlatformOwner` → Zod UUID on `clientProfileId`
    2. Resolve workspace from `client_profiles`
    3. `listCalibrationSignalsForClientProfile`
    4. `buildPerBrandEvidenceReport`
    5. Return `{ report }` JSON

    Route tests mock repository or use fixture signals — mirror profile route 403/404/400 cases.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/admin/quality/brands/\\[clientProfileId\\]/evidence/route.test.ts -x</automated>
  </verify>
  <done>Evidence API owner-gated; returns full per-brand report</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted `clientProfileId`; owner session required |
| API→DB | Scope by server-resolved `(workspaceId, clientProfileId)` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-01 | Spoofing | evidence route | mitigate | `requirePlatformOwner` before DB |
| T-166-02 | Information disclosure | evidence route | mitigate | Profile PK workspace resolution |
| T-166-03 | Repudiation | claims matrix | mitigate | Server-side evaluation only; no client override |
</threat_model>

<verification>
```bash
cd app && npm test -- --run tests/unit/brand-taste/calibration-evidence.test.ts src/app/api/admin/quality/brands/\\[clientProfileId\\]/evidence/
```
</verification>

<success_criteria>
- Per-brand evidence report independent of multi-brand aggregate
- Claims matrix uses brand-local fixtureOnly and evidenceLevel
- missingConditions populated when status insufficient
- EVIDENCE-05 server cases: fixture-only vs mixed-source withholding verified
</success_criteria>

<output>
After completion, create `.planning/phases/166-per-brand-evidence-gate/166-01-SUMMARY.md`
</output>
