---
phase: 166-per-brand-evidence-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 166-01
files_modified:
  - app/src/components/admin/BrandEvidencePanel.tsx
  - app/src/components/admin/BrandEvidencePanel.test.tsx
  - app/src/components/admin/OwnerCalibrationPanel.tsx
  - app/src/components/admin/OwnerCalibrationPanel.test.tsx
  - app/src/components/admin/BrandTasteProfilePanel.tsx
autonomous: true
requirements:
  - EVIDENCE-04
  - EVIDENCE-05
user_setup: []

must_haves:
  truths:
    - "Owner sees Evidência tab with claims allowed/blocked and missing conditions for selected brand"
    - "Fixture-only brands always display explicit fixture caveat in Evidência tab and profile status"
    - "UI never shows customer-validated copy when validated_against_customer_real is blocked"
  artifacts:
    - path: "app/src/components/admin/BrandEvidencePanel.tsx"
      provides: "Per-brand evidence display"
      exports: ["BrandEvidencePanel", "fetchBrandEvidence"]
    - path: "app/src/components/admin/OwnerCalibrationPanel.tsx"
      provides: "Evidência tab wiring"
      exports: ["OwnerCalibrationPanel"]
  key_links:
    - from: "BrandEvidencePanel"
      to: "/api/admin/quality/brands/[id]/evidence"
      via: "fetchBrandEvidence"
      pattern: "apiFetch.*evidence"
    - from: "BrandTasteProfilePanel"
      to: "getCalibrationStatusDisplay"
      via: "fixtureOnly banner"
      pattern: "getCalibrationStatusDisplay"
---

<objective>
Add Evidência tab to owner calibration panel with per-brand claims matrix UI and mandatory fixture caveats.

Purpose: Close EVIDENCE-04 and UI/integration EVIDENCE-05 — honest product claims visible to owner without misleading copy.
Output: `BrandEvidencePanel`, Evidência tab, component tests for fixture vs mixed withholding display.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-per-brand-evidence-gate/166-RESEARCH.md
@.planning/phases/166-per-brand-evidence-gate/166-01-PLAN.md

# Pattern sources
@app/src/components/admin/BrandTasteProfilePanel.tsx
@app/src/components/admin/calibration-status-copy.ts
@app/src/components/feedback/HumanQualityCorpusPanel.tsx (CoverageTabContent claims display)
@app/src/components/admin/OwnerCalibrationPanel.tsx

# API contract (from Plan 01)
GET `/api/admin/quality/brands/{clientProfileId}/evidence` → `{ report: PerBrandEvidenceReport }`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BrandEvidencePanel component</name>
  <files>app/src/components/admin/BrandEvidencePanel.tsx, app/src/components/admin/BrandEvidencePanel.test.tsx</files>
  <behavior>
    - Test: renders evidence level label and source composition counts
    - Test: fixture-only report shows fixtureCaveat banner (warning variant)
    - Test: lists claimsBlocked including validated_against_customer_real for fixture-only
    - Test: mixed-source evidence_backed report does not show fixture caveat; customer-real claim not in blocked list
    - Test: missingConditions rendered as bullet list when non-empty
    - Test: 403 returns forbidden copy
  </behavior>
  <action>
    Create `BrandEvidencePanel` with `fetchBrandEvidence(clientProfileId)` using TanStack Query pattern from `BrandTasteProfilePanel`.

    Display sections:
    1. Status row — evidence level + `getCalibrationStatusDisplay` when profile fields available (or map from report)
    2. **Fixture caveat** — prominent warning when `report.fixtureOnly` (EVIDENCE-04); use `report.fixtureCaveat` text
    3. Source composition table (reuse SOURCE_LABELS from BrandTasteProfilePanel or import shared constant)
    4. Claims allowed / blocked — two lists with human-readable PT-BR labels for claim keys (map object, not raw keys)
    5. Missing conditions — `report.missingConditions` bullets

    Do **not** show green "totalmente calibrado" or customer-validated badges when `validated_against_customer_real` ∈ claimsBlocked.

    Add `BrandEvidencePanel.test.tsx` with mocked fetch responses for fixture-only vs mixed-source fixtures.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/BrandEvidencePanel.test.tsx -x</automated>
  </verify>
  <done>Evidence panel renders claims matrix and fixture caveat; tests cover EVIDENCE-04/05 UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Evidência tab + profile caveat reinforcement</name>
  <files>app/src/components/admin/OwnerCalibrationPanel.tsx, app/src/components/admin/OwnerCalibrationPanel.test.tsx, app/src/components/admin/BrandTasteProfilePanel.tsx</files>
  <behavior>
    - Test: OwnerCalibrationPanel TAB_ITEMS includes "Evidência" tab
    - Test: selecting Evidência tab renders BrandEvidencePanel
    - Test: Profile tab still shows fixture banner via getCalibrationStatusDisplay when fixtureOnly
  </behavior>
  <action>
    Add `{ id: "evidence", label: "Evidência" }` to `TAB_ITEMS` in `OwnerCalibrationPanel` (after Profile or before Proposals).

    Render `<BrandEvidencePanel clientProfileId={clientProfileId} />` when `activeTab === "evidence"`.

    In `BrandTasteProfilePanel`, ensure `fixtureOnly` always triggers warning banner (already via `getCalibrationStatusDisplay` — verify no regression; add test assertion if missing).

    Extend `OwnerCalibrationPanel.test.tsx` with tab navigation to Evidência.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/admin/OwnerCalibrationPanel.test.tsx src/components/admin/BrandEvidencePanel.test.tsx -x</automated>
  </verify>
  <done>Evidência tab live; profile tab retains fixture caveat (EVIDENCE-04)</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-166-04 | Repudiation | BrandEvidencePanel | mitigate | Render server claimsBlocked; no client-side claim promotion |
</threat_model>

<verification>
```bash
cd app && npm test -- --run src/components/admin/BrandEvidencePanel.test.tsx src/components/admin/OwnerCalibrationPanel.test.tsx
```
</verification>

<success_criteria>
- Owner can inspect per-brand evidence gate from calibration panel
- Fixture-only brands always show explicit caveat in Profile and Evidência tabs
- Component tests prove withholding difference between fixture-only and mixed-source
</success_criteria>

<output>
After completion, create `.planning/phases/166-per-brand-evidence-gate/166-02-SUMMARY.md`
</output>
