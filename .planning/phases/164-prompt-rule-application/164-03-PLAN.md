---
phase: 164-prompt-rule-application
plan: "03"
type: execute
wave: 3
depends_on:
  - "164-01"
  - "164-02"
files_modified:
  - app/tests/unit/ai/prompt-rule-isolation.test.ts
autonomous: true
requirements:
  - APPLY-05
must_haves:
  truths:
    - "Profile A approved rule IDs appear in profile A prompt and log but never in profile B"
    - "Profile B corpus_quality tags are absent from profile A prompt text"
    - "Scoped repository queries enforce isolation without app-layer filtering after global fetch"
  artifacts:
    - path: app/tests/unit/ai/prompt-rule-isolation.test.ts
      provides: "Cross-profile prompt and provenance isolation assertions"
  key_links:
    - from: app/tests/unit/ai/prompt-rule-isolation.test.ts
      to: app/src/server/brand-taste/prompt-calibration-loader.ts
      via: "loadPromptCalibrationContext per profile"
      pattern: "loadPromptCalibrationContext"
    - from: app/tests/unit/ai/prompt-rule-isolation.test.ts
      to: app/src/server/ai/prompt-builder.ts
      via: "buildDerivationPrompt with per-profile sections"
      pattern: "buildDerivationPrompt"
---

# Plan 164-03: Cross-Profile Rule Isolation Test

<objective>
Prove rules from one `clientProfileId` never leak into another profile's derivation prompt or generation log provenance.

Purpose: Close APPLY-05 gap — repository filters exist but no integration-style test asserts end-to-end isolation.
Output: `prompt-rule-isolation.test.ts` with two-profile scenario.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-prompt-rule-application/164-RESEARCH.md
@.planning/phases/164-prompt-rule-application/164-VALIDATION.md
@app/src/server/repositories/calibration-rule.ts
@app/src/server/brand-taste/prompt-calibration-loader.ts
@app/src/server/ai/prompt-builder.ts

<interfaces>
Test fixture shape (create in test file):
```typescript
const profileA = { id: "profile-a", workspaceId: "ws-1" };
const profileB = { id: "profile-b", workspaceId: "ws-1" };
const ruleA = { id: "rule-a-1", clientProfileId: "profile-a", category: "brand_nuance", rationale: "..." };
const ruleB = { id: "rule-b-1", clientProfileId: "profile-b", category: "corpus_quality", rationale: "cta_drift: ..." };
```

Use `vi.mock` on `listApprovedCalibrationRulesByCategories` and `listCalibrationSignalsForClientProfile` to return profile-specific rows — no TEST_DATABASE_URL required for APPLY-05 (research Environment table fallback).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loader isolation assertions</name>
  <files>app/tests/unit/ai/prompt-rule-isolation.test.ts</files>
  <behavior>
    - loadPromptCalibrationContext for profile A includes rule-a-1 in appliedBrandRuleIds or appliedCorpusRuleIds as appropriate
    - loadPromptCalibrationContext for profile B never returns rule-a-1 in any applied*Ids array
    - Mock repository receives clientProfileId matching the profile under test on every call
  </behavior>
  <action>
    Create `prompt-rule-isolation.test.ts` (Wave 0 stub from 164-VALIDATION.md). Mock repositories so profile A has brand_nuance + corpus_quality rules and profile B has distinct rule IDs.

    Call `loadPromptCalibrationContext` for each profile. Assert:
    - `appliedBrandRuleIds` / `appliedCorpusRuleIds` contain only that profile's rule IDs
    - No cross-contamination between arrays
    - Repository mock called with `eq(clientProfileId)` matching input (verify mock call args)

    For brand-taste on profile A, mock signals so `buildBrandTasteProfile` yields calibrated evidence (not uncalibrated skip).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/ai/prompt-rule-isolation.test.ts -t "loader isolation"</automated>
  </verify>
  <done>Loader returns profile-scoped rule IDs with no cross-profile leakage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end prompt substring isolation</name>
  <files>app/tests/unit/ai/prompt-rule-isolation.test.ts</files>
  <behavior>
    - Profile A prompt contains [brand-taste:rule-a-1] or [corpus-quality:rule-a-corpus] tags
    - Profile B prompt does not contain profile A rule ID tags
    - Profile A prompt does not contain [corpus-quality:rule-b-1] or [brand-taste:rule-b-1]
  </behavior>
  <action>
    Extend same test file: for each profile, call `loadPromptCalibrationContext` then `buildDerivationPrompt` with minimal campaign fixture (`clientProfileId`, `workspaceId`) plus returned sections.

    Assert prompt string:
    - Includes own tagged constraints (`[brand-taste:${id}]`, `[corpus-quality:${id}]`)
    - Excludes other profile's rule ID substrings
    - Maintains section order headers present when sections non-empty (Olhar before BRAND TASTE before CORPUS QUALITY)

    Simulate generation log patch: `{ appliedBrandRuleIds, appliedCorpusRuleIds }` from loader — assert profile B log arrays never include profile A IDs.

    Do NOT use `listApprovedCorpusQualityRules` in test setup — reinforces pitfall #11.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/ai/prompt-rule-isolation.test.ts</automated>
  </verify>
  <done>Full prompt and provenance isolation test green for two clientProfileIds in same workspace</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile A ↔ profile B | Same workspaceId; isolation is clientProfileId-scoped |
| test mocks → production code | Test proves contract; production must keep scoped queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-06 | Information disclosure | cross-profile prompt | mitigate | Test fails if foreign rule tags appear; production uses scoped Drizzle where |
| T-164-07 | Tampering | global rule list | mitigate | Test asserts listApprovedCorpusQualityRules not used in loader path |
</threat_model>

<verification>
```bash
cd app && npm test -- --run tests/unit/ai/prompt-rule-isolation.test.ts
cd app && npm test -- --run
```
</verification>

<success_criteria>
- Automated test proves profile A rule IDs absent from profile B prompt and log arrays
- Repository mocks verify clientProfileId passed on every rule fetch
- Full test file passes without TEST_DATABASE_URL
</success_criteria>

<output>
After completion, create `.planning/phases/164-prompt-rule-application/164-03-SUMMARY.md`
</output>
