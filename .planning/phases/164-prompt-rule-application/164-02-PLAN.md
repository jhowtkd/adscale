---
phase: 164-prompt-rule-application
plan: "02"
type: execute
wave: 2
depends_on:
  - "164-01"
files_modified:
  - app/src/server/human-quality/learning/corpus-quality-cap.ts
  - app/src/server/human-quality/learning/proposals.ts
  - app/src/server/brand-taste/prompt-calibration-loader.ts
  - app/tests/unit/repositories/calibration-rule-cap.test.ts
autonomous: true
requirements:
  - APPLY-04
must_haves:
  truths:
    - "When approved corpus_quality count exceeds 10, oldest rules are deprecated in DB"
    - "Cap enforcement runs on proposal accept and on derivation load as safety net"
    - "Prompt receives at most 10 active corpus_quality rules after cap"
  artifacts:
    - path: app/src/server/human-quality/learning/corpus-quality-cap.ts
      provides: "enforceCorpusQualityRuleCap with DB deprecation"
      exports: ["enforceCorpusQualityRuleCap"]
    - path: app/src/server/human-quality/learning/proposals.ts
      provides: "accept hook triggers cap after insert"
  key_links:
    - from: app/src/server/human-quality/learning/proposals.ts
      to: app/src/server/human-quality/learning/corpus-quality-cap.ts
      via: "acceptClientLearningProposal after insertCalibrationRule"
      pattern: "enforceCorpusQualityRuleCap"
    - from: app/src/server/brand-taste/prompt-calibration-loader.ts
      to: app/src/server/human-quality/learning/corpus-quality-cap.ts
      via: "replace slice-only cap on corpus path"
      pattern: "enforceCorpusQualityRuleCap"
---

# Plan 164-02: Corpus Quality Cap with DB Deprecation

<objective>
Persist corpus_quality rule cap enforcement by deprecating oldest approved rules on overflow, not only slicing at prompt time.

Purpose: Close APPLY-04 gap — `slice(0, 10)` and `buildCorpusQualityPromptSection` internal cap exist but orphan approved rows accumulate without `deprecateCalibrationRule`.
Output: `enforceCorpusQualityRuleCap`, accept-hook wiring, loader integration, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-prompt-rule-application/164-RESEARCH.md
@docs/superpowers/specs/2026-06-21-corpus-learning-loop-design.md
@app/src/server/brand-taste/calibration-rules.ts
@app/src/server/human-quality/learning/proposals.ts
@app/src/server/brand-taste/prompt-calibration-loader.ts

<interfaces>
From `app/src/server/brand-taste/calibration-rules.ts` (existing):
```typescript
export async function deprecateCalibrationRule(input: {
  workspaceId: string;
  ruleId: string;
}): Promise<CalibrationRule | null>;
```

From `app/src/server/human-quality/learning/corpus-quality-prompt.ts`:
```typescript
const MAX_CORPUS_QUALITY_RULES = 10;
export function buildCorpusQualityPromptSection(rules: ...): string[];
```

**Do not remove** `buildCorpusQualityPromptSection` or reimplement prompt tags — cap module returns pre-filtered active rules only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: enforceCorpusQualityRuleCap with deprecation</name>
  <files>app/src/server/human-quality/learning/corpus-quality-cap.ts, app/tests/unit/repositories/calibration-rule-cap.test.ts</files>
  <behavior>
    - 10 or fewer rules: no deprecation; all returned as active
    - 11 rules: oldest by approvedAt ASC (fallback createdAt) deprecated; newest 10 active
    - deprecateCalibrationRule called once per overflow rule with correct workspaceId
    - Returns deprecatedIds for observability
  </behavior>
  <action>
    Create `enforceCorpusQualityRuleCap({ workspaceId, clientProfileId, rules, maxActive = 10 })` per research Pattern 3. Sort approved rules by `(approvedAt ?? createdAt)` ascending. When count > maxActive, deprecate `sorted.slice(0, length - max)` via existing `deprecateCalibrationRule`; return `{ active: sorted.slice(-max), deprecatedIds }`.

    Default maxActive = 10 aligned with `MAX_CORPUS_QUALITY_RULES` in corpus-quality-prompt.ts. Do not add a second constant in derivation.ts.

    Create `calibration-rule-cap.test.ts` with mocked `deprecateCalibrationRule` asserting call count and which rule IDs are deprecated vs kept active.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/repositories/calibration-rule-cap.test.ts</automated>
  </verify>
  <done>Cap function deprecates overflow rules in DB with passing unit tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire cap on accept and loader safety net</name>
  <files>app/src/server/human-quality/learning/proposals.ts, app/src/server/brand-taste/prompt-calibration-loader.ts, app/tests/unit/human-quality/learning/proposals.test.ts</files>
  <behavior>
    - acceptClientLearningProposal triggers cap after corpus_quality rule insert
    - loadPromptCalibrationContext calls enforceCorpusQualityRuleCap instead of slice-only
    - Proposals test verifies cap invoked post-accept (mock enforceCorpusQualityRuleCap)
  </behavior>
  <action>
    In `acceptClientLearningProposal`, after `insertCalibrationRule` for `corpus_quality`, reload approved corpus_quality rules for `(workspaceId, clientProfileId)` and call `enforceCorpusQualityRuleCap`. Per research open question #1: accept-time enforcement is primary.

    In `loadPromptCalibrationContext` (from 164-01), replace `rules.slice(0, 10)` with `enforceCorpusQualityRuleCap` as load-time safety net for legacy overflow rows.

    Remove redundant `slice(0, 10)` from derivation.ts if any remains after 164-01 refactor. Keep defensive slice inside `buildCorpusQualityPromptSection` as last resort only.

    Extend `proposals.test.ts` with case: after accept, `enforceCorpusQualityRuleCap` is called (vi.mock the cap module).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/repositories/calibration-rule-cap.test.ts tests/unit/human-quality/learning/proposals.test.ts</automated>
  </verify>
  <done>Cap runs on accept and derivation load; no slice-only overflow path remains in loader</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accept hook → calibration_rules | Owner-approved insert then server-side cap mutation |
| cap → deprecateCalibrationRule | Status transition must be workspace-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-04 | Tampering | corpus-quality-cap | mitigate | deprecateCalibrationRule filters by workspaceId + ruleId |
| T-164-05 | Denial of Service | cap loop | accept | Bounded overflow (typically 1 rule per accept); no unbounded deprecations |
</threat_model>

<verification>
```bash
cd app && npm test -- --run tests/unit/repositories/calibration-rule-cap.test.ts tests/unit/human-quality/learning/proposals.test.ts
cd app && npm run lint && npm run build
```
</verification>

<success_criteria>
- Overflow corpus_quality rules get status deprecated in DB, not just omitted from prompt
- Loader and accept path both invoke enforceCorpusQualityRuleCap
- At most 10 active corpus_quality rules per clientProfileId in prompt path
</success_criteria>

<output>
After completion, create `.planning/phases/164-prompt-rule-application/164-02-SUMMARY.md`
</output>
