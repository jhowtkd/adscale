---
phase: 164-prompt-rule-application
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/brand-taste/prompt-calibration-loader.ts
  - app/src/server/brand-taste/taste-application.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/generation-log.ts
  - app/src/server/jobs/derivation.ts
  - app/tests/unit/brand-taste/prompt-calibration-loader.test.ts
  - app/tests/unit/brand-taste/taste-application.test.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/jobs/derivation-generation-log.test.ts
autonomous: true
requirements:
  - APPLY-01
  - APPLY-02
  - APPLY-03
must_haves:
  truths:
    - "Derivation loads brand-taste rules for clientProfileId via selectApplicableRules evidence gate"
    - "Prompt section order is Olhar ADScale then brand-taste then corpus_quality"
    - "Generation log records appliedBrandRuleIds alongside existing appliedCorpusRuleIds"
  artifacts:
    - path: app/src/server/brand-taste/prompt-calibration-loader.ts
      provides: "Unified calibration context for derivation prompts"
      exports: ["loadPromptCalibrationContext"]
    - path: app/src/server/ai/prompt-builder.ts
      provides: "Post-Olhar injection of brand-taste and corpus sections"
    - path: app/src/server/ai/generation-log.ts
      provides: "appliedBrandRuleIds field on DerivationGenerationLog"
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/brand-taste/prompt-calibration-loader.ts
      via: "load-prompt-calibration-context step replaces load-corpus-quality-rules"
      pattern: "loadPromptCalibrationContext"
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/olhar/generation-direction.ts
      via: "buildGenerationDirectionSection before brandTasteSection"
      pattern: "GENERATION_DIRECTION_HEADER.*BRAND TASTE CONSTRAINTS.*CORPUS QUALITY"
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/generation-log.ts
      via: "finalizeGenerationLog with both ID arrays"
      pattern: "appliedBrandRuleIds|appliedCorpusRuleIds"
---

# Plan 164-01: Brand-Taste Wiring, Section Order, Provenance

<objective>
Wire approved brand-taste rules into derivation prompts, enforce Olhar → brand-taste → corpus_quality section order, and log `appliedBrandRuleIds` alongside existing corpus provenance.

Purpose: Close APPLY-01/02/03 gaps on `feat/corpus-learning-loop` without re-implementing existing corpus_quality load, `buildCorpusQualityPromptSection`, or `appliedCorpusRuleIds` — consolidate into one loader and reorder prompt-builder.
Output: `loadPromptCalibrationContext`, prompt-builder reorder, derivation job wiring, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-prompt-rule-application/164-RESEARCH.md
@.planning/phases/164-prompt-rule-application/164-VALIDATION.md
@.planning/ROADMAP.md
@app/src/server/jobs/derivation.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/brand-taste/taste-application.ts
@app/src/server/human-quality/learning/corpus-quality-prompt.ts
@app/src/server/ai/generation-log.ts

<interfaces>
From `app/src/server/brand-taste/taste-application.ts`:
```typescript
export function selectApplicableRules(input: {
  approvedRules: CalibrationRule[];
  profile: BrandTasteProfile | null;
}): TasteApplicationResult;

export function buildBrandTastePromptSection(
  rules: Array<{ id: string; category: string; rationale: string }>
): string[];
```

From `app/src/server/human-quality/learning/corpus-quality-prompt.ts` (existing — do not rewrite):
```typescript
export function buildCorpusQualityPromptSection(
  rules: Array<{ id: string; rationale: string; category: string }>
): string[];
```

From `app/src/server/repositories/calibration-rule.ts` (existing — keep scoped query):
```typescript
export async function listApprovedCalibrationRulesByCategories(input: {
  workspaceId: string;
  clientProfileId: string;
  categories: string[];
}): Promise<CalibrationRule[]>;
```

From `app/src/server/ai/olhar/generation-direction.ts`:
```typescript
export const GENERATION_DIRECTION_HEADER = "DIRECAO DE ARTE PARA GERACAO";
export async function buildGenerationDirectionSection(...): Promise<string[]>;
```

**Branch scout constraint:** `load-corpus-quality-rules` step, `buildCorpusQualityPromptSection`, and `appliedCorpusRuleIds` finalize already exist — refactor into loader; do not duplicate logic in parallel code paths.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibration context loader and taste-application gate tests</name>
  <files>app/src/server/brand-taste/prompt-calibration-loader.ts, app/tests/unit/brand-taste/prompt-calibration-loader.test.ts, app/tests/unit/brand-taste/taste-application.test.ts</files>
  <behavior>
    - loadPromptCalibrationContext returns empty sections and ID arrays when clientProfileId is null
    - Brand-taste categories = all RULE_CATEGORIES except corpus_quality (per research A2)
    - selectApplicableRules gate skips brand-taste for uncalibrated profiles even when approved rules exist
    - Calibrated profile returns real rule IDs in appliedBrandRuleIds (not inline-0 placeholders)
    - Corpus path delegates to buildCorpusQualityPromptSection with scoped listApprovedCalibrationRulesByCategories query
    - Never imports listApprovedCorpusQualityRules (global query — Phase 167 only)
  </behavior>
  <action>
    Create `loadPromptCalibrationContext({ workspaceId, clientProfileId })` returning:
    `{ brandTasteSection, corpusQualitySection, appliedBrandRuleIds, appliedCorpusRuleIds }`.

    Brand-taste path (APPLY-01 gap): parallel-fetch `listCalibrationSignalsForClientProfile` + `listApprovedCalibrationRulesByCategories` with `RULE_CATEGORIES.filter(c => c !== "corpus_quality")`. Build profile via `buildBrandTasteProfile`, apply `selectApplicableRules`, render section with `buildBrandTastePromptSection` on rules whose IDs are in `appliedRuleIds`. Do NOT use `getApprovedRuleConstraints` alone — it bypasses the evidence gate.

    Corpus path (preserve existing behavior): same scoped query as current `load-corpus-quality-rules` step; call existing `buildCorpusQualityPromptSection`; keep prompt-time slice(0, 10) until Plan 164-02 adds DB deprecation.

    Create `taste-application.test.ts` covering `selectApplicableRules` uncalibrated skip + calibrated apply. Create `prompt-calibration-loader.test.ts` with mocked repositories asserting scoped category calls and ID arrays.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/brand-taste/taste-application.test.ts tests/unit/brand-taste/prompt-calibration-loader.test.ts</automated>
  </verify>
  <done>Loader exists with brand-taste wiring and gate tests green; corpus behavior preserved via delegation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt-builder section order regression</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - GENERATION_DIRECTION_HEADER appears before "BRAND TASTE CONSTRAINTS"
    - "BRAND TASTE CONSTRAINTS" appears before "CORPUS QUALITY CONSTRAINTS"
    - Pre-rendered brandTasteSection lines are pushed as-is (no re-wrap via buildBrandTastePromptSection with inline-0 IDs)
  </behavior>
  <action>
    Per APPLY-02: Remove early injection at L431–445 (before `buildGenerationDirectionSection`). Add config field `brandTasteSection?: string[]` (pre-rendered from loader). After `buildGenerationDirectionSection` (L476+), push `brandTasteSection` then `corpusQualitySection` when non-empty.

    Deprecate the `brandTasteConstraints` re-wrap path — if still needed for backward compat, map it only when `brandTasteSection` is undefined; prefer loader output. Pitfall 5 from research: never double-wrap pre-rendered sections.

    Add test `"section order: Olhar before brand-taste before corpus_quality"` in `prompt-builder.test.ts` passing mock `brandTasteSection` and `corpusQualitySection` with index assertions on the three headers.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/prompt-builder.test.ts -t "section order"</automated>
  </verify>
  <done>Prompt-builder enforces mandated section order with regression test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derivation job wiring and generation log provenance</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/ai/generation-log.ts, app/tests/unit/jobs/derivation-generation-log.test.ts</files>
  <behavior>
    - derivationJob step load-prompt-calibration-context replaces load-corpus-quality-rules
    - buildDerivationPrompt receives brandTasteSection + corpusQualitySection on primary and auto-retry paths
    - finalizeGenerationLog sets appliedBrandRuleIds and appliedCorpusRuleIds on primary finalize
    - Auto-retry finalize also merges both ID arrays (research open question #2)
  </behavior>
  <action>
    Extend `DerivationGenerationLog` with `appliedBrandRuleIds?: string[]` (APPLY-03 gap).

    Replace `step.run("load-corpus-quality-rules", ...)` with `load-prompt-calibration-context` calling `loadPromptCalibrationContext`. Pass `brandTasteSection` and `corpusQualitySection` to both `buildDerivationPrompt` call sites (~L643 and ~L1125). Update variable names from `corpusQualityContext` to `calibrationContext`.

    In `finalize-generation-log` (~L1240), add `appliedBrandRuleIds: calibrationContext.appliedBrandRuleIds` alongside existing `appliedCorpusRuleIds`. In auto-retry finalize (~L1141), merge `{ appliedBrandRuleIds, appliedCorpusRuleIds, autoRetryAttempted, autoRetryReason }`.

    Create `derivation-generation-log.test.ts` testing `finalizeGenerationLog` patch includes both ID arrays; mock loader return shape if testing derivation step extraction is too heavy.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/jobs/derivation-generation-log.test.ts</automated>
  </verify>
  <done>Derivation job uses unified loader; generation log records both rule ID arrays on primary and retry paths</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derivationJob → calibration_rules | Server loads rules by (workspaceId, clientProfileId); campaign.clientProfileId must be trusted from DB |
| calibration_rules → prompt string | Approved rule rationale becomes model instruction; owner-approved only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Elevation | prompt-calibration-loader | mitigate | Only `listApprovedCalibrationRulesByCategories` with both IDs; ban global list |
| T-164-02 | Tampering | brand-taste gate | mitigate | `selectApplicableRules` blocks uncalibrated overlay per v13.0 contract |
| T-164-03 | Tampering | prompt-builder order | mitigate | Regression test locks Olhar precedence over taste/corpus |
</threat_model>

<verification>
```bash
cd app && npm test -- --run tests/unit/brand-taste/ src/server/ai/prompt-builder.test.ts tests/unit/jobs/derivation-generation-log.test.ts
cd app && npm run lint && npm run build
```
</verification>

<success_criteria>
- Brand-taste approved rules reach derivation prompt for calibrated profiles only
- Section order Olhar → brand-taste → corpus_quality verified by automated test
- `appliedBrandRuleIds` and `appliedCorpusRuleIds` both present in finalizeGenerationLog patch
</success_criteria>

<output>
After completion, create `.planning/phases/164-prompt-rule-application/164-01-SUMMARY.md`
</output>
