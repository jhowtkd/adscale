---
phase: 119-observable-rubric
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - app/src/server/ai/creative-score.ts
  - app/tests/unit/ai/creative-score.test.ts
autonomous: true
requirements:
  - RUBR-01
  - RUBR-02
  - RUBR-03
  - RUBR-04
must_haves:
  truths:
    - "Score vision prompt extracted to buildCreativeScorePrompt for testability"
    - "Score prompt includes same observable rubric blocks as QA prompt"
    - "Score prompt penalizes visualQuality for overload and generic template; caps score when hook illegible at thumbnail"
  artifacts:
    - path: app/src/server/ai/creative-score.ts
      provides: buildCreativeScorePrompt with observable rubric injection
      exports:
        - buildCreativeScorePrompt
        - analyzeDerivationCreative
    - path: app/tests/unit/ai/creative-score.test.ts
      provides: score prompt rubric parity tests
  key_links:
    - from: app/src/server/ai/creative-score.ts
      to: app/src/server/ai/observable-rubric.ts
      via: buildObservableScoreRubricSection in buildCreativeScorePrompt
      pattern: buildObservableScoreRubricSection
    - from: analyzeDerivationCreative
      to: buildCreativeScorePrompt
      via: prompt = buildCreativeScorePrompt(input) before OpenAI call
      pattern: buildCreativeScorePrompt
---

# Plan 119-03: Extract Score Prompt + Wire Observable Rubric (RUBR-01–04)

<objective>
Extract `buildCreativeScorePrompt` from inline `analyzeDerivationCreative` string, inject `buildObservableScoreRubricSection` for QA/score parity, and add `allowedEntities` block matching QA.

Purpose: Prevent QA/score drift — overload flagged in QA must also depress visualQuality in score path (RUBR-01, RUBR-02).

Output: Testable score prompt builder + extended `creative-score.test.ts`.

**Out of scope:** Numeric score ceilings in code (Phase 121); gate promotion (Phase 120).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-observable-rubric/119-02-SUMMARY.md
@app/src/server/ai/observable-rubric.ts
@app/src/server/ai/creative-score.ts
@app/src/server/ai/creative-qa.ts
@app/tests/unit/ai/creative-score.test.ts

<interfaces>
From `analyzeDerivationCreative` — inline prompt at lines 243-281 to extract.

`AnalyzeInput` shape (from creative-score.ts):
```typescript
// campaign, derivation, contract?, imageBuffer, mimeType, locale
```

Score-specific rubric additions (RESEARCH Pattern 4):
```
- visualQuality: penalize below 50 for severe generic template aesthetic or visual overload regardless of polish.
- Do NOT score visualQuality above 70 when hook is illegible at thumbnail scale.
- scoreIssues must cite visible elements (same OBSERVABLE DEFECT NOTE RULE).
```

QA parity — allowedEntities block from creative-qa.ts lines 125-131:
```typescript
const allowedEntities = resolveAllowedEntitiesForCampaign({ name, client });
// briefMatch comparison against registry
```
</interfaces>
</context>

<tasks>

<task id="119-03-01" type="auto" tdd="true">
  <name>Extract buildCreativeScorePrompt with observable rubric</name>
  <files>app/src/server/ai/creative-score.ts, app/tests/unit/ai/creative-score.test.ts</files>
  <behavior>
    - buildCreativeScorePrompt is exported and returns same structural content as prior inline prompt
    - Score prompt includes buildObservableScoreRubricSection output with overload/generic/thumbnail rules
    - Score prompt includes visualQuality penalty guidance for overload (RUBR-01)
    - Score prompt includes allowedEntities briefMatch block when campaign matches registry (parity with QA)
    - analyzeDerivationCreative calls buildCreativeScorePrompt(input) — no inline prompt string
  </behavior>
  <action>
In `creative-score.ts`:

1. Import `buildObservableScoreRubricSection`, `extractObservableRubricSection` from `./observable-rubric`
2. Import `resolveAllowedEntitiesForCampaign` from creative-corpus (same as creative-qa.ts)

3. Extract `export function buildCreativeScorePrompt(input: AnalyzeInput): string` containing:
   - Existing CTA instruction logic (explicit/inherited/legacy)
   - Campaign context block
   - scoreDimensionPromptLines()
   - CONTRACT VIOLATIONS / CRITICAL INFORMATION PRESERVATION / variationLevelFit blocks (preserve verbatim from inline prompt)
   - Restyling scoring instruction when applicable
   - **allowedEntitiesInstruction** — copy pattern from creative-qa.ts for briefMatch registry (RESEARCH open question #3)
   - **rubricSection** via `buildObservableScoreRubricSection({ generationMode, targetFormat, dominantIdea })`
   - regenerationSuggestion footer

4. Refactor `analyzeDerivationCreative` to:
```typescript
const prompt = buildCreativeScorePrompt(input);
```

Add `creative-score.test.ts` describe `buildCreativeScorePrompt observable rubric`:
- `includes visual overload scoring guidance (RUBR-01)` — match `/visual overload|visualQuality.*below 50|dominant focal/i`
- `includes generic template rubric (RUBR-02)` — match `/GENERIC TEMPLATE|generic template/i`
- `includes observable defect note rule (RUBR-03)` — match `/OBSERVABLE DEFECT|visible elements/i`
- `includes thumbnail preview scale (RUBR-04)` — match `/THUMBNAIL|PREVIEW SCALE|270/i`
- `rubric parity with QA` — same extractObservableRubricSection headers as QA fixture prompt

Import `buildCreativeScorePrompt` in tests (export from creative-score.ts).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-score.test.ts -t "overload|observable rubric|buildCreativeScorePrompt"</automated>
  </verify>
  <done>buildCreativeScorePrompt exported with observable rubric; analyzeDerivationCreative uses it; overload tests pass.</done>
</task>

<task id="119-03-02" type="auto">
  <name>Preserve existing score behavior and OpenAI integration</name>
  <files>app/tests/unit/ai/creative-score.test.ts</files>
  <action>
Ensure existing `creative-score.test.ts` cases still pass:
- `normalizeCreativeScoreResult` suite
- `analyzeDerivationCreative` mock OpenAI integration test (if present)
- `buildRegenerationSuggestion`, `scoreDerivationHeuristic` tests

Verify extraction did not drop scoreBreakdown dimension instructions or variationLevelFit guidance.

Run: `cd app && npm test -- tests/unit/ai/creative-score.test.ts`
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-score.test.ts</automated>
  </verify>
  <done>Full creative-score test file green after prompt extraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA vs score prompt divergence | Single observable-rubric.ts source mitigates drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05 | Tampering | Score/QA rubric drift | mitigate | Shared module + parity test comparing extractObservableRubricSection |
| T-119-06 | Tampering | High visualQuality hides overload | mitigate | Prompt caps visualQuality when hook illegible; penalize overload in rubric |
</threat_model>

<verification>
`cd app && npm test -- tests/unit/ai/creative-score.test.ts` passes including new rubric tests.
</verification>

<success_criteria>
- buildCreativeScorePrompt exported and used by analyzeDerivationCreative
- RUBR-01–04 observable rubric present in score prompt with visualQuality penalty language
- allowedEntities briefMatch block present when campaign has registry entry
- Existing score normalization and API tests unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/119-observable-rubric/119-03-SUMMARY.md`
</output>
