---
phase: 119-observable-rubric
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/observable-rubric.ts
  - app/tests/unit/ai/quality-rubric-regression.test.ts
autonomous: true
requirements:
  - RUBR-01
  - RUBR-02
  - RUBR-03
  - RUBR-04
must_haves:
  truths:
    - "Observable rubric module exports overload, generic-template, observable-note, and thumbnail rubric blocks"
    - "Thumbnail rubric uses format-derived preview dimensions from getTargetDimensions(format, true)"
    - "Rubric maps failures to existing criteria (creativeRisk, legibility, briefMatch) without new checklist keys"
  artifacts:
    - path: app/src/server/ai/observable-rubric.ts
      provides: rubric constants, builders, extractors, Phase 120 note markers
      exports:
        - VISUAL_OVERLOAD_RUBRIC
        - GENERIC_TEMPLATE_RUBRIC
        - OBSERVABLE_DEFECT_NOTE_RULE
        - FORBIDDEN_APPROVAL_TERMS
        - buildObservableQaRubricSection
        - buildObservableScoreRubricSection
        - buildThumbnailHookRubricLine
        - extractObservableRubricSection
        - OVERLOAD_NOTE_MARKERS
        - GENERIC_TEMPLATE_NOTE_MARKERS
    - path: app/tests/unit/ai/quality-rubric-regression.test.ts
      provides: module-level rubric presence tests for RUBR-01–04
  key_links:
    - from: app/src/server/ai/observable-rubric.ts
      to: app/src/lib/formats.ts
      via: getTargetDimensions(targetFormat, true) in buildThumbnailHookRubricLine
      pattern: getTargetDimensions
    - from: app/src/server/ai/observable-rubric.ts
      to: prompt-builder tropes vocabulary
      via: GENERIC_TEMPLATE_RUBRIC reuses neon/glow/glassmorphism trope list
      pattern: neon glow|glassmorphism|holographic
---

# Plan 119-01: Observable Rubric Module Scaffold (RUBR-01–04)

<objective>
Create `observable-rubric.ts` mirroring Phase 117/118 module patterns — shared rubric prose for QA and score vision prompts, with extractors and forward-reference note markers for Phase 120.

Purpose: Single source of truth for observable evaluation criteria before wiring into `creative-qa.ts` and `creative-score.ts`.

Output: New rubric module + module-level unit tests proving RUBR-01–04 content is present.

**Out of scope:** Wiring into QA/score prompts (Plans 02–03); gate hard-failure promotion (Phase 120).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-observable-rubric/119-RESEARCH.md
@.planning/phases/119-observable-rubric/119-VALIDATION.md
@app/src/server/ai/factual-visual-separation.ts
@app/src/server/ai/per-mode-prompt-rules.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/corpus-fixtures.ts
@app/src/lib/formats.ts

<interfaces>
From `app/src/lib/formats.ts`:
```typescript
export function getTargetDimensions(formatId: string, isPreview?: boolean): { width: number; height: number } | null
// isPreview=true → dimensions/4 (e.g. 1:1 → ~270×270)
```

From `app/src/server/ai/prompt-builder.ts` (tropes to align with GENERIC_TEMPLATE_RUBRIC):
```
neon glow stacks, holographic grids, glassmorphism cards, excessive lens flares, volumetric CTA pills, "premium tech" gradient stacks
```

From `app/src/server/ai/corpus-fixtures.ts` (note exemplars to align rubric language):
- `visual_overload`: competing zones, card grid, no dominant focal
- `generic_template_aesthetic`: neon glow, glassmorphism, premium-tech gradient

Criterion mapping (no new checklist keys):
| Rubric | Primary criterion | Secondary |
|--------|-------------------|-----------|
| Visual overload | creativeRisk | briefMatch |
| Generic template | creativeRisk | — |
| Hook illegible at thumbnail | legibility | creativeRisk |
| Observable note quality | all criteria | — |
</interfaces>
</context>

<tasks>

<task id="119-01-01" type="auto" tdd="true">
  <name>Create observable-rubric module with all rubric blocks</name>
  <files>app/src/server/ai/observable-rubric.ts</files>
  <behavior>
    - VISUAL_OVERLOAD_RUBRIC requires single dominant focal, ≤3 competing zones, no competing CTAs (RUBR-01)
    - GENERIC_TEMPLATE_RUBRIC lists neon/glow/glassmorphism tropes and requires brand/campaign justification (RUBR-02)
    - OBSERVABLE_DEFECT_NOTE_RULE forbids polished/professional-only approval; requires visible element citations (RUBR-03)
    - FORBIDDEN_APPROVAL_TERMS lists polished, professional, premium feel, high quality, well designed
    - buildThumbnailHookRubricLine("1:1") includes ~270×270 preview dimensions (RUBR-04)
    - buildObservableQaRubricSection returns joined string[] with criterion mapping header
    - buildObservableScoreRubricSection includes visualQuality scoring caps language
    - extractObservableRubricSection extracts bounded rubric block from full prompt
    - OVERLOAD_NOTE_MARKERS and GENERIC_TEMPLATE_NOTE_MARKERS exported as RegExp for Phase 120
  </behavior>
  <action>
Create `app/src/server/ai/observable-rubric.ts` exporting:

**Constants (per RESEARCH Pattern 1):**
- `OBSERVABLE_DEFECT_NOTE_RULE` — cite zones, text snippets, colors, positions, counts; BAD/GOOD exemplar lines (RUBR-03)
- `FORBIDDEN_APPROVAL_TERMS` — polished, professional, premium feel, high quality, well designed
- `VISUAL_OVERLOAD_RUBRIC` — no dominant focal, >3 competing zones, multiple CTAs vs hook; map to `creativeRisk` + `briefMatch` failed (RUBR-01)
- `GENERIC_TEMPLATE_RUBRIC` — trope list aligned with `VISUAL_HIERARCHY_CONTRACT` in prompt-builder.ts; fail when unjustified by brief/brand (RUBR-02)
- `CRITERION_MAPPING_HEADER` — documents creativeRisk/legibility/briefMatch mapping (no new checklist keys)

**Builders:**
- `ObservableRubricOptions` — `{ generationMode?, targetFormat?, dominantIdea?, renderTier? }`
- `buildThumbnailHookRubricLine(targetFormat: string): string` — calls `getTargetDimensions(targetFormat, true)`; interpolate width×height; fail `legibility` when hook unclear at preview scale (RUBR-04)
- `buildObservableQaRubricSection(options): string` — joins defect rule, overload, generic, thumbnail, criterion mapping
- `buildObservableScoreRubricSection(options): string` — same blocks + score-specific lines: penalize visualQuality below 50 for severe overload/generic; cap visualQuality at 70 when hook illegible at thumbnail; scoreIssues must cite visible elements

**Extractors (mirror Phase 118 `extractPromptPerModeRulesSection`):**
- `extractObservableRubricSection(prompt: string): string` — from first `OBSERVABLE DEFECT NOTES` through end of `THUMBNAIL / PREVIEW SCALE` block or next major section marker

**Phase 120 forward-reference markers:**
- `OVERLOAD_NOTE_MARKERS`, `GENERIC_TEMPLATE_NOTE_MARKERS`, `MISSING_DOMINANT_IDEA_MARKERS` as RegExp exports

Do NOT import or modify `creative-quality-gate.ts` in this plan.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit -p app/tsconfig.json 2>/dev/null || cd app && npm run build --if-present 2>&1 | head -5; test -f app/src/server/ai/observable-rubric.ts</automated>
  </verify>
  <done>Module exports all rubric constants, builders, extractors, and note markers; TypeScript compiles.</done>
</task>

<task id="119-01-02" type="auto" tdd="true">
  <name>Add module-level rubric regression tests</name>
  <files>app/tests/unit/ai/quality-rubric-regression.test.ts</files>
  <behavior>
    - visual overload: buildObservableQaRubricSection matches dominant focal, three zones, competing CTAs
    - generic template: rubric matches GENERIC TEMPLATE and trope vocabulary
    - observable defect: rubric matches OBSERVABLE DEFECT NOTES and forbids polished-only approval
    - thumbnail: buildThumbnailHookRubricLine("1:1") cites 270×270 or 270
    - extractObservableRubricSection round-trips on synthetic prompt containing rubric blocks
  </behavior>
  <action>
Create `app/tests/unit/ai/quality-rubric-regression.test.ts` with describe blocks:

1. `observable rubric module — visual overload (RUBR-01)` — assert `buildObservableQaRubricSection({ targetFormat: "1:1" })` matches `/VISUAL OVERLOAD/i`, `/dominant focal/i`, `/more than three|three information zones/i`, `/competing.*CTA/i`

2. `observable rubric module — generic template (RUBR-02)` — assert rubric matches `/GENERIC TEMPLATE/i`, `/neon glow|glassmorphism|holographic/i`, `/justified.*campaign|brand/i`

3. `observable rubric module — observable defect notes (RUBR-03)` — assert `/OBSERVABLE DEFECT NOTES/i`, `/polished|professional/i` in forbidden context, good/bad exemplar pattern

4. `observable rubric module — thumbnail preview scale (RUBR-04)` — assert `buildThumbnailHookRubricLine("1:1")` matches `/270/` and `/preview|thumbnail/i`; assert full section includes format-derived dimensions

5. `extractObservableRubricSection` — synthetic prompt with rubric blocks; extractor returns non-empty bounded section

Tests import from `@/server/ai/observable-rubric` only — no QA/score wiring yet (Plan 02–03).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts -t "visual overload|generic template|observable defect|thumbnail"</automated>
  </verify>
  <done>Module-level tests pass for RUBR-01–04 rubric content before prompt wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign/contract strings → rubric prose | Untrusted text interpolated into rubric; no elevation to system role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | buildObservableQaRubricSection | mitigate | Observable defect rule requires visible citations; forbids vague approval |
| T-119-02 | Repudiation | Vision model ignores rubric | accept | Phase 120 gate promotion; Phase 123 live validation |
</threat_model>

<verification>
`cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts` passes all RUBR-01–04 module tests.
</verification>

<success_criteria>
- RUBR-01: VISUAL_OVERLOAD_RUBRIC exported with dominant focal, zone count, CTA competition rules
- RUBR-02: GENERIC_TEMPLATE_RUBRIC exported with trope list and justification clause
- RUBR-03: OBSERVABLE_DEFECT_NOTE_RULE + FORBIDDEN_APPROVAL_TERMS exported
- RUBR-04: buildThumbnailHookRubricLine uses getTargetDimensions(format, true) for preview scale
</success_criteria>

<output>
After completion, create `.planning/phases/119-observable-rubric/119-01-SUMMARY.md`
</output>
