---
phase: 119-observable-rubric
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - app/tests/unit/ai/quality-rubric-regression.test.ts
  - app/tests/unit/ai/corpus-baseline.test.ts
autonomous: true
requirements:
  - RUBR-01
  - RUBR-02
  - RUBR-03
  - RUBR-04
must_haves:
  truths:
    - "Every corpus archetype fixture produces QA prompt with full observable rubric and no export-softening"
    - "visual_overload and generic_template_aesthetic archetype prompts enable corpus note vocabulary"
    - "BASELINE_GAP_COUNT remains 4 — no premature gate promotion"
    - "Generic creativeRisk still routes to polishSuggestions at gate (unchanged until Phase 120)"
  artifacts:
    - path: app/tests/unit/ai/quality-rubric-regression.test.ts
      provides: corpus archetype × rubric integration tests
    - path: app/tests/unit/ai/corpus-baseline.test.ts
      provides: unchanged BASELINE_GAP_COUNT assertion
  key_links:
    - from: quality-rubric-regression.test.ts
      to: corpus-fixtures.ts
      via: CORPUS_ARCHETYPE_FIXTURES describe.each
      pattern: CORPUS_ARCHETYPE_FIXTURES
    - from: buildCreativeQaPrompt
      to: corpus fixture contract fields
      via: generationMode, targetFormat, client from fixture
      pattern: buildCreativeQaPrompt
---

# Plan 119-04: Corpus Rubric Regression + Phase Verification (RUBR-01–04)

<objective>
Extend `quality-rubric-regression.test.ts` with corpus archetype integration tests, verify `BASELINE_GAP_COUNT` unchanged, and run phase gate (test + lint + build).

Purpose: Prove rubric prompts align with corpus note exemplars for `visual_overload` and `generic_template_aesthetic` without flipping baseline gap or gate behavior.

Output: Integration regression suite + phase verification green.

**Out of scope:** Greening corpus-baseline `it.fails` cases (Phase 120); updating creative-quality-gate.test.ts generic→hard expectation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-observable-rubric/119-03-SUMMARY.md
@app/src/server/ai/corpus-fixtures.ts
@app/src/server/ai/creative-qa.ts
@app/src/server/ai/observable-rubric.ts
@app/tests/unit/ai/corpus-baseline.test.ts
@app/tests/unit/ai/creative-quality-gate.test.ts

<interfaces>
From `corpus-fixtures.ts`:
```typescript
export const CORPUS_ARCHETYPE_FIXTURES: CorpusArchetypeFixture[]
// archetypes: visual_overload, generic_template_aesthetic, ...
// each has contract, rawQaModelOutput with observable note exemplars
```

From `corpus-baseline.test.ts`:
```typescript
export const BASELINE_GAP_COUNT = CORPUS_ARCHETYPE_FIXTURES.length - 1; // must stay 4
```

Corpus note vocabulary targets (rubric must enable model to produce):
- visual_overload: "competing zones", "card grid", "no dominant focal"
- generic_template_aesthetic: "neon glow", "glassmorphism", "premium-tech gradient"
</interfaces>
</context>

<tasks>

<task id="119-04-01" type="auto" tdd="true">
  <name>Corpus archetype rubric integration tests</name>
  <files>app/tests/unit/ai/quality-rubric-regression.test.ts</files>
  <behavior>
    - For each CORPUS_ARCHETYPE_FIXTURE: buildCreativeQaPrompt includes full observable rubric
    - Prompt does not contain "Export must remain allowed"
    - visual_overload fixture: rubric section matches overload vocabulary aligned with fixture notes
    - generic_template_aesthetic fixture: rubric matches generic template tropes from fixture notes
    - 1:1 format fixtures: thumbnail rubric cites ~270 preview dimensions
    - buildCreativeScorePrompt for same fixtures includes matching rubric sections
  </behavior>
  <action>
Extend `app/tests/unit/ai/quality-rubric-regression.test.ts`:

1. Import `CORPUS_ARCHETYPE_FIXTURES` from `@/server/ai/corpus-fixtures`
2. Import `buildCreativeQaPrompt` from `@/server/ai/creative-qa`
3. Import `buildCreativeScorePrompt` from `@/server/ai/creative-score`
4. Import `extractObservableRubricSection` from `@/server/ai/observable-rubric`

Add `describe.each(CORPUS_ARCHETYPE_FIXTURES)("corpus archetype rubric — $archetype", (fixture) => {`:

- Build minimal campaign/derivation from fixture.contract (client, product, offer, generationMode, targetFormat)
- `it("QA prompt includes observable rubric without export-softening")` — extractObservableRubricSection non-empty; headers present; `expect(prompt).not.toMatch(/Export must remain allowed/i)`

- `it("score prompt includes observable rubric parity")` — same rubric headers in buildCreativeScorePrompt output

Add focused tests:
- `visual_overload archetype enables overload note vocabulary (RUBR-01)` — rubric matches fixture.rawQaModelOutput creativeRisk note themes (competing zones, focal)
- `generic_template_aesthetic archetype enables generic note vocabulary (RUBR-02)` — rubric tropes match fixture note themes

Add `1:1 preview dimensions (RUBR-04)` test using format "1:1" fixture — assert `/270/` in rubric section.

Keep module-level tests from Plan 01 in same file (both describe blocks coexist).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts</automated>
  </verify>
  <done>Full quality-rubric-regression.test.ts passes for all corpus archetypes.</done>
</task>

<task id="119-04-02" type="auto">
  <name>Verify baseline gap unchanged and run phase gate</name>
  <files>app/tests/unit/ai/corpus-baseline.test.ts
  </files>
  <action>
**Do NOT modify** `creative-quality-gate.ts`, `classifyCreativeRiskFailed`, or `BASELINE_GAP_COUNT` value.

Run regression suite confirming Phase 119 did not prematurely promote gate failures:

1. `cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts -t "baseline gap"` — expect `BASELINE_GAP_COUNT` still 4

2. `cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "polish only"` — generic creativeRisk still polish-only

3. Phase gate:
   - `cd app && npm test -- src/server/ai/creative-qa.test.ts tests/unit/ai/quality-rubric-regression.test.ts tests/unit/ai/creative-score.test.ts`
   - `cd app && npm run lint`
   - `cd app && npm run build`

Confirm no diff in `creative-quality-gate.ts` or `creative-quality-taxonomy.ts` from Phase 119 work.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts -t "baseline gap" && cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "polish"</automated>
  </verify>
  <done>BASELINE_GAP_COUNT=4 unchanged; gate polish routing unchanged; phase test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rubric-only change vs gate enforcement | Phase 119 must not flip baseline tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-07 | Tampering | Accidental gate promotion in 119 | mitigate | corpus-baseline + gate polish tests in phase verification |
| T-119-08 | Repudiation | Rubric prompts don't match corpus notes | mitigate | Archetype integration tests assert vocabulary alignment |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/quality-rubric-regression.test.ts` — full file green
- `cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts -t "baseline gap"` — BASELINE_GAP_COUNT=4
- `cd app && npm test && npm run lint && npm run build` — phase gate green
</verification>

<success_criteria>
- RUBR-01–04: Corpus archetype QA and score prompts include observable rubric; no export-softening
- visual_overload and generic_template_aesthetic fixtures have rubric vocabulary alignment tests
- BASELINE_GAP_COUNT remains 4; creative-quality-gate generic→polish behavior unchanged
- Full app test suite, lint, and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/119-observable-rubric/119-04-SUMMARY.md`
</output>
