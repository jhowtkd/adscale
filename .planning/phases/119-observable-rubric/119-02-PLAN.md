---
phase: 119-observable-rubric
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/ai/creative-qa.ts
  - app/src/server/ai/creative-qa.test.ts
autonomous: true
requirements:
  - RUBR-01
  - RUBR-02
  - RUBR-03
  - RUBR-04
must_haves:
  truths:
    - "QA vision prompt includes full observable rubric section from observable-rubric.ts"
    - "QA prompt no longer contains Export must remain allowed export-softening bias"
    - "QA prompt instructs failed checklist status for overload, generic template, and thumbnail hook violations"
  artifacts:
    - path: app/src/server/ai/creative-qa.ts
      provides: buildCreativeQaPrompt wired with buildObservableQaRubricSection
      contains: buildObservableQaRubricSection
    - path: app/src/server/ai/creative-qa.test.ts
      provides: regression tests for rubric injection and export-softening removal
  key_links:
    - from: app/src/server/ai/creative-qa.ts
      to: app/src/server/ai/observable-rubric.ts
      via: buildObservableQaRubricSection after derivation block, before locale line
      pattern: buildObservableQaRubricSection
    - from: app/src/server/ai/creative-qa.ts
      to: extractObservableRubricSection
      via: re-export for tests (match Phase 117/118 extractor pattern)
      pattern: extractObservableRubricSection
---

# Plan 119-02: Wire Observable Rubric into QA Prompt (RUBR-01–04)

<objective>
Inject `buildObservableQaRubricSection` into `buildCreativeQaPrompt`, remove export-softening bias, and add integrity-first evaluation language so the vision QA model reports observable failures honestly.

Purpose: Close RUBR-03 export-copilot gap — model can mark `creativeRisk`/`legibility` as `failed` with visible-element notes.

Output: Updated `creative-qa.ts` + extended `creative-qa.test.ts`.

**Out of scope:** Score prompt wiring (Plan 03); gate promotion (Phase 120).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-observable-rubric/119-01-SUMMARY.md
@app/src/server/ai/observable-rubric.ts
@app/src/server/ai/creative-qa.ts
@app/src/server/ai/creative-qa.test.ts

<interfaces>
From `app/src/server/ai/creative-qa.ts` (current export-softening — REMOVE):
```
Export must remain allowed. Use warning or review to guide the user, not to block them.
```

Replacement (RESEARCH Pattern 3):
```
Evaluate honestly against the rubric below. Use status "failed" on checklist criteria when observable defects are present — especially visual overload, unjustified generic template aesthetics, or hook illegibility at thumbnail scale.
Overall QA status may be "review" when multiple warnings exist; do not mark "ready" if any criterion is "failed".
```

From `buildCreativeQaPrompt` input shape:
```typescript
export function buildCreativeQaPrompt(input: Omit<AnalyzeCreativeQaInput, "imageBuffer" | "mimeType">)
// contract?: CreativeContract | null — use for generationMode, targetFormat, canonicalCreative.dominantIdea
```

Injection point: after derivation block + styleFidelity/allowedEntities instructions, before locale line.
</interfaces>
</context>

<tasks>

<task id="119-02-01" type="auto" tdd="true">
  <name>Wire rubric into buildCreativeQaPrompt and remove export-softening</name>
  <files>app/src/server/ai/creative-qa.ts, app/src/server/ai/creative-qa.test.ts</files>
  <behavior>
    - buildCreativeQaPrompt does NOT contain "Export must remain allowed" (RUBR-03)
    - buildCreativeQaPrompt contains integrity-first evaluation language with "failed" checklist instruction
    - extractObservableRubricSection(prompt) is non-empty and includes VISUAL OVERLOAD, GENERIC TEMPLATE, THUMBNAIL sections
    - Rubric receives targetFormat from derivation.format ?? contract.targetFormat
    - Top-level QA status enum unchanged (ready|warning|review) — checklist items use failed
  </behavior>
  <action>
In `creative-qa.ts`:

1. Import `buildObservableQaRubricSection`, `extractObservableRubricSection` from `./observable-rubric`

2. **Remove** line 140: `Export must remain allowed. Use warning or review to guide the user, not to block them.`

3. **Replace** with integrity-first block (RESEARCH Pattern 3):
```
Evaluate honestly against the rubric below. Use status "failed" on checklist criteria when observable defects are present — especially visual overload, unjustified generic template aesthetics, or hook illegibility at thumbnail scale.
Overall QA status may be "review" when multiple warnings exist; do not mark "ready" if any criterion is "failed".
```

4. After derivation block and `${styleFidelityInstruction}${allowedEntitiesInstruction}`, inject:
```typescript
const rubricSection = buildObservableQaRubricSection({
  generationMode: input.contract?.generationMode ?? input.derivation.generationMode ?? undefined,
  targetFormat: input.derivation.format ?? input.contract?.targetFormat ?? undefined,
  dominantIdea: input.contract?.canonicalCreative?.dominantIdea,
});
```
Append `\n\n${rubricSection}` before the locale line.

5. Re-export `extractObservableRubricSection` from `creative-qa.ts` (match Phase 118 re-export pattern).

Add `creative-qa.test.ts` describe `observable rubric in QA prompt`:
- `does not contain export-softening bias (RUBR-03)` — `expect(prompt).not.toMatch(/Export must remain allowed/i)`
- `includes integrity-first failed-criteria language` — match `/failed.*checklist|checklist.*failed/i`
- `includes observable rubric sections (RUBR-01–04)` — use `extractObservableRubricSection(prompt)`; assert overload, generic, thumbnail, observable defect headers present
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/creative-qa.test.ts -t "export must remain|observable rubric"</automated>
  </verify>
  <done>QA prompt wired with observable rubric; export-softening removed; tests pass.</done>
</task>

<task id="119-02-02" type="auto">
  <name>Preserve existing QA prompt contracts and verify no gate changes</name>
  <files>app/src/server/ai/creative-qa.test.ts</files>
  <action>
Ensure existing `creative-qa.test.ts` cases still pass:
- `builds a QA prompt with CTA, offer, format, and campaign context`
- `styleFidelity criterion` blocks for restyling

Add assertion that rubric injection does not break checklist key list or JSON return instruction.

Confirm **no edits** to `creative-quality-gate.ts`, `creative-quality-taxonomy.ts`, or `corpus-baseline.test.ts` in this plan.

Run wave merge regression:
`cd app && npm test -- src/server/ai/creative-qa.test.ts tests/unit/ai/creative-quality-gate.test.ts -t "polish only"`
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/creative-qa.test.ts</automated>
  </verify>
  <done>All creative-qa tests green; gate behavior unchanged (generic still polish-only until Phase 120).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vision QA model output → gate | Rubric changes reporting only; gate unchanged in 119 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-03 | Tampering | Export-softening removal | mitigate | Regression test asserts absence of export-softening string |
| T-119-04 | Tampering | False negative on overload | accept | Phase 120 promotes creativeRisk failed → hard failure |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/creative-qa.test.ts` passes including observable rubric and export-softening tests.
</verification>

<success_criteria>
- RUBR-01–04: QA prompt includes full observable rubric via buildObservableQaRubricSection
- RUBR-03: "Export must remain allowed" removed; integrity-first language present
- Existing styleFidelity and normalization tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/119-observable-rubric/119-02-SUMMARY.md`
</output>
