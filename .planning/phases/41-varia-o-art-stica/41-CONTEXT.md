# Phase 41 Context: Variação artística

**Requirements:** DRV-01, DRV-02, DRV-03, DRV-04

Manual and auto art-variation paths must collect creativity profile + CTAs before calling `configureAndGenerate`. Auto path pre-fills from analyze/campaign context and remains editable.

**Depends on:** Phase 40 routing shell
