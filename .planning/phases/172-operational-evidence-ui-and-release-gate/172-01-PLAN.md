---
phase: 172-operational-evidence-ui-and-release-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/feedback/FactualAlertsPanel.tsx
  - app/src/components/feedback/FactualAlertsPanel.test.tsx
autonomous: true
requirements:
  - ALERT-01
  - ALERT-02
  - ALERT-03
user_setup: []
must_haves:
  truths:
    - Owner can see factual issue alerts rendered from the factual-alerts API response.
    - Each alert shows sliceKey, workspaceId, clientProfileId, rationale, and aggregate stats.
    - Alerts link to brand calibration and corpus queue context without exposing prompts or storage keys.
    - Alert section is read-only with explicit factual-guard warning copy — no accept/reject/generate actions.
  artifacts:
    - path: app/src/components/feedback/FactualAlertsPanel.tsx
      provides: Read-only factual alerts list with safe evidence links
      exports: ["FactualAlertsPanel", "fetchFactualAlerts"]
    - path: app/src/components/feedback/FactualAlertsPanel.test.tsx
      provides: Unit tests for list render, links, and no-action guard
  key_links:
    - from: app/src/components/feedback/FactualAlertsPanel.tsx
      to: /api/admin/quality/learning/factual-alerts
      via: apiFetch in useQuery
      pattern: "apiFetch.*factual-alerts"
    - from: app/src/components/feedback/FactualAlertsPanel.tsx
      to: /admin/quality/brands/{clientProfileId}
      via: Link href on brand row
      pattern: "/admin/quality/brands/"
---

<objective>
Create `FactualAlertsPanel` — a read-only client component that fetches and renders factual issue alerts with sanitized evidence links.

Purpose: Complete the LEARN-06 alert half (backend already skips factual slices from proposals); ALERT-01..03 need a dedicated UI before panel mounting.
Output: Panel component + Vitest unit tests proving list render, evidence links, and no rule-creation actions.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-operational-evidence-ui-and-release-gate/172-CONTEXT.md
@.planning/phases/172-operational-evidence-ui-and-release-gate/172-RESEARCH.md
@.planning/phases/172-operational-evidence-ui-and-release-gate/172-VALIDATION.md

<interfaces>
From `app/src/server/human-quality/calibration/types.ts`:
```typescript
export interface FactualIssueAlertEvidence {
  corpusItemIds: string[];
  artifactIds?: string[];
  stats: {
    count: number;
    meanSignedDelta: number | null;
    meanAbsError: number | null;
    overScoreCount: number;
    underScoreCount: number;
  };
}

export interface FactualIssueAlert {
  workspaceId: string;
  clientProfileId: string;
  sliceKey: string;
  evidenceRefs: FactualIssueAlertEvidence;
  rationale: "factual_guard_review_required";
}
```

From `app/src/components/feedback/LearningProposalsTab.tsx` (pattern to mirror):
```typescript
// apiFetch + useQuery; 403 → return null → restricted message
// Optional workspaceId / clientProfileId query params
// variant: "workspace" | "brand" for heading copy
```

API response shape:
```json
{ "alerts": FactualIssueAlert[] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FactualAlertsPanel fetch and section shell</name>
  <files>app/src/components/feedback/FactualAlertsPanel.tsx, app/src/components/feedback/FactualAlertsPanel.test.tsx</files>
  <behavior>
    - Test 1: Renders loading then alert list when API returns alerts fixture.
    - Test 2: Returns restricted message when apiFetch status is 403.
    - Test 3: Shows empty state copy when alerts array is empty.
    - Test 4: Fetch URL includes workspaceId and clientProfileId when props set (per D-01 filter pattern).
  </behavior>
  <action>
    Create `FactualAlertsPanel.tsx` in `app/src/components/feedback/` colocated with `LearningProposalsTab`.

    Export `fetchFactualAlerts(workspaceId?, clientProfileId?)` and `FactualAlertsPanel` with props `{ workspaceId?, clientProfileId?, variant?: "workspace" | "brand" }`.

    Per D-01 (alert surface placement): component is standalone — mounting happens in Plan 02. Do **not** add a new admin sidebar route.

    Implement TanStack Query fetch to `GET /api/admin/quality/learning/factual-alerts` with optional query params matching `LearningProposalsTab` filter pattern.

    Wrap in dedicated `<section data-testid="factual-alerts-panel">` with heading per D-03:
    - EN workspace: "Factual issue alerts"
    - PT brand variant: "Alertas de problema factual"

    Apply warning/destructive-adjacent border or badge: "Factual guard — not a calibration rule" (PT variant OK per discretion).

    Include explanatory copy per D-03: slices failed factual guard review and must not be accepted as `corpus_quality` prompt rules; operator should review corpus items manually.

    Match loading/error/403 UX from `LearningProposalsTab` (muted text, no crash on 403).

    Empty state per D-01: "No factual issue slices meet alert thresholds".

    Do **not** add Accept, Reject, Generate, or any rule-creation controls (D-03).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/feedback/FactualAlertsPanel.test.tsx</automated>
  </verify>
  <done>Panel fetches factual-alerts API with scoped filters; section shell renders with factual-guard warning copy and honest empty/403 states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Safe evidence links and stats display</name>
  <files>app/src/components/feedback/FactualAlertsPanel.tsx, app/src/components/feedback/FactualAlertsPanel.test.tsx</files>
  <behavior>
    - Test 1: Each alert shows sliceKey, workspaceId, clientProfileId, rationale, and stats (count, meanSignedDelta, over/under counts).
    - Test 2: Brand link href is `/admin/quality/brands/{clientProfileId}`.
    - Test 3: Each corpusItemId renders as link to `/feedback` with visible id label (or copy button fallback per discretion).
    - Test 4: When artifactIds present, shows count only — no download link, no prompt/storage key fields in DOM.
    - Test 5: Query by role/button finds no Accept, Reject, or Generate buttons.
  </behavior>
  <action>
    Per D-02 (evidence linking), render per alert:
    - `sliceKey`, `workspaceId`, `clientProfileId`, `rationale` (always `factual_guard_review_required`)
    - `evidenceRefs.stats`: count, meanSignedDelta, meanAbsError, overScoreCount, underScoreCount

    Brand link: `<a href="/admin/quality/brands/{clientProfileId}">` — optional `?tab=evidence` deep-link is discretion; default to base brand URL.

    Corpus evidence: for each `corpusItemId` in `evidenceRefs.corpusItemIds`, link to `/feedback` with item id visible as link text or adjacent monospace label. If deep selection is awkward, show id + copy-to-clipboard button (D-02 conservative fallback). **Never** fetch evaluation notes, prompts, `artifactRef`, or S3/storage keys.

    Artifact ids: if `evidenceRefs.artifactIds` present, display count only (e.g. "3 artifacts referenced") — no link to raw artifact endpoints.

    Sanitization bar matches Phase 169/166 evidence panels: IDs and aggregate stats only. Do not render fields named `prompt`, `artifactRef`, `storageKey`, `evaluationNotes`, or similar.

    Visual layout: match `LearningProposalsTab` density (table or card stack — discretion favors consistency with proposals table if present).

    Read-only reinforcement (D-03): no action column, no mutation hooks.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/components/feedback/FactualAlertsPanel.test.tsx</automated>
  </verify>
  <done>Alerts display sanitized evidence with brand and corpus links; tests prove no accept/reject/generate actions and no forbidden field leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser → factual-alerts API | Owner-only alert data crosses to client |
| alert UI → evidence links | Links must not expose private prompt/storage payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-01 | I | FactualAlertsPanel render | mitigate | Render IDs/stats only; forbid prompt/artifactRef/storageKey in JSX; test asserts absence |
| T-172-01b | E | Accept/Reject on factual slice | mitigate | No action buttons; read-only section separated from LearningProposalsTab |
| T-172-02 | I | factual-alerts API response | transfer | Route already owner-gated via requirePlatformOwner; panel handles 403 |
| T-172-03 | S | Corpus item link to /feedback | accept | Owner-only route; link shows id only |
</threat_model>

<verification>
```bash
cd app && npm test -- --run src/components/feedback/FactualAlertsPanel.test.tsx
cd app && npm test -- --run src/app/api/admin/quality/learning/factual-alerts/route.test.ts
```
</verification>

<success_criteria>
- FactualAlertsPanel exports fetch helper and renders alerts from API fixture in tests.
- Brand and corpus evidence links present; artifact count-only when applicable.
- Zero Accept/Reject/Generate controls in panel DOM.
</success_criteria>

<output>
After completion, create `.planning/phases/172-operational-evidence-ui-and-release-gate/172-01-SUMMARY.md`
</output>
