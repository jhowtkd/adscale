---
phase: 163-corpus-learning-proposals
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/human-quality/learning/proposals.ts
  - app/src/server/human-quality/learning/factual-alerts.ts
  - app/src/server/human-quality/calibration/types.ts
  - app/src/app/api/admin/quality/learning/proposals/[id]/accept/route.ts
  - app/src/app/api/admin/quality/learning/factual-alerts/route.ts
  - app/src/app/api/admin/quality/learning/factual-alerts/route.test.ts
  - app/tests/unit/human-quality/learning/proposals.test.ts
  - app/tests/unit/human-quality/learning/factual-alerts.test.ts
autonomous: true
requirements:
  - LEARN-04
  - LEARN-06
must_haves:
  truths:
    - "Accepting fixture-only proposals requires explicit operator acknowledgment"
    - "factual_issue slices meeting thresholds surface in owner factual-alerts API, never as proposals"
    - "Accept still creates approved corpus_quality calibration_rule with evidence refs"
  artifacts:
    - path: app/src/server/human-quality/learning/factual-alerts.ts
      provides: "Detect and list factual_issue alert slices"
    - path: app/src/app/api/admin/quality/learning/factual-alerts/route.ts
      provides: "Owner GET factual alerts"
  key_links:
    - from: app/src/app/api/admin/quality/learning/proposals/[id]/accept/route.ts
      to: app/src/server/human-quality/learning/proposals.ts
      via: "acknowledgeFixtureOnly in accept body"
      pattern: "acknowledgeFixtureOnly"
    - from: app/src/server/human-quality/learning/factual-alerts.ts
      to: app/src/server/human-quality/learning/aggregate.ts
      via: "shared slice bucketing without factual_issue skip"
      pattern: "factual_issue"
---

# Plan 163-02: Accept Hardening and Factual Alerts

<objective>
Complete LEARN-04 fixture acknowledgment on accept and LEARN-06 factual_issue admin alert surface.

Purpose: Accept flow and factual guard are partially implemented; design requires operator ack for fixture-only and alerts instead of prompt rules for factual issues.
Output: Hardened accept, factual-alerts module + API, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-corpus-learning-proposals/163-RESEARCH.md
@docs/superpowers/specs/2026-06-21-corpus-learning-loop-design.md
@app/src/server/human-quality/learning/proposals.ts
@app/src/server/human-quality/learning/aggregate.ts

<interfaces>
From Plan 163-01 (evidence shape):
```typescript
export interface ClientLearningProposalEvidence {
  corpusItemIds: string[];
  artifactIds?: string[];
  fixtureOnly?: boolean;
  stats: { count: number; meanSignedDelta: number | null; /* ... */ };
}
```

Existing accept signature:
```typescript
export async function acceptClientLearningProposal(input: {
  proposalId: string;
  reviewerUserId: string;
}): Promise<{ proposal: ClientLearningProposal; rule: CalibrationRule }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factual issue alert detection and API</name>
  <files>app/src/server/human-quality/learning/factual-alerts.ts, app/src/app/api/admin/quality/learning/factual-alerts/route.ts, app/tests/unit/human-quality/learning/factual-alerts.test.ts, app/src/app/api/admin/quality/learning/factual-alerts/route.test.ts</files>
  <behavior>
    - buildFactualIssueAlerts returns slices where primaryFailureReason is factual_issue AND count ≥ MIN_SLICE_SAMPLE AND |meanSignedDelta| ≥ 15
    - Alerts never create client_learning_proposals
    - GET /api/admin/quality/learning/factual-alerts returns { alerts: [...] } for platform owner
    - Supports workspaceId and clientProfileId query filters
  </behavior>
  <action>
    Create `factual-alerts.ts` reusing `buildClientLearningProposals` slice bucketing logic OR extract shared `buildSliceBuckets(rows)` helper used by aggregate + factual-alerts (minimal refactor — prefer shared private function in `aggregate.ts` exported as `buildLearningSliceBuckets`).

    For `factual_issue` only: apply same threshold checks as visual proposals but emit `FactualIssueAlert` objects `{ workspaceId, clientProfileId, sliceKey, evidenceRefs, rationale: 'factual_guard_review_required' }` — no DB persistence required for v1 (computed on read). Per LEARN-06 and design §7.

    Add `GET /api/admin/quality/learning/factual-alerts` with `requirePlatformOwner`, query schema matching proposals list filters.

    Wire optional call from `learning-proposal-aggregator` job as debug log only — do NOT persist alerts table unless needed.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/factual-alerts.test.ts src/app/api/admin/quality/learning/factual-alerts/route.test.ts</automated>
  </verify>
  <done>Owner can list factual_issue alert slices via dedicated API; no factual_issue proposals created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture acknowledgment on accept</name>
  <files>app/src/server/human-quality/learning/proposals.ts, app/src/app/api/admin/quality/learning/proposals/[id]/accept/route.ts, app/tests/unit/human-quality/learning/proposals.test.ts, app/src/app/api/admin/quality/learning/proposals/[id]/accept/route.test.ts</files>
  <behavior>
    - When evidenceRefs.fixtureOnly is true and acknowledgeFixtureOnly is not true, accept throws insufficient_evidence or fixture_ack_required
    - When acknowledgeFixtureOnly is true, accept proceeds and rule caveats include fixture-only marker
    - supportingSignalIds uses evidenceRefs.artifactIds when present
  </behavior>
  <action>
    Extend `acceptClientLearningProposal` input with `acknowledgeFixtureOnly?: boolean`. Per design §6.2 and research SUMMARY.

    If `proposal.evidenceRefs.fixtureOnly === true` and `!input.acknowledgeFixtureOnly`, throw `ClientLearningProposalError` with new code `fixture_ack_required`.

    Parse accept route body: `z.object({ acknowledgeFixtureOnly: z.boolean().optional() })` — default false.

    On accept for fixture-only: append caveat `"fixture_only_corpus"` to `calibration_rule.caveats` array.

    Ensure `supportingSignalIds` uses `proposal.evidenceRefs.artifactIds ?? []` (populated by Plan 163-01).

    Map `fixture_ack_required` → HTTP 422 in accept route.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/proposals.test.ts src/app/api/admin/quality/learning/proposals/\[id\]/accept/route.test.ts</automated>
  </verify>
  <done>Fixture-only proposals cannot accept without explicit acknowledgment; rules carry fixture caveat</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accept POST body | Untrusted — validate with Zod |
| Factual alerts GET | Owner-only — no workspace member access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-04 | E | accept route | mitigate | requirePlatformOwner; fixture ack prevents silent fixture rule promotion |
| T-163-05 | I | factual-alerts | mitigate | Read-only computed from DB evaluations; no user-supplied slice keys |
| T-163-06 | E | fixture_ack bypass | mitigate | Server-side check on evidenceRefs.fixtureOnly, not client flag alone |
</threat_model>

<verification>
- factual_issue never appears in `buildClientLearningProposals` output (existing test + factual-alerts test)
- Accept without ack fails for fixtureOnly proposals
</verification>

<success_criteria>
- LEARN-06: factual_issue surfaces via factual-alerts API only
- LEARN-04: accept requires fixture ack when applicable; creates corpus_quality rule with artifact refs
</success_criteria>

<output>
After completion, create `.planning/phases/163-corpus-learning-proposals/163-02-SUMMARY.md`
</output>
