---
phase: 163-corpus-learning-proposals
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - app/tests/unit/human-quality/learning/corpus-learning-loop.test.ts
  - .planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md
autonomous: false
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04
  - LEARN-05
  - LEARN-06
must_haves:
  truths:
    - "Vertical test proves eval data → proposal → accept → calibration_rule chain"
    - "All six LEARN requirements have automated verification mapping"
    - "Phase verification document marks requirements PASS with evidence"
  artifacts:
    - path: app/tests/unit/human-quality/learning/corpus-learning-loop.test.ts
      provides: "End-to-end service chain test"
    - path: .planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md
      provides: "Requirement sign-off"
  key_links:
    - from: app/tests/unit/human-quality/learning/corpus-learning-loop.test.ts
      to: app/src/server/human-quality/learning/generate.ts
      via: "generateAndPersistClientLearningProposals"
      pattern: "generateAndPersistClientLearningProposals"
    - from: app/tests/unit/human-quality/learning/corpus-learning-loop.test.ts
      to: app/src/server/human-quality/learning/proposals.ts
      via: "acceptClientLearningProposal"
      pattern: "acceptClientLearningProposal"
---

# Plan 163-03: Vertical Integration Proof and Phase Verification

<objective>
Prove the corpus learning loop end-to-end in one test harness and sign off LEARN-01..06 in 163-VERIFICATION.md.

Purpose: Partial implementation lacks chained proof; milestone requires traceable verification before Phase 164 prompt application.
Output: Vertical slice test, updated validation/verification docs, human checkpoint for staging smoke.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-corpus-learning-proposals/163-VALIDATION.md
@.planning/REQUIREMENTS.md
@app/tests/unit/human-quality/learning/aggregate.test.ts
@app/tests/unit/human-quality/learning/proposals.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corpus learning loop vertical slice test</name>
  <files>app/tests/unit/human-quality/learning/corpus-learning-loop.test.ts</files>
  <behavior>
    - Given 3 evaluated visual_overload rows for one clientProfile, buildClientLearningProposals returns one proposal
    - generateAndPersistClientLearningProposals persists proposal when mocks allow
    - acceptClientLearningProposal creates corpus_quality rule with matching failure reason prefix
    - rejectClientLearningProposal sets cooldown; subsequent generate skips slice
    - factual_issue rows produce alert via buildFactualIssueAlerts, zero proposals
  </behavior>
  <action>
    Create `corpus-learning-loop.test.ts` orchestrating real `buildClientLearningProposals`, `generateAndPersistClientLearningProposals`, `acceptClientLearningProposal`, `rejectClientLearningProposal`, and `buildFactualIssueAlerts` with coordinated repository mocks (same pattern as `generate.test.ts` + `proposals.test.ts`).

    Single `describe('corpus learning loop')` with ordered scenarios — not separate duplicated threshold tests.

  Cover all LEARN requirements in one file with `it` descriptions referencing requirement IDs.

  Do NOT add Playwright or live DB — follow project integration test conventions (mocked db).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/corpus-learning-loop.test.ts</automated>
  </verify>
  <done>Vertical slice test passes; documents full loop behavior</done>
</task>

<task type="auto">
  <name>Task 2: Update validation and verification docs</name>
  <files>.planning/phases/163-corpus-learning-proposals/163-VALIDATION.md, .planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md</files>
  <action>
    Create `163-VERIFICATION.md` modeled on Phase 162/125 verification format:
    - Table: LEARN-01..06 | PASS/FAIL | evidence (test file + behavior)
    - Link to design spec success criteria §15 items 2–3 (proposal + accept; prompt application is Phase 164)

    Update `163-VALIDATION.md`:
    - Mark Wave 0 items complete
    - Set `nyquist_compliant: true` when all automated commands green
    - Update per-task status to green for completed tasks

    Update `.planning/REQUIREMENTS.md` traceability: set LEARN-01..06 status to Complete only if verification PASS (otherwise leave Pending with note "blocked on 163-03").
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/ src/app/api/admin/quality/learning/</automated>
  </verify>
  <done>163-VERIFICATION.md exists with PASS rows backed by test evidence</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Staging smoke — owner learning APIs</name>
  <files>.planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md</files>
  <action>Human verifies learning APIs against staging or local dev with real evaluated corpus data. Record PASS/FAIL per LEARN requirement in 163-VERIFICATION.md after smoke.</action>
  <what-built>Full LEARN gap closure: hardened aggregator, factual alerts, fixture ack accept, vertical tests</what-built>
  <how-to-verify>
    1. Deploy or run app against staging DB with evaluated corpus rows (≥3 per slice for one clientProfile)
    2. As platform owner: `POST /api/admin/quality/learning/proposals/generate` — confirm `generated` ≥ 1
    3. `GET /api/admin/quality/learning/proposals?status=proposed&clientProfileId={id}` — confirm proposal with evidenceRefs.stats
    4. `GET /api/admin/quality/learning/factual-alerts` — confirm factual_issue slices appear if present in data
    5. `POST .../proposals/{id}/accept` with `{ "acknowledgeFixtureOnly": true }` when fixtureOnly — confirm calibration_rule created
    6. `POST .../proposals/{id}/reject` with reason — confirm cooldownUntil ~30 days out
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md && grep -q 'LEARN-01' .planning/phases/163-corpus-learning-proposals/163-VERIFICATION.md</automated>
  </verify>
  <done>Human confirms staging smoke passed; 163-VERIFICATION.md updated with manual smoke notes</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging smoke | Human verifies real DB + auth — complements mocked vertical test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-07 | D | Staging APIs | accept | Owner-only routes; smoke confirms 403 for non-owner |
</threat_model>

<verification>
- `cd app && npm test -- --run tests/unit/human-quality/learning/ src/app/api/admin/quality/learning/` exits 0
- 163-VERIFICATION.md shows PASS for LEARN-01..06
</verification>

<success_criteria>
- Corpus learning loop proven in vertical test
- Human staging smoke approved
- REQUIREMENTS LEARN-01..06 marked Complete in traceability
</success_criteria>

<output>
After completion, create `.planning/phases/163-corpus-learning-proposals/163-03-SUMMARY.md`
</output>
