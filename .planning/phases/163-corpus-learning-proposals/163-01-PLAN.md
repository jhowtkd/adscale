---
phase: 163-corpus-learning-proposals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/repositories/client-learning-proposal.ts
  - app/src/server/repositories/calibration-rule.ts
  - app/src/server/repositories/human-quality-corpus.ts
  - app/src/server/human-quality/calibration/types.ts
  - app/src/server/human-quality/learning/aggregate.ts
  - app/src/server/human-quality/learning/generate.ts
  - app/tests/unit/human-quality/learning/aggregate.test.ts
  - app/tests/unit/human-quality/learning/generate.test.ts
  - app/tests/unit/human-quality/learning/client-learning-proposal-repository.test.ts
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-05
must_haves:
  truths:
    - "Rejected slices cannot re-propose until cooldown expires"
    - "Slices with active approved corpus_quality rule for same failure reason are skipped unless 3+ post-approval evals"
    - "Proposals include artifactIds and fixtureOnly in evidenceRefs"
  artifacts:
    - path: app/src/server/human-quality/learning/generate.ts
      provides: "Cooldown + approved-rule gates before insert"
    - path: app/src/server/human-quality/learning/aggregate.ts
      provides: "artifactIds and fixtureOnly in evidenceRefs"
  key_links:
    - from: app/src/server/human-quality/learning/generate.ts
      to: app/src/server/repositories/client-learning-proposal.ts
      via: "findSliceInCooldown before insertClientLearningProposal"
      pattern: "findSliceInCooldown|cooldownUntil"
    - from: app/src/server/human-quality/learning/aggregate.ts
      to: app/src/server/repositories/human-quality-corpus.ts
      via: "sourceLabel on EvaluatedCorpusRow"
      pattern: "sourceLabel|fixtureOnly"
---

# Plan 163-01: Aggregator Hardening

<objective>
Harden the corpus learning aggregator so proposals respect reject cooldowns, approved-rule gates, and complete evidence refs (artifactIds + fixtureOnly).

Purpose: LEARN-01/02/05 gaps on `feat/corpus-learning-loop` — core logic exists but misses design-spec edge cases.
Output: Repository helpers, enriched aggregate input, generate guards, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-corpus-learning-proposals/163-RESEARCH.md
@docs/superpowers/specs/2026-06-21-corpus-learning-loop-design.md
@app/src/server/human-quality/learning/aggregate.ts
@app/src/server/human-quality/learning/generate.ts
@app/src/server/repositories/client-learning-proposal.ts

<interfaces>
From `app/src/server/human-quality/calibration/types.ts`:
```typescript
export interface EvaluatedCorpusRow {
  item: HumanQualityCorpusItem;
  evaluation: HumanQualityEvaluation;
  sourceLabel?: HumanQualitySourceLabel; // ADD in this plan
}

export interface ClientLearningProposalEvidence {
  corpusItemIds: string[];
  artifactIds?: string[];
  fixtureOnly?: boolean;
  stats: { count: number; meanSignedDelta: number | null; /* ... */ };
}
```

From `app/src/server/repositories/client-learning-proposal.ts` (existing):
```typescript
export async function findActiveProposalBySlice(
  workspaceId: string, clientProfileId: string, sliceKey: string
): Promise<ClientLearningProposal | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository cooldown and approved-rule helpers</name>
  <files>app/src/server/repositories/client-learning-proposal.ts, app/src/server/repositories/calibration-rule.ts, app/tests/unit/human-quality/learning/client-learning-proposal-repository.test.ts</files>
  <behavior>
    - findSliceInCooldown returns rejected row when cooldownUntil > now for same (workspaceId, clientProfileId, sliceKey)
    - findSliceInCooldown returns null when cooldown expired or no rejected row
    - hasApprovedCorpusQualityRuleForFailure returns true when approved rule rationale prefix matches primaryFailureReason
    - listApprovedCorpusQualityRulesForClient filters by workspaceId + clientProfileId + failure reason prefix
  </behavior>
  <action>
    Add `findSliceInCooldown(workspaceId, clientProfileId, sliceKey)` — query `client_learning_proposals` where `status = 'rejected'` AND `cooldown_until > now()` for the slice (most recent). Per LEARN-05.

    Add `getApprovedCorpusQualityRuleForFailure({ workspaceId, clientProfileId, primaryFailureReason })` in `calibration-rule.ts` — match rules where `category = 'corpus_quality'`, `status = 'approved'`, and `rationale` starts with `{primaryFailureReason}:` (same prefix convention as `cross-client.ts`).

    Create `client-learning-proposal-repository.test.ts` with mocked `db` (follow `human-quality-repository.test.ts` pattern). Do NOT duplicate migration or table creation.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/client-learning-proposal-repository.test.ts</automated>
  </verify>
  <done>Cooldown and approved-rule repository helpers exist with passing unit tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich aggregate evidence and generate guards</name>
  <files>app/src/server/repositories/human-quality-corpus.ts, app/src/server/human-quality/calibration/types.ts, app/src/server/human-quality/learning/aggregate.ts, app/src/server/human-quality/learning/generate.ts</files>
  <behavior>
    - EvaluatedCorpusRow includes sourceLabel from candidates join
    - evidenceRefs includes artifactIds from feedback artifacts (or evaluation-linked artifact ids when available)
    - evidenceRefs.fixtureOnly is true when every slice member has sourceLabel synthetic_fixture
    - generate skips insert when findSliceInCooldown returns row
    - generate skips when approved rule exists unless ≥3 evaluations have createdAt after rule.approvedAt
  </behavior>
  <action>
    Extend `listEvaluatedCorpusWithEvaluations` with optional left join to `human_quality_corpus_candidates` on `(workspaceId, derivationId, corpusVersion)` to populate `sourceLabel` on each row. Per design §6.2 fixture gate.

    In `buildClientLearningProposals`: collect `artifactIds` by joining `human_quality_feedback_artifacts` on `corpusItemId` (add lightweight repo helper `listFeedbackArtifactIdsByCorpusItemIds` if needed). Set `fixtureOnly` when all slice members have `sourceLabel === 'synthetic_fixture'`.

    In `generateAndPersistClientLearningProposals` before `insertClientLearningProposal`:
    1. Skip if `findActiveProposalBySlice` returns row (existing).
    2. Skip if `findSliceInCooldown` returns row (LEARN-05).
    3. Skip if approved rule exists for `primaryFailureReason` UNLESS count of slice evaluations with `createdAt > rule.approvedAt` ≥ `MIN_SLICE_SAMPLE` (design §6.2 post-approval exception).

    Do NOT change cross-client detection in generate route — out of Phase 163 scope.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/aggregate.test.ts tests/unit/human-quality/learning/generate.test.ts</automated>
  </verify>
  <done>Aggregate emits complete evidenceRefs; generate enforces cooldown and approved-rule gates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for new aggregator behaviors</name>
  <files>app/tests/unit/human-quality/learning/aggregate.test.ts, app/tests/unit/human-quality/learning/generate.test.ts</files>
  <behavior>
    - Aggregate sets fixtureOnly true for all-synthetic slice
    - Aggregate includes artifactIds when artifacts present
    - Generate skips when cooldown active
    - Generate skips when approved rule exists without post-approval evals
    - Generate allows when 3+ post-approval evals exist
  </behavior>
  <action>
    Extend existing test files (do not create parallel duplicates). Use `makeRow` helpers; add `sourceLabel` override on rows. Mock new repository functions in `generate.test.ts`.

    Per LEARN-01: verify proposal still requires ≥3 evals and |delta|≥15 (existing tests must remain green).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/human-quality/learning/</automated>
  </verify>
  <done>All learning unit tests pass including new cooldown, approved-rule, and fixtureOnly cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregator input | Evaluated corpus rows from DB — trusted after owner evaluation |
| Proposal persistence | Owner-only downstream APIs mutate proposals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | T | generate.ts insert | mitigate | Cooldown + dedupe prevent proposal spam / operator harassment |
| T-163-02 | I | evidenceRefs.fixtureOnly | mitigate | Compute from sourceLabel join, not client-supplied JSON |
| T-163-03 | E | approved-rule skip | accept | Logic-only; no new external input surface |
</threat_model>

<verification>
- `cd app && npm test -- --run tests/unit/human-quality/learning/` exits 0
- No regression in existing aggregate threshold tests
</verification>

<success_criteria>
- Cooldown blocks re-proposal for 30 days after reject
- Approved corpus_quality rules block duplicate proposals unless post-approval sample met
- Proposals carry artifactIds and fixtureOnly in evidenceRefs
</success_criteria>

<output>
After completion, create `.planning/phases/163-corpus-learning-proposals/163-01-SUMMARY.md`
</output>
