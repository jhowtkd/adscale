# Phase 189-01 Plan — v13.6 Ship Gate

## Goal

Close the v13.6 ship-gate evidence gap without hiding staging-only verification behind automated tests.

## Tasks

1. Add Playwright coverage for first action-card confirmation.
   - Use authenticated `/assistant?threadId=...` shell.
   - Mock only the assistant thread/action endpoints needed to render and confirm a pending action card.
   - Assert the confirm route is called and the UI refetches into a non-pending action state.

2. Normalize GSD status.
   - `ROADMAP.md` must show v13.6 as active/not shippable until Phase 189 evidence is recorded.
   - `STATE.md`, `REQUIREMENTS.md` and audit must point QA-04 to Phase 189.

3. Verify locally.
   - Run the guided assistant Playwright smoke.
   - Run the guided targeted Vitest suite.
   - Run `npm run build`.

4. Record verification.
   - Create `189-VERIFICATION.md`.
   - Update audit/requirements/state according to evidence.

## Exit Criteria

- QA-04 automated browser-confirm coverage is green.
- Any remaining live provider/staging validation is named as tech debt.
- v13.6 status is truthful across planning files.
