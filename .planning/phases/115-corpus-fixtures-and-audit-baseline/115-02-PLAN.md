---
phase: 115-corpus-fixtures-and-audit-baseline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/corpus-fixtures.ts
  - app/tests/unit/ai/corpus-fixtures.test.ts
autonomous: true
requirements:
  - FIXT-01
must_haves:
  truths:
    - "Each audit failure archetype has at least one corpus-linked fixture with derivation id reference"
    - "Archetype fixtures use fictional-safe contracts derived from canonical campaigns — not raw customer copy from PNGs"
    - "Five archetypes covered: invented_factual_entity, visual_overload, generic_template_aesthetic, format_campaign_drift, restyling_factual_contamination"
  artifacts:
    - path: app/src/server/ai/corpus-fixtures.ts
      provides: CORPUS_ARCHETYPE_FIXTURES, CorpusArchetype type
      exports:
        - CORPUS_ARCHETYPE_FIXTURES
        - CorpusArchetype
        - CorpusArchetypeFixture
    - path: app/tests/unit/ai/corpus-fixtures.test.ts
      provides: Archetype catalog integrity
  key_links:
    - from: app/src/server/ai/corpus-fixtures.ts
      to: app/src/server/ai/creative-corpus.ts
      via: canonicalSlug + corpusRefId
      pattern: canonicalSlug
    - from: app/src/server/ai/corpus-fixtures.ts
      to: app/src/server/ai/quality-fixtures.ts
      via: shared QualityFixture shape or extended interface
      pattern: expectedVerdict
---

# Plan 115-02: Audit Archetype Fixtures

<objective>
Add corpus-derived quality fixtures for each failure archetype observed in the June audit (FIXT-01).
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/115-corpus-fixtures-and-audit-baseline/115-RESEARCH.md
@app/src/server/ai/creative-corpus.ts
@app/src/server/ai/quality-fixtures.ts
@app/src/server/ai/prompt-builder.test-fixtures.ts
</context>

<tasks>

<task id="115-02-01" type="auto" tdd="true">
  <name>Define CorpusArchetype types and fixture scaffold</name>
  <files>app/src/server/ai/corpus-fixtures.ts, app/tests/unit/ai/corpus-fixtures.test.ts</files>
  <action>
Create `corpus-fixtures.ts` separate from `quality-fixtures.ts` to preserve existing six-fixture tests.

Export:
- `CorpusArchetype` union: `invented_factual_entity` | `visual_overload` | `generic_template_aesthetic` | `format_campaign_drift` | `restyling_factual_contamination`
- `CorpusArchetypeFixture` extending fixture shape with: `corpusRefIds: string[]` (8-char prefixes), `canonicalSlug`, `renderTier`, `archetype`, plus standard `contract`, `rawQaModelOutput`, `expectedHardFailureCodes`, `expectedVerdict` (target post-v12.3 behavior), `baselineVerdict` (current wrongful behavior — typically `acceptable` or `improvable`)

Map archetypes to primary corpus refs per research:
- invented_factual_entity → `27069645`, `a753e357`, `20c0cc8c`
- visual_overload → `8a2bebf9`, `c32bf93c`
- generic_template_aesthetic → `3dffb311`, `47541eb5`
- format_campaign_drift → `538246da`, `27069645`
- restyling_factual_contamination → `d7d9d323`, `a5f65b85`

Use `prompt-builder.test-fixtures` or new corpus-specific contract builders with NR1/education fictional copy — never embed audit PNG bytes.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-fixtures.test.ts</automated>
  </verify>
  <done>Five archetypes declared with corpus id links.</done>
</task>

<task id="115-02-02" type="auto">
  <name>Populate synthetic QA outputs mirroring audit observations</name>
  <files>app/src/server/ai/corpus-fixtures.ts, app/tests/unit/ai/corpus-fixtures.test.ts</files>
  <action>
For each archetype fixture, craft `rawQaModelOutput` that mirrors what a vision QA model would return for the audited failure:

- invented_factual_entity: `creativeRisk` or `briefMatch` failed with note citing invented athlete/team (e.g. Cantona, Manchester United) not in `allowedEntities`
- visual_overload: `creativeRisk` failed — more than three competing information zones, card grid dominates
- generic_template_aesthetic: `creativeRisk` warning/failed — premium tech neon template, no campaign-specific idea
- format_campaign_drift: `briefMatch` + `formatFit` failed — output is different campaign narrative
- restyling_factual_contamination: `styleFidelity` failed — reference imported people/uniforms

Set `expectedHardFailureCodes` to **target** codes (may include codes not yet in taxonomy — document as forward references for Phase 120, or use closest existing codes with `expectedHardFailureCodes` commented in test).

Set `expectedVerdict: "invalid"` for all five.

Integrity tests: 5 archetypes, unique ids, each has ≥1 corpusRefId present in CORPUS_MANIFEST_INDEX, preview and final tiers each represented at least once across fixtures.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-fixtures.test.ts</automated>
  </verify>
  <done>FIXT-01 satisfied — each audit archetype reproducible as fixture.</done>
</task>

</tasks>

<verification>
`cd app && npm test -- tests/unit/ai/corpus-fixtures.test.ts` — 5 archetypes, all linked to manifest ids.
</verification>

<success_criteria>
- FIXT-01: invented entity, overload, generic template, format drift, restyling contamination each have fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/115-corpus-fixtures-and-audit-baseline/115-02-SUMMARY.md`
</output>
