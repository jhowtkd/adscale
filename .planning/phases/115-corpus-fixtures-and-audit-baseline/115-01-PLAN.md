---
phase: 115-corpus-fixtures-and-audit-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/creative-corpus.ts
  - app/tests/fixtures/creative-corpus/manifest-index.json
  - app/tests/unit/ai/creative-corpus.test.ts
autonomous: true
requirements:
  - FIXT-02
  - FIXT-03
must_haves:
  truths:
    - "Four canonical campaigns (Smoke, Nova campanha, Teste 3/CENBRAP NR1, Teste campanha/Master NR1) are registered with allowed entities, modes, and formats"
    - "Every manifest export is indexed by derivation id with preview vs final tier"
    - "Corpus module is CI-safe — references manifest metadata only, no customer secrets or absolute paths"
  artifacts:
    - path: app/src/server/ai/creative-corpus.ts
      provides: CANONICAL_CAMPAIGNS, CorpusManifestEntry, render tier types
      exports:
        - CANONICAL_CAMPAIGNS
        - CORPUS_MANIFEST_INDEX
        - CorpusRenderTier
        - CanonicalCampaignSlug
    - path: app/tests/fixtures/creative-corpus/manifest-index.json
      provides: Slim index from app/exports/render-creatives/manifest.json
    - path: app/tests/unit/ai/creative-corpus.test.ts
      provides: Catalog integrity tests
  key_links:
    - from: app/src/server/ai/creative-corpus.ts
      to: app/tests/fixtures/creative-corpus/manifest-index.json
      via: import or build-time sync
      pattern: manifest-index
---

# Plan 115-01: Canonical Campaign Registry and Manifest Index

<objective>
Freeze audited export metadata into a typed corpus catalog with canonical campaigns and preview/final distinction (FIXT-02, FIXT-03).
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/115-corpus-fixtures-and-audit-baseline/115-RESEARCH.md
@.planning/phases/115-corpus-fixtures-and-audit-baseline/115-VALIDATION.md
@app/exports/render-creatives/manifest.json
@app/src/server/ai/quality-fixtures.ts
</context>

<tasks>

<task id="115-01-01" type="auto" tdd="true">
  <name>Define corpus types and canonical campaign registry</name>
  <files>app/src/server/ai/creative-corpus.ts, app/tests/unit/ai/creative-corpus.test.ts</files>
  <action>
Create `creative-corpus.ts` exporting:

- `CorpusRenderTier = "preview" | "final"` (maps `is_preview` from manifest)
- `CanonicalCampaignSlug` union: `smoke` | `nova-campanha` | `teste-3-nr1` | `teste-campanha-nr1`
- `CanonicalCampaign` with: `slug`, `displayNames: string[]` (manifest campaign strings), `allowedEntities: { people, brands, products, claims }`, `typicalModes`, `typicalFormats`
- `CANONICAL_CAMPAIGNS: Record&lt;CanonicalCampaignSlug, CanonicalCampaign&gt;` populated from audit:
  - smoke: Smoke v11.6 CQA-02 — ADScale tech smoke, no athletes
  - nova-campanha: Nova campanha — education/professors theme
  - teste-3-nr1: Teste 3, CENBRAP NR1 — NR1 compliance cards
  - teste-campanha-nr1: Teste campanha, Master NR1 — dense module grids

`allowedEntities` lists factual tokens that MAY appear; explicitly exclude Cantona, Manchester United, Adidas, Jackie Groenen unless added to a campaign's allowlist.

Add integrity tests: four slugs exist, each has non-empty displayNames and allowedEntities, no `/Users/` strings in module.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-corpus.test.ts</automated>
  </verify>
  <done>Canonical campaign registry typed and tested.</done>
</task>

<task id="115-01-02" type="auto">
  <name>Build manifest index with preview/final tier</name>
  <files>app/tests/fixtures/creative-corpus/manifest-index.json, app/src/server/ai/creative-corpus.ts, app/tests/unit/ai/creative-corpus.test.ts</files>
  <action>
Generate `manifest-index.json` from `app/exports/render-creatives/manifest.json` — slim fields only: `id`, `idPrefix` (first 8 chars), `fileName`, `campaign`, `generation_mode`, `format`, `is_preview`, `renderTier`, `canonicalSlug` (resolved via displayNames), optional `auditArchetype` (null in this plan).

Export `CORPUS_MANIFEST_INDEX` from `creative-corpus.ts` (import JSON or inline typed array).

Tests assert:
- 34 entries match manifest length
- Exactly 10 `renderTier: "preview"`, 24 `"final"`
- Every entry resolves to a `canonicalSlug` or explicit `unmapped` bucket with test documenting why
- Preview entries are all 1:1 per audit
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-corpus.test.ts</automated>
  </verify>
  <done>Full corpus indexed with preview/final distinction (FIXT-03).</done>
</task>

</tasks>

<verification>
`cd app && npm test -- tests/unit/ai/creative-corpus.test.ts` passes; 34 manifest entries indexed.
</verification>

<success_criteria>
- FIXT-02: four canonical campaigns with allowed entities and modes
- FIXT-03: preview vs final distinguished on every indexed export
</success_criteria>

<output>
After completion, create `.planning/phases/115-corpus-fixtures-and-audit-baseline/115-01-SUMMARY.md`
</output>
