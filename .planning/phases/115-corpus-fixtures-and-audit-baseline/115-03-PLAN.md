---
phase: 115-corpus-fixtures-and-audit-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - "115-01"
  - "115-02"
files_modified:
  - app/tests/unit/ai/corpus-baseline.test.ts
autonomous: true
requirements:
  - FIXT-04
must_haves:
  truths:
    - "Automated tests prove current gate classifies corpus archetype fixtures as acceptable/improvable when they should be invalid"
    - "Baseline gap is documented per archetype with explicit current vs expected verdict"
    - "Existing v11.5 six-fixture pipeline regression remains green"
  artifacts:
    - path: app/tests/unit/ai/corpus-baseline.test.ts
      provides: Red baseline gate matrix for corpus archetypes
      contains: CORPUS_ARCHETYPE_FIXTURES
  key_links:
    - from: app/tests/unit/ai/corpus-baseline.test.ts
      to: app/src/server/ai/creative-quality-gate.ts
      via: classifyCreativeQualityGate + deriveQualityVerdict
      pattern: deriveQualityVerdict
    - from: app/tests/unit/ai/corpus-baseline.test.ts
      to: app/src/server/ai/corpus-fixtures.ts
      via: CORPUS_ARCHETYPE_FIXTURES
      pattern: CORPUS_ARCHETYPE_FIXTURES
---

# Plan 115-03: Red Baseline Gate Tests

<objective>
Prove with failing tests that the current quality gate wrongly approves corpus failure archetypes (FIXT-04). These tests document the gap until Phases 120+ harden the gate.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/115-corpus-fixtures-and-audit-baseline/115-RESEARCH.md
@app/src/server/ai/corpus-fixtures.ts
@app/src/server/ai/creative-qa.ts
@app/src/server/ai/creative-quality-gate.ts
@app/tests/unit/ai/quality-fixture-pipeline.test.ts
</context>

<tasks>

<task id="115-03-01" type="auto" tdd="true">
  <name>Baseline-red gate matrix for corpus archetypes</name>
  <files>app/tests/unit/ai/corpus-baseline.test.ts</files>
  <action>
Create `corpus-baseline.test.ts` with `describe.each(CORPUS_ARCHETYPE_FIXTURES)`.

Per fixture:
1. Normalize QA via `normalizeCreativeQaResult(fixture.rawQaModelOutput)`
2. `classifyCreativeQualityGate({ contract, checklist: qa.checklist })`
3. `deriveQualityVerdict({ hardFailures: gate.hardFailures, qualityScore: 85, checklist: qa.checklist })`

**FIXT-04 red baseline pattern** — use Vitest `it.fails` (or `test.fails`) titled `baseline-red: {archetype} should be invalid but gate approves`:

```typescript
it.fails(`baseline-red: ${fixture.id} — gate must reject ${fixture.archetype}`, () => {
  expect(verdict).toBe("invalid");
  expect(fixture.expectedHardFailureCodes.every(code => hardFailures includes code)).toBe(true);
});
```

Also add passing **documentation tests** (normal `it`) that record current wrongful behavior:

```typescript
it(`baseline-snapshot: ${fixture.id} current verdict`, () => {
  expect(verdict).toBe(fixture.baselineVerdict); // e.g. "acceptable"
});
```

This keeps CI green while `it.fails` documents intent — when gate is fixed in Phase 120, flip `it.fails` → `it` and remove snapshot tests.

No OpenAI mocks. No image bytes.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts</automated>
  </verify>
  <done>Red baseline tests exist for all five archetypes.</done>
</task>

<task id="115-03-02" type="auto">
  <name>Regression guard and corpus coverage report</name>
  <files>app/tests/unit/ai/corpus-baseline.test.ts</files>
  <action>
Add test that every `CorpusArchetype` value has at least one fixture in baseline suite.

Add test that primary audit corpus ids (`27069645`, `538246da`, `d7d9d323`) appear in at least one fixture's `corpusRefIds`.

Run alongside existing pipeline test to ensure no regression:

`npm test -- tests/unit/ai/quality-fixture-pipeline.test.ts tests/unit/ai/corpus-baseline.test.ts`

Optional: export `BASELINE_GAP_COUNT` constant from test file for Phase 123 validation script reference.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts tests/unit/ai/quality-fixture-pipeline.test.ts</automated>
  </verify>
  <done>FIXT-04 complete — wrongful approval documented and guarded.</done>
</task>

</tasks>

<verification>
- `corpus-baseline.test.ts` passes (with `it.fails` entries failing as expected)
- `quality-fixture-pipeline.test.ts` still green
</verification>

<success_criteria>
- FIXT-04: tests demonstrate current gate approves corpus-failure archetypes inappropriately
- Clear migration path: Phase 120 converts `it.fails` to passing `it`
</success_criteria>

<output>
After completion, create `.planning/phases/115-corpus-fixtures-and-audit-baseline/115-03-SUMMARY.md`
</output>
