---
phase: 131-learning-impact-measurement
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/drizzle/0045_derivation_output_learning_application.sql
  - app/drizzle/meta/_journal.json
  - app/src/server/db/schema.ts
  - app/src/server/human-quality/corpus.ts
  - app/src/server/human-quality/application-schema.ts
  - app/src/server/repositories/derivation.ts
  - app/src/app/api/campaigns/[id]/derivations/route.ts
  - app/src/app/api/derivations/[id]/regenerate/route.ts
  - app/tests/unit/human-quality/impact/application-schema.test.ts
  - app/src/app/api/campaigns/[id]/derivations/route.test.ts
autonomous: true
requirements:
  - IMPACT-01
must_haves:
  truths:
    - "Every new derivation can persist a bounded output-learning application snapshot in Postgres"
    - "Legacy derivations without application metadata resolve to learningApplied=false with resolution not_recorded"
    - "Regenerate inherits parent outputLearningApplication unless POST overrides"
    - "Batch derivation POST applies identical snapshot to every job in the batch"
  artifacts:
    - path: app/src/server/human-quality/application-schema.ts
      provides: Zod validation and sanitization for OutputLearningApplicationSnapshot
      exports: ["outputLearningApplicationSchema", "sanitizeOutputLearningApplication"]
    - path: app/drizzle/0045_derivation_output_learning_application.sql
      provides: derivations.output_learning_application jsonb column
    - path: app/src/server/human-quality/corpus.ts
      provides: OutputLearningApplicationSnapshot type on HumanQualityQualitySnapshot
    - path: app/src/app/api/campaigns/[id]/derivations/route.ts
      provides: POST accepts optional bounded outputLearningApplication
  key_links:
    - from: app/src/app/api/campaigns/[id]/derivations/route.ts
      to: derivation.outputLearningApplication
      via: Zod parse + sanitize + createDerivation per batch job
      pattern: "outputLearningApplication"
    - from: app/src/app/api/derivations/[id]/regenerate/route.ts
      to: parent.outputLearningApplication
      via: inherit parent snapshot on child derivation create
      pattern: "outputLearningApplication|parent"
---

# Plan 131-01: Application Snapshot Schema and API Persistence (IMPACT-01)

<objective>
Define bounded `OutputLearningApplicationSnapshot` contract and persist it on derivation create/regenerate via Postgres jsonb — enabling auditable attribution before accept-flow wiring and corpus freeze (IMPACT-01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-learning-impact-measurement/131-RESEARCH.md
@.planning/phases/131-learning-impact-measurement/131-VALIDATION.md
@app/src/server/human-quality/corpus.ts
@app/src/server/output-learning/safety/types.ts
@app/src/app/api/campaigns/[id]/derivations/route.ts
@app/src/app/api/derivations/[id]/regenerate/route.ts

<interfaces>
From `output-learning/safety/types.ts`:
```typescript
export interface AppliedLearningTrace {
  traceId: string;
  learningsSource: "postgres";
  safetyVersion: string;
  algorithmVersion: string;
  entries: AppliedLearningTraceEntry[];
  blockedFields: BlockedPrefillField[];
  avoidPatternHints: Array<{ learningId: string; pattern: string; confidence: string }>;
}
```

From `corpus.ts` (extend):
```typescript
export interface HumanQualityQualitySnapshot {
  qualityScore?: number | null;
  // ... existing fields
  outputLearningApplication?: OutputLearningApplicationSnapshot;
}
```

From `repositories/derivation.ts`:
```typescript
export async function createDerivation(data: CreateDerivationInput)
```
</interfaces>
</context>

<tasks>

<task id="131-01-01" type="auto" tdd="true">
  <name>Define bounded application snapshot contract</name>
  <files>app/src/server/human-quality/application-schema.ts, app/src/server/human-quality/corpus.ts, app/tests/unit/human-quality/impact/application-schema.test.ts</files>
  <behavior>
    - Valid snapshot with applied=true, traceId, recommendationId, primaryVariableKey passes Zod
    - Forbidden keys (prompt, signedUrl, modelResponse, entries with unbounded text) rejected
    - sanitizeOutputLearningApplication strips forbidden keys via validatePrivacySafePayload
    - resolution enum: recorded | not_recorded | recommendation_only
    - learningsSource must be "postgres"
    - Default legacy snapshot: { schemaVersion: 1, applied: false, resolution: "not_recorded", learningsSource: "postgres" }
  </behavior>
  <action>
    Add `OutputLearningApplicationSnapshot` interface per RESEARCH Pattern 1 to `corpus.ts` and extend `HumanQualityQualitySnapshot`. Create `application-schema.ts` with Zod schema `outputLearningApplicationSchema`, `sanitizeOutputLearningApplication`, and `buildApplicationSnapshotFromAccept` helper that maps recommendation accept payload (traceId, recommendationId, primaryVariableKey, algorithmVersion, safetyVersion) into bounded snapshot with `applied: true, resolution: "recorded"`. Do NOT store full `AppliedLearningTrace.entries` — IDs and keys only. Export `OUTPUT_LEARNING_APPLICATION_SCHEMA_VERSION = 1`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/impact/application-schema.test.ts</automated>
  </verify>
  <done>Bounded application snapshot type, Zod validation, and sanitization exist with forbidden-key rejection.</done>
</task>

<task id="131-01-02" type="auto" tdd="true">
  <name>Persist application on derivation create and regenerate</name>
  <files>app/drizzle/0045_derivation_output_learning_application.sql, app/drizzle/meta/_journal.json, app/src/server/db/schema.ts, app/src/server/repositories/derivation.ts, app/src/app/api/campaigns/[id]/derivations/route.ts, app/src/app/api/derivations/[id]/regenerate/route.ts, app/src/app/api/campaigns/[id]/derivations/route.test.ts</files>
  <behavior>
    - POST derivations with valid outputLearningApplication persists jsonb on all batch jobs
    - POST without application field stores null; reads as not_recorded
    - Invalid application body returns 400 with validation error
    - Regenerate copies parent outputLearningApplication to child unless POST overrides
    - Server rejects traceId that does not match /^ol-/ or learningsSource !== postgres
  </behavior>
  <action>
    Add migration `0045_derivation_output_learning_application.sql` adding nullable `output_learning_application jsonb` to `derivations`. Update Drizzle schema and `CreateDerivationInput` + `createDerivation` to accept optional sanitized snapshot. In `derivations/route.ts`: parse body with Zod alongside preview/styleAssetId; validate and sanitize application; pass identical snapshot to every job in batch (per RESEARCH Open Questions RESOLVED #2). In `regenerate/route.ts`: read parent `outputLearningApplication` and pass to `createDerivation` on child — inherit unless body overrides (per RESEARCH Open Questions RESOLVED #1). Add route tests mocking createDerivation verifying application threading. Do NOT add prompts or trace entries to column.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/campaigns/\[id\]/derivations/route.test.ts -t "outputLearningApplication"</automated>
  </verify>
  <done>Derivation create and regenerate persist bounded application snapshot; invalid payloads rejected at API boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → derivations POST | Client may submit application snapshot — server must validate bounded schema |
| Regenerate | Child inherits parent attribution — no client override without explicit body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | I | outputLearningApplication jsonb | mitigate | Zod + stripForbiddenPayloadKeys; deny full AppliedLearningTrace entries |
| T-131-02 | T | derivations POST application field | mitigate | Server validates traceId shape and learningsSource=postgres; reject unbounded payloads |
| T-131-03 | E | Cross-workspace attribution | accept | Application has no workspaceId; corpus join enforces workspace isolation at selection |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/impact/application-schema.test.ts`
- `cd app && npm test -- src/app/api/campaigns/\[id\]/derivations/route.test.ts -t "outputLearningApplication"`
</verification>

<success_criteria>
- IMPACT-01 (persistence layer): Bounded application snapshot stored on derivations via API; legacy rows honestly report not_recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/131-learning-impact-measurement/131-01-SUMMARY.md`.
</output>
