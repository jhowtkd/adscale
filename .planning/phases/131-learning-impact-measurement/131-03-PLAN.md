---
phase: 131-learning-impact-measurement
plan: "03"
type: execute
wave: 3
depends_on: ["131-02"]
files_modified:
  - app/scripts/run-learning-impact.ts
  - app/scripts/check-learning-impact-evidence.mjs
  - .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json
  - app/package.json
  - app/src/app/api/feedback/learning-impact/route.ts
  - app/src/app/api/feedback/learning-impact/route.test.ts
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
autonomous: false
requirements:
  - IMPACT-01
  - IMPACT-02
  - IMPACT-03
  - IMPACT-04
user_setup: []
must_haves:
  truths:
    - "CLI generates 131-EVIDENCE.json with global multi-workspace rollup and optional cohort filter"
    - "Evidence checker rejects improvement claims when status is insufficient_sample"
    - "Evidence checker validates separate learningImpactMetrics, intentMetrics, visualMovementMetrics, factualMetrics sections"
    - "Owner/feedback UI shows read-only Impact tab with learned vs non-learned arm metrics and insufficient-sample messaging"
    - "Platform-owner and workspace admin can access learning-impact report API"
  artifacts:
    - path: app/scripts/run-learning-impact.ts
      provides: CLI evidence generation
    - path: app/scripts/check-learning-impact-evidence.mjs
      provides: CI schema and honesty validator
    - path: .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json
      provides: evidence schema contract for Phase 133 gate
    - path: app/src/app/api/feedback/learning-impact/route.ts
      provides: GET learning impact report API
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: Impact read-only tab
  key_links:
    - from: app/scripts/run-learning-impact.ts
      to: runLearningImpact
      via: tsx script with --all-workspaces and --cohort flags
      pattern: "runLearningImpact"
    - from: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      to: /api/feedback/learning-impact
      via: read-only fetch on Impact tab
      pattern: "learning-impact"
    - from: app/scripts/check-learning-impact-evidence.mjs
      to: 131-EVIDENCE.template.json
      via: schema validation + insufficient_sample delta null assertion
      pattern: "insufficient_sample|deltaLearnedMinusNonLearned"
---

# Plan 131-03: Evidence CLI, Impact API and Read-Only UI Tab

<objective>
Ship CLI evidence generation, CI evidence checker with honesty gates, GET learning-impact API with platform-owner/workspace-admin auth, and dedicated read-only Impact tab on HumanQualityCorpusPanel — integrating IMPACT-01 through IMPACT-04 for operator audit and Phase 133 gate precursor.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-learning-impact-measurement/131-RESEARCH.md
@.planning/phases/131-learning-impact-measurement/131-VALIDATION.md
@app/scripts/run-score-calibration.ts
@app/scripts/check-score-calibration-evidence.mjs
@app/src/server/human-quality/impact/service.ts
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/src/server/auth/calibration-access.ts

<interfaces>
From `impact/service.ts` (Plan 131-02):
```typescript
export async function runLearningImpact(options?: {
  workspaceId?: string;
  cohort?: string;
  capturedAt?: string;
}): Promise<LearningImpactReport>
```

From `calibration-access.ts`:
```typescript
export async function requireCalibrationAccess(request: Request, workspaceId: string | null)
```

From `HumanQualityCorpusPanel` (existing tab pattern):
```typescript
const TABS = [{ id: "queue", label: "Queue" }, { id: "calibration", label: "Calibration" }];
// Extend with { id: "impact", label: "Impact" }
```
</interfaces>
</context>

<tasks>

<task id="131-03-01" type="auto">
  <name>CLI evidence script and checker</name>
  <files>app/scripts/run-learning-impact.ts, app/scripts/check-learning-impact-evidence.mjs, .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json, app/package.json</files>
  <action>
    Create `run-learning-impact.ts` mirroring `run-score-calibration.ts`: flags `--out` (default `../.planning/phases/131-learning-impact-measurement/131-EVIDENCE.json`), `--all-workspaces`, `--workspace-id`, `--cohort`. Call `runLearningImpact`, write JSON with `verifiedAt` timestamp and `requirements: ["IMPACT-01","IMPACT-02","IMPACT-03","IMPACT-04"]`. Create `131-EVIDENCE.template.json` with both `ok` and `insufficient_sample` example shapes showing separated metric buckets. Create `check-learning-impact-evidence.mjs` validating: schemaVersion, learningImpactVersion, separate `learningImpactMetrics`/`intentMetrics`/`visualMovementMetrics`/`factualMetrics` (reject blended fields like `overallImpactScore`, `qualityImprovementPathRate`), requirements array, and honesty rule: when `status === "insufficient_sample"` then `visualMovementMetrics.deltaLearnedMinusNonLearned === null` and `learningImpactMetrics.globalVisualScoreDelta === null` and no `improvementClaimed: true`. Optional `--skip-tests` runs impact unit tests. Add npm script `learning-impact-evidence` in package.json.
  </action>
  <verify>
    <automated>node app/scripts/check-learning-impact-evidence.mjs --evidence .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>CI-checkable evidence template validates; CLI can emit milestone impact evidence JSON.</done>
</task>

<task id="131-03-02" type="auto" tdd="true">
  <name>Learning impact API with dual auth</name>
  <files>app/src/app/api/feedback/learning-impact/route.ts, app/src/app/api/feedback/learning-impact/route.test.ts</files>
  <behavior>
    - Platform-owner can GET global rollup without workspaceId
    - Workspace admin can GET with scoped workspaceId
    - Non-admin member receives 403
    - Response caps learningImpactMetrics.rows (or slices drill-down list) at 100 with truncated: true
    - Query params: workspaceId optional, cohort optional
    - Returns LearningImpactReport JSON unchanged in metric structure
  </behavior>
  <action>
    Add GET `/api/feedback/learning-impact` reusing `requireCalibrationAccess` from `calibration-access.ts` (per RESEARCH: same dual auth as calibration). Zod-validate query params. Call `runLearningImpact` with parsed filters. Cap drill-down `rows` in API response to 100 items with `truncated: true` when exceeded — full data remains in CLI JSON. Mirror `score-calibration/route.ts` response shaping. Add route tests for 403, global access, truncation flag.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/learning-impact/route.test.ts</automated>
  </verify>
  <done>Authenticated learning-impact API returns bounded LearningImpactReport for owner/admin roles.</done>
</task>

<task id="131-03-03" type="auto" tdd="true">
  <name>Impact tab on HumanQualityCorpusPanel</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <behavior>
    - Impact tab appears alongside Queue and Calibration tabs
    - Shows status badge ok vs insufficient_sample with insufficientReasons list
    - Renders learned vs non-learned counts and per-slice arm metrics table
    - Shows intentMetrics reject/regenerate rates per arm separately from visualMovementMetrics
    - Shows factualMetrics pass rates per arm without blending into visual headline
    - 403 on impact API hides tab only when both queue and calibration and impact return 403 (extend existing hide logic)
  </behavior>
  <action>
    Add `{ id: "impact", label: "Impact" }` tab to `HumanQualityCorpusPanel`. Fetch `/api/feedback/learning-impact` via useQuery keyed on workspaceId + cohortFilter (reuse cohort filter from calibration tab). Render read-only `ImpactReportView` subcomponent: global status, learned/nonLearned counts, unlabeledCount, slice table with comparability badges, separate sections for intent/visual/factual metrics. When `status === "insufficient_sample"`, show explicit message that improvement cannot be claimed (per IMPACT-04) — do NOT show positive delta as headline. Extend panel hide-when-forbidden logic: hide only when queue, calibration, AND impact all 403. Add component tests for insufficient_sample rendering and metric section separation.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -t "impact"</automated>
  </verify>
  <done>Read-only Impact tab displays learning impact report with honest insufficient-sample UX.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify impact UI and CLI evidence</name>
  <files>.planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json, app/src/components/feedback/HumanQualityCorpusPanel.tsx</files>
  <action>
    Operator verifies automated tests pass, evidence checker validates template, and Impact tab renders learned vs non-learned metrics with honest insufficient-sample messaging.

    Automated pre-check (Claude runs before pausing):
    1. `cd app && npm test -- tests/unit/human-quality/impact src/app/api/feedback/learning-impact src/components/feedback/HumanQualityCorpusPanel.test.tsx`
    2. `node app/scripts/check-learning-impact-evidence.mjs --evidence .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json --skip-tests`
  </action>
  <what-built>Full learning impact measurement stack: attribution (Plan 01), report engine (Plan 02), CLI evidence, API, and Impact UI tab.</what-built>
  <how-to-verify>
    1. Confirm automated pre-check commands above are green.
    2. If staging DB has ≥5 evaluated corpus items with both learned and non-learned arms: run `cd app && npx tsx scripts/run-learning-impact.ts --all-workspaces` and inspect `131-EVIDENCE.json` status and separated metric buckets.
    3. As platform-owner, open HumanQualityCorpusPanel → Impact tab: confirm learned vs non-learned arm table, intent/visual/factual sections separated, and insufficient-sample message when applicable.
    4. Accept an output-learning recommendation, generate a derivation, add to corpus, evaluate — confirm new item shows `learningApplied: true` in evidence rows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | Read-only report; no mutation endpoints |
| CLI evidence → CI | Checker enforces honesty gates before milestone claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-07 | I | learning-impact API response | mitigate | requireCalibrationAccess; workspace filter on query |
| T-131-08 | D | Large report payload | mitigate | Cap API rows at 100 with truncated flag |
| T-131-09 | T | False improvement in evidence JSON | mitigate | check-learning-impact-evidence.mjs rejects deltas when insufficient_sample |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/impact src/app/api/feedback/learning-impact src/components/feedback/HumanQualityCorpusPanel.test.tsx`
- `node app/scripts/check-learning-impact-evidence.mjs --evidence .planning/phases/131-learning-impact-measurement/131-EVIDENCE.template.json --skip-tests`
- `cd app && npm run lint && npm run build`
</verification>

<success_criteria>
- IMPACT-01–04 integrated end-to-end: attribution → report → evidence → API → UI.
- Evidence checker blocks false improvement claims when sample is insufficient.
- Metric buckets remain separated in evidence, API, and UI.
</success_criteria>

<output>
After completion, create `.planning/phases/131-learning-impact-measurement/131-03-SUMMARY.md`.
</output>
