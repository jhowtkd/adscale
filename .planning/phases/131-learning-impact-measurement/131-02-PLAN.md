---
phase: 131-learning-impact-measurement
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - app/src/server/human-quality/impact/types.ts
  - app/src/server/human-quality/impact/enrich.ts
  - app/src/server/human-quality/impact/aggregate.ts
  - app/src/server/human-quality/impact/report.ts
  - app/src/server/human-quality/impact/service.ts
  - app/tests/unit/human-quality/impact/enrich.test.ts
  - app/tests/unit/human-quality/impact/aggregate.test.ts
  - app/tests/unit/human-quality/impact/report.test.ts
autonomous: true
requirements:
  - IMPACT-02
  - IMPACT-03
  - IMPACT-04
must_haves:
  truths:
    - "Impact report partitions evaluated corpus rows into learned vs non-learned arms within clientProfileId × generationMode × format slices"
    - "Report exposes separate learningImpactMetrics, intentMetrics, visualMovementMetrics, and factualMetrics buckets — never blended"
    - "Reject and regenerate intent rates are computed per arm from human evaluation intent field"
    - "When global evaluated count < 5 or a slice lacks ≥3 items in either arm, status is insufficient_sample and movement deltas are null"
  artifacts:
    - path: app/src/server/human-quality/impact/report.ts
      provides: LearningImpactReport builder with status gates
      exports: ["buildLearningImpactReport", "MIN_GLOBAL_IMPACT_ITEMS", "MIN_ARM_SAMPLE"]
    - path: app/src/server/human-quality/impact/aggregate.ts
      provides: slice partition and arm metric reducers
      exports: ["partitionImpactSlices", "computeArmMetrics", "computeSliceComparison"]
    - path: app/src/server/human-quality/impact/service.ts
      provides: runLearningImpact orchestration
      exports: ["runLearningImpact"]
  key_links:
    - from: app/src/server/human-quality/impact/service.ts
      to: listEvaluatedCorpusWithEvaluations
      via: repository join then enrich → aggregate → report
      pattern: "listEvaluatedCorpusWithEvaluations"
    - from: app/src/server/human-quality/impact/report.ts
      to: impact/aggregate.ts
      via: buildLearningImpactReport calls partitionImpactSlices
      pattern: "comparability.*insufficient"
    - from: app/src/server/human-quality/impact/aggregate.ts
      to: ImpactEvaluatedRow.learningApplied
      via: arm split learned vs non_learned
      pattern: "learningApplied"
---

# Plan 131-02: Learning Impact Report Engine (IMPACT-02–04)

<objective>
Build pure TypeScript `human-quality/impact/` module that compares learned vs non-learned arms on the human corpus, emits separated metric buckets, and returns honest `insufficient_sample` when comparability gates fail (IMPACT-02, IMPACT-03, IMPACT-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-learning-impact-measurement/131-RESEARCH.md
@.planning/phases/131-learning-impact-measurement/131-VALIDATION.md
@app/src/server/human-quality/calibration/report.ts
@app/src/server/human-quality/calibration/aggregate.ts
@app/src/server/repositories/human-quality-corpus.ts
@app/src/server/human-quality/impact/enrich.ts

<interfaces>
From `calibration/types.ts`:
```typescript
export interface EvaluatedCorpusRow {
  item: HumanQualityCorpusItem;
  evaluation: HumanQualityEvaluation;
}
```

From Plan 131-01 `enrich.ts`:
```typescript
export function resolveLearningApplied(snapshot): boolean
export function buildImpactRow(row: EvaluatedCorpusRow): ImpactEvaluatedRow
```

From `calibration/report.ts` (mirror thresholds):
```typescript
export const MIN_GLOBAL_EVALUATED_ITEMS = 5;
export const MIN_SLICE_SAMPLE = 3;
```
</interfaces>
</context>

<tasks>

<task id="131-02-01" type="auto" tdd="true">
  <name>Impact types and row enrichment</name>
  <files>app/src/server/human-quality/impact/types.ts, app/src/server/human-quality/impact/enrich.ts, app/tests/unit/human-quality/impact/enrich.test.ts</files>
  <behavior>
    - buildImpactRows maps EvaluatedCorpusRow[] to ImpactEvaluatedRow with clientProfileId, generationMode, format, cohort, learningApplied, visualScore, factualPass, intent
    - applicationResolution not_recorded → learningApplied false
    - unlabeledCount tracks rows with resolution not_recorded
    - sliceKey format: `${clientProfileId}|${generationMode}|${format}`
  </behavior>
  <action>
    Create `impact/types.ts` with `ImpactEvaluatedRow`, `ImpactSliceKey`, `ImpactArmMetrics`, `ImpactSliceComparison`, `LearningImpactReport` per RESEARCH Pattern 2–3. Expand `enrich.ts` with `buildImpactRows` batch helper. Use `item.clientProfileId`, `item.generationMode`, `item.format` from corpus item — NOT workspaceId (per RESEARCH A3). Cohort label is carried but does NOT determine learningApplied (per locked decision: cohort ≠ prefill applied). Export `LEARNING_IMPACT_VERSION = "1.0.0"`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/impact/enrich.test.ts</automated>
  </verify>
  <done>Impact row types and enrichment from evaluated corpus join are complete and tested.</done>
</task>

<task id="131-02-02" type="auto" tdd="true">
  <name>Slice partition and arm metrics</name>
  <files>app/src/server/human-quality/impact/aggregate.ts, app/tests/unit/human-quality/impact/aggregate.test.ts</files>
  <behavior>
    - partitionImpactSlices groups rows by clientProfileId|generationMode|format
    - learned arm: learningApplied===true; non_learned: false
    - computeArmMetrics: meanVisualScore, rejectIntentRate (intent===reject), regenerateIntentRate (intent===regenerate), factualPassRate
    - slice with learned count < 3 OR non_learned < 3 → comparability insufficient, visualScoreDelta null
    - visualScoreDelta = learned.meanVisualScore - nonLearned.meanVisualScore when comparability ok
    - globalVisualScoreDelta: mean of comparable slice deltas only (exclude insufficient slices)
  </behavior>
  <action>
    Implement pure reducers in `aggregate.ts` mirroring `calibration/aggregate.ts` style — no external stats library. Export `partitionImpactSlices`, `computeArmMetrics`, `computeSliceComparison`, `computeGlobalVisualDelta`. Handle null rates when arm count is 0. Do NOT use cohort label for arm assignment. Add supplementary `computeCohortMovement` helper (pre_learning vs post_learning means) for optional `visualMovementMetrics.cohortMovement` section — supplementary only per RESEARCH discretion.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/impact/aggregate.test.ts</automated>
  </verify>
  <done>Learned vs non-learned slice comparison with per-arm intent, visual, and factual rates is tested.</done>
</task>

<task id="131-02-03" type="auto" tdd="true">
  <name>Report assembly and insufficient-sample gates</name>
  <files>app/src/server/human-quality/impact/report.ts, app/src/server/human-quality/impact/service.ts, app/tests/unit/human-quality/impact/report.test.ts</files>
  <behavior>
    - rows.length < 5 → status insufficient_sample, all movement deltas null, insufficientReasons includes global_below_minimum
    - status ok requires global ≥5 AND at least one comparable slice with both arms ≥3
    - insufficient_sample: deltaLearnedMinusNonLearned null, globalVisualScoreDelta null
    - intentMetrics and factualMetrics populated even when insufficient (descriptive counts) but no improvement narrative fields
    - learningImpactMetrics, intentMetrics, visualMovementMetrics, factualMetrics are separate top-level buckets
    - runLearningImpact loads evaluated rows, builds report, caps drill-down rows at 500 (match calibration default)
  </behavior>
  <action>
    Implement `buildLearningImpactReport` in `report.ts` with `MIN_GLOBAL_IMPACT_ITEMS = 5` and `MIN_ARM_SAMPLE = 3` (mirror Phase 130). Emit `insufficientReasons: string[]` when gates fail. Never add `overallImpactScore` or blend factual into visual conclusions. Implement `runLearningImpact({ workspaceId?, cohort?, capturedAt? })` in `service.ts` calling `listEvaluatedCorpusWithEvaluations` → `buildImpactRows` → `buildLearningImpactReport`. Keep module separate from `calibration/report.ts` — do NOT nest inside CalibrationReport. Include `unlabeledCount` in learningImpactMetrics for honest legacy corpus reporting.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/impact/report.test.ts</automated>
  </verify>
  <done>LearningImpactReport with honest insufficient_sample gates and separated metric buckets is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Impact aggregation | Pure functions over evaluated corpus — no live re-scoring or recommendation re-run |
| Report consumer | Must not infer improvement when status ≠ ok |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | I | LearningImpactReport rows drill-down | mitigate | Cap rows at 500; API layer will further cap to 100 in Plan 03 |
| T-131-05 | T | False improvement from small N | mitigate | MIN_ARM_SAMPLE=3 per arm; null deltas when incomparable; insufficientReasons explicit |
| T-131-06 | D | Report generation on large corpus | mitigate | Repository default limit 500; pure in-memory aggregation |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/impact`
- `cd app && npm run lint -- src/server/human-quality/impact`
</verification>

<success_criteria>
- IMPACT-02: Evaluation separates learned outputs from non-learned comparable outputs by client, mode and format.
- IMPACT-03: Impact report measures rejection/regeneration intent, human visual score movement and factual pass rate.
- IMPACT-04: Insufficient sample states are explicit; movement deltas null when gates fail.
</success_criteria>

<output>
After completion, create `.planning/phases/131-learning-impact-measurement/131-02-SUMMARY.md`.
</output>
