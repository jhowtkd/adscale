---
phase: 131-learning-impact-measurement
plan: "02"
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - app/src/lib/hooks/use-derivation-flow.ts
  - app/src/lib/hooks/use-derivations.ts
  - app/src/lib/hooks/use-campaign-workspace.ts
  - app/src/components/campaigns/OutputLearningRecommendationCard.tsx
  - app/src/app/(dashboard)/campaigns/[id]/page.tsx
  - app/src/server/human-quality/service.ts
  - app/src/server/human-quality/impact/enrich.ts
  - app/tests/unit/human-quality/impact/enrich.test.ts
autonomous: true
requirements:
  - IMPACT-01
must_haves:
  truths:
    - "Corpus selection freezes outputLearningApplication into qualitySnapshot at selection time"
    - "Output-learning accept flow threads trace metadata through derivation POST — not performance-learning card"
    - "resolveLearningApplied returns true only when snapshot.applied===true; legacy rows return false"
    - "Historical corpus items without application metadata report learningApplied=false and unlabeledCount increments"
  artifacts:
    - path: app/src/server/human-quality/impact/enrich.ts
      provides: resolveLearningApplied and buildImpactRow for per-row attribution resolution
      exports: ["resolveLearningApplied", "buildImpactRow"]
    - path: app/src/server/human-quality/service.ts
      provides: selectDerivationForCorpus merges derivation.outputLearningApplication into qualitySnapshot
    - path: app/src/components/campaigns/OutputLearningRecommendationCard.tsx
      provides: accept passes applicationSnapshot to derivation flow
  key_links:
    - from: app/src/components/campaigns/OutputLearningRecommendationCard.tsx
      to: derivations POST
      via: useDerivationFlow carries snapshot from accept → useCreateDerivations body
      pattern: "outputLearningApplication"
    - from: app/src/server/human-quality/service.ts
      to: derivation.outputLearningApplication
      via: selectDerivationForCorpus copies into qualitySnapshot
      pattern: "outputLearningApplication"
---

# Plan 131-02: Accept Flow, Corpus Freeze and Enrichment (IMPACT-01)

<objective>
Wire output-learning accept → derivation POST threading, freeze application snapshot into corpus qualitySnapshot at selection time, and provide minimal enrich helpers for per-row learningApplied resolution (IMPACT-01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-learning-impact-measurement/131-01-SUMMARY.md
@.planning/phases/131-learning-impact-measurement/131-RESEARCH.md
@app/src/server/human-quality/application-schema.ts
@app/src/server/human-quality/service.ts
@app/src/components/campaigns/OutputLearningRecommendationCard.tsx
@app/src/lib/hooks/use-derivation-flow.ts

<interfaces>
From Plan 131-01 `application-schema.ts`:
```typescript
export function buildApplicationSnapshotFromAccept(payload): OutputLearningApplicationSnapshot
export function sanitizeOutputLearningApplication(input): OutputLearningApplicationSnapshot
```

From `human-quality/service.ts`:
```typescript
export async function selectDerivationForCorpus(input: SelectCorpusItemInput): Promise<HumanQualityCorpusItem>
```
</interfaces>
</context>

<tasks>

<task id="131-02-01" type="auto">
  <name>Thread accept flow through derivation hooks</name>
  <files>app/src/lib/hooks/use-derivation-flow.ts, app/src/lib/hooks/use-derivations.ts, app/src/lib/hooks/use-campaign-workspace.ts, app/src/components/campaigns/OutputLearningRecommendationCard.tsx, app/src/app/(dashboard)/campaigns/[id]/page.tsx</files>
  <behavior>
    - Accept from OutputLearningRecommendationCard sets pending application on useDerivationFlow
    - useCreateDerivations POST includes outputLearningApplication when pending
    - Pending snapshot cleared on closeFlow
    - Performance-learning card (NextExperimentRecommendationCard) does NOT set application snapshot
  </behavior>
  <action>
    Extend `useDerivationFlow` with `pendingOutputLearningApplication` state + setter cleared on closeFlow. Change `OutputLearningRecommendationCard` `onAccept` signature to pass `{ prefill, applicationSnapshot }` built via `buildApplicationSnapshotFromAccept` using recommendation.appliedLearningTrace — update page.tsx handler. Extend `useCreateDerivations` mutation options to accept `outputLearningApplication` and include in POST JSON. Wire `handleGenerateDerivations` in use-campaign-workspace to pass pending snapshot from derivation flow. Only output-learning accept path sets snapshot — never NextExperimentRecommendationCard.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/campaigns/\[id\]/derivations/route.test.ts -t "outputLearningApplication"</automated>
  </verify>
  <done>Accept→generate threads bounded application metadata from OutputLearningRecommendationCard to derivations POST.</done>
</task>

<task id="131-02-02" type="auto" tdd="true">
  <name>Freeze snapshot at corpus selection and enrich helpers</name>
  <files>app/src/server/human-quality/service.ts, app/src/server/human-quality/impact/enrich.ts, app/tests/unit/human-quality/impact/enrich.test.ts</files>
  <behavior>
    - resolveLearningApplied: snapshot.applied===true → true; missing/null → false
    - buildImpactRow maps applicationResolution from snapshot
    - selectDerivationForCorpus merges derivation.outputLearningApplication into qualitySnapshot
    - Historical rows without application metadata → learningApplied false, resolution not_recorded (per RESEARCH Open Questions RESOLVED #3)
  </behavior>
  <action>
    In `selectDerivationForCorpus`: after `buildQualitySnapshotFromDerivation`, merge `derivation.outputLearningApplication` (sanitized via `sanitizeOutputLearningApplication`) into `qualitySnapshot.outputLearningApplication`. Create `impact/enrich.ts` with `resolveLearningApplied` and `buildImpactRow` (minimal — full report types in Plan 03). Add enrich unit tests covering applied true/false, not_recorded legacy, and buildImpactRow field mapping.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/impact/enrich.test.ts</automated>
  </verify>
  <done>Corpus selection freezes application snapshot; enrich resolves learningApplied per evaluated row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corpus selection | qualitySnapshot must not gain prompts/URLs from derivation row |
| Accept flow | Only output-learning path may set application snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | I | Corpus qualitySnapshot | mitigate | sanitizeOutputLearningApplication before merge at selection |
| T-131-05 | T | Performance-learning mis-attribution | mitigate | Only OutputLearningRecommendationCard sets snapshot; traceId /^ol-/ validated server-side in Plan 01 |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/impact/enrich.test.ts`
- `cd app && npm run lint -- src/server/human-quality/service.ts src/server/human-quality/impact`
</verification>

<success_criteria>
- IMPACT-01 end-to-end: accept → persist → corpus freeze → enrich resolves learningApplied.
- Legacy corpus items honestly report not_recorded without heuristic backfill.
</success_criteria>

<output>
After completion, create `.planning/phases/131-learning-impact-measurement/131-02-SUMMARY.md`.
</output>
