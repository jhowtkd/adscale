---
phase: 68
plan: "02"
title: Dashboard progression card and client hook
wave: 2
depends_on:
  - 68-01
requirements:
  - PROG-01
  - PROG-02
  - PROG-04
files_modified:
  - app/src/lib/hooks/use-progression.ts
  - app/src/lib/hooks/use-progression.test.tsx
  - app/src/components/dashboard/AdsScientistProgressCard.tsx
  - app/src/components/dashboard/AdsScientistProgressCard.test.tsx
  - app/src/app/(dashboard)/page.tsx
  - app/src/i18n/messages/pt-BR.json
  - app/src/i18n/messages/en.json
autonomous: true
must_haves:
  truths:
    - Dashboard shows current Ads Scientist level in a compact top-dashboard module.
    - Card shows level, progress bar, and one next action.
    - CTA routes to an existing app surface and does not open a full mission center.
    - UI tone uses a playful lab/experiment metaphor without childish RPG language.
    - Loading, empty, blocked, and error states do not disrupt the dashboard.
  artifacts:
    - path: app/src/lib/hooks/use-progression.ts
      provides: TanStack Query hook for progression API.
    - path: app/src/components/dashboard/AdsScientistProgressCard.tsx
      provides: Compact dashboard progression module.
    - path: app/src/app/(dashboard)/page.tsx
      provides: Dashboard integration near the top of the page.
---

# Plan 68-02: Dashboard progression card and client hook

<objective>
Expose Ads Scientist progression in the dashboard through a compact, action-oriented module that uses the backend progression API and prepares the UI surface for Phase 69 missions.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/68-progression-foundation/68-CONTEXT.md
@.planning/phases/68-progression-foundation/68-RESEARCH.md
@.planning/phases/68-progression-foundation/68-VALIDATION.md
@.planning/phases/68-progression-foundation/68-01-PLAN.md
@app/AGENTS.md
@app/src/app/(dashboard)/page.tsx
@app/src/components/dashboard/CreditPanel.tsx
@app/src/lib/hooks/use-dashboard-stats.ts
</context>

<interfaces>

Consume the API shape from Plan 68-01. If Plan 68-01 names the exported type differently, import or locally mirror the route response type without creating a circular server/client dependency.

The hook should expose:

```ts
type UseProgressionResult = {
  data?: WorkspaceProgressionResponse;
  isLoading: boolean;
  isError: boolean;
  refetch: () => void;
};
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add TanStack Query hook for progression</name>
  <files>app/src/lib/hooks/use-progression.ts, app/src/lib/hooks/use-progression.test.tsx</files>
  <read_first>
    - app/src/lib/hooks/use-dashboard-stats.ts for query style.
    - app/src/lib/api-client.ts for fetch helper behavior.
    - app/src/lib/query-config.ts for staleTime constants.
  </read_first>
  <action>
Create a `useProgression` hook using TanStack Query v5.

Requirements:
- Query key should be stable, e.g. `["workspace", "progression"]`.
- Fetch `/api/workspace/progression` through `apiFetch`.
- Use an appropriate dynamic stale time; progression can refetch on dashboard focus.
- Return typed data matching the API response.
- Tests cover success, loading/error behavior where existing hook test patterns allow.
  </action>
  <verify>
    <automated>cd app && npm test -- src/lib/hooks/use-progression.test.tsx</automated>
  </verify>
  <done>Hook fetches workspace progression through the API and follows existing query patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Build compact Ads Scientist dashboard card</name>
  <files>app/src/components/dashboard/AdsScientistProgressCard.tsx, app/src/components/dashboard/AdsScientistProgressCard.test.tsx</files>
  <read_first>
    - app/src/components/dashboard/CreditPanel.tsx for compact card/progress bar style.
    - app/src/components/dashboard/ActivityFeed.tsx for dashboard card density.
    - .planning/phases/68-progression-foundation/68-CONTEXT.md for tone and visual weight.
  </read_first>
  <action>
Create a compact dashboard component that renders:
- current level label;
- concise lab/experiment-themed description;
- progress bar with accessible label;
- one next action with CTA link;
- blocked reason when `nextAction.blocked` is true;
- compact loading skeleton;
- non-disruptive error state with retry or fallback copy.

Design constraints:
- Do not create a hero section.
- Do not create a full mission list.
- Do not use RPG/childish language.
- Use lucide icons if an icon is needed.
- Keep copy practical: "complete your next experiment" style.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/dashboard/AdsScientistProgressCard.test.tsx</automated>
  </verify>
  <done>Card renders level, progress, next action, blocked state, loading state, and error state in compact dashboard form.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the card into the dashboard</name>
  <files>app/src/app/(dashboard)/page.tsx, app/src/i18n/messages/pt-BR.json, app/src/i18n/messages/en.json</files>
  <read_first>
    - app/src/app/(dashboard)/page.tsx top header and first content sections.
    - app/src/i18n/messages/pt-BR.json and app/src/i18n/messages/en.json for dashboard translation conventions.
  </read_first>
  <action>
Render `AdsScientistProgressCard` near the top of the dashboard after the header and before or alongside campaign/stat sections.

Integration requirements:
- Preserve existing dashboard search/view controls.
- Keep campaign list visible without a large scroll penalty on desktop and mobile.
- Use translated strings if the app expects dashboard copy in translation files.
- Ensure CTA links only to existing surfaces such as `/campaigns/new`, a campaign page, settings/billing, or dashboard sections.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/dashboard/AdsScientistProgressCard.test.tsx src/lib/hooks/use-progression.test.tsx && npm run lint</automated>
  </verify>
  <done>Dashboard includes compact progression card without disrupting campaign browsing or existing onboarding tour.</done>
</task>

<task type="auto">
  <name>Task 4: Phase verification evidence</name>
  <files>.planning/phases/68-progression-foundation/68-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/68-progression-foundation/68-VALIDATION.md
    - .planning/phases/68-progression-foundation/68-01-PLAN.md
    - .planning/phases/68-progression-foundation/68-02-PLAN.md
  </read_first>
  <action>
Create `68-VERIFICATION.md` documenting:
- focused test commands and results;
- lint/build status or known unrelated blockers;
- manual visual check checklist for desktop/mobile dashboard card;
- requirements coverage for PROG-01..05.
  </action>
  <verify>
    <automated>test -f .planning/phases/68-progression-foundation/68-VERIFICATION.md && rg "PROG-01|PROG-05|npm run lint" .planning/phases/68-progression-foundation/68-VERIFICATION.md</automated>
  </verify>
  <done>Verification artifact exists and maps Phase 68 requirements to automated/manual evidence.</done>
</task>

</tasks>

<verification>

Run after plan completion:

```bash
cd app && npm test -- src/server/progression src/server/repositories/progression src/app/api/workspace/progression src/lib/hooks/use-progression src/components/dashboard/AdsScientistProgressCard && npm run lint
```

If the UI changes are substantial, run a local browser smoke after implementation in `$gsd-execute-phase 68`.

</verification>
