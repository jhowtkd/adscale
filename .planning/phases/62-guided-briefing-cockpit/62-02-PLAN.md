---
phase: 62-guided-briefing-cockpit
plan: "02"
type: execute
wave: 2
depends_on:
  - "62-01"
files_modified:
  - app/src/components/workspace/GuidedBriefingPanel.tsx
  - app/src/components/workspace/GuidedBriefingPanel.test.tsx
  - app/src/app/(dashboard)/campaigns/[id]/page.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - GUIDE-03
  - GUIDE-04
  - GUIDE-05
must_haves:
  truths:
    - User sees one question at a time with accept, edit, skip.
    - Weak brief shows guided panel; user can open full form.
    - Completing guided flow saves pilot briefing and advances workspace.
    - EN and PT-BR strings exist for all guided steps.
  artifacts:
    - GuidedBriefingPanel component with tests.
    - Campaign workspace integration.
    - i18n guidedBriefing namespace.
  key_links:
    - useGuidedBriefing drives state.
    - savePilot on completion.
---

# Plan 62-02: Guided Briefing UI and Workspace Integration

<objective>
Expose guided briefing in the pilot workspace with i18n and component tests.

Output: GuidedBriefingPanel, page wiring, localized copy, build/lint pass.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: GuidedBriefingPanel component</name>
  <files>app/src/components/workspace/GuidedBriefingPanel.tsx, app/src/components/workspace/GuidedBriefingPanel.test.tsx</files>
  <action>
    One-question UI with suggestion card, accept/edit/skip, progress, link to full form.
  </action>
  <verify>cd app && npm test -- src/components/workspace/GuidedBriefingPanel.test.tsx</verify>
  <done>Tests cover accept, skip, edit, and completion callback.</done>
</task>

<task type="auto">
  <name>Task 2: i18n strings</name>
  <files>app/messages/en.json, app/messages/pt-BR.json</files>
  <action>Add guidedBriefing namespace for steps, actions, progress, full-form link.</action>
  <verify>cd app && npm run lint</verify>
  <done>EN and PT-BR keys present for all steps.</done>
</task>

<task type="auto">
  <name>Task 3: Workspace integration</name>
  <files>app/src/app/(dashboard)/campaigns/[id]/page.tsx</files>
  <action>
    Show GuidedBriefingPanel when isBriefWeak(campaign); toggle PilotBriefingForm via full-form link.
    Wire onComplete to savePilot with mapped briefing fields.
  </action>
  <verify>cd app && npm test -- src/components/workspace/GuidedBriefingPanel.test.tsx && npm run build</verify>
  <done>Pilot grid uses guided flow for weak briefs.</done>
</task>

</tasks>

<verification>
- Component tests pass
- `npm run lint`
- `npm run build`
</verification>
