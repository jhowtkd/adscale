---
phase: 130-score-calibration-and-rubric-alignment
plan: "03"
type: execute
wave: 3
depends_on: ["130-02"]
files_modified:
  - app/drizzle/0044_rubric_calibration_adjustments.sql
  - app/src/server/db/schema.ts
  - app/src/server/human-quality/calibration/adjustments.ts
  - app/src/server/human-quality/calibration/service.ts
  - app/src/server/repositories/rubric-calibration-adjustments.ts
  - app/tests/unit/human-quality/calibration/adjustments.test.ts
  - app/tests/unit/human-quality/calibration-adjustments-repository.test.ts
autonomous: true
requirements:
  - CALIB-03
must_haves:
  truths:
    - "Systematic divergence slices with |meanSignedDelta| ≥ 15 and ≥3 items auto-generate proposed adjustments"
    - "Each adjustment cites corpus item IDs and grouped stats with explicit adjustmentVersion"
    - "Adjustments persist as proposed only — no accepted/applied status in this phase"
    - "Adjustment targets reference score_ceiling, observable_rubric, or gate_classifier modules"
  artifacts:
    - path: app/drizzle/0044_rubric_calibration_adjustments.sql
      provides: versioned adjustment registry table
    - path: app/src/server/human-quality/calibration/adjustments.ts
      provides: auto-proposal builder from divergence slices
    - path: app/src/server/repositories/rubric-calibration-adjustments.ts
      provides: insert/list proposed adjustments
    - path: app/src/server/human-quality/calibration/service.ts
      provides: runScoreCalibration orchestrator
  key_links:
    - from: app/src/server/human-quality/calibration/adjustments.ts
      to: rubric_calibration_adjustments
      via: evidenceRefs corpusItemIds + adjustmentVersion
      pattern: "status.*proposed"
    - from: app/src/server/human-quality/calibration/service.ts
      to: listEvaluatedCorpusWithEvaluations → buildCalibrationReport
      via: end-to-end calibration run
      pattern: "buildCalibrationComparisons|buildCalibrationReport|proposeAdjustments"
---

# Plan 130-03: Versioned Rubric/Gate Adjustment Proposal Registry

<objective>
Persist corpus-evidence-backed rubric/gate adjustment proposals with explicit versioning when systematic divergence exceeds thresholds (CALIB-03). Orchestrate full calibration service — proposals only, no code edits to gate/rubric modules.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-score-calibration-and-rubric-alignment/130-CONTEXT.md
@.planning/phases/130-score-calibration-and-rubric-alignment/130-RESEARCH.md
@app/src/server/ai/creative-score-ceilings.ts
@app/src/server/ai/observable-rubric.ts
@app/src/server/human-quality/calibration/report.ts
@app/src/server/output-learning/types.ts

<interfaces>
From creative-score-ceilings.ts (read-only reference for proposals):
```typescript
// SCORE_CEILING_BY_FAILURE — import for current ceiling values in proposal rationale
```

From report.ts (Plan 130-02):
```typescript
export const RUBRIC_CALIBRATION_VERSION = "1.0.0";
export const MIN_SLICE_SAMPLE = 3;
export function buildCalibrationReport(...)
```

Pattern from output-learning:
```typescript
export const OUTPUT_LEARNING_ALGORITHM_VERSION = "1.0.0";
// evidenceRefs with corpus item IDs
```
</interfaces>
</context>

<tasks>

<task id="130-03-01" type="auto" tdd="true">
  <name>Adjustment schema and repository</name>
  <files>app/drizzle/0044_rubric_calibration_adjustments.sql, app/src/server/db/schema.ts, app/src/server/repositories/rubric-calibration-adjustments.ts, app/tests/unit/human-quality/calibration-adjustments-repository.test.ts</files>
  <behavior>
    - Insert adjustment with status proposed, adjustmentVersion, targetModule, targetKey, evidence JSON
    - List adjustments filtered by adjustmentVersion and status
    - evidence JSON contains corpusItemIds, slice stats, per-item scoreDelta refs
    - No update to accepted/superseded in this phase
  </behavior>
  <action>
    Add `rubric_calibration_adjustments` table: id, adjustment_version, status (check: proposed only for inserts in 130), target_module (score_ceiling|observable_rubric|gate_classifier), target_key, slice_key (failure×mode×format), rationale text, evidence_refs jsonb, proposed_at, created_at. Mirror `OUTPUT_LEARNING_ALGORITHM_VERSION` pattern — store `RUBRIC_CALIBRATION_VERSION` on each row. Repository: `insertProposedAdjustment`, `listProposedAdjustments({ adjustmentVersion? })`. Platform-owner write implied via service layer in plan 04 — repository is pure persistence.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration-adjustments-repository.test.ts</automated>
  </verify>
  <done>Postgres registry stores versioned proposed adjustments with corpus evidence refs.</done>
</task>

<task id="130-03-02" type="auto" tdd="true">
  <name>Auto-propose adjustments from divergence slices</name>
  <files>app/src/server/human-quality/calibration/adjustments.ts, app/tests/unit/human-quality/calibration/adjustments.test.ts</files>
  <behavior>
    - Slice with count=3 meanSignedDelta=18 → generates proposed adjustment
    - Slice with count=2 → no proposal (below MIN_SLICE_SAMPLE)
    - Slice with count=5 meanSignedDelta=10 → no proposal (below threshold)
    - factual_issue slices target factual bucket only — no ceiling proposals
    - other failure reason → no auto proposal
    - Each proposal includes evidenceRefs with corpusItemId and scoreDelta per item
  </behavior>
  <action>
    Implement `proposeAdjustments(comparisons, { divergenceThreshold: 15, minSliceSample: 3 })` per CONTEXT D-02: auto-propose when |meanSignedDelta| ≥ 15 AND slice count ≥ 3. Use `failure-bridge.ts` to resolve `targetModule`/`targetKey` from primary failure reason. Import current values from `SCORE_CEILING_BY_FAILURE` and rubric constant names from `observable-rubric.ts` for proposal rationale text only — do NOT edit those source files (Phase 132 applies). Skip `other` and slices dominated by `factual_issue` for visual ceiling proposals. Status always `proposed`. Return `AdjustmentProposalSummary[]` for report embedding.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/adjustments.test.ts</automated>
  </verify>
  <done>CALIB-03 auto-proposals cite corpus evidence with version and target module — proposed status only.</done>
</task>

<task id="130-03-03" type="auto">
  <name>Calibration service orchestrator</name>
  <files>app/src/server/human-quality/calibration/service.ts, app/tests/unit/human-quality/calibration/adjustments.test.ts</files>
  <action>
    Implement `runScoreCalibration({ workspaceId?, cohort? })` in `calibration/service.ts`: (1) `listEvaluatedCorpusWithEvaluations`, (2) `buildCalibrationComparisons`, (3) `buildCalibrationReport`, (4) `proposeAdjustments`, (5) persist new proposals via repository (dedupe by slice_key + adjustmentVersion — skip if identical proposal already exists). Attach `adjustments` array to report. Export types for API/CLI consumers. Do NOT modify `observable-rubric.ts`, `creative-quality-gate.ts`, or `creative-score-ceilings.ts`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/adjustments.test.ts -t "runScoreCalibration|orchestrat"</automated>
  </verify>
  <done>Single service entrypoint produces complete CalibrationReport with persisted proposed adjustments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adjustment writes → Postgres | Only proposed records — no runtime gate mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-05 | T | insertProposedAdjustment | mitigate | Service-layer auth in plan 04; repository tests verify status=proposed only |
| T-130-06 | T | adjustment evidence_refs | mitigate | Store corpus item IDs and bounded stats only — no prompts |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/calibration/adjustments.test.ts tests/unit/human-quality/calibration-adjustments-repository.test.ts`
- Confirm no diffs in `app/src/server/ai/observable-rubric.ts` or `creative-quality-gate.ts`
</verification>

<success_criteria>
- CALIB-03: Gate/rubric adjustments are versioned and backed by corpus evidence.
- All adjustments remain `proposed` — Phase 132 owns acceptance/application.
</success_criteria>

<output>
After completion, create `.planning/phases/130-score-calibration-and-rubric-alignment/130-03-SUMMARY.md`.
</output>
