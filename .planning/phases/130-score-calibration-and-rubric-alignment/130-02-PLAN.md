---
phase: 130-score-calibration-and-rubric-alignment
plan: "02"
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - app/src/server/human-quality/calibration/aggregate.ts
  - app/src/server/human-quality/calibration/report.ts
  - app/tests/unit/human-quality/calibration/aggregate.test.ts
autonomous: true
requirements:
  - CALIB-02
  - CALIB-04
must_haves:
  truths:
    - "Calibration report groups divergences by failure reason, generation mode and format"
    - "Report returns insufficient_corpus when fewer than 5 evaluated items globally"
    - "visualMetrics and factualMetrics are separate buckets — never blended"
    - "highVisualButFactualFail guard list surfaces items where visual scores cannot offset factual failure"
  artifacts:
    - path: app/src/server/human-quality/calibration/aggregate.ts
      provides: GroupSlice aggregation and factual guard helpers
      exports: ["aggregateGroup", "highVisualButFactualFail", "groupComparisonsBy"]
    - path: app/src/server/human-quality/calibration/report.ts
      provides: CalibrationReport builder with metric separation
      exports: ["buildCalibrationReport", "MIN_GLOBAL_EVALUATED_ITEMS"]
  key_links:
    - from: app/src/server/human-quality/calibration/report.ts
      to: visualMetrics + factualMetrics
      via: separate top-level keys mirroring Phase 128 evidence pattern
      pattern: "visualMetrics.*factualMetrics"
    - from: app/src/server/human-quality/calibration/aggregate.ts
      to: primaryFailureReason / generationMode / format
      via: groupComparisonsBy slice keys
      pattern: "divergenceByFailureReason|divergenceByMode|divergenceByFormat"
---

# Plan 130-02: Grouped Divergence Report and Factual Metric Separation

<objective>
Build grouped divergence aggregation and the `CalibrationReport` schema with strict visual/factual metric separation (CALIB-02, CALIB-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-score-calibration-and-rubric-alignment/130-CONTEXT.md
@.planning/phases/130-score-calibration-and-rubric-alignment/130-RESEARCH.md
@.planning/phases/128-evaluation-and-release-gate/128-EVIDENCE.json
@app/src/server/human-quality/calibration/compare.ts
@app/src/server/human-quality/calibration/types.ts

<interfaces>
From Plan 130-01:
```typescript
export interface CalibrationComparison {
  corpusItemId: string;
  automaticQualityScore: number | null;
  humanVisualScore: number;
  scoreDelta: number | null;
  primaryFailureReason: HumanQualityFailureReason;
  factualPass: boolean;
  generationMode: string;
  format: string;
  cohort: string;
}
export const DIVERGENCE_FLAG_THRESHOLD = 15;
```
</interfaces>
</context>

<tasks>

<task id="130-02-01" type="auto" tdd="true">
  <name>Grouped divergence aggregation</name>
  <files>app/src/server/human-quality/calibration/aggregate.ts, app/tests/unit/human-quality/calibration/aggregate.test.ts</files>
  <behavior>
    - aggregateGroup computes count, meanSignedDelta, meanAbsError, overScoreCount (delta > 15), underScoreCount (delta < -15)
    - groupComparisonsBy failure reason produces per-reason GroupSlice
    - groupComparisonsBy mode and format produce parallel slice maps
    - Composite slice key failureReason×mode×format available for adjustment plan 03
    - Empty input returns null means not zero misleading values
  </behavior>
  <action>
    Implement `GroupSlice` and `aggregateGroup` per RESEARCH Pattern 2. Add `groupComparisonsBy(comparisons, key)` for `primaryFailureReason`, `generationMode`, `format`. Add `buildCompositeSliceKey(reason, mode, format)` for CALIB-03 slice grouping. Use `DIVERGENCE_FLAG_THRESHOLD` from compare module for over/under counts. Pure TS reducers only — no stats library.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/aggregate.test.ts -t "group|aggregateGroup"</automated>
  </verify>
  <done>Systematic divergences group by failure type, mode and format with MAE and bias per slice.</done>
</task>

<task id="130-02-02" type="auto" tdd="true">
  <name>Factual bucket and guard list</name>
  <files>app/src/server/human-quality/calibration/aggregate.ts, app/tests/unit/human-quality/calibration/aggregate.test.ts</files>
  <behavior>
    - factualMetrics.factualPassRate computed independently from visual MAE
    - highVisualButFactualFail includes items where factualPass=false AND both scores >= 70
    - No overallQualityPass or blended pass field exists in report type
    - factual_issue human reason items never improve visual pass metrics
  </behavior>
  <action>
    Implement `highVisualButFactualFail(comparisons, visualThreshold=70)` per RESEARCH CALIB-04 example. Add `buildFactualMetrics(comparisons)` returning `{ factualPassRate, factualFailCount, highVisualButFactualFail }`. Per CONTEXT D-04 (locked): factual pass rate cannot offset visual calibration conclusions — do NOT compute any combined pass metric. Mirror Phase 128 `qualityMetrics`/`factualMetrics` separation in type design.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/aggregate.test.ts -t "factual|highVisual"</automated>
  </verify>
  <done>CALIB-04 factual separation enforced with explicit high-visual-but-factual-fail guard list.</done>
</task>

<task id="130-02-03" type="auto" tdd="true">
  <name>CalibrationReport builder with corpus minimum</name>
  <files>app/src/server/human-quality/calibration/report.ts, app/tests/unit/human-quality/calibration/aggregate.test.ts</files>
  <behavior>
    - evaluatedItemCount &lt; 5 → status insufficient_corpus, visual metrics null where appropriate
    - evaluatedItemCount >= 5 → status ok with full visualMetrics and factualMetrics
    - Report includes rubricCalibrationVersion, capturedAt, schemaVersion: 1
    - visualMetrics contains comparisons array, divergenceByFailureReason/Mode/Format, meanAbsError, meanSignedDelta
    - adjustments array present but empty (populated in plan 03)
  </behavior>
  <action>
    Export `MIN_GLOBAL_EVALUATED_ITEMS = 5` and `MIN_SLICE_SAMPLE = 3` constants per CONTEXT D-02/D-03. Implement `buildCalibrationReport({ comparisons, capturedAt, rubricCalibrationVersion })` assembling aggregate slices and factual bucket. When `comparisons.length &lt; MIN_GLOBAL_EVALUATED_ITEMS`, set `status: 'insufficient_corpus'` honestly — do not fabricate aggregates. Include `snapshotCapturedAt` metadata note in report header fields. Export `RUBRIC_CALIBRATION_VERSION = "1.0.0"` constant for adjustment registry.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/aggregate.test.ts -t "buildCalibrationReport|insufficient"</automated>
  </verify>
  <done>Full CalibrationReport schema builds with ok/insufficient_corpus states and metric separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report builder → consumers | Report JSON consumed by CLI, API, UI — schema must not leak prompts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-03 | E | buildCalibrationReport | mitigate | No `overallPass` field; schema test asserts visualMetrics/factualMetrics isolation |
| T-130-04 | D | insufficient_corpus status | mitigate | Hard minimum 5 items before status ok — prevents false calibration claims |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/calibration/aggregate.test.ts`
- `cd app && npm run lint -- src/server/human-quality/calibration`
</verification>

<success_criteria>
- CALIB-02: Report groups divergences by failure type, mode and format.
- CALIB-04: Factual fidelity measured separately; highVisualButFactualFail guard prevents visual offset of factual failure.
</success_criteria>

<output>
After completion, create `.planning/phases/130-score-calibration-and-rubric-alignment/130-02-SUMMARY.md`.
</output>
