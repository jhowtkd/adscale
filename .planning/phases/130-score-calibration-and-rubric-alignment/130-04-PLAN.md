---
phase: 130-score-calibration-and-rubric-alignment
plan: "04"
type: execute
wave: 4
depends_on: ["130-03"]
files_modified:
  - app/scripts/run-score-calibration.ts
  - app/scripts/check-score-calibration-evidence.mjs
  - .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json
  - app/package.json
  - app/src/server/auth/calibration-access.ts
  - app/src/app/api/feedback/score-calibration/route.ts
  - app/src/app/api/feedback/score-calibration/route.test.ts
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
autonomous: false
requirements:
  - CALIB-01
  - CALIB-02
  - CALIB-03
  - CALIB-04
user_setup: []
must_haves:
  truths:
    - "CLI generates 130-EVIDENCE.json with global multi-workspace rollup and cohort filter"
    - "Evidence checker validates schema and visual/factual metric separation for CI"
    - "Owner/feedback UI shows read-only calibration tab with MAE, bias, slice counts and per-item drill-down"
    - "Platform-owner and workspace admin can access calibration report API"
  artifacts:
    - path: app/scripts/run-score-calibration.ts
      provides: CLI evidence generation
    - path: app/scripts/check-score-calibration-evidence.mjs
      provides: CI schema and metric separation validator
    - path: .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json
      provides: evidence schema contract
    - path: app/src/app/api/feedback/score-calibration/route.ts
      provides: GET calibration report API
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: calibration tab in existing panel
  key_links:
    - from: app/scripts/run-score-calibration.ts
      to: runScoreCalibration
      via: tsx script with --all-workspaces and --cohort flags
      pattern: "runScoreCalibration"
    - from: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      to: /api/feedback/score-calibration
      via: read-only fetch on calibration tab
      pattern: "score-calibration"
    - from: app/scripts/check-score-calibration-evidence.mjs
      to: 130-EVIDENCE.template.json
      via: schema validation + no blended metrics assertion
      pattern: "visualMetrics|factualMetrics"
---

# Plan 130-04: Evidence CLI, Calibration API and Read-Only UI Tab

<objective>
Ship CLI evidence generation (mirror Phase 128 pattern), CI evidence checker, GET calibration API with platform-owner/workspace-admin auth, and read-only calibration tab on HumanQualityCorpusPanel (CONTEXT D-01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-score-calibration-and-rubric-alignment/130-CONTEXT.md
@.planning/phases/130-score-calibration-and-rubric-alignment/130-RESEARCH.md
@app/scripts/check-output-learning-evidence.mjs
@app/src/server/human-quality/calibration/service.ts
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/src/server/auth/platform-owner.ts
@app/src/server/auth/workspace.ts

<interfaces>
From calibration/service.ts (Plan 130-03):
```typescript
export async function runScoreCalibration(options?: {
  workspaceId?: string;
  cohort?: HumanQualityCorpusCohort;
}): Promise<CalibrationReport>
```

From HumanQualityCorpusPanel (existing queue tab pattern):
```typescript
export function HumanQualityCorpusPanel()
// Uses apiFetch, useQuery, workspaceId state
```
</interfaces>
</context>

<tasks>

<task id="130-04-01" type="auto">
  <name>CLI evidence script and checker</name>
  <files>app/scripts/run-score-calibration.ts, app/scripts/check-score-calibration-evidence.mjs, .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json, app/package.json</files>
  <action>
    Create `run-score-calibration.ts` mirroring Phase 128 evidence flow: flags `--out` (default `../.planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.json`), `--all-workspaces` (global rollup per D-03), `--workspace-id`, `--cohort`. Call `runScoreCalibration`, write JSON with `verifiedAt` timestamp. Create `130-EVIDENCE.template.json` with ok and insufficient_corpus example shapes. Create `check-score-calibration-evidence.mjs` validating: schemaVersion, rubricCalibrationVersion, separate visualMetrics/factualMetrics (reject overallPass/blended fields), requirements array mapping CALIB-01–04, optional `--skip-tests` running calibration unit tests. Add npm script `score-calibration-evidence` in package.json.
  </action>
  <verify>
    <automated>node app/scripts/check-score-calibration-evidence.mjs --evidence .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>CI-checkable evidence template validates; CLI can emit milestone evidence JSON.</done>
</task>

<task id="130-04-02" type="auto" tdd="true">
  <name>Calibration API with dual auth</name>
  <files>app/src/server/auth/calibration-access.ts, app/src/app/api/feedback/score-calibration/route.ts, app/src/app/api/feedback/score-calibration/route.test.ts</files>
  <behavior>
    - Platform-owner can GET global rollup without workspaceId
    - Workspace admin can GET with their workspace scoped or global per auth helper
    - Non-admin member receives 403
    - Response caps comparisons array (paginate or summarize if >100 items) for DoS mitigation
    - Query params: workspaceId optional, cohort optional
  </behavior>
  <action>
    Add `requireCalibrationAccess(request)` allowing `isPlatformOwnerEmail` OR workspace role owner/admin via `requireWorkspaceAccess` + `requireRole`. GET `/api/feedback/score-calibration` calls `runScoreCalibration` with parsed filters. Zod-validate query params. Cap `visualMetrics.comparisons` in API response to 100 items with `truncated: true` flag when exceeded — full data remains in CLI JSON. Per CONTEXT D-01: broader than Phase 129 platform-owner-only corpus queue.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/score-calibration/route.test.ts</automated>
  </verify>
  <done>Calibration report accessible to platform-owner and workspace admin with bounded payload.</done>
</task>

<task id="130-04-03" type="auto" tdd="true">
  <name>Read-only calibration tab on corpus panel</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <behavior>
    - Panel has Queue and Calibration tabs (layout at implementer discretion per D discretion)
    - Calibration tab shows status, evaluatedItemCount, MAE, signed bias, slice summary tables
    - Per-item drill-down: auto score, human score, delta, failure reason, factual pass
    - insufficient_corpus shows honest message not empty crash
    - Tab is read-only — no evaluation form on calibration tab
  </behavior>
  <action>
    Extend `HumanQualityCorpusPanel` with calibration tab per CONTEXT D-01 — do NOT create separate top-level panel. Fetch `/api/feedback/score-calibration` with optional workspaceId + cohort filter matching queue workspace input. Remove or update "no calibration claims yet" copy. Display `highVisualButFactualFail` count in factual section. Show proposed adjustments list (read-only). Replace insufficient_corpus with clear operator messaging.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -t "calibration"</automated>
  </verify>
  <done>Operator can audit calibration in UI matching CLI report shape — read-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify calibration UI and CLI evidence</name>
  <files>.planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json, app/src/components/feedback/HumanQualityCorpusPanel.tsx</files>
  <action>
    Operator verifies automated tests pass, evidence checker validates template, and calibration tab renders aggregate + drill-down correctly.

    Automated pre-check (Claude runs before pausing):
    1. `cd app && npm test -- tests/unit/human-quality/calibration src/app/api/feedback/score-calibration src/components/feedback/HumanQualityCorpusPanel.test.tsx`
    2. `node app/scripts/check-score-calibration-evidence.mjs --evidence .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json --skip-tests`

    Operator manual steps:
    3. If DATABASE_URL has ≥5 evaluated items: `cd app && npx tsx scripts/run-score-calibration.ts --all-workspaces --out ../.planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.json` — confirm status `ok`
    4. As platform-owner, open owner/feedback → Human Quality Corpus → Calibration tab; confirm MAE, signed bias, slice tables and per-item deltas render; insufficient_corpus shows honest message

    Resume signal: Type "approved" or describe UI/CLI issues.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration && cd .. && node app/scripts/check-score-calibration-evidence.mjs --evidence .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json --skip-tests</automated>
  </verify>
  <done>Operator confirms CLI evidence and read-only calibration tab match report schema; automated tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /api/feedback/score-calibration | Untrusted query params; auth required |
| CLI → DATABASE_URL | Server-side only — no secrets in evidence JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-07 | I | Global rollup API | mitigate | requireCalibrationAccess; platform-owner or workspace admin only |
| T-130-08 | D | Large comparisons payload | mitigate | Cap API comparisons at 100 with truncated flag |
| T-130-09 | I | Evidence JSON | mitigate | No prompts/signed URLs; IDs and bounded metrics only |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/calibration`
- `node app/scripts/check-score-calibration-evidence.mjs --evidence .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.template.json`
- `cd app && npm run lint && npm run build`
</verification>

<success_criteria>
- CALIB-01–04: End-to-end calibration auditable via CLI evidence and read-only UI.
- Phase 130 does not edit gate/rubric/prompt source files.
- Global rollup with cohort filter and min-5 ok status enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/130-score-calibration-and-rubric-alignment/130-04-SUMMARY.md`.
</output>
