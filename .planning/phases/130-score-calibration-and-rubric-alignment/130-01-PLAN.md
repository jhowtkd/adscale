---
phase: 130-score-calibration-and-rubric-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/calibration/types.ts
  - app/src/server/human-quality/calibration/compare.ts
  - app/src/server/human-quality/calibration/failure-bridge.ts
  - app/src/server/repositories/human-quality-corpus.ts
  - app/tests/unit/human-quality/calibration/compare.test.ts
  - app/tests/unit/human-quality/calibration/failure-bridge.test.ts
  - app/tests/unit/human-quality/calibration-evaluated-repository.test.ts
autonomous: true
requirements:
  - CALIB-01
must_haves:
  truths:
    - "Every evaluated corpus item yields automatic vs human visual score comparison from frozen qualitySnapshot"
    - "Comparison uses selection-time automatic score — never live re-scoring"
    - "Global rollup can load evaluated items across workspaces with optional cohort filter"
  artifacts:
    - path: app/src/server/human-quality/calibration/compare.ts
      provides: per-item CalibrationComparison builder
      exports: ["buildComparison", "buildCalibrationComparisons"]
    - path: app/src/server/human-quality/calibration/failure-bridge.ts
      provides: human failure reason to gate/rubric target mapping
    - path: app/src/server/repositories/human-quality-corpus.ts
      provides: listEvaluatedCorpusWithEvaluations global join query
  key_links:
    - from: app/src/server/repositories/human-quality-corpus.ts
      to: human_quality_corpus_items + human_quality_evaluations
      via: Drizzle innerJoin on corpus_item_id where status = evaluated
      pattern: "innerJoin.*humanQualityEvaluations"
    - from: app/src/server/human-quality/calibration/compare.ts
      to: qualitySnapshot.qualityScore
      via: buildComparison reads frozen snapshot not live derivation
      pattern: "qualitySnapshot.*qualityScore"
---

# Plan 130-01: Evaluated Corpus Join and Per-Item Score Comparison

<objective>
Add repository join for evaluated corpus rows and pure comparison functions that diff frozen `qualitySnapshot.qualityScore` against human `visualScore` for every evaluated item (CALIB-01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-score-calibration-and-rubric-alignment/130-CONTEXT.md
@.planning/phases/130-score-calibration-and-rubric-alignment/130-RESEARCH.md
@.planning/phases/130-score-calibration-and-rubric-alignment/130-VALIDATION.md
@app/src/server/human-quality/corpus.ts
@app/src/server/repositories/human-quality-corpus.ts
@app/src/server/db/schema.ts

<interfaces>
From `corpus.ts`:
```typescript
export interface HumanQualityQualitySnapshot {
  qualityScore?: number | null;
  qualityVerdict?: string | null;
  hardFailures?: { code?: string; message?: string }[];
}
export type HumanQualityFailureReason = /* visual_overload | weak_hierarchy | ... | other */;
```

From repository (existing):
```typescript
export async function listPendingCorpusItems(filters: { workspaceId: string; limit?: number })
export async function submitCorpusEvaluation(input: SubmitCorpusEvaluationInput)
```
</interfaces>
</context>

<tasks>

<task id="130-01-01" type="auto" tdd="true">
  <name>Define calibration comparison contracts</name>
  <files>app/src/server/human-quality/calibration/types.ts, app/src/server/human-quality/calibration/compare.ts, app/tests/unit/human-quality/calibration/compare.test.ts</files>
  <behavior>
    - buildComparison: auto=80 human=65 → scoreDelta=15 absError=15
    - buildComparison: auto=null human=70 → scoreDelta=null absError=null
    - buildCalibrationComparisons: maps array of joined rows preserving generationMode, format, cohort, factualPass, primaryFailureReason
    - scoreDelta = automaticQualityScore - humanVisualScore (positive = auto over-scores)
  </behavior>
  <action>
    Create `CalibrationComparison` type per RESEARCH Pattern 1. Implement `buildComparison` and `buildCalibrationComparisons` reading `qualitySnapshot.qualityScore` only — do NOT call `analyzeDerivationCreative` or re-score derivations (per D-04 phase boundary). Export `DIVERGENCE_FLAG_THRESHOLD = 15` constant for downstream plans. Attach `hardFailureCodes` from snapshot `hardFailures[].code` bounded list.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/compare.test.ts</automated>
  </verify>
  <done>Per-item auto vs human comparison is pure, testable, and uses frozen snapshot scores only.</done>
</task>

<task id="130-01-02" type="auto" tdd="true">
  <name>Add human-to-gate failure bridge</name>
  <files>app/src/server/human-quality/calibration/failure-bridge.ts, app/tests/unit/human-quality/calibration/failure-bridge.test.ts</files>
  <behavior>
    - visual_overload maps to gate codes including visual_overload
    - factual_issue maps only to FIDELITY_HARD_FAILURE_CODES targets (factual bucket)
    - other returns empty adjustment targets (report-only)
    - humanGateMismatch when human says factual_issue but snapshot has no fidelity code
  </behavior>
  <action>
    Implement explicit map from `HUMAN_QUALITY_FAILURE_REASONS` to gate/rubric target keys per RESEARCH Pattern 3 table. Import `FIDELITY_HARD_FAILURE_CODES` from `creative-validation-aggregation.ts` for factual_issue. Export `resolveGateTargets(reason)` and `detectHumanGateMismatch(comparison)` helpers. Do not edit gate classifiers or rubric prose — mapping only.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/failure-bridge.test.ts</automated>
  </verify>
  <done>Human failure reasons bridge to gate/rubric targets without conflating factual and visual buckets.</done>
</task>

<task id="130-01-03" type="auto" tdd="true">
  <name>Repository join for evaluated corpus globally</name>
  <files>app/src/server/repositories/human-quality-corpus.ts, app/tests/unit/human-quality/calibration-evaluated-repository.test.ts</files>
  <behavior>
    - listEvaluatedCorpusWithEvaluations returns only status=evaluated rows with evaluation join
    - Global rollup: omit workspaceId filter aggregates all workspaces
    - Optional workspaceId filter scopes to one workspace
    - Optional cohort filter narrows to baseline|pre_learning|post_learning
    - Pending items never appear in results
  </behavior>
  <action>
    Add `EvaluatedCorpusRow` type (item + evaluation). Implement `listEvaluatedCorpusWithEvaluations({ workspaceId?, cohort?, limit? })` using Drizzle `innerJoin` on `corpus_item_id`, `where status = 'evaluated'`, order by `selectedAt desc`. Per CONTEXT D-03: primary scope is global multi-workspace rollup — `workspaceId` is optional filter not required param. Cap default limit (e.g. 500) to prevent unbounded payloads. Mock DB in tests following Phase 129 repository test pattern.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration-evaluated-repository.test.ts</automated>
  </verify>
  <done>Evaluated corpus items join evaluations for calibration with global rollup and cohort filter support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Postgres | Evaluated corpus rows may contain bounded snapshots — no prompt expansion |
| Comparison builder | Pure function — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | I | listEvaluatedCorpusWithEvaluations | mitigate | Optional workspaceId filter; global rollup only via server-side query never client-supplied SQL |
| T-130-02 | I | qualitySnapshot in comparisons | mitigate | Reuse sanitizeCorpusPayloads contract; report cites IDs not raw prompts |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/calibration`
- `cd app && npm run lint -- src/server/human-quality/calibration src/server/repositories/human-quality-corpus.ts`
</verification>

<success_criteria>
- CALIB-01: Every evaluated corpus item can compare automatic quality score with human visual score.
- Frozen snapshot comparison — no live re-scoring introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/130-score-calibration-and-rubric-alignment/130-01-SUMMARY.md`.
</output>
