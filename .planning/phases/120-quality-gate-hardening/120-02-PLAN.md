---
phase: 120-quality-gate-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/ai/creative-quality-gate.ts
  - app/src/server/ai/factual-visual-separation.ts
  - app/tests/unit/ai/creative-quality-gate.test.ts
  - app/src/server/ai/prompt-builder.test.ts
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
must_haves:
  truths:
    - "Severe overload, generic template, and missing-dominant-idea notes promote to hard failures before polish fallback"
    - "campaign_identity_drift promotes from briefMatch/formatFit in format_adaptation before default invalid_format_layout"
    - "styleFidelity failed in restyling emits style_reference_contamination (not copied_style_reference_facts)"
    - "Only checklist status failed promotes; warning rows stay polish-only (GATE-05 guard)"
    - "hardFailures.length > 0 yields invalid even when qualityScore is 85 (GATE-02)"
    - "Mild generic creativeRisk failed stays polish-only; severe template markers hard-fail (GATE-03)"
  artifacts:
    - path: app/src/server/ai/creative-quality-gate.ts
      provides: promote-before-polish classifiers with contract-aware mode guards
      exports:
        - classifyCreativeQualityGate
        - deriveQualityVerdict
    - path: app/src/server/ai/factual-visual-separation.ts
      provides: CONTAMINATION_FAILURE_CODES including new factual/aesthetic contamination codes
  key_links:
    - from: classifyCreativeRiskFailed
      to: creative-quality-taxonomy.ts + observable-rubric markers
      via: noteMatches before pushUnique(polishSuggestions)
      pattern: pushHardFailure.*visual_overload|generic_template_aesthetic
    - from: classifyBriefMatchFailed / formatFit branch
      to: CAMPAIGN_IDENTITY_DRIFT_PATTERN
      via: check drift before invalid_format_layout
      pattern: campaign_identity_drift
    - from: deriveQualityVerdict
      to: hardFailures array
      via: length > 0 → invalid regardless of qualityScore
      pattern: hardFailures\.length > 0
---

# Plan 120-02: Classifier Refactor and CONTAMINATION_FAILURE_CODES (GATE-01–03)

<objective>
Wire GATE-01 patterns into the quality gate classifiers with promote-before-polish ordering, mode guards, and extended contamination codes — so aesthetic and factual defects block export independent of score.

Purpose: Close the enforcement gap where `classifyCreativeRiskFailed` sent overload/generic notes to `polishSuggestions`.

Output: Refactored gate classifiers + CONTAMINATION_FAILURE_CODES + unit tests for promotion paths.

**Out of scope:** Corpus fixture alignment and BASELINE_GAP_COUNT flip (Plan 03); score ceilings (Phase 121); full gate matrix suite (Phase 122); live vision validation (Phase 123).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/120-quality-gate-hardening/120-01-SUMMARY.md
</execution_context>

<context>
@.planning/phases/120-quality-gate-hardening/120-RESEARCH.md
@.planning/phases/120-quality-gate-hardening/120-VALIDATION.md
@app/src/server/ai/creative-quality-gate.ts
@app/src/server/ai/creative-quality-taxonomy.ts
@app/src/server/ai/factual-visual-separation.ts
@app/src/server/ai/corpus-fixtures.ts
@app/tests/unit/ai/creative-quality-gate.test.ts

<interfaces>
From Plan 01 — classifier signature changes:
```typescript
function classifyCreativeRiskFailed(
  hardFailures: CreativeHardFailure[],
  polishSuggestions: string[],
  contract: CreativeContract,  // NEW — mode guards
  note: string
): void

function classifyBriefMatchFailed(
  hardFailures: CreativeHardFailure[],
  contract: CreativeContract,  // NEW — format_adaptation drift
  note: string
): void
```

Promotion priority in `classifyCreativeRiskFailed` (after existing factual paths):
1. OVERLOAD_NOTE_MARKERS → `visual_overload`
2. GENERIC_TEMPLATE_NOTE_MARKERS → `generic_template_aesthetic`
3. MISSING_DOMINANT_IDEA_MARKERS → `missing_dominant_idea`
4. DECORATIVE_ONLY_PATTERN + `contract.generationMode === "art_variation"` → `decorative_only_variation`
5. STYLE_REFERENCE_CONTAMINATION_PATTERN → `style_reference_contamination` (replace `copied_style_reference_facts` emit)
6. else → `pushUnique(polishSuggestions, note)`

`classifyBriefMatchFailed` additions:
- UNAUTHORIZED_BRAND_PATTERN → `unauthorized_brand_or_ip`
- REPLACED_SOURCE_SUBJECT_PATTERN → `replaced_source_subject`
- CAMPAIGN_IDENTITY_DRIFT_PATTERN (format_adaptation only, skip if CAMPAIGN_IDENTITY_SAFE_PATTERN matches) → `campaign_identity_drift`

`formatFit failed` branch: check CAMPAIGN_IDENTITY_DRIFT_PATTERN before unconditional `invalid_format_layout`.

`styleFidelity failed`: emit `style_reference_contamination` (canonical GATE-01 name).

`CONTAMINATION_FAILURE_CODES` extend with:
`style_reference_contamination`, `campaign_identity_drift`, `replaced_source_subject`, `unauthorized_brand_or_ip`
Keep `copied_style_reference_facts` in set during transition.
</interfaces>
</context>

<tasks>

<task id="120-02-01" type="auto" tdd="true">
  <name>Refactor gate classifiers with promote-before-polish and mode guards</name>
  <files>app/src/server/ai/creative-quality-gate.ts</files>
  <behavior>
    - creativeRisk failed + overload note → visual_overload hard failure, not polish
    - creativeRisk failed + severe generic template note → generic_template_aesthetic hard failure
    - creativeRisk failed + mild "Visual feels generic" → polish only (existing test preserved)
    - briefMatch failed + campaign drift note in format_adaptation → campaign_identity_drift
    - formatFit failed + drift note → campaign_identity_drift before invalid_format_layout
    - styleFidelity failed in restyling → style_reference_contamination
    - creativeRisk warning → polish only, never hard failure
    - decorative_only_variation only when generationMode is art_variation
  </behavior>
  <action>
Refactor `creative-quality-gate.ts` per RESEARCH Pattern 1–3:

1. **Add `contract: CreativeContract` param** to `classifyCreativeRiskFailed` and `classifyBriefMatchFailed`; update all call sites in `classifyCreativeQualityGate`.

2. **`classifyCreativeRiskFailed`** — after existing `unsupported_offer`, `invented_factual_entity`, and style-reference checks, promote in order:
   - `OVERLOAD_NOTE_MARKERS` → `visual_overload`
   - `GENERIC_TEMPLATE_NOTE_MARKERS` → `generic_template_aesthetic`
   - `MISSING_DOMINANT_IDEA_MARKERS` → `missing_dominant_idea`
   - `DECORATIVE_ONLY_PATTERN` when `contract.generationMode === "art_variation"` → `decorative_only_variation`
   - Broadened `STYLE_REFERENCE_CONTAMINATION_PATTERN` → `style_reference_contamination` (stop emitting `copied_style_reference_facts`)
   - Only then `pushUnique(polishSuggestions, note)`

3. **`classifyBriefMatchFailed`** — add before existing returns:
   - `UNAUTHORIZED_BRAND_PATTERN` → `unauthorized_brand_or_ip`
   - `REPLACED_SOURCE_SUBJECT_PATTERN` → `replaced_source_subject`
   - When `contract.generationMode === "format_adaptation"`: if `CAMPAIGN_IDENTITY_DRIFT_PATTERN` matches AND `CAMPAIGN_IDENTITY_SAFE_PATTERN` does NOT → `campaign_identity_drift`

4. **`formatFit failed` branch** (format_adaptation): check campaign drift pattern first; emit `campaign_identity_drift` when narrative identity fails; fall back to `invalid_format_layout` only for layout-specific notes (blur band, aspect ratio).

5. **`styleFidelity failed`**: change emitted code from `copied_style_reference_facts` to `style_reference_contamination`.

6. **`classifyScoreIssue`**: extend promotion for new aesthetic/factual patterns (overload, generic, drift, decorative) using same patterns and mode guards.

7. **Gate invariant**: only process `status === "failed"` rows for promotion — warnings flow through `collectChecklistWarnings` to polish only (per locked decision: only failed promotes).

Import new patterns from `creative-quality-taxonomy.ts`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "generic|overload|drift|style_reference|decorative"</automated>
  </verify>
  <done>All corpus archetype note exemplars promote to canonical GATE-01 codes; mild generic stays polish; warnings never hard-fail.</done>
</task>

<task id="120-02-02" type="auto" tdd="true">
  <name>Extend CONTAMINATION_FAILURE_CODES and GATE-02/03 regression tests</name>
  <files>app/src/server/ai/factual-visual-separation.ts, app/tests/unit/ai/creative-quality-gate.test.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - CONTAMINATION_FAILURE_CODES includes style_reference_contamination, campaign_identity_drift, invented_factual_entity, replaced_source_subject, unauthorized_brand_or_ip
    - deriveQualityVerdict with hardFailures + qualityScore 85/92 → invalid (GATE-02)
    - Severe generic template note → hard failure, not polish (GATE-03)
    - "keeps subjective creativeRisk failed as polish only" test updated: mild generic stays polish; add sibling test for severe template
  </behavior>
  <action>
1. **`factual-visual-separation.ts`** — extend `CONTAMINATION_FAILURE_CODES` with:
   `style_reference_contamination`, `campaign_identity_drift`, `replaced_source_subject`, `unauthorized_brand_or_ip`
   Retain `copied_style_reference_facts` for legacy persisted rows.

2. **`creative-quality-gate.test.ts`** updates:
   - **GATE-03**: Add test — creativeRisk failed with corpus generic note ("Generic premium-tech neon template aesthetic...") → `generic_template_aesthetic` hard failure, zero polish for that note.
   - **GATE-03**: Keep/update mild generic test — "Visual feels generic and risky" → polish only, no hard failures.
   - **GATE-02**: Existing `deriveQualityVerdict` tests with qualityScore 88/92 remain green.
   - Add overload promotion test using corpus visual_overload note exemplar.
   - Add campaign_identity_drift test using format_campaign_drift fixture note.
   - Update styleFidelity test to expect `style_reference_contamination` (not `copied_style_reference_facts`).
   - Add decorative_only_variation test for art_variation mode only.

3. **`prompt-builder.test.ts`** — extend `CONTAMINATION_FAILURE_CODES includes lineage hard-failure codes` to assert new codes.

Use `CORPUS_ARCHETYPE_FIXTURES` notes where possible for GATE-04 traceability (full baseline flip in Plan 03).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts tests/unit/ai/prompt-builder.test.ts -t "CONTAMINATION|high qualityScore|generic|overload"</automated>
  </verify>
  <done>CONTAMINATION_FAILURE_CODES extended; GATE-02 and GATE-03 behaviors verified by automated tests; styleFidelity emits canonical code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA checklist → classifyCreativeQualityGate | Untrusted model notes; server-side pattern matching only |
| hardFailures → assertDerivationApprovable | Export block depends on correct promotion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-03 | Tampering | classifyCreativeRiskFailed polish fallback | mitigate | Promote overload/generic before pushUnique |
| T-120-04 | Tampering | False negative on campaign drift | mitigate | Drift pattern before invalid_format_layout default |
| T-120-05 | Tampering | Parent contamination in format_adaptation | mitigate | CONTAMINATION_FAILURE_CODES includes new factual codes |
| T-120-06 | Tampering | Over-broad generic promotion (GATE-05) | mitigate | Severe markers only; failed status only; safe-pattern negation on drift |
</threat_model>

<verification>
Per @.planning/phases/120-quality-gate-hardening/120-VALIDATION.md:
`cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts tests/unit/ai/prompt-builder.test.ts`
</verification>

<success_criteria>
- GATE-01: Classifiers emit all nine GATE-01 codes from representative failed notes
- GATE-02: hardFailures + qualityScore 85 → invalid (existing deriveQualityVerdict path)
- GATE-03: Severe generic template blocks export; mild generic remains polish
</success_criteria>

<output>
After completion, create `.planning/phases/120-quality-gate-hardening/120-02-SUMMARY.md`
</output>
