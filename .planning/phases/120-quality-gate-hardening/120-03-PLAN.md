---
phase: 120-quality-gate-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - app/src/server/ai/corpus-fixtures.ts
  - app/tests/unit/ai/corpus-baseline.test.ts
  - app/tests/unit/ai/creative-quality-gate.test.ts
autonomous: true
requirements:
  - GATE-04
  - GATE-05
must_haves:
  truths:
    - "All five negative corpus archetypes reject with expected canonical GATE-01 codes"
    - "BASELINE_GAP_COUNT is 0; no it.fails in corpus baseline suite"
    - "Corpus IDs 27069645, a753e357, 538246da, a5f65b85, f420bcb2, d7d9d323 linked to blocking fixtures"
    - "Faithful c2c12774 fixture produces no hard failures and assertDerivationApprovable ok"
    - "Mild creativeRisk warning on faithful piece does not promote to generic_template_aesthetic or visual_overload"
  artifacts:
    - path: app/src/server/ai/corpus-fixtures.ts
      provides: aligned expected codes, baselineVerdicts, f420bcb2 ref, faithful c2c12774 fixture
    - path: app/tests/unit/ai/corpus-baseline.test.ts
      provides: BASELINE_GAP_COUNT=0, all archetypes use it (not it.fails)
    - path: app/tests/unit/ai/creative-quality-gate.test.ts
      provides: GATE-05 faithful guard tests
  key_links:
    - from: corpus-baseline.test.ts
      to: classifyCreativeQualityGate + deriveQualityVerdict
      via: runCorpusGatePipeline with qualityScore 85
      pattern: expect\(verdict\)\.toBe\("invalid"\)
    - from: corpus-faithful-format-adaptation fixture
      to: assertDerivationApprovable
      via: empty hardFailures + non-invalid verdict
      pattern: assertDerivationApprovable.*ok.*true
---

# Plan 120-03: Corpus Alignment, Faithful Fixture, Baseline Flip (GATE-04–05)

<objective>
Align corpus fixtures to GATE-01 canonical codes, add the faithful `c2c12774` positive fixture, and flip `BASELINE_GAP_COUNT` to 0 so all corpus baseline tests pass green.

Purpose: GATE-04 corpus audit IDs are blocked; GATE-05 faithful piece remains export-approvable with optional simplification warnings.

Output: Updated fixtures + baseline harness + false-positive guard tests.

**Out of scope:** Score ceilings (Phase 121); full regression suite (Phase 122); live vision re-generation (Phase 123); quality-fixtures regeneration brief updates (optional 120-04 deferred).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@.planning/phases/120-quality-gate-hardening/120-02-SUMMARY.md
</execution_context>

<context>
@.planning/phases/120-quality-gate-hardening/120-RESEARCH.md
@.planning/phases/120-quality-gate-hardening/120-VALIDATION.md
@app/src/server/ai/corpus-fixtures.ts
@app/tests/unit/ai/corpus-baseline.test.ts
@app/tests/unit/ai/creative-quality-gate.test.ts

<interfaces>
GATE-04 corpus audit IDs (must be in fixture corpusRefIds):
- `27069645`, `a753e357` — invented_factual_entity / format drift
- `538246da` — format_campaign_drift
- `a5f65b85`, `f420bcb2`, `d7d9d323` — restyling contamination

Fixture alignment (per RESEARCH flip checklist):
| Archetype | baselineVerdict change | expectedHardFailureCodes change |
|-----------|------------------------|--------------------------------|
| visual_overload | acceptable → invalid | unchanged: visual_overload |
| generic_template_aesthetic | acceptable → invalid | unchanged: generic_template_aesthetic |
| format_campaign_drift | invalid (unchanged) | format_campaign_drift → campaign_identity_drift (+ wrong_brand if note matches) |
| restyling_factual_contamination | invalid (unchanged) | → [style_reference_contamination] single canonical code |

Faithful fixture (GATE-05):
```typescript
{
  id: "corpus-faithful-format-adaptation",
  corpusRefIds: ["c2c12774"],
  canonicalSlug: "teste-3-nr1",
  contract: nr1FormatAdaptationContract({ targetFormat: "4:5" }),
  expectedHardFailureCodes: [],
  expectedVerdict: "improvable", // creativeRisk warning allowed; assertDerivationApprovable ok
  baselineVerdict: "improvable",
}
```

Baseline harness target:
```typescript
export const BASELINE_GAP_COUNT = 0;
const runBaselineRejectionTest = it; // remove invented_factual_entity ternary
```
</interfaces>
</context>

<tasks>

<task id="120-03-01" type="auto">
  <name>Align corpus fixtures and add faithful c2c12774 positive fixture</name>
  <files>app/src/server/ai/corpus-fixtures.ts</files>
  <action>
In `corpus-fixtures.ts`:

1. **Update `CorpusTargetHardFailureCode`** — add canonical GATE-01 codes; keep legacy aliases only if needed for transitional typing.

2. **Negative archetype updates:**
   - `corpus-visual-overload`: `baselineVerdict: "invalid"` (was acceptable)
   - `corpus-generic-template-aesthetic`: `baselineVerdict: "invalid"` (was acceptable)
   - `corpus-format-campaign-drift`: `expectedHardFailureCodes: ["campaign_identity_drift", "wrong_brand"]` (replace `format_campaign_drift`; keep `wrong_brand` only if briefMatch note matches WRONG_BRAND_PATTERN — if not, narrow to `["campaign_identity_drift"]` per RESEARCH open question)
   - `corpus-restyling-factual-contamination`: add `f420bcb2` to `corpusRefIds`; `expectedHardFailureCodes: ["style_reference_contamination"]` (single canonical code)

3. **GATE-04 ID coverage** — verify all six audit IDs appear in at least one fixture `corpusRefIds`:
   `27069645`, `a753e357`, `538246da`, `a5f65b85`, `f420bcb2`, `d7d9d323`

4. **Add faithful positive fixture** `corpus-faithful-format-adaptation`:
   - `corpusRefIds: ["c2c12774"]`
   - `canonicalSlug: "teste-3-nr1"`, `renderTier: "final"`
   - `contract: nr1FormatAdaptationContract({ targetFormat: "4:5" })`
   - `rawQaModelOutput`: briefMatch passed ("CENBRAP NR1 checklist narrative preserved"), formatFit passed ("Faithful NR1 format adaptation in native 4:5 layout"), creativeRisk **warning** ("badge row could be simplified; hook and CTA remain dominant")
   - `expectedHardFailureCodes: []`
   - `expectedVerdict: "improvable"` (warnings without hard failures — export-approvable per `assertDerivationApprovable`)
   - `baselineVerdict: "improvable"`

5. **Export array** — append faithful fixture to `CORPUS_ARCHETYPE_FIXTURES` OR add `CORPUS_POSITIVE_FIXTURES` exported alongside; baseline coverage test must include faithful fixture in suite if separate array.

Do NOT flip `corpus-baseline.test.ts` in this task — Plan 03 task 2 depends on classifiers from Plan 02 already passing against updated expectations.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-fixtures.test.ts</automated>
  </verify>
  <done>Fixture expected codes use GATE-01 canonical names; all six GATE-04 audit IDs linked; faithful c2c12774 fixture exists with empty expectedHardFailureCodes.</done>
</task>

<task id="120-03-02" type="auto" tdd="true">
  <name>Flip baseline harness and add GATE-05 faithful guard tests</name>
  <files>app/tests/unit/ai/corpus-baseline.test.ts, app/tests/unit/ai/creative-quality-gate.test.ts</files>
  <behavior>
    - BASELINE_GAP_COUNT === 0
    - All corpus archetypes use it (not it.fails); every archetype verdict invalid with expected codes
    - baseline-snapshot expects updated baselineVerdict for overload/generic
    - Faithful fixture: no hard failures, verdict improvable or acceptable, assertDerivationApprovable ok
    - Mild generic warning does not promote; faithful format note does not promote campaign_identity_drift
  </behavior>
  <action>
1. **`corpus-baseline.test.ts`**:
   - Set `BASELINE_GAP_COUNT = 0`
   - Replace `runBaselineRejectionTest` ternary with `const runBaselineRejectionTest = it`
   - Update baseline gap assertion test comment to Phase 120 complete
   - If faithful fixture added to CORPUS_ARCHETYPE_FIXTURES: add optional describe block OR extend ARCHETYPES — faithful fixture uses `it` expecting approvable path (verdict improvable/acceptable, empty hard failures) instead of invalid
   - Update `baseline-snapshot` expectations via fixture `baselineVerdict` values

2. **`creative-quality-gate.test.ts`** — add `describe("GATE-05 faithful format adaptation guard")`:
   - Pipeline faithful fixture with `qualityScore: 85` → `hardFailures.length === 0`
   - `assertDerivationApprovable({ qualityVerdict: gate verdict, hardFailures })` → `ok: true`
   - Mild generic **warning** (not failed) → no `generic_template_aesthetic`
   - `formatFit: passed` + "faithful NR1 format adaptation" → no `campaign_identity_drift`
   - `creativeRisk: warning` + "badge row could be simplified" → no `visual_overload`

3. **Primary audit ID test** — extend if needed so `f420bcb2` and `a5f65b85` both covered (fixture update from task 1).

Run full corpus + gate subset per 120-VALIDATION.md before marking done.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts tests/unit/ai/creative-quality-gate.test.ts -t "faithful|baseline gap"</automated>
  </verify>
  <done>BASELINE_GAP_COUNT is 0; all corpus baseline tests green; GATE-05 faithful guard tests pass; six GATE-04 audit IDs linked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus fixtures → gate pipeline | Synthetic QA JSON simulates model output; must mirror real audit observations |
| classifier tightening → faithful positives | GATE-05 guards prevent regression on c2c12774 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-07 | Tampering | False negative on corpus failures | mitigate | BASELINE_GAP_COUNT=0 with all archetypes invalid + expected codes |
| T-120-08 | Tampering | False positive on faithful c2c12774 | mitigate | Positive fixture + safe-pattern negation tests |
</threat_model>

<verification>
Per @.planning/phases/120-quality-gate-hardening/120-VALIDATION.md:
`cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts tests/unit/ai/creative-quality-gate.test.ts`
Phase gate: `cd app && npm test && npm run lint && npm run build`
</verification>

<success_criteria>
- GATE-04: All six corpus audit IDs blocked; five negative archetypes invalid with canonical codes; BASELINE_GAP_COUNT === 0
- GATE-05: c2c12774 faithful fixture approvable (no hard failures); simplification warnings do not invalidate
</success_criteria>

<output>
After completion, create `.planning/phases/120-quality-gate-hardening/120-03-SUMMARY.md`
</output>
