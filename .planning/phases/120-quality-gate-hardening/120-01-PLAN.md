---
phase: 120-quality-gate-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/creative-quality-taxonomy.ts
  - app/src/server/ai/creative-quality-gate.ts
  - app/messages/en.json
  - app/messages/pt-BR.json
  - app/tests/unit/ai/creative-quality-gate.test.ts
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - "CreativeHardFailureCode union includes all nine GATE-01 blocking categories"
    - "normalizeHardFailureCode maps legacy copied_style_reference_facts to style_reference_contamination"
    - "Taxonomy exports factual/drift/decorative patterns plus rubric marker re-exports from observable-rubric"
    - "en.json and pt-BR.json expose human labels for every GATE-01 code including invented_factual_entity"
  artifacts:
    - path: app/src/server/ai/creative-quality-taxonomy.ts
      provides: GATE-01 regex patterns and rubric marker re-exports
      exports:
        - CAMPAIGN_IDENTITY_DRIFT_PATTERN
        - CAMPAIGN_IDENTITY_SAFE_PATTERN
        - REPLACED_SOURCE_SUBJECT_PATTERN
        - DECORATIVE_ONLY_PATTERN
        - OVERLOAD_NOTE_MARKERS
        - GENERIC_TEMPLATE_NOTE_MARKERS
        - MISSING_DOMINANT_IDEA_MARKERS
    - path: app/src/server/ai/creative-quality-gate.ts
      provides: extended CreativeHardFailureCode union and normalizeHardFailureCode
      exports:
        - CreativeHardFailureCode
        - normalizeHardFailureCode
    - path: app/messages/en.json
      provides: hardFailureCodes i18n for all GATE-01 codes
    - path: app/messages/pt-BR.json
      provides: hardFailureCodes i18n for all GATE-01 codes
  key_links:
    - from: app/src/server/ai/creative-quality-taxonomy.ts
      to: app/src/server/ai/observable-rubric.ts
      via: re-export OVERLOAD/GENERIC/MISSING_DOMINANT_IDEA markers (no duplicate regex)
      pattern: from \"./observable-rubric\"
    - from: app/src/server/ai/creative-quality-gate.ts
      to: app/src/server/ai/creative-quality-taxonomy.ts
      via: type-only imports; classifiers wired in Plan 02
      pattern: CreativeHardFailureCode
---

# Plan 120-01: Taxonomy, Type Union, normalizeHardFailureCode, i18n (GATE-01)

<objective>
Establish GATE-01 contracts: extend the hard-failure type union, add taxonomy patterns (including rubric marker re-exports), legacy code normalization, and i18n labels — without changing classifier behavior yet.

Purpose: Plan 02 executors implement against stable types and patterns; persisted `hardFailures` JSON remains readable after rename.

Output: Extended taxonomy + types + i18n + pattern unit tests.

**Out of scope:** Classifier promotion logic (Plan 02); corpus baseline flip (Plan 03); score ceilings (Phase 121); full regression suite (Phase 122); live vision re-generation (Phase 123).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-quality-gate-hardening/120-RESEARCH.md
@.planning/phases/120-quality-gate-hardening/120-VALIDATION.md
@app/src/server/ai/creative-quality-gate.ts
@app/src/server/ai/creative-quality-taxonomy.ts
@app/src/server/ai/observable-rubric.ts
@app/src/server/ai/per-mode-prompt-rules.ts
@app/messages/en.json
@app/messages/pt-BR.json

<interfaces>
From `app/src/server/ai/creative-quality-gate.ts` (current union — extend, do not remove existing codes):
```typescript
export type CreativeHardFailureCode =
  | "cta_drift"
  | "wrong_brand"
  | "unsupported_offer"
  | "invented_factual_entity"
  | "copied_style_reference_facts"  // keep for read-compat; emit style_reference_contamination in Plan 02
  | "cropped_critical_content"
  | "unreadable_required_text"
  | "invalid_format_layout";
```

From `app/src/server/ai/observable-rubric.ts` (import, do not copy):
```typescript
export const OVERLOAD_NOTE_MARKERS = /competing (?:zones|modules)|more than three|.../i;
export const GENERIC_TEMPLATE_NOTE_MARKERS = /generic (?:premium|tech|template)|neon glow|.../i;
export const MISSING_DOMINANT_IDEA_MARKERS = /no (?:NR1|campaign-specific|dominant) (?:visual )?idea|.../i;
```

GATE-01 codes to add to union:
`replaced_source_subject`, `unauthorized_brand_or_ip`, `campaign_identity_drift`, `style_reference_contamination`, `generic_template_aesthetic`, `visual_overload`, `missing_dominant_idea`, `decorative_only_variation`

Legacy alias (per RESEARCH Pattern 3):
`copied_style_reference_facts` → `style_reference_contamination` via `normalizeHardFailureCode`
</interfaces>
</context>

<tasks>

<task id="120-01-01" type="auto" tdd="true">
  <name>Add GATE-01 taxonomy patterns and rubric marker re-exports</name>
  <files>app/src/server/ai/creative-quality-taxonomy.ts</files>
  <behavior>
    - CAMPAIGN_IDENTITY_DRIFT_PATTERN matches corpus briefMatch note "education/professor enrollment narrative instead of CENBRAP NR1"
    - CAMPAIGN_IDENTITY_SAFE_PATTERN does NOT match "faithful NR1 format adaptation" or "same campaign identity"
    - REPLACED_SOURCE_SUBJECT_PATTERN matches "replaced hero photo" / "different subject than base"
    - DECORATIVE_ONLY_PATTERN matches "background-only recolor" / "without mechanism change" vocabulary from DECORATIVE_ONLY_REJECTION
    - STYLE_REFERENCE_CONTAMINATION_PATTERN broadened to match "athlete portraits and team uniforms from style reference"
    - OVERLOAD/GENERIC/MISSING_DOMINANT_IDEA re-exported from observable-rubric (no duplicate literals)
  </behavior>
  <action>
In `creative-quality-taxonomy.ts`:

1. **Re-export rubric markers** (per RESEARCH — single source):
   ```typescript
   export {
     OVERLOAD_NOTE_MARKERS,
     GENERIC_TEMPLATE_NOTE_MARKERS,
     MISSING_DOMINANT_IDEA_MARKERS,
   } from "./observable-rubric";
   ```

2. **Add new patterns** (per RESEARCH GATE-01 mapping table):
   - `CAMPAIGN_IDENTITY_DRIFT_PATTERN` — narrative/product/hero identity swap; include corpus exemplar language
   - `CAMPAIGN_IDENTITY_SAFE_PATTERN` — negation guard for faithful adaptations (GATE-05 prep)
   - `REPLACED_SOURCE_SUBJECT_PATTERN` — hero/subject replacement in art_variation/format_adaptation
   - `DECORATIVE_ONLY_PATTERN` — derive from `DECORATIVE_ONLY_REJECTION` in per-mode-prompt-rules.ts (background-only, glow-only, without mechanism change)
   - Broaden `STYLE_REFERENCE_CONTAMINATION_PATTERN` to match people/uniforms/factual leak from style reference (corpus restyling note)

3. **Wire `UNAUTHORIZED_BRAND_PATTERN`** — already exists; ensure exported for Plan 02 `unauthorized_brand_or_ip` promotion.

Do NOT modify `classifyCreativeQualityGate` in this task.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "pattern"</automated>
  </verify>
  <done>All new patterns exist with unit tests proving match/non-match on corpus exemplar strings; rubric markers re-exported without duplication.</done>
</task>

<task id="120-01-02" type="auto">
  <name>Extend CreativeHardFailureCode union, normalizeHardFailureCode, and i18n</name>
  <files>app/src/server/ai/creative-quality-gate.ts, app/messages/en.json, app/messages/pt-BR.json, app/tests/unit/ai/creative-quality-gate.test.ts</files>
  <behavior>
    - CreativeHardFailureCode includes all nine GATE-01 categories plus existing legacy codes
    - normalizeHardFailureCode("copied_style_reference_facts") returns "style_reference_contamination"
    - normalizeHardFailureCode("format_campaign_drift") returns "campaign_identity_drift" (corpus alias)
    - normalizeHardFailureCode("restyling_factual_contamination") returns "style_reference_contamination"
    - en.json and pt-BR.json hardFailureCodes include invented_factual_entity and all new GATE-01 keys
  </behavior>
  <action>
1. **Extend `CreativeHardFailureCode`** in `creative-quality-gate.ts` with GATE-01 codes:
   `replaced_source_subject`, `unauthorized_brand_or_ip`, `campaign_identity_drift`, `style_reference_contamination`, `generic_template_aesthetic`, `visual_overload`, `missing_dominant_idea`, `decorative_only_variation`
   Keep `copied_style_reference_facts` in union for persisted JSON compat.

2. **Add and export `normalizeHardFailureCode(code: string): CreativeHardFailureCode`** mapping:
   - `copied_style_reference_facts` → `style_reference_contamination`
   - `format_campaign_drift` → `campaign_identity_drift`
   - `restyling_factual_contamination` → `style_reference_contamination`
   - Unknown codes pass through when already valid union members.

3. **i18n** — add `hardFailureCodes` entries in `en.json` and `pt-BR.json` for ALL GATE-01 codes including `invented_factual_entity` (currently missing). Keep `copied_style_reference_facts` label for legacy rows. Use concise operator-facing labels.

4. **Tests** — add `describe("GATE-01 taxonomy patterns")` in `creative-quality-gate.test.ts` with table-driven `noteMatches` cases for each new pattern + `normalizeHardFailureCode` alias cases.

Do NOT change classifier functions yet — types and patterns only.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "GATE-01|normalizeHardFailureCode|pattern"</automated>
  </verify>
  <done>Type union compiles; normalizeHardFailureCode handles legacy aliases; i18n keys exist for every GATE-01 code; pattern tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted hardFailures JSON → UI labels | Legacy codes must normalize at read boundaries |
| model-produced QA notes → regex patterns | Patterns are server-side only; no user input in pattern construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering | normalizeHardFailureCode | mitigate | Map legacy contamination codes so parent lineage checks remain effective |
| T-120-02 | Repudiation | Missing i18n keys | mitigate | Add all GATE-01 labels in en + pt-BR in same plan as type union |
</threat_model>

<verification>
Per @.planning/phases/120-quality-gate-hardening/120-VALIDATION.md:
`cd app && npm test -- tests/unit/ai/creative-quality-gate.test.ts -t "GATE-01|pattern|normalizeHardFailureCode"`
</verification>

<success_criteria>
- GATE-01: All nine blocking category codes exist in `CreativeHardFailureCode` with taxonomy patterns and i18n labels
- Legacy `copied_style_reference_facts` normalizes to `style_reference_contamination`
- Rubric markers imported from observable-rubric, not duplicated
</success_criteria>

<output>
After completion, create `.planning/phases/120-quality-gate-hardening/120-01-SUMMARY.md`
</output>
