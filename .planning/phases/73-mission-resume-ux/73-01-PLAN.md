---
phase: 73-mission-resume-ux
plan: 01
type: execute
autonomous: true
wave: 1
depends_on: []
requirements: [UX-01, UX-02]
---

# Plan 73-01: Mission Resume UX Verification and Test Coverage

## Objective

Verify mission/progression CTAs resume into the intended campaign workflow surface and add focused test coverage for href generation and deep-link routing.

## Must Haves

- Campaign workspace consumes `?tab=` resume targets.
- CTAs for assets, readiness, review, export, and share open the intended surface.
- Default campaign workspace behavior unchanged without resume target.
- Tests prove resume path for at least two representative mission CTAs.

## Tasks

### Task 1: Verify existing deep-link implementation (auto)

- Confirm `parseCampaignTabParam` and `applyCampaignDeepLink` are wired in campaign workspace page.
- Confirm mission anchor IDs exist for all tab keys.
- Confirm `buildMissionHref` and `buildEvidenceHref` emit correct `?tab=` URLs.

### Task 2: Add href and routing regression tests (auto)

- Add `hrefs.test.ts` covering upload→assets, export, share, preview→generate&mode=preview.
- Expand `deep-link-tab.test.ts` for review/export/share → goToActions.

<automated>
cd app && npm test -- src/lib/campaign/deep-link-tab.test.ts src/server/progression/missions/hrefs.test.ts
</automated>

### Task 3: Run focused suite and record evidence (auto)

<automated>
cd app && npm run build && npm run lint && npm test -- src/server/progression src/server/repositories/progression src/app/api/workspace/progression src/lib/hooks/use-progression src/components/dashboard/AdsScientistProgressCard src/app/api/workspace/missions src/lib/hooks/use-missions src/components/dashboard/MissionPathCard src/app/api/workspace/mission-insights src/server/mission-insights src/lib/progression src/server/feedback/mission-credit-signals src/lib/campaign/deep-link-tab src/server/progression/missions/hrefs
</automated>

## Verification

- Build passes with TypeScript checks enabled.
- Focused suite passes including new href/deep-link tests.
- `73-VERIFICATION.md` records evidence.
