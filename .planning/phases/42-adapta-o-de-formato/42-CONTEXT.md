# Phase 42 Context: Adaptação de formato

**Requirements:** DRV-05, DRV-06

Single-select (1 format) and multi-select (batch, default all 3) before `configureAndGenerate` with `targetFormats`.

**Depends on:** Phase 41
