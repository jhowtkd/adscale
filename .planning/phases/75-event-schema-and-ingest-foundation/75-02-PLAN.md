---
phase: 75-event-schema-and-ingest-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/beta-analytics/types.ts
  - app/src/server/beta-analytics/sanitize.ts
  - app/src/server/beta-analytics/sanitize.test.ts
autonomous: true
requirements:
  - INST-05
  - INST-06
must_haves:
  truths:
    - "Unknown property keys are rejected (not silently stripped) at sanitization boundary"
    - "PII-like keys (prompt, email, message, url, assetUrl, token) are rejected"
    - "Properties payload is bounded to ≤32KB serialized size"
    - "Only scalar values (string | number | boolean | null) pass validation"
  artifacts:
    - path: app/src/server/beta-analytics/sanitize.ts
      provides: "Allowlist-only property validation"
      exports: ["sanitizeBetaEventProperties", "BetaEventPropertiesValidationError"]
    - path: app/src/server/beta-analytics/types.ts
      provides: "Event key and property type contracts"
      exports: ["ALLOWED_PROPERTY_KEYS", "createBetaEventBodySchema"]
    - path: app/src/server/beta-analytics/sanitize.test.ts
      provides: "INST-05/06 sanitization coverage"
  key_links:
    - from: app/src/server/beta-analytics/sanitize.ts
      to: zod strictObject
      via: "z.strictObject rejects unknown keys"
      pattern: "strictObject"
---

<objective>
Implement PII-safe allowlist-only property sanitization for beta analytics events.

Purpose: INST-05 requires stricter validation than feedback blocklist — reject unknown keys before any DB write. This module is consumed by `recordBetaAnalyticsEvent()` in Plan 03.
Output: `types.ts`, `sanitize.ts`, comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-event-schema-and-ingest-foundation/75-CONTEXT.md
@.planning/phases/75-event-schema-and-ingest-foundation/75-RESEARCH.md

@app/src/server/mission-insights/sanitize.ts
@app/src/server/feedback/sanitize.ts
@app/src/server/feedback/sanitize.test.ts

<interfaces>
Mission-insight allowlist pattern (pick-only subset — analytics must REJECT unknown):
```typescript
const ALLOWED_DIAGNOSTIC_KEYS = new Set(["readinessStatus", "derivationStatus", ...]);
```

Feedback size bound to mirror:
```typescript
// MAX_DIAGNOSTIC_BYTES = 32 * 1024 in feedback/sanitize.ts
```

Zod strictObject (reject unknown keys per INST-05):
```typescript
const propertiesSchema = z.strictObject({ stage: z.string(), ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define beta analytics types and global property allowlist schema</name>
  <files>app/src/server/beta-analytics/types.ts, app/src/server/beta-analytics/sanitize.ts</files>
  <behavior>
    - ALLOWED_PROPERTY_KEYS includes funnel scalars: stage, missionKey, source, blockingCount, estimateCredits, actualCredits, action, operation, format, isPreview, readinessStatus, durationMs, reasonCode
    - createBetaEventBodySchema validates eventKey (snake_case regex, 1-64 chars), optional sessionId UUID, optional campaignId/derivationId UUID, properties object
    - sanitizeBetaEventProperties throws BetaEventPropertiesValidationError on unknown keys
    - sanitizeBetaEventProperties throws on denied keys: prompt, email, message, url, assetUrl, token, authToken, breadcrumbs, diagnosticContext, note, freeText
  </behavior>
  <action>
Create `app/src/server/beta-analytics/types.ts`:
- Export `ALLOWED_PROPERTY_KEYS` const array (global union for Phase 75 per research recommendation; Phase 76 may extend per event_key family)
- Export `BetaEventSource` = `'client' | 'server'`
- Export `createBetaEventBodySchema` Zod object for HTTP body (eventKey, sessionId?, campaignId?, derivationId?, properties default {})

Create `app/src/server/beta-analytics/sanitize.ts`:
- `BetaEventPropertiesValidationError` with `code: 'validation_error'` and `details` for unknown/denied keys
- `sanitizeBetaEventProperties(input: unknown): Record<string, string | number | boolean | null>` using `z.strictObject()` built from ALLOWED_PROPERTY_KEYS with scalar value union
- `enforcePropertiesSize(props)` — reject if `JSON.stringify(props).length > 32 * 1024` (mirror feedback MAX_DIAGNOSTIC_BYTES)
- Explicit DENIED_KEY_NAMES Set checked before strictObject (belt-and-suspenders for PII key names even if not in allowlist)

Per D-06: allowlist-only — return error on unknown keys, do NOT strip silently (contrast with feedback blocklist).

No DB or HTTP imports in this file — pure validation module.
  </action>
  <verify>
    <automated>test -f app/src/server/beta-analytics/sanitize.ts && test -f app/src/server/beta-analytics/types.ts</automated>
  </verify>
  <done>types.ts and sanitize.ts export validation contracts; strictObject rejects unknown keys; denied PII keys fail fast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive sanitization unit tests</name>
  <files>app/src/server/beta-analytics/sanitize.test.ts</files>
  <behavior>
    - Valid allowlisted scalar properties pass and return same values
    - Unknown key `foo` throws BetaEventPropertiesValidationError
    - Denied keys (prompt, email, message, url) throw even if accidentally added to allowlist in future
    - Nested objects/arrays in property values rejected
    - Payload &gt;32KB rejected
    - Empty properties `{}` accepted
  </behavior>
  <action>
Create `sanitize.test.ts` mirroring `feedback/sanitize.test.ts` structure but asserting **rejection** not stripping:

```typescript
describe("sanitizeBetaEventProperties", () => {
  it("accepts allowlisted scalar properties", () => { ... });
  it("rejects unknown property keys with validation_error", () => {
    expect(() => sanitizeBetaEventProperties({ stage: "briefing", prompt: "secret" }))
      .toThrow(BetaEventPropertiesValidationError);
  });
  it("rejects prompt/email/url keys explicitly", () => { ... });
  it("rejects oversized payload", () => { ... });
  it("rejects nested object values", () => { ... });
});
```

Also test `createBetaEventBodySchema.safeParse` for invalid eventKey formats.

This satisfies INST-06 sanitization coverage independent of HTTP/DB layers.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/sanitize.test.ts</automated>
  </verify>
  <done>All INST-05 behaviors covered by automated tests; tests assert HTTP-style validation_error semantics via thrown error type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client properties → sanitize | Untrusted jsonb must not reach DB without allowlist pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Information disclosure | properties jsonb | mitigate | z.strictObject + DENIED_KEY_NAMES; scalar-only values |
| T-75-05 | Denial of service | oversized payload | mitigate | 32KB JSON size cap in enforcePropertiesSize |
| T-75-06 | Tampering | unknown keys smuggled | mitigate | strictObject rejects unknown keys with 400 upstream |
</threat_model>

<verification>
- `cd app && npm test -- src/server/beta-analytics/sanitize.test.ts` passes
- Tests verify rejection (not silent strip) for disallowed properties
</verification>

<success_criteria>
- INST-05 allowlist enforced with reject-unknown semantics
- INST-06 sanitization tests green
- Module ready for import by recordBetaAnalyticsEvent (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/75-event-schema-and-ingest-foundation/75-02-SUMMARY.md`
</output>
