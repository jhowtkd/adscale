---
phase: 118-per-mode-prompt-rules
plan: "04"
type: execute
wave: 4
depends_on:
  - "118-03"
files_modified:
  - app/tests/unit/ai/quality-prompt-regression.test.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/ai/corpus-baseline.test.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - MODE-05
must_haves:
  truths:
    - "All QUALITY_FIXTURES prompts satisfy per-mode invariants for their generationMode"
    - "Per-mode rules section ordering consistent across art_variation, restyling, format_adaptation"
    - "Corpus baseline gap count remains 4 until Phase 120 gate hardening (no premature gate promotion)"
    - "Full test, lint, and build pass for phase gate"
  artifacts:
    - path: app/tests/unit/ai/quality-prompt-regression.test.ts
      provides: extended per-mode assertions and updated snapshots
    - path: app/src/server/ai/prompt-builder.test.ts
      provides: integration ordering tests for all three modes
  key_links:
    - from: quality-prompt-regression.test.ts
      to: extractPromptPerModeRulesSection
      via: mode-specific header assertions per fixture.generationMode
      pattern: extractPromptPerModeRulesSection
    - from: corpus-baseline.test.ts
      to: BASELINE_GAP_COUNT
      via: unchanged expectation documents Phase 120 handoff
      pattern: BASELINE_GAP_COUNT.*4
---

# Plan 118-04: Regression Snapshots + Phase Integration Verification

<objective>
Extend cross-fixture prompt regression, verify injection ordering for all modes, confirm corpus baseline gap count unchanged (Phase 120 handoff), and run full phase gate.

Purpose: Integration proof that MODE-01–05 ship together with stable regression coverage and explicit boundary vs Phase 120 gate work.

Output: Updated regression tests, phase-green CI commands, SUMMARY documenting gate deferral.

**Explicitly out of scope (Phase 120):** `decorative_only_variation`, `missing_dominant_idea`, `campaign_identity_drift`, `visual_overload`, `generic_template_aesthetic` hard-failure promotion; `BASELINE_GAP_COUNT` flip to 0; GATE-04 corpus piece blocking.

**Explicitly out of scope (Phase 119):** RUBR-01–04 QA rubric rewrite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-per-mode-prompt-rules/118-VALIDATION.md
@app/tests/unit/ai/quality-prompt-regression.test.ts
@app/tests/unit/ai/corpus-baseline.test.ts
@app/src/server/ai/per-mode-prompt-rules.ts

<interfaces>
```typescript
export const BASELINE_GAP_COUNT = CORPUS_ARCHETYPE_FIXTURES.length - 1; // expect 4
```

Per-mode section headers to assert in regression:
- art_variation: DECORATIVE-ONLY, THREE-ZONE
- restyling: entity lock / base-locked
- format_adaptation: CAMPAIGN IDENTITY LOCK, CROSS-FORMAT IDENTITY
</interfaces>
</context>

<tasks>

<task id="118-04-01" type="auto">
  <name>Extend quality-prompt-regression per-mode invariants</name>
  <files>app/tests/unit/ai/quality-prompt-regression.test.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <action>
Import `extractPromptPerModeRulesSection` from `per-mode-prompt-rules.ts` (or re-export from prompt-builder).

In `quality-prompt-regression.test.ts` main loop, after existing mode assertions, add per-mode checks using `extractPromptPerModeRulesSection(prompt)`:

| generationMode | Required patterns |
|----------------|-------------------|
| art_variation | DECORATIVE-ONLY or decorative-only; THREE-ZONE or three main information zones |
| restyling | base-locked entities or entity lock; must not duplicate DENYLIST inside MODE |
| format_adaptation | CAMPAIGN IDENTITY LOCK or same campaign; CROSS-FORMAT IDENTITY; PRESERVE COPY AND FACTS VERBATIM |

Update `compact prompt section snapshots by generation mode` inline snapshots for art_variation and format_adaptation mode sections if text changed in Plans 01–03.

Add `prompt-builder.test.ts` describe `per-mode rules integration (MODE-01–05)`:
- describe.each art_variation, restyling, format_adaptation — classification before MODE before Campaign
- art_variation: creativity guardrail line present when balanced level
- format_adaptation: plan hooks absent when plan fixture includes hooks

Do NOT modify `creative-quality-gate.ts` or add new hard failure codes (Phase 120).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts src/server/ai/prompt-builder.test.ts -t "per-mode|prompt regression"</automated>
  </verify>
  <done>Regression matrix asserts MODE-01–05 headers per fixture mode; snapshots updated.</done>
</task>

<task id="118-04-02" type="auto">
  <name>Verify corpus baseline unchanged and run phase gate</name>
  <files>app/tests/unit/ai/corpus-baseline.test.ts
  </files>
  <action>
Run corpus baseline test — confirm `BASELINE_GAP_COUNT` still `4` and test `documents baseline gap count for Phase 123 validation` passes unchanged.

If any executor accidentally promoted gate failures in Phase 118, revert gate changes — Phase 118 is prompt-only.

Run phase gate commands per VALIDATION.md:
1. `cd app && npm test`
2. `cd app && npm run lint`
3. `cd app && npm run build`

Record in SUMMARY.md Phase 120 handoff bullets:
- Gate codes deferred: decorative_only_variation, missing_dominant_idea, campaign_identity_drift, visual_overload, generic_template_aesthetic
- BASELINE_GAP_COUNT remains 4 until Phase 120
- Prompt contracts delivered for MODE-01–05
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/corpus-baseline.test.ts -t "baseline gap" && cd app && npm test && npm run lint && npm run build</automated>
  </verify>
  <done>Full suite green; baseline gap documented; no Phase 120 gate changes landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → false confidence | Prompt tests pass but gate still approves corpus failures until Phase 120 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-07 | Repudiation | baseline gap silently reduced | mitigate | corpus-baseline.test.ts assertion unchanged |
| T-118-08 | Tampering | accidental gate promotion in 118 | mitigate | Plan forbids creative-quality-gate edits; verify in task |
</threat_model>

<verification>
Phase gate: `cd app && npm test && npm run lint && npm run build` all pass; `BASELINE_GAP_COUNT === 4`.
</verification>

<success_criteria>
- MODE-01–05: integration regression covers all three generation modes
- Phase 120 gate work explicitly not shipped; baseline gap count unchanged
- Phase 118 ready for `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/118-per-mode-prompt-rules/118-04-SUMMARY.md`
</output>
