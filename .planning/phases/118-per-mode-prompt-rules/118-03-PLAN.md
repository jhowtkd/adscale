---
phase: 118-per-mode-prompt-rules
plan: "03"
type: execute
wave: 3
depends_on:
  - "118-02"
files_modified:
  - app/src/server/ai/per-mode-prompt-rules.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
autonomous: true
requirements:
  - MODE-04
  - MODE-05
must_haves:
  truths:
    - "format_adaptation prompts lock campaign identity (people, copy, CTA, brand, dominant idea) — edit not recreate"
    - "format_adaptation omits plan hooks/angles and competitor analyses that pressure new concepts"
    - "All format_adaptation prompts for 1:1, 4:5, 9:16 include cross-format identity rule forbidding new narrative"
    - "Reference asset paragraph for format jobs says same ad new frame, not recreate ad"
  artifacts:
    - path: app/src/server/ai/per-mode-prompt-rules.ts
      provides: format pack, firewall helpers, cross-format rule
      exports:
        - CAMPAIGN_IDENTITY_LOCK
        - CROSS_FORMAT_IDENTITY_RULE
        - buildFormatAdaptationModeRulesSection
        - shouldIncludePlanHooksForMode
        - shouldIncludeCompetitorAnalysesForMode
        - buildFormatFlexibleContextSuffix
        - buildFormatReferenceAssetSuffix
    - path: app/src/server/ai/prompt-builder.ts
      provides: format firewall on plan/competitor blocks; migrated format MODE
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/per-mode-prompt-rules.ts
      via: shouldIncludePlanHooksForMode / shouldIncludeCompetitorAnalysesForMode before pushing plan/competitor sections
      pattern: shouldIncludePlanHooksForMode
    - from: buildFormatAdaptationModeRulesSection
      to: canonicalCreative.dominantIdea
      via: CAMPAIGN IDENTITY LOCK cites dominant idea when present
      pattern: dominantIdea|CAMPAIGN IDENTITY LOCK
---

# Plan 118-03: Format Adaptation Pack + Flexible-Context Firewall (MODE-04/05)

<objective>
Implement `buildFormatAdaptationModeRulesSection` with campaign identity lock and cross-format identity rule, add flexible-context firewall demoting variation pressure for format jobs, migrate inline format MODE strings, and add parameterized cross-format tests.

Purpose: Close MODE-04 and MODE-05 — format_adaptation drift from plan hooks and missing cross-format identity lock causes corpus campaign-substitution failures.

Output: Format rule pack, firewall helpers, wired prompt-builder, tests for 1:1 / 4:5 / 9:16.

**Out of scope:** Gate `campaign_identity_drift` / corpus baseline flip (Phase 120 GATE-04).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-per-mode-prompt-rules/118-RESEARCH.md
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/prompt-builder.test-fixtures.ts

<interfaces>
RESEARCH firewall matrix — format_adaptation:
- Plan strategy: include (layout tone only label)
- Plan angles/hooks: **omit**
- Creative diagnosis: already art-only (omit)
- Competitor analyses: **omit**
- Campaign/brand memory: include with layout-only suffix

Current plan injection (prompt-builder.ts:532-542): pushes strategy, angles, hooks unconditionally.

Current format reference copy (lines 575-579): "Recreate the ad from recognizable parts" — replace with `buildFormatReferenceAssetSuffix()`.

Per-format hints exist at lines 474-479 — migrate into `buildFormatAdaptationModeRulesSection({ targetFormat })`.
</interfaces>
</context>

<tasks>

<task id="118-03-01" type="auto" tdd="true">
  <name>Implement format adaptation pack and firewall helpers</name>
  <files>app/src/server/ai/per-mode-prompt-rules.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - CAMPAIGN_IDENTITY_LOCK preserves people, copy, CTA, brand, concept; only composition/scale/grouping may change
    - CROSS_FORMAT_IDENTITY_RULE states 1:1/4:5/9:16 same campaign, no new narrative/hero photo/offer
    - buildFormatAdaptationModeRulesSection migrates verbatim copy preservation and per-format layout hints
    - shouldIncludePlanHooksForMode returns false for format_adaptation, true for art_variation and restyling
    - shouldIncludeCompetitorAnalysesForMode returns false for format_adaptation, true for art_variation and restyling
    - buildFormatFlexibleContextSuffix labels memory blocks layout-only
    - buildFormatReferenceAssetSuffix uses same-ad-new-frame language, not recreate-ad
  </behavior>
  <action>
In `per-mode-prompt-rules.ts` add:

- `CAMPAIGN_IDENTITY_LOCK` — MODE-04 prose; when `dominantIdea` provided, include `Dominant idea (must not change): {dominantIdea}`
- `CROSS_FORMAT_IDENTITY_RULE` — MODE-05 shared clause per RESEARCH Pattern 3
- `buildFormatAdaptationModeRulesSection({ targetFormat, packageSource?, dominantIdea? }): string[]` — migrate format MODE from prompt-builder lines 451-479; append CROSS_FORMAT_IDENTITY_RULE; keep approved_derivation winner lines when `packageSource === "approved_derivation"`
- `shouldIncludePlanHooksForMode(mode): boolean` — false for format_adaptation
- `shouldIncludeCompetitorAnalysesForMode(mode): boolean` — false for format_adaptation
- `buildFormatFlexibleContextSuffix(): string` — single line: layout-only context reminder
- `buildFormatReferenceAssetSuffix(): string` — "Same ad, new frame — rearrange modules for target aspect ratio; do not introduce new concepts or hero imagery."

Update `buildPerModeRulesSection` format_adaptation arm.

Add **unit tests on helpers/builders only** (no full-prompt firewall tests — those belong in 118-03-02 after prompt-builder wiring):
- `shouldIncludePlanHooksForMode` — returns false for format_adaptation, true for art_variation and restyling
- `shouldIncludeCompetitorAnalysesForMode` — returns false for format_adaptation, true for art_variation and restyling
- `buildFormatAdaptationModeRulesSection` — output contains CAMPAIGN IDENTITY LOCK, PRESERVE COPY AND FACTS VERBATIM, and CROSS-FORMAT IDENTITY
- `format_adaptation cross-format identity — %s` — describe.each 1:1, 4:5, 9:16 on `buildFormatAdaptationModeRulesSection({ targetFormat })` output (not full `buildDerivationPrompt`) — CROSS-FORMAT IDENTITY and no new narrative language
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "shouldIncludePlanHooksForMode|shouldIncludeCompetitorAnalysesForMode|buildFormatAdaptationModeRulesSection|cross-format identity"</automated>
  </verify>
  <done>Format pack and firewall helpers exported; helper/builder unit tests pass.</done>
</task>

<task id="118-03-02" type="auto">
  <name>Wire format firewall and migrate reference copy</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <action>
Remove inline `else if (generationMode === "format_adaptation")` MODE block — handled by `buildPerModeRulesSection`.

Plan section (lines 532-542): wrap angles/hooks push:
```typescript
if (plan) {
  parts.push(`\nCreative Strategy${generationMode === "format_adaptation" ? " (layout tone only)" : ""}: ${plan.strategy}`);
  if (shouldIncludePlanHooksForMode(generationMode)) {
    // angles, hooks, ctas as today
  }
}
```

Competitor block: guard with `shouldIncludeCompetitorAnalysesForMode(generationMode)`.

After campaign/brand memory blocks, when `generationMode === "format_adaptation"`, append `buildFormatFlexibleContextSuffix()`.

Reference asset section: use `buildFormatReferenceAssetSuffix()` **only when `generationMode === "format_adaptation"`** — do NOT apply for restyling or art_variation. Restyling keeps its own reference copy path; art_variation may use variation-oriented language. Guard explicitly:
```typescript
if (generationMode === "format_adaptation") {
  parts.push(buildFormatReferenceAssetSuffix());
} else if (!isArtVariation) {
  // existing non-format reference copy
}
```

Verify format_adaptation still excludes visualTokenBrief (existing guard unchanged).

Add **full-prompt integration test** moved from 118-03-01:
- `format firewall (MODE-04)` — `buildDerivationPrompt` with format_adaptation + plan fixture: contains strategy if present, does NOT contain "Creative Angles:" or "Hook Copy Options:"; no competitor section header

Run full prompt-builder format_adaptation describe blocks — update assertions if snapshot strings shifted but semantics preserved.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "format_adaptation|format firewall|cross-format"</automated>
  </verify>
  <done>All three modes route through buildPerModeRulesSection; format jobs omit concept-pressure context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan hooks → format prompt | Untrusted variation concepts injected into layout-only jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Tampering | plan hooks on format_adaptation | mitigate | shouldIncludePlanHooksForMode firewall |
| T-118-06 | Spoofing | format output as different campaign | accept | Prompt lock now; gate Phase 120 |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "format"` passes including cross-format parameterized suite.
</verification>

<success_criteria>
- MODE-04: format_adaptation edits same campaign; firewall omits hooks/angles/competitor variation pressure
- MODE-05: 1:1, 4:5, 9:16 prompts share cross-format identity rule
</success_criteria>

<output>
After completion, create `.planning/phases/118-per-mode-prompt-rules/118-03-SUMMARY.md`
</output>
