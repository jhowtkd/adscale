---
phase: 118-per-mode-prompt-rules
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/per-mode-prompt-rules.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-02
must_haves:
  truths:
    - "art_variation prompts explicitly reject decorative-only changes (color/glow/background/card chrome without new visual mechanism)"
    - "art_variation prompts cap information layout to three main zones (hook, proof/offer, CTA)"
    - "Per-mode rules inject after Phase 117 classification/transfer blocks and before creativity level"
  artifacts:
    - path: app/src/server/ai/per-mode-prompt-rules.ts
      provides: art_variation rule pack, buildPerModeRulesSection router, extractPromptPerModeRulesSection
      exports:
        - DECORATIVE_ONLY_REJECTION
        - buildArtVariationModeRulesSection
        - buildPerModeRulesSection
        - extractPromptPerModeRulesSection
    - path: app/src/server/ai/prompt-builder.ts
      provides: calls buildPerModeRulesSection instead of inline art_variation MODE strings
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/per-mode-prompt-rules.ts
      via: buildPerModeRulesSection after allowed entities / visual transfer
      pattern: buildPerModeRulesSection
    - from: app/src/server/ai/per-mode-prompt-rules.ts
      to: canonical-creative-contract
      via: THREE-ZONE references hook/proof/CTA from VISUAL HIERARCHY CONTRACT
      pattern: THREE-ZONE VISUAL BUDGET
---

# Plan 118-01: Per-Mode Rules Scaffold + art_variation Pack (MODE-01/02)

<objective>
Create `per-mode-prompt-rules.ts` mirroring the Phase 117 module pattern, implement the `art_variation` behavioral contract (decorative-only rejection + three-zone budget), wire it into `buildDerivationPrompt`, and add unit tests.

Purpose: Close MODE-01 and MODE-02 — current MODE block rewards "perceptibly different" without forbidding decorative-only deltas or enforcing a hard zone budget.

Output: New rule module, migrated art_variation MODE prose, creativity-template guardrails, extractors, tests.

**Out of scope:** Gate promotion of `decorative_only_variation` / `visual_overload` (Phase 120 GATE-01).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-per-mode-prompt-rules/118-RESEARCH.md
@.planning/phases/118-per-mode-prompt-rules/118-VALIDATION.md
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/factual-visual-separation.ts
@app/src/server/ai/canonical-creative-contract.ts

<interfaces>
From `app/src/server/ai/prompt-builder.ts` injection order (Phase 117):
```
HARD RULES → canonical → integrity → classification → allowed entities
→ visual transfer (restyling) → RESTYLING FACTUAL-SOURCE (restyling)
→ [NEW buildPerModeRulesSection] → creativity level → campaign fields
```

From `app/src/server/ai/prompt-builder.ts` lines 437-450 (to migrate):
- Inline `MODE: art_variation` block with "PERCEPTIBLY DIFFERENT" / "Vary background, composition…"
- Must replace with mechanism-first language per RESEARCH Pattern 1

From `CREATIVITY_TEMPLATES` (conservative/balanced): add guardrail line forbidding decorative-only at conservative and balanced levels.

Existing extractor pattern:
```typescript
export function extractPromptModeSection(prompt: string): string
```
</interfaces>
</context>

<tasks>

<task id="118-01-01" type="auto" tdd="true">
  <name>Create per-mode-prompt-rules module with art_variation pack</name>
  <files>app/src/server/ai/per-mode-prompt-rules.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - DECORATIVE_ONLY_REJECTION lists color swap, glow, gradient stack, card chrome without new mechanism
    - buildArtVariationModeRulesSection includes DECORATIVE_ONLY_REJECTION, THREE-ZONE VISUAL BUDGET, and migrated anti-cropping/safe-margin rules from prompt-builder
    - THREE-ZONE block forbids fourth competing module at equal visual weight; references hook, proof/offer, CTA zones
    - buildPerModeRulesSection routes art_variation only (format/restyling stubs throw or return [] until Plans 02–03)
    - extractPromptPerModeRulesSection returns text from first "MODE:" through next "\n\nCampaign:" or "\nCREATIVITY LEVEL:"
    - art_variation prompt contains DECORATIVE-ONLY and THREE-ZONE headers
    - art_variation prompt requires new composition mechanism (focal hierarchy, proof presentation, CTA module architecture)
  </behavior>
  <action>
Create `app/src/server/ai/per-mode-prompt-rules.ts` exporting:

- `DECORATIVE_ONLY_REJECTION` — constant per RESEARCH Pattern 1 (MODE-01)
- `THREE_ZONE_VISUAL_BUDGET` — constant capping art_variation to hook / proof-offer / CTA zones (MODE-02)
- `buildArtVariationModeRulesSection(): string[]` — migrate preservation, anti-cropping, rearrangement, safe-margin, thumbnail rules from `prompt-builder.ts` lines 437-449; **remove** "PERCEPTIBLY DIFFERENT" / vague "Vary background" without mechanism qualifier; **add** `CREATIVE MECHANISM REQUIREMENT` line requiring new focal hierarchy, proof presentation, subject framing, or CTA module architecture
- `PerModeRulesOptions` — `{ generationMode, targetFormat?, packageSource?, dominantIdea? }`
- `buildPerModeRulesSection(options): string[]` — switch on `generationMode`; implement `art_variation` only; for `restyling`/`format_adaptation` return empty array temporarily (Plans 02–03 fill in)
- `extractPromptPerModeRulesSection(prompt: string): string` — bounded extractor between `MODE:` and campaign/creativity markers

Add `prompt-builder.test.ts` describe blocks:
- `art_variation decorative-only rejection (MODE-01)` — assert mode section matches `/DECORATIVE-ONLY|decorative-only/i` and `/new composition mechanism|focal hierarchy|proof presentation/i`
- `art_variation three-zone budget (MODE-02)` — assert `/THREE-ZONE|three main information zones/i` and `/hook.*proof.*CTA|no fourth module/i`
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "decorative-only|three-zone"</automated>
  </verify>
  <done>Module exists with art_variation exports; MODE-01/02 unit tests pass before wiring.</done>
</task>

<task id="118-01-02" type="auto">
  <name>Wire art_variation pack and add creativity guardrails</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <action>
In `buildDerivationPrompt`, after allowed entities and restyling transfer/factual-source blocks, replace the inline `if (isArtVariation) { parts.push("MODE: art_variation…") }` block with:

```typescript
parts.push(
  ...buildPerModeRulesSection({
    generationMode,
    targetFormat,
    packageSource: config.packageSource,
    dominantIdea: effectiveContract.canonicalCreative?.dominantIdea,
  })
);
```

Remove duplicated art_variation MODE strings from prompt-builder (keep format_adaptation and restyling inline until Plans 02–03).

Append to `conservative` and `balanced` CREATIVITY_TEMPLATES one line each:
`Decorative-only changes (background color, glow, card chrome) without a new visual mechanism are invalid — require a new composition mechanism per MODE rules.`

When `creativeDiagnosis` is present, append to diagnosis block: `Explore listed opportunities within the three-zone visual budget only.`

Add ordering test `per-mode rules injection order`:
- art_variation prompt: classification index &lt; MODE index &lt; CREATIVITY LEVEL or Campaign index
- `extractPromptPerModeRulesSection` non-empty for art_variation fixture

Re-export `extractPromptPerModeRulesSection` from `prompt-builder.ts` if tests import from there (match Phase 117 re-export pattern for classification extractors).
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "decorative-only|three-zone|per-mode ordering|creative mechanism"</automated>
  </verify>
  <done>art_variation prompts use per-mode module; creativity templates guard against decorative-only; injection order verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign/diagnosis text → MODE rules | Untrusted strings must not weaken decorative-only rejection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Tampering | buildArtVariationModeRulesSection | mitigate | Mechanism-first language; decorative-only explicit reject list |
| T-118-02 | Spoofing | decorative variation passes as valid art_variation | accept | Prompt-only in 118; gate enforcement deferred Phase 120 |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts` passes for MODE-01/02 and ordering tests.
</verification>

<success_criteria>
- MODE-01: art_variation rejects decorative-only; requires new visual mechanism in prompt
- MODE-02: art_variation enforces three-zone visual budget in prompt
</success_criteria>

<output>
After completion, create `.planning/phases/118-per-mode-prompt-rules/118-01-SUMMARY.md`
</output>
