---
phase: 118-per-mode-prompt-rules
plan: "02"
type: execute
wave: 2
depends_on:
  - "118-01"
files_modified:
  - app/src/server/ai/per-mode-prompt-rules.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/ai/quality-prompt-regression.test.ts
autonomous: true
requirements:
  - MODE-03
must_haves:
  truths:
    - "restyling MODE block lists base-locked factual entities (people, product, offer, CTA, brand) without duplicating SEP-02 denylist"
    - "restyling MODE defers abstract style transfer to VISUAL REFERENCE TRANSFER RULE already injected by Phase 117"
    - "restyling prompt has no contradictory instructions between MODE pack and transfer rule"
  artifacts:
    - path: app/src/server/ai/per-mode-prompt-rules.ts
      provides: buildRestylingModeRulesSection with entity checklist
      exports:
        - buildRestylingModeRulesSection
        - RESTYLING_ENTITY_LOCK_CHECKLIST
    - path: app/src/server/ai/prompt-builder.ts
      provides: restyling uses buildPerModeRulesSection; inline restyling MODE removed
  key_links:
    - from: app/src/server/ai/per-mode-prompt-rules.ts
      to: app/src/server/ai/factual-visual-separation.ts
      via: MODE opens with factual entities locked per INPUT SOURCE CLASSIFICATION; no second denylist
      pattern: INPUT SOURCE CLASSIFICATION|VISUAL REFERENCE TRANSFER RULE
---

# Plan 118-02: Restyling MODE Pack Consolidation (MODE-03)

<objective>
Implement `buildRestylingModeRulesSection` consolidating restyling behavioral rules with Phase 117 SEP infrastructure, migrate inline restyling MODE strings, and extend regression tests.

Purpose: Close MODE-03 — restyling must preserve all factual entities from base and extract only abstract attributes from style reference without redundant/contradictory prose.

Output: Restyling rule pack, wired prompt-builder, updated snapshots for restyling fixtures.

**Out of scope:** Gate `style_reference_contamination` promotion (Phase 120).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-per-mode-prompt-rules/118-RESEARCH.md
@app/src/server/ai/per-mode-prompt-rules.ts
@app/src/server/ai/factual-visual-separation.ts
@app/src/server/ai/prompt-builder.ts

<interfaces>
Phase 117 blocks already injected before MODE (do not duplicate):
- `buildVisualReferenceTransferRuleSection` — ALLOWLIST/DENYLIST (SEP-02)
- `RESTYLING FACTUAL-SOURCE RULE` inline in prompt-builder lines 429-434

Current inline restyling MODE (prompt-builder.ts:481-493) duplicates transfer denylist — consolidate per RESEARCH Pitfall 4.

From `buildPerModeRulesSection` (Plan 01): extend switch arm for `restyling`.
</interfaces>
</context>

<tasks>

<task id="118-02-01" type="auto" tdd="true">
  <name>Implement restyling MODE pack with entity checklist</name>
  <files>app/src/server/ai/per-mode-prompt-rules.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - RESTYLING_ENTITY_LOCK_CHECKLIST lists people, product, offer, CTA, brand locked to factual base
    - buildRestylingModeRulesSection opens with reference to INPUT SOURCE CLASSIFICATION factual base
    - MODE section does NOT duplicate VISUAL REFERENCE TRANSFER RULE denylist verbatim
    - MODE section states abstract attributes come from style reference per transfer rule
    - Preserves format/proportions and no new facts/offers/CTAs rules from legacy block
  </behavior>
  <action>
In `per-mode-prompt-rules.ts` add:

- `RESTYLING_ENTITY_LOCK_CHECKLIST` — bullet list: people/subject, product, offer, CTA text, brand/logo, factual claims — all locked to base image per INPUT SOURCE CLASSIFICATION (MODE-03)
- `buildRestylingModeRulesSection(): string[]` — header `MODE: restyling — Apply abstract visual language from style reference to factual base content.`; entity lock checklist; line `Abstract style attributes (rhythm, texture, chroma, typography, lighting, compositional logic) may transfer from the style reference per VISUAL REFERENCE TRANSFER RULE above — never factual tokens.`; preserve base format/proportions; do not invent new facts

Update `buildPerModeRulesSection` to call `buildRestylingModeRulesSection()` for `restyling`.

Add tests `restyling factual entities (MODE-03)`:
- Prompt contains entity lock / base-locked entities
- `extractPromptModeSection` does NOT contain full denylist duplicates (no second "DENYLIST" block inside MODE)
- Transfer section still present and precedes MODE (existing ordering test extended)
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "restyling factual entities"</automated>
  </verify>
  <done>Restyling pack exports checklist; tests pass before prompt-builder migration.</done>
</task>

<task id="118-02-02" type="auto">
  <name>Migrate restyling MODE wiring and update regression snapshots</name>
  <files>app/src/server/ai/prompt-builder.ts, app/tests/unit/ai/quality-prompt-regression.test.ts</files>
  <action>
Remove inline `else if (generationMode === "restyling") { parts.push("MODE: restyling…") }` block from prompt-builder — restyling MODE now solely from `buildPerModeRulesSection`.

Keep `RESTYLING FACTUAL-SOURCE RULE` block in prompt-builder (Phase 117) — do NOT move to per-mode module unless consolidating without behavior change.

Update `quality-prompt-regression.test.ts` restyling branch:
- Assert mode section contains entity lock language
- Update inline snapshots in `compact prompt section snapshots` if restyling mode text changed

Ensure existing tests still pass:
- `restyling prompt contains allowlist terms and denylist terms` (transfer section, not MODE)
- `Extracted Visual Token Brief` excluded for restyling
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/ai/quality-prompt-regression.test.ts -t "restyling"</automated>
  </verify>
  <done>Restyling prompts use consolidated MODE pack; regression tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| style reference → restyling output | Factual leakage from style asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03 | Tampering | restyling MODE vs transfer rule | mitigate | Single denylist in transfer rule; MODE references it |
| T-118-04 | Tampering | style reference factual tokens | transfer | Phase 117 transfer rule + gate Phase 120 |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/ai/quality-prompt-regression.test.ts -t "restyling"` passes.
</verification>

<success_criteria>
- MODE-03: restyling MODE preserves base factual entities; abstract-only style transfer via cross-reference to Phase 117 rule
</success_criteria>

<output>
After completion, create `.planning/phases/118-per-mode-prompt-rules/118-02-SUMMARY.md`
</output>
