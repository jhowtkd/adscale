---
phase: 94-learning-closure-threshold-tune
plan: 02
type: execute
autonomous: true
wave: 1
depends_on: []
requirements: [READY-10]
---

# Plan 94-02: Readiness Threshold Tune (ctaProminence)

## Objective

Adjust readiness blocking so `ctaProminence` scores no longer hard-block derivation when other dimensions are acceptable. Document override evidence from SESS-03.

## Must Haves

- `ctaProminence` dimension scores surface as suggestions/warnings, not blocking issues (unless preflight critical issue).
- `94-THRESHOLD-EVIDENCE.md` cites session IDs and override rates.
- Unit tests cover non-blocking `ctaProminence` with low score.
- `npm test` for creative-readiness passes.

## Tasks

### Task 1: Tune blocking logic

Exclude `ctaProminence` from dimension-level blocking in `creative-readiness.ts`.

### Task 2: Evidence + tests

Add `94-THRESHOLD-EVIDENCE.md` and regression test for ctaProminence score 20 not blocking.
