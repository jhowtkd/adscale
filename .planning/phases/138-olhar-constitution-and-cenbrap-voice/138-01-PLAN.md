---
phase: 138-olhar-constitution-and-cenbrap-voice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/olhar/constitution.ts
  - app/src/server/ai/olhar/constitution.test.ts
  - app/src/server/ai/olhar/vocabulary.ts
  - app/scripts/check-olhar-vocabulary.mjs
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/per-mode-prompt-rules.ts
  - app/src/server/ai/preflight-analysis.ts
  - app/src/server/ai/observable-rubric.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/src/server/ai/creative-qa.test.ts
  - app/src/server/ai/creative-score.test.ts
autonomous: true
requirements:
  - OLHAR-01
  - OLHAR-03
must_haves:
  truths:
    - "`Olhar ADScale` exists as an implementation-facing module with figure, gestalt, voice, invite and export-separation principles"
    - "Core prompt/rubric language no longer uses `clickable-looking`, `CTA module`, `UI modules`, `CTA button`, or `THREE-ZONE VISUAL BUDGET` as default creative instructions"
    - "CTA is described as invite/reading path unless the source creative visibly uses a button"
    - "Vocabulary audit fails if UI-first terms return to core AI prompt/rubric files"
    - "Existing factual preservation rules remain intact while being reframed away from interface-module language"
  artifacts:
    - path: app/src/server/ai/olhar/constitution.ts
      provides: Global Olhar ADScale principles and prompt-section builder
    - path: app/src/server/ai/olhar/vocabulary.ts
      provides: Forbidden UI-first terms and replacement vocabulary
    - path: app/scripts/check-olhar-vocabulary.mjs
      provides: Deterministic vocabulary audit for core AI prompt/rubric files
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/olhar/constitution.ts
      via: buildOlharAdscaleSection import
      pattern: "buildOlharAdscaleSection"
    - from: app/scripts/check-olhar-vocabulary.mjs
      to: app/src/server/ai/olhar/vocabulary.ts
      via: forbidden vocabulary source of truth
      pattern: "FORBIDDEN_UI_FIRST_CREATIVE_TERMS"
---

# Plan 138-01: Olhar Constitution and Vocabulary Inventory

<objective>
Create the global `Olhar ADScale` foundation and remove UI-first creative vocabulary from the core prompt/rubric sections that currently push outputs toward interface-like layouts.

Purpose: Phase 139 and Phase 140 need a stable creative language before they implement dual verdicts and advisor rewrites.
Output: `olhar/` foundation module, vocabulary audit, prompt/rubric language cleanup and focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-CONTEXT.md
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-RESEARCH.md
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VALIDATION.md
@.planning/REQUIREMENTS.md
@app/CLAUDE.md

<interfaces>
Current problematic source language:
```typescript
// prompt-builder.ts
"CTA module placement"
"UI modules"

// preflight-analysis.ts
"ctaProminence: is the call-to-action visible, contrasting, and clickable-looking?"

// per-mode-prompt-rules.ts
"CTA module architecture"
"THREE-ZONE VISUAL BUDGET"
"CTA buttons"

// observable-rubric.ts
"CTA button"
"button-like modules"
"card grid"
```

New conceptual vocabulary:
```typescript
export type OlharAxis = "figura" | "gestalt" | "voz" | "convite";
export type FutureOlharVerdict = "pronta" | "quase" | "sem_opiniao" | "confusa";
```

Do not change persistence schemas or API response contracts in this plan. Keep compatibility with old `ctaProminence`/score fields until later phases migrate them.
</interfaces>
</context>

<tasks>

<task id="138-01-01" type="auto" tdd="true">
  <name>Global Olhar constitution module</name>
  <files>
    app/src/server/ai/olhar/constitution.ts
    app/src/server/ai/olhar/constitution.test.ts
    app/src/server/ai/prompt-builder.ts
  </files>
  <behavior>
    - `OLHAR_AXES` exposes `figura`, `gestalt`, `voz`, `convite`
    - `buildOlharAdscaleSection()` returns prompt lines containing dominant idea, gestalt, voice, invite and export separation
    - The section explicitly says compliance/export is a second pass and CTA is an invite, not a widget
    - The section does not contain forbidden UI-first terms from `vocabulary.ts`
  </behavior>
  <action>
    Create `app/src/server/ai/olhar/constitution.ts`:
    - Export `OLHAR_AXES`
    - Export `OLHAR_VERDICTS`
    - Export `OLHAR_ADSCALE_PRINCIPLES` as structured data
    - Export `buildOlharAdscaleSection(): string[]`
    - Export `summarizeOlharAxis(axis)` if helpful for tests

    Add `constitution.test.ts` covering behavior above.

    Inject `buildOlharAdscaleSection()` into `buildIntegrityPromptSection()` in `prompt-builder.ts`, before the legacy factual/anti-hallucination rules. Keep factual preservation rules active.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/olhar/constitution.test.ts src/server/ai/prompt-builder.test.ts -x</automated>
  </verify>
  <done>Global Olhar section exists, is test-covered, and reaches prompt builder without changing public schemas.</done>
</task>

<task id="138-01-02" type="auto" tdd="true">
  <name>Vocabulary audit and prompt language cleanup</name>
  <files>
    app/src/server/ai/olhar/vocabulary.ts
    app/scripts/check-olhar-vocabulary.mjs
    app/src/server/ai/prompt-builder.ts
    app/src/server/ai/per-mode-prompt-rules.ts
    app/src/server/ai/preflight-analysis.ts
    app/src/server/ai/observable-rubric.ts
    app/src/server/ai/prompt-builder.test.ts
    app/src/server/ai/creative-qa.test.ts
    app/src/server/ai/creative-score.test.ts
  </files>
  <behavior>
    - `node app/scripts/check-olhar-vocabulary.mjs` fails when a scanned prompt/rubric file contains forbidden terms as live prompt language
    - The audit scans only core AI prompt/rubric files, not UI component files
    - `preflight-analysis.ts` no longer says `clickable-looking`
    - `prompt-builder.ts` and `per-mode-prompt-rules.ts` no longer describe CTA as a module by default
    - `observable-rubric.ts` still detects overload/template/dominant idea failures without using UI-first phrasing as the preferred creative model
  </behavior>
  <action>
    Create `app/src/server/ai/olhar/vocabulary.ts` with:
    - `FORBIDDEN_UI_FIRST_CREATIVE_TERMS`
    - `UI_FIRST_REPLACEMENTS`
    - `SCANNED_OLHAR_PROMPT_FILES`
    - `ALLOWED_CONTEXT_PATTERNS` for tests/docs/script self-reference if needed

    Create `app/scripts/check-olhar-vocabulary.mjs`:
    - Load scanned files from the repo root.
    - Report file, line and term for any violation.
    - Exit 1 on violations.
    - Skip its own file and test files.

    Update prompt/rubric text:
    - Replace `clickable-looking` with reading-path/invite language.
    - Replace `CTA module placement` with invite placement/reading path.
    - Replace `UI modules` with information groups or visible information.
    - Replace `THREE-ZONE VISUAL BUDGET` with a less rigid reading-path/gestalt budget while preserving the "no equal-weight overload" intent.
    - Replace `CTA button(s)` with invite/CTA text unless the sentence specifically describes a visible source button.

    Update focused tests that assert old prompt strings.
  </action>
  <verify>
    <automated>node app/scripts/check-olhar-vocabulary.mjs && cd app && npm test -- src/server/ai/prompt-builder.test.ts src/server/ai/creative-qa.test.ts src/server/ai/creative-score.test.ts -x</automated>
  </verify>
  <done>Vocabulary audit passes and core prompt/rubric files no longer default to UI/widget/module language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt foundation -> image generation | Prompt language can bias model output toward interface-like designs |
| Vocabulary audit -> developer workflow | A weak audit can create false confidence while old language remains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | Prompt text | mitigate | Deterministic vocabulary audit with line-level failures |
| T-138-02 | Information | Prompt section | mitigate | No secrets or campaign-specific private data in global Olhar section |
| T-138-03 | Reliability | Factual preservation | mitigate | Keep existing anti-hallucination and mandatory-fact language while changing composition vocabulary |
| T-138-04 | Spoofing | Creative quality claims | mitigate | Do not claim output improvement in this phase; this is foundation only |
</threat_model>

<verification>
Per @.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VALIDATION.md:
- `cd app && npm test -- src/server/ai/olhar/constitution.test.ts src/server/ai/prompt-builder.test.ts -x`
- `node app/scripts/check-olhar-vocabulary.mjs`
- `cd app && npm test -- src/server/ai/prompt-builder.test.ts src/server/ai/creative-qa.test.ts src/server/ai/creative-score.test.ts -x`
</verification>

<success_criteria>
- OLHAR-01: global art-direction constitution exists and can be injected into prompts.
- OLHAR-03: core creative prompt/rubric vocabulary no longer defaults to UI/widget/module framing.
</success_criteria>

<output>
After completion, create `.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-01-SUMMARY.md`.
</output>
