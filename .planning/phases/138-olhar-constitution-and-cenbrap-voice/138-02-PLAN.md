---
phase: 138-olhar-constitution-and-cenbrap-voice
plan: "02"
type: execute
wave: 2
depends_on:
  - 138-01
files_modified:
  - app/src/server/ai/voices/client-voice.ts
  - app/src/server/ai/voices/client-voice.test.ts
  - app/src/server/ai/voices/cenbrap.ts
  - app/src/server/ai/olhar/art-direction-verdict.ts
  - app/src/server/ai/olhar/art-direction-verdict.test.ts
  - app/src/server/ai/creative-corpus.ts
  - .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md
autonomous: true
requirements:
  - OLHAR-02
  - OLHAR-04
must_haves:
  truths:
    - "Cenbrap has a first client voice overlay with principles, anti-references, authority handling, CTA rhythm and correct-but-soulless examples"
    - "Client voice resolver returns Cenbrap voice for campaign/client strings containing CENBRAP and falls back safely for unknown clients"
    - "Existing visual hard failure codes map to future art-direction verdict language"
    - "`generic_template_aesthetic` and `decorative_only_variation` map to `sem_opiniao`"
    - "`missing_dominant_idea` and `visual_overload` map to `confusa`"
    - "Jhonatan review checkpoint exists even if status remains pending"
  artifacts:
    - path: app/src/server/ai/voices/cenbrap.ts
      provides: First client voice overlay for Cenbrap
    - path: app/src/server/ai/voices/client-voice.ts
      provides: Voice resolver and prompt-section builder
    - path: app/src/server/ai/olhar/art-direction-verdict.ts
      provides: Visual failure to creative verdict mapping
    - path: .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md
      provides: Manual creative-director review checkpoint
  key_links:
    - from: app/src/server/ai/voices/client-voice.ts
      to: app/src/server/ai/voices/cenbrap.ts
      via: resolveClientVoice
      pattern: "cenbrap"
    - from: app/src/server/ai/olhar/art-direction-verdict.ts
      to: app/src/server/ai/creative-quality-gate.ts
      via: existing CreativeHardFailureCode type
      pattern: "generic_template_aesthetic|visual_overload|missing_dominant_idea|decorative_only_variation"
---

# Plan 138-02: Cenbrap Voice and Failure Mapping

<objective>
Create the first client voice overlay for Cenbrap and map existing visual failures into future art-direction verdict semantics.

Purpose: Phase 140 needs a client voice to inject into advisor/generation prompts, and Phase 139 needs a clear bridge from existing hard failures to the new `Olhar` verdict language.
Output: voice resolver, Cenbrap voice, art-direction verdict mapping, tests and owner review artifact.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-CONTEXT.md
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-RESEARCH.md
@.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VALIDATION.md
@.planning/advisor-redesign-v3.md
@app/src/server/ai/creative-corpus.ts
@app/src/server/ai/creative-quality-gate.ts

<interfaces>
Recommended voice contract:
```typescript
export interface ClientVoice {
  id: string;
  displayName: string;
  matchTerms: string[];
  principles: string[];
  positiveSignals: string[];
  negativeSignals: string[];
  authorityAndClaims: string[];
  inviteRhythm: string[];
  correctButSoulless: string[];
  buildPromptSection(): string[];
}
```

Recommended future verdict contract:
```typescript
export type ArtDirectionVerdict = "pronta" | "quase" | "sem_opiniao" | "confusa";
export type ArtDirectionFailureReason =
  | "generic_template_aesthetic"
  | "decorative_only_variation"
  | "missing_dominant_idea"
  | "visual_overload";
```

Do not persist these verdicts to derivation rows yet. That belongs to Phase 139.
</interfaces>
</context>

<tasks>

<task id="138-02-01" type="auto" tdd="true">
  <name>Client voice resolver and Cenbrap voice</name>
  <files>
    app/src/server/ai/voices/client-voice.ts
    app/src/server/ai/voices/client-voice.test.ts
    app/src/server/ai/voices/cenbrap.ts
    app/src/server/ai/creative-corpus.ts
  </files>
  <behavior>
    - `resolveClientVoice({ client: "CENBRAP" })` returns the Cenbrap voice
    - `resolveClientVoice({ name: "Cenbrap em Dobro" })` returns the Cenbrap voice
    - Unknown clients return null without throwing
    - `buildClientVoicePromptSection()` returns no section for null voice
    - Cenbrap section contains authority, education, professors/people, claims caution, invite rhythm and anti-template guidance
    - Cenbrap section avoids prescribing a fixed layout template
  </behavior>
  <action>
    Create `app/src/server/ai/voices/cenbrap.ts` with a first code-facing voice overlay:
    - `id: "cenbrap"`
    - `displayName: "Cenbrap"`
    - `matchTerms: ["cenbrap", "cenbrap em dobro", "imersao nr1", "nr1"]`
    - Principles for serious education/authority, not growth-template gloss.
    - Positive signals: confident hierarchy, readable authority cues, human/professor presence when present in source, restrained proof, invitation that closes the idea.
    - Negative signals: neon/glass template, smartphone mockup without reason, generic tech gradient, crowded badge wall, CTA as fake app button by default, stock-medical mood detached from source.
    - Claims caution: MEC/certification must be preserved only when visible or briefed; never invent authority.
    - Correct-but-soulless examples.

    Create `client-voice.ts`:
    - Export `ClientVoice`
    - Export `resolveClientVoice({ name, client, product })`
    - Export `buildClientVoicePromptSection(voice)`

    Optionally extend `creative-corpus.ts` only if a shared match helper prevents duplication. Do not change allowed-entity behavior in this plan.

    Add `client-voice.test.ts` for matching and prompt-section behavior.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/voices/client-voice.test.ts -x</automated>
  </verify>
  <done>Cenbrap voice resolves correctly and has test-covered prompt-section output.</done>
</task>

<task id="138-02-02" type="auto" tdd="true">
  <name>Art-direction failure mapping</name>
  <files>
    app/src/server/ai/olhar/art-direction-verdict.ts
    app/src/server/ai/olhar/art-direction-verdict.test.ts
    .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md
  </files>
  <behavior>
    - `generic_template_aesthetic` maps to `sem_opiniao`
    - `decorative_only_variation` maps to `sem_opiniao`
    - `missing_dominant_idea` maps to `confusa`
    - `visual_overload` maps to `confusa`
    - Non-art-direction hard failures return null or export-only classification
    - Multiple failures choose the most severe art-direction verdict in deterministic order
    - `138-VOICE-REVIEW.md` exists with `status: pending_review` by default
  </behavior>
  <action>
    Create `art-direction-verdict.ts`:
    - Export `ArtDirectionVerdict`
    - Export `ART_DIRECTION_FAILURE_TO_VERDICT`
    - Export `ART_DIRECTION_FAILURE_REASONS`
    - Export `resolveArtDirectionVerdictFromFailures(failures)`
    - Return `null` for export/factual failures such as `wrong_brand`, `cta_drift`, `unsupported_offer`, `unreadable_required_text`, `invalid_format_layout`

    Create `art-direction-verdict.test.ts` covering all visual mappings and at least two export-only non-mappings.

    Create `138-VOICE-REVIEW.md`:
    - Frontmatter: `status: pending_review`
    - Checklist for Jhonatan: "represents my eye", "too generic", "too rigid", "missing Cenbrap nuance", "approved for Phase 140 injection"
    - Space for corrections and dated sign-off.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/olhar/art-direction-verdict.test.ts -x && cd .. && test -f .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md && rg -n "status: pending_review|status: approved|status: changes_requested" .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md</automated>
  </verify>
  <done>Visual failure mapping and voice review checkpoint exist and are test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Campaign/client strings -> voice resolver | Generic names or test campaigns can falsely select a voice |
| Visual hard failures -> future creative verdict | Incorrect mapping can blur art-direction and export failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-05 | Spoofing | Client voice resolver | mitigate | Match on bounded terms and return null for unknown clients |
| T-138-06 | Tampering | Voice prompt text | mitigate | Tests ensure voice contains required sections and avoids fixed-layout prescription |
| T-138-07 | Elevation | Export failures as art verdicts | mitigate | Export/factual failures map to null in art-direction mapping |
| T-138-08 | Repudiation | Voice approval | mitigate | `138-VOICE-REVIEW.md` captures pending/approved/changes_requested status |
</threat_model>

<verification>
Per @.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VALIDATION.md:
- `cd app && npm test -- src/server/ai/voices/client-voice.test.ts -x`
- `cd app && npm test -- src/server/ai/olhar/art-direction-verdict.test.ts -x`
- `test -f .planning/phases/138-olhar-constitution-and-cenbrap-voice/138-VOICE-REVIEW.md`
</verification>

<success_criteria>
- OLHAR-02: Cenbrap voice exists, resolves deterministically and has an owner review checkpoint.
- OLHAR-04: existing visual failure concepts map to first-class art-direction verdict language.
</success_criteria>

<output>
After completion, create `.planning/phases/138-olhar-constitution-and-cenbrap-voice/138-02-SUMMARY.md`.
</output>
