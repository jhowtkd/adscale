---
phase: 44-native-format-adaptation
plan: "02"
type: execute
wave: 2
depends_on:
  - "44-01"
files_modified:
  - .planning/phases/44-native-format-adaptation/44-UAT.md
autonomous: false
requirements:
  - FMT-01
  - FMT-02
  - FMT-03
user_setup: []
must_haves:
  truths:
    - A real 4:5 format adaptation output has been generated or a concrete model/API failure has been recorded.
    - A real 9:16 format adaptation output has been generated or a concrete model/API failure has been recorded.
    - Visual evidence distinguishes accepted native layouts from failed poster/band/crowding outputs.
  artifacts:
    - .planning/phases/44-native-format-adaptation/44-UAT.md records the campaign/asset, output keys or image paths, visual verdicts, and residual risk.
    - A local contact sheet or screenshot path is recorded when image outputs exist.
  key_links:
    - Visual evidence uses the real derivation flow after Plan 44-01 changes, not a standalone mock.
    - UAT verdict maps directly to FMT-01, FMT-02, and FMT-03.
---

# Plan 44-02: Real Visual UAT Evidence

<objective>
Generate and review real 4:5 and 9:16 format adaptation outputs so Phase 44 is not marked complete on tests alone.

Purpose: prove that `Variar tamanho` now produces native target-format layouts, or capture concrete model failure evidence for the later quality-gate phase.

Output: `44-UAT.md` with generated output evidence, visual verdicts, contact sheet path, and a human visual approval checkpoint.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
@/Users/jhonatan/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-native-format-adaptation/44-CONTEXT.md
@.planning/phases/44-native-format-adaptation/44-RESEARCH.md
@.planning/phases/44-native-format-adaptation/44-VALIDATION.md
@.planning/phases/44-native-format-adaptation/44-01-SUMMARY.md
@app/AGENTS.md
@app/src/app/api/campaigns/[id]/derivations/route.ts
@app/src/server/jobs/derivation.ts
@app/src/lib/hooks/use-derivations.ts
@app/src/components/workspace/FormatAdaptationConfigModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare a real format-adaptation UAT target</name>
  <files>.planning/phases/44-native-format-adaptation/44-UAT.md</files>
  <action>
    Identify a real campaign asset suitable for 1:1 -> 4:5 and 1:1 -> 9:16 format adaptation.

    Preferred target:
    - Campaign `fd018597-f2c8-49f5-86d9-7ca82267605c`, because it produced the original bad outputs.

    If that campaign is inaccessible through the current workspace/session, do not spend Phase 44 fixing workspace access UI. Instead:
    - document the access mismatch in `44-UAT.md`,
    - select another accessible campaign with a base asset, or create a minimal local campaign fixture through the existing app/API path,
    - keep the UAT focused on the real format adaptation pipeline.

    In `44-UAT.md`, record:
    - campaign id,
    - workspace id if known,
    - base asset id/key,
    - selected target formats (`4:5`, `9:16`),
    - whether the UAT used the UI, API, or direct job path.
  </action>
  <verify>test -f .planning/phases/44-native-format-adaptation/44-UAT.md && rg -n "campaign|base asset|4:5|9:16" .planning/phases/44-native-format-adaptation/44-UAT.md</verify>
  <done>
    `44-UAT.md` identifies a real campaign/base asset or documents why the original campaign could not be used and what accessible fixture replaced it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and collect 4:5 and 9:16 outputs</name>
  <files>.planning/phases/44-native-format-adaptation/44-UAT.md</files>
  <action>
    Run the real generation path after Plan 44-01 is complete.

    Required behavior:
    - Queue exactly `format_adaptation` outputs for `4:5` and `9:16`.
    - Prefer non-preview generation so the result reflects production behavior.
    - Wait for derivations to reach completed or failed state.
    - Download or open the generated outputs from their signed URLs/object keys.
    - Create a contact sheet under `tmp/phase-44-format-uat/` if both images are available.

    Record in `44-UAT.md`:
    - derivation ids,
    - output keys or signed URL notes,
    - exact generated dimensions after normalization,
    - contact sheet absolute path if created,
    - any API/model/job failure with exact error text.

    Do not mark model/API failure as success. If either target cannot be generated, record the blocker and keep the verdict as failed/inconclusive.
  </action>
  <verify>rg -n "derivation|output|dimensions|contact|failed|inconclusive" .planning/phases/44-native-format-adaptation/44-UAT.md</verify>
  <done>
    `44-UAT.md` contains either completed 4:5 and 9:16 output evidence or a concrete failure record for each missing output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Perform visual verdict against native-layout criteria</name>
  <files>.planning/phases/44-native-format-adaptation/44-UAT.md</files>
  <action>
    Inspect the generated outputs directly, using the contact sheet or individual images.

    Apply the Phase 44 criteria:
    - 4:5: no blurred bands, no pasted square poster, no crowded text/photo/CTA/logo cluster, portrait-feed spacing, critical facts inside safe areas.
    - 9:16: no blurred bands, no pasted square poster, clear vertical zones, important content inside safe areas, no critical crop.
    - Both: decorative background may bleed/crop; factual modules may not be cut or dropped.

    Record a verdict per output in `44-UAT.md`:
    - `accepted` only if the output looks native and passes all hard visual criteria,
    - `failed` if it shows bands, pasted-poster composition, crowding, or critical crop,
    - `inconclusive` only for unavailable outputs or environment blockers.

    If the agent's visual assessment is ambiguous, mark it as ambiguous and ask for user approval rather than calling it done.
  </action>
  <verify>rg -n "4:5 verdict|9:16 verdict|accepted|failed|inconclusive|no blurred|no pasted|no crowded" .planning/phases/44-native-format-adaptation/44-UAT.md</verify>
  <done>
    `44-UAT.md` has explicit 4:5 and 9:16 visual verdicts tied to the hard visual criteria from `44-CONTEXT.md`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 44 visual UAT evidence recorded in `.planning/phases/44-native-format-adaptation/44-UAT.md`; contact sheet path listed there when available.</what-built>
  <how-to-verify>Inspect the contact sheet or generated outputs shown by the agent. Approve only if 4:5 and 9:16 look like native ad layouts: no blurred bands, no square poster pasted in the center, no crowded modules, and no critical information cropped.</how-to-verify>
  <resume-signal>Type "approved" or describe the visual issues.</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `44-UAT.md` exists and records campaign/base asset source.
- [ ] `44-UAT.md` records 4:5 and 9:16 derivation evidence or concrete failure.
- [ ] Generated 4:5 and 9:16 outputs have explicit visual verdicts.
- [ ] User visual approval checkpoint is passed, or the plan summary records the exact unresolved visual issue.
</verification>

<success_criteria>
- Real UAT evidence exists for both target formats, or concrete model/API failure evidence is recorded.
- User-visible criteria from `44-CONTEXT.md` are applied directly.
- No output with bands, pasted-poster layout, crowded modules, or critical crop is marked accepted.
- Phase 44 has enough visual evidence for `$gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/44-native-format-adaptation/44-02-SUMMARY.md`.
</output>
