---
phase: 185
plan: 01
slug: journey-cards-and-start-composer
status: complete
created: 2026-06-26
wave: 1
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-04
depends_on:
  - 184-02
files_modified:
  - app/src/components/assistant/AssistantJourneyCards.tsx
  - app/src/components/assistant/AssistantJourneyCards.test.tsx
  - app/src/components/assistant/AssistantStartComposer.tsx
  - app/src/components/assistant/AssistantStartComposer.test.tsx
  - app/src/lib/hooks/use-guided-flow.ts
  - app/messages/pt-BR.json
  - app/messages/en.json
---

# Plan 185-01 - Journey Cards and Start Composer

## Goal

Present two equal-weight guided journey cards on `/assistant` start while preserving the freeform composer and mobile parity.

## Context

Use `.planning/phases/185-assistant-entry-ux/185-CONTEXT.md`. Cards sit above the existing composer in `AssistantStartComposer`; clicking creates a thread then PATCHes guided-flow with path and initial step.

## Tasks

### 185-01-01 - Journey cards component

**Requirements:** ENTRY-01

<task>
Create `AssistantJourneyCards` with two cards (`existing_creative`, `from_zero`), icons, i18n titles/subtitles under `assistant.start.journeys`, responsive 2-col desktop / stacked mobile grid, `data-testid` hooks.
</task>

<verify automated>
cd app && npm test -- --run src/components/assistant/AssistantJourneyCards.test.tsx
</verify>

### 185-01-02 - Wire start composer to guided-flow upsert

**Requirements:** ENTRY-01, ENTRY-04

<task>
Extend `AssistantStartComposer` to render cards above composer. On card click: create thread, `useUpsertGuidedFlow` with `initialStepForPath(path)`, navigate to thread. Same surface for desktop/mobile — no duplicate path selectors in sidebar.
</task>

<verify automated>
cd app && npm test -- --run src/components/assistant/AssistantStartComposer.test.tsx
</verify>

## Must Haves

- truths:
  - `/assistant` start shows `Já tenho peça` and `Produzir do zero` as primary cards above composer
  - Card click creates thread and persists guided path with correct initial step
  - Mobile and desktop share the same start surface without hidden primary actions
- artifacts:
  - app/src/components/assistant/AssistantJourneyCards.tsx
  - app/src/components/assistant/AssistantStartComposer.tsx
  - app/src/lib/hooks/use-guided-flow.ts
- key_links:
  - from: AssistantStartComposer.startJourney
    to: /api/assistant/threads/[threadId]/guided-flow
    via: useUpsertGuidedFlow mutation

## Verification

```bash
cd app && npm test -- --run src/components/assistant/AssistantJourneyCards.test.tsx src/components/assistant/AssistantStartComposer.test.tsx
```
