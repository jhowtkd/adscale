---
phase: 64-client-approval-package
plan: "02"
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - app/src/lib/hooks/use-approval-package.ts
  - app/src/lib/hooks/use-approval-package.test.tsx
  - app/src/components/workspace/ClientApprovalPackagePanel.tsx
  - app/src/components/workspace/ClientApprovalPackagePanel.test.tsx
  - app/src/app/share/[token]/page.tsx
  - app/src/lib/hooks/use-campaign-workspace.ts
  - app/src/app/(dashboard)/campaigns/[id]/page.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
requirements:
  - DELIVER-01
  - DELIVER-02
  - DELIVER-03
  - DELIVER-04
must_haves:
  truths:
    - User selects approved derivations and creates/refreshes package from campaign workspace.
    - Panel shows formats, notes, status, download and share actions.
    - Share page uses signed asset URLs.
  artifacts:
    - ClientApprovalPackagePanel with tests.
    - Hook and campaign workspace integration.
---

# Plan 64-02: Approval Package UI

<objective>
Add client approval package panel, hook, i18n, share page signed URLs, and campaign workspace wiring.

Output: ClientApprovalPackagePanel, use-approval-package, integration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: UI panel and integration</name>
  <files>ClientApprovalPackagePanel, use-approval-package, campaign page, share page, i18n</files>
  <action>
    Build panel with multi-select, notes, status, share copy, downloads, refresh; wire into campaign workspace actions area.
  </action>
  <verify>cd app && npm test -- src/components/workspace/ClientApprovalPackagePanel.test.tsx src/lib/hooks/use-approval-package.test.tsx && npm run lint && npm run build</verify>
  <done>Tests, lint, and build pass.</done>
</task>

</tasks>

<verification>
- Component and hook tests pass
- `npm run lint` and `npm run build`
</verification>

<output>
After completion, create `.planning/phases/64-client-approval-package/64-02-SUMMARY.md` and `64-VERIFICATION.md`.
</output>
