---
phase: 72-build-and-data-integrity-hardening
plan: 01
type: execute
autonomous: true
wave: 1
depends_on: []
requirements: [DATA-01, DATA-02, DATA-03]
---

# Plan 72-01: Data Integrity Regression Closure

## Objective

Verify and close the v11.7 data integrity findings: mission insight runtime validation and progression snapshot atomic persistence.

## Must Haves

- Invalid mission insight `missionKey` values are rejected before feedback records are created.
- Progression snapshot persistence is conflict-safe for concurrent first access.
- Regression tests cover both behaviors.
- Existing fixes are verified and reused when already correct.

## Tasks

### Task 1: Mission insight runtime validation (auto)

- Inspect `app/src/server/mission-insights/sanitize.ts`.
- Confirm `missionKey` is validated against known mission keys before casting to `MissionKey`.
- If missing or incomplete, validate against `MISSION_ORDER` or `MISSION_DEFINITIONS`.
- Confirm invalid mission keys cause `/api/workspace/mission-insights` to return validation failure and do not call `recordMissionInsight`.

<automated>
cd app && npm test -- src/server/mission-insights src/app/api/workspace/mission-insights
</automated>

### Task 2: Progression atomic upsert (auto)

- Inspect `app/src/server/repositories/progression.ts`.
- Confirm `upsertWorkspaceProgressionSnapshot` uses Drizzle `.onConflictDoUpdate()` targeting `workspaceProgression.workspaceId`, or an equivalent conflict-safe path.
- If the repository still uses select-then-insert, replace it with atomic upsert.
- Keep workspace-scoped behavior unchanged.

<automated>
cd app && npm test -- src/server/repositories/progression src/server/progression
</automated>

### Task 3: Regression coverage review (auto)

- Confirm tests cover invalid `missionKey` and upsert conflict behavior.
- Add or adjust tests only where coverage is missing.
- Keep tests focused on phase requirements; do not broaden into Phase 73 resume UX or Phase 74 migration/UAT.

<automated>
cd app && npm test -- src/server/mission-insights src/server/repositories/progression src/server/progression src/app/api/workspace/mission-insights
</automated>

## Verification

- Focused backend tests pass.
- No mission resume UX, migration, or UAT files are modified by this plan.
