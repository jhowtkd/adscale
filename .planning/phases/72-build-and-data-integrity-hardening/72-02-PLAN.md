---
phase: 72-build-and-data-integrity-hardening
plan: 02
type: execute
autonomous: true
wave: 2
depends_on: [72-01]
requirements: [STAB-01, STAB-02]
---

# Plan 72-02: Build, Focused Suite, and Verification Evidence

## Objective

Prove Phase 72 is release-gate clean by running the required build/lint/test bundle and recording evidence in the phase directory.

## Must Haves

- `npm run build` passes without disabling Next.js TypeScript checks.
- The `mission` feedback category is handled in exhaustive category maps.
- Lint and the focused progression/missions/insights/feedback test suite pass.
- `72-VERIFICATION.md` records command summaries, results, and caveats.

## Tasks

### Task 1: Build blocker verification (auto)

- Inspect `app/src/server/ai/regeneration-correction-brief.ts`.
- Confirm `FeedbackCategory` exhaustive labels include `"mission"`.
- Run `npm run build`; fix any Phase 72-related type errors that remain.
- Do not set `typescript.ignoreBuildErrors`.

<automated>
cd app && npm run build
</automated>

### Task 2: Lint and focused suite (auto)

- Run lint and document whether warnings are pre-existing or newly introduced.
- Run the focused progression/missions/insights/feedback suite from the validation strategy.
- Fix any failures caused by Phase 72 scope.

<automated>
cd app && npm run lint && npm test -- src/server/progression src/server/repositories/progression src/app/api/workspace/progression src/lib/hooks/use-progression src/components/dashboard/AdsScientistProgressCard src/app/api/workspace/missions src/lib/hooks/use-missions src/components/dashboard/MissionPathCard src/app/api/workspace/mission-insights src/server/mission-insights src/lib/progression src/server/feedback/mission-credit-signals
</automated>

### Task 3: Verification evidence (auto)

- Create `.planning/phases/72-build-and-data-integrity-hardening/72-VERIFICATION.md`.
- Include: files inspected/fixed, commands run, pass/fail results, known caveats, and explicit handoff to Phase 73/74.
- Mark Phase 72 blockers as closed only if build, lint, and focused tests pass.

<manual>
Read `72-VERIFICATION.md` and confirm it is clear enough for beta readiness handoff.
</manual>

## Verification

- `72-VERIFICATION.md` exists and references all Phase 72 requirements.
- Build, lint, and focused suite results are recorded.
