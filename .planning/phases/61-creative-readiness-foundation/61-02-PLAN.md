---
phase: 61-creative-readiness-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "61-01"
files_modified:
  - app/src/components/workspace/CreativeReadinessPanel.tsx
  - app/src/components/workspace/CreativeReadinessPanel.test.tsx
  - app/src/components/workspace/PilotUploadPanel.tsx
  - app/src/components/workspace/PilotUploadPanel.test.tsx
  - app/src/components/workspace/PilotSidebar.tsx
  - app/src/lib/hooks/use-campaign-workspace.ts
  - app/messages/pt-BR.json
  - app/messages/en.json
autonomous: true
requirements:
  - READY-01
  - READY-02
  - READY-03
  - READY-04
  - READY-05
must_haves:
  truths:
    - Users can understand readiness before generating.
    - The readiness panel shows blocking issues above suggestions.
    - Rerun is available after brief or base creative changes.
    - Existing upload/analyze flow remains intact.
  artifacts:
    - Campaign workspace readiness panel.
    - Component tests for all readiness states.
    - Updated upload/workspace wiring.
  key_links:
    - `useAnalyzePreflight` and `usePreflightScore` remain the data path.
    - `PilotUploadPanel` keeps upload and analysis responsibilities.
    - `CreativeReadinessPanel` owns readiness display, not raw preflight parsing inside upload UI.
---

# Plan 61-02: Campaign Workspace Readiness UI

<objective>
Expose Creative Readiness Score in the campaign workspace so users can see whether the base creative and brief are ready before generation.

Output: readiness panel, workspace integration, rerun affordance, component tests, and final build/lint validation.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-creative-readiness-foundation/61-CONTEXT.md
@.planning/phases/61-creative-readiness-foundation/61-RESEARCH.md
@.planning/phases/61-creative-readiness-foundation/61-VALIDATION.md
@.planning/phases/61-creative-readiness-foundation/61-01-PLAN.md
@app/AGENTS.md
@app/src/components/workspace/PilotUploadPanel.tsx
@app/src/components/workspace/PilotSidebar.tsx
@app/src/lib/hooks/use-preflight.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CreativeReadinessPanel</name>
  <files>app/src/components/workspace/CreativeReadinessPanel.tsx, app/src/components/workspace/CreativeReadinessPanel.test.tsx</files>
  <action>
    Add a focused readiness display component.

    Required states:
    - no base creative yet;
    - pending/analyzing;
    - failed;
    - ready;
    - needs attention;
    - blocked.

    Required UI:
    - overall score;
    - six dimension rows;
    - blocking issues section before suggestions;
    - rerun button;
    - compact copy that explains this is pre-generation guidance, not a guarantee.

    Use restrained dashboard/workspace styling. Do not add a marketing hero or nested cards.
  </action>
  <verify>cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx</verify>
  <done>
    Component tests cover all states and prove blocking issues render before suggestions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire readiness into pilot upload/workspace flow</name>
  <files>app/src/components/workspace/PilotUploadPanel.tsx, app/src/lib/hooks/use-campaign-workspace.ts, app/src/components/workspace/PilotSidebar.tsx</files>
  <action>
    Integrate the readiness panel into the existing campaign workspace flow.

    Required behavior:
    - after upload/preflight succeeds, the readiness panel can render from the preflight/readiness response;
    - if the asset already has cached readiness, the panel can load it through `usePreflightScore`;
    - rerun calls `useAnalyzePreflight` with the explicit force/rerun option from Plan 61-01;
    - replacing the base creative updates the panel to the new asset;
    - generation entry points remain accessible, but readiness status is visible before action selection.

    Keep the existing upload analysis behavior and tests intact.
  </action>
  <verify>cd app && npm test -- src/components/workspace/PilotUploadPanel.test.tsx src/components/workspace/CreativeReadinessPanel.test.tsx</verify>
  <done>
    Existing upload flow still completes, and readiness panel updates after upload and rerun.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add localized readiness copy</name>
  <files>app/messages/pt-BR.json, app/messages/en.json</files>
  <action>
    Add PT-BR and EN messages for the readiness panel.

    Required copy:
    - title and short subtitle;
    - labels for ready, needs attention, blocked, analyzing, failed, and missing creative;
    - six dimension labels;
    - blocking issues and suggestions headings;
    - rerun action;
    - short disclaimer that readiness reduces risk but does not guarantee generated output quality.

    Keep copy concise and product-facing.
  </action>
  <verify>cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx</verify>
  <done>
    Component tests render localized labels without missing-key output.
  </done>
</task>

<task type="auto">
  <name>Task 4: Browser and build verification</name>
  <files>app/src/components/workspace/CreativeReadinessPanel.tsx, app/src/components/workspace/PilotUploadPanel.tsx, app/messages/pt-BR.json, app/messages/en.json</files>
  <action>
    Validate the workspace experience and final build.

    Required commands:
    - `cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx src/components/workspace/PilotUploadPanel.test.tsx`
    - `cd app && npm run lint`
    - `cd app && npm run build`

    Manual smoke:
    - start the app if needed;
    - open a campaign draft;
    - upload a base creative;
    - confirm readiness appears after analysis;
    - rerun readiness;
    - confirm generation actions remain visible but readiness is clearly shown first.

    Document manual evidence in `61-VERIFICATION.md` if execution performs it in this phase.
  </action>
  <verify>cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx src/components/workspace/PilotUploadPanel.test.tsx && npm run lint && npm run build</verify>
  <done>
    Focused component tests, lint, build, and browser smoke evidence are complete or documented with exact blockers.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cd app && npm test -- src/components/workspace/CreativeReadinessPanel.test.tsx`
- [ ] `cd app && npm test -- src/components/workspace/PilotUploadPanel.test.tsx`
- [ ] `cd app && npm run lint`
- [ ] `cd app && npm run build`
- [ ] Browser smoke confirms the readiness panel is visible in the campaign workspace.
</verification>

<success_criteria>
- READY-01 is visible to the user in the workspace.
- READY-02 dimensions are readable and stable.
- READY-03 blocking issues are separated from suggestions.
- READY-04 rerun and asset replacement update readiness state.
- READY-05 remains true because the UI consumes existing readiness/preflight primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/61-creative-readiness-foundation/61-02-SUMMARY.md`.
</output>
