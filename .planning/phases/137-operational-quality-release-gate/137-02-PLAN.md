---
phase: 137-operational-quality-release-gate
plan: "02"
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - app/scripts/run-operational-quality-release-gate.mjs
  - app/package.json
autonomous: true
requirements:
  - QALIVE-01
  - QALIVE-02
must_haves:
  truths:
    - "operational-quality-release-gate runs technical block to completion before operational block"
    - "Technical block reuses Phase 133 vitest matrix and sub-checkers"
    - "Operational block runs sampling and trend checkers plus 137 evidence validator"
    - "Process exits 1 only when technical block fails; operational insufficient_sample records without aborting technical reporting"
    - "--dry-run prints both block step matrices without executing commands"
  artifacts:
    - path: app/scripts/run-operational-quality-release-gate.mjs
      provides: QALIVE-01 operational release orchestrator with dual blocks
    - path: app/package.json
      provides: operational-quality-release-gate npm script
  key_links:
    - from: app/scripts/run-operational-quality-release-gate.mjs
      to: app/scripts/run-real-quality-release-gate.mjs
      via: TECHNICAL_STEPS reuse or execFileSync delegation
      pattern: "real-quality-release-gate|FOCUSED_CORPUS_EVAL_TESTS"
    - from: app/scripts/run-operational-quality-release-gate.mjs
      to: app/scripts/check-operational-quality-release-evidence.mjs
      via: OPERATIONAL_STEPS final checker
      pattern: check-operational-quality-release-evidence.mjs
    - from: app/scripts/run-operational-quality-release-gate.mjs
      to: .planning/phases/137-operational-quality-release-gate/137-EVIDENCE.json
      via: writeAutomatedStep updates both blocks
      pattern: "automated|technicalRegression|operationalEvidence"
---

# Plan 137-02: Operational Release Gate Orchestrator (QALIVE-01, QALIVE-02)

<objective>
Implement `run-operational-quality-release-gate.mjs` — the v12.6 orchestrator composing a **technical block** (Phase 133 regression surface) and an **operational block** (live evidence checkers) with independent pass/fail semantics.

Purpose: Single reproducible command proves v12.6 milestone without conflating technical CI green with operational live evidence (QALIVE-02).

Output: `run-operational-quality-release-gate.mjs`, `operational-quality-release-gate` npm script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-operational-quality-release-gate/137-RESEARCH.md
@.planning/phases/137-operational-quality-release-gate/137-01-SUMMARY.md
@app/scripts/run-real-quality-release-gate.mjs
@app/scripts/run-output-learning-release-gate.mjs
@app/scripts/check-sampling-sufficiency-evidence.mjs
@app/scripts/check-quality-trend-evidence.mjs

<interfaces>
From `run-output-learning-release-gate.mjs`:
```javascript
function writeAutomatedStep(step, result) {
  const evidence = existsSync(evidencePath) ? JSON.parse(readFileSync(evidencePath, "utf8")) : { automated: {} };
  evidence.automated = { ...evidence.automated, [step]: result };
  writeFileSync(evidencePath, JSON.stringify(evidence, null, 2));
}
```

From `run-real-quality-release-gate.mjs` — reuse step constants (import or duplicate string paths):
```javascript
// TECHNICAL_BLOCK steps 1-14 from Phase 133 QA-22 matrix
// corpus-eval, score-calibration, learning-impact, quality-improvement vitest subsets
// sub-checkers with --skip-tests, v12_3-factual, v12_4-output-learning, unit, lint, build
```

phaseDir: `.planning/phases/137-operational-quality-release-gate`
evidencePath: `137-EVIDENCE.json` (copy from template on first run if missing)

Exit policy (RESEARCH Pattern 1):
| technicalRegression | operationalEvidence | Process exit | root status |
|--------------------|---------------------|--------------|-------------|
| pass | ok | 0 | ok |
| pass | insufficient_sample | 0 | tech_debt |
| fail | any | 1 | blocked |
| pass | gaps_found | 0 | gaps_found |
</interfaces>
</context>

<tasks>

<task id="137-02-01" type="auto">
  <name>Dual-block orchestrator with technical and operational steps</name>
  <files>app/scripts/run-operational-quality-release-gate.mjs</files>
  <action>
Create `run-operational-quality-release-gate.mjs` mirroring `run-real-quality-release-gate.mjs` structure with **two explicit blocks**:

**Paths:** `phaseDir` → `.planning/phases/137-operational-quality-release-gate`, `evidencePath` → `137-EVIDENCE.json`. On start, if `137-EVIDENCE.json` missing, copy from `137-EVIDENCE.template.json`.

**BLOCK A — Technical (QALIVE-02, must complete even if Block B will be insufficient):**
Reuse Phase 133 step matrix from `run-real-quality-release-gate.mjs` (import constants or exec `node scripts/run-real-quality-release-gate.mjs` with env flag to stop before final 133 checker — prefer inlining same TECHNICAL_STEPS to avoid nested exit-code confusion):
1. `corpus-eval` — vitest FOCUSED_CORPUS_EVAL_TESTS
2. `score-calibration` — vitest CALIBRATION_UNIT_TESTS
3. `learning-impact` — vitest IMPACT_UNIT_TESTS
4. `quality-improvement` — vitest from check-quality-improvement-evidence.mjs
5. `score-calibration-evidence` — check-score-calibration-evidence.mjs --skip-tests
6. `learning-impact-evidence` — check-learning-impact-evidence.mjs --skip-tests
7. `quality-improvement-evidence` — check-quality-improvement-evidence.mjs --skip-tests
8. `v12_3-factual` — vitest V12_3_REGRESSION_TESTS
9. `v12_4-output-learning` — vitest OUTPUT_LEARNING_EVAL_TESTS
10. `output-learning-evidence` — check-output-learning-evidence.mjs --skip-tests
11. `unit` — npm test
12. `lint` — npm run lint
13. `build` — npm run build
14. `real-quality-evidence` — check-real-quality-release-evidence.mjs --skip-tests (v12.5 artifact still valid)

After Block A: set `evidence.technicalRegression.status` to `"pass"` or `"fail"` based on whether any technical step failed. Record `gateMatrixPass`, factual rates from 133 checker output or template. **If any technical step fails, exit 1 immediately after recording technicalRegression — do NOT skip to Block B.**

**BLOCK B — Operational (runs only when Block A passes):**
1. `sampling-sufficiency-evidence` — `node scripts/check-sampling-sufficiency-evidence.mjs --skip-tests`
2. `quality-trend-evidence` — `node scripts/check-quality-trend-evidence.mjs --skip-tests`
3. `operational-evidence` — `node scripts/check-operational-quality-release-evidence.mjs --evidence {evidencePath} --skip-tests`

Operational block failures (insufficient_sample in child evidence) must NOT change `technicalRegression.status` or cause exit 1. Update `evidence.operationalEvidence.status` from checker result. Set root `evidence.status` per exit policy table.

Use `writeAutomatedStep` after each passing step with block prefix (`technical:corpus-eval`, `operational:sampling-sufficiency-evidence`).

**--dry-run flag:** Log both blocks with step labels + commands; exit 0.

**Do NOT** pass `--run-regression` or `--aggregate` in this plan (Plan 137-03).

Console banner: `Operational quality release gate passed (technical: pass, operational: {status}).` / `failed (technical: fail).`
  </action>
  <verify>
    <automated>node app/scripts/run-operational-quality-release-gate.mjs --dry-run</automated>
  </verify>
  <done>Orchestrator exists with dual-block step matrix; dry-run lists technical and operational steps.</done>
</task>

<task id="137-02-02" type="auto">
  <name>npm script and integration smoke</name>
  <files>app/package.json, app/scripts/run-operational-quality-release-gate.mjs</files>
  <action>
Add to `app/package.json` scripts:
`"operational-quality-release-gate": "node scripts/run-operational-quality-release-gate.mjs"`

Run gate once (without `--run-regression`) to populate `137-EVIDENCE.json` automated section:
`cd app && npm run operational-quality-release-gate`

Ensure orchestrator forwards `--dry-run` when invoked as `npm run operational-quality-release-gate -- --dry-run`.

Add unit test in `operational-quality-release-evidence.test.ts`: mock evidence where technical pass + operational insufficient_sample → `resolveMilestoneStatus` returns technical pass without aborting (extract helper if needed).

Confirm `137-EVIDENCE.template.json` defines both `technicalRegression` and `operationalEvidence` top-level sections (dual-status schema contract for Plan 137-03 aggregation).

Update `evidence.requirements` QALIVE-01 row: result pass, automated `cd app && npm run operational-quality-release-gate`.
  </action>
  <verify>
    <automated>cd app && npm run operational-quality-release-gate -- --dry-run && node -e "const e=require('../.planning/phases/137-operational-quality-release-gate/137-EVIDENCE.template.json'); if(!e.technicalRegression||!e.operationalEvidence) process.exit(1)" && cd app && npm test -- tests/unit/release/operational-quality-release-evidence.test.ts -t resolveMilestoneStatus</automated>
  </verify>
  <done>npm script registered; dry-run works via npm; template defines dual sections; resolveMilestoneStatus unit test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → sub-checkers | Composed scripts must not weaken each other's thresholds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-05 | Denial of service | Skipping full npm test in technical block | mitigate | Step 11 runs full `npm test` per Phase 133 QA-22 precedent |
| T-137-06 | Elevation | Operational insufficient_sample aborts technical block | mitigate | Block B runs only after Block A completes; exit 1 only on technical fail |
| T-137-07 | Tampering | Stale automated steps in evidence | mitigate | Orchestrator overwrites automated object each run with block-prefixed keys |
</threat_model>

<verification>
- `cd app && npm run operational-quality-release-gate -- --dry-run`
- Full run: `cd app && npm run operational-quality-release-gate` (uses committed child evidence templates)
</verification>

<success_criteria>
- QALIVE-01: Single command runs technical regression surface plus operational evidence checkers.
- QALIVE-02: technicalRegression.status recorded independently; operational insufficient_sample does not fail process when technical passes.
</success_criteria>

<output>
After completion, create `.planning/phases/137-operational-quality-release-gate/137-02-SUMMARY.md`
</output>
