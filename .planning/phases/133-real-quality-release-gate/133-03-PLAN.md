---
phase: 133-real-quality-release-gate
plan: "03"
type: execute
wave: 3
depends_on: ["133-01", "133-02"]
files_modified:
  - app/scripts/check-real-quality-release-evidence.mjs
  - app/scripts/check-creative-validation-evidence.mjs
  - app/scripts/run-real-quality-release-gate.mjs
  - .planning/phases/133-real-quality-release-gate/133-EVIDENCE.template.json
  - app/tests/unit/release/real-quality-release-evidence.test.ts
autonomous: true
requirements:
  - QA-22
  - QA-23
  - QA-24
must_haves:
  truths:
    - "Checker --aggregate reads 130/131/132/123 evidence files and embeds summaries into 133-EVIDENCE.json"
    - "Checker --run-regression re-runs v12.3 factual-only and v12.4 output-learning scripts without QA-19 visual block"
    - "check-creative-validation-evidence.mjs --factual-only validates factualFidelityRate 1.0 without meanQualityScore threshold"
    - "Orchestrator passes --run-regression to final 133 checker when REAL_QUALITY_RUN_REGRESSION=1 or --run-regression argv"
    - "regressionMetrics section records gateMatrixPass and script pass statuses separately from quality metrics"
  artifacts:
    - path: app/scripts/check-real-quality-release-evidence.mjs
      provides: --aggregate and --run-regression modes
    - path: app/scripts/check-creative-validation-evidence.mjs
      provides: --factual-only regression mode
    - path: .planning/phases/133-real-quality-release-gate/133-EVIDENCE.template.json
      provides: regressionMetrics and sourcePath fields
  key_links:
    - from: app/scripts/check-real-quality-release-evidence.mjs
      to: .planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.json
      via: --aggregate reads visualMetrics/factualMetrics
      pattern: "sourcePath|meanHumanVisualScore|evaluatedItemCount"
    - from: app/scripts/check-real-quality-release-evidence.mjs
      to: app/scripts/check-creative-validation-evidence.mjs
      via: --run-regression exec --factual-only
      pattern: "--factual-only"
    - from: app/scripts/run-real-quality-release-gate.mjs
      to: app/scripts/check-real-quality-release-evidence.mjs
      via: --run-regression on final step when flag set
      pattern: "--run-regression"
---

# Plan 133-03: Evidence Aggregation + Regression Mode (QA-22, QA-23, QA-24)

<objective>
Extend the Phase 133 checker to aggregate sub-phase evidence from Phases 130–132 and 123, and add `--run-regression` with `--factual-only` v12.3 creative validation so milestone regression guards factual 1.0 without QA-19 visual threshold blocking.

Purpose: Milestone evidence must reflect live sub-phase JSON and re-verify v12.3/v12.4 regression rates per Phase 132 locked decision.

Output: Aggregation logic, `--factual-only` flag, orchestrator regression wiring, updated template.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-real-quality-release-gate/133-RESEARCH.md
@.planning/phases/133-real-quality-release-gate/133-01-SUMMARY.md
@.planning/phases/133-real-quality-release-gate/133-02-SUMMARY.md
@app/scripts/check-quality-improvement-evidence.mjs
@app/scripts/check-creative-validation-evidence.mjs
@.planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.json
@.planning/phases/131-learning-impact-measurement/131-EVIDENCE.json
@.planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.json
@.planning/phases/123-visual-validation-gate/123-EVIDENCE.json

<interfaces>
Sub-phase evidence paths (default aggregation sources):
```javascript
const PHASE_EVIDENCE = {
  calibration: ".planning/phases/130-score-calibration-and-rubric-alignment/130-EVIDENCE.json",
  impact: ".planning/phases/131-learning-impact-measurement/131-EVIDENCE.json",
  quality: ".planning/phases/132-targeted-creative-quality-improvements/132-EVIDENCE.json",
  fixture: ".planning/phases/123-visual-validation-gate/123-EVIDENCE.json",
};
```

From 130 evidence — map to 133 qualityMetrics.humanCorpus:
- `evaluatedItemCount`, mean human score from visualMetrics/comparisons or report aggregate
- `calibrationStatus`: ok | insufficient_corpus

From 131 — map to learningImpactMetrics:
- `status`, `globalVisualScoreDelta`, `learnedFactualPassRate`

From 132 — map quality improvement:
- `qualityImprovementStatus`, `targetedFailureDelta`

From 123 — map fixtureValidation:
- `aggregate.meanQualityScore` (70.17), `aggregate.factualFidelityRate` (1.0)

From `check-quality-improvement-evidence.mjs` --run-regression pattern:
```javascript
execFileSync("node", ["app/scripts/check-output-learning-evidence.mjs", "--skip-tests"]);
// creative validation: use --factual-only instead of --stage final
```
</interfaces>
</context>

<tasks>

<task id="133-03-01" type="auto">
  <name>--factual-only on creative validation checker</name>
  <files>app/scripts/check-creative-validation-evidence.mjs</files>
  <action>
Add `--factual-only` flag to `check-creative-validation-evidence.mjs` (per RESEARCH Pitfall 1 and Phase 132 STATE decision):

When `--factual-only` is set (with optional `--evidence PATH`, default 123-EVIDENCE.json):
- Load evidence and recompute aggregate from afterCaptures (reuse existing helpers)
- Assert `aggregate.factualFidelityRate >= 0.95` (or === 1.0 if evidence shows 1.0) and zero fidelity hard failures on after captures (FIDELITY_HARD_FAILURE_CODES)
- **Do NOT** assert `meanQualityScore >= 75` — skip QA-19 visual threshold
- Exit 0 on factual pass; exit 1 with `CREATIVE-EVIDENCE:` prefix on factual breach only
- Update usage() string to document flag

Add unit coverage in existing `creative-validation-evidence-guard.test.ts` OR extend `real-quality-release-evidence.test.ts` with mock that factual-only ignores meanQualityScore 70.17.

Do not change `--stage final` behavior — it still enforces QA-19 for v12.3 gate.
  </action>
  <verify>
    <automated>node app/scripts/check-creative-validation-evidence.mjs --factual-only</automated>
  </verify>
  <done>v12.3 factual regression script passes on committed 123-EVIDENCE despite meanQualityScore 70.17 &lt; 75.</done>
</task>

<task id="133-03-02" type="auto">
  <name>Evidence aggregation from sub-phase JSON</name>
  <files>app/scripts/check-real-quality-release-evidence.mjs, .planning/phases/133-real-quality-release-gate/133-EVIDENCE.template.json</files>
  <action>
Extend `check-real-quality-release-evidence.mjs` with `--aggregate` flag:

When `--aggregate` (optionally `--evidence PATH` to write merged output):
1. Read PHASE_EVIDENCE files; fail with clear error if any missing (suggest operator run 130/131/132 CLIs)
2. **qualityMetrics.humanCorpus** — from 130: evaluatedItemCount, derive meanHumanVisualScore from comparisons (average humanVisualScore) or null when insufficient_corpus; calibrationStatus from evaluatedItemCount vs min threshold (5); sourcePath
3. **qualityMetrics.fixtureValidation** — from 123 aggregate: meanQualityScore, factualFidelityRate, sourcePath, reference note
4. **qualityMetrics.humanCorpus.qualityImprovementStatus** — from 132 re-eval status; targetedFailureDelta from 132 visualImprovementMetrics if present
5. **factualMetrics** — humanCorpusFactualPassRate from 130 factualMetrics.factualPassRate (must be 1.0 for pass) OR 1.0 when all evaluated items factualPass; v12_3FactualFidelityRate from 123; safetyGuardPassRate from 128 or 132 regressionMetrics; v12_3RegressionSubsetPassed stub false until --run-regression
6. **learningImpactMetrics** — copy status, globalVisualScoreDelta, learnedFactualPassRate from 131; sourcePath
7. **regressionMetrics** — `{ gateMatrixPass, creativeValidationScript, outputLearningScript }` placeholders
8. Preserve existing `acceptedCaveats` from evidence file if present; do not auto-generate caveats
9. Set `capturedAt` to now; write merged JSON back to evidence path

Update `133-EVIDENCE.template.json` with `regressionMetrics` section and all `sourcePath` fields.

**QA-23:** Aggregation must not copy blended fields to root; only place metrics in correct buckets.

Run: `node app/scripts/check-real-quality-release-evidence.mjs --aggregate --skip-tests` then validate with checker.
  </action>
  <verify>
    <automated>node app/scripts/check-real-quality-release-evidence.mjs --aggregate --skip-tests && node app/scripts/check-real-quality-release-evidence.mjs --skip-tests</automated>
  </verify>
  <done>133-EVIDENCE.json populated from sub-phase files with separated metric buckets.</done>
</task>

<task id="133-03-03" type="auto">
  <name>--run-regression and orchestrator wiring</name>
  <files>app/scripts/check-real-quality-release-evidence.mjs, app/scripts/run-real-quality-release-gate.mjs, app/tests/unit/release/real-quality-release-evidence.test.ts</files>
  <action>
Add `--run-regression` to `check-real-quality-release-evidence.mjs`:

When set (after schema validation, optionally after --aggregate):
1. Run vitest V12_3_REGRESSION_TESTS (gate-failure-matrix, creative-quality-gate) unless --skip-tests
2. `node app/scripts/check-creative-validation-evidence.mjs --factual-only`
3. `node app/scripts/check-output-learning-evidence.mjs --skip-tests`
4. `node app/scripts/check-quality-improvement-evidence.mjs --skip-tests` (132 template)
5. Merge results into `regressionMetrics`:
   - `gateMatrixPass: true` when vitest passes
   - `creativeValidationScript: "factual_only_pass"`
   - `outputLearningScript: "pass"` when output-learning checker passes
6. Set `factualMetrics.v12_3FactualFidelityRate` from 123 evidence (1.0) and `factualMetrics.safetyGuardPassRate` from 128 (1.0)
7. Re-run assertQa24 — aggregation may leave meanHumanVisualScore null; template caveat must satisfy Path B for CI until operator updates

Update `run-real-quality-release-gate.mjs`:
- Accept `--run-regression` argv; pass to final checker step
- Support env `REAL_QUALITY_RUN_REGRESSION=1` for CI

Add test: `--run-regression` updates regressionMetrics without blending into qualityMetrics root.

Document in SUMMARY: full milestone command `cd app && npm run real-quality-release-gate -- --run-regression` after operator aggregates live evidence.
  </action>
  <verify>
    <automated>node app/scripts/check-real-quality-release-evidence.mjs --run-regression --skip-tests</automated>
  </verify>
  <done>Regression mode green on committed evidence; factual-only decoupled from QA-19 visual threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sub-phase evidence → 133 aggregate | Stale or hand-edited sub-phase JSON could misrepresent live corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-08 | Tampering | Aggregated metrics without sourcePath | mitigate | Require sourcePath on each bucket; audit trail in milestone doc |
| T-133-09 | Elevation | --stage final in regression | mitigate | Use --factual-only per Phase 132 locked decision |
| T-133-10 | Information | insufficient_sample reported as improvement | mitigate | Preserve status fields; audit prose must not claim uplift |
</threat_model>

<verification>
- `node app/scripts/check-creative-validation-evidence.mjs --factual-only`
- `node app/scripts/check-real-quality-release-evidence.mjs --aggregate --skip-tests`
- `node app/scripts/check-real-quality-release-evidence.mjs --run-regression --skip-tests`
</verification>

<success_criteria>
- QA-22: Regression subset runnable via 133 checker without full creative-release-gate.
- QA-23: Aggregated evidence keeps metric separation with sourcePath audit trail.
- QA-24: Regression preserves factual 1.0 rates in factualMetrics while visual path uses human corpus or caveat.
</success_criteria>

<output>
After completion, create `.planning/phases/133-real-quality-release-gate/133-03-SUMMARY.md`
</output>
