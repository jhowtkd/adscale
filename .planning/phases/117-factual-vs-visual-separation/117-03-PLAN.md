---
phase: 117-factual-vs-visual-separation
plan: "03"
type: execute
wave: 2
depends_on:
  - "117-01"
files_modified:
  - app/src/server/ai/factual-visual-separation.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/jobs/derivation.test.ts
  - app/src/app/api/derivations/[id]/delivery-package/route.test.ts
autonomous: true
requirements:
  - SEP-03
must_haves:
  truths:
    - "format_adaptation jobs using parent output reject parents with qualityVerdict invalid"
    - "format_adaptation jobs reject parents whose hardFailures include any CONTAMINATION_FAILURE_CODES member"
    - "Error thrown before downloading parent output buffer when lineage is contaminated"
    - "Delivery package route continues blocking unapprovable derivations (existing assertDerivationApprovable path)"
  artifacts:
    - path: app/src/server/ai/factual-visual-separation.ts
      provides: assertParentFactualLineage, parentHasContamination helper
    - path: app/src/server/jobs/derivation.ts
      provides: lineage guard before parent download
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: assertParentFactualLineage
      via: call when usesParentOutput && parentDerivation before downloadBuffer
      pattern: assertParentFactualLineage
    - from: app/src/server/jobs/derivation.ts
      to: CONTAMINATION_FAILURE_CODES
      via: parent hardFailures code membership check
      pattern: CONTAMINATION_FAILURE_CODES
---

# Plan 117-03: Contaminated Lineage Firewall for Format Adaptation (SEP-03)

<objective>
Block format_adaptation jobs from using parent derivation output when the parent failed factual integrity checks, closing the pipeline path where manually approved contaminated creatives propagate to new formats.

Purpose: Close SEP-03 — today `usesParentOutput` checks only `parentId` + `outputKey`, not parent gate status.

Output: `assertParentFactualLineage`, derivation job guard, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-factual-vs-visual-separation/117-RESEARCH.md
@app/src/server/jobs/derivation.ts
@app/src/server/ai/factual-visual-separation.ts

<interfaces>
Parent output path in derivation.ts (~L392-491):
```typescript
const usesParentOutput =
  effectiveGenerationMode === "format_adaptation" &&
  derivation.parentId &&
  parentDerivation?.outputKey;
// Currently downloads parent without gate check
if (usesParentOutput && parentDerivation?.outputKey) {
  referenceBuffer = await downloadBuffer(parentDerivation.outputKey);
}
```

Existing API guards (extend tests only):
- `review/route.ts` — assertDerivationApprovable
- `delivery-package/route.ts` — assertDerivationApprovable

From Plan 01 `CONTAMINATION_FAILURE_CODES` includes: copied_style_reference_facts, wrong_brand, unsupported_offer
Plan 04 will add invented_factual_entity to the set — assertParentFactualLineage must read the live Set export, not hardcode codes.
</interfaces>
</context>

<tasks>

<task id="117-03-01" type="auto" tdd="true">
  <name>Implement assertParentFactualLineage helper</name>
  <files>app/src/server/ai/factual-visual-separation.ts, app/src/server/jobs/derivation.test.ts</files>
  <behavior>
    - parent null/undefined → no throw
    - parent qualityVerdict === "invalid" → throws with message mentioning factual integrity
    - parent hardFailures containing copied_style_reference_facts → throws
    - parent hardFailures containing wrong_brand → throws
    - clean parent with qualityVerdict acceptable → no throw
  </behavior>
  <action>
Add to `factual-visual-separation.ts`:

```typescript
export function parentHasContamination(parent: {
  qualityVerdict?: string | null;
  hardFailures?: Array<{ code: string }> | null;
}): boolean;

export function assertParentFactualLineage(
  parent: { qualityVerdict?: string | null; hardFailures?: Array<{ code: string }> | null } | null
): void;
```

Logic per RESEARCH Pattern 3:
- Return true if `qualityVerdict === "invalid"`
- Return true if any `hardFailures[].code` is in `CONTAMINATION_FAILURE_CODES`
- Throw descriptive Error: "Parent derivation failed factual integrity checks and cannot be used for format adaptation."

Add unit tests in `derivation.test.ts` describe `contaminated parent`:
  - Test helper directly with fixture parent rows (no full Inngest run required)
  - Cases: invalid verdict, copied_style_reference_facts, clean parent
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/jobs/derivation.test.ts -t "contaminated parent"</automated>
  </verify>
  <done>Lineage helper throws on contaminated parents; passes on clean parents.</done>
</task>

<task id="117-03-02" type="auto">
  <name>Wire lineage guard into derivation job before parent download</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/jobs/derivation.test.ts, app/src/app/api/derivations/[id]/delivery-package/route.test.ts</files>
  <action>
In `derivation.ts` inside `generate-and-store-output`, immediately after `usesParentOutput` is determined and before `downloadBuffer(parentDerivation.outputKey)`:

```typescript
if (usesParentOutput) {
  assertParentFactualLineage(parentDerivation);
}
```

Ensure `parentDerivation` row loaded earlier includes `qualityVerdict` and `hardFailures` fields from repository — extend select if missing.

Add integration-style test in `derivation.test.ts`:
  - Mock parent with `hardFailures: [{ code: "copied_style_reference_facts", ... }]` → job step throws before download

Extend `delivery-package/route.test.ts` (if not already covered):
  - Assert derivation with contamination hard failure cannot produce delivery package (existing assertDerivationApprovable behavior documented; add case for copied_style_reference_facts if missing)

Do NOT add DB migration for `factualLineageStatus` — derive at job time per RESEARCH A4.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/jobs/derivation.test.ts -t "contaminated parent" src/app/api/derivations/\[id\]/delivery-package/route.test.ts</automated>
  </verify>
  <done>Format adaptation jobs fail fast on contaminated parents; delivery route remains blocked for invalid lineage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved parent output → child format job | Parent gate status must be verified server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-05 | Tampering | manual DB approval of contaminated parent | mitigate | Job-time assertParentFactualLineage before download |
| T-117-06 | Repudiation | lineage block without audit trail | accept | Parent hardFailures persisted on derivation row |
</threat_model>

<verification>
`cd app && npm test -- src/server/jobs/derivation.test.ts src/app/api/derivations/\[id\]/delivery-package/route.test.ts` passes.
</verification>

<success_criteria>
- SEP-03: Contaminated derivations cannot feed subsequent format adaptations at job time
</success_criteria>

<output>
After completion, create `.planning/phases/117-factual-vs-visual-separation/117-03-SUMMARY.md`
</output>
