---
phase: 117-factual-vs-visual-separation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/factual-visual-separation.ts
  - app/src/server/ai/creative-contract.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/src/server/jobs/derivation.test.ts
autonomous: true
requirements:
  - SEP-01
must_haves:
  truths:
    - "Every derivation prompt lists factual base, visual reference, brand kit, and auxiliary references in an INPUT SOURCE CLASSIFICATION block"
    - "CreativeContract carries machine-readable inputSourceClassification persisted from the derivation job"
    - "Classification appears after integrity injection and before MODE-specific blocks for art_variation, format_adaptation, and restyling"
  artifacts:
    - path: app/src/server/ai/factual-visual-separation.ts
      provides: InputSourceClassification types, CONTAMINATION_FAILURE_CODES, resolveInputSourceClassification, buildInputClassificationPromptSection, extractPromptInputClassificationSection
      exports:
        - InputSourceRole
        - InputSourceClassification
        - CONTAMINATION_FAILURE_CODES
        - resolveInputSourceClassification
        - buildInputClassificationPromptSection
        - extractPromptInputClassificationSection
    - path: app/src/server/ai/creative-contract.ts
      provides: optional inputSourceClassification on CreativeContract
    - path: app/src/server/ai/prompt-builder.ts
      provides: classification section wired in buildDerivationPrompt
    - path: app/src/server/jobs/derivation.ts
      provides: resolvedContract.inputSourceClassification populated before prompt build
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: app/src/server/ai/factual-visual-separation.ts
      via: buildInputClassificationPromptSection after buildIntegrityPromptSection
      pattern: INPUT SOURCE CLASSIFICATION
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/ai/factual-visual-separation.ts
      via: resolveInputSourceClassification on resolvedContract
      pattern: inputSourceClassification
---

# Plan 117-01: Input Source Classification Module and Prompt Injection (SEP-01)

<objective>
Create the `factual-visual-separation` module and wire explicit input-role classification into contracts, prompts, and the derivation job so the model sees which inputs are factual vs visual before MODE blocks.

Purpose: Close SEP-01 — `FACTUAL_SOURCE_RULES` is stored but never surfaced as a model-facing classification block.

Output: `factual-visual-separation.ts`, extended `CreativeContract`, prompt injection, job persistence, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-factual-vs-visual-separation/117-RESEARCH.md
@.planning/phases/117-factual-vs-visual-separation/117-VALIDATION.md
@app/src/server/ai/creative-contract.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/jobs/derivation.ts

<interfaces>
From `app/src/server/ai/creative-contract.ts`:
```typescript
export type SourcePackage = "campaign_asset" | "approved_derivation";
export interface CreativeContract {
  generationMode: GenerationMode;
  baseAssetId: string | null;
  styleAssetId: string | null;
  sourcePackage?: SourcePackage;
  factualSourceRules?: FactualSourceRules;
  canonicalCreative?: CanonicalCreative;
}
export const FACTUAL_SOURCE_RULES: FactualSourceRules;
```

From `app/src/server/ai/prompt-builder.ts` injection order (Phase 116):
```
HARD RULES → buildCanonicalContractPromptSection → buildIntegrityPromptSection → [NEW classification] → restyling factual-source → MODE
```

From `app/src/server/jobs/derivation.ts`:
- `packageSource` / `sourcePackage` resolved from `usesParentOutput` for format_adaptation
- `resolvedContract` assembled ~L443-485 before `buildDerivationPrompt`
</interfaces>
</context>

<tasks>

<task id="117-01-01" type="auto" tdd="true">
  <name>Create factual-visual-separation module with classification types and builder</name>
  <files>app/src/server/ai/factual-visual-separation.ts, app/src/server/ai/creative-contract.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - resolveInputSourceClassification maps campaign_asset → factual base from campaign base asset
    - approved_derivation sourcePackage → factual base labeled as approved parent output
    - restyling with styleAssetId → visual_reference role present
    - buildInputClassificationPromptSection emits header "INPUT SOURCE CLASSIFICATION:" with factual base, visual reference, brand kit, auxiliary refs lines
    - extractPromptInputClassificationSection returns section text between header and next "\nMODE:" block
    - CONTAMINATION_FAILURE_CODES exports Set including copied_style_reference_facts, wrong_brand, unsupported_offer (invented_factual_entity added in Plan 04)
  </behavior>
  <action>
Create `app/src/server/ai/factual-visual-separation.ts` exporting:

- `InputSourceRole` — `"factual_base" | "visual_reference" | "brand_kit" | "auxiliary_reference"`
- `InputSourceClassification` — `{ factualBase: { role, label }; visualReference: { role, label } | null; brandKit: { role, label } | null; auxiliaryReferences: { role, count, label } }`
- `CONTAMINATION_FAILURE_CODES` — `ReadonlySet` of existing `CreativeHardFailureCode` values used for lineage (import type from creative-quality-gate; do NOT add invented_factual_entity yet — Plan 04 extends the set)
- `resolveInputSourceClassification(contract, ctx)` where ctx includes `{ hasBrandKit: boolean; clientReferenceCount: number; packageSource?: SourcePackage }`
- `buildInputClassificationPromptSection(classification)` — prose block per RESEARCH Pattern 1; factual base line must state it is the sole source of people, products, brands, logos, copy, offers, CTAs, claims
- `extractPromptInputClassificationSection(prompt: string)` — test helper per RESEARCH Code Examples

Extend `CreativeContract` in `creative-contract.ts` with optional `inputSourceClassification?: InputSourceClassification`.

Add `prompt-builder.test.ts` describe block `input classification`:
  - art_variation with campaign asset: section lists factual base, visual reference none, brand kit not attached
  - format_adaptation with approved_derivation: factual base mentions approved parent
  - restyling with styleAssetId: visual reference line present
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "input classification"</automated>
  </verify>
  <done>Module exports classification types and prompt section; extractor tests pass for three modes.</done>
</task>

<task id="117-01-02" type="auto">
  <name>Wire classification into prompt builder and derivation job</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/jobs/derivation.ts, app/src/server/jobs/derivation.test.ts</files>
  <action>
In `buildDerivationPrompt` after `buildIntegrityPromptSection()` (per D-116 injection order), call:

```typescript
const classification = resolveInputSourceClassification(effectiveContract, {
  hasBrandKit: Boolean(config.brandKit),
  clientReferenceCount: config.clientReferences?.length ?? 0,
  packageSource: config.packageSource,
});
parts.push(...buildInputClassificationPromptSection(classification));
```

Pass `effectiveContract.inputSourceClassification` when already set, or resolve inline if missing.

In `derivation.ts` after `resolvedContract` is built with `canonicalCreative`, set:

```typescript
inputSourceClassification: resolveInputSourceClassification(resolvedContract, {
  hasBrandKit: Boolean(brandKit),
  clientReferenceCount: clientReferences?.length ?? 0,
  packageSource: sourcePackage,
}),
```

Ensure persisted `creativeContract` on derivation row includes `inputSourceClassification`.

Add `derivation.test.ts` describe `input classification`:
  - Mock or unit-test `resolveInputSourceClassification` wiring via exported helper or fixture contract asserting persisted shape includes all four role categories when brand kit + client refs present.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/jobs/derivation.test.ts -t "input classification" src/server/ai/prompt-builder.test.ts -t "input classification"</automated>
  </verify>
  <done>Prompts for all three modes contain INPUT SOURCE CLASSIFICATION; job persists classification on creativeContract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign fields → classification labels | Untrusted text embedded in prompt classification prose |
| client references → auxiliary role | Must never be labeled factual base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Tampering | buildInputClassificationPromptSection | mitigate | Plain-text join; auxiliary refs explicitly subordinate to factual base |
| T-117-02 | Spoofing | approved_derivation labeled factual | accept | SEP-03 job guard validates parent gate status separately |
</threat_model>

<verification>
`cd app && npm test -- src/server/ai/prompt-builder.test.ts src/server/jobs/derivation.test.ts` passes with input classification tests green.
</verification>

<success_criteria>
- SEP-01: Inputs explicitly classified as factual base, visual reference, brand kit, auxiliary references in prompt and persisted contract
</success_criteria>

<output>
After completion, create `.planning/phases/117-factual-vs-visual-separation/117-01-SUMMARY.md`
</output>
