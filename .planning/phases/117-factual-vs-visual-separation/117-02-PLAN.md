---
phase: 117-factual-vs-visual-separation
plan: "02"
type: execute
wave: 2
depends_on:
  - "117-01"
files_modified:
  - app/src/server/ai/factual-visual-separation.ts
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/ai/quality-prompt-regression.test.ts
autonomous: true
requirements:
  - SEP-02
must_haves:
  truths:
    - "Restyling prompts include VISUAL REFERENCE TRANSFER RULE with abstract allowlist (ritmo, textura, cromia, tipografia, iluminação, lógica compositiva) and factual denylist"
    - "Restyling prompts never include Extracted Visual Token Brief section"
    - "RESTYLING FACTUAL-SOURCE RULE injects for all restyling jobs regardless of styleAssetId presence at prompt-build time"
  artifacts:
    - path: app/src/server/ai/factual-visual-separation.ts
      provides: buildVisualReferenceTransferRuleSection, extractPromptVisualReferenceTransferSection
    - path: app/src/server/ai/prompt-builder.ts
      provides: visualTokenBrief guard for restyling; unconditional restyling factual-source rule
  key_links:
    - from: app/src/server/ai/prompt-builder.ts
      to: VISUAL REFERENCE TRANSFER RULE
      via: buildVisualReferenceTransferRuleSection when generationMode === restyling
      pattern: VISUAL REFERENCE TRANSFER RULE
    - from: app/src/server/ai/prompt-builder.ts
      to: visualTokenBrief
      via: skip injection when generationMode === restyling
      pattern: generationMode !== "restyling"
---

# Plan 117-02: Visual Reference Transfer Allowlist and visualTokenBrief Guard (SEP-02)

<objective>
Enforce that visual style references transfer only abstract design attributes — never people, brands, copy, or claims — by adding an explicit transfer rule section and blocking factual token brief injection for restyling.

Purpose: Close SEP-02 gap where `visualTokenBrief` still applies to restyling (prompt-builder.ts:553-558) and RESTYLING FACTUAL-SOURCE RULE is gated on `styleAssetId`.

Output: Visual transfer prompt section, restyling guards, regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-factual-vs-visual-separation/117-RESEARCH.md
@app/src/server/ai/factual-visual-separation.ts
@app/src/server/ai/prompt-builder.ts

<interfaces>
Current restyling guard (to fix):
```typescript
// prompt-builder.ts:397 — gated on styleAssetId; change to generationMode === "restyling"
if (generationMode === "restyling" && contract?.styleAssetId) { ... RESTYLING FACTUAL-SOURCE RULE ... }

// prompt-builder.ts:553-558 — visualTokenBrief applies to restyling; add restyling exclusion
if (!isArtVariation && visualTokenBrief?.trim() && generationMode !== "format_adaptation") { ... }
```

SEP-02 allowlist (must appear verbatim in prompt): ritmo, textura, cromia, tipografia, iluminação, lógica compositiva
SEP-02 denylist: pessoas, uniformes, produtos, marcas, logos, textos, alegações
</interfaces>
</context>

<tasks>

<task id="117-02-01" type="auto" tdd="true">
  <name>Add VISUAL REFERENCE TRANSFER RULE builder and extractor</name>
  <files>app/src/server/ai/factual-visual-separation.ts, app/src/server/ai/prompt-builder.test.ts</files>
  <behavior>
    - buildVisualReferenceTransferRuleSection returns header "VISUAL REFERENCE TRANSFER RULE:" with allowlist and denylist per SEP-02
    - extractPromptVisualReferenceTransferSection extracts section between header and next major block
    - Section states style reference is abstract-only; base image remains sole factual source
  </behavior>
  <action>
Extend `factual-visual-separation.ts`:

- `buildVisualReferenceTransferRuleSection()` — inject after INPUT SOURCE CLASSIFICATION for restyling; include:
  - Allowlist: rhythm/ritmo, texture/textura, chroma/cromia, typography/tipografia, lighting/iluminação, compositional logic/lógica compositiva
  - Denylist: people, uniforms, products, brands, logos, texts, claims (Portuguese + English labels acceptable per project locale patterns)
  - Explicit: "Never copy factual content from the style reference"
- `extractPromptVisualReferenceTransferSection(prompt: string)`

Add `prompt-builder.test.ts` describe `visual reference transfer`:
  - Restyling prompt contains allowlist terms and denylist terms
  - art_variation prompt does NOT contain VISUAL REFERENCE TRANSFER RULE header
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/ai/prompt-builder.test.ts -t "visual reference transfer"</automated>
  </verify>
  <done>Transfer rule builder and extractor tested; allowlist/denylist present in restyling prompts.</done>
</task>

<task id="117-02-02" type="auto" tdd="true">
  <name>Guard visualTokenBrief and unconditional restyling factual-source rule</name>
  <files>app/src/server/ai/prompt-builder.ts, app/tests/unit/ai/quality-prompt-regression.test.ts</files>
  <behavior>
    - Restyling prompt with visualTokenBrief configured does NOT contain "Extracted Visual Token Brief"
    - format_adaptation and art_variation behavior unchanged for visualTokenBrief when applicable
    - RESTYLING FACTUAL-SOURCE RULE present when generationMode === restyling even if contract.styleAssetId is null at build time
    - Injection order: integrity → classification → visual transfer rule → RESTYLING FACTUAL-SOURCE RULE → MODE
  </behavior>
  <action>
In `prompt-builder.ts`:

1. Change RESTYLING FACTUAL-SOURCE RULE condition from `generationMode === "restyling" && contract?.styleAssetId` to `generationMode === "restyling"` (job already throws if assets missing).

2. After classification section, when `generationMode === "restyling"`, push `buildVisualReferenceTransferRuleSection()`.

3. Change visualTokenBrief guard to:
```typescript
if (
  !isArtVariation &&
  visualTokenBrief?.trim() &&
  generationMode !== "format_adaptation" &&
  generationMode !== "restyling"
) { ... }
```

4. When client references include `style` kind (if detectable from config.clientReferences), add one line in classification or transfer section clarifying client style refs are visual-only (reuse auxiliary_reference label if no distinct kind field).

Extend `quality-prompt-regression.test.ts`:
  - Restyling fixture: assert no "Extracted Visual Token Brief"
  - Restyling fixture: assert VISUAL REFERENCE TRANSFER RULE and RESTYLING FACTUAL-SOURCE RULE both present
  - Snapshot or substring asserts for SEP-02 allowlist terms in restyling regression case
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts -t "restyling" src/server/ai/prompt-builder.test.ts -t "visual reference transfer"</automated>
  </verify>
  <done>Restyling prompts exclude visualTokenBrief and include full factual/visual separation rules unconditionally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| style reference image → model | Visual-only channel must not carry factual tokens via brief |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | Tampering | visualTokenBrief factual leak | mitigate | Skip injection for restyling; transfer rule denylist in prompt |
| T-117-04 | Spoofing | style ref athlete/brand in output | transfer | Vision QA + gate (Plan 04); prompt rules are first line |
</threat_model>

<verification>
`cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts src/server/ai/prompt-builder.test.ts` passes; restyling cases assert no visualTokenBrief.
</verification>

<success_criteria>
- SEP-02: Visual reference transfers only abstract style attributes; factual content from style reference blocked in prompts
</success_criteria>

<output>
After completion, create `.planning/phases/117-factual-vs-visual-separation/117-02-SUMMARY.md`
</output>
