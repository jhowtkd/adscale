---
phase: 59-feedback-informed-regeneration
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/src/components/workspace/RegenerateFeedbackDialog.tsx
  - app/src/lib/derivation-regeneration-feedback.ts
  - app/src/lib/hooks/use-campaign-workspace.ts
  - app/src/lib/hooks/use-derivations.ts
  - app/src/app/(dashboard)/campaigns/[id]/page.tsx
  - app/src/app/api/campaigns/[id]/derivations/route.ts
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
requirements:
  - AIR-03
user_setup: []
must_haves:
  truths:
    - User sees primary regeneration reason and structured blocking issues before confirming regenerate-with-fixes.
    - Hard-failure / invalid paths always open summary dialog; polish-only one-click path unchanged.
    - PT-BR and EN copy distinguish blocking vs advisory labels.
  artifacts:
    - path: app/src/components/workspace/RegenerateFeedbackDialog.tsx
      provides: Read-only issue summary above editable textarea
    - path: app/messages/en.json
      provides: review.regenerationPrimaryReason.* i18n keys
  key_links:
    - from: app/src/lib/hooks/use-campaign-workspace.ts
      to: RegenerateFeedbackDialog
      via: primaryReason + issueBreakdown props from derivation DTO
      pattern: regenerationPrimaryReason
    - from: app/src/app/api/campaigns/[id]/derivations/route.ts
      to: deriveRegenerationPreview
      via: server-side brief preview on list response
      pattern: deriveRegenerationPreview
---

# Plan 59-03: Pre-Confirm Regeneration Reason UI

<objective>
Show users why regeneration is recommended (hard failures, QA failures, score issues) in a read-only summary before they edit feedback and confirm.

Purpose: satisfy AIR-03 (primary reason visible before confirm).

Output: Extended RegenerateFeedbackDialog, i18n keys, workspace hook wiring, server-computed `regenerationPrimaryReason` on derivation list API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-feedback-informed-regeneration/59-CONTEXT.md
@.planning/phases/58-scoring-and-qa-alignment/58-02-SUMMARY.md

<interfaces>
From Plan 59-01:
```typescript
export function deriveRegenerationPreview(derivation): {
  primaryReason: string;
  issueBreakdown: {
    hardFailures: Array<{ code: string; message: string }>;
    scoreIssues: string[];
    qaFailed: Array<{ criterion: string; note: string }>;
    qaWarnings: Array<{ criterion: string; note: string }>;
  };
};
```

From app/src/lib/hooks/use-derivations.ts Derivation interface — extend with:
```typescript
regenerationPrimaryReason?: string | null;
regenerationIssueBreakdown?: {...} | null;
```

From app/src/components/workspace/RegenerateFeedbackDialog.tsx — currently only textarea, no summary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose regeneration preview on derivations list API</name>
  <files>app/src/app/api/campaigns/[id]/derivations/route.ts, app/src/lib/hooks/use-derivations.ts</files>
  <action>
    Per D-03 discretion (minimum: embed primaryReason server-side), in `derivations/route.ts` GET handler after loading items:

    For each derivation where `hardFailures?.length > 0` OR `regenerationSuggestion` OR `scoreIssues?.length` OR qa checklist has failed/warning items, call `deriveRegenerationPreview(d)` and attach:
    - `regenerationPrimaryReason`
    - `regenerationIssueBreakdown` (structured lists for UI — no feedback report message)

    Skip preview fields when derivation has no quality issues (null/omit).

    Extend `Derivation` interface in `use-derivations.ts` with optional `regenerationPrimaryReason` and `regenerationIssueBreakdown` matching server shape.

    Do NOT add a separate preview endpoint (planner discretion).
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Derivation list API returns server-computed primary reason for derivations with quality issues.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RegenerateFeedbackDialog with read-only issue summary</name>
  <files>app/src/components/workspace/RegenerateFeedbackDialog.tsx, app/src/lib/derivation-regeneration-feedback.ts, app/messages/en.json, app/messages/pt-BR.json</files>
  <action>
    Per D-03 (AIR-03), extend dialog props:

    ```typescript
    primaryReason?: string;
    issueBreakdown?: {
      hardFailures?: Array<{ code: string; message: string }>;
      scoreIssues?: string[];
      qaFailed?: Array<{ criterion: string; note: string }>;
      qaWarnings?: Array<{ criterion: string; note: string }>;
    };
    ```

    Add read-only summary section **above** the textarea:
    - Heading: `review.regenerationWhatWeFix` ("What we'll try to fix" / PT-BR equivalent)
    - `primaryReason` as lead sentence when present
    - Bulleted lists grouped by blocking (hard failures + QA failed + score issues) vs advisory (QA warnings) using `review.regenerationBlocking` / `review.regenerationAdvisory` labels
    - Reuse `review.hardFailureCodes.{code}` for hard failure titles (Phase 58 pattern) with `message` as secondary detail

    Update `buildRegenerationFeedback` in `derivation-regeneration-feedback.ts` to return `{ feedbackText, primaryReason?, issueBreakdown? }` instead of plain string (update all call sites in this plan).

    Textarea `initialFeedback` remains full machine brief (prefill for edit); summary is separate read-only block.

    i18n: add keys under `review.regenerationWhatWeFix`, `review.regenerationBlocking`, `review.regenerationAdvisory` in EN and PT-BR.
  </action>
  <verify>
    <automated>cd app && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>Dialog renders structured summary + editable feedback; i18n keys exist in both locales.</done>
</task>

<task type="auto">
  <name>Task 3: Wire workspace hook and campaign page to pass preview data</name>
  <files>app/src/lib/hooks/use-campaign-workspace.ts, app/src/app/(dashboard)/campaigns/[id]/page.tsx</files>
  <action>
    Update `handleRequestRegenerate` in `use-campaign-workspace.ts`:

    1. When opening dialog (`preset.trim()`), use `buildRegenerationFeedback` return value for feedback text AND pass `regenerationPrimaryReason` / `regenerationIssueBreakdown` from derivation DTO (prefer API fields; fallback to breakdown from hardFailures only if API fields missing).

    2. Extend `regenerateDialog` state to `{ id, feedback, primaryReason?, issueBreakdown? }`.

    3. Hard-failure / invalid paths: always show dialog when `primaryReason` OR `hardFailures.length > 0` OR non-empty regeneration suggestion — per D-03 (existing preset behavior, ensure invalid paths never skip summary).

    4. Polish-only path (no hard failures, no suggestion, no blocking QA): keep one-click `regenerateMutation.mutate` without dialog.

    Update `page.tsx` `RegenerateFeedbackDialog` usage to pass new props from `regenerateDialog` state.

    Do not change DerivationCard/ReviewModal regenerate entry points beyond what hook provides (they call same handlers).
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Workspace opens dialog with primary reason for blocking regenerations; polish-only one-click unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | Displays server-computed primaryReason; user edits separate textarea |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | Client-side summary | accept | Summary is informational; server re-merges brief on POST regardless of UI |
| T-59-08 | Information disclosure | issueBreakdown in API | accept | Same data already on derivation row for workspace members |
</threat_model>

<verification>
- Build and lint pass
- Dialog component accepts and renders primaryReason + breakdown props
- Hook opens dialog for hard-failure derivations
</verification>

<success_criteria>
- AIR-03: User sees what regeneration will try to fix before confirming
- Blocking vs advisory labels localized EN/PT-BR
- One-click polish regeneration preserved
</success_criteria>

<output>
After completion, create `.planning/phases/59-feedback-informed-regeneration/59-03-SUMMARY.md`
</output>
