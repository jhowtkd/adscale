---
phase: 135-sampling-sufficiency-and-evidence-honesty
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/sampling/types.ts
  - app/src/server/human-quality/sampling/thresholds.ts
  - app/src/server/human-quality/sampling/guidance.ts
  - app/src/server/human-quality/calibration/report.ts
  - app/src/server/human-quality/impact/report.ts
  - app/src/server/human-quality/impact/aggregate.ts
  - app/src/server/human-quality/improvement/reevaluate.ts
  - app/tests/unit/human-quality/sampling/thresholds.test.ts
  - app/tests/unit/human-quality/sampling/guidance.test.ts
  - app/tests/unit/human-quality/calibration/aggregate.test.ts
  - app/tests/unit/human-quality/impact/report.test.ts
  - app/tests/unit/human-quality/improvement/reevaluate.test.ts
autonomous: true
requirements:
  - SAMPLE-01
  - SAMPLE-02
must_haves:
  truths:
    - "All human-quality gates read minimum thresholds from one canonical sampling module"
    - "Trend sufficiency constants exist for Phase 136 without building trend charts"
    - "Calibration, impact and quality-improvement reports expose structured sampleGuidance when below minimum"
    - "Guidance items include currentCount, requiredCount, additionalNeeded and blockedClaim"
    - "Numeric threshold behavior stays 5 global / 3 slice-arm — no value drift across modules"
  artifacts:
    - path: app/src/server/human-quality/sampling/thresholds.ts
      provides: "Canonical SAMPLE_GLOBAL_MIN, SAMPLE_SLICE_MIN, TREND_* constants and backward-compat re-exports"
      exports: ["SAMPLE_GLOBAL_MIN", "SAMPLE_SLICE_MIN", "TREND_GLOBAL_MIN_EVALUATED", "MIN_GLOBAL_EVALUATED_ITEMS"]
    - path: app/src/server/human-quality/sampling/guidance.ts
      provides: "buildSampleGuidance helpers and computeAdditionalNeeded"
      exports: ["computeAdditionalNeeded", "buildCalibrationGuidance", "buildImpactGuidance", "buildQualityImprovementGuidance"]
    - path: app/src/server/human-quality/calibration/report.ts
      provides: "CalibrationReport with optional sampleGuidance when insufficient_corpus"
      contains: "sampleGuidance"
    - path: app/src/server/human-quality/impact/report.ts
      provides: "LearningImpactReport with sampleGuidance alongside insufficientReasons"
      contains: "sampleGuidance"
    - path: app/src/server/human-quality/improvement/reevaluate.ts
      provides: "QualityImprovementReport with sampleGuidance per targeted reason"
      contains: "sampleGuidance"
  key_links:
    - from: app/src/server/human-quality/calibration/report.ts
      to: app/src/server/human-quality/sampling/thresholds.ts
      via: "import MIN_GLOBAL_EVALUATED_ITEMS re-export"
      pattern: "from.*sampling/thresholds"
    - from: app/src/server/human-quality/impact/report.ts
      to: app/src/server/human-quality/sampling/guidance.ts
      via: "buildImpactGuidance on insufficient status"
      pattern: "sampleGuidance"
    - from: app/src/server/human-quality/improvement/reevaluate.ts
      to: app/src/server/human-quality/sampling/thresholds.ts
      via: "MIN_SLICE_SAMPLE re-export"
      pattern: "from.*sampling/thresholds"
---

# Plan 135-01: Canonical Sampling Module and Report Guidance

<objective>
Establish `human-quality/sampling/` as the single source of truth for sample thresholds and structured next-sample guidance, then attach `sampleGuidance` to calibration, impact and quality-improvement reports without changing existing numeric gate behavior.
</objective>

Purpose: SAMPLE-01 and SAMPLE-02 require consolidated thresholds and actionable guidance — today constants and status labels drift across three modules.
Output: `sampling/types.ts`, `thresholds.ts`, `guidance.ts`; refactored report builders with `sampleGuidance`; unit tests green.

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-RESEARCH.md
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
From `app/src/server/human-quality/calibration/report.ts`:
```typescript
export const MIN_GLOBAL_EVALUATED_ITEMS = 5;
export const MIN_SLICE_SAMPLE = 3;
export interface CalibrationReport {
  status: "ok" | "insufficient_corpus";
  evaluatedItemCount: number;
  // Phase 135: add sampleGuidance?: SampleGuidance[]
}
```

From `app/src/server/human-quality/impact/report.ts`:
```typescript
export const MIN_GLOBAL_IMPACT_ITEMS = 5;
function resolveReportStatus(...): { status: "ok" | "insufficient_sample"; insufficientReasons: string[] }
export interface LearningImpactReport {
  status: "ok" | "insufficient_sample";
  insufficientReasons: string[];
  learningImpactMetrics: { slices: ImpactSliceComparison[]; ... };
}
```

From `app/src/server/human-quality/improvement/reevaluate.ts`:
```typescript
export interface QualityImprovementReport {
  status: "ok" | "insufficient_sample";
  visualMetrics: { deltaRateByReason: Record<string, number | null> };
  fixtureMetrics?: { targetedArchetypePassRateBefore; targetedArchetypePassRateAfter };
}
```

Locked decisions (non-negotiable):
- Preserve numeric thresholds 5 global / 3 slice-arm per Phase 130–132 — do not change values.
- Keep calibration status label `insufficient_corpus`; map to shared guidance without renaming (Pitfall 1).
- Define `TREND_*` constants only — no trend chart UI in this phase (deferred to Phase 136).
</interfaces>
</context>

<tasks>

<task id="135-01-01" type="auto" tdd="true">
  <name>Task 1: Canonical thresholds and types</name>
  <files>app/src/server/human-quality/sampling/types.ts, app/src/server/human-quality/sampling/thresholds.ts, app/tests/unit/human-quality/sampling/thresholds.test.ts</files>
  <behavior>
    - SAMPLE_GLOBAL_MIN === 5, SAMPLE_SLICE_MIN === 3, SAMPLE_ARM_MIN === 3
    - TREND_GLOBAL_MIN_EVALUATED === SAMPLE_GLOBAL_MIN, TREND_SLICE_MIN === SAMPLE_SLICE_MIN, TREND_MIN_TIME_BUCKETS === 2
    - Backward-compat re-exports: MIN_GLOBAL_EVALUATED_ITEMS, MIN_SLICE_SAMPLE, MIN_GLOBAL_IMPACT_ITEMS, MIN_ARM_SAMPLE equal canonical values
  </behavior>
  <action>
    Create `sampling/types.ts` with `SampleGuidance`, `SampleGate` union (`calibration_global`, `calibration_slice`, `impact_global`, `impact_slice_arm`, `quality_improvement_reason`, `trend_global`), `EvidenceSource` union for downstream SAMPLE-03, and optional `samplingStatus: "insufficient_sample"` alias helper type.

    Create `sampling/thresholds.ts` as SINGLE source of truth per research Pattern 1. Export canonical names plus backward-compat re-exports so existing imports from `calibration/report.ts` and `impact/report.ts` can be redirected without numeric drift.

    Write `thresholds.test.ts` first (RED), then implement. Do not duplicate literals in report modules after this task.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/sampling/thresholds.test.ts -x</automated>
  </verify>
  <done>SAMPLE-01 canonical constants and TREND_* exports exist; tests assert 5/3/3 values and re-export parity.</done>
</task>

<task id="135-01-02" type="auto" tdd="true">
  <name>Task 2: Guidance builders with additionalNeeded math</name>
  <files>app/src/server/human-quality/sampling/guidance.ts, app/tests/unit/human-quality/sampling/guidance.test.ts</files>
  <behavior>
    - computeAdditionalNeeded(2, 5) === 3; computeAdditionalNeeded(5, 5) === 0
    - buildCalibrationGuidance returns global gap when count &lt; SAMPLE_GLOBAL_MIN with blockedClaim describing visual calibration claims
    - buildImpactGuidance emits per-slice arm gaps when learned or nonLearned count &lt; SAMPLE_ARM_MIN
    - buildQualityImprovementGuidance emits per-reason gaps when before or after arm &lt; SAMPLE_SLICE_MIN
    - Every guidance item has currentCount, requiredCount, additionalNeeded, blockedClaim
  </behavior>
  <action>
    Implement pure guidance functions in `sampling/guidance.ts` per research Pattern 2. Include `normalizeSamplingStatus()` that maps `insufficient_corpus` → `insufficient_sample` for coverage rollup without changing calibration report `status` field.

    Write `guidance.test.ts` covering global, slice-arm, and per-reason cases. Use human-readable `blockedClaim` strings (e.g., "calibration visual divergence", "learning impact movement delta", "targeted failure-frequency improvement").

    Do not hardcode threshold numbers in guidance strings — read from `thresholds.ts`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/sampling/guidance.test.ts -x</automated>
  </verify>
  <done>SAMPLE-02 guidance math and gate-specific builders are tested and exported.</done>
</task>

<task id="135-01-03" type="auto">
  <name>Task 3: Refactor reports to import thresholds and attach sampleGuidance</name>
  <files>app/src/server/human-quality/calibration/report.ts, app/src/server/human-quality/impact/report.ts, app/src/server/human-quality/impact/aggregate.ts, app/src/server/human-quality/improvement/reevaluate.ts, app/tests/unit/human-quality/calibration/aggregate.test.ts, app/tests/unit/human-quality/impact/report.test.ts, app/tests/unit/human-quality/improvement/reevaluate.test.ts</files>
  <action>
    Refactor `calibration/report.ts`, `impact/report.ts`, `impact/aggregate.ts`, and `improvement/reevaluate.ts` to import thresholds from `sampling/thresholds.ts`. Keep exported constant names at module boundaries via re-export (`export { MIN_GLOBAL_EVALUATED_ITEMS } from "../sampling/thresholds"`) so downstream imports do not break.

    Extend report interfaces:
    - `CalibrationReport`: add `sampleGuidance: SampleGuidance[]` (empty when `status === "ok"`)
    - `LearningImpactReport`: add `sampleGuidance` alongside existing `insufficientReasons`
    - `QualityImprovementReport`: add `sampleGuidance` for per-reason arm gaps

    In `buildCalibrationReport`, call `buildCalibrationGuidance` when `!hasSufficientCorpus`. In `buildLearningImpactReport`, call `buildImpactGuidance` when status is `insufficient_sample`. In quality improvement builder, call `buildQualityImprovementGuidance` when any targeted reason fails MIN_SLICE_SAMPLE in either arm.

  Preserve existing behavior: visual aggregates still null when insufficient; movement deltas still null when insufficient; factual metrics still computed from available rows (Phase 130 precedent).

    Extend existing aggregate/report/reevaluate tests to assert `sampleGuidance` shape and `additionalNeeded` on insufficient fixtures. Do not remove `insufficientReasons` — guidance is additive.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/calibration/aggregate.test.ts tests/unit/human-quality/impact/report.test.ts tests/unit/human-quality/improvement/reevaluate.test.ts tests/unit/human-quality/sampling -x</automated>
  </verify>
  <done>All three report types emit structured sampleGuidance when below minimum; threshold imports consolidated; existing gate behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report builders → API consumers | Untrusted counts must not bypass guidance when status is insufficient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Tampering | calibration/report.ts | mitigate | Guidance derived from same count used for status — no separate override path |
| T-135-02 | Information disclosure | sampleGuidance blockedClaim | accept | Operational messages only; no PII in guidance strings |
| T-135-03 | Denial of service | guidance builders | accept | O(n) over in-memory report slices; same bounds as existing reports |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/sampling tests/unit/human-quality/calibration/aggregate.test.ts tests/unit/human-quality/impact/report.test.ts tests/unit/human-quality/improvement/reevaluate.test.ts`
- `cd app && npm run build`
- Grep confirms no duplicate `= 5` or `= 3` threshold literals outside `sampling/thresholds.ts` in human-quality modules
</verification>

<success_criteria>
- SAMPLE-01: Single canonical threshold module with trend constants for Phase 136.
- SAMPLE-02: Reports return `sampleGuidance` with `additionalNeeded` when thresholds not met.
- Calibration keeps `insufficient_corpus` label; guidance still actionable.
- No trend chart or time-series UI added.
</success_criteria>

<output>
After completion, create `.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-01-SUMMARY.md`.
</output>
