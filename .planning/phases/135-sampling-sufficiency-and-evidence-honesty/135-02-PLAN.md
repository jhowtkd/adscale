---
phase: 135-sampling-sufficiency-and-evidence-honesty
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/scripts/check-real-quality-release-evidence.mjs
  - app/scripts/check-score-calibration-evidence.mjs
  - app/scripts/check-learning-impact-evidence.mjs
  - app/scripts/check-quality-improvement-evidence.mjs
  - app/scripts/check-sampling-sufficiency-evidence.mjs
  - app/scripts/run-sample-coverage.ts
  - app/tests/unit/release/real-quality-release-evidence.test.ts
  - app/tests/unit/human-quality/sampling/evidence-honesty.test.ts
  - .planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.template.json
autonomous: true
requirements:
  - SAMPLE-03
must_haves:
  truths:
    - "Human corpus, fixture validation and accepted caveats carry explicit evidenceSource tags"
    - "Child evidence checkers reject blended denominators and missing guidance on insufficient reports"
    - "Fixture pass rates never appear as live human corpus denominators"
    - "Phase 135 evidence template documents sampling honesty contract for Phase 137"
  artifacts:
    - path: app/scripts/check-real-quality-release-evidence.mjs
      provides: "QA-23 validation extended with evidenceSource requirements"
      contains: "evidenceSource"
    - path: app/scripts/check-sampling-sufficiency-evidence.mjs
      provides: "Phase 135 evidence gate validating sampleGuidance and source tags"
    - path: .planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.template.json
      provides: "Canonical Phase 135 evidence artifact schema"
      contains: "evidenceSource"
    - path: app/tests/unit/human-quality/sampling/evidence-honesty.test.ts
      provides: "Unit tests for evidence honesty rules"
  key_links:
    - from: app/scripts/check-real-quality-release-evidence.mjs
      to: .planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.template.json
      via: "aggregate or validate evidenceSource on child phases"
      pattern: "evidenceSource.*live_human"
    - from: app/scripts/check-score-calibration-evidence.mjs
      to: app/src/server/human-quality/sampling/types.ts
      via: "validate sampleGuidance when status insufficient_corpus"
      pattern: "sampleGuidance"
---


# Plan 135-02: Evidence Source Separation and Honesty Checkers

<objective>
Propagate `evidenceSource` tags through human-quality evidence JSON and harden checkers so fixture metrics, live human metrics and accepted caveats never share denominators — extending the Phase 133 QA-23 pattern to child evidence and Phase 135 artifacts.
</objective>

Purpose: SAMPLE-03 requires release evidence to distinguish fixture, live human and accepted-caveat metrics at every artifact boundary.
Output: Extended checkers, `135-EVIDENCE.template.json`, optional `run-sample-coverage.ts` evidence CLI, honesty unit tests.

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-RESEARCH.md
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-VALIDATION.md
@app/scripts/check-real-quality-release-evidence.mjs
@app/scripts/check-score-calibration-evidence.mjs
@app/scripts/check-learning-impact-evidence.mjs
@app/scripts/check-quality-improvement-evidence.mjs
@app/tests/unit/release/real-quality-release-evidence.test.ts

Depends on Plan 135-01: `SampleGuidance` and `EvidenceSource` types in `sampling/types.ts`; reports must emit `sampleGuidance` for checker validation.

Locked decisions:
- QA-23 aggregate separation pattern is authoritative — extend, do not replace.
- Fixture 70.17 remains reference baseline only; human corpus claims require live_human source.
- Factual pass conservative default when count=0 stays; visual claims must remain blocked via guidance.
</context>

<tasks>

<task id="135-02-01" type="auto" tdd="true">
  <name>Task 1: evidenceSource on aggregate and child evidence schemas</name>
  <files>app/scripts/check-real-quality-release-evidence.mjs, app/tests/unit/release/real-quality-release-evidence.test.ts, .planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.template.json</files>
  <behavior>
    - validateMetricSeparation rejects humanCorpus sections missing evidenceSource: "live_human"
    - validateMetricSeparation rejects fixtureValidation sections missing evidenceSource: "fixture"
    - acceptedCaveats entries require evidenceSource: "accepted_caveat"
    - BLENDED_FIELD_DENYLIST still enforced at root
    - Empty human corpus (evaluatedItemCount=0) may have null meanHumanVisualScore but must not claim ok visual status
  </behavior>
  <action>
    Extend `check-real-quality-release-evidence.mjs` `validateMetricSeparation` (and aggregate builder if present) to require `evidenceSource` on `qualityMetrics.humanCorpus`, `qualityMetrics.fixtureValidation`, and each `acceptedCaveats[]` entry per research Pattern 3. Add optional `denominatorNote` string validation when present.

    Create `.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.template.json` documenting Phase 135 sampling honesty contract: `schemaVersion`, `requirements: ["SAMPLE-01","SAMPLE-02","SAMPLE-03","SAMPLE-04"]`, nested `gates` with `sampleGuidance` snapshots, and `evidenceSource` tags on each metrics section (`live_human`, `fixture`, `accepted_caveat`, `technical_regression`).

    Extend `real-quality-release-evidence.test.ts` with cases for missing evidenceSource, mixed denominators, and valid tagged aggregate. Export test helpers if needed for evidence-honesty.test.ts reuse.

    When aggregating child phase evidence, ensure fixture counts are never copied into `humanCorpus.evaluatedItemCount`.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/release/real-quality-release-evidence.test.ts -x</automated>
  </verify>
  <done>QA-23 aggregate validator requires evidenceSource tags; 135-EVIDENCE.template.json exists with tagged sections.</done>
</task>

<task id="135-02-02" type="auto" tdd="true">
  <name>Task 2: Child evidence checkers + Phase 135 gate</name>
  <files>app/scripts/check-score-calibration-evidence.mjs, app/scripts/check-learning-impact-evidence.mjs, app/scripts/check-quality-improvement-evidence.mjs, app/scripts/check-sampling-sufficiency-evidence.mjs, app/scripts/run-sample-coverage.ts, app/tests/unit/human-quality/sampling/evidence-honesty.test.ts</files>
  <behavior>
    - Calibration checker: when status is insufficient_corpus, evidence must include non-empty sampleGuidance with additionalNeeded &gt; 0
    - Impact checker: when status is insufficient_sample, sampleGuidance required; movement deltas must be null
    - Quality checker: fixtureMetrics must have evidenceSource fixture; deltaRateByReason null when insufficient_sample
    - Sampling sufficiency checker validates 135-EVIDENCE.template.json shape and SAMPLE-01..04 requirement IDs
    - Checkers reject improvementClaimed or non-null movement deltas when any guidance item has additionalNeeded &gt; 0
  </behavior>
  <action>
    Extend the three child evidence checkers (130/131/132 scripts) to validate:
    1. `sampleGuidance` array present when status is insufficient (`insufficient_corpus` or `insufficient_sample`)
    2. Each guidance item has `currentCount`, `requiredCount`, `additionalNeeded`, `blockedClaim`
    3. `evidenceSource` on metric sections where applicable (calibration visual vs factual; quality `fixtureMetrics` tagged `fixture`)
    4. Reject non-null visual movement/improvement deltas when status is insufficient (Phase 131/132 precedent)

    Create `check-sampling-sufficiency-evidence.mjs` mirroring Phase 130–132 checker pattern: validate `135-EVIDENCE.template.json` (or `--evidence PATH`), run sampling unit tests unless `--skip-tests`, assert requirement IDs SAMPLE-01..04 documented.

    Add optional `run-sample-coverage.ts` CLI that calls `runSampleCoverage` from plan 135-03 service (import only — if service not yet merged, stub import and wire in 135-03; prefer implementing checker first with static template validation). Write output to `.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-EVIDENCE.json` when run with `--write`.

    Create `evidence-honesty.test.ts` importing exported validation functions from check scripts (or duplicate minimal pure validators in a shared `app/scripts/lib/evidence-honesty.mjs` if scripts are not importable — prefer extracting pure validators to testable module).

    Do not weaken existing CALIB/IMPACT/QUALITY requirement checks — only add SAMPLE-03 constraints.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/sampling/evidence-honesty.test.ts -x</automated>
  </verify>
  <done>Child checkers enforce sampleGuidance and evidenceSource; Phase 135 evidence gate script exists and passes on template.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence JSON → release gate | Untrusted manual edits to evidence files cross CI boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-04 | Tampering | evidence JSON files | mitigate | Checkers validate evidenceSource and reject blended fields via BLENDED_FIELD_DENYLIST |
| T-135-05 | Spoofing | fixtureMetrics as live_human | mitigate | Require evidenceSource: "fixture" on fixture sections; deny fixture counts in humanCorpus denominators |
| T-135-06 | Repudiation | missing guidance on insufficient | mitigate | Child checkers fail when insufficient status lacks sampleGuidance |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/release/real-quality-release-evidence.test.ts tests/unit/human-quality/sampling/evidence-honesty.test.ts`
- `node app/scripts/check-sampling-sufficiency-evidence.mjs --skip-tests`
- `node app/scripts/check-real-quality-release-evidence.mjs --skip-tests` (existing aggregate still passes with extended rules on template)
</verification>

<success_criteria>
- SAMPLE-03: Fixture, live human and accepted-caveat metrics are tagged and checker-enforced.
- Insufficient reports cannot pass evidence gate without structured sampleGuidance.
- No denominator mixing between humanCorpus and fixtureValidation buckets.
</success_criteria>

<output>
After completion, create `.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-02-SUMMARY.md`.
</output>
