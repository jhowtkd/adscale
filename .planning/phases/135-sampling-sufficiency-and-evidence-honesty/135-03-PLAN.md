---
phase: 135-sampling-sufficiency-and-evidence-honesty
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/human-quality/sampling/coverage.ts
  - app/src/server/human-quality/sampling/service.ts
  - app/src/app/api/feedback/sample-coverage/route.ts
  - app/src/app/api/feedback/sample-coverage/route.test.ts
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  - app/tests/unit/human-quality/sampling/coverage.test.ts
autonomous: true
requirements:
  - SAMPLE-04
must_haves:
  truths:
    - "Operator can open Coverage view and see cross-gate slice gaps in one place"
    - "Coverage report lists calibration, impact and quality-improvement blockers with additionalNeeded counts"
    - "Panel renders guidance from API — no hardcoded threshold prose"
    - "Coverage API is platform-owner / workspace-admin gated like other human-quality read APIs"
    - "Empty corpus shows all global gates insufficient with actionable nextOperatorAction"
  artifacts:
    - path: app/src/server/human-quality/sampling/coverage.ts
      provides: "buildSampleCoverageReport cross-gate rollup"
      exports: ["buildSampleCoverageReport"]
    - path: app/src/server/human-quality/sampling/service.ts
      provides: "runSampleCoverage orchestrating existing report runners"
      exports: ["runSampleCoverage"]
    - path: app/src/app/api/feedback/sample-coverage/route.ts
      provides: "GET sample coverage for workspace"
      exports: ["GET"]
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: "Coverage tab with slice gap table and nextOperatorAction"
      contains: "coverage"
  key_links:
    - from: app/src/app/api/feedback/sample-coverage/route.ts
      to: app/src/server/human-quality/sampling/service.ts
      via: "runSampleCoverage"
      pattern: "runSampleCoverage"
    - from: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      to: /api/feedback/sample-coverage
      via: "useQuery fetchSampleCoverage"
      pattern: "sample-coverage"
    - from: app/src/server/human-quality/sampling/coverage.ts
      to: app/src/server/human-quality/sampling/guidance.ts
      via: "merge guidance from calibration/impact/quality reports"
      pattern: "buildSampleCoverageReport"
---

# Plan 135-03: Operator Coverage API and Panel

<objective>
Give operators a unified read-only view of which slices need more evaluations before stronger release claims — cross-gate rollup via Coverage API and a new Coverage tab on HumanQualityCorpusPanel.
</objective>

Purpose: SAMPLE-04 requires operators to see slice gaps without reading Calibration, Impact and Quality tabs separately.
Output: `coverage.ts`, `service.ts`, `GET /api/feedback/sample-coverage`, Coverage tab UI, tests.

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-RESEARCH.md
@.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-VALIDATION.md
@app/src/app/api/feedback/learning-impact/route.ts
@app/src/server/auth/calibration-access.ts
@app/src/components/feedback/HumanQualityCorpusPanel.tsx

Depends on Plan 135-01: `SampleGuidance`, guidance builders, and reports with `sampleGuidance` attached.

<interfaces>
From `app/src/server/auth/calibration-access.ts`:
```typescript
export async function requireCalibrationAccess(
  request: Request,
  requestedWorkspaceId?: string | null
): Promise<CalibrationAccessResult>;
```

From `app/src/app/api/feedback/learning-impact/route.ts` (API pattern):
```typescript
const querySchema = z.object({
  workspaceId: z.string().uuid().optional(),
  cohort: z.enum(HUMAN_QUALITY_CORPUS_COHORTS).optional(),
});
// requireCalibrationAccess → runLearningImpact → NextResponse.json({ report })
```

From `HumanQualityCorpusPanel.tsx`:
```typescript
const PANEL_TABS = [
  { id: "queue", label: "Queue" },
  { id: "calibration", label: "Calibration" },
  { id: "impact", label: "Impact" },
  { id: "quality", label: "Quality" },
];
// Panel hides when queue + calibration + impact + quality all return 403
```

Research Pattern 4 target:
```typescript
export interface SampleCoverageReport {
  schemaVersion: 1;
  capturedAt: string;
  evaluatedItemCount: number;
  gates: Array<{ id: string; status: string; blockedClaims: string[] }>;
  sliceGaps: SampleGuidance[];
  nextOperatorAction: string;
}
```

Claude's discretion: Add **Coverage** tab (not Queue summary card) — matches research recommendation for dedicated operator view.
Do NOT build trend charts or time-series — snapshot only (Pitfall 4).
</interfaces>
</context>

<tasks>

<task id="135-03-01" type="auto" tdd="true">
  <name>Task 1: Cross-gate coverage rollup service</name>
  <files>app/src/server/human-quality/sampling/coverage.ts, app/src/server/human-quality/sampling/service.ts, app/tests/unit/human-quality/sampling/coverage.test.ts</files>
  <behavior>
    - buildSampleCoverageReport merges sampleGuidance from calibration, impact and quality reports
    - sliceGaps sorted by additionalNeeded descending
    - gates array includes calibration_global, impact_global, quality_improvement with correct status
    - evaluatedItemCount=0 yields all global gates insufficient and nextOperatorAction mentions evaluating corpus items
    - nextGate field identifies which gate blocks strongest claim (calibration before impact before release)
  </behavior>
  <action>
    Implement `buildSampleCoverageReport` in `coverage.ts` per research Pattern 4. Accept pre-built calibration, impact and quality reports (or their `sampleGuidance` + status + evaluatedItemCount). Normalize `insufficient_corpus` to `insufficient_sample` in gate status for display via `normalizeSamplingStatus` from guidance.ts.

    Implement `runSampleCoverage` in `service.ts` that reuses existing `runScoreCalibration`, `runLearningImpact`, and quality improvement service functions (same as individual APIs) with workspace/cohort filters. Respect existing row caps (calibration 500, impact truncation). Return `SampleCoverageReport`.

    Write `coverage.test.ts` with fixture reports: empty corpus, partial global sufficiency, slice-arm gaps. Assert `sliceGaps` length, `additionalNeeded` values, and `nextOperatorAction` string.

    Do not query Postgres directly for counts if report runners already provide evaluatedItemCount — reuse report outputs for consistency with tab views.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/human-quality/sampling/coverage.test.ts -x</automated>
  </verify>
  <done>SAMPLE-04 server-side cross-gate rollup exists with tested gap merging and operator action summary.</done>
</task>

<task id="135-03-02" type="auto" tdd="true">
  <name>Task 2: Coverage API with platform-owner auth</name>
  <files>app/src/app/api/feedback/sample-coverage/route.ts, app/src/app/api/feedback/sample-coverage/route.test.ts</files>
  <behavior>
    - GET returns 200 with SampleCoverageReport for authorized platform owner
    - GET returns 403 for unauthorized user
    - Invalid workspaceId UUID returns 400 validation_error
    - Response shape includes gates, sliceGaps, nextOperatorAction, evaluatedItemCount
  </behavior>
  <action>
    Create `GET /api/feedback/sample-coverage/route.ts` mirroring `learning-impact/route.ts` pattern: Zod query schema (`workspaceId` optional uuid, `cohort` optional enum), `requireCalibrationAccess`, call `runSampleCoverage`, return `{ report }` JSON.

    Write `route.test.ts` with mocked auth and service. Test 403, 400, and 200 happy path.

    Workspace-scoped by default per research recommendation (match Phase 130); platform owner may omit workspaceId for global rollup if existing services support it — follow `runScoreCalibration` workspace filter semantics.

    Read Next.js breaking-change guide in `node_modules/next/dist/docs/` before creating App Router route file.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/sample-coverage/route.test.ts -x</automated>
  </verify>
  <done>Coverage API enforces auth and returns SampleCoverageReport JSON.</done>
</task>

<task id="135-03-03" type="auto">
  <name>Task 3: Coverage tab and guidance-driven insufficient UI</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>
    Add `{ id: "coverage", label: "Coverage" }` to `PANEL_TABS`. Add `fetchSampleCoverage(workspaceId, cohortFilter)` helper returning null on 403 (same pattern as other fetches). Add `coverageQuery` with invalidation on evaluation submit alongside other report queries.

    Build `CoverageTabContent` component rendering:
    - `evaluatedItemCount` and `nextOperatorAction` summary
    - Gate status table (calibration / impact / quality_improvement / trend_global placeholder with threshold note — trend charts deferred to Phase 136)
    - Slice gaps table from `report.sliceGaps` showing gate, sliceKey, arm, additionalNeeded, blockedClaim

    Replace hardcoded insufficient prose in Impact, Quality and Calibration tab components (lines referencing "≥5 evaluated items" and "≥3 per arm") with `report.sampleGuidance?.map(...)` rendering per research panel example. Keep fallback message if guidance array empty.

    Add `fixtureMetrics` badge `evidenceSource: fixture` in Quality tab when fixtureMetrics present (SAMPLE-03 UI parity).

    Extend panel hide logic: include `coverageForbidden` in the all-403 check (five APIs: queue, calibration, impact, quality, coverage).

    Extend `HumanQualityCorpusPanel.test.tsx`: Coverage tab renders gaps from mocked API; insufficient tabs render guidance list not hardcoded thresholds.
  </action>
  <verify>
    <automated>cd app && npm test -- src/components/feedback/HumanQualityCorpusPanel.test.tsx -x</automated>
  </verify>
  <done>Operator sees Coverage tab with cross-gate gaps; existing tabs use API-driven sampleGuidance messaging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → sample-coverage API | Untrusted workspaceId/cohort query params |
| Coverage API → Postgres | Cross-tenant data via corpus joins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-07 | Information disclosure | sample-coverage route | mitigate | requireCalibrationAccess + workspace filter on runSampleCoverage |
| T-135-08 | Denial of service | runSampleCoverage | mitigate | Reuse existing report row caps; no unbounded slice enumeration |
| T-135-09 | Tampering | client-side gap display | transfer | Server computes all counts; UI read-only |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/human-quality/sampling/coverage.test.ts src/app/api/feedback/sample-coverage/route.test.ts src/components/feedback/HumanQualityCorpusPanel.test.tsx`
- `cd app && npm run build`
- Manual (deferred): with live evaluated items, Coverage tab counts match Calibration/Impact tabs
</verification>

<success_criteria>
- SAMPLE-04: Operator can see which slices need more samples before stronger release claims.
- Coverage is snapshot-only — no trend charts or time-series filters.
- Panel uses API sampleGuidance instead of hardcoded threshold strings.
</success_criteria>

<output>
After completion, create `.planning/phases/135-sampling-sufficiency-and-evidence-honesty/135-03-SUMMARY.md`.
</output>
