---
phase: 134-live-corpus-operations
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  - app/src/components/feedback/OwnerAnalyticsPanel.tsx
autonomous: true
requirements:
  - LIVEQUAL-02
  - LIVEQUAL-03
  - LIVEQUAL-04
must_haves:
  truths:
    - "Owner panel shows operational queue progress before individual review"
    - "Reviewer sees workspace/campaign/mode/format/cohort/version context while evaluating"
    - "Reviewer can move through pending items efficiently after submit"
    - "Required evaluation fields stay enforced"
    - "Preview image remains ephemeral UI data and is never submitted back as corpus payload"
  artifacts:
    - path: app/src/components/feedback/HumanQualityCorpusPanel.tsx
      provides: compact live corpus operations UI
    - path: app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
      provides: queue progress and fast-review coverage
---

# Plan 134-02: Operator Review UX and Fast Evaluation Loop

<objective>
Upgrade the existing human-quality corpus panel from a basic pending queue into a practical live-operations review surface with progress, compact metadata and fast repeated evaluation.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-live-corpus-operations/134-CONTEXT.md
@.planning/phases/134-live-corpus-operations/134-RESEARCH.md
@.planning/phases/134-live-corpus-operations/134-VALIDATION.md
@.planning/phases/134-live-corpus-operations/134-01-PLAN.md
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/src/components/feedback/OwnerAnalyticsPanel.tsx
</context>

<tasks>

<task id="134-02-01" type="test-first">
  <name>Show live queue progress and review position</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>Render the progress metadata returned by the corpus API: pending/evaluated totals, counts by cohort/mode/format, and current review position. Keep the UI dense and operational, using existing tabs/panels rather than introducing a new dashboard surface.</action>
  <verify><automated>cd app && npm test -- app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</automated></verify>
  <done>Operator can see what remains to review and which corpus dimensions are represented.</done>
</task>

<task id="134-02-02" type="test-first">
  <name>Make evaluation faster while preserving required judgment fields</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>Improve the queue review flow for repeated evaluations: compact metadata, clear current item, required visual score/factual pass/intent/failure reason controls, submit-and-advance behavior, and stable loading/error states. Do not allow submission without required fields.</action>
  <verify><automated>cd app && npm test -- app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</automated></verify>
  <done>Reviewer can evaluate pending items quickly without weakening the structured rubric.</done>
</task>

<task id="134-02-03" type="test-first">
  <name>Guard UI payloads and owner-only fallback states</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>Ensure UI never posts previewImageUrl, signed URL, outputKey, prompt or model payload fields back to the corpus API. Preserve the existing behavior where the panel hides or degrades cleanly on 403, and show operational empty states when the queue is empty.</action>
  <verify><automated>cd app && npm test -- app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</automated></verify>
  <done>Fast review UI remains privacy-safe and usable for owner-only workflows.</done>
</task>

</tasks>

<verification>
- `cd app && npm test -- app/src/components/feedback/HumanQualityCorpusPanel.test.tsx`
- `cd app && npm run build`
</verification>

<success_criteria>
- LIVEQUAL-02: Queue progress is visible in the operator panel.
- LIVEQUAL-03: Reviewer can complete structured evaluations in a faster repeatable flow.
- LIVEQUAL-04: UI does not leak or resubmit unsafe artifact fields.
</success_criteria>

<output>
After completion, create `.planning/phases/134-live-corpus-operations/134-02-SUMMARY.md`.
</output>
