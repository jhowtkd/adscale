---
phase: 134-live-corpus-operations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/service.ts
  - app/src/server/repositories/human-quality-corpus.ts
  - app/src/app/api/feedback/human-quality-corpus/route.ts
  - app/tests/unit/human-quality/live-corpus-operations.test.ts
  - app/src/app/api/feedback/human-quality-corpus/route.test.ts
autonomous: true
requirements:
  - LIVEQUAL-01
  - LIVEQUAL-02
  - LIVEQUAL-04
must_haves:
  truths:
    - "Operator can submit a controlled batch of real derivation IDs for corpus selection"
    - "Batch API reports per-item selected, duplicate, invalid, missing-profile and unsafe-payload outcomes"
    - "Queue progress can be queried by workspace and includes pending/evaluated counts grouped by cohort, mode and format"
    - "No raw prompts, signed URLs, output keys, auth material or unbounded payloads are persisted"
  artifacts:
    - path: app/src/server/human-quality/service.ts
      provides: batch selection and queue progress service contracts
    - path: app/src/server/repositories/human-quality-corpus.ts
      provides: progress summary queries for operational queue state
    - path: app/src/app/api/feedback/human-quality-corpus/route.ts
      provides: owner-only API shape for batch selection and progress response
---

# Plan 134-01: Batch Selection and Queue Progress Contracts

<objective>
Add the operational backend contracts for live corpus batches: controlled multi-item selection, explicit per-item outcomes, and queue progress summaries for owner workflows.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-live-corpus-operations/134-CONTEXT.md
@.planning/phases/134-live-corpus-operations/134-RESEARCH.md
@.planning/phases/134-live-corpus-operations/134-VALIDATION.md
@.planning/REQUIREMENTS.md
@app/src/server/human-quality/service.ts
@app/src/server/repositories/human-quality-corpus.ts
@app/src/app/api/feedback/human-quality-corpus/route.ts
</context>

<tasks>

<task id="134-01-01" type="test-first">
  <name>Add batch selection service with per-item outcomes</name>
  <files>app/src/server/human-quality/service.ts, app/tests/unit/human-quality/live-corpus-operations.test.ts</files>
  <action>Introduce a batch-oriented service helper that accepts workspaceId, campaignId, corpusVersion, cohort, selectedByUserId and a bounded list of derivationIds. Reuse `selectDerivationForCorpus` for the actual selection path so all existing membership, dedupe, client-profile and payload safety checks remain authoritative. Return per-item results instead of failing the whole batch on duplicates or invalid rows.</action>
  <verify><automated>cd app && npm test -- tests/unit/human-quality/live-corpus-operations.test.ts</automated></verify>
  <done>Operator batch selection can process mixed-validity real derivations and produce actionable per-item outcomes.</done>
</task>

<task id="134-01-02" type="test-first">
  <name>Add queue progress summary repository/service contracts</name>
  <files>app/src/server/repositories/human-quality-corpus.ts, app/src/server/human-quality/service.ts, app/tests/unit/human-quality/live-corpus-operations.test.ts</files>
  <action>Add a repository query and service wrapper that summarize corpus operations for a workspace: total pending, total evaluated, counts by cohort, generationMode and format, plus latest selected/evaluated timestamps where available. Keep this operational; do not compute trend or sufficiency claims in this phase.</action>
  <verify><automated>cd app && npm test -- tests/unit/human-quality/live-corpus-operations.test.ts</automated></verify>
  <done>Owner UI/API can show live queue progress without inferring quality movement.</done>
</task>

<task id="134-01-03" type="test-first">
  <name>Expose owner-only batch/progress API without weakening privacy boundaries</name>
  <files>app/src/app/api/feedback/human-quality-corpus/route.ts, app/src/app/api/feedback/human-quality-corpus/route.test.ts</files>
  <action>Extend the existing platform-owner route so `GET` can include progress metadata and `POST` can accept either the existing single derivation payload or a controlled batch payload. Enforce a conservative batch size cap, stable validation errors, and no persistence of preview URLs or forbidden payload keys.</action>
  <verify><automated>cd app && npm test -- app/src/app/api/feedback/human-quality-corpus/route.test.ts tests/unit/human-quality/live-corpus-operations.test.ts</automated></verify>
  <done>API supports live corpus operations while preserving owner-only access and payload safety.</done>
</task>

</tasks>

<verification>
- `cd app && npm test -- tests/unit/human-quality/live-corpus-operations.test.ts app/src/app/api/feedback/human-quality-corpus/route.test.ts`
- `cd app && npm run build`
</verification>

<success_criteria>
- LIVEQUAL-01: Controlled weekly-style batch selection is possible through service/API contracts.
- LIVEQUAL-02: Queue progress can be queried by operational dimensions.
- LIVEQUAL-04: Unsafe artifacts remain rejected and signed URLs remain ephemeral only.
</success_criteria>

<output>
After completion, create `.planning/phases/134-live-corpus-operations/134-01-SUMMARY.md`.
</output>
