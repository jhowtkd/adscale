---
phase: 169-real-corpus-and-claim-gates
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/server/repositories/human-quality-feedback-artifact.ts
  - app/src/server/human-quality/global-evidence-service.ts
  - app/src/server/human-quality/global-evidence.ts
  - app/src/app/api/feedback/global-corpus-evidence/route.ts
  - app/tests/unit/human-quality/global-evidence.test.ts
  - app/src/app/api/feedback/global-corpus-evidence/route.test.ts
  - app/tests/unit/brand-taste/calibration-evidence.test.ts
autonomous: true
requirements:
  - SOURCE-02
  - SOURCE-03
  - SOURCE-04
user_setup: []
must_haves:
  truths:
    - Source composition can be evaluated for the active workspace/clientProfile scope.
    - Claims remain withheld when the active scope is fixture-only, operator-only or sample-insufficient.
    - Global reports may summarize scopes, but brand-level claims use selected profile sufficiency.
  artifacts:
    - app/src/server/human-quality/global-evidence.ts
    - app/src/server/human-quality/global-evidence-service.ts
    - app/tests/unit/human-quality/global-evidence.test.ts
  key_links:
    - Source composition counts align with the same active filters as evaluated rows.
    - Per-brand calibration evidence stays isolated by `clientProfileId`.
---

<objective>
Gate customer-real and quality claims by active brand evidence, not by global fixture success.

Purpose: The app can have many rows and still be unable to make a claim for the brand the user is looking at. Phase 169 must make that distinction explicit.
Output: filtered evidence service/API behavior plus tests for fixture-only, operator-only and sufficient real-customer scopes.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-real-corpus-and-claim-gates/169-CONTEXT.md
@.planning/phases/169-real-corpus-and-claim-gates/169-RESEARCH.md
@.planning/phases/169-real-corpus-and-claim-gates/169-01-SUMMARY.md
@app/src/server/human-quality/global-evidence.ts
@app/src/server/human-quality/global-evidence-service.ts
@app/src/server/repositories/human-quality-feedback-artifact.ts
@app/src/app/api/feedback/global-corpus-evidence/route.ts
@app/tests/unit/human-quality/global-evidence.test.ts
@app/tests/unit/brand-taste/calibration-evidence.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active profile filters to evidence source composition</name>
  <files>app/src/server/repositories/human-quality-feedback-artifact.ts, app/src/server/human-quality/global-evidence-service.ts</files>
  <action>
    Extend source-composition counting to support optional `clientProfileId`. If artifact rows do not carry `clientProfileId`, join through the corpus item table rather than denormalizing blindly. Preserve existing `workspaceId` and `cohort` filters.

    Extend `RunGlobalCorpusEvidenceInput` with optional `workspaceId` and `clientProfileId`. Pass the same active filters into progress, source composition, sample coverage and evaluated-row queries where those repositories already support it. If a downstream path cannot support the filter yet, surface that as a withheld/insufficient operational status rather than silently using global data.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/global-evidence.test.ts</verify>
  <done>Source composition for an active profile excludes rows from other profiles/workspaces.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen claim evaluation semantics</name>
  <files>app/src/server/human-quality/global-evidence.ts, app/tests/unit/human-quality/global-evidence.test.ts</files>
  <action>
    Update claim evaluation names/messages so `fixtureOnly` means "no real_customer rows in the active scope" and not "only synthetic rows". Add tests for:
    1. synthetic-only scope blocks `validated_against_customer_real`;
    2. operator-only scope also blocks `validated_against_customer_real`;
    3. mixed operator plus synthetic without real customer remains blocked;
    4. sufficient real_customer count plus passing sample coverage can allow customer-real claims.

    Keep `operator_imported` visible and useful, but never equivalent to `real_customer`.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/global-evidence.test.ts</verify>
  <done>Claim tests fail if operator-only evidence unlocks customer-real proof.</done>
</task>

<task type="auto">
  <name>Task 3: Expose scoped evidence route parameters</name>
  <files>app/src/app/api/feedback/global-corpus-evidence/route.ts, app/src/app/api/feedback/global-corpus-evidence/route.test.ts</files>
  <action>
    Add optional `workspaceId` and `clientProfileId` query params to the owner global evidence route. Validate them as bounded strings and pass them to `runGlobalCorpusEvidence`. Keep existing `cohort` behavior. Tests should prove parameters are accepted and malformed values are rejected.
  </action>
  <verify>cd app && npm test -- --run src/app/api/feedback/global-corpus-evidence/route.test.ts</verify>
  <done>Owner evidence route can request active-scope evidence for a selected brand/profile.</done>
</task>

<task type="auto">
  <name>Task 4: Prove per-brand calibration claim isolation</name>
  <files>app/tests/unit/brand-taste/calibration-evidence.test.ts</files>
  <action>
    Add a focused test with two `clientProfileId` values and mixed source labels. Prove the selected brand evidence does not count source/sample rows from another profile and does not produce external claim language when the selected profile lacks real-customer sufficiency.
  </action>
  <verify>cd app && npm test -- --run tests/unit/brand-taste/calibration-evidence.test.ts</verify>
  <done>Brand evidence remains isolated even when another profile has sufficient real-customer evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm test -- --run tests/unit/human-quality/global-evidence.test.ts src/app/api/feedback/global-corpus-evidence/route.test.ts tests/unit/brand-taste/calibration-evidence.test.ts`
- [ ] `rg -n "validated_against_customer_real|operator_imported|real_customer|synthetic_fixture" app/src/server/human-quality app/tests/unit/human-quality app/tests/unit/brand-taste`
- [ ] Confirm route/service scopes do not fall back to global data when `clientProfileId` is requested.
</verification>

<success_criteria>

- Evidence source composition supports active brand/profile scope.
- Fixture-only and operator-only evidence cannot unlock customer-real claims.
- Customer-real/agreement/improvement claims are withheld until selected scope sufficiency passes.
- Per-brand evidence remains isolated by `clientProfileId`.
</success_criteria>

<output>
After completion, create `.planning/phases/169-real-corpus-and-claim-gates/169-02-SUMMARY.md`
</output>
