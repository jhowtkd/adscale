---
phase: 169-real-corpus-and-claim-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/candidate-promotion.ts
  - app/src/server/human-quality/source-label.ts
  - app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.ts
  - app/tests/unit/human-quality/candidate-promotion.test.ts
  - app/tests/unit/human-quality/candidate-capture.test.ts
  - app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.test.ts
autonomous: true
requirements:
  - SOURCE-01
  - SOURCE-02
user_setup: []
must_haves:
  truths:
    - Owner/operator promotion can explicitly preserve or set a valid source label for any candidate with `clientProfileId`.
    - Auto-captured derivation candidates keep the existing source-label defaults and are not silently relabeled.
    - No customer-name-specific path exists; Cenbrap data can only move through the generic source-labeled path.
  artifacts:
    - app/src/server/human-quality/candidate-promotion.ts
    - app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.ts
    - app/tests/unit/human-quality/candidate-promotion.test.ts
  key_links:
    - Candidate `clientProfileId` remains required before queue promotion.
    - Source labels remain one of `synthetic_fixture`, `operator_imported`, `real_customer`.
---

<objective>
Make real/operator corpus intake explicit and client-agnostic.

Purpose: Phase 169 starts by ensuring the owner can promote or import source-labeled corpus rows for any brand/profile without treating a test customer as the model path.
Output: promotion service/API support and tests proving `clientProfileId` plus source label are preserved and validated.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-real-corpus-and-claim-gates/169-CONTEXT.md
@.planning/phases/169-real-corpus-and-claim-gates/169-RESEARCH.md
@app/src/server/human-quality/source-label.ts
@app/src/server/human-quality/candidate-capture.ts
@app/src/server/human-quality/candidate-promotion.ts
@app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.ts
@app/tests/unit/human-quality/candidate-capture.test.ts
@app/tests/unit/human-quality/candidate-promotion.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit source-label validation for promotion</name>
  <files>app/src/server/human-quality/source-label.ts, app/src/server/human-quality/candidate-promotion.ts</files>
  <action>
    Add a narrow parser/helper for owner-supplied source labels. Accept only `operator_imported` and `real_customer` for explicit owner/operator promotion unless the existing candidate already carries `synthetic_fixture`. Preserve the candidate's existing source label by default.

    Extend `promoteCorpusCandidateToQueue` input with an optional explicit source-label field. When supplied, validate it and use it for the queue/corpus item source semantics without changing workspace, campaign, derivation or `clientProfileId`. Continue rejecting candidates with missing `clientProfileId`. Do not add any branch for Cenbrap or customer names.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/candidate-promotion.test.ts</verify>
  <done>Promotion accepts only valid explicit owner source labels and preserves `clientProfileId`/source metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Expose source label in owner promote route</name>
  <files>app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.ts, app/src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.test.ts</files>
  <action>
    Extend the promote route schema from `{ cohort }` to `{ cohort, sourceLabel? }`. Keep platform-owner authorization unchanged. Pass `sourceLabel` through to `promoteCorpusCandidateToQueue` only after validation. Invalid labels must return a 400-style response with a clear error.

    Keep the route backward compatible: requests without `sourceLabel` continue to promote using the candidate's existing label.
  </action>
  <verify>cd app && npm test -- --run 'src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.test.ts'</verify>
  <done>Route tests cover default promotion, explicit `operator_imported`, explicit `real_customer` and invalid label rejection.</done>
</task>

<task type="auto">
  <name>Task 3: Prove auto-capture defaults still work</name>
  <files>app/tests/unit/human-quality/candidate-capture.test.ts, app/tests/unit/human-quality/candidate-promotion.test.ts</files>
  <action>
    Add or keep tests proving completed derivation capture still resolves `real_customer` by default and `synthetic_fixture` for configured synthetic workspaces. Add promotion tests proving explicit source selection does not mutate unrelated candidate fields and cannot bypass missing `clientProfileId`.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/candidate-capture.test.ts tests/unit/human-quality/candidate-promotion.test.ts</verify>
  <done>Auto-capture and explicit promotion paths are both covered without customer-specific fixtures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm test -- --run tests/unit/human-quality/candidate-capture.test.ts tests/unit/human-quality/candidate-promotion.test.ts 'src/app/api/feedback/human-quality-corpus/candidates/[id]/promote/route.test.ts'`
- [ ] `rg -n "Cenbrap|CENBRAP" app/src/server/human-quality app/src/app/api/feedback/human-quality-corpus/candidates`
- [ ] Confirm any Cenbrap match is pre-existing seed/fixture text, not a new Phase 169 branch.
</verification>

<success_criteria>

- Owner promotion supports explicit source-labeled real/operator corpus rows.
- Missing `clientProfileId` remains a hard stop.
- Auto-capture source behavior is unchanged.
- No customer-specific path is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/169-real-corpus-and-claim-gates/169-01-SUMMARY.md`
</output>
