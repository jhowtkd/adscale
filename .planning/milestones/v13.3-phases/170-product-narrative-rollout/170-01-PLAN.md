---
phase: 170-product-narrative-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketing/brand/in-app-copy-checklist.md
  - app/tests/unit/i18n/product-narrative-copy.test.ts
autonomous: true
requirements:
  - BRAND-04
user_setup: []
must_haves:
  truths:
    - In-repo checklist exists with 7 auditable criteria gating in-app copy changes.
    - Automated test scans narrative namespaces for forbidden claims and locale key parity.
    - Checklist explicitly withholds performance lift, customer-real proof, and Cenbrap references on general surfaces.
  artifacts:
    - path: marketing/brand/in-app-copy-checklist.md
      provides: BRAND-04 human audit gate
      contains: "Curator > operator"
    - path: app/tests/unit/i18n/product-narrative-copy.test.ts
      provides: Automated copy guard
      exports: ["describe", "it"]
  key_links:
    - from: app/tests/unit/i18n/product-narrative-copy.test.ts
      to: app/messages/en.json
      via: JSON import and pattern scan
      pattern: "en\\.json|pt-BR\\.json"
    - from: marketing/brand/in-app-copy-checklist.md
      to: marketing/brand/conceituacao.md
      via: derived criteria reference
      pattern: "conceituacao|§4"
---

<objective>
Establish the BRAND-04 copy review gate and automated guard before any narrative edits land.

Purpose: Phase 170 copy changes must be auditable and machine-checked against forbidden claims before onboarding/workflow strings are rewritten.
Output: `in-app-copy-checklist.md` plus Vitest copy guard scaffold (Wave 0).
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-product-narrative-rollout/170-CONTEXT.md
@.planning/phases/170-product-narrative-rollout/170-RESEARCH.md
@marketing/brand/conceituacao.md
@marketing/brand/README.md
@app/messages/en.json
@app/messages/pt-BR.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add i18n copy guard test scaffold</name>
  <files>app/tests/unit/i18n/product-narrative-copy.test.ts</files>
  <behavior>
    - Test 1: `en.json` and `pt-BR.json` have identical key sets for namespaces `onboarding`, `dashboard.home`, `metadata`, `navigation`, `auth`, `campaign`, `steps`, `generation`, `library`, `template`, `review` (top-level approve/reject guidance keys only — exclude `review.olhar*`, `review.export*`, `review.decision*` subtrees).
    - Test 2: Scanned narrative strings must NOT match forbidden patterns (case-insensitive): Cenbrap, calibration, corpus, agreement rate, sample sufficiency, Olhar (in onboarding/dashboard/empty namespaces), magic AI phrases (`magic`, `revolutionary`, `game-changer`, `does everything`, `one click`, `substitutes the designer`, `10x`, `ROAS`, `scale without hiring`).
    - Test 3: `onboarding.step1Desc` through `step5Desc` in EN must include curator-role vocabulary (`curator` or `brief` + `batch` or `curate`/`approve`) once Plan 02 lands — initially expect RED until copy updated; use `test.todo` or skip with comment only for pre-Plan-02 baseline if needed, but structure test now.
  </behavior>
  <action>
    Create Vitest file importing `app/messages/en.json` and `app/messages/pt-BR.json`. Implement namespace key-parity helper and forbidden-pattern scanner over string values in narrative namespaces listed in RESEARCH.md.

    For Olhar exclusion: when scanning `review` namespace, only include keys used on general user guidance (`approve`, `reject`, `feedbackPlaceholder`, `blockingFailureHint`, `polishSuggestionHint`, `regenerateWithFixesDescription`) — skip `olhar*`, `export*`, `decision*`, `hardFailureCodes`, `olharVerdict*`.

    Per D-04 / BRAND-04: encode Phase 169 claim-gate constraints (no customer-real proof, no performance lift) in forbidden list. Test may fail on current copy for positive curator assertions — Plans 02–03 will turn those green.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts</automated>
  </verify>
  <done>Copy guard test file exists; parity and forbidden-pattern tests run (parity may pass immediately; curator positive tests may fail until Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Publish in-app copy review checklist</name>
  <files>marketing/brand/in-app-copy-checklist.md</files>
  <action>
    Create checklist derived from CONTEXT decisions and conceituacao §4.2–4.4. Use numbered criteria (auditable list per D-04 discretion):

    1. Reflects Curator > operator at workflow decision points.
    2. No overclaim of full automation.
    3. No performance lift, ROAS/CPA, or speed-multiple claims.
    4. No customer-real, agreement-rate, or quality-improvement proof implied.
    5. No Cenbrap or brand-specific fixture as product proof.
    6. No calibration/corpus/Olhar jargon on general user surfaces.
    7. Uses canonical vocabulary; avoids conceituacao forbidden list.

    Add a "How to run" section referencing `npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts` and manual spot-check steps. Note conceituacao.md remains reference-only / production frozen per CONTEXT.
  </action>
  <verify>
    <automated>test -f marketing/brand/in-app-copy-checklist.md && rg -n "Curator > operator|performance lift|Cenbrap|calibration" marketing/brand/in-app-copy-checklist.md</automated>
  </verify>
  <done>Checklist file exists with all 7 criteria and execution instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy author → i18n JSON | Human/AI-authored strings become user-visible product promises |
| i18n → authenticated UI | All logged-in surfaces render next-intl messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-01 | I | app/messages/*.json | mitigate | Forbidden-pattern Vitest scan + checklist gate |
| T-170-02 | D | in-app-copy-checklist.md | accept | Static markdown; integrity via git review |
| T-170-03 | E | marketing claims in copy | mitigate | Withhold performance/customer-real phrases per checklist criterion 3–4 |
</threat_model>

<verification>
- [ ] `cd app && npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts`
- [ ] `rg -n "^[0-9]+\\." marketing/brand/in-app-copy-checklist.md | wc -l` returns 7
</verification>

<success_criteria>
- BRAND-04 checklist is auditable in-repo.
- Automated copy guard exists as Wave 0 for Plans 02–03.
</success_criteria>

<output>
After completion, create `.planning/phases/170-product-narrative-rollout/170-01-SUMMARY.md`
</output>
