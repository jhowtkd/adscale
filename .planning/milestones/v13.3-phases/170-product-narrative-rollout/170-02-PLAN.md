---
phase: 170-product-narrative-rollout
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
user_setup: []
must_haves:
  truths:
    - Dashboard tour 5 steps explain briefing → batch → curate arc in plain language.
    - Empty states on dashboard, campaigns, library, generation, and templates invite curator workflow CTAs.
    - No Cenbrap, calibration, corpus, or Olhar jargon in onboarding or empty-state copy.
    - EN and PT-BR onboarding/empty keys stay in parity.
  artifacts:
    - path: app/messages/en.json
      provides: English onboarding and empty-state narrative
      contains: "onboarding"
    - path: app/messages/pt-BR.json
      provides: Portuguese onboarding and empty-state narrative
      contains: "curador"
  key_links:
    - from: app/src/components/dashboard/OnboardingTour.tsx
      to: onboarding.* keys
      via: useTranslations("onboarding")
      pattern: "useTranslations\\(\"onboarding\"\\)"
    - from: app/src/app/(dashboard)/page.tsx
      to: dashboard.home.empty*
      via: useTranslations("dashboard")
      pattern: "emptyNoCampaigns|emptyCreateHint"
---

<objective>
Rewrite onboarding tour and empty-state copy with conservative curator framing (BRAND-01 at first-touch surfaces, BRAND-02).

Purpose: Users landing on dashboard or hitting zero-data states should understand ADScale as a curator workflow, not a generic image generator.
Output: Updated `onboarding.*`, `dashboard.home.empty*`, campaigns/library/generation/template empty keys in both locales.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/170-product-narrative-rollout/170-CONTEXT.md
@.planning/phases/170-product-narrative-rollout/170-RESEARCH.md
@marketing/brand/in-app-copy-checklist.md
@app/messages/en.json
@app/messages/pt-BR.json
@app/src/components/dashboard/OnboardingTour.tsx
@app/src/app/(dashboard)/page.tsx
@app/src/app/(dashboard)/campaigns/page.tsx

<interfaces>
Onboarding keys (rewrite all 5 steps, keep key names):
- `onboarding.step1Title` .. `step5Desc`, `skip`, `back`, `next`, `finish`

Empty-state keys to rewrite:
- `dashboard.home.emptyNoResults`, `emptyNoCampaigns`, `emptyAdjustSearch`, `emptyCreateHint`, `createCampaign`
- Campaign list empty keys under `dashboard` / campaigns page (grep `emptyNoCampaigns`, `emptyCreateHint`, `campaigns` list empty)
- `library.emptyTitle`, `library.emptyDescription`
- Generation gallery: `emptyTitle`, `emptyDescription`, `emptyAction` (under generation namespace — grep current keys)
- `template.emptyTitle`, `template.emptyDescription`

Approved direction (CONTEXT):
- EN: "Define your brief, generate variations as a batch, then curate what ships."
- PT: equivalent with "curador", never "operador" as user identity.
- Remove emoji from step1Title per discretion (tone-down).
- Forbidden: Cenbrap, calibration jargon, performance guarantees, magic AI language.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard onboarding tour copy</name>
  <files>app/messages/en.json, app/messages/pt-BR.json</files>
  <action>
    Rewrite `onboarding.step1Title` through `step5Desc` in EN and PT-BR per D-01/BRAND-02:

    - Step 1: Welcome + curator role intro (you define the brief; ADScale is your curation workspace).
    - Step 2: Quick actions framed as briefing inputs and batch starting points (not "AI generates magic").
    - Step 3: Campaign list as curation queue — review and approve what ships.
    - Step 4: Create campaign CTA — start with structured brief before generation.
    - Step 5: Credits as operational resource (neutral, no performance promise).

    Use canonical terms per D-03: briefing/plano criativo, variação, batch, aprovar/curar, workspace. EN uses "curator"; PT uses "curador". Remove 👋 from step1Title if present.

    Do NOT change tour component code, step count, or onboarding completion logic (Phase 26 scope lock).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts</automated>
  </verify>
  <done>All 5 onboarding steps convey curator arc; copy guard passes parity and forbidden-pattern checks for onboarding namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite empty-state copy across dashboard and library surfaces</name>
  <files>app/messages/en.json, app/messages/pt-BR.json</files>
  <action>
    Update empty-state keys per D-02/BRAND-02:

    - `dashboard.home.empty*` — explain first campaign as brief → batch → curate; CTA stays create campaign.
    - Campaigns list empty states (grep campaigns page for `EmptyState` title/description keys).
    - `library.emptyTitle` / `emptyDescription` — assets feed the briefing/batch workflow.
    - Generation gallery empty keys — point user to define brief and run batch, then curate.
    - `template.empty*` — templates save briefing patterns for repeat curation.

    Keep tone direct, technical, confident (founder-scientist at product level). No fixture/calibration/Cenbrap references. Update PT-BR in parallel for every EN change.

    Do not modify `EmptyState.tsx` unless a key structure change is required (should not be).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts && rg -n "Cenbrap|calibration|corpus|Olhar|agreement" app/messages/en.json app/messages/pt-BR.json | rg -v "review\.|corpus|owner|feedback" || true</automated>
  </verify>
  <done>Empty states explain curator role; no forbidden jargon in onboarding/dashboard/library/generation/template empty keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| First-run tour → new user mental model | Onboarding copy shapes product expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170-04 | I | onboarding.* strings | mitigate | Checklist + copy guard; conservative framing per CONTEXT |
| T-170-05 | S | false automation claims | mitigate | Avoid "AI decides alone" / "magic" phrasing in empty states |
</threat_model>

<verification>
- [ ] `cd app && npm test -- --run tests/unit/i18n/product-narrative-copy.test.ts`
- [ ] Manual grep: no Cenbrap in `onboarding` or `dashboard.home` keys
</verification>

<success_criteria>
- BRAND-02: Onboarding and empty states explain curator role without internal jargon.
- BRAND-01 partial: Curator framing at first-touch workflow decision points.
</success_criteria>

<output>
After completion, create `.planning/phases/170-product-narrative-rollout/170-02-SUMMARY.md`
</output>
