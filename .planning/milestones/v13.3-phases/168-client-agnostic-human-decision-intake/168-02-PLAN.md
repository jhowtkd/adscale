---
phase: 168-client-agnostic-human-decision-intake
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/components/feedback/HumanQualityCorpusPanel.tsx
  - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
  - app/src/components/admin/calibration-status-copy.ts
  - app/src/components/admin/calibration-status-copy.test.ts
autonomous: true
requirements:
  - DECISION-01
  - DECISION-03
user_setup: []
must_haves:
  truths:
    - Owner-facing corpus review shows the item clientProfileId and source label clearly.
    - Fixture rows are described as fixture/seed or operator evidence only, not customer-real proof.
    - The queue can be narrowed to a clientProfileId when that filter is available.
  artifacts:
    - app/src/components/feedback/HumanQualityCorpusPanel.tsx
    - app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
    - app/src/components/admin/calibration-status-copy.ts
  key_links:
    - UI source copy aligns with backend source labels and per-brand evidence caveats.
    - HumanQualityCorpusPanel query params include clientProfileId only when the user selects a profile filter.
---

<objective>
Make the owner review surface visibly client-agnostic and fixture-safe.

Purpose: The owner should be able to review decisions for any brand/profile while seeing exactly what kind of evidence is being created. This avoids treating a test client fixture as product proof.
Output: UI copy, optional client profile filter wiring and component tests for source/copy behavior.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-client-agnostic-human-decision-intake/168-CONTEXT.md
@.planning/phases/168-client-agnostic-human-decision-intake/168-RESEARCH.md
@.planning/phases/168-client-agnostic-human-decision-intake/168-01-SUMMARY.md
@app/src/components/feedback/HumanQualityCorpusPanel.tsx
@app/src/components/feedback/HumanQualityCorpusPanel.test.tsx
@app/src/components/admin/calibration-status-copy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clientProfileId visibility and filter to corpus queue</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>
    Extend `CorpusQueueFilterState` with `clientProfileId: string`. Include it in `fetchPendingQueue` query params when present and in the query key invalidation list. Add a compact text input or select-compatible filter control labeled for client profile id. Keep the UI dense and consistent with existing queue filters.

    In the current item metadata block, show `clientProfileId` when present. Do not expose prompts, storage keys or signed URLs.
  </action>
  <verify>cd app && npm test -- --run app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</verify>
  <done>Tests prove the queue sends `clientProfileId` when set and current item metadata shows the profile id without breaking existing filters.</done>
</task>

<task type="auto">
  <name>Task 2: Make fixture copy explicit and non-customer-specific</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/admin/calibration-status-copy.ts, app/src/components/admin/calibration-status-copy.test.ts</files>
  <action>
    Add a small source-label copy helper if useful. `synthetic_fixture` must read as fixture/seed evidence that validates workflow only. `operator_imported` should read as operator-imported evidence. `real_customer` should read as real customer evidence. Do not mention Cenbrap unless the underlying data label/name is already displayed elsewhere.

    Update `calibration-status-copy.ts` if needed so fixture/operator warning copy does not imply operator-imported evidence is the same as customer-real validation. Keep text concise.
  </action>
  <verify>cd app && npm test -- --run app/src/components/feedback/HumanQualityCorpusPanel.test.tsx app/src/components/admin/calibration-status-copy.test.ts</verify>
  <done>Component/unit tests assert fixture warning copy and ensure there is no product-proof wording for fixture rows.</done>
</task>

<task type="auto">
  <name>Task 3: Keep submit UX compatible with generic decision recording</name>
  <files>app/src/components/feedback/HumanQualityCorpusPanel.tsx, app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</files>
  <action>
    After Plan 01 returns additional decision evidence metadata, keep the submit mutation tolerant: it should not depend on the new response field for UI success. Invalidate corpus queue and calibration/evidence query keys as before; add any brand profile/evidence query invalidation only if those query keys are already used in the panel.
  </action>
  <verify>cd app && npm test -- --run app/src/components/feedback/HumanQualityCorpusPanel.test.tsx</verify>
  <done>Submitting evaluation still succeeds with both old and new response shapes in tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm test -- --run app/src/components/feedback/HumanQualityCorpusPanel.test.tsx`
- [ ] `cd app && npm test -- --run app/src/components/admin/calibration-status-copy.test.ts`
- [ ] `rg -n "Cenbrap|CENBRAP" app/src/components/feedback app/src/components/admin` shows no new customer-specific copy in Phase 168 files.
</verification>

<success_criteria>

- Queue review can be narrowed by `clientProfileId`.
- Current item metadata makes profile/source visible.
- Fixture rows are explicitly not customer-real proof.
- Existing queue submit behavior remains stable.
</success_criteria>

<output>
After completion, create `.planning/phases/168-client-agnostic-human-decision-intake/168-02-SUMMARY.md`
</output>
