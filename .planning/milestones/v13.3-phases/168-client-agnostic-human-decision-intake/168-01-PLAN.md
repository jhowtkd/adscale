---
phase: 168-client-agnostic-human-decision-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/human-quality/human-decision-calibration.ts
  - app/src/server/human-quality/service.ts
  - app/tests/unit/human-quality/human-quality-service.test.ts
autonomous: true
requirements:
  - DECISION-02
  - DECISION-04
  - DECISION-05
user_setup: []
must_haves:
  truths:
    - A submitted human-quality corpus evaluation records canonical output decision evidence for the evaluated item's workspace, campaign, derivation and clientProfileId.
    - The same evaluation records or idempotently skips a calibration signal with the resolved source label.
    - Global mode still resolves workspace from the corpus item, not browser input.
  artifacts:
    - app/src/server/human-quality/human-decision-calibration.ts
    - app/src/server/human-quality/service.ts
    - app/tests/unit/human-quality/human-quality-service.test.ts
  key_links:
    - submitHumanEvaluation calls the generic decision/calibration bridge after evaluation and feedback artifact creation.
    - The bridge uses existing output-decision and calibration-signal recorders instead of duplicating repository writes.
---

<objective>
Create the server-side bridge from generic human-quality corpus evaluation to canonical decision and calibration evidence.

Purpose: Phase 168 must stop treating human decisions as Cenbrap-specific script data. Any evaluated corpus item with a `clientProfileId` should teach the per-brand evidence system.
Output: A reusable server helper, service wiring, and unit coverage for source labels, idempotency, global mode and client-profile isolation.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-client-agnostic-human-decision-intake/168-CONTEXT.md
@.planning/phases/168-client-agnostic-human-decision-intake/168-RESEARCH.md
@app/src/server/human-quality/service.ts
@app/src/server/output-learning/output-decision-recorder.ts
@app/src/server/output-learning/output-decision-events.ts
@app/src/server/brand-taste/calibration-signal-recorder.ts
@app/src/server/brand-taste/calibration-signal.ts
@app/tests/unit/human-quality/human-quality-service.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generic human decision calibration bridge</name>
  <files>app/src/server/human-quality/human-decision-calibration.ts</files>
  <action>
    Create a helper that accepts the evaluated corpus item, human evaluation, reviewer id, resolved source label and optional feedback artifact id. Map human-quality intent to output-decision action: approve -> approved, reject -> rejected, regenerate -> regenerated. Build a sanitized decision snapshot from the corpus item qualitySnapshot and evaluation reason fields. Call `recordOutputDecisionEvidence`, then `recordCalibrationSignalFromOutputDecisionEvent`.

    Use deterministic idempotency keys scoped to the evaluation id, for example `human-quality-evaluation:${evaluation.id}:output-decision` and `human-quality-evaluation:${evaluation.id}:calibration-signal`, so API retries do not create duplicate calibration signals. Preserve `workspaceId`, `clientProfileId`, `campaignId`, `derivationId`, `reviewerId`, `reviewedAt`, source label and reviewer rationale. Do not add any Cenbrap branch or customer-name branch.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/human-quality-service.test.ts</verify>
  <done>The helper composes existing recorders and exposes a typed result containing output decision event id and calibration signal status.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bridge from submitHumanEvaluation</name>
  <files>app/src/server/human-quality/service.ts</files>
  <action>
    After `submitCorpusEvaluation`, candidate source resolution and feedback artifact insertion, call the new bridge with `result.item`, `result.evaluation`, `input.reviewerUserId`, `sourceLabel` and `feedbackArtifact.id`. Keep the existing return shape backward compatible, adding a nested `decisionEvidence` or similarly explicit field only if tests need it.

    The service must still reject duplicate evaluations before any evidence is written. Global evaluation mode must continue to load the item by id and use `item.workspaceId` for all writes. If `item.clientProfileId` is null, skip calibration signal recording but still keep evaluation/artifact behavior unchanged; document that in helper return status.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/human-quality-service.test.ts 'src/app/api/feedback/human-quality-corpus/[id]/evaluation/route.test.ts'</verify>
  <done>`submitHumanEvaluation` creates canonical decision evidence for client-profile corpus evaluations without breaking existing API route behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extend service tests for source labels and isolation</name>
  <files>app/tests/unit/human-quality/human-quality-service.test.ts</files>
  <action>
    Mock `recordOutputDecisionEvidence` and `recordCalibrationSignalFromOutputDecisionEvent`. Add tests proving:
    1. an approved `operator_imported` evaluation writes an output decision event with the item `clientProfileId`, and then records a calibration signal with sourceLabel `operator_imported`;
    2. global mode resolves workspace from the item and does not use browser workspace input;
    3. `synthetic_fixture` sourceLabel remains `synthetic_fixture` in the calibration signal path;
    4. two different item `clientProfileId` values are passed through unchanged and do not cross-contaminate calls.

    Keep mocks precise. Do not assert implementation details unrelated to the bridge.
  </action>
  <verify>cd app && npm test -- --run tests/unit/human-quality/human-quality-service.test.ts</verify>
  <done>Unit tests fail without the bridge and pass with client-agnostic evidence recording.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm test -- --run tests/unit/human-quality/human-quality-service.test.ts`
- [ ] `cd app && npm test -- --run 'src/app/api/feedback/human-quality-corpus/[id]/evaluation/route.test.ts'`
- [ ] Confirm no code path branches on `Cenbrap`, `CENBRAP`, or a customer name.
</verification>

<success_criteria>

- Generic corpus evaluations write canonical output decision evidence.
- Generic corpus evaluations write or idempotently skip calibration signals.
- Source label and clientProfileId are preserved from corpus item/candidate data.
- Existing API route tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/168-client-agnostic-human-decision-intake/168-01-SUMMARY.md`
</output>
