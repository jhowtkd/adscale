---
phase: 171-persistent-product-trust-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/lib/hooks/use-workspace-settings.ts
  - app/src/lib/hooks/use-workspace-settings.test.tsx
  - app/src/components/settings/WorkspaceTab.tsx
autonomous: true
requirements:
  - TRUST-02
  - TRUST-04
user_setup: []
must_haves:
  truths:
    - WorkspaceTab loads settings from API on mount with loading skeleton.
    - Save persists via PATCH with idle/saving/saved states and no fake delay.
    - Danger zone delete button still shows comingSoon toast (not persisted).
    - Members see data but save is disabled or errors with forbidden if they attempt PATCH.
  artifacts:
    - path: app/src/lib/hooks/use-workspace-settings.ts
      provides: Workspace settings react-query hook
      exports: ["useWorkspaceSettings", "useUpdateWorkspaceSettings"]
    - path: app/src/components/settings/WorkspaceTab.tsx
      provides: API-backed workspace settings UI
  key_links:
    - from: app/src/components/settings/WorkspaceTab.tsx
      to: /api/workspace/settings
      via: useWorkspaceSettings hooks
      pattern: "useWorkspaceSettings|/api/workspace/settings"
---

<objective>
Wire WorkspaceTab to real workspace settings APIs using the BrandKitTab save/load pattern.

Purpose: TRUST-02 requires workspace values to survive refresh/login — UI must stop using Zustand mock state and fake delays.
Output: `use-workspace-settings` hook with tests and updated WorkspaceTab.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-persistent-product-trust-baseline/171-CONTEXT.md
@.planning/phases/171-persistent-product-trust-baseline/171-RESEARCH.md
@app/src/lib/hooks/use-brand-kit.ts
@app/src/components/settings/BrandKitTab.tsx
@app/src/components/settings/WorkspaceTab.tsx

<interfaces>
Workspace settings API response:
```typescript
export interface WorkspaceSettingsResponse {
  name: string;
  slug: string;
  description: string;
  industry: string;
  website: string;
  timezone: string;
  canEdit: boolean; // optional: derive from membership role if API exposes it, or infer from PATCH 403
}
```

From `app/src/server/auth/workspace.ts`:
```typescript
export type WorkspaceMemberRole = "owner" | "admin" | "member";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create use-workspace-settings hook with tests</name>
  <files>app/src/lib/hooks/use-workspace-settings.ts, app/src/lib/hooks/use-workspace-settings.test.tsx</files>
  <behavior>
    - useWorkspaceSettings GET /api/workspace/settings; maps null DB fields to empty strings in client shape.
    - useUpdateWorkspaceSettings PATCH partial settings; invalidates ["workspace-settings"] on success.
    - Mutation surfaces API error message on 403/409.
  </behavior>
  <action>
    Mirror `use-brand-kit.ts`:
    - Query key `["workspace-settings"]`
    - `fetchWorkspaceSettings` and `updateWorkspaceSettings` via apiFetch
    - Export typed response interface

    Tests follow `use-billing.test.tsx` pattern with mocked apiFetch.

    Optional: if Plan 01 API returns `role` or `canEdit` boolean, expose in hook for UI to disable Save for members.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/lib/hooks/use-workspace-settings.test.tsx</automated>
  </verify>
  <done>Hook + tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire WorkspaceTab to API-backed persistence</name>
  <files>app/src/components/settings/WorkspaceTab.tsx</files>
  <action>
    Per CONTEXT D-02 and D-04:

    1. Remove `useAppStore` for `workspaceSettings` and `updateWorkspaceSettings`.
    2. Load with `useWorkspaceSettings()` — Skeleton while pending.
    3. Sync form from query data on load/refetch.
    4. `hasChanges` vs query data.
    5. `handleSave`: mutation PATCH, saveState pattern identical to BrandKitTab/ProfileTab — **no fake 800ms delay**.
    6. On 403 error: toast with forbidden message; saveState idle.
    7. On 409 slug conflict: toast with API message.
    8. Danger zone delete: **keep** existing `comingSoon` toast — do NOT wire delete API (out of scope).
    9. If user is member (canEdit false): disable Save button and show read-only hint if product already has pattern, or disable inputs — conservative: disable Save when `canEdit === false`.
    10. Do NOT change i18n keys from Phase 170.

    Remove `await new Promise(r => setTimeout(r, 800))`.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/lib/hooks/use-workspace-settings.test.tsx && cd app && npm run build</automated>
  </verify>
  <done>WorkspaceTab API-backed; fake delay removed; delete stays comingSoon; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkspaceTab → settings API | Workspace identity fields cross to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-08 | E | Member save attempt | mitigate | Server 403 + client disable Save when canEdit false |
| T-171-09 | T | Slug change | mitigate | Server uniqueness check; client shows 409 error |
</threat_model>

<verification>
- Hook tests green.
- `rg "setTimeout.*800" app/src/components/settings/WorkspaceTab.tsx` returns empty.
- Danger zone still uses comingSoon toast (grep `comingSoon` in WorkspaceTab).
</verification>

<success_criteria>
- Workspace tab loads/saves via API with deterministic UX.
- Workspace delete not faked as persistent.
</success_criteria>

<output>
After completion, create `.planning/phases/171-persistent-product-trust-baseline/171-03-SUMMARY.md`
</output>
