---
phase: 171-persistent-product-trust-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/src/lib/hooks/use-user-profile.ts
  - app/src/lib/hooks/use-user-profile.test.tsx
  - app/src/components/settings/ProfileTab.tsx
autonomous: true
requirements:
  - TRUST-01
  - TRUST-04
user_setup: []
must_haves:
  truths:
    - ProfileTab loads profile from API on mount with loading skeleton.
    - Save button persists via PATCH and shows idle/saving/saved states without fake delay.
    - Email field is read-only; avatar upload uses API not base64 persistence.
    - Save errors show toast and return to idle (no stuck saving).
  artifacts:
    - path: app/src/lib/hooks/use-user-profile.ts
      provides: Profile react-query hook
      exports: ["useUserProfile", "useUpdateUserProfile", "useUploadProfileAvatar"]
    - path: app/src/components/settings/ProfileTab.tsx
      provides: API-backed profile settings UI
  key_links:
    - from: app/src/components/settings/ProfileTab.tsx
      to: /api/user/profile
      via: useUserProfile + useUpdateUserProfile
      pattern: "useUserProfile|/api/user/profile"
    - from: app/src/components/settings/ProfileTab.tsx
      to: /api/user/profile/avatar
      via: useUploadProfileAvatar on file select
      pattern: "useUploadProfileAvatar|profile/avatar"
---

<objective>
Wire ProfileTab to real profile APIs using the BrandKitTab save/load pattern.

Purpose: TRUST-01 requires profile values to survive refresh/login — UI must stop using Zustand mock state and fake delays.
Output: `use-user-profile` hook with tests and updated ProfileTab.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-persistent-product-trust-baseline/171-CONTEXT.md
@.planning/phases/171-persistent-product-trust-baseline/171-RESEARCH.md
@app/src/lib/hooks/use-brand-kit.ts
@app/src/components/settings/BrandKitTab.tsx
@app/src/components/settings/ProfileTab.tsx

<interfaces>
From Plan 01 `UserProfileResponse`:
```typescript
export interface UserProfileResponse {
  firstName: string;
  lastName: string;
  email: string;
  bio: string;
  timezone: string;
  avatarUrl: string | null;
}
```

From BrandKitTab save pattern:
```typescript
// saveState: "idle" | "saving" | "saved"
// hasChanges compares form vs query data
// Save disabled when !hasChanges || saveState === "saving"
// On success: toast + saved 2s → idle
// On error: toast + idle immediately
// NO await new Promise(setTimeout...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create use-user-profile hook with tests</name>
  <files>app/src/lib/hooks/use-user-profile.ts, app/src/lib/hooks/use-user-profile.test.tsx</files>
  <behavior>
    - useUserProfile fetches GET /api/user/profile via apiFetch; throws on !ok.
    - useUpdateUserProfile PATCHes partial profile; invalidates ["user-profile"] query key on success.
    - useUploadProfileAvatar POSTs FormData to /api/user/profile/avatar; invalidates query on success.
  </behavior>
  <action>
    Create hook file mirroring `use-brand-kit.ts` structure:
    - Query key: `["user-profile"]`
    - `STALE_TIME.STATIC` for load
    - Typed `UserProfileResponse` export
    - Error messages from `err.error` JSON body

    Tests in `use-user-profile.test.tsx` following `use-billing.test.tsx`:
    - Mock `@/lib/api-client` apiFetch
    - renderHook with QueryClientProvider
    - Assert fetch on mount, mutation calls PATCH, upload calls POST FormData
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/lib/hooks/use-user-profile.test.tsx</automated>
  </verify>
  <done>Hook exports three hooks; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire ProfileTab to API-backed persistence</name>
  <files>app/src/components/settings/ProfileTab.tsx</files>
  <action>
    Per CONTEXT D-01 and D-04 (BrandKit pattern):

    1. Remove `useAppStore` reads for `profile` and `updateProfile`.
    2. Load with `useUserProfile()` — show Skeleton sections while `isPending` (copy BrandKitTab skeleton usage).
    3. Initialize form state from query data via `useEffect` when data arrives (reset when profile refetches after save).
    4. `hasChanges` compares local form vs query data (not Zustand).
    5. `handleSave`:
       - Set saveState saving
       - Call `useUpdateUserProfile.mutateAsync` with { firstName, lastName, bio, timezone }
       - If avatar file changed (track `pendingAvatarFile` ref): call upload mutation first, then PATCH if needed
       - On success: toast success, saveState saved → idle after 2s
       - On error: toast with error.message, saveState idle immediately
       - **Remove** `await new Promise(r => setTimeout(r, 800))` entirely
    6. Email input: `readOnly` + `disabled` styling; value from API email (session-sourced).
    7. Avatar preview: show `avatarUrl` from API; on file select validate 5MB client-side (keep existing check); upload on Save (not inline base64 to DB). Remove FileReader base64 preview path OR keep preview-only locally until save uploads.
    8. Do NOT change i18n keys or onboarding restart behavior.

    Grep for other `updateProfile` consumers before removing store action — if used elsewhere, leave store action but ProfileTab must not use it.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/lib/hooks/use-user-profile.test.tsx && cd app && npm run build</automated>
  </verify>
  <done>ProfileTab uses API hook; no fake delay; email read-only; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProfileTab → profile API | Form fields cross to server on save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-06 | T | ProfileTab save | mitigate | Server rejects email PATCH; client readOnly email |
| T-171-07 | I | Avatar file picker | mitigate | Client 5MB check + server upload validation from Plan 01 |
</threat_model>

<verification>
- Hook tests green.
- ProfileTab contains no `setTimeout(r, 800)` or `updateProfile` from Zustand.
- `rg "setTimeout.*800" app/src/components/settings/ProfileTab.tsx` returns empty.
</verification>

<success_criteria>
- Profile tab loads from API and saves deterministically.
- Fake delay removed from ProfileTab.
</success_criteria>

<output>
After completion, create `.planning/phases/171-persistent-product-trust-baseline/171-02-SUMMARY.md`
</output>
