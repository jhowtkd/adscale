---
phase: 171-persistent-product-trust-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - app/src/app/(dashboard)/settings/settings-tabs.ts
  - app/src/app/(dashboard)/settings/settings-nav.test.ts
  - app/src/lib/store.ts
  - app/tests/unit/settings/settings-regression.test.ts
autonomous: true
requirements:
  - TRUST-03
  - TRUST-05
user_setup: []
must_haves:
  truths:
    - Profile and workspace tabs are enabled and navigable in settings.
    - Integrations tab remains disabled with honest fallback to brandKit default.
    - Default tab remains brandKit (least disruptive).
    - Brand kit, billing, privacy hooks/tests still pass unchanged.
    - Zustand no longer misleads with mock profile/workspace as persistence source.
  artifacts:
    - path: app/src/app/(dashboard)/settings/settings-tabs.ts
      provides: Honest tab enable flags
      contains: "enabled: true"
    - path: app/tests/unit/settings/settings-regression.test.ts
      provides: TRUST-05 regression guard
  key_links:
    - from: settings-tabs.ts
      to: ProfileTab / WorkspaceTab
      via: enabled flags allow navigation
      pattern: "profile.*enabled: true|workspace.*enabled: true"
    - from: settings-regression.test.ts
      to: use-billing / brand-kit paths
      via: import smoke or documented skip
      pattern: "use-billing|brand-kit"
---

<objective>
Enable profile/workspace tabs honestly, clean up misleading Zustand mock state, and guard unaffected settings surfaces.

Purpose: TRUST-03 requires enabled tabs only when APIs work; TRUST-05 prevents regressions to brand kit, billing, and privacy.
Output: Updated tab gating, nav tests, store cleanup, regression test file.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-persistent-product-trust-baseline/171-CONTEXT.md
@.planning/phases/171-persistent-product-trust-baseline/171-RESEARCH.md
@app/src/app/(dashboard)/settings/settings-tabs.ts
@app/src/app/(dashboard)/settings/settings-nav.test.ts
@app/src/lib/store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable tabs and update navigation tests</name>
  <files>app/src/app/(dashboard)/settings/settings-tabs.ts, app/src/app/(dashboard)/settings/settings-nav.test.ts</files>
  <behavior>
    - isSettingsTabEnabled("profile") and ("workspace") return true.
    - isSettingsTabEnabled("integrations") remains false.
    - resolveSettingsTab(null) still returns "brandKit" (default unchanged per CONTEXT D-03).
    - resolveSettingsTab("profile") returns "profile" when enabled.
    - resolveSettingsTab("integrations") falls back to "brandKit".
  </behavior>
  <action>
    Per CONTEXT D-03:
    - Set `profile` and `workspace` to `enabled: true` in `settings-tabs.ts`.
    - Keep `integrations` `enabled: false`.
    - **Do NOT** reorder tabs to change default — `brandKit` stays first enabled tab for default resolution.
    - Update `settings-nav.test.ts` expectations:
      - profile/workspace enabled true
      - resolveSettingsTab("profile") → "profile"
      - resolveSettingsTab("workspace") → "workspace"
      - integrations fallback unchanged
  </action>
  <verify>
    <automated>cd app && npm test -- --run 'src/app/(dashboard)/settings/settings-nav.test.ts'</automated>
  </verify>
  <done>Profile/workspace tabs enabled; nav tests pass; integrations stays disabled; default brandKit.</done>
</task>

<task type="auto">
  <name>Task 2: Demote Zustand mock state and add regression guard</name>
  <files>app/src/lib/store.ts, app/tests/unit/settings/settings-regression.test.ts</files>
  <action>
    Per CONTEXT D-05 (regression guard):

    **store.ts:**
    - Remove `profile` and `workspaceSettings` from initial state OR replace with empty/neutral placeholders not used by settings tabs.
    - Remove `updateProfile` and `updateWorkspaceSettings` actions if no remaining consumers (grep first).
    - Keep `sidebarCollapsed`, `toasts`, `billing`, `user` header slices unchanged.
    - Ensure `partialize` still only persists `sidebarCollapsed`.

    **settings-regression.test.ts** (new):
    - Document TRUST-05 scope: brand kit, team, billing, credit history, plans, privacy untouched.
    - Test 1: Static import `use-brand-kit` exports still exist (smoke).
    - Test 2: Static import billing hooks from `use-billing.ts` still exist.
    - Test 3: Assert `settings-tabs.ts` integrations still `enabled: false`.
    - Test 4: Assert brandKit still `enabled: true`.

    Run existing billing hook tests as regression signal — do NOT modify BrandKitTab, TeamTab, BillingTab, CreditHistoryTab, PlansTab, PrivacyTab source files.

    Update `.planning/phases/171-persistent-product-trust-baseline/171-VALIDATION.md` task statuses to reflect Wave 3 commands (optional metadata only).
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/settings/settings-regression.test.ts src/lib/hooks/use-billing.test.tsx 'src/app/(dashboard)/settings/settings-nav.test.ts'</automated>
  </verify>
  <done>Zustand demoted; regression test passes; billing hooks still green; no changes to other settings tab components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings-tabs → user navigation | Mis-enabled tab implies working feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-10 | S | integrations enabled early | mitigate | Keep integrations false; test asserts |
| T-171-11 | T | Zustand mock reset | mitigate | Remove misleading defaults; API is source of truth |
</threat_model>

<verification>
- Nav tests pass with new enable flags.
- `rg "enabled: false" app/src/app/\(dashboard\)/settings/settings-tabs.ts` still matches integrations only among profile/workspace/integrations trio.
- Billing hook tests pass without modification to billing source.
</verification>

<success_criteria>
- Honest tab gating: profile/workspace on, integrations off.
- Unaffected settings surfaces regression-guarded.
</success_criteria>

<output>
After completion, create `.planning/phases/171-persistent-product-trust-baseline/171-04-SUMMARY.md`
</output>
