---
phase: 59-feedback-informed-regeneration
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/src/server/db/schema.ts
  - app/drizzle/0028_regeneration_correction_brief.sql
  - app/src/server/ai/creative-contract.ts
  - app/src/server/repositories/derivation.ts
  - app/src/server/repositories/derivation.test.ts
  - app/src/server/repositories/feedback.ts
  - app/src/app/api/derivations/[id]/regenerate/route.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/jobs/derivation.test.ts
autonomous: true
requirements:
  - AIR-02
  - AIR-04
user_setup: []
must_haves:
  truths:
    - Regenerate route loads parent creativeContract JSON when present instead of minimal fallback contract.
    - Child derivations persist final promptFeedback and structured regenerationCorrectionBrief JSON.
    - Regenerated children inherit parent creative contract fields for generation job contract resolution.
    - Optional beta feedback category enriches brief without quoting report message.
  artifacts:
    - path: app/drizzle/0028_regeneration_correction_brief.sql
      provides: regeneration_correction_brief jsonb column on derivations
    - path: app/src/app/api/derivations/[id]/regenerate/route.ts
      provides: Brief resolution, user-note merge, child persistence
  key_links:
    - from: app/src/app/api/derivations/[id]/regenerate/route.ts
      to: app/src/server/ai/regeneration-correction-brief.ts
      via: buildRegenerationCorrectionBrief + mergeUserRegenerationNotes
      pattern: buildRegenerationCorrectionBrief
    - from: app/src/server/jobs/derivation.ts
      to: parent creativeContract
      via: resolvedContract inherits parent JSON when parentId set
      pattern: parentDerivation\.creativeContract
---

# Plan 59-02: Regenerate Route Persistence and Parent Contract Inheritance

<objective>
Wire the unified correction brief into the regenerate API, persist structured brief metadata on child derivations, and ensure regeneration jobs inherit the parent's full creative contract.

Purpose: satisfy AIR-02 (contract-safe feedback merge) and AIR-04 (child records correction brief + parent context).

Output: Drizzle migration, repository typings, refactored regenerate route, derivation job contract inheritance.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-feedback-informed-regeneration/59-CONTEXT.md
@.planning/phases/59-feedback-informed-regeneration/59-01-SUMMARY.md

<interfaces>
From Plan 59-01 (regeneration-correction-brief.ts):
```typescript
export function buildRegenerationCorrectionBrief(input: {...}): RegenerationCorrectionBrief;
export function mergeUserRegenerationNotes(machineBrief: string, userText?: string): string;
export function deriveRegenerationPreview(derivation: DerivationRow): {
  primaryReason: string;
  issueBreakdown: { hardFailures; scoreIssues; qaFailed; qaWarnings };
};
```

From app/src/app/api/derivations/[id]/regenerate/route.ts (current — replace resolveRegenerationFeedback):
```typescript
function resolveRegenerationFeedback(original, explicitFeedback?): string | undefined
// explicit feedback currently REPLACES stored suggestion — fix in this plan
```

From app/src/server/repositories/derivation.ts:
```typescript
export interface CreateDerivationInput {
  campaignId: string;
  workspaceId: string;
  parentId?: string;
  feedback?: string;
  // extend with regenerationCorrectionBrief
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regeneration_correction_brief column and repository support</name>
  <files>app/src/server/db/schema.ts, app/drizzle/0028_regeneration_correction_brief.sql, app/src/server/ai/creative-contract.ts, app/src/server/repositories/derivation.ts, app/src/server/repositories/derivation.test.ts</files>
  <action>
    Per D-04 (AIR-04), add Drizzle migration `app/drizzle/0028_regeneration_correction_brief.sql`:

    ```sql
    ALTER TABLE derivations ADD COLUMN IF NOT EXISTS regeneration_correction_brief jsonb;
    ```

    Add `regenerationCorrectionBrief` jsonb column to `derivations` in `schema.ts` typed as `RegenerationCorrectionBrief["structured"] & { promptFeedback: string }` or a dedicated `RegenerationCorrectionBriefRecord` type exported from `creative-contract.ts` (planner discretion: separate type in creative-contract.ts keeps schema imports clean).

    Extend `CreateDerivationInput` with optional `regenerationCorrectionBrief` and `creativeContract` (for child rows that should start with inherited contract).

    Update `createDerivation` insert to persist both fields when provided.

    Add repository test proving `regenerationCorrectionBrief` and `creativeContract` round-trip on create (mirror 57-01 pattern in `derivation.test.ts`).
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/repositories/derivation.test.ts</automated>
  </verify>
  <done>Migration and schema exist; createDerivation accepts and persists correction brief JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor regenerate route with contract load, brief merge, and child persistence</name>
  <files>app/src/server/repositories/feedback.ts, app/src/app/api/derivations/[id]/regenerate/route.ts</files>
  <action>
    Add `getLatestOpenFeedbackReportForDerivation(workspaceId, derivationId)` to `feedback.ts`:
    - Filter `derivationId` match, `workspaceId`, status in (`new`, `reviewing`)
    - Order by `createdAt` desc, limit 1
    - Return category only to caller (never message body into prompts)

    Replace `resolveRegenerationFeedback` with `resolveRegenerationBrief(original, workspaceId, explicitFeedback?)` returning `{ promptFeedback, structured, primaryReason }`:

    1. Resolve `contract`: prefer `original.creativeContract` JSON when present; else build fallback via `resolveCtaSemantics` + derivation fields (current pattern) — per D-02 (AIR-02).

    2. Lookup optional `feedbackCategory` from feedback repository.

    3. Call `buildRegenerationCorrectionBrief` with hardFailures, scoreIssues, qaChecklist, qaIssues, feedbackCategory, contract, parentDerivationId=original.id.

    4. Apply `mergeUserRegenerationNotes(brief.promptFeedback, explicitFeedback)` — fixes CONTEXT gap where explicit feedback replaced blocking context.

    5. If parent has stored `regenerationSuggestion` and no hard failures/score/qa issues, still prefer fresh brief builder output for consistency (stored suggestion may be stale); only use stored suggestion text as `modelSuggestion` hint inside builder when reconstructing.

    Update `createDerivation` call to pass:
    - `feedback`: final merged `promptFeedback`
    - `regenerationCorrectionBrief`: structured brief + promptFeedback snapshot
    - `creativeContract`: parent's `creativeContract` when present (inherit full contract on child row)

    Keep credit idempotency key using final feedback string. Keep 429 active-child guard unchanged.

    Do NOT expose full brief internals in feedback diagnostics APIs.
  </action>
  <verify>
    <automated>cd app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Regenerate route builds merged brief, inherits parent contract, persists brief on child.</done>
</task>

<task type="auto">
  <name>Task 3: Inherit parent creativeContract in derivation job for regenerations</name>
  <files>app/src/server/jobs/derivation.ts, app/src/server/jobs/derivation.test.ts</files>
  <action>
    Per D-04 (AIR-04), when `derivation.parentId` is set:

    1. If child row has `creativeContract` JSON (set by regenerate route), use it as the base for `resolvedContract` instead of rebuilding minimal contract from campaign fields only.

    2. Overlay job event fields (`generationMode`, `format`, `ctaText`, `styleAssetId`) only when explicitly provided and non-null — do not wipe parent brand/product/offer/sourcePackage/factualSourceRules.

    3. For restyling children, preserve parent `baseAssetId`/`styleAssetId` from inherited contract when job does not override.

    Add/update `derivation.test.ts` case: child with `parentId`, inherited `creativeContract`, and `feedback` → job persists same contract fields on output provenance (expect client/product/offer from parent contract in `updateDerivationPromptProvenance` call).

    Do not change approval/export gate behavior.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/jobs/derivation.test.ts</automated>
  </verify>
  <done>Regeneration children keep parent contract context through the derivation job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /regenerate body | Untrusted user feedback string |
| feedback_reports | Untrusted user message — category only crosses boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Tampering | regenerate route | mitigate | mergeUserRegenerationNotes; contract from parent creativeContract JSON |
| T-59-05 | Information disclosure | regenerationCorrectionBrief | accept | Owner/dev inspect via derivation row only; not exposed in beta diagnostics |
| T-59-06 | Spoofing | feedback category lookup | mitigate | Scope query by workspaceId + derivationId |
</threat_model>

<verification>
- Repository test passes for new jsonb column
- Derivation job test proves parent contract inheritance
- Build succeeds after route refactor
</verification>

<success_criteria>
- AIR-02: User feedback cannot override contract preservation lines
- AIR-04: Child derivations store correction brief JSON and inherit parent contract
- Regenerate route no longer replaces gate context with raw user text
</success_criteria>

<output>
After completion, create `.planning/phases/59-feedback-informed-regeneration/59-02-SUMMARY.md`
</output>
