---
phase: 59-feedback-informed-regeneration
plan: "04"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - app/src/app/api/derivations/[id]/regenerate/route.test.ts
  - app/tests/unit/creative-score.test.ts
  - app/tests/unit/ai/regeneration-correction-brief.test.ts
autonomous: true
requirements:
  - AIR-05
user_setup: []
must_haves:
  truths:
    - Route tests cover explicit feedback merge, stored suggestion, hard-failure reconstruction, QA/score in brief, and feedback category context.
    - Bounded feedback size (2001 chars) still returns 400.
    - Unit tests prove brief delegation from buildHardFailureRegenerationSuggestion.
  artifacts:
    - path: app/src/app/api/derivations/[id]/regenerate/route.test.ts
      provides: Five regeneration input path integration tests
  key_links:
    - from: app/src/app/api/derivations/[id]/regenerate/route.test.ts
      to: mergeUserRegenerationNotes
      via: explicit feedback merges with machine brief
      pattern: Additional notes:
---

# Plan 59-04: Regeneration Route and Brief Test Coverage

<objective>
Extend route and unit tests to cover all regeneration input paths specified in AIR-05, including feedback-informed context and safe user-feedback merge.

Purpose: satisfy AIR-05 (route tests for explicit feedback, stored suggestion, hard-failure reconstruction, QA/score context, feedback category).

Output: Comprehensive `route.test.ts` coverage and aligned creative-score unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-feedback-informed-regeneration/59-CONTEXT.md
@.planning/phases/59-feedback-informed-regeneration/59-02-SUMMARY.md

<interfaces>
Existing route.test.ts cases to update (not replace):
- rejects feedback over bounded size (2001 → 400)
- rejects active child (429)
- creates queued child
- uses parent regenerationSuggestion when feedback omitted — UPDATE: expect unified brief shape
- keeps explicit user feedback — UPDATE: expect merge with "Additional notes:" not full replace

New mocks needed:
```typescript
vi.mock("@/server/repositories/feedback", () => ({
  getLatestOpenFeedbackReportForDerivation: vi.fn(),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend regenerate route integration tests</name>
  <files>app/src/app/api/derivations/[id]/regenerate/route.test.ts</files>
  <behavior>
    - Explicit user feedback merges with machine brief (contains blocking sections + Additional notes).
    - Omitted feedback uses built brief from parent quality fields.
    - Hard-failure reconstruction when suggestion empty but hardFailures present.
    - Brief includes QA checklist failures and scoreIssues when on parent row.
    - feedbackCategory from mocked repository adds Feedback context line without message body.
    - 2001 char feedback still 400.
    - createDerivation receives regenerationCorrectionBrief jsonb.
  </behavior>
  <action>
    Per D-05 (AIR-05), extend `route.test.ts`:

    1. Mock `@/server/repositories/feedback` with `getLatestOpenFeedbackReportForDerivation`.

    2. **Update** "keeps explicit user feedback" test: expect `feedback` to contain both `Hard failures:` (or `Score issues:`) AND `Additional notes: warmer palette only` when parent has hard failures + user provides text.

    3. **Update** "uses parent regenerationSuggestion when feedback omitted" to pass parent with `hardFailures`, `scoreIssues`, `qaChecklist` — expect `createDerivation` feedback to include `QA issues:` and `Score issues:` sections (not only stored suggestion string).

    4. **Add** "reconstructs brief from hardFailures when suggestion empty":
       - Parent: hardFailures populated, regenerationSuggestion null
       - Expect 201 and feedback containing hard failure codes + preservation tail

    5. **Add** "includes feedback category context when open report exists":
       - Mock returns `{ category: "generation" }`
       - Expect feedback contains `Feedback context (generation)` and does NOT contain report message

    6. **Add** "persists regenerationCorrectionBrief on child":
       - Expect `createDerivation` called with `regenerationCorrectionBrief` object containing `sources` array and `contractSnapshot`

    7. **Add** "inherits parent creativeContract on child":
       - Parent has full `creativeContract` JSON with client/product/offer
       - Expect `createDerivation` `creativeContract` matches parent

    Keep existing 429 and 2001-char tests unchanged.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/app/api/derivations/\\[id\\]/regenerate/route.test.ts</automated>
  </verify>
  <done>Route tests cover all five AIR-05 paths plus persistence and merge behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align creative-score and brief unit tests with delegation</name>
  <files>app/tests/unit/creative-score.test.ts, app/tests/unit/ai/regeneration-correction-brief.test.ts</files>
  <action>
    Update `creative-score.test.ts` `buildHardFailureRegenerationSuggestion` tests if output format changed (labeled sections from brief builder). Assertions should still expect:
    - Hard failure codes and messages
    - CTA/format/mode preservation tail
    - Restyling asset IDs in tail
    - Score-only delegation when no hard failures

    Add brief unit test case: QA `failed` items appear in `QA issues:` section; warnings capped in promptFeedback but visible in primaryReason/issue breakdown.

    Add brief unit test: `feedbackCategory: "generation"` produces category line without user message.

    Ensure all tests use CreativeContract fixtures aligned with `prompt-builder.test-fixtures.ts` where applicable.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/creative-score.test.ts tests/unit/ai/regeneration-correction-brief.test.ts</automated>
  </verify>
  <done>Unit tests pass and document brief section format contract for downstream phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route tests | Verify server merge behavior is enforced regardless of client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Tampering | explicit feedback path | mitigate | Test asserts Additional notes merge, not replace |
| T-59-10 | Information disclosure | feedback category test | mitigate | Assert message body absent from promptFeedback |
</threat_model>

<verification>
- Full regenerate route test file passes
- creative-score and brief unit tests pass
- No regression on 2001-char and 429 cases
</verification>

<success_criteria>
- AIR-05: All regeneration input paths covered by automated tests
- User feedback merge behavior regression-locked
- Child brief persistence regression-locked
</success_criteria>

<output>
After completion, create `.planning/phases/59-feedback-informed-regeneration/59-04-SUMMARY.md`
</output>
