---
phase: 60-quality-fixtures-and-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/quality-fixtures.ts
  - app/tests/unit/ai/quality-fixtures.test.ts
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - "Repository exports a typed catalog of six synthetic quality failure fixtures"
    - "Each fixture uses fictional Acme Demo data — no real customer assets or Desktop paths"
    - "Every fixture declares contract, synthetic model outputs, and expected gate outcomes"
  artifacts:
    - path: app/src/server/ai/quality-fixtures.ts
      provides: QUALITY_FIXTURES catalog and QualityFailureMode types
      exports:
        - QUALITY_FIXTURES
        - QualityFailureMode
        - QualityFixture
    - path: app/tests/unit/ai/quality-fixtures.test.ts
      provides: Catalog integrity tests
      contains: QUALITY_FIXTURES
  key_links:
    - from: app/tests/unit/ai/quality-fixtures.test.ts
      to: app/src/server/ai/quality-fixtures.ts
      via: import QUALITY_FIXTURES
      pattern: from \"@/server/ai/quality-fixtures\"
    - from: app/src/server/ai/quality-fixtures.ts
      to: app/src/server/ai/creative-contract.ts
      via: CreativeContract per fixture
      pattern: generationMode
---

<objective>
Create the centralized synthetic quality fixture catalog for all six known failure modes.

Purpose: Give downstream regression tests a single, CI-safe source of truth for contracts and expected quality outcomes (FIX-01).
Output: `quality-fixtures.ts` + integrity unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-quality-fixtures-and-verification/60-CONTEXT.md
@app/src/server/ai/prompt-builder.test-fixtures.ts
@app/src/server/ai/creative-contract.ts
@app/src/server/ai/creative-quality-taxonomy.ts
@app/tests/unit/ai/creative-quality-gate.test.ts

<interfaces>
From app/src/server/ai/creative-contract.ts:
```typescript
export interface CreativeContract {
  generationMode: "art_variation" | "format_adaptation" | "restyling";
  targetFormat: string;
  ctaSemantics: CtaSemantics;
  baseAssetId: string | null;
  styleAssetId: string | null;
  client: string | null;
  product: string | null;
  offer: string | null;
  constraints: string | null;
  sourcePackage?: "campaign_asset" | "approved_derivation";
}
```

From app/src/server/ai/creative-quality-gate.ts:
```typescript
export type CreativeHardFailureCode =
  | "cta_drift" | "wrong_brand" | "unsupported_offer"
  | "copied_style_reference_facts" | "cropped_critical_content"
  | "unreadable_required_text" | "invalid_format_layout";
export type QualityVerdict = "acceptable" | "improvable" | "invalid";
```

From prompt-builder.test-fixtures.ts — reuse fictional values (Acme Corp, Widget Pro, Auditoria gratuita) per D-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define QualityFixture types and catalog scaffold (D-01, D-02)</name>
  <files>app/src/server/ai/quality-fixtures.ts, app/tests/unit/ai/quality-fixtures.test.ts</files>
  <behavior>
    - QUALITY_FIXTURES exports exactly 6 entries with unique ids
    - failureMode union covers wrong_cta, cropped_text_logo, style_reference_contamination, poor_format_adaptation, weak_preservation, low_legibility
    - Each fixture has contract, rawQaModelOutput, expectedHardFailureCodes (non-empty for invalid verdicts), expectedVerdict
    - No fixture references /Users/, Desktop, or real customer brand names beyond fictional Acme Demo set
  </behavior>
  <action>
    Create `app/src/server/ai/quality-fixtures.ts` per D-01 through D-04.

    Export:
    - `QualityFailureMode` union (six values)
    - `QualityFixture` interface with fields: `id`, `failureMode`, `label`, `contract`, `rawQaModelOutput`, optional `rawScoreModelOutput`, `expectedHardFailureCodes`, `expectedVerdict`, optional `expectedRegenerationSnippets: string[]`
    - `QUALITY_FIXTURES: QualityFixture[]` — one primary fixture per failure mode

    Map failure modes to contracts (per D-02):
    - `wrong_cta`: art_variation, explicit CTA "Comprar agora", QA ctaOffer failed with CTA drift note
    - `cropped_text_logo`: art_variation, informationPreservation failed with crop note
    - `style_reference_contamination`: restyling + styleAssetId, styleFidelity failed
    - `poor_format_adaptation`: format_adaptation 9:16, formatFit failed with blur-band note
    - `weak_preservation`: art_variation, briefMatch or informationPreservation failed with product/offer loss (pick path that yields hard failure per Phase 58 rules)
    - `low_legibility`: art_variation, legibility failed with illegible note

    `rawQaModelOutput` shape must be consumable by `normalizeCreativeQaResult` (status + checklist + optional issues/suggestions). Include `styleFidelity` only on restyling fixture.

    Add `app/tests/unit/ai/quality-fixtures.test.ts` with catalog integrity tests (count, unique ids, required fields, fictional brand check).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts</automated>
  </verify>
  <done>Six-fixture catalog exists with types exported and integrity tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Populate expected regeneration snippets per fixture (D-03)</name>
  <files>app/src/server/ai/quality-fixtures.ts, app/tests/unit/ai/quality-fixtures.test.ts</files>
  <action>
    For each fixture, set `expectedRegenerationSnippets` to strings that `buildRegenerationCorrectionBrief` should include when given the fixture's hard failures and contract — e.g. hard-failure code labels, preservation tail fragments (`Preserve the exact CTA`, target format, generation mode).

    Extend integrity tests to assert every `invalid` verdict fixture has at least one expected hard-failure code and at least one regeneration snippet. `improvable` fixtures may have empty hard-failure lists if used for advisory-only cases (prefer all six as `invalid` with hard failures for clarity).

    Optionally add `rawScoreModelOutput` on 2–3 fixtures where score-issue promotion is relevant (e.g. weak_preservation with brand mismatch in scoreIssues) — not required on all six.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts</automated>
  </verify>
  <done>All fixtures include gate expectations and regeneration snippet hints for pipeline tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data → git repo | Synthetic fixtures must not embed secrets, PII, or private creative paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Information Disclosure | quality-fixtures.ts | mitigate | Use fictional Acme Demo copy only; grep guard in integrity test rejects `/Users/` and `Desktop` |
| T-60-02 | Tampering | QUALITY_FIXTURES | accept | Test-only catalog; production code does not consume fixtures at runtime |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts` passes
- `QUALITY_FIXTURES.length === 6` with all failure modes represented
</verification>

<success_criteria>
- FIX-01 satisfied: synthetic fixture set exists for all six known failure modes
- Catalog is importable by downstream regression plans without OpenAI or image bytes
</success_criteria>

<output>
After completion, create `.planning/phases/60-quality-fixtures-and-verification/60-01-SUMMARY.md`
</output>
