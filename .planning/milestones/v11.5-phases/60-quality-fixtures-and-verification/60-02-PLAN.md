---
phase: 60-quality-fixtures-and-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "60-01"
files_modified:
  - app/tests/unit/ai/quality-prompt-regression.test.ts
  - app/src/server/ai/prompt-builder.test-fixtures.ts
autonomous: true
requirements:
  - FIX-02
must_haves:
  truths:
    - "Prompt regression tests import fixture contracts for all three derivation modes"
    - "Art variation, format adaptation, and restyling each have fixture-linked invariants or compact snapshots"
    - "Failure-mode contracts produce prompts that reinforce the non-negotiable rules for that mode"
  artifacts:
    - path: app/tests/unit/ai/quality-prompt-regression.test.ts
      provides: Fixture-linked prompt contract regression matrix
      contains: QUALITY_FIXTURES
  key_links:
    - from: app/tests/unit/ai/quality-prompt-regression.test.ts
      to: app/src/server/ai/quality-fixtures.ts
      via: fixture.contract → buildDerivationPrompt
      pattern: buildDerivationPrompt
    - from: app/tests/unit/ai/quality-prompt-regression.test.ts
      to: app/src/server/ai/prompt-builder.ts
      via: extractPrompt*Section helpers
      pattern: extractPromptHardRulesSection|extractPromptModeSection|extractPromptRestylingFactualSourceSection
---

<objective>
Add fixture-linked prompt contract regression tests covering art variation, format adaptation, and restyling with compact snapshots.

Purpose: Prove prompt-builder still encodes contract rules for known failure-mode contexts (FIX-02).
Output: `quality-prompt-regression.test.ts` matrix.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-quality-fixtures-and-verification/60-CONTEXT.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-02-SUMMARY.md
@app/src/server/ai/quality-fixtures.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/prompt-builder.test.ts
@app/src/server/ai/prompt-builder.test-fixtures.ts

<interfaces>
From prompt-builder.ts (snapshot helpers — import if exported, or duplicate minimal extractors matching prompt-builder.test.ts):
```typescript
export function buildDerivationPrompt(config: DerivationPromptConfig): string;
export function extractPromptHardRulesSection(prompt: string): string;
export function extractPromptModeSection(prompt: string): string;
export function extractPromptRestylingFactualSourceSection(prompt: string): string;
```

From prompt-builder.test-fixtures.ts:
```typescript
export function derivationConfigFromContract(contract: CreativeContract, overrides?): DerivationPromptConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture-linked prompt matrix for all modes (D-05, D-06)</name>
  <files>app/tests/unit/ai/quality-prompt-regression.test.ts, app/src/server/ai/prompt-builder.test-fixtures.ts</files>
  <action>
    Create `app/tests/unit/ai/quality-prompt-regression.test.ts`.

    For each `QUALITY_FIXTURES` entry:
    - Build prompt via `buildDerivationPrompt(derivationConfigFromContract(fixture.contract))`
    - Assert mode-specific invariants:
      - All modes: hard rules mention target format; CTA rules match contract semantics
      - `format_adaptation` fixtures: native layout, no blur bands / poster padding language
      - `restyling` fixtures: factual-source section separates base vs style reference
      - `wrong_cta` fixture: literal CTA text appears in hard rules when explicit

    Group tests with `describe.each` or parameterized `it` by `fixture.id`.

    Extend `prompt-builder.test-fixtures.ts` only if a fixture contract needs a helper variant (e.g. approved_derivation format package) — keep diff minimal.
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts</automated>
  </verify>
  <done>Every fixture produces a prompt passing mode-specific invariant assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Compact snapshots for one fixture per mode (D-07)</name>
  <files>app/tests/unit/ai/quality-prompt-regression.test.ts</files>
  <action>
    Add at least three `toMatchInlineSnapshot` tests on compact sections only (D-07):
    - One art_variation fixture → `extractPromptHardRulesSection`
    - One format_adaptation fixture → `extractPromptModeSection`
    - One restyling fixture → `extractPromptRestylingFactualSourceSection`

    Pick fixtures from `QUALITY_FIXTURES` by `contract.generationMode`. Do not snapshot full prompts.

  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts src/server/ai/prompt-builder.test.ts</automated>
  </verify>
  <done>FIX-02: automated prompt contract snapshots exist for art variation, format adaptation, and restyling via fixture-linked tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test prompts | Contain fictional campaign data only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Tampering | prompt snapshots | accept | Snapshots are dev regression aids; compact sections limit churn |
</threat_model>

<verification>
- `cd app && npm test -- tests/unit/ai/quality-prompt-regression.test.ts` passes
- All three generation modes covered in snapshot or invariant blocks
</verification>

<success_criteria>
- FIX-02 satisfied: fixture-linked prompt regression for all derivation modes
- No full-prompt snapshots added
</success_criteria>

<output>
After completion, create `.planning/phases/60-quality-fixtures-and-verification/60-02-SUMMARY.md`
</output>
