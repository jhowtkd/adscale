---
phase: 60-quality-fixtures-and-verification
plan: "04"
type: execute
wave: 3
depends_on:
  - "60-02"
  - "60-03"
files_modified:
  - .planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md
  - .planning/phases/60-quality-fixtures-and-verification/60-LIMITATIONS.md
  - .planning/phases/60-quality-fixtures-and-verification/60-VALIDATION.md
autonomous: true
requirements:
  - FIX-04
  - FIX-05
must_haves:
  truths:
    - "Manual verification guide documents full quality loop from prompt to regenerated child"
    - "Residual limitations explicitly name text rendering, consistency, and composition gaps"
    - "Phase validation doc lists automated commands and final build gate"
  artifacts:
    - path: .planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md
      provides: Manual quality loop verification guide
      contains: regeneration
    - path: .planning/phases/60-quality-fixtures-and-verification/60-LIMITATIONS.md
      provides: Model-dependent residual limitations
      contains: text rendering
    - path: .planning/phases/60-quality-fixtures-and-verification/60-VALIDATION.md
      provides: Per-plan test commands and sign-off checklist
      contains: nyquist_compliant
  key_links:
    - from: .planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md
      to: app/tests/unit/ai/quality-fixture-pipeline.test.ts
      via: referenced automated pre-check commands
      pattern: quality-fixture-pipeline
---

<objective>
Document manual verification path and residual model limitations; finalize phase validation contract and full test/lint/build gate.

Purpose: Enable owner/developer handoff after v11.5 quality work (FIX-04, FIX-05).
Output: HANDOFF.md, LIMITATIONS.md, VALIDATION.md + green full suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-quality-fixtures-and-verification/60-CONTEXT.md
@.planning/phases/56-verification-and-privacy-audit/56-HANDOFF.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-VALIDATION.md
@.planning/phases/59-feedback-informed-regeneration/59-VERIFICATION.md
@app/src/app/api/derivations/[id]/regenerate/route.ts
@app/src/components/workspace/RegenerateFeedbackDialog.tsx

</context>

<tasks>

<task type="auto">
  <name>Task 1: Write manual verification handoff (D-11, D-12, D-13, D-14)</name>
  <files>.planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md</files>
  <action>
    Create `60-HANDOFF.md` following Phase 56 handoff structure.

    Required sections:
    1. **Purpose** — who this is for (platform owner / developer validating beta quality)
    2. **Automated pre-check** — run fixture suite:
       `cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts tests/unit/ai/quality-prompt-regression.test.ts tests/unit/ai/quality-fixture-pipeline.test.ts`
    3. **Manual quality loop** — numbered steps (D-12):
       - Pick a derivation with `qualityVerdict` improvable or invalid (or generate one in dev)
       - Inspect DB/API fields: `creativeContract`, `promptProvenance`, `inputPrompt`, `prompt` (revised)
       - Review score: `qualityScore`, `scoreBreakdown`, `scoreIssues`, `scoreStatus`
       - Review QA: `qaChecklist`, `qaIssues`, `qaStatus` (or trigger `POST /api/derivations/[id]/qa` on approved output)
       - Confirm gate: `qualityVerdict`, `hardFailures` vs `polishSuggestions`, localized code titles in review UI
       - Read regeneration: `regenerationSuggestion`, open regenerate dialog → verify primary reason summary (Phase 59)
       - Confirm regeneration: submit → child has `parentId`, `feedback`, `regenerationCorrectionBrief`, inherited contract
    4. **What to record** — note failure mode category, fixture id if similar, whether regeneration improved output
    5. **Out of scope** — beta users cannot see full prompts; no automatic retry loops

    Reference real UI routes/components by path. Use dev/staging URLs as placeholders (`https://app.example.com`) if production URL unknown.
  </action>
  <verify>
    <automated>test -f .planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md && rg -n "regeneration|hard failure|creativeContract|qualityVerdict" .planning/phases/60-quality-fixtures-and-verification/60-HANDOFF.md</automated>
  </verify>
  <done>FIX-04: handoff explains complete manual quality loop evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Document residual limitations (D-15, D-16)</name>
  <files>.planning/phases/60-quality-fixtures-and-verification/60-LIMITATIONS.md</files>
  <action>
    Create `60-LIMITATIONS.md` per D-15/D-16.

    Required content:
    - **Text rendering** — model may misspell, truncate, or stylize CTA/headline despite contract rules; fixtures catch prompt/regression wiring not pixel-perfect text
    - **Visual consistency** — regenerations may drift color/layout; scoring is advisory except hard failures
    - **Precise composition** — format adaptation may still produce near-miss layouts; human review required for edge cases
    - **Vision model variability** — score/QA JSON can vary run-to-run; normalization + gate exist to bound impact
    - **What fixtures guarantee** — contract prompts, taxonomy classification, brief assembly, approval blocks on hard failures
    - **What still needs human review** — final pixel check before export, locale-specific copy nuance

    Cross-link to `60-HANDOFF.md` and OpenAI image limitations (general, no vendor marketing).
  </action>
  <verify>
    <automated>test -f .planning/phases/60-quality-fixtures-and-verification/60-LIMITATIONS.md && rg -n "text rendering|visual consistency|composition" .planning/phases/60-quality-fixtures-and-verification/60-LIMITATIONS.md</automated>
  </verify>
  <done>FIX-05: residual model-dependent limitations documented explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Phase validation doc + full suite gate (D-17)</name>
  <files>.planning/phases/60-quality-fixtures-and-verification/60-VALIDATION.md</files>
  <action>
    Create `60-VALIDATION.md` mirroring Phase 57 validation format with frontmatter `nyquist_compliant: true`.

    Include:
    - Per-plan focused commands for 60-01, 60-02, 60-03
    - Wave-final command bundling all fixture tests + existing quality unit tests:
      `cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts tests/unit/ai/quality-prompt-regression.test.ts tests/unit/ai/quality-fixture-pipeline.test.ts tests/unit/ai/creative-quality-gate.test.ts tests/unit/ai/regeneration-correction-brief.test.ts src/server/ai/prompt-builder.test.ts`
    - Final gate: `cd app && npm run lint && npm run build`
    - Manual-only: reference 60-HANDOFF.md for visual spot-check (documented, not blocking automation)

    Run the wave-final test command and lint/build; fix any failures in test code from plans 01–03 if needed (doc-only plan should not require production code changes unless tests fail).
  </action>
  <verify>
    <automated>cd app && npm test -- tests/unit/ai/quality-fixtures.test.ts tests/unit/ai/quality-prompt-regression.test.ts tests/unit/ai/quality-fixture-pipeline.test.ts && cd app && npm run lint && npm run build</automated>
  </verify>
  <done>Validation doc exists; automated suite and build pass (or failures documented in SUMMARY if pre-existing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handoff doc → operators | Documents owner-only inspection paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Information Disclosure | 60-HANDOFF.md | mitigate | Do not instruct exposing prompts to beta users; owner/dev paths only per D-13 |
</threat_model>

<verification>
- HANDOFF, LIMITATIONS, VALIDATION files exist with required keywords
- Full fixture test bundle passes
- `npm run lint` and `npm run build` pass in `app/`
</verification>

<success_criteria>
- FIX-04 and FIX-05 satisfied
- v11.5 quality milestone has executable verification contract
</success_criteria>

<output>
After completion, create `.planning/phases/60-quality-fixtures-and-verification/60-04-SUMMARY.md`
</output>
