---
phase: 57-creative-contract-and-prompt-provenance
plan: "02"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - app/src/server/ai/prompt-builder.ts
  - app/src/server/ai/prompt-builder.test.ts
  - app/tests/unit/prompt-builder.test.ts
  - app/src/server/jobs/derivation.test.ts
autonomous: true
requirements:
  - AIC-02
  - AIC-03
  - AIC-04
user_setup: []
must_haves:
  truths:
    - Art variation prompts preserve required campaign/source information while the selected creative level changes composition.
    - Format adaptation prompts require native target layouts and reject padding, cropped posters, blurred bands, and letterboxing.
    - Restyling prompts treat the base image as factual source and the style reference as visual language only.
    - Literal CTA, inherited CTA, target format, brand/client, product, offer, and source package rules remain stronger than strategy, feedback, references, and brand memory.
  artifacts:
    - Prompt-builder tests cover all three generation modes with comparable fixtures.
    - Focused snapshots cover compact hard-rule/source-package sections rather than entire long prompts.
    - Final focused test bundle and build are recorded in the plan summary.
  key_links:
    - Prompt tests use the persisted contract/source-package shape from Plan 57-01.
    - Prompt hard rules remain aligned with `CreativeContract` and `promptProvenance` semantics.
---

# Plan 57-02: Prompt Contract Regression Coverage

<objective>
Normalize prompt-builder regression coverage so art variation, format adaptation, and restyling all prove their non-negotiable contract rules through invariants plus compact snapshots.

Purpose: make prompt regressions visible before they create bad beta outputs, without turning full long prompts into brittle snapshots.

Output: comparable prompt fixtures/tests for AIC-02, AIC-03, and AIC-04, plus final focused validation for Phase 57.
</objective>

<execution_context>
@/Users/jhonatan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jhonatan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-CONTEXT.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-RESEARCH.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-VALIDATION.md
@.planning/phases/57-creative-contract-and-prompt-provenance/57-01-SUMMARY.md
@app/AGENTS.md
@app/src/server/ai/creative-contract.ts
@app/src/server/ai/prompt-builder.ts
@app/src/server/ai/prompt-builder.test.ts
@app/tests/unit/prompt-builder.test.ts
@app/src/server/jobs/derivation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prompt test fixtures and compact snapshot helpers</name>
  <files>app/src/server/ai/prompt-builder.ts, app/src/server/ai/prompt-builder.test.ts, app/tests/unit/prompt-builder.test.ts</files>
  <action>
    Create a consistent test style for prompt contract regression coverage.

    Preferred implementation:
    - Add local test helpers that build representative `CreativeContract` objects for art variation, format adaptation, and restyling.
    - Add a local helper that extracts compact sections from a prompt, such as hard rules, mode section, source-package section, and restyling factual-source section.
    - Use `toMatchInlineSnapshot` or targeted snapshots only for those compact sections.
    - Keep broad behavior checks as invariant assertions (`toContain`, ordering checks, regex checks).

    Avoid snapshotting the full prompt. Do not introduce a new test framework.
  </action>
  <verify>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts</verify>
  <done>
    Prompt tests have reusable mode fixtures and compact snapshot helpers without full-prompt snapshots.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cover art variation contract invariants</name>
  <files>app/src/server/ai/prompt-builder.test.ts, app/tests/unit/prompt-builder.test.ts</files>
  <action>
    Add or strengthen art variation tests for AIC-02.

    Required assertions:
    - The prompt includes the selected creativity level and its operational rules.
    - The prompt requires visible headline, offer, CTA, product/service, logo if present, legal/small-print if present, and key visual subject to be preserved.
    - The prompt permits composition/background/layout variation but not brand/product/offer/CTA drift.
    - Literal CTA rules appear before plan CTA recommendations, brand memory, client references, revision feedback, or other flexible guidance.
    - Inherited CTA means preserve the source CTA, not "no CTA required".
    - Brand/client, product, offer, constraints, and target format appear as non-negotiable or preservation context when provided.

    Add one compact snapshot for the art variation hard-rule section using the finalized contract shape from Plan 57-01.
  </action>
  <verify>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts</verify>
  <done>
    Tests fail if art variation no longer preserves required facts while changing composition according to creative level.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cover format adaptation native-layout invariants</name>
  <files>app/src/server/ai/prompt-builder.test.ts, app/tests/unit/prompt-builder.test.ts</files>
  <action>
    Add or strengthen format adaptation tests for AIC-03.

    Required assertions:
    - The prompt treats the source ad as modules: headline, photo/subject, offer/proof, CTA, logo, badges, legal copy, and decorative background.
    - The prompt requires native target-format reconstruction, not a resized poster.
    - The prompt explicitly rejects blank bands, blurred padding, letterboxing, cropped poster behavior, stretched edge filler, pasted-poster composition, and crowded clusters.
    - The prompt preserves all factual/critical content and keeps important modules inside safe areas.
    - `9:16` includes top/middle/lower vertical zones.
    - `4:5` includes portrait-feed spacing and separated CTA/logo area.
    - `approved_derivation` source package says the reference image is the approved winning creative and must not return to the original campaign asset.

    Add one compact snapshot for the format adaptation mode/source-package section.
  </action>
  <verify>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts</verify>
  <done>
    Tests fail if format adaptation prompts allow padding, poster resizing, letterboxing, unsafe crop, or source-package drift.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cover restyling factual-source invariants</name>
  <files>app/src/server/ai/prompt-builder.test.ts, app/tests/unit/prompt-builder.test.ts</files>
  <action>
    Add or strengthen restyling tests for AIC-04.

    Required assertions:
    - The prompt says the base image is the only factual source for brand name, product name, offer, CTA, price, course name, logo, and source facts.
    - The prompt says style reference contributes only visual language: color, typography style, layout rhythm, composition, mood, and decorative language.
    - The prompt forbids copying factual claims, text, prices, offers, brand names, or CTAs from the style reference.
    - The prompt preserves inherited CTA from the base image and never emits "no CTA required" for null restyling CTA.
    - The test fixture includes `baseAssetId` and `styleAssetId` from the persisted contract shape.

    Add one compact snapshot for the restyling factual-source section.
  </action>
  <verify>cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts</verify>
  <done>
    Tests fail if restyling can replace base-image facts with style-reference facts.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run Phase 57 focused validation and build</name>
  <files>app/src/server/repositories/derivation.test.ts, app/src/server/jobs/derivation.test.ts, app/src/server/ai/prompt-builder.test.ts, app/tests/unit/prompt-builder.test.ts</files>
  <action>
    Run the full Phase 57 validation bundle after Plans 57-01 and 57-02 are complete.

    Required commands:
    - `cd app && npm test -- src/server/repositories/derivation.test.ts src/server/jobs/derivation.test.ts src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts`
    - `cd app && npm run build`

    Fix failures caused by Phase 57 changes. If unrelated pre-existing failures appear, isolate and record exact evidence in the summary.
  </action>
  <verify>cd app && npm test -- src/server/repositories/derivation.test.ts src/server/jobs/derivation.test.ts src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts && npm run build</verify>
  <done>
    The Phase 57 focused test bundle and build pass, or unrelated failures are documented precisely.
  </done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `cd app && npm test -- src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts`
- [ ] `cd app && npm test -- src/server/repositories/derivation.test.ts src/server/jobs/derivation.test.ts src/server/ai/prompt-builder.test.ts tests/unit/prompt-builder.test.ts`
- [ ] `cd app && npm run build`
- [ ] Prompt snapshots are compact sections, not full long prompts.
- [ ] No prompt debug fields are exposed to beta users.
</verification>

<success_criteria>
- AIC-02 is covered by art variation prompt invariants and compact snapshot coverage.
- AIC-03 is covered by format adaptation prompt invariants and compact snapshot coverage.
- AIC-04 is covered by restyling factual-source invariants and compact snapshot coverage.
- Phase 57 has enough automated coverage for later score/QA/regeneration phases to trust the stored contract.
</success_criteria>

<output>
After completion, create `.planning/phases/57-creative-contract-and-prompt-provenance/57-02-SUMMARY.md`.
</output>
