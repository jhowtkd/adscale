---
phase: 58-scoring-and-qa-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/creative-quality-taxonomy.ts
  - app/src/server/ai/creative-score.ts
  - app/src/server/ai/creative-qa.ts
  - app/src/server/ai/creative-qa.test.ts
  - app/tests/unit/ai/creative-score.test.ts
autonomous: true
requirements:
  - AIQ-01
  - AIQ-02
must_haves:
  truths:
    - "Score and QA reference the same canonical quality dimension IDs"
    - "Malformed or missing score JSON cannot silently produce 70 defaults"
    - "QA normalization rejects ready status when all core criteria used fallback notes"
    - "Unknown QA checklist keys are ignored, not merged into canonical criteria"
  artifacts:
    - path: app/src/server/ai/creative-quality-taxonomy.ts
      provides: Canonical dimension IDs, score aliases, display order
      exports:
        - CREATIVE_QA_CRITERIA
        - SCORE_BREAKDOWN_ALIASES
        - mapScoreBreakdownKeyToCriterion
    - path: app/src/server/ai/creative-score.ts
      provides: normalizeCreativeScoreResult and taxonomy-aligned scoring prompt
      contains: normalizeCreativeScoreResult
    - path: app/src/server/ai/creative-qa.ts
      provides: Taxonomy-imported criterion types and tightened normalization
      contains: normalizeCreativeQaResult
  key_links:
    - from: app/src/server/ai/creative-qa.ts
      to: app/src/server/ai/creative-quality-taxonomy.ts
      via: import CREATIVE_QA_CRITERIA
      pattern: from \"./creative-quality-taxonomy\"
    - from: app/src/server/ai/creative-score.ts
      to: app/src/server/ai/creative-quality-taxonomy.ts
      via: prompt dimension names and alias mapping
      pattern: creative-quality-taxonomy
    - from: app/src/server/ai/creative-score.ts
      to: analyzeDerivationCreative
      via: normalizeCreativeScoreResult after JSON.parse
      pattern: normalizeCreativeScoreResult
---

<objective>
Introduce the shared quality taxonomy module and fail-safe score/QA normalization so model outputs cannot silently read as high-confidence good results.

Purpose: Close the default-70 parsing bug and unify dimension naming before gate alignment work.
Output: `creative-quality-taxonomy.ts`, `normalizeCreativeScoreResult`, tightened `normalizeCreativeQaResult`, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-scoring-and-qa-alignment/58-CONTEXT.md
@.planning/phases/58-scoring-and-qa-alignment/58-RESEARCH.md

<interfaces>
From app/src/server/ai/creative-qa.ts (types to migrate to taxonomy):
```typescript
export type CreativeQaCriterion =
  | "legibility" | "ctaOffer" | "informationPreservation"
  | "briefMatch" | "formatFit" | "creativeRisk" | "styleFidelity";
export type CreativeQaCheckStatus = "passed" | "warning" | "failed";
export interface CreativeQaCriterionResult { status: CreativeQaCheckStatus; note: string; }
```

From app/src/server/ai/creative-score.ts (parsing gap to fix):
```typescript
// Current anti-pattern — lines 194-204 use ?? 70 for every field
ctaClarity: clamp(parsed.scoreBreakdown?.ctaClarity ?? 70),
qualityScore: clamp(parsed.qualityScore ?? 70),
```

From app/src/server/repositories/derivation.ts:
```typescript
export type ScoreStatus = "pending" | "heuristic" | "analyzed" | "failed";
export interface CreativeScoreBreakdown {
  ctaClarity: number; textLegibility: number; briefMatch: number;
  visualQuality: number; formatFit: number; variationLevelFit: number;
  informationPreservation: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared quality taxonomy module (AIQ-01)</name>
  <files>app/src/server/ai/creative-quality-taxonomy.ts, app/src/server/ai/creative-qa.ts</files>
  <behavior>
    - CREATIVE_QA_CRITERIA includes legibility, ctaOffer, informationPreservation, briefMatch, formatFit, creativeRisk in display order
    - styleFidelity is restyling-only optional criterion (documented, not in core array)
    - SCORE_BREAKDOWN_ALIASES maps textLegibility→legibility, ctaClarity→ctaOffer, visualQuality→creativeRisk, variationLevelFit→variationLevelFit, etc. per CONTEXT D-31
    - mapScoreBreakdownKeyToCriterion returns correct criterion for each score key
  </behavior>
  <action>
    Create `app/src/server/ai/creative-quality-taxonomy.ts` per locked decision D-31 (AIQ-01).

    Export:
    - `CREATIVE_QA_CORE_CRITERIA` — ordered array of the six always-present QA criteria
    - `CREATIVE_QA_OPTIONAL_CRITERIA` — `["styleFidelity"]` for restyling
    - `CreativeQualityDimensionId` union covering all canonical IDs including `variationLevelFit`
    - `SCORE_BREAKDOWN_TO_CRITERION` record mapping score JSON keys to taxonomy IDs
    - `mapScoreBreakdownKeyToCriterion(key: string): CreativeQualityDimensionId | null`
    - `QA_CRITERION_DISPLAY_ORDER` for gate/UI ordering consistency

    Refactor `creative-qa.ts` to import criterion lists from taxonomy instead of local `CRITERIA` constant. Re-export `CreativeQaCriterion` type from taxonomy (or alias) so existing imports in gate/tests keep working via `creative-qa.ts` re-exports.

    Do NOT add DB columns. Do NOT add new hard-failure codes.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/creative-qa.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Taxonomy module exists; creative-qa imports canonical criterion IDs; no duplicated string union in qa.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Score Zod schema and normalizeCreativeScoreResult (AIQ-02)</name>
  <files>app/src/server/ai/creative-score.ts, app/tests/unit/ai/creative-score.test.ts</files>
  <behavior>
    - Empty/malformed JSON → scoreStatus "failed", qualityScore conservative (0), empty scoreIssues
    - Missing qualityScore with no valid breakdown → scoreStatus "failed"
    - Missing breakdown dimension → stored as 0 (not 70) OR null internally then 0 at persist boundary
    - Partial valid breakdown → scoreStatus "analyzed" only when at least one dimension is valid number
    - scoreIssues capped at 3 strings
    - analyzeDerivationCreative uses normalizeCreativeScoreResult instead of inline ?? 70 parsing
  </behavior>
  <action>
    Per locked decision D-48 (AIQ-02):

    1. Add Zod schema(s) for raw score JSON (`qualityScore`, `scoreBreakdown`, `scoreIssues`, `regenerationSuggestion`) colocated in `creative-score.ts` or imported from taxonomy file per discretion.

    2. Implement `export function normalizeCreativeScoreResult(value: unknown): ScoreResult`:
       - On parse failure or non-object input: `{ scoreStatus: "failed", qualityScore: 0, scoreBreakdown: all zeros, scoreIssues: [], regenerationSuggestion: "" }`
       - Never use `?? 70` for missing fields
       - Valid numeric dimensions clamp 0-100; missing/NaN dimensions → 0 in persisted breakdown
       - `qualityScore`: use parsed value if valid; else average of valid dimensions if any; else 0 with `scoreStatus: "failed"`
       - `scoreStatus`: `"analyzed"` only when `qualityScore` is valid OR at least one breakdown dimension parsed as finite number; otherwise `"failed"`
       - Cap `scoreIssues` to 3 trimmed strings

    3. Update `analyzeDerivationCreative` to `normalizeCreativeScoreResult(JSON.parse(raw))` then apply `buildRegenerationSuggestion` on normalized issues.

    4. Update scoring prompt to reference taxonomy-aligned dimension language where score keys differ (mention both score key and canonical concern, e.g. "ctaClarity (CTA/offer preservation)").

    5. Leave `scoreDerivationHeuristic` unchanged except import any shared constants if useful.

    Add unit tests in `app/tests/unit/ai/creative-score.test.ts` for normalization edge cases (empty object, missing breakdown, malformed qualityScore). Mock path tests for analyzeDerivationCreative should still pass.
  </action>
  <verify>
    <automated>cd app && npm test -- --run tests/unit/ai/creative-score.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>No ?? 70 defaults in analyze path; normalizeCreativeScoreResult exported and tested; malformed input yields failed/conservative scores</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten QA normalization (AIQ-02)</name>
  <files>app/src/server/ai/creative-qa.ts, app/src/server/ai/creative-qa.test.ts</files>
  <behavior>
    - Unknown checklist keys (e.g. "randomKey") are ignored
    - When all core criteria use fallback note, status is "warning" not "ready" even if model sent ready
    - styleFidelity absent for non-restyling input stays absent (no injected fallback)
    - styleFidelity present when model returns it is preserved
  </behavior>
  <action>
    Tighten `normalizeCreativeQaResult` per CONTEXT D-55-D-58 (AIQ-02):

    1. Build checklist only from taxonomy core criteria + optional styleFidelity when present in raw input — ignore unknown keys entirely.

    2. When `hasFallback` is true (any core criterion note is `"Needs a quick manual review."`), force `status: "warning"` even if model sent `"ready"`.

    3. Keep `asCheckStatus`/`asStatus` fallbacks to warning for unknown status strings.

    4. Update `buildCreativeQaPrompt` checklist key string to derive from `CREATIVE_QA_CORE_CRITERIA` (+ styleFidelity when restyling) so prompt stays in sync with taxonomy.

    Extend `app/src/server/ai/creative-qa.test.ts` with cases: unknown key ignored, model ready + all fallbacks → warning, partial real notes → respects model status.
  </action>
  <verify>
    <automated>cd app && npm test -- --run src/server/ai/creative-qa.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>QA normalization fail-safe; prompt checklist keys match taxonomy; tests cover new edge cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAI vision JSON → normalize* | Untrusted model output parsed at server boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | T | normalizeCreativeScoreResult | mitigate | Zod parse + conservative defaults; never inflate to 70 |
| T-58-02 | T | normalizeCreativeQaResult | mitigate | Ignore unknown keys; downgrade ready when all fallbacks |
| T-58-03 | I | ScoreResult persisted | accept | Scores are non-secret quality metadata |
</threat_model>

<verification>
- `npm test -- --run tests/unit/ai/creative-score.test.ts src/server/ai/creative-qa.test.ts` passes
- Grep `?? 70` in creative-score.ts analyze path returns zero matches
- Taxonomy module imported by both score and qa
</verification>

<success_criteria>
1. AIQ-01: Single taxonomy module owns dimension IDs and score↔QA aliases
2. AIQ-02: Score and QA outputs normalized with fail-safe defaults (no silent 70s)
3. All three tasks have automated test verification
</success_criteria>

<output>
After completion, create `.planning/phases/58-scoring-and-qa-alignment/58-01-SUMMARY.md`
</output>
