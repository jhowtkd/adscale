---
phase: 64-client-approval-package
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/ai/client-approval-package.ts
  - app/src/server/ai/client-approval-package.test.ts
  - app/src/server/repositories/share-link.ts
  - app/src/app/api/campaigns/[id]/approval-package/route.ts
  - app/src/app/api/campaigns/[id]/approval-package/route.test.ts
  - app/src/app/api/share/[token]/asset/[derivationId]/route.ts
autonomous: true
requirements:
  - DELIVER-01
  - DELIVER-03
  - DELIVER-04
must_haves:
  truths:
    - Approved derivations can be assembled into a package snapshot with staleness detection.
    - Share gallery uses token-scoped signed asset URLs.
    - Package can be refreshed when derivation selection changes.
  artifacts:
    - client-approval-package module with unit tests.
    - Campaign approval-package API route with tests.
---

# Plan 64-01: Approval Package Server Contract

<objective>
Add server-side approval package assembly, campaign API routes, share-link helpers, and signed asset access for public gallery.

Output: client-approval-package.ts, API routes, tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Server module and API</name>
  <files>app/src/server/ai/client-approval-package.ts, share-link repo, approval-package route, share asset route</files>
  <action>
    Implement package item builder, staleness check, share-link upsert, GET/POST approval-package route, signed asset route for share tokens.
  </action>
  <verify>cd app && npm test -- src/server/ai/client-approval-package.test.ts src/app/api/campaigns/\[id\]/approval-package/route.test.ts</verify>
  <done>Unit and route tests pass.</done>
</task>

</tasks>

<verification>
- `npm test -- src/server/ai/client-approval-package.test.ts`
- `npm test -- src/app/api/campaigns/[id]/approval-package/route.test.ts`
</verification>

<output>
After completion, create `.planning/phases/64-client-approval-package/64-01-SUMMARY.md`.
</output>
