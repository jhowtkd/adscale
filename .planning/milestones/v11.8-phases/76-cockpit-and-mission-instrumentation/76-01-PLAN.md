---
phase: 76-cockpit-and-mission-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/server/beta-analytics/types.ts
  - app/src/server/beta-analytics/record.ts
  - app/src/server/beta-analytics/record.test.ts
  - app/src/server/beta-analytics/session.ts
  - app/src/server/beta-analytics/session.test.ts
  - app/src/app/api/analytics/events/route.ts
  - app/src/app/api/analytics/events/route.test.ts
autonomous: true
requirements:
  - INST-04
must_haves:
  truths:
    - "Unknown Phase 76 event_key values are rejected before DB insert"
    - "Closed enum documents all Phase 76 server and client event keys"
    - "Client ingest reads optional sessionId from sessionStorage contract via request body"
    - "Server routes can read optional sessionId from x-beta-session-id header"
  artifacts:
    - path: app/src/server/beta-analytics/types.ts
      provides: "PHASE_76_BETA_EVENT_KEYS closed enum"
      exports: ["PHASE_76_BETA_EVENT_KEYS", "Phase76BetaEventKey"]
    - path: app/src/server/beta-analytics/session.ts
      provides: "Session ID extraction for server routes"
      exports: ["BETA_SESSION_STORAGE_KEY", "getBetaSessionIdFromRequest"]
    - path: app/src/server/beta-analytics/record.ts
      provides: "event_key validation against closed enum"
      exports: ["recordBetaAnalyticsEvent"]
  key_links:
    - from: app/src/server/beta-analytics/record.ts
      to: app/src/server/beta-analytics/types.ts
      via: "PHASE_76_BETA_EVENT_KEYS membership check"
      pattern: "PHASE_76_BETA_EVENT_KEYS"
    - from: app/src/app/api/analytics/events/route.ts
      to: app/src/server/beta-analytics/session.ts
      via: "merge header sessionId when body omits it"
      pattern: "getBetaSessionIdFromRequest"
---

<objective>
Establish Phase 76 event taxonomy contracts and beta-session ID plumbing shared by server and client instrumentation.

Purpose: Lock D-01 event_key snake_case enum and D-04 session grouping primitives before wiring cockpit hooks. Reject unknown keys at `recordBetaAnalyticsEvent` per CONTEXT allowlist-extension decision.
Output: Closed enum, session helpers, validated write path, route tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-cockpit-and-mission-instrumentation/76-CONTEXT.md
@.planning/phases/75-event-schema-and-ingest-foundation/75-03-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-VALIDATION.md

@app/src/server/beta-analytics/types.ts
@app/src/server/beta-analytics/record.ts
@app/src/app/api/analytics/events/route.ts

<interfaces>
From Phase 75 (record.ts):
```typescript
export async function recordBetaAnalyticsEvent(input: {
  workspaceId: string;
  userId: string;
  eventKey: string;
  properties?: unknown;
  sessionId?: string | null;
  campaignId?: string | null;
  derivationId?: string | null;
  source?: BetaEventSource;
}): Promise<BetaAnalyticsEvent>
```

From Phase 75 (types.ts) — existing ALLOWED_PROPERTY_KEYS already includes:
`stage`, `missionKey`, `blockingCount`, `readinessStatus`, `actualCredits`, `estimateCredits`, `operation`, `reasonCode`, `source`

Closed enum per D-01 (add to types.ts):
```typescript
export const PHASE_76_BETA_EVENT_KEYS = [
  "readiness_blocked", "readiness_completed", "credit_spend", "credit_blocked", "mission_completed",
  "cockpit_stage_entered", "cockpit_stage_completed", "cockpit_stage_abandoned",
] as const;
```

Session contract per D-04:
- Client: `sessionStorage` key `adscale_beta_session_id` (read in Plan 03 hook)
- Server: header `x-beta-session-id` (UUID)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Closed event_key enum and record validation</name>
  <files>app/src/server/beta-analytics/types.ts, app/src/server/beta-analytics/record.ts, app/src/server/beta-analytics/record.test.ts</files>
  <behavior>
    - Test: recordBetaAnalyticsEvent accepts each PHASE_76_BETA_EVENT_KEYS value
    - Test: recordBetaAnalyticsEvent rejects unknown key (e.g. `stage_entered`) with BetaEventPropertiesValidationError before insert
    - Test: existing sanitize/insert flow unchanged for allowed keys
  </behavior>
  <action>
    Per D-01, add `PHASE_76_BETA_EVENT_KEYS` const array and `Phase76BetaEventKey` type to `types.ts`. Do NOT add separate `event_type` column.

    In `recordBetaAnalyticsEvent`, after property sanitization, validate `input.eventKey` is in `PHASE_76_BETA_EVENT_KEYS`; throw `BetaEventPropertiesValidationError` with message `unknown event_key` if not. Keep existing session/campaign/derivation validation.

    Extend `record.test.ts` with enum acceptance + rejection cases. Mock `insertBetaAnalyticsEvent` as existing tests do.

    Do NOT extend ALLOWED_PROPERTY_KEYS unless a Phase 76 property is missing — current list already covers CONTEXT fields.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/record.test.ts</automated>
  </verify>
  <done>All 8 Phase 76 keys accepted; unknown keys rejected before DB insert; record tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Beta session ID plumbing for ingest and server routes</name>
  <files>app/src/server/beta-analytics/session.ts, app/src/server/beta-analytics/session.test.ts, app/src/app/api/analytics/events/route.ts, app/src/app/api/analytics/events/route.test.ts</files>
  <behavior>
    - Test: getBetaSessionIdFromRequest returns UUID from x-beta-session-id when valid
    - Test: getBetaSessionIdFromRequest returns undefined for missing/invalid header
    - Test: POST /api/analytics/events uses body.sessionId when present, else header sessionId
    - Test: BETA_SESSION_STORAGE_KEY constant equals `adscale_beta_session_id`
  </behavior>
  <action>
    Per D-04, create `app/src/server/beta-analytics/session.ts`:
    - Export `BETA_SESSION_STORAGE_KEY = "adscale_beta_session_id"` (for client hook in Plan 03)
    - Export `getBetaSessionIdFromRequest(request: Request): string | undefined` — read `x-beta-session-id`, validate UUID with zod, return undefined if absent/invalid

    Update `POST /api/analytics/events/route.ts`: resolve `sessionId` as `parsed.data.sessionId ?? getBetaSessionIdFromRequest(request)`.

    Add route test proving header-only sessionId reaches `recordBetaAnalyticsEvent`. No session CRUD — Phase 77 owns that.

    Export session helper for Plan 02 server routes (preflight, exports, share).
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/beta-analytics/session.test.ts src/app/api/analytics/events/route.test.ts</automated>
  </verify>
  <done>Session header plumbing works; analytics route merges body + header sessionId; constants exported for client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/analytics/events | Untrusted event_key and properties; must pass closed enum + allowlist |
| Server → recordBetaAnalyticsEvent | Trusted callers only; enum prevents typo keys polluting funnel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | Client event_key spoofing | mitigate | Closed enum rejection in recordBetaAnalyticsEvent (Task 1) |
| T-76-02 | Spoofing | sessionId cross-workspace attach | mitigate | Existing getBetaSessionById workspace ownership check in record.ts |
| T-76-03 | Info disclosure | Invalid header logging | accept | No property bodies logged; header UUID only |
</threat_model>

<verification>
- `cd app && npm test -- src/server/beta-analytics/record.test.ts src/server/beta-analytics/session.test.ts src/app/api/analytics/events/route.test.ts`
- Enum contains exactly the 8 keys from 76-CONTEXT.md Decisions
</verification>

<success_criteria>
- PHASE_76_BETA_EVENT_KEYS exported and enforced at write boundary
- Session ID readable from x-beta-session-id on ingest route
- No session CRUD added (deferred to Phase 77)
</success_criteria>

<output>
After completion, create `.planning/phases/76-cockpit-and-mission-instrumentation/76-01-SUMMARY.md`
</output>
