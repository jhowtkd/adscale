---
phase: 76-cockpit-and-mission-instrumentation
plan: 02
type: execute
wave: 2
depends_on:
  - "76-01"
files_modified:
  - app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.ts
  - app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.test.ts
  - app/src/server/billing/credits.ts
  - app/src/server/billing/credits.test.ts
  - app/src/server/billing/gates.ts
  - app/src/server/billing/gates.test.ts
  - app/src/app/api/exports/route.ts
  - app/src/app/api/exports/route.test.ts
  - app/src/app/api/share/route.ts
  - app/src/app/api/share/route.test.ts
  - app/src/app/api/derivations/[id]/review/route.ts
  - app/src/app/api/derivations/[id]/review/route.test.ts
autonomous: true
requirements:
  - INST-02
  - INST-04
must_haves:
  truths:
    - "Preflight POST records readiness_blocked when blocking issues exist"
    - "Preflight POST records readiness_completed when analysis succeeds without blocks"
    - "Credit spend records credit_spend with actualCredits and operation"
    - "Credit gate rejection records credit_blocked with reasonCode"
    - "Durable evidence writes emit mission_completed with correct missionKey"
    - "All server events use source server and optional sessionId from header"
  artifacts:
    - path: app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.ts
      provides: "readiness_blocked / readiness_completed emission"
    - path: app/src/server/billing/credits.ts
      provides: "credit_spend on successful recordUsage"
    - path: app/src/server/billing/gates.ts
      provides: "credit_blocked on spendCreditsOrApiError block"
    - path: app/src/app/api/exports/route.ts
      provides: "mission_completed export"
    - path: app/src/app/api/share/route.ts
      provides: "mission_completed share"
  key_links:
    - from: app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.ts
      to: app/src/server/beta-analytics/record.ts
      via: "recordBetaAnalyticsEvent after readiness computed"
      pattern: "recordBetaAnalyticsEvent"
    - from: app/src/server/billing/gates.ts
      to: app/src/server/beta-analytics/record.ts
      via: "credit_blocked on blocked status"
      pattern: "credit_blocked"
    - from: app/src/app/api/exports/route.ts
      to: app/src/server/beta-analytics/record.ts
      via: "mission_completed missionKey export"
      pattern: "mission_completed"
---

<objective>
Wire authoritative server-side beta analytics at readiness, credit, and mission-completion boundaries.

Purpose: Deliver INST-02 server events that cannot be spoofed by clients. Per D-02/D-03, emit at API route boundaries after validation using `recordBetaAnalyticsEvent` with `source: 'server'`.
Output: Instrumented preflight, billing gates, export, share, and derivation-approve routes with tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-cockpit-and-mission-instrumentation/76-CONTEXT.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/76-cockpit-and-mission-instrumentation/76-VALIDATION.md

@app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.ts
@app/src/server/billing/credits.ts
@app/src/server/billing/gates.ts
@app/src/app/api/exports/route.ts
@app/src/app/api/share/route.ts
@app/src/app/api/derivations/[id]/review/route.ts

<interfaces>
From Plan 76-01:
```typescript
export const PHASE_76_BETA_EVENT_KEYS = [/* readiness_*, credit_*, mission_completed, cockpit_* */] as const;
export function getBetaSessionIdFromRequest(request: Request): string | undefined;
export async function recordBetaAnalyticsEvent(input: { /* ... */ source?: "client" | "server" }): Promise<BetaAnalyticsEvent>;
```

Readiness shape from creative-readiness.ts:
```typescript
type ReadinessStatus = "ready" | "needs_attention" | "blocked";
interface CreativeReadiness { status: ReadinessStatus; blockingIssues: string[]; /* ... */ }
```

Mission key mapping per ARCHITECTURE.md:
- export POST → `missionKey: "export"`
- share POST → `missionKey: "share"`
- derivation review approve → `missionKey: "review"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preflight readiness and credit-block instrumentation</name>
  <files>app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.ts, app/src/app/api/campaigns/[id]/assets/[assetId]/preflight/route.test.ts</files>
  <behavior>
    - Test: when spendCreditsOrApiError returns 402, recordBetaAnalyticsEvent called with credit_blocked, operation creative_qa, reasonCode from check
    - Test: after successful analysis with blockingIssues.length > 0, emits readiness_blocked with blockingCount and readinessStatus
    - Test: after successful analysis with no blocks, emits readiness_completed with blockingCount 0
    - Test: cached completed response (early return) also emits appropriate readiness event
    - Test: sessionId from getBetaSessionIdFromRequest passed when header present
  </behavior>
  <action>
    Per D-02 preflight decision: import `recordBetaAnalyticsEvent` and `getBetaSessionIdFromRequest`.

    Destructure `{ user, workspace }` from `requireWorkspaceAccess` in POST (currently workspace-only).

    **Credit block:** When `spendCreditsOrApiError` returns non-null (402), before returning response, fire-and-forget `recordBetaAnalyticsEvent` with:
    - `eventKey: "credit_blocked"`, `source: "server"`, `campaignId`, `sessionId` from header
    - `properties: { operation: "creative_qa", reasonCode: check.reason, estimateCredits: 1, stage: "readiness", missionKey: "readiness" }`
    Wrap in try/catch; log warn on failure — never block API response.

    **Readiness result:** After readiness object computed (both cached early-return path and fresh analysis path), emit:
    - `readiness_blocked` if `readiness.blockingIssues.length > 0` OR `readiness.status === "blocked"`
    - else `readiness_completed`
    - `properties: { stage: "readiness", missionKey: "readiness", blockingCount, readinessStatus: readiness.status }`

    Mock `recordBetaAnalyticsEvent` in route.test.ts; extend existing POST tests.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/campaigns/\[id\]/assets/\[assetId\]/preflight/route.test.ts</automated>
  </verify>
  <done>Preflight route emits readiness_* and credit_blocked; tests assert call args; API behavior unchanged on analytics failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Credit spend and gate-block instrumentation</name>
  <files>app/src/server/billing/credits.ts, app/src/server/billing/credits.test.ts, app/src/server/billing/gates.ts, app/src/server/billing/gates.test.ts</files>
  <behavior>
    - Test: recordUsage success (status recorded) emits credit_spend with actualCredits from check.amount, operation from action
    - Test: recordUsage blocked emits credit_blocked with reasonCode insufficient_credits when userId provided
    - Test: spendCreditsOrApiError blocked path emits credit_blocked when userId in input
    - Test: analytics emit failure does not change recordUsage return value
  </behavior>
  <action>
    Per D-02 credit decisions:

    **recordUsage success path:** After `status: "recorded"` return path, when `input.userId` present, call `recordBetaAnalyticsEvent` with:
    - `eventKey: "credit_spend"`, `source: "server"`
    - `properties: { operation: input.action, actualCredits: check.amount, estimateCredits: typeof meta.estimateCredits === "number" ? meta.estimateCredits : check.amount }`
    - `campaignId` / `derivationId` from metadata when string
    - `sessionId` from `metadata.betaSessionId` when string UUID (optional — routes can pass)

    **recordUsage blocked path:** When returning `status: "blocked"`, emit `credit_blocked` with `reasonCode: check.reason`, `operation: input.action`, `estimateCredits: check.amount` when userId present.

    **spendCreditsOrApiError:** When `result.status === "blocked"` and `input.userId`, emit `credit_blocked` (avoid double-emit if recordUsage already emitted — prefer single emission point in recordUsage; gates should only emit if recordUsage did not, OR remove duplicate by only instrumenting in recordUsage for both paths). **Choose one path:** instrument blocked + success only in `recordUsage` since gates delegates to it; gates.test only needs mock verification if gates adds header sessionId passthrough to metadata — keep gates thin.

    Use void Promise + catch logger pattern; never throw from analytics.

    Extend credits.test.ts with mocked recordBetaAnalyticsEvent.
  </action>
  <verify>
    <automated>cd app && npm test -- src/server/billing/credits.test.ts src/server/billing/gates.test.ts</automated>
  </verify>
  <done>credit_spend and credit_blocked emitted from billing layer; duplicate events avoided; billing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mission_completed at durable evidence boundaries</name>
  <files>app/src/app/api/exports/route.ts, app/src/app/api/exports/route.test.ts, app/src/app/api/share/route.ts, app/src/app/api/share/route.test.ts, app/src/app/api/derivations/[id]/review/route.ts, app/src/app/api/derivations/[id]/review/route.test.ts</files>
  <behavior>
    - Test: exports POST success emits mission_completed with missionKey export
    - Test: share POST success emits mission_completed with missionKey share
    - Test: derivation review PATCH approved emits mission_completed with missionKey review
    - Test: rejected review does NOT emit mission_completed
    - Test: sessionId from request header forwarded on each emit
  </behavior>
  <action>
    Per D-02 mission completion discretion (multiple evidence hooks, not getWorkspaceMissions):

  1. **exports/route.ts** — after successful `exportIndividual` or `exportAllApproved`, before JSON response:
     - `mission_completed` with `properties: { missionKey: "export", stage: "export", operation: type }`, `campaignId`, `derivationId` when individual

  2. **share/route.ts** — after `createShareToken` succeeds:
     - `mission_completed` with `properties: { missionKey: "share", stage: "share" }`, `campaignId`

  3. **derivations/[id]/review/route.ts** — after `updateDerivationStatus` when `status === "approved"`:
     - `mission_completed` with `properties: { missionKey: "review", stage: "review" }`, `campaignId`, `derivationId: id`

    All calls: `source: "server"`, `userId` from requireWorkspaceAccess, `sessionId: getBetaSessionIdFromRequest(request)`.

    Create route.test.ts files if missing (follow preflight mock pattern). Mock recordBetaAnalyticsEvent.

    Do NOT hook inferMissionCompletions refresh — too heavy per CONTEXT.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/exports/route.test.ts src/app/api/share/route.test.ts src/app/api/derivations/\[id\]/review/route.test.ts</automated>
  </verify>
  <done>Three durable boundaries emit mission_completed; approve-only for review; route tests assert emissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Billing layer → analytics | Server-only; captures authoritative credit truth |
| Evidence routes → analytics | Emitted only after successful domain write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Repudiation | credit_spend missing | mitigate | Emit only after trackUsage succeeds (Task 2) |
| T-76-05 | Denial of service | Analytics throw blocks billing | mitigate | try/catch swallow; log warn only |
| T-76-06 | Tampering | Fake mission_completed from client | mitigate | mission_completed server-only keys; client enum separate cockpit_* keys |
</threat_model>

<verification>
- `cd app && npm test -- src/app/api/campaigns src/server/billing src/app/api/exports src/app/api/share src/app/api/derivations`
- Manual: preflight POST in dev shows readiness_* row in beta_analytics_events
</verification>

<success_criteria>
- All five server event types (readiness_blocked, readiness_completed, credit_spend, credit_blocked, mission_completed) emitted from authoritative paths
- source: server on every server emission
- sessionId forwarded when x-beta-session-id present
</success_criteria>

<output>
After completion, create `.planning/phases/76-cockpit-and-mission-instrumentation/76-02-SUMMARY.md`
</output>
