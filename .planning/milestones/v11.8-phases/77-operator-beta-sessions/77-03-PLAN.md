---
phase: 77-operator-beta-sessions
plan: 03
type: execute
wave: 3
depends_on:
  - 77-02
files_modified:
  - app/src/app/api/feedback/beta-sessions/[id]/notes/route.ts
  - app/src/app/api/feedback/beta-sessions/[id]/notes/route.test.ts
  - app/src/app/api/feedback/beta-sessions/[id]/summary/route.ts
  - app/src/app/api/feedback/beta-sessions/[id]/summary/route.test.ts
  - app/src/server/repositories/beta-sessions.ts
autonomous: true
requirements:
  - SESS-02
  - SESS-04
must_haves:
  truths:
    - "PATCH notes merges stage-keyed updates without wiping other stages"
    - "GET summary returns session metadata, stages completed, blockers, event IDs"
  artifacts:
    - path: app/src/app/api/feedback/beta-sessions/[id]/notes/route.ts
      provides: "Stage note merge endpoint"
      exports: ["PATCH"]
    - path: app/src/app/api/feedback/beta-sessions/[id]/summary/route.ts
      provides: "Session summary JSON for Phase 78"
      exports: ["GET"]
  key_links:
    - from: app/src/app/api/feedback/beta-sessions/[id]/summary/route.ts
      to: app/src/server/repositories/beta-sessions.ts
      via: "buildBetaSessionSummary"
      pattern: "buildBetaSessionSummary"
---

<objective>
Structured per-stage notes API and session summary export (SESS-02, SESS-04).

Purpose: Runbook-aligned note storage and dashboard-ready summary artifact.
Output: Notes PATCH route, summary GET route, buildBetaSessionSummary in repository.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/77-operator-beta-sessions/77-CONTEXT.md
@.planning/phases/67-milestone-archive-and-beta-runbook/67-BETA-RUNBOOK.md
@app/src/server/beta-sessions/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH stage notes route</name>
  <files>app/src/app/api/feedback/beta-sessions/[id]/notes/route.ts, app/src/app/api/feedback/beta-sessions/[id]/notes/route.test.ts</files>
  <behavior>
    - Body shape: `{ stages: { [stageKey]: BetaStageNote } }`
    - Invalid stage keys rejected
    - Existing stages preserved when patching one stage
  </behavior>
  <action>
    PATCH merges via `mergeBetaSessionNotes`. Validate keys against BETA_RUNBOOK_STAGES. requirePlatformOwner. Return updated session.
  </action>
  <verify>
    <automated>cd app && npm test -- "src/app/api/feedback/beta-sessions/[id]/notes/route.test.ts"</automated>
  </verify>
  <done>Notes merge API tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Summary export route and builder</name>
  <files>app/src/server/repositories/beta-sessions.ts, app/src/app/api/feedback/beta-sessions/[id]/summary/route.ts, app/src/app/api/feedback/beta-sessions/[id]/summary/route.test.ts</files>
  <behavior>
    - Summary includes sessionId, workspaceId, startedAt, endedAt, cohortLabel, assistanceLevel
    - stagesCompleted lists stages with completedAt
    - blockers aggregates blockerIds across stages
    - eventIds lists beta_analytics_events for session
    - feedbackReportIds from stage notes
  </behavior>
  <action>
    Add `buildBetaSessionSummary(sessionId)` in repository using listBetaAnalyticsEvents. GET `/summary` returns JSON for Phase 78 input.
  </action>
  <verify>
    <automated>cd app && npm test -- "src/app/api/feedback/beta-sessions/[id]/summary/route.test.ts"</automated>
  </verify>
  <done>Summary route returns complete artifact shape</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05 | T | notes PATCH | mitigate | Zod validate stage keys and note fields |
| T-77-06 | I | summary GET | mitigate | Owner-only; no cross-tenant workspace leak in response |
</threat_model>

<verification>
`cd app && npm test -- src/app/api/feedback/beta-sessions`
</verification>

<success_criteria>
SESS-02 notes and SESS-04 summary export complete.
</success_criteria>

<output>
After completion, create `.planning/phases/77-operator-beta-sessions/77-03-SUMMARY.md`
</output>
