---
phase: 77-operator-beta-sessions
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - app/src/app/api/feedback/beta-sessions/route.ts
  - app/src/app/api/feedback/beta-sessions/route.test.ts
  - app/src/app/api/feedback/beta-sessions/[id]/route.ts
  - app/src/app/api/feedback/beta-sessions/[id]/route.test.ts
autonomous: true
requirements:
  - SESS-01
must_haves:
  truths:
    - "Platform owner can POST start session with workspaceId"
    - "Platform owner can PATCH end session"
    - "Platform owner can GET list (workspaceId, activeOnly) and GET by id"
    - "Non-owners receive 403"
  artifacts:
    - path: app/src/app/api/feedback/beta-sessions/route.ts
      provides: "POST start + GET list"
      exports: ["POST", "GET"]
    - path: app/src/app/api/feedback/beta-sessions/[id]/route.ts
      provides: "GET single + PATCH end"
      exports: ["GET", "PATCH"]
  key_links:
    - from: app/src/app/api/feedback/beta-sessions/route.ts
      to: app/src/server/auth/platform-owner.ts
      via: "requirePlatformOwner"
      pattern: "requirePlatformOwner"
---

<objective>
Deliver owner-only session CRUD HTTP routes per CONTEXT D (SESS-01).

Purpose: Wire repository to `/api/feedback/beta-sessions` surface matching feedback triage auth.
Output: Start, end, list, get-by-id routes with route tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/77-operator-beta-sessions/77-CONTEXT.md
@app/src/app/api/feedback/reports/route.ts
@app/src/server/repositories/beta-sessions.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST start and GET list routes</name>
  <files>app/src/app/api/feedback/beta-sessions/route.ts, app/src/app/api/feedback/beta-sessions/route.test.ts</files>
  <behavior>
    - POST with workspaceId creates session; optional cohortLabel, assistanceLevel
    - GET supports workspaceId and activeOnly query params
    - 403 when requirePlatformOwner fails
  </behavior>
  <action>
    Implement routes with Zod validation. POST body: `{ workspaceId, cohortLabel?, assistanceLevel? }`. GET query: `workspaceId`, `activeOnly=true`. Follow `feedback/reports/route.ts` error handling.
  </action>
  <verify>
    <automated>cd app && npm test -- src/app/api/feedback/beta-sessions/route.test.ts</automated>
  </verify>
  <done>Start and list routes tested with owner auth mock</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET by id and PATCH end routes</name>
  <files>app/src/app/api/feedback/beta-sessions/[id]/route.ts, app/src/app/api/feedback/beta-sessions/[id]/route.test.ts</files>
  <behavior>
    - GET returns session by id or 404
    - PATCH sets endedAt (optional body endedAt ISO string)
    - 403 for non-owner
  </behavior>
  <action>
    Dynamic route `[id]`. PATCH accepts optional `endedAt`; default `new Date()`. Return `{ session }` JSON shape consistent with POST.
  </action>
  <verify>
    <automated>cd app && npm test -- "src/app/api/feedback/beta-sessions/[id]/route.test.ts"</automated>
  </verify>
  <done>Single session fetch and end session tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted operator UI; owner auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03 | S | beta-sessions routes | mitigate | requirePlatformOwner on every handler |
| T-77-04 | E | POST start | mitigate | Zod UUID validation on workspaceId |
</threat_model>

<verification>
`cd app && npm test -- src/app/api/feedback/beta-sessions`
</verification>

<success_criteria>
SESS-01 API routes complete with owner-only access.
</success_criteria>

<output>
After completion, create `.planning/phases/77-operator-beta-sessions/77-02-SUMMARY.md`
</output>
