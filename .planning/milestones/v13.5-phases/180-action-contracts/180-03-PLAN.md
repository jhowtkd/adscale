---
phase: 180-action-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - "180-01"
requirements:
  - ACT-02
  - EXEC-01
files_modified:
  - app/src/server/assistant/action-contracts/validate.ts
  - app/src/server/assistant/action-contracts/validate.test.ts
  - app/src/server/assistant/tools/stubs/propose-action.ts
  - app/src/server/assistant/tools/policy.ts
  - app/src/server/assistant/tools/policy.test.ts
autonomous: true
must_haves:
  truths:
    - "propose_action rejects unregistered actionType before createAssistantAction"
    - "propose_action validates inputSnapshot against contract Zod schema and contract allowedRoles"
    - "Missing optional inputs do not block propose; display includes riskCopyLines from templates"
    - "Enriched display includes riskLabel, creditImpact, confirmationPolicy, actionType"
    - "Contract validation failures return policy deny not uncaught exception"
    - "Only pending action cards are created — execution deferred until confirm (Phase 182+)"
  artifacts:
    - path: app/src/server/assistant/action-contracts/validate.ts
      provides: "validateProposeAction contract gate"
      exports: ["validateProposeAction"]
    - path: app/src/server/assistant/tools/stubs/propose-action.ts
      provides: "Registry-enforced propose handler"
      contains: "getActionContract"
    - path: app/src/server/assistant/tools/policy.ts
      provides: "AssistantActionValidationError catch"
      contains: "contract_validation_failed"
  key_links:
    - from: app/src/server/assistant/tools/stubs/propose-action.ts
      to: app/src/server/assistant/action-contracts/validate.ts
      via: "validateProposeAction before createAssistantAction"
      pattern: "validateProposeAction"
    - from: app/src/server/assistant/tools/stubs/propose-action.ts
      to: app/src/server/repositories/assistant-action.ts
      via: "createAssistantAction with enriched display"
      pattern: "createAssistantAction"
    - from: app/src/server/assistant/tools/policy.ts
      to: app/src/server/repositories/assistant-action.ts
      via: "catch AssistantActionValidationError"
      pattern: "AssistantActionValidationError"
---

<objective>
Enforce action contracts in `propose_action`: registry lookup, per-contract Zod validation, role gate, risk copy, enriched display metadata, and safe policy error handling.

Purpose: ACT-02 validation at propose time; EXEC-01 ensures write/credit actions only create pending cards with `confirmationPolicy: "required"` metadata before any execution.
Output: `validate.ts`, updated `propose-action.ts` and `policy.ts`, and extended policy/validate tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/180-action-contracts/180-CONTEXT.md
@.planning/milestones/v13.5-phases/180-action-contracts/180-RESEARCH.md
@app/src/server/assistant/tools/stubs/propose-action.ts
@app/src/server/assistant/tools/policy.ts
@app/src/server/repositories/assistant-action.ts
@app/src/server/assistant/context/sanitize.ts

## Locked decisions

- **ACT-02 / D-validate:** `propose_action` validates against registry: Zod per contract, role gate, required inputs present.
- **ACT-02 / D-optionals:** Missing optionals do not block — `buildRiskCopyLines` in display only.
- **EXEC-01 / D-pending:** `createAssistantAction` creates `pending` only; no execution in Phase 180.
- **RESEARCH Q1 RESOLVED:** Catch `AssistantActionValidationError` explicitly → `deny("contract_validation_failed")`; rethrow unexpected errors.

<interfaces>
From `app/src/server/repositories/assistant-action.ts`:

```typescript
export class AssistantActionValidationError extends Error {
  constructor(message: string);
}

export async function createAssistantAction(
  workspaceId: string,
  input: CreateAssistantActionInput
): Promise<{ action; message }>;
```

From `propose-action.ts` (current):

```typescript
export async function handleProposeAction(ctx, args): Promise<ToolHandlerResult>;
export const proposeActionTool: RegisteredTool = {
  name: "propose_action",
  requiresConfirmation: true,
  allowedRoles: ["owner", "admin", "member"],
  // ...
};
```

From `policy.ts`:

```typescript
export async function evaluateToolCall(ctx, call): Promise<ToolPolicyResult>;
function deny(reason: string): ToolPolicyResult;
```
</interfaces>
</context>

<tasks>

<task id="180-03-01" type="auto" tdd="true">
  <name>Implement validateProposeAction contract gate</name>
  <files>app/src/server/assistant/action-contracts/validate.ts, app/src/server/assistant/action-contracts/validate.test.ts</files>
  <behavior>
    - Unknown actionType → throws AssistantActionValidationError("unknown_action_type")
    - Missing required field in snapshot → throws AssistantActionValidationError("invalid_action_inputs")
    - Valid quick_restyle with only baseCreativeId → success; riskCopyLines length 1 when styleReferenceId absent
    - Valid quick_restyle with styleReferenceId → riskCopyLines empty
    - Role not in contract.allowedRoles → throws AssistantActionValidationError (via requireRole)
    - Returns display object with label, actionType, riskLabel, creditImpact, riskCopyLines, confirmationPolicy
  </behavior>
  <action>
    Create `validate.ts` (ACT-02):

    Import `getActionContract`, `buildRiskCopyLines`, `AssistantActionValidationError` from repos, `requireRole` from workspace.

    Export `validateProposeAction(ctx: ToolHandlerContext, parsed: { actionType; label; inputSnapshot })`:

    1. `getActionContract(parsed.actionType)` — if missing throw `AssistantActionValidationError("unknown_action_type")`
    2. `await requireRole(ctx.workspaceId, ctx.userId, contract.allowedRoles)` — let WorkspaceAuthError propagate (policy handles)
    3. `contract.inputSchema.safeParse(inputSnapshot)` — on failure throw `AssistantActionValidationError("invalid_action_inputs")`
    4. `buildRiskCopyLines(contract, inputSnapshot)`
    5. Return `{ display: { label, actionType, riskLabel, creditImpact, riskCopyLines, confirmationPolicy }, contract }`

    Ensure `import "@/server/assistant/action-contracts/registry"` or registry side-effect loads contracts in tests.

    Write `validate.test.ts` with mocked `requireRole` covering behaviors above. Use valid UUIDs in fixtures.

    Do NOT implement confirm revalidation here — Plan 04 adds `revalidateOnConfirm`.
    Do NOT execute writes or spend credits.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/assistant/action-contracts/validate.test.ts -x</automated>
  </verify>
  <done>validateProposeAction enforces registry, schema, roles; optional missing fields produce risk copy without throw.</done>
</task>

<task id="180-03-02" type="auto" tdd="true">
  <name>Wire propose_action and policy error handling</name>
  <files>app/src/server/assistant/tools/stubs/propose-action.ts, app/src/server/assistant/tools/policy.ts, app/src/server/assistant/tools/policy.test.ts</files>
  <behavior>
    - propose_action with actionType "restyle" (unregistered) → deny contract_validation_failed
    - propose_action with quick_restyle + valid baseCreativeId → allowed, requiresConfirmation true, actionRecordId set
    - createAssistantAction called with display containing riskLabel and confirmationPolicy
    - WorkspaceAuthError from requireRole still → deny forbidden (unchanged)
  </behavior>
  <action>
    Update `handleProposeAction` (ACT-02, EXEC-01):

    - Keep `proposeActionSchema` envelope and `stripDeniedFields` / `containsDeniedPersistenceKeys` checks
    - Call `validateProposeAction(ctx, { actionType, label, inputSnapshot })` before `createAssistantAction`
    - Pass returned `display` to `createAssistantAction` (replaces minimal `{ label, actionType }`)

    Update `evaluateToolCall` in `policy.ts` (RESEARCH Pitfall 1, Q1):

    Wrap `tool.handler(...)` in try/catch:
    - `AssistantActionValidationError` → `return deny("contract_validation_failed")`
    - Re-throw other errors

    Update `policy.test.ts`:

    - Change `actionType: "restyle"` test → deny unknown OR use `quick_restyle` with `{ baseCreativeId: "<valid-uuid>" }`
    - Add test: unknown actionType → `denialReason: "contract_validation_failed"`
    - Add test: valid quick_restyle → allowed + `mockCreateAction` receives display with `confirmationPolicy: "required"`

    Mock `validateProposeAction` only if needed; prefer integration through registry with real contracts from Plan 01.

    Tool-level `allowedRoles` remains superset; contract may further restrict per RESEARCH anti-pattern note.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/assistant/tools/policy.test.ts -x</automated>
  </verify>
  <done>propose_action registry-enforced; policy maps validation errors to deny; fixtures use quick_restyle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider tool_call → propose_action | Untrusted actionType and inputSnapshot from model |
| propose_action → database | inputSnapshot persisted after sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-06 | Elevation of privilege | Unregistered actionType | mitigate | Registry lookup rejects unknown types |
| T-180-07 | Spoofing | Role escalation via propose | mitigate | requireRole at tool + contract allowedRoles |
| T-180-08 | Tampering | Denied keys in snapshot | mitigate | stripDeniedFields + containsDeniedPersistenceKeys unchanged |
| T-180-09 | Repudiation | Credit spend without card | mitigate | confirmationPolicy required; pending only until confirm |
</threat_model>

<verification>
- `cd app && npx vitest run src/server/assistant/action-contracts/validate.test.ts src/server/assistant/tools/policy.test.ts -x` passes
- No free-form actionType reaches createAssistantAction
</verification>

<success_criteria>
- ACT-02: propose_action validates registry, schema, roles, risk copy
- EXEC-01: Pending cards only; display exposes confirmationPolicy required
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/180-action-contracts/180-03-SUMMARY.md`
</output>
