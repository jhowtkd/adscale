---
phase: 180-action-contracts
plan: 04
type: execute
wave: 3
depends_on:
  - "180-01"
  - "180-03"
requirements:
  - EXEC-01
files_modified:
  - app/src/server/assistant/action-contracts/validate.ts
  - app/src/server/assistant/action-contracts/confirm-validation.test.ts
  - app/src/app/api/assistant/actions/[actionId]/confirm/route.ts
  - app/src/app/api/assistant/actions/[actionId]/confirm/route.test.ts
autonomous: true
must_haves:
  truths:
    - "Confirm route rejects transition when action status is not pending"
    - "Confirm revalidates inputSnapshot against contract schema before confirmed transition"
    - "Confirm resolves contract from display.actionType on action card message"
    - "Invalid or stale snapshot cannot reach confirmed status"
    - "Confirm does not show second form or mutate inputSnapshot"
  artifacts:
    - path: app/src/server/assistant/action-contracts/validate.ts
      provides: "revalidateOnConfirm"
      exports: ["revalidateOnConfirm"]
    - path: app/src/app/api/assistant/actions/[actionId]/confirm/route.ts
      provides: "Confirm with contract revalidation"
      contains: "revalidateOnConfirm"
  key_links:
    - from: app/src/app/api/assistant/actions/[actionId]/confirm/route.ts
      to: app/src/server/assistant/action-contracts/validate.ts
      via: "revalidateOnConfirm before confirmAssistantAction"
      pattern: "revalidateOnConfirm"
    - from: app/src/server/assistant/action-contracts/validate.ts
      to: app/src/server/repositories/assistant-action.ts
      via: "load action + message display"
      pattern: "getAssistantActionById"
---

<objective>
Add light contract revalidation on action confirm: pending gate, schema re-check on stored inputSnapshot, no second form.

Purpose: EXEC-01 requires confirmed action cards before writes/credits; stale or invalid snapshots must not reach `confirmed`.
Output: `revalidateOnConfirm`, updated confirm route, and colocated tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/180-action-contracts/180-CONTEXT.md
@.planning/milestones/v13.5-phases/180-action-contracts/180-RESEARCH.md
@app/src/app/api/assistant/actions/[actionId]/confirm/route.ts
@app/src/server/repositories/assistant-action.ts
@app/src/server/repositories/assistant-message.ts

## Locked decisions

- **EXEC-01 / D-revalidate:** Light revalidation on confirm — inputs still valid, status `pending`; no second form.
- **RESEARCH Pitfall 4:** `display.actionType` on action card message is authoritative for contract lookup.
- **Deferred:** Actual execution after confirm — Phase 182+.

<interfaces>
From `confirm/route.ts`:

```typescript
export async function POST(request, { params }): Promise<NextResponse>;
// Currently: confirmAssistantAction(workspace.id, actionId) only
```

From `assistant-action.ts`:

```typescript
export async function getAssistantActionById(workspaceId, actionId);
export async function confirmAssistantAction(workspaceId, actionId);
export class InvalidActionTransitionError extends Error;
export class AssistantActionValidationError extends Error;
```

Action card message payload shape (Phase 178):

```typescript
{ actionRecordId, status: "pending", display: { actionType, label, riskLabel, creditImpact, ... } }
```
</interfaces>
</context>

<tasks>

<task id="180-04-01" type="auto" tdd="true">
  <name>Implement revalidateOnConfirm</name>
  <files>app/src/server/assistant/action-contracts/validate.ts, app/src/server/assistant/action-contracts/confirm-validation.test.ts</files>
  <behavior>
    - Action not found → throws or returns error shape consumed by route
    - Status not pending → throws InvalidActionTransitionError or AssistantActionValidationError
    - display.actionType missing → AssistantActionValidationError
    - inputSnapshot fails contract schema → AssistantActionValidationError("invalid_action_inputs")
    - Valid pending quick_restyle action → passes without mutating snapshot
  </behavior>
  <action>
    Add to `validate.ts` (EXEC-01, RESEARCH Pattern 4):

    Export `revalidateOnConfirm(workspaceId: string, actionId: string): Promise<void>`:

    1. `getAssistantActionById(workspaceId, actionId)` — if null throw `AssistantActionValidationError("action_not_found")`
    2. If `action.status !== "pending"` throw `InvalidActionTransitionError` or `AssistantActionValidationError("not_pending")` (match route error handling pattern)
    3. Load action card message via `action.messageId` — read `payload.display.actionType` (add helper in repos if needed, or query assistantMessages by id)
    4. `getActionContract(actionType)` — if missing throw `unknown_action_type`
    5. `contract.inputSchema.safeParse(action.inputSnapshot)` — on failure throw `invalid_action_inputs`
    6. Read-only — do NOT patch inputSnapshot (repo forbids while pending)

    Write `confirm-validation.test.ts` with mocked `getAssistantActionById` and message loader covering behaviors.

    If message fetch helper missing, add minimal `getAssistantMessageById` in assistant-message repo — keep under 20 lines, no unrelated changes.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/assistant/action-contracts/confirm-validation.test.ts -x</automated>
  </verify>
  <done>revalidateOnConfirm gates confirm on pending status and valid contract schema.</done>
</task>

<task id="180-04-02" type="auto" tdd="true">
  <name>Integrate revalidation into confirm route</name>
  <files>app/src/app/api/assistant/actions/[actionId]/confirm/route.ts, app/src/app/api/assistant/actions/[actionId]/confirm/route.test.ts</files>
  <behavior>
    - POST confirm with valid pending action → 200 + action status confirmed
    - POST confirm with invalid snapshot → 400 invalidInput
    - POST confirm on already confirmed action → 400 invalidInput
    - requireWorkspaceAccess still enforced
  </behavior>
  <action>
    Update `confirm/route.ts`:

    After `requireWorkspaceAccess`, before `confirmAssistantAction`:
    - `await revalidateOnConfirm(workspace.id, actionId)`

    Map errors:
    - `InvalidActionTransitionError` → existing `apiError("invalidInput", 400)`
    - `AssistantActionValidationError` → `apiError("invalidInput", 400, { message })` (generic safe message ok)

    Write `route.test.ts` mocking `revalidateOnConfirm` and `confirmAssistantAction` (follow chat route.test.ts patterns from Phase 179 if present; otherwise vitest + vi.mock).

    Run full assistant test slice to ensure Plan 03 fixture updates still green:

    Update `orchestrator.test.ts` tool_call fixture `actionType` from `"restyle"` to `"quick_restyle"` if not done in Plan 02/03.

    Do NOT trigger job execution or credit spend on confirm — Phase 182 responsibility.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/app/api/assistant/actions/\\[actionId\\]/confirm/route.test.ts src/server/assistant/action-contracts/confirm-validation.test.ts -x</automated>
  </verify>
  <done>Confirm route revalidates contract; invalid/stale actions return 400; confirmed transition only when valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST confirm | Authenticated workspace member; actionId from client |
| Confirm → database | Status transition pending → confirmed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-10 | Tampering | Confirmed action with invalid snapshot | mitigate | revalidateOnConfirm schema check before transition |
| T-180-11 | Elevation of privilege | Confirm another workspace's action | mitigate | requireWorkspaceAccess + workspaceId scoping on load |
| T-180-12 | Repudiation | Skip confirmation for write actions | mitigate | pending gate; execution only after confirmed (182+) |
</threat_model>

<verification>
- `cd app && npm test -- --run src/server/assistant/action-contracts/ src/server/assistant/tools/policy.test.ts src/server/assistant/orchestrator.test.ts` passes
- `cd app && npm run build` succeeds
</verification>

<success_criteria>
- EXEC-01: Confirm path revalidates pending + schema; invalid snapshots blocked
- No execution or credit spend on confirm in Phase 180
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/180-action-contracts/180-04-SUMMARY.md`
</output>
