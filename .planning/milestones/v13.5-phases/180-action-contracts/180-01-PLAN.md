---
phase: 180-action-contracts
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - ACT-02
files_modified:
  - app/src/server/assistant/action-contracts/types.ts
  - app/src/server/assistant/action-contracts/registry.ts
  - app/src/server/assistant/action-contracts/risk-copy.ts
  - app/src/server/assistant/action-contracts/contracts/quick-restyle.ts
  - app/src/server/assistant/action-contracts/contracts/start-complete-campaign.ts
  - app/src/server/assistant/action-contracts/contracts/index.ts
  - app/src/server/assistant/action-contracts/registry.test.ts
  - app/src/server/assistant/action-contracts/risk-copy.test.ts
autonomous: true
must_haves:
  truths:
    - "ACTION_CONTRACT_REGISTRY contains quick_restyle and start_complete_campaign with full metadata"
    - "Each contract declares actionType, requiredFields, optionalFields with risk templates, allowedRoles, riskLabel, creditImpact, confirmationPolicy"
    - "registerActionContract allows Phase 182 to add contracts without registry refactor"
    - "Missing optional fields produce deterministic risk copy lines from templates"
  artifacts:
    - path: app/src/server/assistant/action-contracts/types.ts
      provides: "ActionContract, CreditImpact, IntentFamily types"
      exports: ["ActionContract", "ConfirmationPolicy", "RiskLabel", "CreditImpact", "OptionalFieldMeta"]
    - path: app/src/server/assistant/action-contracts/registry.ts
      provides: "ACTION_CONTRACT_REGISTRY and registerActionContract"
      exports: ["ACTION_CONTRACT_REGISTRY", "registerActionContract", "getActionContract"]
    - path: app/src/server/assistant/action-contracts/contracts/quick-restyle.ts
      provides: "quick_restyle example contract"
      contains: "quick_restyle"
    - path: app/src/server/assistant/action-contracts/contracts/start-complete-campaign.ts
      provides: "start_complete_campaign example contract"
      contains: "start_complete_campaign"
  key_links:
    - from: app/src/server/assistant/action-contracts/registry.ts
      to: app/src/server/assistant/action-contracts/contracts/index.ts
      via: "side-effect import registers both contracts"
      pattern: "import.*contracts"
    - from: app/src/server/assistant/action-contracts/contracts/quick-restyle.ts
      to: app/src/server/billing/credits.ts
      via: "CREDIT_COSTS.restyling for creditImpact"
      pattern: "CREDIT_COSTS"
---

<objective>
Establish the typed action-contract grammar: core types, central registry, risk-copy builder, and two example contracts (`quick_restyle`, `start_complete_campaign`).

Purpose: ACT-02 requires every supported action to declare inputs, roles, risk, credits, and confirmation policy; the registry is the single source of truth Phase 182 extends add-only.
Output: `app/src/server/assistant/action-contracts/` foundation with registry + example contracts and unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/180-action-contracts/180-CONTEXT.md
@.planning/milestones/v13.5-phases/180-action-contracts/180-RESEARCH.md
@app/src/server/assistant/tools/registry.ts
@app/src/server/billing/credits.ts
@app/src/server/auth/workspace.ts

## Locked decisions (D-registry)

- **ACT-02 / D-registry:** Central typed registry at `action-contracts/registry.ts`; mirror `TOOL_REGISTRY` pattern with `registerActionContract()`.
- **ACT-02 / D-examples:** Only `quick_restyle` and `start_complete_campaign` in Phase 180; ACT-04 contracts deferred to Phase 182.
- **ACT-02 / D-risk-copy:** Static `riskCopyWhenMissing` templates per optional field — no LLM-generated copy.
- **EXEC-01 / D-confirm-policy:** Both example contracts use `confirmationPolicy: "required"`.
- **Discretion:** `creditImpact` uses `{ kind: "creditAction", action: "restyling" | "creative_plan" }` referencing `CREDIT_COSTS` (RESEARCH A2 resolved).

<interfaces>
From `app/src/server/assistant/tools/registry.ts`:

```typescript
export const TOOL_REGISTRY: Record<string, RegisteredTool> = { ... };
export function registerTool(tool: RegisteredTool): void { ... }
export function getToolDefinition(name: string): RegisteredTool | undefined { ... }
```

From `app/src/server/billing/credits.ts`:

```typescript
export const CREDIT_COSTS = {
  creative_plan: 1,
  restyling: 5,
  // ...
} as const;
export type CreditAction = keyof typeof CREDIT_COSTS;
```

From `app/src/server/auth/workspace.ts`:

```typescript
export type WorkspaceMemberRole = "owner" | "admin" | "member";
```
</interfaces>
</context>

<tasks>

<task id="180-01-01" type="auto">
  <name>Define action-contract types, registry, and risk-copy builder</name>
  <files>app/src/server/assistant/action-contracts/types.ts, app/src/server/assistant/action-contracts/registry.ts, app/src/server/assistant/action-contracts/risk-copy.ts</files>
  <action>
    Create `types.ts` per RESEARCH Pattern 1 (ACT-02):

    - `ConfirmationPolicy`: `"required" | "none"`
    - `RiskLabel`: `"low" | "medium" | "high"`
    - `IntentFamily`: `"quick_action" | "complete_campaign"`
    - `CreditImpact`: discriminated union `{ kind: "fixed"; credits: number; label?: string } | { kind: "creditAction"; action: CreditAction; label?: string }`
    - `OptionalFieldMeta`: `{ key: string; riskCopyWhenMissing: string }` (pt-BR templates)
    - `ActionContract<T extends z.ZodType>`: `actionType`, `intentFamily`, `label`, `inputSchema`, `requiredFields`, `optionalFields`, `allowedRoles`, `riskLabel`, `creditImpact`, `confirmationPolicy`

    Create `registry.ts` mirroring `tools/registry.ts`:

    - `ACTION_CONTRACT_REGISTRY: Record<string, ActionContract>`
    - `registerActionContract(contract)` — overwrites by `actionType`
    - `getActionContract(actionType)` — returns undefined for unknown
    - Bottom of file: `import "./contracts"` (side-effect registration per RESEARCH open question #2 RESOLVED)

    Create `risk-copy.ts`:

    - `buildRiskCopyLines(contract, snapshot)`: filter `optionalFields` where snapshot value is `undefined`, `null`, or `""`; map to `riskCopyWhenMissing` strings
    - Do NOT block propose on missing optionals — builder only produces copy lines

    Do NOT implement validation logic here — that belongs in Plan 03 `validate.ts`.
    Do NOT add LLM risk copy or deferred ACT-04 contracts.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Types exported; registry and risk-copy modules compile; no runtime registration yet.</done>
</task>

<task id="180-01-02" type="auto" tdd="true">
  <name>Register quick_restyle and start_complete_campaign example contracts</name>
  <files>app/src/server/assistant/action-contracts/contracts/quick-restyle.ts, app/src/server/assistant/action-contracts/contracts/start-complete-campaign.ts, app/src/server/assistant/action-contracts/contracts/index.ts, app/src/server/assistant/action-contracts/registry.test.ts, app/src/server/assistant/action-contracts/risk-copy.test.ts</files>
  <behavior>
    - Registry contains `quick_restyle` with `intentFamily: "quick_action"`, `confirmationPolicy: "required"`, `creditImpact.action: "restyling"`
    - Registry contains `start_complete_campaign` with `intentFamily: "complete_campaign"`, `confirmationPolicy: "required"`, `creditImpact.action: "creative_plan"`
    - `quick_restyle` requires only `baseCreativeId` (uuid); `styleReferenceId` optional with pt-BR risk template
    - `start_complete_campaign` requires productOffer, audience, objective, cta, platformOrFormat, constraints, baseCreativeId
    - `buildRiskCopyLines` returns style-reference warning when `styleReferenceId` absent; empty array when present
  </behavior>
  <action>
  Implement contracts per RESEARCH examples (ACT-02 / D-examples):

  **`contracts/quick-restyle.ts`:**
  - `quickRestyleInputSchema`: `.strict()` Zod object — `baseCreativeId` uuid required; `styleReferenceId` uuid optional; `notes` string max 2000 optional
  - `requiredFields: ["baseCreativeId"]`
  - `optionalFields`: styleReferenceId with "Sem referência de estilo, o resultado pode divergir mais da marca."
  - `allowedRoles: ["owner", "admin", "member"]`, `riskLabel: "medium"`
  - `creditImpact: { kind: "creditAction", action: "restyling", label: \`${CREDIT_COSTS.restyling} créditos\` }`

  **`contracts/start-complete-campaign.ts`:**
  - Schema per RESEARCH: productOffer, audience, objective, cta, platformOrFormat, constraints (all min 1 string), baseCreativeId uuid, styleReferenceId optional
  - `intentFamily: "complete_campaign"`, `riskLabel: "high"`
  - Campaign optional risk copy: "Sem referência de estilo, a direção visual inicial pode precisar de mais revisões."

  **`contracts/index.ts`:** import both and call `registerActionContract` for each.

  Write `registry.test.ts`: assert both actionTypes registered; assert `confirmationPolicy === "required"` for EXEC-01; assert `getActionContract("unknown")` undefined.

  Write `risk-copy.test.ts`: missing `styleReferenceId` → one line; present id → empty array.

  Use `.strict()` on all Zod schemas — match tool policy convention from Phase 179.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/assistant/action-contracts/registry.test.ts src/server/assistant/action-contracts/risk-copy.test.ts -x</automated>
  </verify>
  <done>Both example contracts registered; registry and risk-copy tests pass; schemas use strict Zod objects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract definitions → runtime registry | Static server-side data; must not accept user input at registration time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-01 | Elevation of privilege | Unregistered actionType | mitigate | Registry lookup in Plan 03 rejects unknown types before card creation |
| T-180-02 | Tampering | Contract metadata drift | accept | Contracts are code-defined; changes require deploy |
| T-180-03 | Information disclosure | Risk copy templates | accept | Static pt-BR strings; no secrets in templates |
</threat_model>

<verification>
- `cd app && npx vitest run src/server/assistant/action-contracts/ -x` passes
- Registry exports both example contracts with `confirmationPolicy: "required"`
</verification>

<success_criteria>
- ACTION_CONTRACT_REGISTRY contains `quick_restyle` and `start_complete_campaign` with full ACT-02 metadata
- `registerActionContract` enables add-only extension for Phase 182
- Risk copy builder produces honest templates for missing optionals without blocking
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/180-action-contracts/180-01-SUMMARY.md`
</output>
