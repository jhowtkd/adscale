---
phase: 178-conversation-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "178-01"
requirements:
  - EXEC-02
files_modified:
  - app/src/server/repositories/assistant-thread.ts
  - app/src/server/repositories/assistant-thread.test.ts
autonomous: true
must_haves:
  truths:
    - "Server can create client-level threads (campaignId null) scoped by workspaceId + clientProfileId"
    - "Server can create multiple named campaign threads with deterministic default thread resolution"
    - "Client thread can be linked to a campaign via FK update without losing message history"
    - "Cross-workspace thread access returns null (404 at API layer)"
  artifacts:
    - path: app/src/server/repositories/assistant-thread.ts
      provides: "Thread CRUD, default resolution, campaign link"
      exports: ["createAssistantThread", "getAssistantThreadById", "listAssistantThreads", "getOrCreateDefaultCampaignThread", "linkThreadToCampaign"]
    - path: app/src/server/repositories/assistant-thread.test.ts
      provides: "Scoped thread tests"
  key_links:
    - from: app/src/server/repositories/assistant-thread.ts
      to: client_profiles
      via: "getClientProfile validation on create"
      pattern: "getClientProfile"
    - from: app/src/server/repositories/assistant-thread.ts
      to: campaigns
      via: "resolveCampaignClientProfileId alignment check"
      pattern: "resolveCampaignClientProfileId"
---

<objective>
Implement workspace-scoped assistant thread repository with client/campaign hierarchy, default thread semantics, and campaign link migration.

Purpose: Threads are the root entity for all conversation persistence; downstream message/action repos and APIs depend on these contracts.
Output: `assistant-thread.ts` repository + colocated Vitest tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-CONTEXT.md
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-RESEARCH.md
@app/src/server/repositories/client-reference.ts
@app/src/server/repositories/assistant-types.ts

<interfaces>
From `@/server/repositories/client-reference`:
```typescript
export async function getClientProfile(workspaceId: string, id: string);
export async function resolveCampaignClientProfileId(
  workspaceId: string,
  campaign: { id: string; client?: string | null; clientProfileId?: string | null }
): Promise<string | null>;
```

From plan 01 `assistant-types.ts`:
```typescript
export type ActionStatus = "pending" | "confirmed" | "running" | "completed" | "failed" | "canceled";
```
</interfaces>
</context>

<tasks>

<task id="178-02-01" type="auto" tdd="true">
  <name>Implement assistant thread repository</name>
  <files>app/src/server/repositories/assistant-thread.ts, app/src/server/repositories/assistant-thread.test.ts</files>
  <behavior>
    - createAssistantThread with clientProfileId only → campaignId null, validates profile belongs to workspace
    - createAssistantThread with campaignId → validates campaign belongs to workspace AND resolveCampaignClientProfileId matches clientProfileId
    - getAssistantThreadById(workspaceId, threadId) → returns null when workspace mismatch
    - listAssistantThreads(workspaceId, { clientProfileId, campaignId? }) → filters by both scope fields
    - getOrCreateDefaultCampaignThread(workspaceId, clientProfileId, campaignId) → returns existing isDefault=true thread or creates one named "Padrão" with isDefault=true
    - linkThreadToCampaign(workspaceId, threadId, campaignId) → sets campaign_id FK, sets migratedFromThreadId if previously client-level, validates profile alignment; does NOT copy messages
    - Setting new default clears previous isDefault on same campaign in same transaction
  </behavior>
  <action>
    Follow mocked-db Vitest pattern from `brand-kit.test.ts` (vi.hoisted state + db mock chain).

    Implement per locked decision D-01 (Cliente > Campanha > Thread):
    - Client-level threads: `campaignId = null`
    - Campaign threads: require `campaignId`; support `name` parameter
    - Default thread: use `isDefault` flag (NOT "most recent" heuristic) per RESEARCH Pitfall 5
    - Campaign link: FK update strategy with `migratedFromThreadId` audit per RESEARCH recommendation

    Every SELECT/UPDATE MUST include `eq(table.workspaceId, workspaceId)`.

    Export typed inputs: `CreateAssistantThreadInput`, `ListAssistantThreadsFilter`.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-thread.test.ts -x</automated>
  </verify>
  <done>All behavior cases pass; repository enforces workspace + clientProfileId scoping on every operation.</done>
</task>

<task id="178-02-02" type="auto">
  <name>Verify thread repository isolation edge cases</name>
  <files>app/src/server/repositories/assistant-thread.test.ts</files>
  <action>
    Add tests proving:
    - Thread create rejects clientProfileId from another workspace (404/null profile)
    - Campaign thread create rejects when campaign.clientProfileId mismatches supplied clientProfileId
    - linkThreadToCampaign rejects when campaign profile does not match thread's clientProfileId
    - getOrCreateDefaultCampaignThread is idempotent — second call returns same thread id

    These guard Phase 177 multi-profile isolation (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-thread.test.ts -x</automated>
  </verify>
  <done>Cross-profile and cross-workspace isolation tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → DB | Untrusted threadId/campaignId from future API routes must be workspace-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-03 | Elevation of privilege | getAssistantThreadById | mitigate | Always filter workspaceId; return null not error detail |
| T-178-04 | Elevation of privilege | Thread create without clientProfileId | mitigate | Require explicit clientProfileId; validate via getClientProfile |
| T-178-05 | Tampering | linkThreadToCampaign cross-profile | mitigate | Validate resolveCampaignClientProfileId matches thread.clientProfileId |
</threat_model>

<verification>
- `cd app && npx vitest run src/server/repositories/assistant-thread.test.ts -x`
</verification>

<success_criteria>
- Thread CRUD works for client-level and campaign-level contexts
- Default campaign thread resolution is deterministic via isDefault flag
- Client-to-campaign link preserves history via FK update
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/178-conversation-persistence/178-02-SUMMARY.md`
</output>
