---
phase: 178-conversation-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - "178-01"
requirements:
  - EXEC-02
files_modified:
  - app/src/server/repositories/assistant-message.ts
  - app/src/server/repositories/assistant-message.test.ts
  - app/src/server/repositories/assistant-action.ts
  - app/src/server/repositories/assistant-action.test.ts
autonomous: true
must_haves:
  truths:
    - "Messages append to a chronological stream ordered by sequence within a thread"
    - "Tool messages persist only toolName + sanitized summary — no raw args or reasoning"
    - "Action cards are messages linked 1:1 to action records with pending immutability"
    - "Action records transition through full lifecycle pending→confirmed→running→completed|failed|canceled"
    - "Failed/canceled actions update action_card payload in-place with safe user-facing error"
  artifacts:
    - path: app/src/server/repositories/assistant-message.ts
      provides: "Message stream persistence with sanitization guard"
      exports: ["createAssistantMessage", "listAssistantMessages", "updateActionCardPayload"]
    - path: app/src/server/repositories/assistant-action.ts
      provides: "Action record lifecycle"
      exports: ["createAssistantAction", "transitionAssistantAction", "confirmAssistantAction", "cancelAssistantAction"]
  key_links:
    - from: app/src/server/repositories/assistant-action.ts
      to: app/src/server/repositories/assistant-message.ts
      via: "updateActionCardPayload on status change"
      pattern: "updateActionCardPayload"
    - from: app/src/server/repositories/assistant-message.ts
      to: assistant-types.ts
      via: "PERSISTENCE_DENYLIST rejection"
      pattern: "PERSISTENCE_DENYLIST"
---

<objective>
Implement message stream and action record repositories with sanitization guards, chronological ordering, and full action lifecycle support.

Purpose: EXEC-02 requires persisted action status visible in thread history; action_card messages and action records must stay synchronized.
Output: `assistant-message.ts`, `assistant-action.ts`, and colocated tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-CONTEXT.md
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-RESEARCH.md
@app/src/server/repositories/assistant-types.ts

## Locked decisions

- D-02: Single chronological stream with types user|assistant|tool|action_card; assistant messages only after stream complete (caller contract — repo accepts final content only)
- D-02: Never persist reasoning/thinking; tool calls = name + sanitized summary only
- D-03: Pending cards immutable until confirm/cancel; failure updates card in-place with safeError
- D-04: jobRefs stored on action record; status mutations allowed on pending→confirmed→running→terminal states
</context>

<tasks>

<task id="178-03-01" type="auto" tdd="true">
  <name>Implement assistant message repository</name>
  <files>app/src/server/repositories/assistant-message.ts, app/src/server/repositories/assistant-message.test.ts</files>
  <behavior>
    - createAssistantMessage assigns monotonic sequence per thread (max+1)
    - listAssistantMessages(workspaceId, threadId) orders by sequence ASC; filters workspaceId AND threadId
    - Payload containing PERSISTENCE_DENYLIST keys throws before insert
    - tool type requires payload.toolName + payload.summary; rejects rawArgs
    - assistant type stores content only; empty reasoning fields rejected if nested in payload
    - updateActionCardPayload(workspaceId, messageId, patch) updates payload.status and display fields only
  </behavior>
  <action>
    Use mocked-db Vitest pattern. Denormalize workspaceId on insert — copy from thread lookup or accept as input validated against thread.

    For sequence: use transaction or `SELECT MAX(sequence)+1` pattern consistent with existing repos.

    Document in JSDoc: callers (Phase 179) must call createAssistantMessage for assistant type only after streaming completes — repository does not buffer partial streams.

    Export `CreateAssistantMessageInput` with discriminated union on `type` where applicable.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-message.test.ts -x</automated>
  </verify>
  <done>Message repository passes ordering, scoping, and sanitization tests.</done>
</task>

<task id="178-03-02" type="auto" tdd="true">
  <name>Implement assistant action repository</name>
  <files>app/src/server/repositories/assistant-action.ts, app/src/server/repositories/assistant-action.test.ts</files>
  <behavior>
    - createAssistantAction creates action record + linked action_card message atomically (message type action_card, payload.actionRecordId set)
    - inputSnapshot immutable after create — transitionAssistantAction rejects display/input mutations while pending except via confirm/cancel paths
    - confirmAssistantAction: pending→confirmed; adds confirmation as inline history (update action_card payload status, optional confirmation message NOT required — in-place card update per D-02)
    - cancelAssistantAction: pending→canceled with safeError optional
    - transitionAssistantAction validates ACTION_TRANSITIONS; running→completed|failed|canceled
    - failed transition stores safeError only — rejects error.stack or raw Inngest messages (map to generic user string)
    - jobRefs append-only array; duplicate kind+id ignored
    - Each status change calls updateActionCardPayload on linked messageId
  </behavior>
  <action>
    Implement `transitionAssistantAction(workspaceId, actionId, nextStatus, patch?)` as the central mutation primitive used by confirm, cancel, and job sync (plan 04).

    Pending immutability: while status=pending, only cancel allowed; inputSnapshot column cannot change.

    Use ACTION_TRANSITIONS from assistant-types.ts — throw typed `InvalidActionTransitionError` on illegal transitions.

    All queries filter workspaceId.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-action.test.ts -x</automated>
  </verify>
  <done>Full lifecycle transitions pass; pending immutability and safeError mapping enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Message write boundary | Untrusted payloads from future orchestration/API must be sanitized before DB insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-06 | Information disclosure | Tool/assistant payloads | mitigate | PERSISTENCE_DENYLIST check at createAssistantMessage |
| T-178-07 | Information disclosure | safeError on failed actions | mitigate | Map internal errors to generic strings; log full error server-side only |
| T-178-08 | Tampering | Pending card input mutation | mitigate | Reject inputSnapshot updates when status=pending |
| T-178-09 | Repudiation | Action lifecycle | mitigate | Immutable inputSnapshot + status timestamps on action records |
</threat_model>

<verification>
- `cd app && npx vitest run src/server/repositories/assistant-message.test.ts -x`
- `cd app && npx vitest run src/server/repositories/assistant-action.test.ts -x`
</verification>

<success_criteria>
- Chronological message stream with typed entries and sanitization guards
- Action records with full lifecycle and synchronized action_card messages
- Pending cards immutable until confirm or cancel
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/178-conversation-persistence/178-03-SUMMARY.md`
</output>
