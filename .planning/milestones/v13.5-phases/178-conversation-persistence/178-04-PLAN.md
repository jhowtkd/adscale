---
phase: 178-conversation-persistence
plan: 04
type: execute
wave: 3
depends_on:
  - "178-02"
  - "178-03"
requirements:
  - EXEC-02
files_modified:
  - app/src/server/repositories/assistant-job-sync.ts
  - app/src/server/repositories/assistant-job-sync.test.ts
  - app/src/server/jobs/derivation.ts
  - app/src/server/jobs/derivation.test.ts
  - app/src/app/api/assistant/threads/route.ts
  - app/src/app/api/assistant/threads/[threadId]/route.ts
  - app/src/app/api/assistant/threads/[threadId]/messages/route.ts
  - app/src/app/api/assistant/threads/[threadId]/link-campaign/route.ts
  - app/src/app/api/assistant/actions/[actionId]/confirm/route.ts
  - app/src/app/api/assistant/actions/[actionId]/cancel/route.ts
  - app/src/app/api/assistant/threads/[threadId]/route.test.ts
autonomous: true
must_haves:
  truths:
    - "derivationJob updates assistant action status when assistantActionId is present in event data"
    - "REST API can create/list threads, read thread+messages, append messages, confirm/cancel actions"
    - "GET thread returns action_card messages with current job status from persisted payload"
    - "Long-running derivation correlates via jobRef { kind: derivation, id: derivationId }"
  artifacts:
    - path: app/src/server/repositories/assistant-job-sync.ts
      provides: "syncAssistantActionFromJob helper"
      exports: ["syncAssistantActionFromJob"]
    - path: app/src/app/api/assistant/threads/[threadId]/route.ts
      provides: "Thread detail + message history API"
      exports: ["GET"]
    - path: app/src/server/jobs/derivation.ts
      provides: "Optional assistant action sync in mark-processing/completed/failed steps"
  key_links:
    - from: app/src/server/jobs/derivation.ts
      to: app/src/server/repositories/assistant-job-sync.ts
      via: "syncAssistantActionFromJob in step.run blocks"
      pattern: "syncAssistantActionFromJob"
    - from: app/src/app/api/assistant/threads/[threadId]/route.ts
      to: app/src/server/repositories/assistant-message.ts
      via: "listAssistantMessages"
      pattern: "listAssistantMessages"
---

<objective>
Wire Inngest job status into assistant action records and expose REST APIs for thread/message/action operations.

Purpose: EXEC-02 requires long-running pipeline jobs to reflect status in the assistant thread; downstream phases 179–181 consume these APIs.
Output: `assistant-job-sync.ts`, derivation job wiring, REST routes under `/api/assistant/`, route tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-CONTEXT.md
@.planning/milestones/v13.5-phases/178-conversation-persistence/178-RESEARCH.md
@app/src/server/jobs/derivation.ts
@app/src/app/api/client-profiles/[id]/references/route.ts
@app/src/server/repositories/assistant-action.ts
@app/src/server/repositories/assistant-thread.ts

<interfaces>
From plan 02–03 repositories:
```typescript
export async function getAssistantThreadById(workspaceId: string, threadId: string);
export async function listAssistantThreads(workspaceId: string, filter: ListAssistantThreadsFilter);
export async function createAssistantThread(workspaceId: string, input: CreateAssistantThreadInput);
export async function getOrCreateDefaultCampaignThread(workspaceId: string, clientProfileId: string, campaignId: string);
export async function linkThreadToCampaign(workspaceId: string, threadId: string, campaignId: string);
export async function listAssistantMessages(workspaceId: string, threadId: string);
export async function createAssistantMessage(workspaceId: string, input: CreateAssistantMessageInput);
export async function confirmAssistantAction(workspaceId: string, actionId: string);
export async function cancelAssistantAction(workspaceId: string, actionId: string);
export async function transitionAssistantAction(workspaceId: string, actionId: string, nextStatus: ActionStatus, patch?);
```

Existing Inngest pattern from derivation.ts:
```typescript
await step.run("mark-processing", async () => { /* update derivations.status */ });
await step.run("mark-completed", async () => { /* ... */ });
await step.run("mark-failed", async () => { /* ... */ });
```
</interfaces>
</context>

<tasks>

<task id="178-04-01" type="auto" tdd="true">
  <name>Implement job sync and wire derivationJob</name>
  <files>app/src/server/repositories/assistant-job-sync.ts, app/src/server/repositories/assistant-job-sync.test.ts, app/src/server/jobs/derivation.ts, app/src/server/jobs/derivation.test.ts</files>
  <behavior>
    - syncAssistantActionFromJob({ workspaceId, actionId, status, jobRef?, safeError? }) maps derivation statuses: processing→running, completed→completed, failed→failed
    - Appends jobRef to action record if not duplicate
    - Calls transitionAssistantAction + updateActionCardPayload
    - derivationJob: when event.data.assistantActionId present, call sync in mark-processing, mark-completed, and mark-failed/onFailure step.run blocks
    - derivationJob test: mock syncAssistantActionFromJob; assert called with correct status when assistantActionId set; not called when absent
  </behavior>
  <action>
    Implement `syncAssistantActionFromJob` in `assistant-job-sync.ts` — thin wrapper over `transitionAssistantAction` with status mapping and safeError sanitization (RESEARCH Pitfall 4: never persist error.stack).

    Extend `derivation.generate` event data type with optional `assistantActionId?: string` (do not break existing senders).

    Wire into derivation.ts inside existing step.run blocks AFTER derivation status update — same durability guarantees.

    Extend `derivation.test.ts` with `-t assistant` describe block per VALIDATION.md.

    jobRef uses domain ID: `{ kind: "derivation", id: derivationId }` per RESEARCH Assumption A1.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-job-sync.test.ts src/server/jobs/derivation.test.ts -x -t assistant</automated>
  </verify>
  <done>Job sync helper tested; derivationJob calls sync when assistantActionId provided.</done>
</task>

<task id="178-04-02" type="auto" tdd="true">
  <name>Expose assistant REST API routes</name>
  <files>app/src/app/api/assistant/threads/route.ts, app/src/app/api/assistant/threads/[threadId]/route.ts, app/src/app/api/assistant/threads/[threadId]/messages/route.ts, app/src/app/api/assistant/threads/[threadId]/link-campaign/route.ts, app/src/app/api/assistant/actions/[actionId]/confirm/route.ts, app/src/app/api/assistant/actions/[actionId]/cancel/route.ts, app/src/app/api/assistant/threads/[threadId]/route.test.ts</files>
  <behavior>
    - GET/POST /api/assistant/threads — list/create; query/body requires clientProfileId; optional campaignId, name, isDefault
    - GET /api/assistant/threads/[threadId] — returns { thread, messages } with action_card payload.status current
    - POST /api/assistant/threads/[threadId]/messages — append message (Zod validated type + content + payload)
    - POST /api/assistant/threads/[threadId]/link-campaign — body { campaignId }; calls linkThreadToCampaign
    - POST /api/assistant/actions/[actionId]/confirm — confirm pending action
    - POST /api/assistant/actions/[actionId]/cancel — cancel pending action
    - All routes use requireWorkspaceAccess + handleApiError; 404 for missing scoped resources
    - Route test: GET thread returns 404 when thread not in workspace
  </behavior>
  <action>
    Follow API pattern from `client-profiles/[id]/references/route.ts`: requireWorkspaceAccess, Zod safeParse, apiError codes, handleApiError.

    POST threads with campaignId + isDefault=true should delegate to getOrCreateDefaultCampaignThread when appropriate (CHAT-04 prep per D-01).

    POST messages: validate type enum; repository sanitization is second line of defense.

    Do NOT build UI — backend only per phase boundary.

    Role gates: use requireWorkspaceAccess only (no owner gate) per RESEARCH open question recommendation; Phase 180 adds confirmation policy.

    Route tests mock repositories like `references/route.test.ts`.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/app/api/assistant/threads/\\[threadId\\]/route.test.ts -x</automated>
  </verify>
  <done>All assistant API routes respond with correct auth, validation, and scoping; route test passes.</done>
</task>

<task id="178-04-03" type="auto">
  <name>Phase verification sweep</name>
  <files>app/src/server/repositories/assistant-thread.test.ts, app/src/server/repositories/assistant-message.test.ts, app/src/server/repositories/assistant-action.test.ts</files>
  <action>
    Run all assistant-targeted tests plus build and drizzle-kit check. Fix any integration issues between job sync, repositories, and routes.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/server/repositories/assistant-thread.test.ts src/server/repositories/assistant-message.test.ts src/server/repositories/assistant-action.test.ts src/server/repositories/assistant-job-sync.test.ts -x && npm run build && npx drizzle-kit check</automated>
  </verify>
  <done>All assistant tests green; build and migration check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API routes | Untrusted JSON bodies; workspace from session only |
| Inngest worker → action records | Event data assistantActionId must be scoped to workspaceId in sync helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-10 | Elevation of privilege | Thread/action API routes | mitigate | requireWorkspaceAccess; scope all repo calls with workspace.id |
| T-178-11 | Tampering | Action confirm/cancel | mitigate | Verify action.workspaceId matches; reject cross-workspace actionId |
| T-178-12 | Information disclosure | GET thread+messages | mitigate | 404 for missing threads; no cross-workspace leakage |
| T-178-13 | Information disclosure | Job failure sync | mitigate | safeError mapping in syncAssistantActionFromJob |
</threat_model>

<verification>
- `cd app && npx vitest run src/server/repositories/assistant-job-sync.test.ts -x`
- `cd app && npx vitest run src/server/jobs/derivation.test.ts -x -t assistant`
- `cd app && npx vitest run src/app/api/assistant/threads/\[threadId\]/route.test.ts -x`
- `cd app && npm run build && npx drizzle-kit check`
</verification>

<success_criteria>
- derivationJob reflects assistant action status when assistantActionId present (EXEC-02)
- REST APIs expose scoped thread/message/action operations for phases 179–181
- Thread history includes action_card entries with current job status
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/178-conversation-persistence/178-04-SUMMARY.md`
</output>
