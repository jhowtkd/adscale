---
phase: 181-assistant-surface
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
requirements:
  - CHAT-02
  - CHAT-03
files_modified:
  - app/src/components/assistant/AssistantTreeSidebar.tsx
  - app/src/components/assistant/AssistantTreeSidebar.test.tsx
  - app/src/components/assistant/AssistantEmptyState.tsx
  - app/src/components/assistant/AssistantCreateClientDialog.tsx
  - app/src/components/assistant/AssistantCreateCampaignDialog.tsx
  - app/src/app/(dashboard)/assistant/page.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
must_haves:
  truths:
    - "User sees Client → Campaign → Thread tree in left sidebar"
    - "Selecting a thread updates URL ?threadId= for deep links"
    - "Without thread selected, guided empty state prompts tree selection or creation"
    - "User can create client profile, campaign draft, and new thread from assistant flow"
  artifacts:
    - path: app/src/components/assistant/AssistantTreeSidebar.tsx
      provides: "Hierarchical navigation tree"
      exports: ["AssistantTreeSidebar"]
    - path: app/src/components/assistant/AssistantEmptyState.tsx
      provides: "Guided CTA when no thread selected"
    - path: app/src/components/assistant/AssistantCreateClientDialog.tsx
      provides: "Modal create client (CHAT-03)"
    - path: app/src/components/assistant/AssistantCreateCampaignDialog.tsx
      provides: "Modal create campaign draft with clientProfileId (CHAT-03)"
  key_links:
    - from: app/src/components/assistant/AssistantTreeSidebar.tsx
      to: app/src/lib/hooks/use-assistant-threads.ts
      via: "useAssistantThreads per expanded node"
      pattern: "useAssistantThreads"
    - from: app/src/components/assistant/AssistantTreeSidebar.tsx
      to: app/src/lib/hooks/use-client-profiles.ts
      via: "useClientProfiles root nodes"
      pattern: "useClientProfiles"
    - from: app/src/components/assistant/AssistantTreeSidebar.tsx
      to: app/src/lib/hooks/use-campaigns.ts
      via: "client-side filter by clientProfileId"
      pattern: "clientProfileId"
    - from: app/src/app/(dashboard)/assistant/page.tsx
      to: URL searchParams
      via: "router.replace ?threadId="
      pattern: "threadId"
---

<objective>
Implement CHAT-02 navigation tree and CHAT-03 creation flows: client/campaign/thread hierarchy, URL deep links, guided empty state, and modal create actions.

Purpose: Users orient by Cliente > Campanha > Thread and can bootstrap new entities without leaving assistant surface.
Output: Tree sidebar, empty state, create dialogs, wired assistant page sidebar slot.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/181-assistant-surface/181-CONTEXT.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-RESEARCH.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-01-SUMMARY.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-02-SUMMARY.md
@app/src/lib/hooks/use-client-profiles.ts
@app/src/lib/hooks/use-campaigns.ts
@app/src/lib/hooks/use-assistant-threads.ts

## Locked decisions

- **D-CHAT-02:** Tree Cliente → Campanha → Thread; multiple named threads per campaign
- **D-CHAT-03:** Create client, campaign draft, start thread from assistant; modal pattern per RESEARCH
- **D-empty:** No thread → guided CTA; chat input disabled until context selected (enforced in Plan 04)
- **RESEARCH:** Filter campaigns client-side from `useCampaigns({ limit: 100 })`
- **RESEARCH:** Deep link `/assistant?threadId={uuid}`

<interfaces>
From hooks (Plan 01):

```typescript
export function useAssistantThreads(clientProfileId, campaignId?): UseQueryResult;
export function useCreateAssistantThread(): UseMutationResult;
export function useCreateClientProfile(): UseMutationResult;
export function useCreateCampaign(): UseMutationResult;
export function useCampaigns(filters?: { limit?: number }): UseQueryResult;
```
</interfaces>
</context>

<tasks>

<task id="181-03-01" type="auto" tdd="true">
  <name>Assistant tree sidebar with URL thread selection</name>
  <files>app/src/components/assistant/AssistantTreeSidebar.tsx, app/src/components/assistant/AssistantTreeSidebar.test.tsx</files>
  <behavior>
    - Renders expandable client nodes from useClientProfiles
    - Under each client, campaigns filtered by clientProfileId
    - Under each campaign, threads from useAssistantThreads(clientId, campaignId)
    - Client-level threads via useAssistantThreads(clientId, null) — avoid mixing campaign threads (Pitfall 3)
    - Clicking thread calls onSelectThread(id) and router.replace with ?threadId=
    - Highlights active thread from URL param
  </behavior>
  <action>
    Build `AssistantTreeSidebar` with props `{ selectedThreadId?: string; onSelectThread: (id: string) => void }`.

    Progressive expansion: track expanded client/campaign ids in component state.

    Thread list buttons show thread.name; default campaign thread visually indicated if `thread.isDefault`.

    Header actions: "+" opens create dialogs (wired in task 181-03-03).

    Test with mocked hooks: selecting thread invokes router.replace with threadId query.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/AssistantTreeSidebar.test.tsx -x</automated>
  </verify>
  <done>Tree renders three levels and syncs selection to URL.</done>
</task>

<task id="181-03-02" type="auto">
  <name>Guided empty state without thread context</name>
  <files>app/src/components/assistant/AssistantEmptyState.tsx, app/messages/en.json, app/messages/pt-BR.json</files>
  <action>
    `AssistantEmptyState` — centered in main chat column when `!threadId`:

    - Headline + body explaining select client/campaign in tree or create new
    - Primary CTA scrolls/focuses tree (or expands first client if none)
    - Secondary CTA opens create client dialog

    Add i18n keys: `assistant.empty.title`, `assistant.empty.body`, `assistant.empty.selectTree`, `assistant.empty.createClient`.

    Wire in `assistant/page.tsx` main slot: if no threadId show EmptyState; pass threadId from searchParams.

    Do NOT enable chat input here — Plan 04 gates input on threadId.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/AssistantTreeSidebar.test.tsx -x</automated>
  </verify>
  <done>Empty state displays when no threadId; i18n strings added.</done>
</task>

<task id="181-03-03" type="auto" tdd="true">
  <name>Create client, campaign draft, and thread mutations</name>
  <files>app/src/components/assistant/AssistantCreateClientDialog.tsx, app/src/components/assistant/AssistantCreateCampaignDialog.tsx, app/src/app/(dashboard)/assistant/page.tsx</files>
  <behavior>
    - Create client: useCreateClientProfile with name field → on success expand tree to new client
    - Create campaign: requires selected clientProfileId from tree context; useCreateCampaign with draft defaults matching campaigns/new pattern
    - New thread: useCreateAssistantThread with clientProfileId + optional campaignId + name → navigate to ?threadId=newId
    - Campaign draft always passes clientProfileId (Pitfall 6)
  </behavior>
  <action>
    Per D-CHAT-03 and RESEARCH (modal pattern):

    `AssistantCreateClientDialog` — Dialog with name input, submit via useCreateClientProfile.

    `AssistantCreateCampaignDialog` — requires `clientProfileId` prop; name field; `useCreateCampaign` with `{ clientProfileId, name, status: "draft" }` or equivalent existing bootstrap shape from `campaigns/new/page.tsx`.

    Tree header: "New client" / "New campaign" (campaign disabled until client selected) / "New thread" (requires client).

    On thread create success: `router.replace(`/assistant?threadId=${thread.id}`)`.

    Extend `assistant/page.tsx` to compose AssistantShell slots: sidebar=TreeSidebar, main=EmptyState or chat placeholder.

    Add tests in TreeSidebar test file for create-thread mutation chain mock OR dedicated test if cleaner.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/lib/hooks/use-assistant-threads.test.tsx -x</automated>
  </verify>
  <done>CHAT-03 creation chain works; page composes tree + empty state in shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Create dialogs → API | User-supplied names validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-07 | Tampering | create campaign body | mitigate | Always include clientProfileId from tree selection; server validates ownership |
| T-181-08 | Elevation | thread list scoping | transfer | Server scopes threads by workspaceId on GET |
</threat_model>

<verification>
- `cd app && npx vitest run src/components/assistant/AssistantTreeSidebar.test.tsx src/lib/hooks/use-assistant-threads.test.tsx -x`
</verification>

<success_criteria>
- CHAT-02 tree navigation with URL deep links
- CHAT-03 create client, campaign draft, and thread from assistant
- Guided empty state when no thread selected
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/181-assistant-surface/181-03-SUMMARY.md`
</output>
