---
phase: 181-assistant-surface
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
requirements:
  - CHAT-01
  - CHAT-02
files_modified:
  - app/src/components/assistant/AssistantChatCore.tsx
  - app/src/components/assistant/AssistantChatCore.test.tsx
  - app/src/components/assistant/AssistantMessageList.tsx
  - app/src/components/assistant/AssistantChatInput.tsx
  - app/src/components/assistant/AssistantActionCard.tsx
  - app/src/components/assistant/AssistantContextPanel.tsx
  - app/src/lib/assistant/contract-display.ts
  - app/src/app/(dashboard)/assistant/page.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
must_haves:
  truths:
    - "User sees chat history and can send messages with SSE streaming in center column"
    - "Action cards render inline with risk copy, credit impact, confirmation policy from contract display payload"
    - "User can confirm or cancel pending action cards"
    - "Context panel shows contract readiness hints, suggested actions, and long-job status from action_card messages"
    - "Assistant text streams without showing reasoning/thinking (AI-05)"
    - "Chat input disabled until threadId selected"
  artifacts:
    - path: app/src/components/assistant/AssistantChatCore.tsx
      provides: "Shared chat surface for full page and drawer"
      exports: ["AssistantChatCore"]
    - path: app/src/components/assistant/AssistantActionCard.tsx
      provides: "Inline action card with confirm/cancel"
    - path: app/src/components/assistant/AssistantContextPanel.tsx
      provides: "Right panel contextual data"
    - path: app/src/lib/assistant/contract-display.ts
      provides: "Client display helpers for action_card.display payload"
  key_links:
    - from: app/src/components/assistant/AssistantChatCore.tsx
      to: app/src/lib/hooks/use-assistant-chat.ts
      via: "sendMessage + streaming state"
      pattern: "useAssistantChat"
    - from: app/src/components/assistant/AssistantActionCard.tsx
      to: app/src/lib/hooks/use-assistant-actions.ts
      via: "confirm/cancel mutations"
      pattern: "useConfirmAssistantAction|useCancelAssistantAction"
    - from: app/src/components/assistant/AssistantContextPanel.tsx
      to: app/src/lib/hooks/use-assistant-thread.ts
      via: "poll thread while cards pending/running"
      pattern: "useAssistantThread"
---

<objective>
Build the chat center column and contextual right panel: shared `AssistantChatCore`, SSE message rendering, inline action cards with contract metadata, and context panel.

Purpose: Complete the Codex three-column assistant surface (CHAT-02 chat + contextual panel); `AssistantChatCore` is reused by campaign drawer in Plan 05 (CHAT-04).
Output: Chat components, contract display helpers, fully wired `/assistant` page.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/181-assistant-surface/181-CONTEXT.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-RESEARCH.md
@app/src/server/assistant/action-contracts/validate.ts
@app/src/server/repositories/assistant-types.ts

## Locked decisions

- **D-SSE:** Events text_delta, tool_summary, action_card, done, error
- **D-action-card:** Inline in history; confirm/cancel via APIs 178; show contract 180 metadata (riskCopyLines, creditImpact, confirmationPolicy, riskLabel)
- **D-context-panel:** Contract readiness, suggested actions, job status — NO full creative review (deferred 183)
- **D-AI-05:** No reasoning/thinking display
- **D-empty-input:** Disable chat until threadId set (Pitfall 4)

<interfaces>
Action card display shape from `validate.ts` propose path:

```typescript
display: {
  label: string;
  actionType: string;
  riskLabel: RiskLabel;
  creditImpact: CreditImpact;
  riskCopyLines: string[];
  confirmationPolicy: ConfirmationPolicy;
}
```

From `use-assistant-chat.ts` (Plan 01):

```typescript
export function useAssistantChat(threadId: string | null): {
  messages: ...;
  streamingText: string;
  sendMessage: (text: string) => Promise<void>;
  isStreaming: boolean;
  error: string | null;
};
```
</interfaces>
</context>

<tasks>

<task id="181-04-01" type="auto" tdd="true">
  <name>AssistantChatCore with message list and input</name>
  <files>app/src/components/assistant/AssistantChatCore.tsx, app/src/components/assistant/AssistantChatCore.test.tsx, app/src/components/assistant/AssistantMessageList.tsx, app/src/components/assistant/AssistantChatInput.tsx</files>
  <behavior>
    - variant 'full' | 'drawer' adjusts padding/height only — same logic
    - Loads history via useAssistantThread(threadId)
    - sendMessage disabled when !threadId or isStreaming
    - Renders user/assistant/tool messages; assistant text plain (no dangerouslySetInnerHTML)
    - Streaming assistant bubble shows accumulating text_delta
    - tool_summary renders compact summary line
  </behavior>
  <action>
    `AssistantChatCore` props: `{ threadId: string | null; variant?: 'full' | 'drawer'; onClose?: () => void }`.

    `AssistantMessageList` — maps messages; delegates action_card type to AssistantActionCard (task 181-04-02).

    `AssistantChatInput` — textarea + send button; disabled states with i18n hint when no thread.

    Integrate `useAssistantChat(threadId)` for send + live streaming overlay on history.

    `AssistantChatCore.test.tsx`: mock hooks; assert input disabled without threadId; assert send calls hook.

    Do NOT render reasoning/thinking fields if present in payloads.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/AssistantChatCore.test.tsx -x</automated>
  </verify>
  <done>Chat core renders history, streams text, gates input on threadId.</done>
</task>

<task id="181-04-02" type="auto" tdd="true">
  <name>Inline action cards with confirm/cancel</name>
  <files>app/src/components/assistant/AssistantActionCard.tsx, app/src/lib/assistant/contract-display.ts, app/src/lib/hooks/use-assistant-actions.test.tsx</files>
  <behavior>
    - Displays label, riskLabel badge, creditImpact label, riskCopyLines list, confirmationPolicy
    - pending: show Confirm + Cancel buttons
    - confirmed/running: show status + disable confirm
    - completed/failed/canceled: terminal UI, no confirm button (Pitfall 5)
    - Confirm/cancel call mutations with actionRecordId
  </behavior>
  <action>
    `contract-display.ts` — parse `ActionCardMessagePayload.display` safely:

    - `formatCreditImpact(creditImpact)` → human string
    - `getRiskLabelVariant(riskLabel)` → badge variant
    - Do NOT import server contracts client-side

    `AssistantActionCard` — props from message payload + threadId for invalidation.

    Wire confirm/cancel via `useConfirmAssistantAction` / `useCancelAssistantAction`.

    Poll/refetch: parent ChatCore or ActionCard triggers `useAssistantThread` refetchInterval while any card status is pending|running (use STALE_TIME.REALTIME pattern from project).

    Extend actions hook tests if needed for invalidation behavior.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/lib/hooks/use-assistant-actions.test.tsx -x</automated>
  </verify>
  <done>Action cards show contract metadata and respect terminal states.</done>
</task>

<task id="181-04-03" type="auto">
  <name>Context panel and full page composition</name>
  <files>app/src/components/assistant/AssistantContextPanel.tsx, app/src/app/(dashboard)/assistant/page.tsx, app/messages/en.json, app/messages/pt-BR.json</files>
  <action>
    `AssistantContextPanel` — props `{ threadId: string | null }`:

    - Uses `useAssistantThread(threadId)` data
    - **Contract readiness:** from latest pending action_card `display.riskCopyLines` + `display.label` (missing optional fields copy — no server contract import)
    - **Suggested actions:** list recent action_card messages with pending status
    - **Job status:** for cards in running/confirmed, show status + job ref from message payload if present; poll thread until terminal

    No creative review UI — text/status only per D-context-panel.

    Update `assistant/page.tsx` final composition:

    ```tsx
    <AssistantShell
      sidebar={<AssistantTreeSidebar ... />}
      main={threadId ? <AssistantChatCore threadId={threadId} variant="full" /> : <AssistantEmptyState ... />}
      contextPanel={<AssistantContextPanel threadId={threadId} />}
    />
    ```

    Add remaining i18n keys for chat input, action card buttons, context panel sections.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/AssistantChatCore.test.tsx src/components/assistant/AssistantShell.test.tsx -x</automated>
  </verify>
  <done>Full /assistant page: tree + chat + context panel integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model output → DOM | Assistant message text and card display payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-09 | Tampering | XSS via assistant text | mitigate | Render assistant content as plain text; no dangerouslySetInnerHTML on model output |
| T-181-10 | Spoofing | confirm button | mitigate | Server revalidates on confirm; UI only POSTs actionRecordId |
| T-181-11 | Information disclosure | reasoning fields | mitigate | Do not render reasoning/thinking (AI-05); server denylist already strips persistence |
</threat_model>

<verification>
- `cd app && npx vitest run src/components/assistant/AssistantChatCore.test.tsx src/lib/hooks/use-assistant-actions.test.tsx src/lib/assistant/parse-sse.test.ts -x`
</verification>

<success_criteria>
- Desktop three-column assistant fully functional for chat
- SSE streaming and action cards work end-to-end against APIs
- Context panel shows contract/job status without business logic duplication
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/181-assistant-surface/181-04-SUMMARY.md`
</output>
