---
phase: 181-assistant-surface
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - CHAT-02
  - CHAT-03
  - CHAT-04
files_modified:
  - app/src/lib/assistant/parse-sse.ts
  - app/src/lib/assistant/parse-sse.test.ts
  - app/src/lib/hooks/use-assistant-threads.ts
  - app/src/lib/hooks/use-assistant-threads.test.tsx
  - app/src/lib/hooks/use-assistant-actions.ts
  - app/src/lib/hooks/use-assistant-actions.test.tsx
  - app/src/lib/hooks/use-assistant-chat.ts
  - app/src/lib/hooks/use-assistant-chat.test.tsx
autonomous: true
must_haves:
  truths:
    - "Client can list threads scoped by clientProfileId and optional campaignId"
    - "Client can fetch a thread with messages for chat history"
    - "Client can create threads including default campaign thread via POST isDefault"
    - "Client can stream assistant chat via POST SSE parsing all ASSISTANT_SSE_EVENTS"
    - "Client can confirm and cancel action cards via authenticated POST mutations"
  artifacts:
    - path: app/src/lib/assistant/parse-sse.ts
      provides: "SSE frame parser matching encodeAssistantSseEvent wire format"
      exports: ["readAssistantSseStream", "parseAssistantSseFrame"]
    - path: app/src/lib/hooks/use-assistant-threads.ts
      provides: "Thread list, detail, create hooks"
      exports: ["useAssistantThreads", "useAssistantThread", "useCreateAssistantThread"]
    - path: app/src/lib/hooks/use-assistant-chat.ts
      provides: "Streaming send message hook with AbortController"
      exports: ["useAssistantChat"]
    - path: app/src/lib/hooks/use-assistant-actions.ts
      provides: "Confirm/cancel mutations"
      exports: ["useConfirmAssistantAction", "useCancelAssistantAction"]
  key_links:
    - from: app/src/lib/hooks/use-assistant-chat.ts
      to: app/src/lib/assistant/parse-sse.ts
      via: "readAssistantSseStream on fetch response body"
      pattern: "readAssistantSseStream"
    - from: app/src/lib/hooks/use-assistant-threads.ts
      to: /api/assistant/threads
      via: "apiFetch GET/POST"
      pattern: "api/assistant/threads"
    - from: app/src/lib/hooks/use-assistant-actions.ts
      to: /api/assistant/actions
      via: "confirm/cancel POST"
      pattern: "api/assistant/actions/.*/(confirm|cancel)"
---

<objective>
Build the assistant client data layer: SSE parser, thread CRUD hooks, streaming chat hook, and confirm/cancel action mutations.

Purpose: All UI surfaces (full `/assistant` and campaign drawer) share one tested API integration layer; avoids duplicate SSE or fetch logic per CHAT-04.
Output: `lib/assistant/parse-sse.ts` + four React Query hooks with unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/181-assistant-surface/181-CONTEXT.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-RESEARCH.md
@app/src/lib/hooks/use-client-profiles.ts
@app/src/lib/api-client.ts
@app/src/server/assistant/stream/sse.ts
@app/src/app/api/assistant/threads/route.ts
@app/src/app/api/assistant/threads/[threadId]/route.ts
@app/src/app/api/assistant/threads/[threadId]/chat/route.test.ts

<interfaces>
From `app/src/server/assistant/stream/sse.ts`:

```typescript
export const ASSISTANT_SSE_EVENTS = [
  "text_delta", "tool_summary", "action_card", "done", "error",
] as const;
export type AssistantSseEventType = (typeof ASSISTANT_SSE_EVENTS)[number];
export function encodeAssistantSseEvent(event, data): Uint8Array;
```

From `app/src/server/repositories/assistant-types.ts`:

```typescript
export type ActionStatus = "pending" | "confirmed" | "running" | "completed" | "failed" | "canceled";
export interface ActionCardMessagePayload {
  actionRecordId: string;
  status: ActionStatus;
  display: Record<string, unknown>;
}
```

From `app/src/lib/hooks/use-client-profiles.ts` (hook pattern):

```typescript
export function useClientProfiles() { /* useQuery + apiFetch */ }
export function useCreateClientProfile() { /* useMutation */ }
```

Threads API contracts [VERIFIED: threads/route.ts]:
- `GET /api/assistant/threads?clientProfileId={uuid}&campaignId={uuid?}`
- `POST /api/assistant/threads` body: `{ clientProfileId, campaignId?, name?, isDefault? }`
- `GET /api/assistant/threads/[threadId]` → `{ thread, messages }`
- `POST /api/assistant/threads/[threadId]/chat` → SSE stream
- `POST /api/assistant/actions/[actionId]/confirm` | `/cancel`
</interfaces>
</context>

<tasks>

<task id="181-01-01" type="auto" tdd="true">
  <name>SSE frame parser for assistant chat stream</name>
  <files>app/src/lib/assistant/parse-sse.ts, app/src/lib/assistant/parse-sse.test.ts</files>
  <behavior>
    - Parses `event: text_delta\ndata: {"text":"hi"}\n\n` into `{ event, data }`
    - Handles multiple frames in one chunk and partial frames across chunks
    - Covers all ASSISTANT_SSE_EVENTS: text_delta, tool_summary, action_card, done, error
    - Ignores/does not surface reasoning or thinking fields if present in data (AI-05)
    - readAssistantSseStream async generator aborts when AbortSignal fires
  </behavior>
  <action>
    Create `parse-sse.ts` mirroring the collector in `chat/route.test.ts` `collectSseBody` / RESEARCH Pattern 3:

    - `parseAssistantSseFrame(block: string)` → `{ event: AssistantSseEventType, data: Record<string, unknown> } | null`
    - `readAssistantSseStream(response: Response, signal?: AbortSignal)` async generator yielding parsed frames
    - Use `ReadableStream` reader + buffer for incomplete frames
    - Do NOT add `@microsoft/fetch-event-source` unless parser proves insufficient (RESEARCH resolved: hand-roll first)

    Write `parse-sse.test.ts` with Uint8Array chunks simulating multi-frame bodies; include abort test with AbortController.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/lib/assistant/parse-sse.test.ts -x</automated>
  </verify>
  <done>Parser round-trips all five SSE event types; abort stops iteration.</done>
</task>

<task id="181-01-02" type="auto" tdd="true">
  <name>Thread list, detail, and create hooks</name>
  <files>app/src/lib/hooks/use-assistant-threads.ts, app/src/lib/hooks/use-assistant-threads.test.tsx</files>
  <behavior>
    - useAssistantThreads(clientProfileId, campaignId?) calls GET with correct query params
    - When campaignId is explicitly null, request distinguishes client-level threads (pass campaignId as omitted or API-accepted null sentinel per route behavior)
    - useAssistantThread(threadId) returns thread + messages; disabled when threadId undefined
    - useCreateAssistantThread posts body and invalidates thread list queries
    - create with { clientProfileId, campaignId, isDefault: true } supports CHAT-04 default thread
  </behavior>
  <action>
    Follow `use-client-profiles.ts` patterns: `apiFetch`, `STALE_TIME`, query keys under `["assistant","threads",...]`.

    Export types: `AssistantThread`, `AssistantMessage` (align with API JSON shapes).

  `useAssistantThreads(clientProfileId: string | null, campaignId?: string | null)` — enabled only when clientProfileId set.

  `useAssistantThread(threadId: string | null)` — `GET /api/assistant/threads/${threadId}`.

  `useCreateAssistantThread()` — mutation `POST /api/assistant/threads`; onSuccess invalidate `["assistant","threads"]`.

    Mock `apiFetch` in tests; verify URL construction and mutation invalidation. Cover CHAT-03 chain prerequisite: create thread after client/campaign exists.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/lib/hooks/use-assistant-threads.test.tsx -x</automated>
  </verify>
  <done>Thread hooks fetch and create against assistant APIs with tests green.</done>
</task>

<task id="181-01-03" type="auto" tdd="true">
  <name>Streaming chat and action confirm/cancel hooks</name>
  <files>app/src/lib/hooks/use-assistant-chat.ts, app/src/lib/hooks/use-assistant-chat.test.tsx, app/src/lib/hooks/use-assistant-actions.ts, app/src/lib/hooks/use-assistant-actions.test.tsx</files>
  <behavior>
    - useAssistantChat.sendMessage posts to chat endpoint, accumulates text_delta, upserts action_card events, finalizes on done
    - sendMessage accepts AbortSignal; cleanup aborts in-flight stream on unmount
    - On error event, exposes error message without throwing uncaught
    - useConfirmAssistantAction / useCancelAssistantAction POST correct URLs and invalidate thread query
  </behavior>
  <action>
    `useAssistantChat(threadId)`:

    - `sendMessage(text: string)` → optimistic user message in local state
    - `fetch` POST `/api/assistant/threads/${threadId}/chat` with `{ message: text }`, credentials include
    - Parse stream via `readAssistantSseStream`; update streaming assistant text buffer
    - On `action_card`, merge into messages list by actionRecordId
    - On `done`, invalidate `useAssistantThread` query key
    - Store AbortController ref; abort prior stream when new message sent

    `use-assistant-actions.ts`:

    - `useConfirmAssistantAction()` → `POST /api/assistant/actions/${id}/confirm`
    - `useCancelAssistantAction()` → `POST /api/assistant/actions/${id}/cancel`
    - onSuccess invalidate `["assistant","thread", threadId]` (accept threadId param in mutate)

    Tests: mock fetch with ReadableStream of encoded SSE bytes; mock apiFetch for actions.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/lib/hooks/use-assistant-chat.test.tsx src/lib/hooks/use-assistant-actions.test.tsx -x</automated>
  </verify>
  <done>Chat streaming and action mutations work with automated tests; no EventSource usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/assistant/* | Untrusted user input; cookie-authenticated POST |
| SSE stream → UI state | Untrusted model output in text_delta and action_card payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-01 | Spoofing | confirm/cancel hooks | mitigate | Use apiFetch with credentials; server enforces workspace scope |
| T-181-02 | Tampering | chat sendMessage body | mitigate | Server validates threadId + role; client sends only message string |
| T-181-03 | Information disclosure | SSE parser | mitigate | Do not log or persist reasoning/thinking fields; plain-text render only downstream |
| T-181-04 | Denial of service | streaming hook | accept | AbortController on navigation; single in-flight stream per hook instance |
</threat_model>

<verification>
- `cd app && npx vitest run src/lib/assistant/parse-sse.test.ts src/lib/hooks/use-assistant-threads.test.tsx src/lib/hooks/use-assistant-chat.test.tsx src/lib/hooks/use-assistant-actions.test.tsx -x`
</verification>

<success_criteria>
- All four hook modules and SSE parser exist with passing unit tests
- Hooks use apiFetch and match existing TanStack Query conventions
- CHAT-02/03/04 data prerequisites available for UI plans
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/181-assistant-surface/181-01-SUMMARY.md`
</output>
