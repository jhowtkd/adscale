---
phase: 181-assistant-surface
plan: 05
type: execute
wave: 4
depends_on:
  - "04"
requirements:
  - CHAT-04
files_modified:
  - app/src/components/assistant/CampaignAssistantDrawer.tsx
  - app/src/components/assistant/CampaignAssistantDrawer.test.tsx
  - app/src/app/(dashboard)/campaigns/[id]/page.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
must_haves:
  truths:
    - "Campaign workspace has a control to open assistant chat drawer"
    - "Drawer loads or creates default campaign thread via POST isDefault"
    - "Drawer uses AssistantChatCore — not a duplicate chat implementation"
    - "Same campaign thread continues between /assistant tree and campaign drawer"
  artifacts:
    - path: app/src/components/assistant/CampaignAssistantDrawer.tsx
      provides: "Sheet drawer with compact chat"
      exports: ["CampaignAssistantDrawer"]
    - path: app/src/app/(dashboard)/campaigns/[id]/page.tsx
      provides: "Drawer trigger in campaign workspace"
  key_links:
    - from: app/src/components/assistant/CampaignAssistantDrawer.tsx
      to: app/src/components/assistant/AssistantChatCore.tsx
      via: "variant drawer"
      pattern: "AssistantChatCore"
    - from: app/src/components/assistant/CampaignAssistantDrawer.tsx
      to: /api/assistant/threads
      via: "POST isDefault true with campaignId"
      pattern: "isDefault.*true"
    - from: app/src/components/assistant/CampaignAssistantDrawer.tsx
      to: app/src/components/ui/sheet.tsx
      via: "Sheet SheetContent size xl"
      pattern: "SheetContent"
---

<objective>
Deliver CHAT-04: campaign workspace drawer that resumes the default campaign thread using shared `AssistantChatCore`.

Purpose: Users continue the same thread from campaign context without a second chat implementation.
Output: `CampaignAssistantDrawer` + trigger on campaign detail page.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/181-assistant-surface/181-CONTEXT.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-RESEARCH.md
@app/src/components/ui/sheet.tsx
@app/src/components/assistant/AssistantChatCore.tsx
@app/src/lib/hooks/use-assistant-threads.ts

## Locked decisions

- **D-CHAT-04:** Drawer on `/campaigns/[id]`; resumes default thread (Phase 178 `getOrCreateDefaultCampaignThread`)
- **D-CHAT-04-reuse:** Same message/SSE/action card components — `AssistantChatCore` only
- **RESEARCH Pattern 2:** Sheet `side="right"` `size="xl"`; `variant="drawer"`

<interfaces>
From `useCreateAssistantThread` / threads POST:

```typescript
// POST { clientProfileId, campaignId, isDefault: true } → getOrCreateDefaultCampaignThread
const { thread } = await res.json();
```

From `useCampaign(id)` — campaign record includes `clientProfileId` needed for thread POST.

Sheet pattern [VERIFIED: DerivationReviewSheet / sheet.tsx]:
```tsx
<Sheet open={open} onOpenChange={setOpen}>
  <SheetContent side="right" size="xl" className="p-0">...</SheetContent>
</Sheet>
```
</interfaces>
</context>

<tasks>

<task id="181-05-01" type="auto" tdd="true">
  <name>CampaignAssistantDrawer with default thread resolution</name>
  <files>app/src/components/assistant/CampaignAssistantDrawer.tsx, app/src/components/assistant/CampaignAssistantDrawer.test.tsx, app/messages/en.json, app/messages/pt-BR.json</files>
  <behavior>
    - When open becomes true, resolves thread via useCreateAssistantThread mutateAsync with isDefault:true OR useAssistantThreads if default already exists
    - Shows loading state while thread resolving
    - Renders AssistantChatCore with resolved threadId and variant drawer
    - onClose passed to ChatCore header; aborts stream on drawer close (ChatCore cleanup)
    - Error state if campaign missing clientProfileId
  </behavior>
  <action>
    Props: `{ open: boolean; onOpenChange: (open: boolean) => void; campaignId: string; clientProfileId: string }`.

    On `open && !threadId`: call `createThread.mutateAsync({ clientProfileId, campaignId, isDefault: true })` — idempotent getOrCreate per server.

    Sheet layout: minimal header with title i18n `assistant.drawer.title` + close button.

    `CampaignAssistantDrawer.test.tsx`:

    - Mock useCreateAssistantThread → returns thread id
    - Assert AssistantChatCore receives threadId and variant drawer
    - Assert no duplicate SSE hook instantiation (mock AssistantChatCore)

    Do NOT duplicate chat logic — only wrapper + thread bootstrap.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/CampaignAssistantDrawer.test.tsx -x</automated>
  </verify>
  <done>Drawer resolves default thread and renders shared chat core.</done>
</task>

<task id="181-05-02" type="auto">
  <name>Wire drawer trigger on campaign workspace page</name>
  <files>app/src/app/(dashboard)/campaigns/[id]/page.tsx</files>
  <action>
    On campaign detail page, add assistant entry point per D-CHAT-04:

    - Import `CampaignAssistantDrawer` dynamically if page bundle size concern (optional — prefer static import if small)
    - Add button to `WorkspaceActionBar` or `PageHeader` actions area: icon + label i18n `assistant.drawer.open`
    - Pass `campaignId` from route params and `clientProfileId` from `useCampaign(id)` data
    - Local `useState` for drawer open

    Place trigger where other workspace actions live — match existing action button styling (tokens, size).

    Guard: if campaign loads without clientProfileId, disable button with tooltip i18n explaining requirement.

    Do NOT implement quick action execution (deferred Phase 182).
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/CampaignAssistantDrawer.test.tsx src/components/assistant/AssistantChatCore.test.tsx -x</automated>
  </verify>
  <done>CHAT-04 drawer accessible from campaign workspace; continues default thread.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Campaign page → drawer | campaignId/clientProfileId from loaded campaign record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-12 | Elevation | thread bootstrap | transfer | Server validates campaign belongs to workspace + clientProfileId |
| T-181-13 | Information disclosure | drawer chat history | mitigate | Same auth gate as /assistant; thread scoped server-side |
</threat_model>

<verification>
- `cd app && npx vitest run src/components/assistant/CampaignAssistantDrawer.test.tsx src/components/assistant/AssistantChatCore.test.tsx -x`
- `cd app && npm run build`
</verification>

<success_criteria>
- CHAT-04: drawer opens from campaign workspace and shows same default thread chat
- Single chat implementation shared with /assistant
- Phase 181 requirement set complete
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/181-assistant-surface/181-05-SUMMARY.md`
</output>
