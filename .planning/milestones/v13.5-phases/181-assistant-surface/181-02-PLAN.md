---
phase: 181-assistant-surface
plan: 02
type: execute
wave: 1
depends_on: []
requirements:
  - CHAT-01
files_modified:
  - app/src/components/layout/TopBar.tsx
  - app/src/components/layout/TopBar.test.tsx
  - app/src/components/layout/DashboardShellSwitcher.tsx
  - app/src/app/(dashboard)/layout.tsx
  - app/src/app/(dashboard)/assistant/layout.tsx
  - app/src/app/(dashboard)/assistant/page.tsx
  - app/src/components/assistant/AssistantShell.tsx
  - app/src/components/assistant/AssistantShell.test.tsx
  - app/src/components/assistant/AssistantMobileTabs.tsx
  - app/messages/en.json
  - app/messages/pt-BR.json
autonomous: true
must_haves:
  truths:
    - "Authenticated user can toggle Painel ↔ Chat from TopBar without dedicated Assistente nav item"
    - "Chat mode navigates to /assistant; panel mode returns to last panel route"
    - "Assistant route renders without AppShell bottom nav or panel footer double-wrap"
    - "Desktop shows three-column shell slots; mobile shows Tree | Chat | Context tabs"
  artifacts:
    - path: app/src/components/layout/TopBar.tsx
      provides: "Segmented Panel/Chat mode toggle per D-CHAT-01"
    - path: app/src/app/(dashboard)/assistant/layout.tsx
      provides: "AssistantShell wrapper"
    - path: app/src/components/assistant/AssistantShell.tsx
      provides: "Codex-style 3-column layout + mobile tabs"
      exports: ["AssistantShell"]
    - path: app/src/components/layout/DashboardShellSwitcher.tsx
      provides: "Skips AppShell for /assistant paths"
  key_links:
    - from: app/src/components/layout/TopBar.tsx
      to: /assistant
      via: "router.push on Chat segment click"
      pattern: "router\\.push.*assistant"
    - from: app/src/app/(dashboard)/layout.tsx
      to: app/src/components/layout/DashboardShellSwitcher.tsx
      via: "conditional AppShell bypass"
      pattern: "DashboardShellSwitcher"
    - from: app/src/components/assistant/AssistantShell.tsx
      to: app/src/components/layout/TopBar.tsx
      via: "renders TopBar at top"
      pattern: "TopBar"
---

<objective>
Deliver CHAT-01: global Panel ↔ Chat mode toggle, canonical `/assistant` route, and `AssistantShell` layout without AppShell double-wrap.

Purpose: Primary assistant access is mode toggle (locked D-CHAT-01), not a separate nav item; dedicated shell enables Codex three-column surface.
Output: TopBar toggle, route, shell skeleton with mobile tabs, i18n keys.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v13.5-phases/181-assistant-surface/181-CONTEXT.md
@.planning/milestones/v13.5-phases/181-assistant-surface/181-RESEARCH.md
@app/src/components/layout/AppShell.tsx
@app/src/components/layout/TopBar.tsx
@app/src/components/layout/TopBar.test.tsx

## Locked decisions

- **D-CHAT-01:** TopBar global toggle; NO separate "Assistente" nav link
- **D-CHAT-01-mobile:** Toggle stays on TopBar; bottom tabs in Chat mode only (Tree | Chat | Context)
- **RESEARCH resolved:** Segmented control with i18n labels `assistant.mode.panel` / `assistant.mode.chat`
- **RESEARCH resolved:** `sessionStorage` key `adscale:panel-return` for last panel path only (not full mode persistence — deferred localStorage)

<interfaces>
From `AppShell.tsx` — includes TopBar, main, Footer, mobile bottom nav (must NOT appear in chat mode).

From `TopBar.tsx` — client component with `usePathname`, `useRouter`, `useIsMobile`.
</interfaces>
</context>

<tasks>

<task id="181-02-01" type="auto" tdd="true">
  <name>TopBar Panel ↔ Chat segmented toggle</name>
  <files>app/src/components/layout/TopBar.tsx, app/src/components/layout/TopBar.test.tsx, app/messages/en.json, app/messages/pt-BR.json</files>
  <behavior>
    - When pathname starts with /assistant, Chat segment is active
    - Clicking Chat navigates to /assistant preserving threadId searchParam if present
    - Clicking Panel saves current path to sessionStorage adscale:panel-return and navigates there (fallback /)
    - No new navigation link labeled Assistente appears in AppShell bottom nav or TopBar menus
  </behavior>
  <action>
    Per D-CHAT-01 and RESEARCH Pattern 1:

    Add segmented control left of `LanguageSwitcher` (desktop + mobile compact variant):

    - `const isChatMode = pathname.startsWith("/assistant")`
    - `switchToChat()` → `router.push(`/assistant${searchParams.get("threadId") ? `?threadId=...` : ""}`)`
    - `switchToPanel()` → read/write `sessionStorage.getItem/setItem("adscale:panel-return", pathname)` then `router.push(stored ?? "/")`

    Add i18n keys under `assistant.mode.panel` and `assistant.mode.chat` in `messages/en.json` and `messages/pt-BR.json`.

    Extend `TopBar.test.tsx`: mock `usePathname`/`useRouter`, assert push targets for both segments. Do NOT add Assistente to `AppShell` nav items.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/layout/TopBar.test.tsx -x</automated>
  </verify>
  <done>Toggle routes correctly; tests pass; no dedicated assistant nav item added.</done>
</task>

<task id="181-02-02" type="auto">
  <name>Dashboard shell switcher and assistant route</name>
  <files>app/src/components/layout/DashboardShellSwitcher.tsx, app/src/app/(dashboard)/layout.tsx, app/src/app/(dashboard)/assistant/layout.tsx, app/src/app/(dashboard)/assistant/page.tsx</files>
  <action>
    Fix Pitfall 1 (AppShell double-wrap) per RESEARCH:

    Create client `DashboardShellSwitcher.tsx`:
    - `usePathname()` — if `pathname.startsWith("/assistant")` render `{children}` only
    - Else render `<AppShell>{children}</AppShell>`

    Update `(dashboard)/layout.tsx` to use `DashboardShellSwitcher`.

    Create `assistant/layout.tsx` — renders `AssistantShell` wrapping `{children}`.

    Create `assistant/page.tsx` — placeholder reading `searchParams.threadId` (via page props in Next 16); renders slot placeholders for tree/chat/context until Plans 03–04 wire components. Show minimal "select a thread" stub if no threadId.

    Auth inherits existing dashboard group — no new auth wrapper needed.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>/assistant route exists; AppShell footer/bottom nav absent on assistant paths.</done>
</task>

<task id="181-02-03" type="auto" tdd="true">
  <name>AssistantShell three-column layout and mobile tabs</name>
  <files>app/src/components/assistant/AssistantShell.tsx, app/src/components/assistant/AssistantShell.test.tsx, app/src/components/assistant/AssistantMobileTabs.tsx</files>
  <behavior>
    - Desktop: left ~240px sidebar slot, flex center, right ~320px panel slot (collapsible via useState, default open)
    - Mobile: AssistantMobileTabs switches visible panel among tree | chat | context
    - Shell includes TopBar (same as AppShell) but NOT Footer or panel bottom nav
    - Uses CSS tokens --surface-base, --border-dim, layer-shell-floating
  </behavior>
  <action>
    `AssistantShell` accepts `sidebar`, `main`, `contextPanel` ReactNode props (composition for Plans 03–04).

    Desktop grid: `grid-cols-[240px_1fr_320px]` with right column collapsible (`sessionStorage` key `adscale:assistant-context-open` optional per RESEARCH).

    `AssistantMobileTabs` — three tabs with i18n `assistant.tabs.tree|chat|context`; only rendered `md:hidden`.

    Reuse `FeedbackProvider` / providers from root layout only — do not duplicate provider tree.

    `AssistantShell.test.tsx`: smoke render with test slots; assert three regions exist on desktop markup; mobile tabs render.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/components/assistant/AssistantShell.test.tsx -x</automated>
  </verify>
  <done>Three-column shell and mobile tab scaffold ready for content plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client routing | Mode toggle only changes client-side routes within authenticated dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-05 | Information disclosure | sessionStorage panel-return | accept | Stores only internal app paths; no secrets |
| T-181-06 | Spoofing | /assistant route | transfer | Server auth on API layer; dashboard session gate unchanged |
</threat_model>

<verification>
- `cd app && npx vitest run src/components/layout/TopBar.test.tsx src/components/assistant/AssistantShell.test.tsx -x`
- `cd app && npx tsc --noEmit -p tsconfig.json`
</verification>

<success_criteria>
- User reaches `/assistant` via TopBar toggle (CHAT-01)
- No Assistente nav item added
- Assistant layout free of panel bottom navigation
- Mobile tabs scaffold present
</success_criteria>

<output>
After completion, create `.planning/milestones/v13.5-phases/181-assistant-surface/181-02-SUMMARY.md`
</output>
