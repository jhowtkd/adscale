# Phase 81 Context (auto)

**Locked:** Add `operation_key` (preview|batch|creative_qa) and `creditDelta` on `credit_spend` when estimate≠actual. Thread `estimateCredits` from derivations route. Keep `operation` as billing action for backward compat.
