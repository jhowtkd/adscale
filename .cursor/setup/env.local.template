# Generated into app/.env.local by .cursor/setup/install.sh for Cloud Agent runs.
# Real Postgres runs locally; external services use schema-valid placeholder
# values so the lazy env proxy (app/src/server/validation/env.ts) never throws
# on module load. Replace placeholders with real credentials to exercise the
# corresponding integrations (OpenAI, Stripe, R2, Resend, etc.).

# Database (provisioned by .cursor/setup/start.sh)
DATABASE_URL="postgresql://adscale:adscale123@localhost:5432/adscale_db?sslmode=disable"
TEST_DATABASE_URL="postgres://adscale:adscale123@localhost:5432/adscale_test?sslmode=disable"

# Auth
BETTER_AUTH_SECRET="local-dev-better-auth-secret-please-change-0123456789"
BETTER_AUTH_URL="http://localhost:3000"

# OpenAI (placeholder — real key required only for live AI generation)
OPENAI_API_KEY="sk-local-development-placeholder-key"
OPENAI_TEXT_MODEL="gpt-5.6-sol"
OPENAI_IMAGE_MODEL="gpt-image-2-2026-04-21"

# R2 Storage (placeholder)
R2_ACCOUNT_ID="local"
R2_ACCESS_KEY_ID="local"
R2_SECRET_ACCESS_KEY="local"
R2_BUCKET="adscale"
R2_PUBLIC_BASE_URL="https://example.r2.dev"

# Inngest (dev)
INNGEST_EVENT_KEY="local"
INNGEST_SIGNING_KEY="local"

# Email (placeholder)
RESEND_API_KEY="re_local_development_placeholder"
EMAIL_FROM="ADScale <onboarding@resend.dev>"

# App
APP_URL="http://localhost:3000"
MARKETING_URL="http://localhost:3000/hi"
MARKETING_UPSTREAM_URL="http://localhost:5173"
MARKETING_ALLOWED_ORIGINS="http://localhost:5173"

# Stripe (test-mode placeholders)
STRIPE_SECRET_KEY="sk_test_local_development_placeholder"
STRIPE_WEBHOOK_SECRET="whsec_local_development_placeholder"
STRIPE_STARTER_PRICE_ID="price_local_starter"
STRIPE_GROWTH_PRICE_ID="price_local_growth"
STRIPE_SCALE_PRICE_ID="price_local_scale"
STRIPE_SUCCESS_URL="http://localhost:3000/settings?tab=billing&checkout=success"
STRIPE_CANCEL_URL="http://localhost:3000/settings?tab=plans&checkout=cancel"

# Rollout switches
CREATIVE_WORK_QUALITY_RECOVERY_ENABLED="false"
BRAND_CORTEX_SINGLE_PIECE_ENABLED="false"

# Dev convenience — skip email verification / credit debits for this owner
DEV_ADMIN_EMAIL="dev@adscale.local"
